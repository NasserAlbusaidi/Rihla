# Revised spec (round-3) — #520: createGroup false offline error

> Round-3 rework. Rounds: 1. Gate: **clean** (P1s: 0). Refuter: **ready-to-implement** (survives: true).

Spec: docs/plans/2026-06-14-p2-rework-520.md

## Summary
Offline createGroup raises a LOUD false error + spurious Sentry alert even though the group is created. Re-verified against live code: create_group_screen.dart:144-151 wraps createGroup().timeout(15s); group_provider.dart:166 `await batch.commit()` (+ chained member .set() :172-184) resolve only on SERVER ack (#412), so offline neither completes in 15s → TimeoutException → generic catch (create_group_screen.dart:170-183) sets groupErrorProvider + Sentry.captureException + groupCreateError snackbar. Meanwhile the batch is queued in the SDK and will replay. Fix = mirror the #412 connectivity short-circuit from add_expense_screen.dart:51-88 / settle_up_screen.dart:530-562: extract GroupService.stageGroup (sync, returns ({Group group, Future<void> ack})) with createGroup becoming a thin wrapper; screen reads connectivityProvider, races awaitServerAck(staged.ack, skipWait: status != online), shows the existing share sheet + a new groupCreatedWillSync affordance, no error/Sentry offline. Sequential batch-commit-THEN-member-set ordering preserved via .then (no Future.wait, no rules change). Round-3 spec at docs/plans/2026-06-14-p2-rework-520.md resolves all 7 round-2 residuals.

## Approach
CLIENT change + thin service extraction, no firestore.rules change, no change to createGroup's external Future<Group> contract. (1) group_provider.dart: extract createGroup's body (:118-198) into a SYNCHRONOUS stageGroup that runs the uid-null + anonymous guards FIRST (sync throws), builds the local Group immediately, and returns ack = batch.commit().then((_) => members.set({...})) — sequential, member chained after commit so validMemberCreate→isGroupMember→exists(group) (firestore.rules:120-124,764-766) holds on first-write AND replay. createGroup becomes `final s = stageGroup(...); await s.ack; return s.group;`. (2) create_group_screen.dart._createGroup: capture connectivity (mirror add_expense_screen.dart:51-52); INSIDE the existing try call stageGroup (sync), await awaitServerAck(staged.ack, skipWait: connectivityStatus != ConnectivityStatus.online), noteLocalWrite/noteQueuedWrite, show groupCreatedWillSync snackbar on queued, then _showSharePrompt(staged.group). The on DurableCredentialRequiredException catch (:162-169) and generic catch (:170-183) keep their positions; the latter is now online-failure-only. Only behavior change: offline → queued-success instead of 15s timeout-error.

## Files to change
- lib/features/groups/providers/group_provider.dart — add synchronous stageGroup({name,currency}) returning ({Group group, Future<void> ack}); make createGroup a thin wrapper that awaits staged.ack
- lib/features/groups/screens/create_group_screen.dart — connectivity short-circuit; stageGroup call + awaitServerAck + will-sync snackbar + _showSharePrompt all INSIDE the existing try (replaces the .timeout(15s) block :144-183)
- lib/l10n/app_en.arb — add groupCreatedWillSync after groupCreateError @-block (~:1494): 'Group created — will sync when online.'
- lib/l10n/app_ar.arb — add groupCreatedWillSync after groupCreateError (~:606): parallel ar translation
- test/features/groups/create_group_offline_412_test.dart — NEW RED→GREEN; never-acking Completer offline; asserts false-error pre-fix, share sheet + will-sync + syncing post-fix
- test/features/groups/durable_gate_wiring_test.dart — flip create when()/verify()/verifyNever() from createGroup to stageGroup; add startPeriodicChecks:false connectivity override to wrap()
- test/features/groups/notification_prompt_wiring_test.dart — flip create when()/verify() to stageGroup; add connectivity override to wrap()
- test/features/groups/create_join_group_test.dart — add connectivity override to the USD-persistence test ProviderScope (:160-180) only

## Data contracts
NEW GroupService.stageGroup: `({Group group, Future<void> ack}) stageGroup({required String name, required String currency})` — SYNCHRONOUS (not async). Sync uid-null guard `throw Exception('User not authenticated')` + anonymous guard `throw const DurableCredentialRequiredException()` BEFORE building anything (mirrors group_provider.dart:122-130). Builds groupId/memberId/inviteCode/now + local Group identically to :135-139,188-197, returns group with NO await. ack = batch.commit().then((_) => groups/{id}/members/{memberId}.set({...})). Byte-identical persisted writes: group doc EXACT keys at :145-159 (id, name, inviteCode, createdBy, memberIds:[uid], currency, isDeleted:false, deletedAt:null, createdAt/updatedAt serverTimestamp); inviteCode doc :161-164 (groupId, createdAt serverTimestamp); member doc :178-184 (id, userId:uid, displayName, role:'CREATOR', joinedAt serverTimestamp, isShadow:false). EXISTING createGroup: wrapper `final s = stageGroup(name:name,currency:currency); await s.ack; return s.group;` — identical Future<Group> contract. awaitServerAck (write_ack.dart:30-60): Future<WriteAck> awaitServerAck(Future<void> ack,{Duration timeout=5s,bool skipWait=false,void Function(Object,StackTrace)? onLateError}); screen passes skipWait: connectivityStatus != ConnectivityStatus.online, onLateError null (genuine replay rejection still Sentry'd via observeLate :36-47). NEW l10n key groupCreatedWillSync (en 'Group created — will sync when online.'; ar parallel), no placeholders, mirrors settleUpRecordedWillSync (en:870/ar:364). connectivity: ref.read(connectivityProvider)→ConnectivityStatus (provider :9); .notifier→ConnectivityNotifier noteLocalWrite()(:133)/noteQueuedWrite()(:146-148→syncing).

## RED test plan
NEW test/features/groups/create_group_offline_412_test.dart. Harness mirrors add_expense_offline_412_test.dart + durable_gate_wiring_test.dart wrap(). _MockGroupService extends Mock implements GroupService. Stub BOTH shapes on the same mock (add_expense_offline_412_test.dart:40-71 precedent — unused stub harmless): createGroup→Completer<Group>().future (never acks, current offline); stageGroup→(group:_group, ack: Completer<void>().future) (never-acking ack, post-fix offline). connectivity = ConnectivityNotifier(startPeriodicChecks:false)..setOffline(). Overrides: sharedPreferencesProvider (seed settings_device_name), connectivityProvider, groupServiceProvider.overrideWithValue, notificationPromptProvider (no-op), durableCredentialGateProvider→a gate with isAnonymous:()=>false + presentSheet→true (real gate reads FirebaseConfig→[core/no-app] in tests; declare a local gate or copy RecordingGate — it is test-local to durable_gate_wiring_test.dart, not importable). MaterialApp.router with /create-group + /group/:id + /home. Enter name + display name, tap createGroupButton. FIXED pumps only (NEVER pumpAndSettle — never-acking ack hangs it): pump(); pump(16s) (>15s timeout); pump(500ms). Symptom asserted = LOUD FALSE ERROR not hang. TWO-COMMIT split (both pasted, #329): C1 (RED) = extract stageGroup + wrapper, screen UNCHANGED (still createGroup().timeout(15s)) → RED asserts groupErrorProvider isNotNull, find.textContaining('Error:') findsOneWidget, find.text('ABCDEF') findsNothing, connectivity.state==offline. stageGroup exists (C1) so it compiles; behavioral RED on the live defect. C2 (GREEN) = wire screen to stageGroup+skipWait+snackbar; SAME test flips to groupErrorProvider isNull, no 'Error:' snackbar, find.text('ABCDEF') findsOneWidget (share sheet InviteCodeDisplay), find.text('Group created — will sync when online.') findsOneWidget, connectivity.state==syncing. _group fixture uses inviteCode 'ABCDEF'.

## Acceptance boxes
- Offline create shows the share sheet with the invite code; NO groupErrorProvider, NO groupCreateError snackbar, NO Sentry (post-fix RED: groupErrorProvider null; find.textContaining('Error:') findsNothing; find.text('ABCDEF') findsOneWidget)
- connectivityProvider set to syncing via noteQueuedWrite() on the offline path (connectivity.state == ConnectivityStatus.syncing)
- A 'Group created — will sync when online.' affordance appears offline (new groupCreatedWillSync en+ar), shown BEFORE awaiting _showSharePrompt, behavior:floating, explicit duration 4s, NO action (no persist trap)
- [P1-b] stageGroup() call, awaitServerAck(), the will-sync snackbar, AND _showSharePrompt() all execute INSIDE the existing try; synchronous DurableCredentialRequiredException + uid-null Exception from stageGroup are caught; durable_gate defense-path test still shows durableCredentialRequired snackbar with no uncaught exception
- Online create unchanged in outcome: success shows the share sheet; an online failure within the 5s wait still sets groupErrorProvider + Sentry + groupCreateError snackbar
- createGroup retains Future<Group> contract as a stageGroup wrapper; test/unit/group_service_test.dart, test/unit/group_service_durable_gate_test.dart, test/features/groups/user_groups_visibility_test.dart stay GREEN UNCHANGED
- stageGroup writes byte-identical docs: group keys group_provider.dart:145-159 (incl isDeleted:false, deletedAt:null), inviteCode :161-164, member :178-184 — proven by user_groups_visibility_test.dart:108-120
- Sequential ordering preserved: member set chained via .then on batch.commit(); no Future.wait, no rules relaxation; security/firestore.rules UNCHANGED in diff
- [P2] durable_gate_wiring_test.dart:142-147 flips verifyNever(createGroup) → verifyNever(() => groupService.stageGroup(name: any(named:'name'), currency: any(named:'currency'))) so the declined-gate-blocks-write guard stays meaningful
- durable_gate_wiring_test.dart 'passed gate proceeds' (:174-198) flips when()/verify() to stageGroup (stub returns (group:_group, ack: Future<void>.value())); defense-path (:200-230) flips when() to stageGroup.thenThrow(const DurableCredentialRequiredException())
- notification_prompt_wiring_test.dart create test (:108-146) flips when()/verify() to stageGroup; prompt-once assertions unchanged
- [P2] connectivityProvider.overrideWith((ref)=>ConnectivityNotifier(startPeriodicChecks:false)) + import added ONLY where a test taps createGroupButton + passes validation + pumpAndSettles on the create path: create_join_group_test.dart USD test (:160-180/197-198), durable_gate_wiring_test.dart wrap() (:88-98), notification_prompt_wiring_test.dart wrap (:70-76); render-only + early-return-validation tests excluded
- flutter analyze clean; flutter gen-l10n run; full suite green

## Read-path trace
Write-path: create_group_screen._createGroup → GroupService.stageGroup → batch(groups/{id} + inviteCodes/{code}).commit() → .then → groups/{id}/members/{memberId}.set. (1) groupErrorProvider reader: grep -rn 'groupErrorProvider' lib/ returns ONLY its declaration group_provider.dart:27 — never watched for display in lib/; the user-visible error today is the snackbar :175-182, not a widget. Fix stops writing it offline; RED reads it stays null + 'Error:' snackbar absent. (2) created-group reader: userGroupsProvider (group_provider.dart:468-474)→watchUserGroups (memberIds arrayContains uid); offline batch is in SDK local cache so home list reflects it immediately. Balances key on the GROUP doc memberIds/participantIds (recomputeNet universe), NOT the member subcollection — group visible + balance-correct even if member doc lags. (3) connectivity syncing reader: OfflineBanner + add/settle affordances; noteQueuedWrite()→syncing (connectivity_provider.dart:146-148); periodic/resume probe resolves it back, no timer leak. (4) staged.group reader: _showSharePrompt → _SharePrompt renders group.name + InviteCodeDisplay(code: group.inviteCode) (create_group_screen.dart:705,724), both from the locally-built Group, no server round-trip. (5) [P3] groupMembersProvider→watchMembers (:483) drives /group/:id roster; offline it may briefly show empty/partial until the chained members.set replays — IDENTICAL to today's offline createGroup (member .set() :172 also never resolves offline), so NOT a new regression; flag for RD-QA.

## Alternatives rejected
1. Future.wait two-write (Round-1) — relies on unproven SDK batch-before-member replay ordering; member-first replay → PERMISSION_DENIED (validMemberCreate→isGroupMember→exists(group), firestore.rules:120-124,764-766) → group replays member-less + re-Sentry. stageGroup .then chain preserves today's sequential ordering. 2. One batch for all 3 + relax validMemberCreate — rules change = larger Gate blast radius; exists(group) guard is a real trust boundary; sequential chain solves it without touching rules. 3. Change createGroup's signature directly — forces when()/verify() rewrites across all callers + burns the Future<Group> contract group_service_test/user_groups_visibility rely on; thin wrapper keeps it intact. 4. Bump groupErrorProvider-watching UI — N/A, nothing watches it (verified grep). 5. appMessengerKey for the snackbar — unnecessary: _showSharePrompt is showModalBottomSheet (:187), not a route replacement, so ScaffoldMessenger.of(context) is valid (matches settle_up_screen.dart:548). 6. Hang/no-success RED (Round-2 C1) — REJECTED [P1-a]: proves nothing-happens, not the loud false error; would not have caught the original bug. 7. EventService.createEvent has the same offline-hang (no timeout at all, obs 28926) — NOT bundled (one PR one thing); follow-up issue.

## Risks
1. RED compile/sequencing: stageGroup is a NEW symbol; mitigated by C1 (extract stageGroup+wrapper, screen unchanged) so the RED compiles AND fails on the real false-error symptom, then C2 wires the screen; PR pastes C1 failing output (#329). 2. Synchronous-throw escaping the try [P1-b]: hoisting `final staged = stageGroup(...)` above the try lets the sync guard throw escape → crash + defense-path red; mitigated by the explicit acceptance box pinning stageGroup/awaitServerAck/snackbar/_showSharePrompt inside the try, and staged being consumed inside the try (no hoist motive). 3. Connectivity 60s timer reddens existing tests: mitigated by startPeriodicChecks:false override at exactly the 3 create-path-pumpAndSettle sites (each re-verified to have no existing override). 4. Byte-identical keys [P2]: careless extraction could drop isDeleted/deletedAt; mitigated by user_groups_visibility_test.dart:108-120 (unchanged guard). 5. Vacuous verifyNever [P2]: flipping only when()/verify() leaves :142 always-passing; mitigated by the box flipping it to verifyNever(stageGroup). 6. l10n: missed gen-l10n → analyze fails; build runs it, CI catches. 7. Late genuine replay failure now Sentry'd via observeLate (write_ack.dart:36-47) — correct, same as add_expense/settle_up, not a regression. 8. Gate classification: schema-adjacent write path + l10n + screen; firestore.rules explicitly UNCHANGED — confirm in /automerge diff review.

## Resolves residuals
[P1-a] RED reproduces the REAL symptom (loud false error, not hang): RED stubs createGroup→never-acking Completer<Group>, offline, pumps 16s past the live 15s timeout, asserts the BUG against the CURRENT screen shape — groupErrorProvider non-null + groupCreateError ('Error:') snackbar + no share sheet. Post-fix C2 SAME test asserts all clean: groupErrorProvider null, no 'Error:' snackbar, share sheet 'ABCDEF' present, groupCreatedWillSync present, connectivity syncing. C1/C2 commit split gives a true behavioral RED→GREEN (not a compile error), satisfying #329 RED-evidence; spec states explicitly the asserted symptom is the false-error. [P1-b] sync throw stays inside try: approach text + dedicated acceptance box pin that stageGroup() call, awaitServerAck(), the will-sync snackbar, and _showSharePrompt() ALL run inside the existing try; staged is declared/consumed inside the try so there is no hoist motive; defense-path test (stageGroup.thenThrow sync) still shows durableCredentialRequired with no uncaught crash. [P2 vacuous verifyNever]: acceptance box flips durable_gate_wiring_test.dart:142-147 verifyNever(createGroup)→verifyNever(stageGroup). [P2 byte-identical keys]: dataContracts enumerates exact group/inviteCode/member doc keys (group_provider.dart:145-159 incl isDeleted:false/deletedAt:null, :161-164, :178-184); named regression guards with CORRECTED paths — test/unit/group_service_test.dart (uid-null :104-107), test/unit/group_service_durable_gate_test.dart (anonymous-reject :53-68 + proceeds-returning-Group :93-113), test/features/groups/user_groups_visibility_test.dart (isDeleted/deletedAt readback :80-120) — stay GREEN unchanged (round-2 wrongly pathed the first two under test/features/groups; they are under test/unit). [P2 connectivity placement]: acceptance box + risk restate add the startPeriodicChecks:false override ONLY at taps-create + passes-validation + pumpAndSettle sites (USD test :160-180/197-198; durable wrap :88-98 for create pumpAndSettle :127,136; notification_prompt wrap :70-76 for :120,130,136); render-only + early-return validation tests excluded and out of filesToChange. [P3 snackbar timing/styling]: will-sync snackbar pinned to fire BEFORE awaiting _showSharePrompt with behavior:floating + explicit duration 4s + no action (no persist:true trap), matching settle_up_screen. [P3 member-roster window]: readPathTrace notes offline watchMembers may briefly show empty/partial roster on /group/:id until the chained members.set replays — identical to today (member .set() group_provider.dart:172 also never resolves offline), not a new regression, flagged for RD-QA.

## Re-gate findings
- **[P2]** (Verify every concrete claim against code; spell out exact data contracts)
  notification_prompt_wiring_test.dart 'successful create' and durable_gate_wiring_test.dart 'passed gate proceeds' stub createGroup and verify it. Post-fix the screen calls stageGroup (sync, NON-NULLABLE record (Group, Future<void> ack)) on the MockGroupService, not createGroup. The spec flips when()/verify() to stageGroup with ack: Future<void>.value(). Subtlety not called out: an unstubbed mocktail call to a non-nullable record-returning method THROWS, not returns null. So any test reaching the create path must stub stageGroup. The declined-gate test (durable_gate_wiring_test.dart:130-152) and gate-intent test (:154-172) are safe only because the gate-ok early-return (create_group_screen.dart:121) fires before stageGroup.
  → **Fix:** In C2 keep `if (!gateOk || !mounted) return;` (create_group_screen.dart:121) strictly above the stageGroup() call so declined-gate tests need no stageGroup stub; every test that reaches stageGroup must stub it to return a record literal with a real Future<void> ack. State this in the test plan.

- **[P3]** (Spell out exact data contracts; adversarial pass on an orthogonal axis)
  The offline will-sync snackbar fires BEFORE awaiting _showSharePrompt, which is showModalBottomSheet (create_group_screen.dart:187, isScrollControlled:true). The floating snackbar renders concurrently with the modal sheet for 4s and may be obscured at the bottom. Cosmetic only; matches settle_up_screen ordering.
  → **Fix:** Acceptable as-is. Optional polish: show the snackbar after the sheet dismisses or surface will-sync inside _SharePrompt. Flag for /automerge review and RD-QA; do not block.

- **[P3]** (Trace one read-path per write-path; adversarial pass)
  Offline, the chained members.set never resolves until replay, so /group/:id roster (groupMembersProvider->watchMembers) may show empty/partial — identical to today (group_provider.dart:172), confirmed not a new regression. The success path navigates to /group/{id} (create_group_screen.dart:199), so offline the creator lands on their own group with an empty roster until reconnect; balances stay correct (recomputeNet universe = group-doc memberIds/participantIds, not member subcollection).
  → **Fix:** No code change. Keep the RD-QA flag and note in the PR body that offline-create roster-lag is pre-existing/unchanged so the /automerge reviewer does not read it as a regression.

## Re-refute
**Verdict:** ready-to-implement · survives=true

**Flaw:** No reproducing flaw found. Every attack was anticipated by the spec and matched live code: (1) the connectivity-timer hang is correctly pre-empted by the startPeriodicChecks:false overrides at exactly the 3 create-path-pumpAndSettle sites I confirmed have no existing override; (2) the bug is real — create_group_screen.dart:144-151 wraps createGroup().timeout(15s), group_provider.dart:166 await batch.commit() resolves only on server ack offline (#412) → TimeoutException → generic catch :170-183 sets groupErrorProvider+Sentry+snackbar while the SDK queued the write; groupErrorProvider is never watched for display (only written :124/:173) so the user-visible error IS the snackbar; (3) sequential .then ordering preserved, rules untouched; (4) l10n @-block structure confirmed (settleUpRecordedWillSync has no @-block, groupCreateError block ends :1494). The 3 Gate findings are real P2/P3 refinements, not blockers.

**Severity:** P2 is correct and not overstated. The bug produces a loud false error (groupCreateError snackbar) plus a spurious Sentry alert on every offline create, but causes NO data loss — the batch is queued in the SDK and replays (confirmed: batch.commit() + chained members.set() are the same writes that replay). Not P1 (no money/identity/data corruption); not P3 (it falsely tells the user creation failed and pollutes error monitoring, both user-visible and operationally noisy). The diagnosis that groupErrorProvider has no display reader (only the snackbar is user-facing) is accurate and bounds the blast radius correctly.

