# Recovery tails: #428 deferred boxes + #439 failure visibility — Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Ship the three #428 deferred boxes (gate-conflict switch dialog, in-flight intent persistence across the forced restart, Profile account-section rework with a guarded Google restore entry) and retarget #439's recovery-failure-visibility fix onto the new `restoreWithGoogle`/`restoreWithEmailLink` paths.

**Architecture:** Four PRs. PR-A1 builds dormant intent-persistence machinery (SharedPreferences marker + boot replay + screen prefill). PR-A2 wires the gate-conflict switch: `linkGoogleToCurrentUser` surfaces the failed-link credential via a typed exception; the gate sheet offers "switch to that account" → persists the intent → `restoreWithGoogle(credential: reused)`. PR-B reworks the Profile Account card (Google-linked row, durable-gated sign-out, zero-group-guarded restore entries, nudge repoint). PR-C writes a PII-safe outcome marker before the guaranteed restart and surfaces it on the next cold boot + Sentry.

**Tech Stack:** Flutter/Riverpod 2.x, SharedPreferences, firebase_auth, google_sign_in 7.x, mocktail, Sentry.

**Issue wiring:** PR-A1/PR-A2/PR-B carry `Refs #428` in COMMIT MESSAGE bodies (partial deliveries); PR-B's commit closes nothing; the LAST #428 box to merge carries `Closes #428`. PR-C carries `Closes #439`. Never write a bare `Closes #N` inside explanatory prose (the 6fb16c16 trap — GitHub matches keywords inside quotations).

---

## Verified context (all re-checked against live code 2026-06-11)

- Gate sheet conflict arm is display-only: `durable_credential_sheet.dart:61-74` maps `credential-already-in-use`/`email-already-in-use`/`provider-already-linked` → inline `durableGateConflict` dead-end copy. No credential capture, no switch path.
- `linkGoogleToCurrentUser` (`auth_recovery_service.dart:205-214`) obtains the credential internally; it dies with the throw — the conflict handler cannot reuse it today. `FirebaseAuthException.credential` is nullable (flutterfire #9920) and must never be trusted.
- `restoreWithGoogle({AuthCredential? credential, …})` (`auth_recovery_service.dart:241-272`): credential-first → `_removeFcmToken()` → `engageIsolation()` → flush → `markFirestorePersistenceDirty` → `signInWithCredential` → `finally { restart() }`. The `credential` reuse hook has zero production callers. NO signOut ever (#213/#414).
- `isGoogleAccountAlreadyInUse` (`auth_recovery_service.dart:35-38`) — zero production callers; matches the two conflict codes only (NOT `provider-already-linked`).
- `provider-already-linked` means the CURRENT user already has Google linked (stale `isAnonymous` read) — the shipped "use a different Google account" copy is wrong advice for it.
- Gate call sites: `join_group_screen.dart:83-86` and `create_group_screen.dart:73-77`, both BEFORE `setDeviceName` — typed display name is unpersisted when a restart could fire.
- Invite code arrives as route path param `/join/:code` (`app_router.dart` joins → `JoinGroupScreen(initialInviteCode:)`); deep-link prefill does NOT auto-submit (typed-6th-char only, `join_group_screen.dart:238-241`).
- Restart machinery: `PlatformCacheIsolationController.restart()` → MethodChannel `com.safar.safar/cache_isolation` → `MainActivity.restartApp()` = launch intent + CLEAR_TASK + `Runtime.exit(0)` synchronously — `await restart()` never returns on Android; channel failure flips `cacheIsolationRestartFailedProvider` (manual-restart overlay), never rethrows.
- Persisted-marker precedents: `auth.pendingLinkEmail`/`auth.inFlightOp` (write-at-send, read-and-dispatch at boot by `authEmailLinkBootstrapProvider`, per-key-guarded clear) and `auth.firestorePersistenceDirty` (`markFirestorePersistenceDirty` awaited-before-restart → `CacheUidBarrier.reconcile` read-act-clear).
- `restoreWithEmailLink`'s `finally` clears op-state BEFORE restart (`_safeClearRecoveryOpState`, boot-loop guard) — a failed recovery leaves ZERO persistent trace (#439 made worse by PR4).
- `FirebaseConfig.log` is `kDebugMode`-only (`firebase_config.dart:223-224`); zero Sentry calls in `auth_recovery_service.dart` / `auth_email_link_bootstrap_provider.dart`; zero `Sentry.addBreadcrumb` in lib/.
- Profile Account card (`profile_screen.dart:816-950`): linked-email row (`Not set` → push `/profile/link-email`), sign-out row gated `if (isLinked)` where `isLinked = linkedEmailProvider != null`, delete row. `signOutCurrentDevice` throws StateError without `user.email` — a Google-linked user without top-level email is treated as anonymous (can't sign out).
- Home restore CTAs render only on the zero-group empty state (`home_screen.dart:311-342`); the zero-group guard is the load-bearing discard-shell safety argument (`docs/ACCOUNT-RECOVERY.md:63`).
- `AccountBackupNudge` (home widget) shows while `linkedEmailProvider == null && !emailLinkNudgeSeen`, CTA pushes the EMAIL link flow.
- `docs/ACCOUNT-RECOVERY.md:91-94` claims the gate "offers switching via the restore path" — FALSE until PR-A2 ships; fix the doc in PR-A2.
- PR5's prod check verified zero anon-owned groups in prod; rules enforce `sign_in_provider != 'anonymous'` on group create + join callable, so no NEW populated anon shells can appear. PR-A2 still adds a local `groups.isEmpty` guard as defense-in-depth.

## Invariants that must survive every PR (Gate checklist)

1. **No `signOut` on any failure path, anywhere** (#213/#414).
2. **`finally { restart() }` stays guaranteed** in all three swap methods; nothing added between `markFirestorePersistenceDirty` and the swap may throw uncaught before the marker write semantics below are honored.
3. **All new pre-restart prefs writes are awaited and individually guarded** (a failed prefs write must never skip the restart).
4. **`_safeClearRecoveryOpState` ordering unchanged** (boot-loop guard).
5. **The discard-shell switch is offered ONLY when the current shell is provably empty** (`groups.isEmpty` watched live, plus the server-enforced gate invariant).
6. **No new marker may auto-fire a money write on boot.** Replay navigates and prefills; the user always taps the final submit.

---

# PR-A1 — `feat(auth): pending gate-intent persistence across forced restart (dormant)` — `Refs #428`

Branch: `feat/gate-intent-persistence-428`. Worktree: `../Rihla-428-a1`.

### Task A1.1: PendingGateIntent model + prefs store

**Files:**
- Create: `lib/features/auth/services/pending_gate_intent.dart`
- Test: `test/unit/pending_gate_intent_test.dart`

**Step 1: Write failing tests** — round-trip save/read for both intent types; `read` returns null on: missing key, malformed JSON, unknown `type`, expired (older than 30 min); `clear` removes; `save` overwrites.

```dart
// Shape (complete contract — exact keys):
// prefs key: 'auth.pendingGateIntent'
// JSON: {'v': 1, 'type': 'join'|'create', 'joinCode': String?, 'groupName': String?,
//        'displayName': String?, 'currencyCode': String?, 'atMillis': int}
@immutable
class PendingGateIntent {
  const PendingGateIntent.join({required this.joinCode, required this.displayName, this.atMillis})
      : type = 'join', groupName = null, currencyCode = null;
  const PendingGateIntent.create({required this.groupName, required this.displayName,
      required this.currencyCode, this.atMillis})
      : type = 'create', joinCode = null;
  // static const prefsKey = 'auth.pendingGateIntent';
  // static const ttl = Duration(minutes: 30);
  // static Future<void> save(SharedPreferences prefs, PendingGateIntent intent) — guarded, never throws
  // static PendingGateIntent? read(SharedPreferences prefs) — null on malformed/expired/unknown-type
  // static Future<void> clear(SharedPreferences prefs) — guarded, never throws
}
```

`atMillis` defaults to `DateTime.now().millisecondsSinceEpoch` at save time. TTL check happens in `read`.

**Step 2:** Run `flutter test test/unit/pending_gate_intent_test.dart` — FAIL (file not found).
**Step 3:** Implement minimal class.
**Step 4:** Run — PASS.
**Step 5:** Commit `feat(auth): PendingGateIntent model + guarded prefs store (Refs #428)`.

### Task A1.2: screen-side consumption (prefill + one-shot clear)

**Files:**
- Modify: `lib/features/groups/screens/join_group_screen.dart` (initState)
- Modify: `lib/features/groups/screens/create_group_screen.dart` (initState)
- Test: `test/features/groups/gate_intent_prefill_test.dart`

**Step 1: Failing tests.** Seed `sharedPreferencesProvider` with a saved join intent (code `ABC123`, displayName `Nasser`) → pump `JoinGroupScreen()` (no initialInviteCode) → both controllers prefilled, marker cleared from prefs. Same for create (name/displayName/currency prefilled — currency only if it's a valid supported code; else default stands). A screen pumped with NO marker behaves exactly as today (controllers empty/seeded-from-settings). A `join` marker must NOT prefill `CreateGroupScreen` and vice versa (type mismatch → leave marker alone? No — wrong-screen reads leave the marker UNTOUCHED so the right screen can still consume it).

**Step 2:** RED. **Step 3:** In each screen's `initState`: read marker via `ref.read(sharedPreferencesProvider)`; if type matches this screen → prefill controllers (join: displayName + code only when `initialInviteCode` didn't already supply one; create: name/displayName/`_selectedCurrency` when valid) → `unawaited(PendingGateIntent.clear(prefs))`. Type mismatch or null → no-op.
**Step 4:** GREEN + run existing `test/features/groups/` suites (prefill must not break the deep-link `initialInviteCode` path or `settingsProvider.deviceName` seeding — marker wins over the settings seed, `initialInviteCode` wins over the marker code).
**Step 5:** Commit `feat(groups): join/create screens consume a pending gate intent one-shot (Refs #428)`.

### Task A1.3: boot replay navigation

**Files:**
- Create: `lib/features/auth/services/gate_intent_replay.dart`
- Modify: `lib/main.dart` (SafarApp.initState postFrame, after `DeepLinkService.init`)
- Test: `test/unit/gate_intent_replay_test.dart`

**Step 1: Failing tests.** `GateIntentReplay.maybeReplay(prefs, go)` (where `go` is `void Function(String location)` for testability):
- join marker → calls `go('/join/ABC123')`, marker NOT cleared (the screen clears it)
- create marker → `go('/create-group')`
- no/expired marker → no navigation
- `inFlightOp == 'recover'` pending (`auth.inFlightOp` prefs key) → NO navigation, marker untouched (the email-restore bootstrap restarts the app; replay waits for the next boot)

**Step 2:** RED. **Step 3:** Implement; wire in `SafarApp.initState` postFrame AFTER `DeepLinkService.init`, skipped while `cacheIsolationProvider` is true (mirror the DeepLink gating at `main.dart:193-199`). Route via `ref.read(routerProvider).go(...)`. A cold-start deep link landing after replay simply wins (last `go`) — acceptable, both end in a join screen.
**Step 4:** GREEN. `flutter analyze` clean.
**Step 5:** Commit `feat(auth): boot replay for pending gate intents (Refs #428)`.

### Task A1.4: PR

Full suite + analyze → push → PR `feat(auth): pending gate-intent persistence across forced restart (#428 PR-A1)` with `Refs #428` in commit + body; note "dormant — production write site lands in PR-A2". `/automerge` (classifier may call it exempt; accept Gate-category if flagged).

---

# PR-A2 — `feat(auth): gate-conflict switch dialog → discard-shell restore with reused credential` — `Refs #428`

Branch: `feat/gate-conflict-switch-428`. Worktree: `../Rihla-428-a2`. Depends on A1.

### Task A2.1: surface the credential on link conflict

**Files:**
- Modify: `lib/features/auth/services/durable_credential_exception.dart` (add class)
- Modify: `lib/features/auth/services/auth_recovery_service.dart:205-214` (`linkGoogleToCurrentUser`)
- Test: extend `test/unit/auth_recovery_service_google_restore_test.dart` (or sibling unit file)

**Step 1: Failing test.** Mock `user.linkWithCredential` to throw `FirebaseAuthException(code: 'credential-already-in-use')` → `linkGoogleToCurrentUser()` rejects with `GoogleLinkConflictException` whose `.credential` is identical to the factory-produced credential and `.cause.code == 'credential-already-in-use'`. Same for `email-already-in-use`. A non-conflict `FirebaseAuthException` (e.g. `network-request-failed`) still propagates UNCHANGED. **Audit existing pins first:** grep tests for `linkGoogleToCurrentUser` conflict-propagation assertions and update them to the new typed throw — they pin the old contract on purpose; this PR changes that contract deliberately.

```dart
/// Thrown by [AuthRecoveryService.linkGoogleToCurrentUser] when the Google
/// account is already bound to another Rihla account. Carries the exact
/// credential that failed to link so the gate's switch path can reuse it
/// without a second Credential Manager sheet — never read
/// [FirebaseAuthException.credential] (nullable, flutterfire #9920).
class GoogleLinkConflictException implements Exception {
  const GoogleLinkConflictException({required this.credential, required this.cause});
  final AuthCredential credential;
  final FirebaseAuthException cause;
}
```

In `linkGoogleToCurrentUser`: wrap `user.linkWithCredential(credential)` in try/catch; `on FirebaseAuthException catch (e) { if (isGoogleAccountAlreadyInUse(e)) throw GoogleLinkConflictException(credential: credential, cause: e); rethrow; }`. This gives `isGoogleAccountAlreadyInUse` its first production caller. `provider-already-linked` is NOT a conflict (it propagates; the sheet handles it as success — Task A2.2).

**Steps 2-5:** RED → implement → GREEN → commit `feat(auth): link conflict carries the failed credential (Refs #428)`.

### Task A2.2: sheet conflict state with Switch / different-account / Not-now

**Files:**
- Modify: `lib/features/auth/widgets/durable_credential_sheet.dart`
- Modify: `lib/features/auth/providers/durable_credential_gate_provider.dart` (`ensure` gains `{PendingGateIntent? intent}`)
- Modify: `lib/l10n/app_en.arb` + `app_ar.arb` (+ regen)
- Test: `test/features/auth/durable_credential_sheet_conflict_test.dart`

**Behavior contract (exact):**
- `showDurableCredentialSheet(context, {PendingGateIntent? intent})`; `DurableCredentialGate.ensure(context, {PendingGateIntent? intent})` passes it through.
- `_continueWithGoogle` catches `GoogleLinkConflictException` → branch on the live `userGroupsProvider` value: **data+empty** → switch offer; **data+non-empty** (legacy populated anon shell — defense-in-depth) → today's dead-end `durableGateConflict` copy; **error** → dead-end copy (fail-safe); **loading** → keep the sheet's progress state until the stream resolves (Gate R1 P2: don't render the dead-end advice during a cold-stream loading window — a genuinely-empty shell on a slow network would read "use a different account" and give up). Otherwise enter a **conflict state**: title `durableGateConflictTitle` ("That account already has Rihla data"), body `durableGateConflictSwitchBody` ("This Google account already has Rihla data. Switch to it? This device will continue with that account."), primary button `durableGateSwitch` ("Switch account"), secondary `durableGateUseDifferent` ("Use a different account") which resets to the initial state, tertiary keep "Not now" → pop(false).
- Switch tap: `if (intent != null) await PendingGateIntent.save(prefs, intent);` **awaited BEFORE** `restoreWithGoogle(credential: conflict.credential)`. On success the call never returns (restart). Error handling mirrors `triggerGoogleRestore`: post-isolation failures die in the guaranteed restart; pre-isolation throws (`_removeFcmToken`) → reset sheet state + `durableGateError`.
- `provider-already-linked` (in the existing `FirebaseAuthException` arm): the current user IS already durable — behave as success: `await _auth.currentUser?.getIdToken(true)` then pop(true). Remove it from the conflict copy arm.
- NEVER signOut anywhere; the anon session survives "Use a different account"/"Not now" untouched.

**Step 1: Failing widget tests** (mocktail `AuthRecoveryService`, override `authRecoveryServiceProvider`, `sharedPreferencesProvider`, `userGroupsProvider`):
1. link throws `GoogleLinkConflictException` + zero groups → conflict UI with Switch button (`Key('durableGate.switch')`)
2. Switch tap with `intent` → `PendingGateIntent.read(prefs)` non-null BEFORE `restoreWithGoogle` resolves AND `verify(restoreWithGoogle(credential: same))` — pin ordering with a mocktail answer that asserts the marker exists at call time
3. Switch tap with `intent == null` (Profile/nudge path later) → no marker written, restore still called
4. non-empty `userGroupsProvider` → NO Switch button, dead-end copy shown
5. `provider-already-linked` → sheet pops `true`
6. "Use a different account" → back to initial state, NO signOut interaction on the mock
7. `userGroupsProvider` loading (never-emitting stream) → progress state, NO dead-end copy, NO switch button
**Existing pin to update (Gate R1 P2):** `test/features/auth/durable_credential_sheet_test.dart:127` ("conflict shows durableGateConflict inline") — its harness doesn't override `userGroupsProvider`, so post-A2.2 it survives only via the error→dead-end fail-safe ([core/no-app] throw → AsyncError). Don't leave that dependency silent: keep the test but override `userGroupsProvider` with a NON-EMPTY value so it pins the populated-shell dead-end branch explicitly.
**Step 2:** RED. **Step 3:** Implement + l10n EN/AR. **Step 4:** GREEN; run full `test/features/auth/` + `test/features/groups/`. **Step 5:** Commit.

### Task A2.3: call sites pass the intent

**Files:**
- Modify: `lib/features/groups/screens/join_group_screen.dart:83-86` — `ensure(context, intent: PendingGateIntent.join(joinCode: _codeController.text.trim().toUpperCase(), displayName: trimmedName))`
- Modify: `lib/features/groups/screens/create_group_screen.dart:73-77` — `ensure(context, intent: PendingGateIntent.create(groupName: _nameController.text.trim(), displayName: _displayNameController.text.trim(), currencyCode: _selectedCurrency))`
- Test: extend the screens' gate tests — verify `ensure` receives the typed form values.

RED → implement → GREEN → commit.

### Task A2.4: docs + PR

- Fix `docs/ACCOUNT-RECOVERY.md:91-94` to describe the now-real switch offer + intent persistence + the `groups.isEmpty` guard; fix the stale dead-end-copy description.
- Full suite + analyze → PR `feat(auth): gate-conflict switch → discard-shell restore (#428 PR-A2)`, `Refs #428` in commit body. `/automerge` — Gate-category (auth swap path).

---

# PR-B — `feat(profile): account-section rework — Google row, durable sign-out, guarded restore entries` — closes #428 (last box)

Branch: `feat/profile-account-rework-428`. Worktree: `../Rihla-428-b`. Independent of A1/A2 except it reuses `showDurableCredentialSheet` (merge after A2 to avoid conflicts in the sheet signature).

### Task B.1: providers — Google-linked state + durable flag

**Files:**
- Modify: `lib/features/auth/providers/auth_provider.dart`
- Test: `test/unit/auth_providers_durable_test.dart`

```dart
/// providerData entry for google.com, or null. Email may be null even when linked.
final googleAccountProvider = Provider<({String? email})?>((ref) { ... });
/// True when the current user holds ANY durable credential (= !isAnonymous).
final isDurableUserProvider = Provider<bool>((ref) { ... });
```
Both derive from the existing `authUserChangesProvider` (`userChanges()`); both must survive the no-Firebase test path the same way `linkedEmailProvider` does (null/false on stream error). RED (mock user with `providerData: [UserInfo google.com]`, anonymous user, error stream) → implement → GREEN → commit.

### Task B.2: relax `signOutCurrentDevice` precondition (email → durable)

**Files:**
- Modify: `lib/features/auth/services/auth_recovery_service.dart:369-378`
- Test: extend `test/unit/` sign-out tests

**Step 1: Failing tests:** Google-linked user with `email == null` + `isAnonymous == false` → sign-out proceeds (protocol unchanged: engage → flush → markDirty → signOut → finally restart). Anonymous user → still `StateError` (THE data-loss guard — pin it explicitly). Email-linked → still works.
**Steps 2-5:** RED → change the guard from `email` non-empty to `!user.isAnonymous` (update doc comment: anonymous sign-out = orphaned data, durable = recoverable) → GREEN → commit `feat(auth): sign-out gates on durable credential, not email (Refs #428)`.

### Task B.3: Account card rework

**Files:**
- Modify: `lib/features/settings/screens/profile_screen.dart` (`_AccountCard`, `_signOut`)
- Modify: `lib/features/settings/keys/profile_keys.dart` (new tile keys)
- Modify: l10n EN/AR
- Test: `test/features/settings/profile_account_card_test.dart`

**Row matrix (exact render conditions):**

| Row | Key | Condition | Action |
|---|---|---|---|
| Google account | `googleAccountTile` | `googleAccountProvider != null` | display-only (email or "Linked") |
| Keep your account safe | `googleLinkTile` | `isAnonymous` | `showDurableCredentialSheet(context)` (no intent) |
| Linked email | `linkedEmailTile` (existing) | always | unchanged |
| Sign out | `signOutDeviceTile` | `isDurableUserProvider` (was: email-linked) | `_signOut` — confirm dialog: email-linked → existing copy with email; Google-no-email → Google-specific copy variant (Gate R1 P2: `sign_out_confirm_dialog.dart:46-55` renders prefix+email+suffix and promises recovery "via the same email" — empty email = gap + wrong promise; add `signOutContentGoogle` l10n EN/AR) |
| Restore with Google | `profileRestoreGoogleTile` | `isAnonymous && groups.isEmpty` | `triggerGoogleRestore(context, ref)` |
| Restore with email | `profileRestoreEmailTile` | `isAnonymous && groups.isEmpty` | `context.push('/recover')` |
| Delete account | `deleteAccountTile` (existing) | always | unchanged |

`groups.isEmpty` from `userGroupsProvider`; loading/error → treat as NON-empty (restore rows hidden — fail-safe, mirrors A2). The zero-group + anonymous guard is the home-CTA safety argument transplanted; cite it in a comment (the one place a WHY comment is warranted).

**Step 1: Failing widget tests** — the full visibility matrix: (a) anon + zero groups → both restore rows + link row, no sign-out; (b) anon + groups → NO restore rows; (c) Google-linked (no email) → Google row + sign-out, no link row, no restore rows; (d) email-linked → unchanged legacy behavior + sign-out. Tap tests: restore-Google calls `restoreWithGoogle` (mock), link row opens the sheet.
**Steps 2-5:** RED → implement → GREEN → commit.

### Task B.4: AccountBackupNudge repoint

**Files:**
- Modify: `lib/features/home/widgets/account_backup_nudge.dart`
- Test: extend its existing test file

Show condition: `linkedEmailProvider == null` → `isAnonymous` (via `isDurableUserProvider`); CTA: push `AppRoutes.linkEmail` → `showDurableCredentialSheet(context)` (voluntary link; on `true` the nudge disappears via the provider). Read the file first; keep the `emailLinkNudgeSeen` one-shot semantics exactly (tri-state discipline from #352: only mark seen when actually actioned/dismissed per current behavior). Copy: retitle to Google-first (`accountBackupNudge*` keys) EN/AR. RED → implement → GREEN → commit.

### Task B.5: docs + PR

- Update `docs/ACCOUNT-RECOVERY.md:63` (restore entries: home empty state + Profile, both zero-group+anonymous guarded).
- Full suite + analyze. PR `feat(profile): account-section rework (#428 PR-B)`. The COMMIT MESSAGE body carries `Closes #428` ONLY if A1+A2 are already merged (it's the last box); otherwise `Refs #428` and close manually when all three are in. `/automerge` — Gate-category (auth swap precondition change).

---

# PR-C — `fix(auth): recovery outcome visible after forced restart + release diagnostics` — closes #439

Branch: `fix/recovery-outcome-439`. Worktree: `../Rihla-439`. Salvage CONCEPTS (not diffs) from `feat/recovery-diagnostics` (closed PR #440): `humanizeAuthErrorCode` extraction, marker helper shape.

### Task C.1: outcome marker store (RED first — this is the bug)

**Files:**
- Create: `lib/features/auth/services/recovery_outcome.dart`
- Test: `test/unit/recovery_outcome_test.dart`

```dart
// prefs key: 'auth.recoveryOutcome'
// JSON: {'v': 1, 'op': 'google'|'recover'|'signout', 'ok': bool, 'code': String?, 'atMillis': int}
// PII rules: `code` is ONLY a FirebaseAuthException.code or error runtimeType string —
// NEVER message/email/uid/oobCode/link. No other fields.
// writeRecoveryOutcome(prefs, {op, ok, code}) — guarded, never throws
// readAndClearRecoveryOutcome(prefs) — one-shot; null on malformed/missing
```
Round-trip + guard tests. RED → implement → GREEN → commit.

### Task C.2: write the marker in the three swap methods (the #439 regression test)

**Files:**
- Modify: `lib/features/auth/services/auth_recovery_service.dart` (`restoreWithGoogle`, `restoreWithEmailLink`, `signOutCurrentDevice`)
- Test: `test/unit/auth_recovery_service_outcome_marker_test.dart`

**Step 1: THE failing test that would have caught #439:** mock `signInWithEmailLink` to throw `FirebaseAuthException(code: 'invalid-action-code')`; mock isolation controller records call order. Assert: call rejects AND `prefs` contains `{op:'recover', ok:false, code:'invalid-action-code'}` AND the marker write happened BEFORE `restart()` (order via a shared log list). Today: no marker exists → RED for the right reason. Mirror tests: Google failure path, success paths write `ok:true`, sign-out failure. Pin the invariants: rethrow still happens, `_safeClearRecoveryOpState` still runs, restart still guaranteed, NO signOut.

**Step 3: Implementation shape (identical in all three):** inside the existing `try`: on swap success `await writeRecoveryOutcome(prefs, op:…, ok:true)` before returning; add `catch (e) { await writeRecoveryOutcome(prefs, op:…, ok:false, code: codeOf(e)); rethrow; }` before the `finally`. `codeOf`: `FirebaseAuthException.code`, else `e.runtimeType.toString()`. The write is guarded internally — it can never skip the restart. **Do NOT log the marker write into the test event-order lists** — `auth_recovery_service_google_restore_test.dart:110-118` pins exactly `['obtainCredential','removeToken','engage','signIn','restart']` and must stay green unchanged (Gate R1 P3).
**Steps 4-5:** GREEN → full auth test suite → commit `fix(auth): persist PII-safe recovery outcome before the forced restart (Refs #439 — keyword intentionally split: Re fs)` — no, use plain prose without the keyword; the PR squash commit carries `Closes #439` once, nowhere else.

### Task C.3: boot-time surfacing + Sentry

**Files:**
- Create: `lib/features/auth/providers/recovery_outcome_notice_provider.dart`
- Modify: `lib/core/providers/app_bootstrap_provider.dart` (chain it) or `lib/main.dart` postFrame
- Modify: extract `humanizeAuthErrorCode` from `auth_email_link_bootstrap_provider.dart`'s `_humanize` into `lib/features/auth/services/auth_error_humanizer.dart` (reuse, don't duplicate)
- Test: `test/features/auth/recovery_outcome_notice_test.dart`

Behavior: on cold boot (after first frame, skipped under cache isolation), `readAndClearRecoveryOutcome` → if `ok:false`: snackbar `humanizeAuthErrorCode(code)` via the #352 context-free pattern (`routerProvider.routerDelegate.navigatorKey.currentContext`) + `Sentry.captureMessage('recovery_failed op=<op> code=<code>', level: error)` — the authoritative release-build signal. If `ok:true` and op is a restore: success snackbar (`restoreCompleted` l10n, EN/AR). One-shot (marker already cleared by read). Tests: marker → snackbar text + capture invoked (mock Sentry seam or verify via a injectable capture callback) + second boot shows nothing. RED → implement → GREEN → commit.

### Task C.4: breadcrumbs on non-restarting paths

**Files:**
- Modify: `lib/features/auth/providers/auth_email_link_bootstrap_provider.dart` (catch paths), `lib/features/auth/widgets/durable_credential_sheet.dart` (conflict arm)

`Sentry.addBreadcrumb` (PII-safe: op + code only) at: recovery dispatch start, `completeEmailLink` failure, gate link conflict. Plus `Sentry.captureException` in the bootstrap's non-restarting generic catch (today snackbar-only). Small diff; extend existing tests only where a seam already exists — breadcrumbs are best-effort. Commit.

### Task C.5: PR

Full suite + analyze. PR `fix(auth): recovery outcome visible after forced restart + release diagnostics (#439)`; squash commit body `Closes #439`; PR body documents the RED-first evidence (paste the failing-test output) AND two deliberate divergences from the issue body (Gate R1 P3s): (a) no FNV-1a UID fingerprint in the marker — PII-safer, correlation deferred; (b) `humanizeAuthErrorCode` reuses the existing hardcoded-English `_humanize` strings — l10n debt pre-exists, noted not fixed here. `/automerge` — Gate-category. After merge: delete `feat/recovery-diagnostics` remote branch + `../Rihla-recovery-diag` worktree (salvage complete) — refute-check first per verified-sweep discipline.

---

## Execution order

A1 → A2 → B → C, sequential PRs (B and C touch `auth_recovery_service.dart` like A2 — sequencing avoids rebase churn). Each PR: worktree, TDD, `flutter analyze` clean, full `flutter test`, `/automerge`.

## Out of scope (noted follow-ups, do NOT bundle)

- Identity-card "Anonymous traveller" tagline for durable users (visual pass, needs design).
- `ProfileKeys.coffeeTile` dead key removal.
- CLAUDE.md stale gotcha ("darkTheme has no snackBarTheme" — fixed by #419/9bcddaad): separate docs PR.
- General stale-worktree sweep (Rihla-427, Rihla-441-pr1/pr2, …): separate verified-sweep.
- #429 triage: separate action with evidence (likely close as superseded — no-merge restore signs into the original UID whose email IS set).
- #446 remaining boxes are user-only (CI `CONFIG_JSON` secret + device QA).
