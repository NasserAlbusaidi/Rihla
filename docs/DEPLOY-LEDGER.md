# Backend Deploy Ledger

Human-readable history of Firebase backend deploys (Cloud Functions + Firestore
rules + indexes) for project `rihla-safar`.

**This file is not parsed by any script.** The machine source of truth for "what
is deployed" is the moving git tag **`backend-deployed`**, advanced by
`tool/deploy_firebase_backend.sh` on a successful deploy and read by
`tool/pending_deploy.sh`. This ledger is the discoverable log of what went live
when. If a row is ever missing, `pending_deploy.sh` is still correct — only the
human history loses a line.

Append newest at the **bottom**.

| Date | Deployed SHA | What shipped | Verify |
|------|--------------|--------------|--------|
| 2026-06-06 | `e08b929` | #290 server-authoritative leaveGroup callable + `validSelfLeave` rules drop; also-shipped undeployed #170 recovery TTL + #76 deletionReaper | prod-state PASS; first server-authoritative leave live |
| 2026-06-07 | `f105862` | #318 server-authoritative `removeMember` callable (+ creator-remove rules drop), #294 member-doc lookup by `userId` field in `deleteAccount`/`cleanupAnonUidArtifacts`, #275 `cleanupAnonUidArtifacts` chunked BatchWriter (off the 500-write transaction cliff) | prod-state PASS; `removeMember` created, all 12 functions deployed |
| 2026-06-07 | `cc8c84e` | #270 server allocators (`groupNetBalance.ts` `allocateShares`/`allocateExact`/`allocatePercent`) gain the negative-value→equal-split guard — full byte-for-byte parity with the client `BalanceCalculator` (shared oracle → `deleteGroup`/`leaveGroup`/`removeMember`) | prod-state PASS; all 12 functions updated |
| 2026-06-07 | `b53433d` | #248 PR 1 (#337): expense `lastEditedBy` field rule pins (`==auth.uid`, presence-gated create / diff-gated update; `validSoftDelete` carries it). #248 PR 2 (#339): new `expenseAuditLogger` `onDocumentWritten` trigger (server-owned expense CREATE/UPDATE/soft-DELETE audit log → event `activity_logs`) + `validActivityCreate` removed (event `activity_logs` now server-only) | prod-state PASS; `expenseAuditLogger` **created**, all 13 functions deployed |
| 2026-06-07 | `786c2f1` | #248 PR 4 (#343): `firestore.rules` opens expense edit/soft-delete to **any event participant** (drop `requesterIsRecordCreator()` from `validExpenseUpdate`) AND makes the `lastEditedBy == auth.uid` pin **mandatory** on every update (was diff-gated) — closes a merge-time-Gate-refuter [P1] where omitting `lastEditedBy` let the audit trigger mis-attribute an edit to the creator. Rules-only (no function add/remove); 13 functions unchanged | prod-state PASS; rules match `main`, all 13 functions present |
| 2026-06-08 | `5eaacf7` | #261 PR-0b (#371): mixed-currency balance-gate guard. `groupNetBalance.ts recomputeNet` now returns `currencies: Set<string>` (EXPENSE-fold only, **function-scope** so cross-event mixing is caught, `toUpperCase()`-normalized so legacy `omr`/`OMR` stays one currency); `deleteGroup`/`leaveGroup`/`removeMember` reject `failed-precondition` when `currencies.size > 1`, **before** the `isZero()` gate — closes the mixed-EXPENSE-currency false-zero (`+10 OMR / −10 USD` → fake `Decimal 0`) money-loss path. Functions-only (no rules/index/function-set change) | prod-state PASS; rules already up to date, all 13 functions updated |
| 2026-06-08 | `edd6421` | #261 PR-1 (#374): `firestore.rules` make `group.currency` authoritative + immutable (Model A foundation). New `currencyMatchesGroup(d)` helper pins a money doc's `currency` to `groupData(groupId).currency` on expense **create** (unconditional), expense **update** (diff-gated — a pure soft-delete is never re-checked, so a legacy mismatched doc stays soft-deletable), and **event-settlement create** (mirrors the already-live group-settlement equality). `validCreatorMetadataUpdate` drops `currency` from its `hasOnly` allow-list → currency settable ONLY at create. Client `updateGroup(currency:)` param removed (defense-in-depth). Rules-only (no function add/remove); a tautology for every current all-OMR write, forward-enforcement once Phase 2 moves the `'OMR'` write hardcodes | prod-state PASS; rules match `main`, all 13 functions present |
| 2026-06-09 | `b9163a1d` | #279 (#388): server-authoritative display-name collision guard in `joinGroupByInviteCode`. Inside the join txn, for a **brand-new member only** (gated on `didJoin == !memberSnap.exists && !memberIds.includes(uid)`, so the #53 heal-path and idempotent re-join are exempt), reject `already-exists` when the joiner's `trim().toLowerCase()` name matches an existing member (compared across member docs by `userId`/`displayName` **fields**, not doc id — #294 creator-doc trap). `already-exists ∉ isLookupFailure` so a collision never burns the 5/hr join throttle. Client maps the error to l10n `groupJoinNameTaken`. Functions-only (no rules/index/function-set change) | prod-state PASS; `joinGroupByInviteCode` updated, all 13 functions present |
| 2026-06-10 | `7370b307` | #366 (#421): server-maintained per-group balance aggregate. NEW `groups/{gid}/aggregates/balance` doc (schema v1: `netMilli`/`perEventNetMilli`/`currencies`/`eventCount`/`degraded`/`sourceTimeMs`) maintained by 4 new diff-gated `onDocumentWritten` triggers (`eventModuleBalanceAggregator` wildcard over expenses+settlements, `groupSettlementBalanceAggregator`, `eventBalanceAggregator`, `memberBalanceAggregator`) + NEW daily `balanceReconciler` (backfill + drift-heal). All call the shared `recomputeNet` oracle (extended with the participantIds-only `perEventNet` drill-down + `eventCount` — behavior-preserving for deleteGroup/leaveGroup/removeMember). Rules: new `aggregates/{aggregateId}` block (member read, client write denied). `deleteGroup` cascade now deletes the aggregate doc. Display-cache only — never OUTBOUND. Client read-path lands in PR2 | prod-state PASS; 5 functions CREATED (`eventModuleBalanceAggregator`, `groupSettlementBalanceAggregator`, `eventBalanceAggregator`, `memberBalanceAggregator`, `balanceReconciler`), all 18 present; rules deployed; first reconciler run (≤24h) backfills existing groups |
| 2026-06-11 | `20689860` | #441 PR2 (#444): the durable-credential gate, server side. `firestore.rules` gains `isDurableSignIn()` (`request.auth.token.firebase.sign_in_provider != 'anonymous'`) on `validGroupCreate` + the `inviteCodes` `allow create` chain — an anonymous-provider client (incl. an old ungated build's offline-replayed batch) can no longer create groups; `recoveryCleanupIntents` deliberately excluded (anon-written by design until PR5 deletes it). `joinGroupByInviteCode` rejects anonymous-provider tokens `permission-denied` **pre-throttle** (outside the tx catch, so no `joinAttempts` burn) via an optional-chained token read; closes the #197 anon-rotation throttle bypass for this callable. Client gate + sheet shipped in the same PR (not deploy-relevant) | prod-state PASS; rules deployed, `joinGroupByInviteCode` updated, all 18 functions present |
| 2026-06-11 | `c009b700` | #441 PR5 (#450): the cross-UID merge engine DELETED from prod. `cleanupAnonUidArtifacts` callable removed via `deploy --force` (18 → **17 functions**: 5 callables + 10 triggers + 2 scheduled); `recoveryCleanupIntents` rules match block + `validCleanupIntent` removed (collection now falls to the deny-all catch-all — fails closed); its TTL fieldOverride dropped (TTL disable applied by the deploy; the lingering index-override shell needed a manual Admin-API `indexConfig: {}` revert before the prod-state indexes check went green — firebase-tools does not delete an emptied fieldOverride). 4 residual `recoveryCleanupIntents` QA docs swept pre-deploy (FxtaZT…, lgb7GQ…, nFf1H1…, zh0Jf…). Mitigation-7 prod check recorded honestly in the spec: 28/29 member UIDs anon-only across ~35 groups, ALL identifiable dev QA data; proceeded on no-real-users + explicit owner order. PR3-device-QA ordering consciously overridden (callable restorable from `057f79f8`). Epic #441 CLOSED | prod-state PASS (all 13 checks); `firebase functions:list` confirms the callable gone (one-directional check the prod-state script cannot do); tag advanced `20689860` → `c009b700` |
| 2026-06-11 | `fdf8460b` | #461: security — scoped npm `overrides` pin of `@grpc/grpc-js` to `^1.14.4` under `google-gax` (GHSA-5375-pq7m-f5r2 / GHSA-99f4-grh7-6pcq: malformed request → server crash). Re-bundled into prod Functions. **Deps-only** — functions source logic, `firestore.rules`, and `firestore.indexes.json` are byte-identical to `c009b700`; `--force` re-asserts them unchanged. Phase 1 of the v1.5.0 release (see `docs/plans/2026-06-11-release-v1.5.0.md`) | prod-state PASS (all 13 checks); function set unchanged (17 present); tag advanced `c009b700` → `fdf8460b` |
| 2026-06-12 | `8acb7fdf` | #382 PR-2 (#467): server balance oracle bucketed per currency. `groupNetBalance.ts` `foldEventNet`/`recomputeNet` now return per-currency `net: Map<ccy,Map<uid,Decimal>>` + `perEventNet: Map<eid,Map<ccy,Map<uid,Decimal>>>` — expenses AND event/group settlements bucket by their OWN per-doc currency (raw key via `currencyOf`; the `currencies` Set stays expense-only + `toUpperCase()` for the v1 aggregate doc, NO LONGER a gate input). `deleteGroup`/`leaveGroup`/`removeMember` drop the `currencies.size>1` refusal → require **zero in every currency bucket** (no FX, each currency clears independently). `balanceAggregator` **Shim #2** flattens the sole bucket → v1 aggregate doc byte-identical (`finalizeNet`'s no-money OMR fallback fires ONLY at zero real currencies, so a single-currency group with an empty event never false-`degrade`s; `bucketizeDrill` keeps no-money perEvent zero-rows); `balanceReconciler` untouched. Behavior change: a cross-currency settlement no longer "settles" a debt (mirrors the merged client PR-1). Functions-only — no rules/index/function-set change; backward-compatible (single-currency = all prod = identical behavior; per-bucket logic unreachable until PR-6 relaxes the uniformity rules). Mid-v1.5.0 deploy verified safe: preserves the `fdf8460b` grpc-js pin, no function-set drift | prod-state PASS (all 13 checks); 17 functions unchanged, rules+indexes match `main`; tag advanced `fdf8460b` → `8acb7fdf` |
| 2026-06-12 | `b12a3f00` | #382 PR-3 (#471): aggregate doc v2 — per-currency buckets, both shims dropped. `balanceAggregator` deletes **Shim #2** and encodes `schemaVersion: 2` with `netMilliByCurrency` `{ccy:{uid:int}}` + `perEventNetMilliByCurrency` `{eid:{ccy:{uid:int}}}` (eventId-major, raw case-preserved bucket keys, ×1000 milli exact per bucket); the `currencies[]` array is gone (derive from bucket keys — it was expense-only and would omit settlement-only buckets); `degraded` is now **byte-cap-only** (the multiCurrency degrade died with the shim — a mixed group gets a real v2 doc). `balanceReconciler` `fingerprint()` re-keyed to the v2 field names in the SAME deploy (drift detection stays live; the first post-deploy sweep IS the v1→v2 backfill — every pre-existing doc warns drift once, by design). Dead `RecomputeResult.currencies` + `foldEventNet` plumbing deleted from `groupNetBalance.ts` (the 3 gates destructure only `net`/`liveEventRefs` — untouched). Client half (v2 decoder, facade chooser serves mixed docs, Shim #1 dropped, bucket-key cross-group fold, honest currency labels) shipped in the same merged PR #471; old v1-pinned clients decode v2 docs as null → once-path fallback (free compat). Functions-only — no rules/index/function-set change; uniformity rules still LIVE so prod buckets stay singletons until PR-6 | prod-state PASS (12 checks); 17 functions updated, rules+indexes match `main`; tag advanced `8acb7fdf` → `b12a3f00` |
| 2026-06-13 | `afff017e` | #382 PR-6 (#477): **the flip — mixed-currency data now creatable; epic #382 CLOSED.** `firestore.rules` deletes the four `currencyMatchesGroup` / group-equality clauses (expense **create**, expense **update** diff-gate, **event-settlement create**, **group-settlement create**) + the now-orphan `currencyMatchesGroup` helper — the `validCurrency` 10-code floor already runs in `validExpenseBase` + `validSettlementCore`, so a money doc's currency need only be **supported**, not equal to `group.currency`; an unsupported code (e.g. `XYZ`) stays denied (RED-proven: 4 "divergent denied" rules tests flipped to allowed + 3 XYZ-still-denied). `group.currency` stays create-only-immutable (re-cast as the per-expense **default**). Client half (per-expense currency picker reusing `CurrencyPickerSheet` + smart default last-used-in-event→group-default + soft fat-finger warning; all FOUR editor currency consumers re-pointed to the picked currency, fixing a JPY `toSubunits→toBigInt` truncation + an EXACT-split currency mismatch caught by the Gate) shipped in the same merged PR. **Rules-only — no function add/remove/logic change** (the server oracle + aggregate doc already bucket per-currency from PR-2/PR-3); `--force` re-asserts the 17 functions + indexes unchanged | prod-state PASS (all 13 checks); rules released to `cloud.firestore`, 17 functions unchanged, indexes + hosting match `main`; tag advanced `b12a3f00` → `afff017e` |
| 2026-06-13 | `25a3519b` | #483 (#493): localize push notification copy. `strings.ts` moves the empty actor/joiner-name fallback INTO `settlementBody`/`memberJoinBody` via a new per-locale `actorLabel` helper (`شخص ما` / `Someone`), dropping the pre-build `'Someone'` literals in `settlementNotifier.ts` + `memberJoinNotifier.ts` — an Arabic recipient of a third-party-recorded settlement now gets `سجّل شخص ما …` instead of the English `Someone` spliced into an RTL sentence (mirrors the existing `groupLabel` localized fallback). The third-party recorder branch (`createdBy` is neither party → empty `actorName`) now localizes correctly; the EN path is byte-identical for a non-empty name. Client half (token `locale` re-saved on app-language change via a new `appBootstrap` `languageCode` listener → `NotificationService.refreshTokenLocale`, so the server's `fcm_tokens/{uid}.locale`-driven copy follows an EN↔AR switch instead of freezing at first-write) shipped in the same merged PR — **not deploy-relevant**. **Functions-only — no rules/index/function-set change**; `--force` re-asserts the 17 functions + rules + indexes unchanged | prod-state PASS (all 13 checks); 17 functions updated (incl. `eventSettlementNotifier`/`groupSettlementNotifier`), rules + indexes + hosting match `main`; tag advanced `afff017e` → `25a3519b` |
| 2026-06-14 | `6ad69418` | #179 (#503, #509): the 3rd + 4th #179 push senders — **expense-created** (`expenseNotifier`) and **event-created** (`eventNotifier`) notifiers, both NEW triggers (17 → **19 functions**). Both `onDocumentCreated` (create-only, so open-edits #248 / soft-deletes / recovery uid-migration `update()`s never re-fire), skip the actor (`uid !== createdBy`), and target event participants by **auth UID** → `fcm_tokens/{uid}` (participantIds ⊆ `memberIds` = uids, rules-enforced). `expenseNotifier` mirrors the `BalanceCalculator` owe-set (non-empty `splitDistribution` v>0 keys → `customSplitParticipants` → personal `[]` → event participants), reads expense fields by their `toFirestore` names (`amountFils`/`currency`/`payerParticipantId`/…); en/ar copy via `strings.ts` (`#483` `actorLabel` empty-name fallback, free-text name appended after ` · `). Registered as single-line `export { … } from` re-exports (visible to `tool/list_expected_functions.sh`). Both Gate-cleared (fresh-context Opus review + independent refuter; refuter ran the emulator suite 7/7 + `tsc --noEmit` clean). Client needs no change — a `type:'expense'`/`'event'` tap just opens the app (`_routeFromData` early-returns for non-`{settlement,member_join}`). **Functions-only — no rules/index change**; `--force` re-asserts rules + indexes + the other 17 functions unchanged | prod-state PASS (all 13 checks); `expenseNotifier` + `eventNotifier` CREATED, all **19** functions present; rules + indexes + hosting match `main`; tag advanced `25a3519b` → `6ad69418` |
| 2026-06-17 | `80932c51` | #519 + #529 (#543): deleteGroup lock lifecycle. **#529** — dropped `canClearObservedLock` in `deleteGroup.ts` so an invocation clears only a lock it created (`createdLock:true`); a concurrent caller hitting `failed-precondition` can no longer wipe a peer's **live** lock. **#519** — new hourly `deleteGroupLockReaper` scheduled fn (1h grace > 540s timeout) resumes the SHARED extracted `finalizeGroupDeletion` core for stale `deletingInProgress` locks (settled → complete soft-delete, unsettled → clear lock). **Money-safe ONLY because it never bumps `deleteLockedAt`**, so `recomputeNet`'s resume horizon re-includes a partial cascade (provably always-settled — the balance gate runs before any mutation, so an unsettled-clear cannot corrupt). Reaper query `where('deletingInProgress','==',true).limit(50)` + in-memory age filter (no composite index; default single-field auto-index). 19 → **20 functions**. Spec `docs/plans/2026-06-16-deletegroup-lock-pair-519-529.md`; pre-impl Gate 0 P1 + `/automerge` review 0 P1 + refuter `refuted:false`. Also bundled **#544** (`caa23852`): pin `form-data 2.5.6` + `protobufjs 7.6.4` via `functions/package.json` `overrides` — fixes the readiness `npm audit --omit=dev` gate that began failing on every functions PR after new `form-data`/`protobufjs` advisories (deps-only). **Functions-only — no rules/index change**; `--force` re-asserts rules + indexes + the other 19 functions unchanged | prod-state PASS (all 13 checks); `deleteGroupLockReaper` CREATED (independently confirmed via `firebase functions:list` — v2 `scheduled`, us-central1, nodejs22), all **20** functions present; rules + indexes + hosting match `main`; tag advanced `6ad69418` → `80932c51` |
| 2026-06-17 | `54fd786a` | #550 + #549 + #548 (#278 PR1 add-by-name backend batch): the consciously-held 3-commit batch shipped together. **#550** — new `addShadowMember` callable (creator adds a placeholder "shadow" member **by name**): mints a uuid (`id == userId`), writes `members/{uuid}` `isShadow:true` + `arrayUnion` into `group.memberIds`; App-Check + creator-only + anon-reject + #279 case-insensitive name-collision + 50-member shadow-add cap; new shared `normalizeRequiredDisplayName` (rejects the `(former member)` suffix so a shadow name can't later break client event-create). Balance oracle is identity-blind → a shadow uuid is just another member id, **no oracle/rules change for the add path** (20 → **21 functions**). **#549** — `deleteAccount` no longer silently spares a durable Google/email account when the active session is an anonymous shell (identity-honest delete). **#548** — `firestore.rules` binds a client-created member doc id to `request.auth.uid` (#524 forged-duplicate-member guard, +7 rule lines). No-real-users → the held batch shipped freely as one deploy. Client add-by-name UI (PR2 #551 chips-at-create · PR3 #553 in-group add+badge+remove · PR4 #552 split-picker shadow markers) all merged to `main` the same day — **not deploy-relevant** (the callable is mocked in widget tests). **Functions + rules** — `--force` re-asserts indexes + the other 20 functions unchanged | prod-state PASS (all 13 checks); `addShadowMember` CREATED (`Successful create operation` confirmed in the deploy log), all **21** functions present; rules released to `cloud.firestore`, indexes + hosting match `main`; tag advanced `80932c51` → `54fd786a` |
| 2026-06-18 | `3eaa7fd2` | #278 claim/merge backend (PR6+7+8+9) — **the shadow-member claim flow; epic #278 CLOSED.** **PR6** (#556) `mergeUidMapKey` SUM-on-collision map re-keyer (shared helper PR7 uses for `splitDistribution` — sums two same-mode slices instead of clobbering on a uid collision). **PR7** (#557) `claimShadowEngine` uuid→uid re-key (two-phase `BatchWriter` cascade, `memberIds` rewritten LAST; a 3-term **D8 financial-universe pre-scan** rejects a claimer already in `priorNet`'s key-set ∪ `memberIds`, so an equal-split divisor shift / departed-but-still-participant can never break additive parity; post-commit `recomputeNet` `simNet==actualNet` parity backstop). Extracted `shared/batchWriter.ts` + `shared/mapReKey.ts` + `recomputeNet` out of deleteGroup/deleteAccount (behavior-preserving — those suites stayed green); `expenseAuditLogger` gains a `claimRekeyAt` sentinel-skip so a re-key burst logs ZERO phantom audit rows. **PR8** (#559) the **creator-approval gate**: 4 callables `requestClaimShadow`/`decideClaimRequest`/`listMyClaimRequests`/`listGroupClaimRequests` + `claimRequests` rules block (`allow read, write: if false` — fully callable-mediated, even the creator is denied a direct read) + raw `claimShadow` onCall **DE-EXPORTED** (the re-key is reachable ONLY via an approved `decideClaimRequest`; the request target = the doc's `requesterUid`, never a uid in the input — the impersonation seal); `decideClaimRequest` writes `status:'claimed'` ONLY on a fresh re-key, never inferred from `memberIds` (the phantom-claim guard, 2 refute rounds); shared `ids.ts`/`inviteCode.ts`. **PR9** (#561) `listUnclaimedShadows` read-only pre-join discovery callable (durable-gated, no membership gate, SAME claimable predicate as `requestClaimShadow` so it never lists a dead-offer or leaks a real member). 5 new callables CREATED (21 → **26 functions**); `claimShadow` is engine-only (never a deployed function). Each slice Gate-cleared pre-impl (fresh-context Opus, 0 P1) + `/automerge` fresh review + independent refuter at merge. Client half (join picker + permanent-confirm + waiting-poll + creator approve UI + 5 typed wrappers + l10n EN+AR) shipped in PR9 — **not deploy-relevant** (callables mocked in widget tests). **Functions + rules** — `--force` re-asserts indexes + the other 21 functions unchanged | prod-state PASS (all 13 checks); `requestClaimShadow`/`decideClaimRequest`/`listMyClaimRequests`/`listGroupClaimRequests`/`listUnclaimedShadows` CREATED (`Successful create operation` ×5 in the deploy log), all **26** functions present; rules released to `cloud.firestore`, indexes + hosting match `main`; tag advanced `54fd786a` → `3eaa7fd2` |
| 2026-06-18 | `390c45e2` | #560 (#563): notify the group creator when a placeholder-claim request arrives — the #278 PR9 discoverability tail (P9-1). NEW `claimRequestNotifier` FCM trigger (26 → **27 functions**). `onDocumentWritten` on `groups/{gid}/claimRequests/{id}` with a `before.status != 'pending' && after.status == 'pending'` guard: fires on create (∅→pending) AND declined→pending **re-open** — the reason it's `...Written` not `...Created`: `requestClaimShadow` re-`set()`s a deterministic-id (`${uid}__${shadowMemberId}`) doc, so a re-request after a creator decline is an UPDATE that `onDocumentCreated` would miss. Skips every `decideClaimRequest` transition (→claimed / →declined, incl. the already-claimed decline), the no-op pending→pending rewrite, and deletes (`claimed→pending` is impossible — `requestClaimShadow` throws pre-`.set()` on a `claimed` doc). Provably correct for the full `claimRequests` writer set (only `requestClaimShadow`→pending and `decideClaimRequest`→claimed/declined; no delete/cascade writer exists — grep-confirmed). Read-only: targets `group.createdBy` (an auth uid → `fcm_tokens/{uid}`; an anon creator with no token is silently skipped by `sendToUids`), self-skips `requesterUid == createdBy`, persists nothing to a domain doc. Bilingual en/ar copy (`claimRequestTitle`/`claimRequestBody`; #483 `actorLabel` empty-requester fallback + a dedicated empty-shadow stand-in avoiding a dangling possessive). Single-line `export { … } from` re-export (visible to `tool/list_expected_functions.sh`). Pre-impl Gate 0 P1 (1 P2 applied — writer-table completeness) + `/automerge` fresh-context review 0 P1 + independent refuter `refuted:false` (it also ruled out the #294 uuid-keying trap on `createdBy`). RED→GREEN, full functions emulator suite 493/493. Client needs no change (`type:'claim_request'` tap just opens the app; tap-routing is v2). **Functions-only — no rules/index/function-logic change to the other 26**; `--force` re-asserts rules + indexes + the other 26 functions unchanged | prod-state PASS (all 13 checks); `claimRequestNotifier` CREATED (`Successful create operation` in the deploy log), all **27** functions present; rules + indexes + hosting match `main`; tag advanced `3eaa7fd2` → `390c45e2` |
| 2026-06-20 | `327d6284` | #528 + #525 + #526 + #565 (#588, #582, #583, #585): the 1.6.0 backend bug-fix + hardening batch. **#528** (#588) — `firestore.rules` `positiveInt` now caps `amountFils <= 9007199254740991` (`Number.MAX_SAFE_INTEGER`, 2^53−1), backstopping a forged Dart/Admin write above the int64↔JS-number divergence point (the app's `MoneySerializer.fitsSafeSubunits` guards the normal client path — both shipped in the same PR, client = not deploy-relevant). One edit covers BOTH `amountFils` sites (expense + settlement) via the shared `positiveInt`. The JS rules-unit-testing harness cannot emit a >2^53 `integerValue` (the SDK serializes such numbers as `doubleValue`, caught by `value is int` one clause earlier; BigInt writes are refused) — so the clause is **defense-in-depth with a safe-max ACCEPT pin**, not a JS-exercisable reject; the client `fitsSafeSubunits` table tests carry the tested money-safety weight. Pre-impl Gate 0 P1 + `/automerge` review 0 P1 + refuter `refuted:false`. **#525** (#582) — `balanceReconciler` now sweeps ALL live groups via cursor pagination (the prior `where('isDeleted','==',false)` skipped legacy field-absent groups). **#526** (#583) — `writeRateMonitor` stops double-counting expenses (the server `activity_logs` create was being attributed to the actor). **#565** (#585) — `claimRequestNotifier` + `strings.ts` now ALSO notify the claim **requester** when the creator decides (approve/decline), bilingual en/ar; extends the existing trigger — **no new function**. **Rules + functions** — `--force` re-asserts indexes + the 27 functions unchanged (no add/remove; all 27 `Successful update operation`). NB: the subsequent `cd33b5ad` (#586, the #527 UTF-16 test pin) is **test-only** (`functions/test/…` only, no `functions/src`/rules) — no deployable delta, so `pending_deploy.sh` flagging it as pending is a known false-positive, NOT ghost-debt | prod-state PASS (all 13 checks); rules released to `cloud.firestore`, all **27** functions updated (none created/deleted), indexes + hosting match `main`; tag advanced `390c45e2` → `327d6284` |
| 2026-06-21 | `29abede0` | #203 Slice 1 (#609) + #287 glyph rules (#612) deployed together as the **superset** — resolves a concurrent-deploy collision (two sessions deploying #609 and #612 separately ping-ponged prod rules; deploying `main` `29abede0` with BOTH converged it + made further deploys idempotent). **#203** (#609, `28253f98`): expense `splitExplanation` — an OPAQUE display-only map (itemized-split metadata for the future editor; balance truth stays `splitDistribution`, persisted as `SplitMode.exact`). Added to BOTH expense allowlists (`validExpenseBase` `hasOnly` + `validExpenseUpdate` `affectedKeys`) + a `splitExplanationBounded` wrapper (`is map` + top-level `size()<=64`, entry-count only — the 1MB doc limit is the payload backstop), wired unconditional-on-create + diff-gated-on-update (legacy oversized docs stay soft-deletable). Server never reads it (0 `functions/src` refs) → no oracle/function change; itemized reduces to a standard `exact` split so client↔server parity holds. **#287 glyph** (#612, `29abede0`): group `glyph`/`inkIndex` allow-list + `validGlyph`/`validInkIndex` bounds on group **create** + `validCreatorMetadataUpdate` (present-or-valid; clear via field-delete). #610 (`dc58f666`, glyph render foundation — model + 12 SVGs) is **client-only** (no backend surface); #586 (`cd33b5ad`, #527 UTF-16 test pin) is **test-only** (no deployable delta — its pending false-positive clears as the tag advances past it). **Rules-only — no function add/remove/logic change**; `--force` re-asserts the 27 functions + indexes unchanged. Deployed with `RIHLA_ALLOW_DIRTY` (untracked `design-web/` + docs sit outside the deploy surface; deployed artifacts byte-identical to a clean tree at `29abede0`). | prod-state PASS (all 13 checks); rules released to `cloud.firestore` (both rulesets present), all **27** functions unchanged (`Successful update operation`), indexes + hosting match `main`; tag advanced `327d6284` → `29abede0` |
| 2026-06-24 | `6dcf05e6` | #648 (#654): **un-gate join for anonymous users** — "gate creation, not participation." `joinGroupByInviteCode.ts` drops the `sign_in_provider==='anonymous'` reject so an anon user may JOIN + add expenses; group + invite-code **CREATE** stay durable-gated (`firestore.rules` `isDurableSignIn` at inviteCode/group create + client `stageGroup`, all unchanged). Money rules already key on participation (`validExpenseCreate`→`isEventParticipant`, `validEventCreate`→`isGroupMember`), so **no rules change**. The `#197` per-UID join-throttle anon-rotation bypass re-opens, contained by enforced App Check + invite-code entropy (32⁶ ≈ 1.07 B) — the rewritten in-file comment forbids re-adding the reject / per-IP / per-code. Client half (removed the join durable gate + the **cross-UID swap guard**: extracted shared `shell_emptiness_gate.outgoingShellProvablyEmpty` and gated `triggerGoogleRestore` so a now-reachable *populated anon shell* can't hit the irreversible Google discard-shell swap via the cold-start `userGroupsProvider` false-empty — Gate R1's catch) shipped in the same merged PR — **not deploy-relevant**. Pre-impl Gate 3 rounds → 0 P1; `/automerge` fresh-context review 0 P1 + independent refuter `refuted:false` (RED→GREEN guard test). **Functions-only — no rules/index/function-set change** (`joinGroupByInviteCode` updated in place, no add/remove); `--force` re-asserts rules + indexes + the other 26 functions unchanged. Deployed with `RIHLA_ALLOW_DIRTY` (untracked `design-web/` + docs sit outside the deploy surface; deployed artifact byte-identical to a clean tree at `6dcf05e6`). | prod-state PASS (all checks); all **27** functions updated (`joinGroupByInviteCode` incl.; none created/deleted), rules + indexes + hosting match `main`; tag advanced `29abede0` → `6dcf05e6` |

<!--
Seed row reconstructed from memory on 2026-06-07. e08b929 = PR #319 merge (the
#290 deploy on 2026-06-06). No backend file changed between e08b929 and the actual
deploy commit, so it is the correct last-deployed backend marker; the
`backend-deployed` tag is seeded here.

Known pending at seed time (surfaced by tool/pending_deploy.sh): #318 removeMember
(+ rules drop) and #294 member-doc keying — these land in the NEXT ledger row when
deployed.
-->
