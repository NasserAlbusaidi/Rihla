# Backend Deploy Ledger

Human-readable history of Firebase backend deploys (Cloud Functions + Firestore
rules + indexes) for project `rihla-safar`.

**This file is not parsed by any script.** The machine source of truth for "what
is deployed" is the moving git tag **`backend-deployed`**, advanced by
`tool/deploy_firebase_backend.sh` on a successful deploy and read by
`tool/pending_deploy.sh`. This ledger is the discoverable log of what went live
when. If a row is ever missing, `pending_deploy.sh` is still correct — only the
human history loses a line.

Append newest at the **bottom**.

| Date | Deployed SHA | What shipped | Verify |
|------|--------------|--------------|--------|
| 2026-06-06 | `e08b929` | #290 server-authoritative leaveGroup callable + `validSelfLeave` rules drop; also-shipped undeployed #170 recovery TTL + #76 deletionReaper | prod-state PASS; first server-authoritative leave live |
| 2026-06-07 | `f105862` | #318 server-authoritative `removeMember` callable (+ creator-remove rules drop), #294 member-doc lookup by `userId` field in `deleteAccount`/`cleanupAnonUidArtifacts`, #275 `cleanupAnonUidArtifacts` chunked BatchWriter (off the 500-write transaction cliff) | prod-state PASS; `removeMember` created, all 12 functions deployed |
| 2026-06-07 | `cc8c84e` | #270 server allocators (`groupNetBalance.ts` `allocateShares`/`allocateExact`/`allocatePercent`) gain the negative-value→equal-split guard — full byte-for-byte parity with the client `BalanceCalculator` (shared oracle → `deleteGroup`/`leaveGroup`/`removeMember`) | prod-state PASS; all 12 functions updated |
| 2026-06-07 | `b53433d` | #248 PR 1 (#337): expense `lastEditedBy` field rule pins (`==auth.uid`, presence-gated create / diff-gated update; `validSoftDelete` carries it). #248 PR 2 (#339): new `expenseAuditLogger` `onDocumentWritten` trigger (server-owned expense CREATE/UPDATE/soft-DELETE audit log → event `activity_logs`) + `validActivityCreate` removed (event `activity_logs` now server-only) | prod-state PASS; `expenseAuditLogger` **created**, all 13 functions deployed |
| 2026-06-07 | `786c2f1` | #248 PR 4 (#343): `firestore.rules` opens expense edit/soft-delete to **any event participant** (drop `requesterIsRecordCreator()` from `validExpenseUpdate`) AND makes the `lastEditedBy == auth.uid` pin **mandatory** on every update (was diff-gated) — closes a merge-time-Gate-refuter [P1] where omitting `lastEditedBy` let the audit trigger mis-attribute an edit to the creator. Rules-only (no function add/remove); 13 functions unchanged | prod-state PASS; rules match `main`, all 13 functions present |
| 2026-06-08 | `5eaacf7` | #261 PR-0b (#371): mixed-currency balance-gate guard. `groupNetBalance.ts recomputeNet` now returns `currencies: Set<string>` (EXPENSE-fold only, **function-scope** so cross-event mixing is caught, `toUpperCase()`-normalized so legacy `omr`/`OMR` stays one currency); `deleteGroup`/`leaveGroup`/`removeMember` reject `failed-precondition` when `currencies.size > 1`, **before** the `isZero()` gate — closes the mixed-EXPENSE-currency false-zero (`+10 OMR / −10 USD` → fake `Decimal 0`) money-loss path. Functions-only (no rules/index/function-set change) | prod-state PASS; rules already up to date, all 13 functions updated |
| 2026-06-08 | `edd6421` | #261 PR-1 (#374): `firestore.rules` make `group.currency` authoritative + immutable (Model A foundation). New `currencyMatchesGroup(d)` helper pins a money doc's `currency` to `groupData(groupId).currency` on expense **create** (unconditional), expense **update** (diff-gated — a pure soft-delete is never re-checked, so a legacy mismatched doc stays soft-deletable), and **event-settlement create** (mirrors the already-live group-settlement equality). `validCreatorMetadataUpdate` drops `currency` from its `hasOnly` allow-list → currency settable ONLY at create. Client `updateGroup(currency:)` param removed (defense-in-depth). Rules-only (no function add/remove); a tautology for every current all-OMR write, forward-enforcement once Phase 2 moves the `'OMR'` write hardcodes | prod-state PASS; rules match `main`, all 13 functions present |
| 2026-06-09 | `b9163a1d` | #279 (#388): server-authoritative display-name collision guard in `joinGroupByInviteCode`. Inside the join txn, for a **brand-new member only** (gated on `didJoin == !memberSnap.exists && !memberIds.includes(uid)`, so the #53 heal-path and idempotent re-join are exempt), reject `already-exists` when the joiner's `trim().toLowerCase()` name matches an existing member (compared across member docs by `userId`/`displayName` **fields**, not doc id — #294 creator-doc trap). `already-exists ∉ isLookupFailure` so a collision never burns the 5/hr join throttle. Client maps the error to l10n `groupJoinNameTaken`. Functions-only (no rules/index/function-set change) | prod-state PASS; `joinGroupByInviteCode` updated, all 13 functions present |
| 2026-06-10 | `7370b307` | #366 (#421): server-maintained per-group balance aggregate. NEW `groups/{gid}/aggregates/balance` doc (schema v1: `netMilli`/`perEventNetMilli`/`currencies`/`eventCount`/`degraded`/`sourceTimeMs`) maintained by 4 new diff-gated `onDocumentWritten` triggers (`eventModuleBalanceAggregator` wildcard over expenses+settlements, `groupSettlementBalanceAggregator`, `eventBalanceAggregator`, `memberBalanceAggregator`) + NEW daily `balanceReconciler` (backfill + drift-heal). All call the shared `recomputeNet` oracle (extended with the participantIds-only `perEventNet` drill-down + `eventCount` — behavior-preserving for deleteGroup/leaveGroup/removeMember). Rules: new `aggregates/{aggregateId}` block (member read, client write denied). `deleteGroup` cascade now deletes the aggregate doc. Display-cache only — never OUTBOUND. Client read-path lands in PR2 | prod-state PASS; 5 functions CREATED (`eventModuleBalanceAggregator`, `groupSettlementBalanceAggregator`, `eventBalanceAggregator`, `memberBalanceAggregator`, `balanceReconciler`), all 18 present; rules deployed; first reconciler run (≤24h) backfills existing groups |
| 2026-06-11 | `20689860` | #441 PR2 (#444): the durable-credential gate, server side. `firestore.rules` gains `isDurableSignIn()` (`request.auth.token.firebase.sign_in_provider != 'anonymous'`) on `validGroupCreate` + the `inviteCodes` `allow create` chain — an anonymous-provider client (incl. an old ungated build's offline-replayed batch) can no longer create groups; `recoveryCleanupIntents` deliberately excluded (anon-written by design until PR5 deletes it). `joinGroupByInviteCode` rejects anonymous-provider tokens `permission-denied` **pre-throttle** (outside the tx catch, so no `joinAttempts` burn) via an optional-chained token read; closes the #197 anon-rotation throttle bypass for this callable. Client gate + sheet shipped in the same PR (not deploy-relevant) | prod-state PASS; rules deployed, `joinGroupByInviteCode` updated, all 18 functions present |
| 2026-06-11 | `c009b700` | #441 PR5 (#450): the cross-UID merge engine DELETED from prod. `cleanupAnonUidArtifacts` callable removed via `deploy --force` (18 → **17 functions**: 5 callables + 10 triggers + 2 scheduled); `recoveryCleanupIntents` rules match block + `validCleanupIntent` removed (collection now falls to the deny-all catch-all — fails closed); its TTL fieldOverride dropped (TTL disable applied by the deploy; the lingering index-override shell needed a manual Admin-API `indexConfig: {}` revert before the prod-state indexes check went green — firebase-tools does not delete an emptied fieldOverride). 4 residual `recoveryCleanupIntents` QA docs swept pre-deploy (FxtaZT…, lgb7GQ…, nFf1H1…, zh0Jf…). Mitigation-7 prod check recorded honestly in the spec: 28/29 member UIDs anon-only across ~35 groups, ALL identifiable dev QA data; proceeded on no-real-users + explicit owner order. PR3-device-QA ordering consciously overridden (callable restorable from `057f79f8`). Epic #441 CLOSED | prod-state PASS (all 13 checks); `firebase functions:list` confirms the callable gone (one-directional check the prod-state script cannot do); tag advanced `20689860` → `c009b700` |
| 2026-06-11 | `fdf8460b` | #461: security — scoped npm `overrides` pin of `@grpc/grpc-js` to `^1.14.4` under `google-gax` (GHSA-5375-pq7m-f5r2 / GHSA-99f4-grh7-6pcq: malformed request → server crash). Re-bundled into prod Functions. **Deps-only** — functions source logic, `firestore.rules`, and `firestore.indexes.json` are byte-identical to `c009b700`; `--force` re-asserts them unchanged. Phase 1 of the v1.5.0 release (see `docs/plans/2026-06-11-release-v1.5.0.md`) | prod-state PASS (all 13 checks); function set unchanged (17 present); tag advanced `c009b700` → `fdf8460b` |
| 2026-06-12 | `8acb7fdf` | #382 PR-2 (#467): server balance oracle bucketed per currency. `groupNetBalance.ts` `foldEventNet`/`recomputeNet` now return per-currency `net: Map<ccy,Map<uid,Decimal>>` + `perEventNet: Map<eid,Map<ccy,Map<uid,Decimal>>>` — expenses AND event/group settlements bucket by their OWN per-doc currency (raw key via `currencyOf`; the `currencies` Set stays expense-only + `toUpperCase()` for the v1 aggregate doc, NO LONGER a gate input). `deleteGroup`/`leaveGroup`/`removeMember` drop the `currencies.size>1` refusal → require **zero in every currency bucket** (no FX, each currency clears independently). `balanceAggregator` **Shim #2** flattens the sole bucket → v1 aggregate doc byte-identical (`finalizeNet`'s no-money OMR fallback fires ONLY at zero real currencies, so a single-currency group with an empty event never false-`degrade`s; `bucketizeDrill` keeps no-money perEvent zero-rows); `balanceReconciler` untouched. Behavior change: a cross-currency settlement no longer "settles" a debt (mirrors the merged client PR-1). Functions-only — no rules/index/function-set change; backward-compatible (single-currency = all prod = identical behavior; per-bucket logic unreachable until PR-6 relaxes the uniformity rules). Mid-v1.5.0 deploy verified safe: preserves the `fdf8460b` grpc-js pin, no function-set drift | prod-state PASS (all 13 checks); 17 functions unchanged, rules+indexes match `main`; tag advanced `fdf8460b` → `8acb7fdf` |
| 2026-06-12 | `b12a3f00` | #382 PR-3 (#471): aggregate doc v2 — per-currency buckets, both shims dropped. `balanceAggregator` deletes **Shim #2** and encodes `schemaVersion: 2` with `netMilliByCurrency` `{ccy:{uid:int}}` + `perEventNetMilliByCurrency` `{eid:{ccy:{uid:int}}}` (eventId-major, raw case-preserved bucket keys, ×1000 milli exact per bucket); the `currencies[]` array is gone (derive from bucket keys — it was expense-only and would omit settlement-only buckets); `degraded` is now **byte-cap-only** (the multiCurrency degrade died with the shim — a mixed group gets a real v2 doc). `balanceReconciler` `fingerprint()` re-keyed to the v2 field names in the SAME deploy (drift detection stays live; the first post-deploy sweep IS the v1→v2 backfill — every pre-existing doc warns drift once, by design). Dead `RecomputeResult.currencies` + `foldEventNet` plumbing deleted from `groupNetBalance.ts` (the 3 gates destructure only `net`/`liveEventRefs` — untouched). Client half (v2 decoder, facade chooser serves mixed docs, Shim #1 dropped, bucket-key cross-group fold, honest currency labels) shipped in the same merged PR #471; old v1-pinned clients decode v2 docs as null → once-path fallback (free compat). Functions-only — no rules/index/function-set change; uniformity rules still LIVE so prod buckets stay singletons until PR-6 | prod-state PASS (12 checks); 17 functions updated, rules+indexes match `main`; tag advanced `8acb7fdf` → `b12a3f00` |
| 2026-06-13 | `afff017e` | #382 PR-6 (#477): **the flip — mixed-currency data now creatable; epic #382 CLOSED.** `firestore.rules` deletes the four `currencyMatchesGroup` / group-equality clauses (expense **create**, expense **update** diff-gate, **event-settlement create**, **group-settlement create**) + the now-orphan `currencyMatchesGroup` helper — the `validCurrency` 10-code floor already runs in `validExpenseBase` + `validSettlementCore`, so a money doc's currency need only be **supported**, not equal to `group.currency`; an unsupported code (e.g. `XYZ`) stays denied (RED-proven: 4 "divergent denied" rules tests flipped to allowed + 3 XYZ-still-denied). `group.currency` stays create-only-immutable (re-cast as the per-expense **default**). Client half (per-expense currency picker reusing `CurrencyPickerSheet` + smart default last-used-in-event→group-default + soft fat-finger warning; all FOUR editor currency consumers re-pointed to the picked currency, fixing a JPY `toSubunits→toBigInt` truncation + an EXACT-split currency mismatch caught by the Gate) shipped in the same merged PR. **Rules-only — no function add/remove/logic change** (the server oracle + aggregate doc already bucket per-currency from PR-2/PR-3); `--force` re-asserts the 17 functions + indexes unchanged | prod-state PASS (all 13 checks); rules released to `cloud.firestore`, 17 functions unchanged, indexes + hosting match `main`; tag advanced `b12a3f00` → `afff017e` |
| 2026-06-13 | `25a3519b` | #483 (#493): localize push notification copy. `strings.ts` moves the empty actor/joiner-name fallback INTO `settlementBody`/`memberJoinBody` via a new per-locale `actorLabel` helper (`شخص ما` / `Someone`), dropping the pre-build `'Someone'` literals in `settlementNotifier.ts` + `memberJoinNotifier.ts` — an Arabic recipient of a third-party-recorded settlement now gets `سجّل شخص ما …` instead of the English `Someone` spliced into an RTL sentence (mirrors the existing `groupLabel` localized fallback). The third-party recorder branch (`createdBy` is neither party → empty `actorName`) now localizes correctly; the EN path is byte-identical for a non-empty name. Client half (token `locale` re-saved on app-language change via a new `appBootstrap` `languageCode` listener → `NotificationService.refreshTokenLocale`, so the server's `fcm_tokens/{uid}.locale`-driven copy follows an EN↔AR switch instead of freezing at first-write) shipped in the same merged PR — **not deploy-relevant**. **Functions-only — no rules/index/function-set change**; `--force` re-asserts the 17 functions + rules + indexes unchanged | prod-state PASS (all 13 checks); 17 functions updated (incl. `eventSettlementNotifier`/`groupSettlementNotifier`), rules + indexes + hosting match `main`; tag advanced `afff017e` → `25a3519b` |
| 2026-06-14 | `6ad69418` | #179 (#503, #509): the 3rd + 4th #179 push senders — **expense-created** (`expenseNotifier`) and **event-created** (`eventNotifier`) notifiers, both NEW triggers (17 → **19 functions**). Both `onDocumentCreated` (create-only, so open-edits #248 / soft-deletes / recovery uid-migration `update()`s never re-fire), skip the actor (`uid !== createdBy`), and target event participants by **auth UID** → `fcm_tokens/{uid}` (participantIds ⊆ `memberIds` = uids, rules-enforced). `expenseNotifier` mirrors the `BalanceCalculator` owe-set (non-empty `splitDistribution` v>0 keys → `customSplitParticipants` → personal `[]` → event participants), reads expense fields by their `toFirestore` names (`amountFils`/`currency`/`payerParticipantId`/…); en/ar copy via `strings.ts` (`#483` `actorLabel` empty-name fallback, free-text name appended after ` · `). Registered as single-line `export { … } from` re-exports (visible to `tool/list_expected_functions.sh`). Both Gate-cleared (fresh-context Opus review + independent refuter; refuter ran the emulator suite 7/7 + `tsc --noEmit` clean). Client needs no change — a `type:'expense'`/`'event'` tap just opens the app (`_routeFromData` early-returns for non-`{settlement,member_join}`). **Functions-only — no rules/index change**; `--force` re-asserts rules + indexes + the other 17 functions unchanged | prod-state PASS (all 13 checks); `expenseNotifier` + `eventNotifier` CREATED, all **19** functions present; rules + indexes + hosting match `main`; tag advanced `25a3519b` → `6ad69418` |
| 2026-06-17 | `80932c51` | #519 + #529 (#543): deleteGroup lock lifecycle. **#529** — dropped `canClearObservedLock` in `deleteGroup.ts` so an invocation clears only a lock it created (`createdLock:true`); a concurrent caller hitting `failed-precondition` can no longer wipe a peer's **live** lock. **#519** — new hourly `deleteGroupLockReaper` scheduled fn (1h grace > 540s timeout) resumes the SHARED extracted `finalizeGroupDeletion` core for stale `deletingInProgress` locks (settled → complete soft-delete, unsettled → clear lock). **Money-safe ONLY because it never bumps `deleteLockedAt`**, so `recomputeNet`'s resume horizon re-includes a partial cascade (provably always-settled — the balance gate runs before any mutation, so an unsettled-clear cannot corrupt). Reaper query `where('deletingInProgress','==',true).limit(50)` + in-memory age filter (no composite index; default single-field auto-index). 19 → **20 functions**. Spec `docs/plans/2026-06-16-deletegroup-lock-pair-519-529.md`; pre-impl Gate 0 P1 + `/automerge` review 0 P1 + refuter `refuted:false`. Also bundled **#544** (`caa23852`): pin `form-data 2.5.6` + `protobufjs 7.6.4` via `functions/package.json` `overrides` — fixes the readiness `npm audit --omit=dev` gate that began failing on every functions PR after new `form-data`/`protobufjs` advisories (deps-only). **Functions-only — no rules/index change**; `--force` re-asserts rules + indexes + the other 19 functions unchanged | prod-state PASS (all 13 checks); `deleteGroupLockReaper` CREATED (independently confirmed via `firebase functions:list` — v2 `scheduled`, us-central1, nodejs22), all **20** functions present; rules + indexes + hosting match `main`; tag advanced `6ad69418` → `80932c51` |
| 2026-06-17 | `54fd786a` | #550 + #549 + #548 (#278 PR1 add-by-name backend batch): the consciously-held 3-commit batch shipped together. **#550** — new `addShadowMember` callable (creator adds a placeholder "shadow" member **by name**): mints a uuid (`id == userId`), writes `members/{uuid}` `isShadow:true` + `arrayUnion` into `group.memberIds`; App-Check + creator-only + anon-reject + #279 case-insensitive name-collision + 50-member shadow-add cap; new shared `normalizeRequiredDisplayName` (rejects the `(former member)` suffix so a shadow name can't later break client event-create). Balance oracle is identity-blind → a shadow uuid is just another member id, **no oracle/rules change for the add path** (20 → **21 functions**). **#549** — `deleteAccount` no longer silently spares a durable Google/email account when the active session is an anonymous shell (identity-honest delete). **#548** — `firestore.rules` binds a client-created member doc id to `request.auth.uid` (#524 forged-duplicate-member guard, +7 rule lines). No-real-users → the held batch shipped freely as one deploy. Client add-by-name UI (PR2 #551 chips-at-create · PR3 #553 in-group add+badge+remove · PR4 #552 split-picker shadow markers) all merged to `main` the same day — **not deploy-relevant** (the callable is mocked in widget tests). **Functions + rules** — `--force` re-asserts indexes + the other 20 functions unchanged | prod-state PASS (all 13 checks); `addShadowMember` CREATED (`Successful create operation` confirmed in the deploy log), all **21** functions present; rules released to `cloud.firestore`, indexes + hosting match `main`; tag advanced `80932c51` → `54fd786a` |
| 2026-06-18 | `3eaa7fd2` | #278 claim/merge backend (PR6+7+8+9) — **the shadow-member claim flow; epic #278 CLOSED.** **PR6** (#556) `mergeUidMapKey` SUM-on-collision map re-keyer (shared helper PR7 uses for `splitDistribution` — sums two same-mode slices instead of clobbering on a uid collision). **PR7** (#557) `claimShadowEngine` uuid→uid re-key (two-phase `BatchWriter` cascade, `memberIds` rewritten LAST; a 3-term **D8 financial-universe pre-scan** rejects a claimer already in `priorNet`'s key-set ∪ `memberIds`, so an equal-split divisor shift / departed-but-still-participant can never break additive parity; post-commit `recomputeNet` `simNet==actualNet` parity backstop). Extracted `shared/batchWriter.ts` + `shared/mapReKey.ts` + `recomputeNet` out of deleteGroup/deleteAccount (behavior-preserving — those suites stayed green); `expenseAuditLogger` gains a `claimRekeyAt` sentinel-skip so a re-key burst logs ZERO phantom audit rows. **PR8** (#559) the **creator-approval gate**: 4 callables `requestClaimShadow`/`decideClaimRequest`/`listMyClaimRequests`/`listGroupClaimRequests` + `claimRequests` rules block (`allow read, write: if false` — fully callable-mediated, even the creator is denied a direct read) + raw `claimShadow` onCall **DE-EXPORTED** (the re-key is reachable ONLY via an approved `decideClaimRequest`; the request target = the doc's `requesterUid`, never a uid in the input — the impersonation seal); `decideClaimRequest` writes `status:'claimed'` ONLY on a fresh re-key, never inferred from `memberIds` (the phantom-claim guard, 2 refute rounds); shared `ids.ts`/`inviteCode.ts`. **PR9** (#561) `listUnclaimedShadows` read-only pre-join discovery callable (durable-gated, no membership gate, SAME claimable predicate as `requestClaimShadow` so it never lists a dead-offer or leaks a real member). 5 new callables CREATED (21 → **26 functions**); `claimShadow` is engine-only (never a deployed function). Each slice Gate-cleared pre-impl (fresh-context Opus, 0 P1) + `/automerge` fresh review + independent refuter at merge. Client half (join picker + permanent-confirm + waiting-poll + creator approve UI + 5 typed wrappers + l10n EN+AR) shipped in PR9 — **not deploy-relevant** (callables mocked in widget tests). **Functions + rules** — `--force` re-asserts indexes + the other 21 functions unchanged | prod-state PASS (all 13 checks); `requestClaimShadow`/`decideClaimRequest`/`listMyClaimRequests`/`listGroupClaimRequests`/`listUnclaimedShadows` CREATED (`Successful create operation` ×5 in the deploy log), all **26** functions present; rules released to `cloud.firestore`, indexes + hosting match `main`; tag advanced `54fd786a` → `3eaa7fd2` |
| 2026-06-18 | `390c45e2` | #560 (#563): notify the group creator when a placeholder-claim request arrives — the #278 PR9 discoverability tail (P9-1). NEW `claimRequestNotifier` FCM trigger (26 → **27 functions**). `onDocumentWritten` on `groups/{gid}/claimRequests/{id}` with a `before.status != 'pending' && after.status == 'pending'` guard: fires on create (∅→pending) AND declined→pending **re-open** — the reason it's `...Written` not `...Created`: `requestClaimShadow` re-`set()`s a deterministic-id (`${uid}__${shadowMemberId}`) doc, so a re-request after a creator decline is an UPDATE that `onDocumentCreated` would miss. Skips every `decideClaimRequest` transition (→claimed / →declined, incl. the already-claimed decline), the no-op pending→pending rewrite, and deletes (`claimed→pending` is impossible — `requestClaimShadow` throws pre-`.set()` on a `claimed` doc). Provably correct for the full `claimRequests` writer set (only `requestClaimShadow`→pending and `decideClaimRequest`→claimed/declined; no delete/cascade writer exists — grep-confirmed). Read-only: targets `group.createdBy` (an auth uid → `fcm_tokens/{uid}`; an anon creator with no token is silently skipped by `sendToUids`), self-skips `requesterUid == createdBy`, persists nothing to a domain doc. Bilingual en/ar copy (`claimRequestTitle`/`claimRequestBody`; #483 `actorLabel` empty-requester fallback + a dedicated empty-shadow stand-in avoiding a dangling possessive). Single-line `export { … } from` re-export (visible to `tool/list_expected_functions.sh`). Pre-impl Gate 0 P1 (1 P2 applied — writer-table completeness) + `/automerge` fresh-context review 0 P1 + independent refuter `refuted:false` (it also ruled out the #294 uuid-keying trap on `createdBy`). RED→GREEN, full functions emulator suite 493/493. Client needs no change (`type:'claim_request'` tap just opens the app; tap-routing is v2). **Functions-only — no rules/index/function-logic change to the other 26**; `--force` re-asserts rules + indexes + the other 26 functions unchanged | prod-state PASS (all 13 checks); `claimRequestNotifier` CREATED (`Successful create operation` in the deploy log), all **27** functions present; rules + indexes + hosting match `main`; tag advanced `3eaa7fd2` → `390c45e2` |
| 2026-06-20 | `327d6284` | #528 + #525 + #526 + #565 (#588, #582, #583, #585): the 1.6.0 backend bug-fix + hardening batch. **#528** (#588) — `firestore.rules` `positiveInt` now caps `amountFils <= 9007199254740991` (`Number.MAX_SAFE_INTEGER`, 2^53−1), backstopping a forged Dart/Admin write above the int64↔JS-number divergence point (the app's `MoneySerializer.fitsSafeSubunits` guards the normal client path — both shipped in the same PR, client = not deploy-relevant). One edit covers BOTH `amountFils` sites (expense + settlement) via the shared `positiveInt`. The JS rules-unit-testing harness cannot emit a >2^53 `integerValue` (the SDK serializes such numbers as `doubleValue`, caught by `value is int` one clause earlier; BigInt writes are refused) — so the clause is **defense-in-depth with a safe-max ACCEPT pin**, not a JS-exercisable reject; the client `fitsSafeSubunits` table tests carry the tested money-safety weight. Pre-impl Gate 0 P1 + `/automerge` review 0 P1 + refuter `refuted:false`. **#525** (#582) — `balanceReconciler` now sweeps ALL live groups via cursor pagination (the prior `where('isDeleted','==',false)` skipped legacy field-absent groups). **#526** (#583) — `writeRateMonitor` stops double-counting expenses (the server `activity_logs` create was being attributed to the actor). **#565** (#585) — `claimRequestNotifier` + `strings.ts` now ALSO notify the claim **requester** when the creator decides (approve/decline), bilingual en/ar; extends the existing trigger — **no new function**. **Rules + functions** — `--force` re-asserts indexes + the 27 functions unchanged (no add/remove; all 27 `Successful update operation`). NB: the subsequent `cd33b5ad` (#586, the #527 UTF-16 test pin) is **test-only** (`functions/test/…` only, no `functions/src`/rules) — no deployable delta, so `pending_deploy.sh` flagging it as pending is a known false-positive, NOT ghost-debt | prod-state PASS (all 13 checks); rules released to `cloud.firestore`, all **27** functions updated (none created/deleted), indexes + hosting match `main`; tag advanced `390c45e2` → `327d6284` |
| 2026-06-21 | `29abede0` | #203 Slice 1 (#609) + #287 glyph rules (#612) deployed together as the **superset** — resolves a concurrent-deploy collision (two sessions deploying #609 and #612 separately ping-ponged prod rules; deploying `main` `29abede0` with BOTH converged it + made further deploys idempotent). **#203** (#609, `28253f98`): expense `splitExplanation` — an OPAQUE display-only map (itemized-split metadata for the future editor; balance truth stays `splitDistribution`, persisted as `SplitMode.exact`). Added to BOTH expense allowlists (`validExpenseBase` `hasOnly` + `validExpenseUpdate` `affectedKeys`) + a `splitExplanationBounded` wrapper (`is map` + top-level `size()<=64`, entry-count only — the 1MB doc limit is the payload backstop), wired unconditional-on-create + diff-gated-on-update (legacy oversized docs stay soft-deletable). Server never reads it (0 `functions/src` refs) → no oracle/function change; itemized reduces to a standard `exact` split so client↔server parity holds. **#287 glyph** (#612, `29abede0`): group `glyph`/`inkIndex` allow-list + `validGlyph`/`validInkIndex` bounds on group **create** + `validCreatorMetadataUpdate` (present-or-valid; clear via field-delete). #610 (`dc58f666`, glyph render foundation — model + 12 SVGs) is **client-only** (no backend surface); #586 (`cd33b5ad`, #527 UTF-16 test pin) is **test-only** (no deployable delta — its pending false-positive clears as the tag advances past it). **Rules-only — no function add/remove/logic change**; `--force` re-asserts the 27 functions + indexes unchanged. Deployed with `RIHLA_ALLOW_DIRTY` (untracked `design-web/` + docs sit outside the deploy surface; deployed artifacts byte-identical to a clean tree at `29abede0`). | prod-state PASS (all 13 checks); rules released to `cloud.firestore` (both rulesets present), all **27** functions unchanged (`Successful update operation`), indexes + hosting match `main`; tag advanced `327d6284` → `29abede0` |
| 2026-06-24 | `6dcf05e6` | #648 (#654): **un-gate join for anonymous users** — "gate creation, not participation." `joinGroupByInviteCode.ts` drops the `sign_in_provider==='anonymous'` reject so an anon user may JOIN + add expenses; group + invite-code **CREATE** stay durable-gated (`firestore.rules` `isDurableSignIn` at inviteCode/group create + client `stageGroup`, all unchanged). Money rules already key on participation (`validExpenseCreate`→`isEventParticipant`, `validEventCreate`→`isGroupMember`), so **no rules change**. The `#197` per-UID join-throttle anon-rotation bypass re-opens, contained by enforced App Check + invite-code entropy (32⁶ ≈ 1.07 B) — the rewritten in-file comment forbids re-adding the reject / per-IP / per-code. Client half (removed the join durable gate + the **cross-UID swap guard**: extracted shared `shell_emptiness_gate.outgoingShellProvablyEmpty` and gated `triggerGoogleRestore` so a now-reachable *populated anon shell* can't hit the irreversible Google discard-shell swap via the cold-start `userGroupsProvider` false-empty — Gate R1's catch) shipped in the same merged PR — **not deploy-relevant**. Pre-impl Gate 3 rounds → 0 P1; `/automerge` fresh-context review 0 P1 + independent refuter `refuted:false` (RED→GREEN guard test). **Functions-only — no rules/index/function-set change** (`joinGroupByInviteCode` updated in place, no add/remove); `--force` re-asserts rules + indexes + the other 26 functions unchanged. Deployed with `RIHLA_ALLOW_DIRTY` (untracked `design-web/` + docs sit outside the deploy surface; deployed artifact byte-identical to a clean tree at `6dcf05e6`). | prod-state PASS (all checks); all **27** functions updated (`joinGroupByInviteCode` incl.; none created/deleted), rules + indexes + hosting match `main`; tag advanced `29abede0` → `6dcf05e6` |
| 2026-06-25 | `9caab3e0` | #673 (#673) + #672 (#672) — deleteGroup lock-lifecycle hardening; **#676 (#676)** doc/comment refresh. **#673** (Closes #668): a *malformed* delete-group lock (`deletingInProgress:true` but `deleteLockedAt` **absent** — un-reapable by the timestamp-driven `deleteGroupLockReaper`) now self-clears. New `clearMalformedObservedDeleteGroupLock` (`deleteGroup.ts`) + `clearMalformedLock` (`deleteGroupLockReaper.ts`) transactionally set `deletingInProgress:false` + `FieldValue.delete()` the lock fields ONLY when `deletingInProgress===true && deleteLockedAt==null && deleteLockedBy` matches the observed owner; `deleteGroup` then throws `failed-precondition` ("Malformed delete lock was cleared. Retry") so a stuck group becomes deletable instead of wedged forever. **#672** (Closes #663): `leaveGroup` + `removeMember` (Admin SDK **bypasses** `firestore.rules`) now honor the delete-quiesce marker like every membership writer — reject `not-found` when `group.isDeleted===true \|\| group.deletingInProgress===true`, and move the membership mutation from a plain `batch` into a `runTransaction` that re-reads the group + lock state **fresh** before removing the member, so a leave/remove can't race past an in-flight `deleteGroup` and mutate membership during the quiesce window (idempotent `alreadyLeft` early-return + `member_left` activity row preserved). **#676**: comment-only refresh across `claimShadow`/`deleteAccount`/`joinGroupByInviteCode`/`eventNotifier`/`expenseAuditLogger`/`expenseNotifier` (member-keying #524 + currency guidance; **no logic change** — `--force` re-asserts byte-identical behavior). **Functions-only — no rules/index/function-set change** (`leaveGroup`/`removeMember`/`deleteGroup`/`deleteGroupLockReaper` updated in place, none created/deleted); `--force` re-asserts rules + indexes + the other functions unchanged. Deployed from clean `main` @ `9caab3e0`. | prod-state PASS (all checks: rules, indexes, all **27** functions present, hosting ×8); tag advanced `6dcf05e6` → `9caab3e0` |
| 2026-06-26 | `aff3dd1f` | #558 (#711): **claimShadow post-commit parity backstop — close two TOCTOU holes; #558 CLOSED.** `claimShadowEngine`'s `assertExactParity` previously threw `internal` *after* the Phase B/C uuid→uid cascade was already durable (no rollback) AND false-rejected any benign concurrent edit whose `simNet != actualNet` drift landed on a *correct* claim; its idempotent retry returned `alreadyClaimed` before any verification, silently blessing a torn/corrupt state. The reachable corruption: a concurrent OPEN-edit expense keying the to-be-deleted shadow lands *after* Phase B enumerates the event's expenses → never re-keyed → Phase C deletes the shadow member doc → the oracle's member-gate (`groupNetBalance.ts`) **drops** the shadow's slice → money vanishes AND the shadow is **absent from `net`** (so `netHasUid` is structurally blind; absolute `bucket-sum==0` conservation can't distinguish it from the *honest* #249 admin-shrink drop). The fix (confined to `claimShadow.ts`, no oracle/rules/client/`decideClaimRequest` change) adds a **mode/scope-gated lingering-shadow-reference scan** `snapshotReferencesShadow` that reads field-for-field the SAME identity set the oracle uses (member `userId`, live event `participantIds`, expense `payerParticipantId` + custom-scope `customSplitParticipants` + non-`equally` `splitDistribution` keys, event+group settlement parties): **Edit A** — `assertExactParity` throws iff the shadow is still referenced; a shadow-gone `mismatch` is a benign concurrent edit → `actualNet` authoritative → advisory `warn`, no throw. **Edit B** — `:357` idempotent return scans the **reused** Phase-A snapshot (zero extra reads) → throw on torn, `alreadyClaimed` on clean. **Edit C** — `:511` Phase-C-already-retired return scans a fresh snapshot. Gate-cleared pre-impl (2 fresh-context Opus rounds, 0 [P1]); `/automerge` fresh-context review 0 P1 + independent refuter `refuted:false` (refuter reverted the engine and reproduced the pasted RED — T28 `mismatch:"OMR/owner: sim=6 actual=11"` shadowSurvived:false, T29 false-reject, T30 silent-bless — verbatim; built the field-for-field oracle↔scan parity table). RED→GREEN: claimShadow 35/35, claimRequest 24/24, full functions suite 542/542; `tsc --noEmit` + `eslint src` clean. **Functions-only — no rules/index/function-set change** (`claimShadow` is engine-only, never a deployed function; the 27 deployed functions that wrap it — `requestClaimShadow`/`decideClaimRequest`/`addShadowMember`/… — updated in place, none created/deleted); `--force` re-asserts rules + indexes + all 27 functions. Follow-up [[#710]]: the two-engine concurrent double-approve claimer-A/B torn mix (no surviving shadow ref) is outside the scan's discriminating power — needs a per-shadow lock. Deployed from clean `main` @ `aff3dd1f`. | prod-state PASS (all checks: Firestore rules + indexes match `main`, all **27** functions present/updated, hosting ×8); tag advanced `9caab3e0` → `aff3dd1f` |
| 2026-06-27 | `18306fc6` | #714 (#714, **Closes #710**): claimShadow **per-shadow locking** — the #710 follow-up to #558's post-commit parity backstop. Adds per-shadow claim **reservations** (a `claimShadowLocks/{shadowId}` doc + a `claiming` transient member state + lock-token verification + mutation markers + compare-release on completion) so two concurrently-approved claims of the **same** shadow can't interleave into a torn uuid→uid cascade — the residual #710 hole #558's reference-scan couldn't discriminate (no surviving shadow ref). New scheduled **`claimShadowLockReaper`** (27 → **28 functions**) finalizes/clears a lock whose owner crashed: a `completedByThisLock` flag + an atomic `finalizeClaimAndRelease` (status + lock-delete + freeze-clear in ONE tx, guarded on `status==='claiming'`) close a reaper/decide **permafreeze** (the headline emulator-masked bug — RED-proven by the refuter: reverting the two `completedByThisLock` lines leaves `status` stuck at `'claiming'`). **Freezes** Admin/rules client-write paths during claim + account-deletion identity rewrites (`groupAllowsClientWrites` ANDs `claimingInProgress` / `accountDeletionInProgress`; the sentinels are presence-allowed in `validExpenseBase.hasOnly` but excluded from every per-op mutation `hasOnly` and blocked on create — non-client-writable), with a `processGroup` release-on-every-exit + `deletionReaper` backstop so a crashed rewrite can't permafreeze a group; protects pre-join claimants from account deletion; extends claim/delete re-key to itemized `splitExplanation` + recursive activity metadata. **Two new COLLECTION_GROUP index overrides** (`claimRequests.requesterUid`, `claimShadowLocks.claimerUid`) — `deleteAccount`'s two `collectionGroup().where()` scrub queries are **dead in prod** without them (emulator masks the missing index), so deployed WITH the functions. Gate-cleared pre-impl (2 fresh-context Opus rounds) + `/automerge` fresh-context review 0 P1 + independent refuter `refuted:false` (refuter reverted the `completedByThisLock` fix and reproduced the post-Phase-C-crash permafreeze RED, and re-verified the two overrides exactly satisfy the single-equality collectionGroup scrub queries). RED→GREEN: **298 emulator tests** green across the 8 changed suites; `tsc --noEmit` + `eslint` clean. **Functions + rules + indexes** — `claimShadowLockReaper` CREATED, the other 27 updated in place (none deleted); `--force` re-asserts the unchanged surface. Deployed from clean `main` @ `18306fc6`. **NB:** the post-deploy prod-state check false-failed once on a `ttl` absent-vs-`false` index-normalizer gap in `tool/check_firebase_prod_state.sh` (Firebase materializes `ttl:false` on non-TTL fieldOverrides; the local file omits it — a #211-class cosmetic false-fail). Prod indexes were **correct throughout** (independently fetched — both overrides carry the `ASCENDING COLLECTION_GROUP` index; only the `ttl:false` scalar differed). Fixed by deleting `ttl` when default-`false` in the normalizer (still catches real `true`↔`false` drift) + re-verified PASS before advancing the tag. | prod-state PASS (all checks: Firestore rules + indexes match `main`, all **28** functions present — `claimShadowLockReaper` `Successful create operation`, the other 27 `Successful update operation` — hosting ×8); tag advanced `aff3dd1f` → `18306fc6`. First check run FAILED on the `ttl` index-normalizer false-fail (see row NB); re-run clean after the `check_firebase_prod_state.sh` fix |
| 2026-06-29 | `26c5cdac` | #752 PR1 (#754): **decompose a group-level settle-up into per-event settlement writes.** `firestore.rules` **ADDITIVE/permissive** — (1) optional `groupSettleUpId` link on settlements added to `validSettlementCore`, guarded `!('groupSettleUpId' in data) \|\| data.groupSettleUpId is string` (the core runs on EVERY settlement create; the client OMITS the key when null, so a direct `data.groupSettleUpId` access on the absent key would deny ALL keyless settle-ups — i.e. every existing one); (2) `groupSettleUpId` added to the event-settlement (`validEventSettlementCreate` `hasOnly`) and group-settlement (`hasOnly`) allow-lists; (3) `validEventSettlementCreate` writer gate relaxed `isEventParticipant(groupId,eventId)`→`isGroupMember(groupId)` so a group settle-up decomposed into per-event docs **and** #595 settle-on-behalf are accepted — counterparties are STILL gated to event participants (`validEventSettlementBase` `payerParticipantId`/`recipientParticipantId in participants()`), so it is a shape-gate widening, not a value-gate; nothing previously-allowed becomes denied. Server **oracle UNTOUCHED** — `groupNetBalance.ts` already folds event settlements per-event + group settlements globally by collection PATH (not the `scope` field), so a decomposed settle-up is byte-for-byte equivalent at the aggregate; both writes fire `balanceAggregator` recompute. Client half (capped subunit allocator `BalanceCalculator.decomposeGroupSettlement` → N event-settlement writes + 1 residual group settlement, display breakdown, group-history union of tagged event settlements, correct-button hidden on tagged rows) shipped on `main` — **not deploy-relevant**; the whole-issue corrections flow is **PR2 #753 (#755)** which is CLIENT-ONLY (no rules/functions) and needs no deploy. **Rules + `functions/test/` only** — no function add/remove/logic change. Deployed from clean detached `main` @ `26c5cdac`. | prod-state PASS (all 13 checks: Firestore rules match `main`, all **28** functions present — `Successful update operation` ×28, none created/deleted — indexes + hosting ×8 match `main`); tag advanced `18306fc6` → `26c5cdac` |
| 2026-06-30 | `90d67bb6` | #723 (#763): **event close lifecycle — freeze spending, settlements stay live (Slice 5 of #202).** `firestore.rules` adds `eventAcceptsExpenseWrites` (L190) which **REPLACES** `eventAllowsClientWrites` in the TWO expense write paths only (expense create L706 + update L762) — it folds `isDeleted` (absent/false) **and** `isClosed==false` into the **same** `eventData().get(key,default)` reference count, so a closed event blocks expense create/edit/soft-delete with **no extra `get()`** against the rules-engine 1000-expression-per-request ceiling. Settlements keep the untouched shared `eventAllowsClientWrites` (L862/L873) → **stay writable after close** (the epic contract: a closed event still settles). New `validEventCloseToggle` (L557, wired `\|\| validEventCloseToggle()` in the event `allow update` OR-chain L583) = admin close **and** reopen in ONE function: cheap `diff().hasOnly(['isClosed','closedAt','closedBy','updatedAt'])` FIRST, then a close branch (open/legacy→closed, `closedBy == request.auth.uid` pinned, `closedAt is timestamp`) XOR a reopen branch (closed→open, clears `closedAt`/`closedBy`); it deliberately does **NOT** call `validEventBase`/`validEventUpdateCommon` because re-running `participantIds.hasOnly(groupMembers())` would permanently block closing an event with a departed participant (#249) — and re-running it twice would also risk tipping the heaviest event-update path over the 1000-expression ceiling. Expense create also pins the close triple absent-or-false (L484-486) so a create can't be born closed. Schema-only model add (`Event.isClosed`/`closedAt`/`closedBy`, mirrors soft-delete; `isClosed` is the load-bearing immediately-readable offline-safe gate) with **ZERO money-math change** — no provider/oracle/`recomputeNet` reads the close fields (grep=0); a closed event still contributes to balances. Client half (close/reopen danger-zone tile, "🔒 Closed · spending frozen" hub banner, disabled Add-expense CTAs with settle-up still live, add/edit screens reject closed, l10n en+ar) shipped on `main` — **not deploy-relevant**. Pre-impl Gate 0 P1 (1 P2 + 2 P3 applied pre-code); `/automerge` fresh-context review + independent refuter cleared. 184 rules tests pass (17 new incl. settlement-stays-live-after-close + departed-participant-close); full Flutter suite 2618/0. **Rules-only — no function add/remove/logic change** (`--force` re-asserts the 28 functions + indexes unchanged). Deployed from clean `main` @ `90d67bb6`. | prod-state PASS (all checks: Firestore rules match `main`, all **28** functions present/updated — `Successful update operation` ×28, none created/deleted — indexes + hosting ×8 match `main`); tag advanced `26c5cdac` → `90d67bb6` |
| 2026-06-30 | `65030ad9` | #766 (#767): **freeze spending snapshot at close (Slice 6 of #202).** `firestore.rules` `validEventCloseToggle` gains a 5th diff key `spendingSnapshot` in its cheap `diff().hasOnly(['isClosed','closedAt','closedBy','updatedAt','spendingSnapshot'])` gate + a new `spendingSnapshotBounded(d)` opaque guard (`!d.keys().hasAny(['spendingSnapshot']) \|\| (d.spendingSnapshot is map && d.spendingSnapshot.size() <= 16)`, mirroring `splitExplanationBounded`) evaluated **AFTER** the cheap diff gate — so it only runs on a close/reopen-shaped write, deliberately does **NOT** touch `validEventBase`, and adds no 1000-expression-ceiling risk to the heaviest event-update path (full 189-test rules suite green, no regression). The blob is **opaque display-only** (the frozen SPENDING half of the event recap), written by the client in the close partial-`.update()` and **DELETEd on reopen** (`FieldValue.delete()` — an explicit null would fail the `is map` guard); the oracle / `recomputeNet` / every Cloud Function **NEVER** read it (grep=0, splitExplanation-class). The light/admin update allow-lists still **EXCLUDE** `spendingSnapshot`, so it is writable **ONLY** via a genuine admin close/reopen transition (a no-transition or non-admin write is denied — pinned by the new `#766 writing spendingSnapshot WITHOUT a close transition is rejected` test). Client half (`SpendingSnapshot` model, `EventRecap.fromSnapshot` frozen-spending + live-settlement, `eventRecapProvider` freeze branch, capture-at-close in the danger section, "spending frozen" caption, en+ar l10n, `Event.spendingSnapshot` schema add — **OMITTED** from `toFirestoreMap` so `validEventCreate keys().hasOnly` stays unbroken, copyWith-preserved, `fromDoc` TOTAL-PARSE) shipped on `main` — **not deploy-relevant**. Pre-impl Gate R1 1 P1 (per-participant owed from `balances`, not `EventRecap`) → R2 0 P1; `/automerge` fresh-context diff review (0 P1) + independent refuter (`REFUTED:false`, both re-ran the rules emulator) cleared. 189 rules tests pass (5 new #766: bounded-accept, >16-key reject, non-map reject, reopen-delete accept, close-only reject); full Flutter suite 2641/0. **Rules-only — no function add/remove/logic change** (`--force` re-asserts the 28 functions + indexes unchanged). Deployed from clean `main` @ `65030ad9`. | prod-state PASS (all checks: Firestore rules match `main`, all **28** functions present/updated — none created/deleted — indexes + hosting ×8 match `main`); tag advanced `90d67bb6` → `65030ad9` |
| 2026-07-01 | `f0de3a98` | #780 (Refs #179): **notification idempotency + claim-decision routing hardening.** Cloud Functions only — no `firestore.rules`/index/function-set change. `fcmSender.sendToUids` gains an optional 4th `options.dedupeKey`; when set, a `claimDeliveryMarker` `runTransaction` creates `notificationDeliveries/{sha256(dedupeKey)}` (admin-SDK only; client default-deny via the root `match /{document=**}` if-false — no rules match, no client read) and **skips the send if the marker already exists**, collapsing Eventarc retry redeliveries of the SAME CloudEvent to one user-facing buzz. The 5 Eventarc notification triggers (`eventSettlementNotifier`/`groupSettlementNotifier` in `settlementNotifier`, `expenseNotifier`, `eventNotifier`, `claimRequestNotifier`) thread a dedupe key = type-prefix + doc ids + the **stable CloudEvent `event.id`** (retries dedupe; distinct events — incl. #752 decomposed per-event settlements — each still notify; distinct type prefixes + globally-unique `event.id` ⇒ no cross-trigger sha collision). `claimRequestNotifier`'s claim-decision payload adds `decision`/`inviteCode`/`routeability` (`member`/`pre_join`, derived from live `groups/{gid}.memberIds`) so a **declined already-member** routes to `/group/:gid` instead of a `/join/<code>` they can't complete. `memberJoinNotifier` intentionally omits the key (callable-gated by its own `didJoin` idempotency, not Eventarc). Marker is best-effort (created before send; a crash between marker+send drops one buzz — documented acceptable, no money/data impact; the transaction is contention-safe so concurrent redelivery never double-sends). Client half (`notification_service.dart` claim routing across the 4 `decision`×`routeability` combos, `group_settings_screen.dart` direct-entry back-nav → `/home` fallback) shipped on `main` — **not deploy-relevant**. Merge-time Gate: `/automerge` fresh-context diff review (0 P1) + independent refuter (`REFUTED:false`, both re-ran the route/key enumeration against live code). RED evidence in-diff (retry-sends-once ×5 + routeability + 6 client-routing cases + back-fallback) with pasted failing-before-fix output; `Refs #179` (partial — multi-device tokens, edit/delete/leave/group-deleted pushes, tray QA stay open), no `Closes` in PR body or commit messages. Deployed from clean `main` @ `f0de3a98`. | prod-state PASS (all checks: Firestore rules + indexes match `main`, all **28** functions present/updated — `Successful update operation` ×28, none created/deleted — hosting ×8 match `main`); tag advanced `65030ad9` → `f0de3a98` |
| 2026-07-02 | `bcb27382` | #793 + #794 (Closes #245): **shadow event fan-in + auto-seed default ledger event.** #793 (Cloud Functions only — no rules/index/function-set change): `addShadowMember` now fans the new shadow into every LIVE event's `participantIds`/`participantNames` via `shared/eventFanIn.ts`, extracted **verbatim** from `joinGroupByInviteCode`'s join fan-in (join refactored onto the shared helper — its 54-test suite green through it = no-drift proof). Semantics identical to join: skip soft-deleted events, include closed (#723 blocks new expenses there anyway), `arrayUnion` id + merge name map + bump `updatedAt`, `MAX_FAN_IN_EVENTS=400` tx guard (500-write ceiling). Root cause: the expense editor rosters `event.participantIds` and NO client path grows it — only join fanned, so a shadow added at group-create chips or later via Manage Members was un-splittable in existing events. #794 (client; backend surface = its emulator rules test only, no runtime change): `stageGroup` seeds one `trip` event per new group (name = group name, creator-only, `Event.toFirestoreMap()` single-serializer) chained batch→member→event — the seed exists before `_seedShadowMembers` fires, so #793's fan-in populates it. Spec-Gate before code (round 1: 0 P1); merge-time `/automerge` fresh-Opus review (0 P1) + refuter (refuted:false) on BOTH PRs. `Closes #245` in #794's commit body (issue auto-closed). Deploy note: first run (user shell) was killed by a 2-minute timeout AFTER all 28 update ops were initiated — server-side ops completed (interim prod-state PASS); the recorded deploy is the clean rerun from `main` @ `bcb27382` that did deploy→verify→tag atomically. | prod-state PASS (rules + indexes match `main`, all 28 functions present, hosting ×8 match); tag advanced `f0de3a98` → `bcb27382` |
| 2026-07-03 | `07864871` | #810 (Refs #808 PR1): **expense fan-in to the group activity feed.** `expenseAuditLogger` also writes `expense_added`/`expense_edited`/`expense_deleted` entries to `groups/{gid}/activity` in the exact `GroupActivityLog` client shape (idempotent `.set` keyed on the CloudEvent `event.id`, verb-phrase description without actor name, ISO-string timestamp, metadata = ids + money scalars only — no uid-keyed maps, so claim-rekey `rekeyActivityLogs` and the `deleteAccount` scrub cover it unchanged; `claimRekeyAt` suppression gates BOTH writes); `firestore.rules` `validGroupActivityCreate` swaps `type is string` for the 4-type client allow-list (`event_created`/`event_deleted`/`group_settlement`/`member_joined` — `member_left` + `expense_*` are Admin-SDK-only, closing a pre-existing forgery hole); `groupActivityWriteRateMonitor` skips `expense_*` creates (T1 already counted the underlying expense write; the skip is type-keyed and safe ONLY because the rules allow-list makes `expense_*` un-forgeable — coupled pair, extend both together). Oracle/#366 aggregate untouched. Rules 193/193, trigger 17/17, monitor 12/12, full emulator suite green at deploy time. **NB (why this row landed late):** PR #812 — meant to record this ceremony — instead carried a stale pre-PR1 snapshot that silently REVERTED the fan-in + rules allow-list + monitor skip (+ their tests) from `main` (−282 lines). Prod was never affected (the `backend-deployed` tag stayed at `07864871`, which contains everything), but `main` was one deploy away from clobbering it. Restored byte-identical from the deployed commit, and this row added, by the restore PR. | prod-state PASS (recorded at the 2026-07-03 ceremony: rules + indexes match, all 28 functions updated in place — none created/deleted — hosting ×8 match); tag advanced `bcb27382` → `07864871` |
| 2026-07-03 | `35785be4` | #814 (#820): **value-domain floor for client-writable group-activity metadata — rules-only.** `validGroupActivityCreate` gains `validActivityMetadata(md)`: `amountFils` absent-or-int ≥ 0 (rejects NaN/Infinity/doubles/negatives), `currency` absent-or-`validCurrency` allow-list, legacy `amount` absent-or-string, `eventName`/`memberName`/`memberAction` absent-or-string (the PR3-review type-forge axis), `size() <= 16`; ids/unknown keys stay opaque per the issue. All via the single-reference `map.get(key, default)` pattern (#723) in the standalone `/activity` create block — no near-ceiling path gains expressions. Client display half shipped earlier in #815/#816 (isFinite + type guards); server writers (`expense_*` fan-in, `member_left` callables) are Admin-SDK and bypass rules — unaffected. Functions/indexes had ZERO content delta vs `07864871` (the #812/#813 churn nets out), so this deploy's only real payload is the 17-line rules change; the 28 functions redeployed in place as a side effect. Gate: spec review 0 P1 rd-1; merge-time diff review 0 P1/P2/P3 + refuter REFUTED:false; rules suite 201/201 (independently re-run by the orchestrator, not just the implementer's claim). NB: the first deploy run was killed at the session-harness 2-min cap mid-function-updates (rules already released); immediately re-run to completion — prod-state PASS + tag advance recorded on the completed run. | prod-state PASS (rules + indexes match, all 28 functions updated in place — none created/deleted — hosting ×8 match); tag advanced `07864871` → `35785be4` |
| 2026-07-03 | `18b1d3e7` | #826 (Refs #818 Wave 2, Decision 0): **anon-create gate removed.** `firestore.rules` loses `isDurableSignIn()` (helper + its only two conjuncts: `validGroupCreate`, `inviteCodes` create) — anonymous users can now create groups + invite codes; join was already un-gated (#648 "gate creation, not participation"), so the ONLY durability-gated server surface left is `addShadowMember`. That callable is comment-only in this delta: its anon `permission-denied` reject STAYS as the server backstop for the client-side **shadow carve-out** (the three by-name-add affordances — create-screen chips, group-detail #807 shortcut, members-section — gate on `isDurableUserProvider` with the `shadowAddRequiresLink` hint; an anon-created group can hold no shadows, so the claim cluster is transitively unreachable for anon creators). Client half in the same commit: `stageGroup` anon guard + gate provider + `DurableCredentialRequiredException` + `durableCredentialRequired` key deleted; the #285 `AccountBackupNudge` (which fires for any anon user) is the replacement, non-blocking prompt; gate-intent machinery retained-inert (cleanup = #825). Spec-Gate 2 rounds (1P1/1P2/3P3 → 0P1 — the P1 was exactly the newly-reachable `addShadowMember` reject); merge-time `/automerge` fresh-Opus diff review (0 P1/0 P2/1 P3) + refuter REFUTED:false — the refuter re-ran the emulator suite itself (201/201, incl. both inverted anon-create tests + the #814 `validActivityMetadata` suite) and test-merged `main` (already up to date). RED evidence in PR body: both anon-create tests failed `PERMISSION_DENIED` against unmodified rules. | prod-state PASS (rules + indexes match, all functions updated in place — none created/deleted — hosting ×8 match); tag advanced `35785be4` → `18b1d3e7` |
| 2026-07-03 | `abee70e8` | #830 (Refs #818 Wave 3.1): **settlement direction in activity feeds — rules half.** `validActivityMetadata` gains 5 absent-or-typed string clauses (`recipientId` promoted from opaque to typed; `fromUserId`/`toUserId`/`fromName`/`toName` — the direction keys both `group_settle_up_screen.dart` log sites now stamp). Pure tightening: the type allow-list, `hasOnly` key list, and 16-key cap are untouched, and the previously-deployed floor already accepted the new keys as unknown-opaque (refuter verified against this tag pre-merge), so there was zero ordering hazard. Client half in the same commit: directional `_settlementText` resolver (actor-relative paid/received/between, legacy+forged → generic fallback), unconditional sage `+` row deleted (settlement rows regain their timestamp), search matcher gains the two name haystacks. Emulator suite 202/202 incl. new forge-rejection + legacy-shape-anchor tests. Spec-Gate 1 round (0P1/2P2/4P3 — both P2s were compile-breakers, folded in pre-implementation); `/automerge` fresh-Opus review 0P1/0P2/0P3 + refuter REFUTED:false (simulated the cross-merge with #829, checked the deployed-tag rules). Follow-up filed: #831 (event-scoped settlements write no activity row at all). **Rules-only deploy-relevant change** — no function-set/index change; `--force` re-asserts functions + indexes unchanged | prod-state PASS (all checks; rules match `main`, functions updated in place, hosting ×8 match); tag advanced `18b1d3e7` → `abee70e8` |
| 2026-07-04 | `68cd3d89` | #874 (#876): **offline group creation is atomic — one WriteBatch + after-state create rules.** `firestore.rules` adds `groupFoundingBatchByCreator(groupId)` (`!exists(group) && existsAfter(group) && uid in groupAfterData.memberIds && groupAfterData.createdBy == uid`) so the creator member doc + the #245 seeded event can be validated against **after-state** when they ride in the SAME atomic batch as their brand-new group; the `!exists` guard forces any EXISTING (incl. soft-deleted/deleting/claiming/accountDeletion — groups are never hard-deleted) group down the standard `groupAllowsClientWrites` path, and `validGroupCreate` pins `createdBy==uid`/`memberIds==[uid]`/`isDeleted==false` so the after-state branch grants nothing the standard path wouldn't (no forgery, no soft-delete bypass; atomicity means a bad group leg sinks the whole batch). `validMemberCreate` gains the founding OR-branch + swaps its CREATOR `createdBy` read to `groupAfterData` (createdBy immutable post-create → byte-neutral on the standard path). `validEventCreate` refactored to a create-only `validEventCreateFields` twin (MUST-MIRROR `validEventBase`'s field list) + a per-branch participant-membership check — `validEventBase` and the entire event-UPDATE OR-chain are left **BYTE-IDENTICAL** (they sit near the 1000-expression ceiling #723; duplication over extraction on purpose, so zero hot-path change). Same `getAfter`-in-batch pattern already shipped for the `inviteCodes` create leg. Client half: `stageGroup` now writes all four founding docs (group, inviteCode, member, seeded event) in ONE `batch.commit()` — was chained on `batch.commit().then().then()`, so OFFLINE the callbacks never fired and member+event were dropped (empty shell; permanent loss on process-kill); eager separate `.set()`s were **refuted at the spec gate** (FlutterFire offloads each host call onto a shared `cachedThreadPool` with no serial TaskQueue → separate calls race into the native mutation queue, program order != enqueue order). Backward-compatible: the old client's chained writes still hit the standard `isGroupMember` path. Gate: fresh-context **codex** spec-review + **Opus** orthogonal adversary converged **P1-clean in 4 rounds** (R1 killed the eager-issue design; R2-3 refined the rules + eliminated the ceiling risk via duplication; R4 both clean); PR-diff gate **codex MERGE-CLEAR + Opus refuter CANNOT-REFUTE**. RED evidence in-PR: the 4-write founding batch is `PERMISSION_DENIED` on pre-#874 rules; the Dart mocktail regression shows `batch.set` 2→4 (member/event were behind a never-acking `commit()`). Rules suite 207/207 (5 new #874: founding-batch positive + 4 negatives — malformed-group-leg atomicity, `!exists` soft-delete-bypass, `existsAfter`-false standalone, event participant outside after-memberIds); full Flutter suite green; `flutter analyze` clean. **Rules-relevant change** — no function add/remove/logic change; `--force` re-asserts the 28 functions + indexes unchanged. Deployed from clean `main` @ `68cd3d89`. | prod-state PASS (all 13 checks: Firestore rules match `main`, all 28 functions present/updated in place — `Successful update operation` ×28, none created/deleted — indexes + hosting ×8 match `main`); tag advanced `abee70e8` → `68cd3d89` |
| 2026-07-04 | `c47bf943` | #892 (Closes #783): notification idempotency markers now expire. `fcmSender.claimDeliveryMarker` stamps `notificationDeliveries/{sha256(dedupeKey)}` with `createdAt` and a 90-day `expiresAt`, and `firestore.indexes.json` adds the TTL field override for `notificationDeliveries.expiresAt`; marker shape is otherwise unchanged, and repeated deliveries with the same key still send once. #891 (Closes #831): event-scoped settle-up writes a client `event_settlement` group-activity row; `firestore.rules` allow-lists that new client type while keeping Admin-only `expense_*` forged out. #882 (Closes #872): server oracle `allocateWeighted` now mirrors the client positive-weight remainder target so a 0-share/0-percent participant never absorbs rounding residue. #875 (Closes #825, Refs #885): pending gate intent cleanup; deploy-relevant backend surface is comment/test/docs only around `joinGroupByInviteCode` plus client token behavior, with no runtime rules/index/function-set change. Recorded deploy is the clean rerun from `main` @ `c47bf943` after the earlier post-deploy Hosting check hit a transient curl failure; the successful run completed deploy -> prod-state verify -> tag advance atomically. | prod-state PASS (Firestore rules + indexes match `main`, all 28 functions present; rerun skipped unchanged functions because the previous deploy attempt had already updated them, hosting checks passed on both domains); tag advanced `68cd3d89` -> `c47bf943`; `tool/pending_deploy.sh` exits clean |
| 2026-07-05 | `474da89b` | #893 (Closes #889): **machine-readable settlement-correction marker.** Settlements gain an Admin-only `correctionOfSettlementId` field (a soft link from a correcting row to the settlement it corrects), plus two new Admin-only callables — `correctSettlement` (one event/group settlement) and `correctLogicalSettleUp` (a whole `groupSettleUpId` cohort) — that write the offsetting correction rows and stamp the marker. `security/firestore.rules` (+20) validates the marker as absent-or-typed and rejects any client attempt to set it (Admin-only); `functions/src/callables/shared/settlementCorrection.ts` + `shared/ids.ts` hold the shared correction/ID engine, and the `settlementNotifier` / `writeRateMonitor` triggers learn the correction rows. **Oracle-invisible**: `recomputeNet` never reads the marker — it is a display/audit link only (same contract as `splitExplanation` / `groupSettleUpId`), so client↔server balance parity is untouched and no client change is gated on it. The correctable enum is **live-events-only** on purpose (soft-deleted event slices sit outside the balance universe); the `{EN,AR}` correction sentinel note is frozen and input notes are never validated against it. Spec-Gate: fresh-context dual-reviewer, **4 rounds, 0 P1**. The first deploy attempt this session hit the 2-minute client timeout mid-create (the functions still landed server-side); the **recorded deploy is the clean rerun from `main` @ `474da89b`** that completed deploy → prod-state verify → tag advance atomically. | prod-state PASS (all 13 checks: Firestore rules + indexes match `main`; `correctSettlement` + `correctLogicalSettleUp` **created**, all other functions updated in place — **30 functions** now, was 28; hosting ×8 across both `web.app` and `firebaseapp.com` domains); tag advanced `c47bf943` → `474da89b`; `tool/pending_deploy.sh` exits clean |

<!--
Seed row reconstructed from memory on 2026-06-07. e08b929 = PR #319 merge (the
#290 deploy on 2026-06-06). No backend file changed between e08b929 and the actual
deploy commit, so it is the correct last-deployed backend marker; the
`backend-deployed` tag is seeded here.

Known pending at seed time (surfaced by tool/pending_deploy.sh): #318 removeMember
(+ rules drop) and #294 member-doc keying — these land in the NEXT ledger row when
deployed.
-->
