
# Rihla Architecture

## 1. Overview

Rihla ("Journey") is an offline-first Flutter mobile app for group coordination and event planning. Users create persistent groups, spin up events inside those groups, and track shared finances across all of them.

```
┌─────────────────────────────────────────────────────────┐
│                     Flutter UI Layer                    │
│   Screens (ConsumerWidget / ConsumerStatefulWidget)     │
│   shared/widgets/ — reusable components                 │
└───────────────────────────┬─────────────────────────────┘
                            │ ref.watch / ref.read
┌───────────────────────────▼─────────────────────────────┐
│               Riverpod Provider Layer                   │
│   StreamProvider.family — Firestore real-time streams   │
│   Provider.family — derived/computed state              │
│   StateNotifierProvider — mutable state (settings)      │
└────────────┬──────────────────────────┬─────────────────┘
             │                          │
┌────────────▼────────────┐  ┌──────────▼─────────────────┐
│    Firebase Backend     │  │     SQLite (Local Cache)    │
│  Firestore (primary)    │  │  LocalDatabase (sqflite)    │
│  Firebase Auth (anon)   │  │  BalanceCacheRepository     │
│  Firebase Storage       │  │  CacheService               │
│  Firebase Messaging     │  └────────────────────────────┘
└─────────────────────────┘
```

### Key Design Decisions

- **Firebase as primary data store** — Firestore with `persistenceEnabled: true` handles offline writes automatically. The SDK replays pending mutations when connectivity returns; no manual sync queue is needed.
- **SQLite as a secondary read cache** — Used specifically by `BalanceCalculator` and legacy providers. Written as a side effect of Firestore stream `asyncMap` pipelines; never the source of truth.
- **No login screen** — Firebase anonymous auth is established silently on first launch via `FirebaseConfig.ensureAnonymousSession()`. All Firestore security rules work against the anonymous UID.
- **No shared repository abstraction** — Services call Firestore directly. `FirestoreRepository` is a thin base class providing the typed `eventSubcollection()` helper and a test-injectable constructor, not a full repository pattern.
- **Feature-first code organisation** — Each feature is self-contained. No shared service layer.
- **Immutable models** — All domain models (`Group`, `Event`, `Expense`, etc.) are immutable value types with `copyWith`.

---

## 2. Feature-First Structure

```
lib/
├── main.dart                     # Bootstrap: Firebase → Auth → SharedPrefs → runApp
├── firebase_options.dart         # Auto-generated by flutterfire configure
├── core/
│   ├── config/                   # FirebaseConfig (init, auth session, Firestore accessors)
│   ├── models/                   # Cross-feature models (AppSettings)
│   ├── providers/                # App-wide Riverpod providers
│   ├── router/                   # GoRouter definition (AppRoutes, routerProvider)
│   ├── services/                 # Shared infrastructure services
│   ├── theme/                    # AppTheme + ThemeExtension token classes
│   ├── types/                    # Shared typedefs (EventRef)
│   └── utils/                    # Utility helpers
├── features/
│   ├── activity/                 # Event-level activity feed
│   ├── auth/                     # Firebase auth stream providers
│   ├── events/                   # Event lifecycle (create, list, detail)
│   ├── groups/                   # Persistent groups (create, join, detail)
│   ├── home/                     # Home screen + dashboard providers
│   ├── ledger/                   # Expenses, settlements, balance calculator
│   ├── settings/                 # User profile, theme, notifications
│   └── trip/                     # Legacy trip model (retained for SQLite schema)
└── shared/
    ├── animations/               # Shared animation helpers
    └── widgets/                  # Reusable UI components
```

Each feature under `lib/features/` is independently structured:

| Subdirectory  | Contents                                                        |
|---------------|-----------------------------------------------------------------|
| `models/`     | Immutable data classes with `fromDoc` (Firestore) / `fromMap` (SQLite) / `copyWith` |
| `providers/`  | Riverpod providers and StateNotifiers for that feature          |
| `screens/`    | Screen widgets (`ConsumerWidget` or `ConsumerStatefulWidget`)   |
| `services/`   | Firestore CRUD classes extending `FirestoreRepository`          |
| `widgets/`    | Feature-specific UI components                                  |
| `keys/`       | `ValueKey` / `Key` constants for widget tests                   |

---

## 3. State Management

The app uses **Riverpod 2.x** with manually defined providers (no code generation).

### Provider Patterns

```dart
// 1. StreamProvider.family — Firestore real-time subscriptions
//    Family parameter is EventRef (a Dart record typedef)
final eventExpensesProvider =
    StreamProvider.family<List<Expense>, EventRef>((ref, eventRef) {
  return service.watchExpenses(eventRef.groupId, eventRef.eventId)
    .asyncMap((expenses) async {
      await cache.cacheExpenses(eventRef.eventId, expenses); // SQLite side-write
      return expenses;
    });
});

// 2. Provider.family — derived state computed from multiple streams
final eventBalancesProvider =
    Provider.family<AsyncValue<List<UserBalance>>, ({EventRef, Event})>(...);

// 3. StateNotifierProvider — mutable settings state
final settingsProvider =
    StateNotifierProvider<SettingsNotifier, AppSettings>(...);

// 4. StateProvider — simple boolean/string flags
final expenseLoadingProvider = StateProvider<bool>((ref) => false);
```

### Key Providers

| Provider | Type | Purpose |
|----------|------|---------|
| `routerProvider` | `Provider<GoRouter>` | GoRouter instance with redirect logic |
| `authStateProvider` | `StreamProvider<User?>` | Firebase auth state stream |
| `connectivityProvider` | `StateNotifierProvider<ConnectivityNotifier, ConnectivityStatus>` | Online/offline status |
| `settingsProvider` | `StateNotifierProvider<SettingsNotifier, AppSettings>` | User settings + device name |
| `groupEventsProvider` | `StreamProvider.family<List<Event>, String>` | All events in a group |
| `eventDetailProvider` | `StreamProvider.family<Event?, ({groupId, eventId})>` | Single event stream |
| `eventExpensesProvider` | `StreamProvider.family<List<Expense>, EventRef>` | Event expenses with SQLite side-write |
| `eventSettlementsProvider` | `StreamProvider.family<List<Settlement>, EventRef>` | Event settlements with SQLite side-write |
| `eventBalancesProvider` | `Provider.family` | Computed balances via `BalanceCalculator` |
| `eventUnifiedLedgerProvider` | `Provider.family<AsyncValue<List<Transaction>>, EventRef>` | Merged expenses + settlements |
| `sharedPreferencesProvider` | `Provider<SharedPreferences>` | Injected via `ProviderScope.overrides` in `main()` |
| `appBootstrapProvider` | `Provider<void>` | Push notification lifecycle sync |

### `EventRef` — Canonical Event Key

All Firestore-backed event providers use a Dart record typedef as the family parameter:

```dart
typedef EventRef = ({String groupId, String eventId});
```

This enables deep-link-safe data fetching: screens receive raw strings from GoRouter path parameters and construct an `EventRef` record inline.

---

## 4. Data Flow

### Firestore → UI (Happy Path)

```
Firestore snapshot
  │
  ▼ (GroupService / ExpenseService / etc.)
StreamProvider.family.asyncMap
  │ (side-write to SQLite via BalanceCacheRepository)
  ▼
Riverpod provider graph
  │ (ref.watch)
  ▼
Screen widget (ConsumerWidget)
  │ (provider.when / provider.valueOrNull)
  ▼
UI renders loaded / loading / error state
```

### Write Path

```
User action in UI
  │
  ▼ (ref.read(service).addExpense(...))
FirestoreRepository subclass (ExpenseService / GroupService / etc.)
  │ (WriteBatch or single doc.set)
  ▼
Firestore (persists locally immediately, uploads when online)
  │
  ▼ (Firestore snapshot fires on local write)
StreamProvider re-emits → UI re-renders
```

### Offline Write Behaviour

Firestore's offline persistence (`persistenceEnabled: true`, `cacheSizeBytes: CACHE_SIZE_UNLIMITED`) stores pending mutations locally. When connectivity is restored, the SDK replays them automatically — no manual sync queue is maintained in this codebase.

### Connectivity Check

`ConnectivityNotifier` pings Firestore every 60 seconds with `Source.server` against the `inviteCodes` collection (publicly readable). State transitions between `online`, `offline`, and `syncing`. The `OfflineBanner` widget subscribes to `connectivityProvider`.

---

## 5. Navigation

Routing is handled entirely by **GoRouter 13.x** with declarative routes. No `Navigator.push` anywhere in production code.

### Route Tree

```
/ (splash — auto-redirect)
├── /home                  (FadeTransition)
├── /profile               (FadeTransition)
├── /activity              (FadeTransition — CrossGroupActivityScreen)
├── /create-group          (slide-up)
├── /join-group            (slide-up)
└── /group/:gid            (slide-right — GroupDetailScreen)
    ├── /settings          (slide-right — GroupSettingsScreen)
    ├── /settle-up         (slide-right — GroupSettleUpScreen, ?memberId query param)
    ├── /activity          (slide-right — GroupActivityScreen)
    ├── /create-event      (slide-right — EventTypePickerScreen)
    ├── /create-event/:type (slide-right — CreateEventScreen)
    └── /event/:eid        (slide-right — EventCommandCenter)
        ├── /ledger        (slide-right — LedgerScreen)
        │   ├── /add       (slide-right — AddExpenseScreen)
        │   ├── /edit/:expId (slide-right — EditExpenseScreen)
        │   └── /settle-up (slide-right — SettleUpScreen)
        ├── /activity      (slide-right — EventActivityScreen placeholder)
        └── /settings      (slide-right — EventSettingsScreen)
```

### Transition Patterns

| Route | Transition |
|-------|-----------|
| `/home`, `/profile`, `/activity` | `FadeTransition` |
| `/create-group`, `/join-group` | Slide-up (`Offset(0,1)` → zero) |
| All other routes | Slide-right (`Offset(1,0)` → zero, `Curves.easeOutCubic`) |

The shared `_slideRightTransition` function in `app_router.dart` is used by all module-level routes to ensure consistent navigation feel.

### Splash Redirect

The splash route redirects directly to `/home`. Anonymous Firebase auth is handled before `SafarApp` renders by `_AuthGate` in `main.dart`.

### Type-Safe Route Names

All route path strings are defined as constants on `AppRoutes`:

```dart
class AppRoutes {
  static const String groupDetail = '/group/:gid';
  static const String eventHub = '/group/:gid/event/:eid';
  // ...
}
```

---

## 6. Offline Architecture

### SQLite (`LocalDatabase`)

File: `lib/core/services/local_database.dart`  
Database: `safar_cache.db`, schema version 6

Tables:

| Table | Purpose |
|-------|---------|
| `trips` | Legacy trip cache (event data pre-Firestore migration) |
| `expenses` | Expense cache — keyed by `trip_id` (= Firestore event ID) |
| `settlements` | Settlement cache |
| `gear_items` | Legacy gear item cache retained for SQLite compatibility |
| `participants` | Participant cache |
| `sub_groups` / `sub_group_members` | Sub-group membership cache |
| `activity_logs` | Activity log cache |
| `categories` | Expense category cache |
| `groups` | Group cache (added in v6) |
| `group_members` | Group member cache (added in v6) |
| `group_ledger` | Group-level balance cache (added in v6) |

All `expenses`, legacy `gear_items`, and `settlements` rows include `is_deleted` and `deleted_at` for soft-delete support. `LocalDatabase` handles versioned migrations via `_onUpgrade`.

### `BalanceCacheRepository`

File: `lib/core/services/balance_cache_repository.dart`

A narrow SQLite wrapper that bridges Firestore streams to `BalanceCalculator`. It does not extend `FirestoreRepository`.

- `cacheExpenses(eventId, expenses)` — called inside `eventExpensesProvider.asyncMap`; replaces the full expense list for the event
- `cacheSettlements(eventId, settlements)` — called inside `eventSettlementsProvider.asyncMap`
- `getExpenses(eventId)` / `getSettlements(eventId)` — synchronous reads for calculator
- `watchExpenses` / `watchSettlements` — deprecated stream interface for legacy providers

### `CacheService`

File: `lib/core/services/cache_service.dart`

Static methods for batch reading and writing domain objects to SQLite. Handles `Trip`, `Expense`, `Settlement`, `Participant`, `SubGroup`, `Group`, `GroupMember`, and more. Used by legacy trip-scoped providers.

### `ConnectivityNotifier`

File: `lib/core/providers/connectivity_provider.dart`

Checks connectivity by firing a server-sourced Firestore read every 60 seconds. Exposes `ConnectivityStatus.online / offline / syncing` via `StateNotifierProvider`. The `OfflineBanner` widget watches this provider.

### Firestore Offline Persistence

Configured in `FirebaseConfig.initialize()`:

```dart
FirebaseFirestore.instance.settings = const Settings(
  persistenceEnabled: true,
  cacheSizeBytes: Settings.CACHE_SIZE_UNLIMITED,
);
```

This must be called before any Firestore read/write. All pending writes are replayed by the SDK on reconnection.

---

## 7. Design System

### Token Architecture

Three `ThemeExtension` classes are registered in `AppTheme.lightTheme.extensions`:

| Class | File | Contents |
|-------|------|----------|
| `AppColorTokens` | `lib/core/theme/tokens/color_tokens.dart` | 38 typed `Color` fields + 2 computed `LinearGradient` getters |
| `AppSpacingTokens` | `lib/core/theme/tokens/spacing_tokens.dart` | 7 spacing values (4–32dp), 3 border radii, button height |
| `AppShadowTokens` | `lib/core/theme/tokens/shadow_tokens.dart` | 3 elevation levels (`flat`, `raised`, `floating`) |

All token classes implement `ThemeExtension<T>` with `copyWith` and `lerp`. Tokens are accessed from any widget via:

```dart
final colors = Theme.of(context).extension<AppColorTokens>()!;
```

### Color Palette (Light)

| Token | Value | Use |
|-------|-------|-----|
| `primary` | `#0D7B74` | Buttons, FABs, links, focused inputs |
| `primaryDark` | `#0A6B65` | CTA gradient end |
| `scaffoldBackground` | `#FFFFFF` | Page background |
| `cardSurface` | `#F8F9FA` | Card backgrounds |
| `textPrimary` | `#111827` | Body text (17.15:1 on white — AAA) |
| `textSecondary` | `#6B7280` | Secondary labels (5.74:1 — AA) |
| `textMuted` | `#9CA3AF` | Decorative only — below AA, never functional text |
| `moduleLedger` | `#0D7B74` | Ledger module accent |
| Neutral module accent | `#6B7280` | Non-ledger module accents |

### Typography

Font family: **Plus Jakarta Sans** (loaded via `google_fonts`). Weight hierarchy: 800 display → 700 headline → 600 title/label → 400 body.

### Shared Widgets (`lib/shared/widgets/`)

| Widget | Purpose |
|--------|---------|
| `ModuleHeader` | Dark gradient header used across all module screens |
| `AppTabBar` | Tab bar with teal gradient pill indicator |
| `OfflineBanner` | Amber connectivity indicator, watches `connectivityProvider` |
| `EmptyStateView` | Consistent empty states with optional CTA |
| `SearchFilterBar` | Expandable search with filter chip row |
| `SmartModuleCard` | Module cards on `EventCommandCenter` |
| `LoadingButton` | Button with loading spinner state |
| `SkeletonLoader` / `SkeletonPrimitives` | Shimmer skeleton loading states |
| `AnimatedCurrencyText` | Currency display with value-change animation |
| `GrainOverlay` | Texture layer (grain.png asset) |
| `InitialsCircle` | Avatar fallback from display name initials |
| `DotStepIndicator` | Step/page indicator for multi-step flows |

---

## 8. Financial Calculations

### Money Representation

All monetary values use the **`decimal` package** (`Decimal` type). Floating-point arithmetic is never used for money.

At the Firestore boundary, `MoneySerializer` converts between `Decimal` and integer subunits:

```
OMR 10.500  →  10500 (fils, scale 1000)
USD 9.99    →  999   (cents, scale 100)
JPY 100     →  100   (units, scale 1)
```

Supported currencies: OMR, USD, EUR, GBP, SAR, AED, JPY, KWD, BHD, QAR.

Primary currency is **OMR** (Omani Rial, 3 decimal places).

### `BalanceCalculator`

Defined in `lib/features/ledger/providers/expense_provider.dart`.

**`calculateBalances()`** processes four expense scopes:

| Scope | Split logic |
|-------|-------------|
| `global` | Split evenly among all event participants |
| `subGroup` | Split among members of the specified `SubGroup` |
| `personal` | No split — payer is solely responsible |
| `custom` | Split among a specified subset of `customSplitParticipants` |

Per-head cost uses 3-decimal-place fixed precision:

```dart
final perHead = (expense.amount / Decimal.fromInt(splitCount))
    .toDecimal(scaleOnInfinitePrecision: 3);
```

Settlement adjustments are applied after the split phase. The result is a `List<UserBalance>` with `totalPaid`, `totalOwed`, and `netBalance` per participant.

### Settlement Optimisation

**`calculateOptimalSettlements()`** uses a greedy minimum-transactions algorithm:

1. Separate participants into debtors (`netBalance < 0`) and creditors (`netBalance > 0`)
2. Sort debtors ascending by balance; creditors descending
3. Pair the largest debtor with the largest creditor; settle the minimum of the two
4. Advance the pointer of whichever side reaches zero
5. Repeat until all balances are resolved

Result: minimal number of transfers to clear all debts.

### Group-Level Balances

The `group_ledger` SQLite table accumulates `net_amount_subunits` per `(group_id, member_id, counterparty_id)` pair, tracking balances across all events in a group. `GroupBalanceProvider` (in `lib/features/groups/providers/group_balance_provider.dart`) computes these from Firestore-backed settlement streams.

### Payments

Payment processing is out of scope for the shippable v1 surface. Ledger settlement records track who owes whom; the app does not initiate real payment collection.

---

## 9. Key Dependencies

| Package | Version | Role |
|---------|---------|------|
| `flutter_riverpod` | `^2.4.9` | State management — providers, notifiers |
| `go_router` | `^13.2.0` | Declarative routing, deep links |
| `cloud_firestore` | `^6.2.0` | Primary data store with offline persistence |
| `firebase_auth` | `^6.3.0` | Anonymous authentication |
| `firebase_app_check` | `^0.4.3` | App Check tokens for callable/backend protection |
| `firebase_messaging` | `^16.1.3` | Push notifications (FCM) |
| `firebase_core` | `^4.6.0` | Firebase SDK initialisation |
| `app_links` | `^7.0.0` | Deep-link and email-link handling |
| `sqflite` | `^2.4.2` | Local SQLite cache (`safar_cache.db`) |
| `decimal` | `^3.2.4` | Precise money arithmetic — no floating point |
| `google_fonts` | `^8.0.2` | Plus Jakarta Sans typeface |
| `flutter_animate` | `^4.5.0` | Micro-interaction animations |
| `animations` | `^2.0.0` | Material 3 page transitions |
| `shimmer` | `^3.0.0` | Skeleton loading shimmer effect |
| `sentry_flutter` | `^9.0.0` | Error monitoring and performance tracing |
| `shared_preferences` | `^2.5.4` | Settings persistence (device name, theme, etc.) |
| `qr_flutter` | `^4.1.0` | Invite-code QR rendering |
| `url_launcher` | `^6.3.2` | External links and settings/help actions |
| `pdf` / `csv` | `^3.11.1` / `^6.0.0` | Ledger export |
| `share_plus` | `^10.1.4` | Native share sheet |
| `timeago` | `^3.6.1` | Relative timestamps |
| `uuid` | `^4.3.3` | UUID generation for Firestore document IDs |
| `intl` | `^0.20.2` | Date/number formatting, localisation |
| `mocktail` | `^1.0.4` | Test mocking |
| `fake_cloud_firestore` | `^4.1.0+1` | Firestore test injection |
| `sqflite_common_ffi` | `^2.3.4` | SQLite in-memory testing |
