# Rihla Dead-Code Audit — 2026-05-29

> Generated by a 13-finder reference-graph sweep + adversarial per-finding verification
> (172 agents). `flutter analyze` is already clean, so **none** of this is analyzer-visible —
> it is all unreferenced public symbols, files imported by nothing, features wired out of the
> UI, unused providers/deps/assets. Read-only audit; **removes nothing**.
>
> **Independent spot-check (publisher, not the workflow):**
> - 6 dependencies re-grepped → all 0 refs in `lib/test/integration_test` (`pdf`, `csv`, `http/`, `animations`, `path_provider`, `CupertinoIcons`).
> - `assets/app_icon.svg` exists, not declared in `pubspec.yaml` → orphaned.
> - The 3 "non-zero importer" counts are README mentions only (+ the dead `ledger_provider`→`transaction_model` self-import). Dead chains confirmed.
> - **CLAUDE.md bear trap confirmed:** `appRouteRedirect` (`app_router.dart:67-73`) only redirects splash→home; the code comment says onboarding is *intentionally kept out of the route tree*. The CLAUDE.md Key Invariant "router hard-redirects all non-onboarding routes until complete" is **stale** — see PR-16.

## 1. Executive Summary

All findings are confirmed-dead, each independently re-verified by an adversarial skeptic against current code (not docs/memory). 22 candidates were rejected as false positives (framework callbacks, `.family` providers, route targets, JSON serialization, live children).

**Counts by kind** (deduped — many symbols appear under both `unused-public-symbol` and `unused-file`):

| Kind | Findings |
|---|---|
| unused-file | 38 |
| unused-public-symbol | 44 |
| unused-provider | 13 |
| dead-feature | 17 |
| unused-dependency | 6 |
| could-be-private (DEAD subset) | 4 |
| unused-asset | 1 |

**Net removable surface (deduped to physical artifacts):**
- **~40 source files** deletable outright (whole-file dead).
- **~20 in-file symbols** removable without deleting the host file (siblings live).
- **6 pubspec dependencies** (`pdf`, `csv`, `path_provider`, `http`, `animations`, `cupertino_icons`).
- **1 asset** (`assets/app_icon.svg`).
- **0 Cloud Functions** dead in this batch (the dead receipts/OCR feature is client-only; `joinGroupByInviteCode` and the deletion-cascade callables are live).

**Risk profile:** Overwhelmingly low. ~70% `trivial`, ~28% `low`, 1 `medium` (`DailySpending` — must be removed as a provider+widget+typedef cluster), 0 high. **No money-math, no `firestore.rules`, no live route, no schema field is touched.** `BalanceCalculator` / `MoneySerializer` / settlement math are entirely untouched.

---

## 2. Dead FEATURES (highest value — whole subsystems)

### F1. Receipts / OCR (dead end-to-end)
ML Kit removed; receipts are upload-only and never wired. Picker, OCR stub, and l10n keys all orphaned.
- `lib/features/ledger/services/ocr_service.dart` (whole file: `OcrService` + `OcrResult` + `OcrStatus`) — 0 importers.
- `lib/features/ledger/widgets/receipt_picker_section.dart` — 0 importers; `ExpenseEditorBody` renders no receipt UI.
- **Order:** delete both files (independent). **Follow-up (separate PR):** prune orphaned `editorReceipt*` ARB keys. Do **not** touch `ExpenseModel.receiptUrl/receiptPath` (serialization fields).

### F2. Onboarding screen (archived, out of route tree)
- `lib/features/onboarding/screens/onboarding_screen.dart` (`OnboardingScreen`) — not in `app_router.dart`; router comment + `settings_provider.dart` call it "archived." Test-only.
- **Order:** delete file + `test/features/onboarding/onboarding_screen_test.dart`. **Follow-up:** assess write-only `onboardingComplete` plumbing; **fix CLAUDE.md** — its "router hard-redirects until onboarding complete" invariant is stale (verified: `appRouteRedirect` is splash→home only).

### F3. Legacy `Transaction` ledger chain (orphaned post-EventRef migration)
Self-contained dead chain; live UI uses `eventExpensesProvider`+`eventSettlementsProvider` directly.
- `lib/features/ledger/providers/ledger_provider.dart` (`eventUnifiedLedgerProvider`) — 0 consumers.
- `lib/features/ledger/models/transaction_model.dart` (`Transaction`, `TransactionType`, `Transaction.actorId`) — imported only by the dead provider.
- **Order:** delete both files together (model alone breaks the provider's import).

### F4. `WeeklySpendingCard` home cluster (orphaned by Saffron home rewrite, `abd2409`)
- `lib/features/home/widgets/weekly_spending_card.dart` (`WeeklySpendingCard`).
- `lib/features/home/providers/dashboard_providers.dart`: `weeklyGroupSpendingProvider` (L182), `weeklyGroupExpensesProvider` (L101), `DailySpending` typedef (L85), `_combineExpenseStreams`/`_emptyWeek` helpers.
- `HomeKeys.weeklySpendingCard`.
- **Order:** card → `weeklyGroupSpendingProvider` → `weeklyGroupExpensesProvider` → key → tests. **KEEP** `crossGroupActivityProvider`/`CrossGroupActivityEntry` in the same file — live.

### F5. `QuickActionTray` home widget (same `abd2409` rewrite)
- `lib/features/home/widgets/quick_action_tray.dart` + 5 orphaned `HomeKeys` (`quickActionTray`, `addExpenseAction`, `settleUpAction`, `inviteAction`, `activityAction`).
- **Order:** delete file → keys → test group.

### F6. Trip back-compat + superseded participant providers
- `lib/features/trip/models/trip_model.dart`: remove **only** `Trip` + `TripModules` (test-only back-compat shim). **KEEP** `Participant`/`ParticipantRole` — imported by 6 live files. Prefer splitting those into their own file.
- `lib/features/trip/providers/trip_provider.dart`: `currentParticipantProvider` + its sole dependency `userGroupsForParticipantProvider`. **KEEP** `currentEventParticipantProvider` (live successor).

### F7. Deprecated activity-service shims
- `lib/features/activity/services/activity_service.dart`: `eventTransactionActivityProvider`, `tripActivityProvider`, `tripTransactionActivityProvider` (legacy `Stream.value([])` shims, `@Deprecated`). **KEEP** `eventActivityProvider`/`watchActivityLogs`.

### F8. Abandoned 3-step Add-Expense flow (replaced by inline `ExpenseEditorBody`)
- `lib/features/ledger/widgets/amount_input_section.dart` (`AmountInputSection`) — test-only.
- `lib/features/ledger/widgets/category_selection_step.dart` (`CategorySelectionStep`) — test-only.
- `lib/features/ledger/widgets/category_picker_sheet.dart` (`showCategoryPickerSheet`/`CategoryPickerSheet`) — 0 importers.
- **Order:** independent file deletes; the first two share `test/features/ledger/expense_entry_widgets_test.dart`.

### F9. `settle_up_screen` split orphans (`dee03e3`; superseded by `settle_up_page_body.dart`)
All under `lib/features/ledger/widgets/`: `recent_expenses_section.dart`, `recorded_settlements_section.dart`, `settlement_summary_card.dart`, `settlement_row.dart`, `settlement_tile.dart` (`SettlementTile`+`SettlementGroupCard`), `expense_card.dart`.

### F10. Phase-39 module/dashboard remnants & superseded group/profile widgets
- Module cards / shared: `lib/shared/widgets/smart_module_card.dart`, `app_tab_bar.dart`, `search_filter_bar.dart`.
- Group duplicates: `group_member_tile.dart`, `group_spending_stats.dart`, `group_stats_grid.dart`, `group_activity_tile.dart`, `group_balance_hero.dart`, `group_member_balance_card.dart`.
- Profile sections (built, never wired): `profile_stats_section.dart`, `profile_about_section.dart`, `profile_notifications_section.dart`, `profile_support_section.dart`.
- Activity hero cards: `activity_entry_card.dart`, `activity_hero_card.dart`.

### F11. Animations barrel + dead members
- `lib/shared/animations/animations.dart` (barrel, 0 importers — consumers import `tap_bounce.dart` directly).
- `lib/shared/animations/fade_in_list.dart` (`FadeInList`), `staggered_grid.dart` (`StaggeredGrid`) — test-only. **KEEP** `tap_bounce.dart`.

> `EventCommandCenter` is **dead-but-kept** per CLAUDE.md — not removed here. Its orphaned `EventKeys` constants (§4) are safe to strip.

---

## 3. Unused FILES (safe deletes) — sorted by risk ascending

| Path | What it is | Risk | Evidence |
|---|---|---|---|
| `lib/features/ledger/services/ocr_service.dart` | OCR stub (dead feature) | trivial | 0 importers; symbols self-contained |
| `lib/features/ledger/widgets/expense_card.dart` | superseded by `_ExpenseRow` | trivial | 0 importers; `LedgerKeys.expenseCard` keys `_ExpenseRow`, not this |
| `lib/features/ledger/widgets/settlement_summary_card.dart` | superseded by `GroupSettlementSummaryCard` | trivial | 0 importers; distinct class |
| `lib/features/ledger/widgets/settlement_row.dart` | superseded by `LedgerSettleRow` | trivial | 0 importers; 3 plan docs flag orphan |
| `lib/features/ledger/widgets/settlement_tile.dart` | self-contained island | trivial | 0 importers; distinct from `GroupSettlementTile` |
| `lib/features/ledger/widgets/receipt_picker_section.dart` | dead receipts UI | trivial | 0 importers; editor renders no receipt |
| `lib/features/ledger/widgets/recent_expenses_section.dart` | orphan (distinct from private `_RecentExpensesSection`) | trivial | 0 importers |
| `lib/features/ledger/widgets/recorded_settlements_section.dart` | orphan; live path uses `_PaymentHistorySection` | trivial | 0 importers |
| `lib/features/ledger/widgets/category_picker_sheet.dart` | superseded by inline `_CategoryStrip` | trivial | 0 importers (incl. `showCategoryPickerSheet`) |
| `lib/features/ledger/models/transaction_model.dart` | dead `Transaction` model | trivial | imported only by dead `ledger_provider.dart` |
| `lib/features/ledger/providers/ledger_provider.dart` | orphaned `eventUnifiedLedgerProvider` | trivial | 0 consumers |
| `lib/features/groups/widgets/group_member_tile.dart` | superseded by inline `_buildMemberTile` | trivial | 0 importers; key string is unrelated |
| `lib/features/groups/widgets/group_spending_stats.dart` | removed in `3ef75ec` | trivial | 0 importers |
| `lib/features/groups/widgets/group_activity_tile.dart` | superseded by inline `_ActivityRow` | trivial | test-only; 0 lib refs |
| `lib/features/activity/widgets/activity_entry_card.dart` | superseded by `_ActivityRow` | trivial | 0 importers |
| `lib/features/activity/widgets/activity_hero_card.dart` | never wired | trivial | 0 importers |
| `lib/shared/widgets/route_mark.dart` | brand mark; icon tool hand-dups geometry | trivial | 0 importers |
| `lib/shared/animations/animations.dart` | unused barrel | trivial | 0 importers |
| `lib/shared/widgets/smart_module_card.dart` | Phase-39 module card | trivial | test-only; 0 lib refs |
| `lib/features/settings/widgets/profile_stats_section.dart` | superseded by `_StatsGrid` | trivial | 0 importers |
| `lib/features/settings/widgets/profile_about_section.dart` | superseded by `_AboutCard` | trivial | 0 importers |
| `lib/features/settings/widgets/profile_notifications_section.dart` | superseded by `_NotificationPrefRow` | trivial | 0 importers; l10n self-labels "dead" |
| `lib/features/ledger/widgets/category_selection_step.dart` | abandoned step flow | trivial | test-only; 0 lib refs |
| `lib/features/events/widgets/event_card.dart` | superseded by `_EventRow` (`abd2409`) | low | test-only (2 files) |
| `lib/features/home/widgets/quick_action_tray.dart` | orphaned by `abd2409` | low | test-only |
| `lib/features/home/widgets/weekly_spending_card.dart` | orphaned by `abd2409` | low | test-only; slated for delete in saffron-overhaul.md |
| `lib/features/groups/widgets/group_stats_grid.dart` | old layout | low | test-only; regression test asserts absent |
| `lib/features/groups/widgets/group_balance_hero.dart` | removed in `3ef75ec` | low | test-only |
| `lib/features/groups/widgets/group_member_balance_card.dart` | never wired | low | test-only |
| `lib/features/settings/widgets/profile_support_section.dart` | un-wired Phase-26 redesign | low | comment-only test ref |
| `lib/shared/animations/fade_in_list.dart` | ripped out; regression-guarded | low | test-only; `findsNothing` guard |
| `lib/shared/animations/staggered_grid.dart` | never used | low | test-only |
| `lib/shared/widgets/app_tab_bar.dart` | Phase-39 (Gear tabs) | low | test-only |
| `lib/shared/widgets/dot_step_indicator.dart` | stale doc; onboarding uses private dots | low | test-only |
| `lib/shared/widgets/search_filter_bar.dart` | Phase-39 (Gear/Vault) | low | test-only |
| `lib/shared/widgets/animated_currency_text.dart` | superseded by `RAmount` | low | test-only; deferred-delete in saffron-overhaul.md |
| `lib/features/ledger/widgets/amount_input_section.dart` | abandoned step flow | low | test-only |
| `lib/features/onboarding/screens/onboarding_screen.dart` | archived | low | test-only; not in router |

---

## 4. Unused PUBLIC SYMBOLS (in files that must stay) — sorted by risk ascending

| Path:line | Symbol | Kind | Risk |
|---|---|---|---|
| `lib/core/utils/name_validators.dart:10` | `kDisplayNameMinLength` | unused const | trivial |
| `lib/core/utils/localized_dates.dart:41` | `formatTimelineDayLabel` | unused fn | trivial |
| `lib/core/services/haptic_service.dart:19` | `HapticService.warning` | unused method | trivial |
| `lib/core/theme/app_theme.dart:20` | `AppTheme.fontFamily` | unused const (self-labeled back-compat) | trivial |
| `lib/features/ledger/providers/expense_provider.dart:25` | `expenseErrorProvider` | unused provider | trivial |
| `lib/features/ledger/providers/category_provider.dart:6` | `categoryLoadingProvider` | unused provider | trivial |
| `lib/features/ledger/providers/category_provider.dart:9` | `categoryErrorProvider` | unused provider | trivial |
| `lib/features/ledger/models/transaction_model.dart:58` | `Transaction.actorId` | unused getter | trivial |
| `lib/features/events/keys/event_keys.dart` (13 members) | `eventExpenseHeroScreen`, `activityCard`, `addExpenseFab`, `moduleList`, `moduleGrid`, `expenseHeroTotal`, `eventType{Trip,Camping,DayOut,Dinner,Custom}Option`, `moduleLedgerToggle`, `eventCard` | dead key consts | trivial |
| `lib/features/events/keys/event_keys.dart:24` | `EventKeys.modulesSection` | dead key const | low |
| `lib/features/trip/models/trip_model.dart:2` | `Trip` | back-compat shim | low |
| `lib/features/trip/models/trip_model.dart:145` | `TripModules` | back-compat shim | low |
| `lib/features/events/services/event_service.dart:160` | `EventService.addParticipant` | unused method | low |
| `lib/features/events/services/event_service.dart:191` | `EventService.removeParticipant` | unused method | low |
| `lib/features/events/models/event_model.dart:224` | `Event.isOngoing` | unused getter | low |
| `lib/features/auth/providers/auth_provider.dart:71` | `AuthService` | unused class | trivial |
| `lib/features/ledger/providers/category_provider.dart:81` | `CategoryService` | unused class (no-op stub) | low |
| `lib/features/activity/utils/activity_display.dart:47` | `localizedGroupActivityTitle` | unused fn | trivial |
| `lib/features/home/providers/dashboard_providers.dart:85` | `DailySpending` | dead typedef (cluster) | medium |
| `lib/shared/widgets/loading_button.dart:65` | `GlassCard` | unused widget (sibling of live `LoadingButton`) | low |
| `lib/shared/widgets/loading_button.dart:92` | `GradientContainer` | unused widget | trivial |
| `lib/shared/widgets/loading_button.dart:127` | `ShimmerPlaceholder` | unused widget | low |
| `lib/core/services/notification_service.dart:154` | `NotificationService.isInitialized` | unused getter | trivial |

> `loading_button.dart` keeps `LoadingButton` (live in `join_group_screen`, `create_event_screen`); only the three siblings are dead.

---

## 5. Unused PROVIDERS / DEPENDENCIES / ASSETS / CLOUD FUNCTIONS

### 5a. Providers (production-dead)
| Path:line | Symbol | Risk | Evidence |
|---|---|---|---|
| `lib/features/ledger/providers/expense_provider.dart:25` | `expenseErrorProvider` | trivial | 1 hit (decl only); twin `expenseLoadingProvider` is live |
| `lib/features/ledger/providers/category_provider.dart:6` | `categoryLoadingProvider` | trivial | test-only |
| `lib/features/ledger/providers/category_provider.dart:9` | `categoryErrorProvider` | trivial | test-only |
| `lib/features/ledger/providers/category_provider.dart:72` | `categoryServiceProvider` | low | test-only; paired with `CategoryService` |
| `lib/features/ledger/providers/ledger_provider.dart:9` | `eventUnifiedLedgerProvider` | trivial | 0 consumers |
| `lib/features/home/providers/dashboard_providers.dart:182` | `weeklyGroupSpendingProvider` | low | sole consumer is dead `WeeklySpendingCard` |
| `lib/features/home/providers/dashboard_providers.dart:101` | `weeklyGroupExpensesProvider` | low | chain terminates at dead card |
| `lib/features/trip/providers/trip_provider.dart:50` | `currentParticipantProvider` | low | 0 consumers; superseded |
| `lib/features/trip/providers/trip_provider.dart:12` | `userGroupsForParticipantProvider` | low | only consumer is dead `currentParticipantProvider` |
| `lib/features/auth/providers/auth_provider.dart:66` | `authServiceProvider` | trivial | test-only; `FirebaseConfig` covers equivalents |
| `lib/features/activity/services/activity_service.dart:27` | `eventTransactionActivityProvider` | low | test-only |
| `lib/features/activity/services/activity_service.dart:38` | `tripActivityProvider` | trivial | test-only `@Deprecated` shim |
| `lib/features/activity/services/activity_service.dart:48` | `tripTransactionActivityProvider` | trivial | test-only `@Deprecated` shim |

### 5b. Dependencies (`pubspec.yaml`) — sorted by risk ascending
| Line | Package | Risk | Evidence |
|---|---|---|---|
| 51 | `cupertino_icons` | trivial | 0 `CupertinoIcons` refs repo-wide; `direct main` in lock |
| 61 | `pdf` | trivial | 0 `package:pdf` refs; export feature removed |
| 62 | `csv` | trivial | 0 `package:csv` refs |
| 64 | `path_provider` | trivial | 0 refs; only consumer was SQLite cache (#50) |
| 81 | `animations` | trivial | 0 `package:animations` refs (local `shared/animations/` dir is unrelated; `flutter_animate` is a different, live pkg) |
| 79 | `http` | low | 0 `package:http` refs; networking via Firebase SDKs |

Re-verified at zero in `lib/test/integration_test`. Run `flutter pub get` after each removal. Keep `share_plus`, `flutter_animate`, `flutter_localizations`.

### 5c. Assets
| Path | Risk | Evidence |
|---|---|---|
| `assets/app_icon.svg` | trivial | Not in `flutter.assets`; no `flutter_svg` dep; launcher icons use PNGs. Stale leftover source. |

### 5d. Cloud Functions
None dead in this batch. The dead receipts/OCR feature is client-only; `joinGroupByInviteCode` and the deletion-cascade functions are live.

---

## 6. Test-only references (delete the test in the same change)

Per CLAUDE.md ("Removing a UI element: grep tests for the removed label/key and delete obsolete assertions, don't patch them"), each of these has its only live reference under `test/` — deleting the production code requires deleting/pruning the cited test in the **same** commit, or the suite fails to compile.

| Production symbol/file | Test(s) to clean |
|---|---|
| `EventCard` | `test/features/group_detail_screen_test.dart` (EventCard block), `test/features/events/group_detail_events_test.dart` ("EventCard financial total" group) |
| `QuickActionTray` | `test/features/home/balance_hero_card_test.dart` (~150-228) |
| `WeeklySpendingCard` + `weeklyGroupSpendingProvider`/`weeklyGroupExpensesProvider`/`DailySpending` | `test/features/home/widgets_test.dart`, `test/unit/dashboard_providers_test.dart`, `home_screen_*_test.dart` `overrideWith` stubs |
| `GroupStatsGrid` | `test/features/groups/group_settle_up_screen_test.dart` (~405-455); `group_detail_screen_test.dart:391` `findsNothing` guard may stay |
| `GroupBalanceHero` + `SharedKeys.groupBalanceHero/groupBalanceSettleUpButton` | `test/unit/widget_coverage_test.dart`; fix stale comment `group_screens_test.dart:58` |
| `GroupMemberBalanceCard` + `GroupKeys.memberBalanceCard` | `test/features/group_balance_card_test.dart`; fix comment `group_screens_test.dart:90` |
| `GroupActivityTile` | `test/unit/group_activity_tile_test.dart` (whole file) |
| `FadeInList` | `test/unit/fade_in_list_test.dart`; drop import in `group_detail_events_test.dart:9` |
| `StaggeredGrid` | `test/unit/staggered_grid_test.dart` |
| `DotStepIndicator` | `test/unit/shared_widgets_test.dart` (~389-398), `shared_widgets_theme_test.dart:90` |
| `SearchFilterBar` | `test/unit/shared_widgets_test.dart`, `shared_widgets_theme_test.dart:136-137` |
| `SmartModuleCard` | `test/unit/widget_coverage_test.dart`, `shared_widgets_theme_test.dart` (3 cases) |
| `AnimatedCurrencyText` | `test/shared/widgets/animated_currency_text_test.dart`, `shared_widgets_theme_test.dart:84` |
| `AppTabBar` + `SharedKeys.appTabBarTab` | `test/unit/widget_coverage_test.dart`, `shared_widgets_theme_test.dart` |
| `GlassCard` / `GradientContainer` / `ShimmerPlaceholder` | `test/unit/shared_widgets_test.dart`, `widget_coverage_test.dart`, `shared_widgets_theme_test.dart:120-122` |
| `CategorySelectionStep` + `AmountInputSection` | `test/features/ledger/expense_entry_widgets_test.dart` (two groups) |
| `CategoryService` + `categoryServiceProvider` + `category{Loading,Error}Provider` | `test/features/ledger/category_provider_test.dart` |
| `AuthService` + `authServiceProvider` | `test/features/auth/auth_provider_test.dart:114-120` |
| `EventService.addParticipant`/`removeParticipant` | `test/features/events/services/event_service_test.dart` (two groups) |
| `Event.isOngoing` | `test/features/events/models/event_model_test.dart:338-363`, `trip_model_back_compat_test.dart` |
| `Trip` / `TripModules` | `test/unit/trip_model_back_compat_test.dart` |
| `EventKeys.modulesSection` | `test/features/events/create_event_test.dart:283` (redundant with line 284) |
| `NotificationService.isInitialized` | `test/features/profile/profile_screen_test.dart:99` (orphan mock stub) |
| `eventTransactionActivityProvider`/`tripActivityProvider`/`tripTransactionActivityProvider` | `test/features/activity/activity_service_test.dart` (145-198) |
| `OnboardingScreen` | `test/features/onboarding/onboarding_screen_test.dart` (whole file) |
| `ActivityEntryCard`/`ActivityHeroCard`/`GroupActivityTile` | `test/unit/group_activity_tile_test.dart` |

> **Coverage note:** several are coverage-gate filler (e.g. `widget_coverage_test.dart` blocks). Deleting widget + its covering test is coverage-neutral; verify the 80% gate (`readiness_check.yml`) stays green after each PR.

---

## 7. could-be-private (low priority — NOT removals)

Alive (reachable transitively) but exported wider than needed. Listed for completeness; **do not delete**. (The four flagged DEAD in §4/§5 — `userGroupsForParticipantProvider`, `eventUnifiedLedgerProvider`, etc. — are handled above as deletions, not privatizations.)

Leave or privatize opportunistically, separate from this cleanup: `ConnectivityProbe`, `SettingsService.languageKey` (needed by `integration_test/`), `kEmailMaxLength`, `CurrencyConfig` (**high blast radius — keep public**), `AppLinks.inviteLinkHost`, `AppTypography.*`, `CoverPalette`/`CoverArtPainter`, `LedgerDayStamp`/`LedgerSettleRow`/`LedgerDayGroup`, `firebaseUserProvider`, `MemberDisplay`, `EventTypeColorRole`, the auth DI typedefs, `activityServiceProvider`. **Do not "fix" these as part of dead-code removal** — privatizing `CurrencyConfig`/`AppTypography.*`/`LedgerSettleRow`/`MemberDisplay` touches money-display and ledger render paths and is out of scope.

---

## 8. Suggested PR slicing (one concern each, lowest-risk first)

Each PR independently revertable. Run `flutter analyze` + relevant tests after each; watch the 80% coverage gate.

1. **PR-1 `chore: drop unused pubspec dependencies`** — `pdf`, `csv`, `path_provider`, `http`, `animations`, `cupertino_icons`; `flutter pub get`. (No code.)
2. **PR-2 `chore: delete orphaned app_icon.svg asset`** — single file; verify icon-gen tool unaffected.
3. **PR-3 `refactor: remove dead bare consts/methods`** — `kDisplayNameMinLength`, `AppTheme.fontFamily`, `HapticService.warning`, `localizedGroupActivityTitle`, `formatTimelineDayLabel`, `NotificationService.isInitialized` (+ its mock stub).
4. **PR-4 `refactor: delete receipts/OCR dead feature`** — F1. Follow-up issue for `editorReceipt*` ARB keys.
5. **PR-5 `refactor: delete legacy Transaction ledger chain`** — F3.
6. **PR-6 `refactor: delete settle_up split orphan widgets`** — F9 (6 files).
7. **PR-7 `refactor: delete abandoned 3-step add-expense flow`** — F8 (3 files + shared test).
8. **PR-8 `refactor: delete dead home dashboard cluster`** — F4 + F5. The one `medium`-risk cluster; keep `crossGroupActivityProvider`.
9. **PR-9 `refactor: delete superseded group widgets`** — F10 group sub-list (6 files) + orphaned `GroupKeys`/`SharedKeys`.
10. **PR-10 `refactor: delete unwired profile section widgets`** — F10 profile sub-list (4 files) + fix dangling dartdoc/test comments.
11. **PR-11 `refactor: delete orphaned activity & shared widgets`** — `activity_entry_card`, `activity_hero_card`, `route_mark`, `smart_module_card`, `app_tab_bar`, `search_filter_bar`, `dot_step_indicator`, `animated_currency_text`, `loading_button.dart` siblings.
12. **PR-12 `refactor: delete animations barrel + dead members`** — F11.
13. **PR-13 `refactor: delete deprecated activity-service shims`** — F7.
14. **PR-14 `refactor: remove dead trip back-compat + participant providers`** — F6.
15. **PR-15 `refactor: remove unused event/category/auth service members`** — `EventService.add/removeParticipant`, `Event.isOngoing`, `CategoryService`+providers, `AuthService`+provider, `expenseErrorProvider`.
16. **PR-16 `refactor: archive onboarding screen + dead EventKeys`** — F2 + 13 `EventKeys` members + `modulesSection`. **Include the CLAUDE.md fix** for the stale onboarding-redirect invariant (bear trap: the documented router redirect does not exist — `appRouteRedirect` is splash→home only).
