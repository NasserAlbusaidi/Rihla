import 'package:flutter_test/flutter_test.dart';

void main() {
  group('groupBalancesProvider', () {
    test('aggregates expenses from all events in a group',
        skip: 'Awaiting Plan 05-03: groupBalancesProvider implementation',
        () {});

    test('includes group-level settlements in balance calculation',
        skip: 'Awaiting Plan 05-03: groupBalancesProvider implementation',
        () {});

    test('computes per-event breakdown per member',
        skip: 'Awaiting Plan 05-03: groupBalancesProvider implementation',
        () {});

    test('returns loading when events stream has no value yet',
        skip: 'Awaiting Plan 05-03: groupBalancesProvider implementation',
        () {});

    test('totalSpent sums all expenses across all events',
        skip: 'Awaiting Plan 05-03: groupBalancesProvider implementation',
        () {});

    test('contribution percentage per member computed from totalPaid',
        skip: 'Awaiting Plan 05-03: groupBalancesProvider implementation',
        () {});
  });
}
