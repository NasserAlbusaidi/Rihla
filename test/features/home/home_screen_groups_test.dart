import 'package:flutter_test/flutter_test.dart';

void main() {
  group('HomeScreen groups', () {
    test(
      'shows "Your Groups" header',
      skip: 'Awaiting Plan 02-02: HomeScreen replacement',
      () {},
    );

    test(
      'shows GroupCard for each group from userGroupsProvider',
      skip: 'Awaiting Plan 02-02: HomeScreen replacement',
      () {},
    );

    test(
      'shows empty state when user has no groups',
      skip: 'Awaiting Plan 02-02: HomeScreen replacement',
      () {},
    );

    test(
      'FAB opens bottom sheet with Create and Join options',
      skip: 'Awaiting Plan 02-02: HomeScreen replacement',
      () {},
    );

    test(
      'tapping GroupCard navigates to group detail',
      skip: 'Awaiting Plan 02-02: HomeScreen replacement',
      () {},
    );

    test(
      'does not reference userTripsProvider (trip code removed)',
      skip: 'Awaiting Plan 02-02: HomeScreen replacement',
      () {},
    );
  });
}
