---
grill_target: "current diff vs main — ledger and money code (no off-limits)"
off_limits: []
project_root: /Users/nasseralbusaidi/Desktop/Personal/Rihla
base_branch: main
base_commit: 2b4f53dbfca80316e652fc26d1914814362190ae
---

# Code Review — ledger/money diff vs main — 2026-05-25

> Generated by /grill-my-code, mode 4b. Issue 1 was grilled interactively;
> the user accepted the rest as catalog FIXes and dispatched the bundle to a
> fresh `/fix-my-code` session.

## FIX (user conceded)

- [ ] `lib/core/services/cache/expense_cache_repository.dart:152-163` — `_decodeSplitValue` hardcodes `'OMR'` while the symmetric `_encodeSplitValue` takes `String currency`; same-file encode/decode disagree on currency contract — *principle: broken invariant*
  - **Why it bites**: when multi-currency lands, the encode-side will silently start writing USD subunits (cents) to a decode-side that still reads OMR (fils). A $5.00 share is written as `500`, read back as `0.500 OMR`. Worse than uniform hardcoding because half the system will work, so the failure mode is "cache renders wrong, then heals on next Firestore emit." The diff was a chance to thread `currency` through `_decodeSplitDistribution → _decodeSplitValue` while touching this exact line (`parsePersistedSubunit` swap); it wasn't taken. Either rip `String currency` out of `_encodeSplitValue` (commit to "cache is OMR-only forever") or thread it through the decode path. The current half-state is a bear-trap for the deferred multi-currency cluster.
  - **Code at the time of review**:
    ```dart
    int _encodeSplitValue(Decimal value, SplitMode mode, String currency) {
      return switch (mode) {
        SplitMode.exact => MoneySerializer.toSubunits(value, currency),
        SplitMode.percent => (value * Decimal.fromInt(1000)).toBigInt().toInt(),
        SplitMode.shares || SplitMode.equally => value.toBigInt().toInt(),
      };
    }

    Decimal _decodeSplitValue(Object? value, SplitMode mode) {
      final persistedValue =
          parsePersistedSubunit(value, field: 'cache.splitDistribution');
      return switch (mode) {
        SplitMode.exact => MoneySerializer.fromSubunits(persistedValue, 'OMR'),
        SplitMode.percent =>
          (Decimal.fromInt(persistedValue) / Decimal.fromInt(1000)).toDecimal(
            scaleOnInfinitePrecision: 3,
          ),
        SplitMode.shares || SplitMode.equally => Decimal.fromInt(persistedValue),
      };
    }
    ```

- [ ] `lib/features/ledger/providers/expense_provider.dart:505-507` — `calculateTotalExpenses` does not filter `isDeleted`, while the sibling `calculateBalances` now does (line 192) — *principle: broken invariant*
  - **Why it bites**: the same defense-in-depth that justified `if (expense.isDeleted) continue;` at line 192 ("don't trust that callers pre-filter") wasn't applied to its sibling sum. In production, both share the same Firestore stream which filters server-side, so the bug is latent. It bites the moment anything else feeds `calculateTotalExpenses`: a cache repopulation path, a debug/admin view, a test fixture, or any future caller. The two functions become a silent split-brain — net balance ignores a deleted expense while `totalSpent` includes it, so the group hero card displays "Spent: 1000 OMR" while the per-member balances sum as if 500 OMR was spent. No error, no log.
  - **Code at the time of review**:
    ```dart
    // line 191-192 (already filters):
    for (final expense in expenses) {
      if (expense.isDeleted) continue;
      ...
    }

    // line 505-507 (does NOT filter):
    static Decimal calculateTotalExpenses(List<Expense> expenses) {
      return expenses.fold(Decimal.zero, (sum, e) => sum + e.amount);
    }
    ```

- [ ] `lib/features/ledger/providers/expense_provider.dart:283-296` — settlement fold silently drops payer contributions when the payer is not in the participant list, breaking `sum(netBalance) == 0` invariant at the per-event view — *principle: fail-silent / broken invariant*
  - **Why it bites**: `paidMap` and `settlementAdjustmentMap` are seeded from `participants` (lines 183-188, 280-282). At the per-event view (`eventBalancesProvider`, `ledger_screen.dart`, `settle_up_screen.dart`), `participants` is derived strictly from `event.participantIds` — former members who left the group are NOT included. When a former-member-payer settles to a current-member-recipient: `paidMap.containsKey(formerUid)` is false → payer contribution dropped; `settlementAdjustmentMap.containsKey(recipientUid)` is true → recipient's net reduced by `s.amount`. Net effect: the recipient sees their `netBalance` drop by the settlement amount with no offsetting positive elsewhere; `sum(net)` across visible participants is off by `s.amount`. The group rollup compensates by extending `eventParticipants` with `eventLocalFormerActors` — the per-event consumers do not. This is pre-existing, but the diff touched these exact lines (the gate flipped from `settlementAdjustmentMap.containsKey` to `paidMap.containsKey`) and could have used `putIfAbsent` to seed missing actors instead. Fix: extend the participant seeding to include UIDs that appear only in expenses/settlements, OR have callers pass the union (matching `group_balance_provider`'s pattern).
  - **Code at the time of review**:
    ```dart
    for (final s in settlements) {
      if (s.payerParticipantId != null &&
          paidMap.containsKey(s.payerParticipantId)) {
        paidMap[s.payerParticipantId!] =
            paidMap[s.payerParticipantId!]! + s.amount;
      }
      if (s.recipientParticipantId != null &&
          settlementAdjustmentMap.containsKey(s.recipientParticipantId)) {
        settlementAdjustmentMap[s.recipientParticipantId!] =
            settlementAdjustmentMap[s.recipientParticipantId!]! -
            s.amount;
      }
    }
    ```

- [ ] `lib/features/ledger/providers/expense_provider.dart:336-369` — new `_allocateExact` extends OMR-hardcoded `_toOmaniPrecision` into a path that previously preserved user-typed exact values verbatim — *principle: leaky abstraction*
  - **Why it bites**: pre-fix, exact mode returned `Map<String, Decimal>.from(distribution)` — the user's typed numbers passed through untouched. Post-fix, every non-last share is run through `_toOmaniPrecision` (OMR fils truncation), and the last share absorbs the remainder. For OMR this is correct. For a USD exact split of `{p1: 5.005, p2: 4.995}` totalling `$10.00`: `_toOmaniPrecision(5.005)` returns `5.005` (3dp, not 2dp) because it scales by 1000 not 100; the persistence boundary then truncates `5.005` to `500 cents = $5.00`, and `4.995` to `499 cents = $4.99`. The OMR-shaped normalization quietly produces non-USD-precision intermediates that the persist step re-truncates. Same multi-currency deferral applies, but the diff *extended* the OMR-only assumption into a code path that didn't have it. Either accept this as part of the multi-currency cluster and document, or pass `expense.currency` through `_toOmaniPrecision` (rename: `_toCurrencyPrecision(value, currency)`).
  - **Code at the time of review**:
    ```dart
    // Normalize to OMR precision so sub-fils noise can't survive into the
    // persistence boundary's truncating toSubunits call. Alphabetically-last
    // recipient absorbs the remainder, matching the documented invariant for
    // equal/shares/percent splits (see CLAUDE.md).
    final sortedRecipients = distribution.keys.toList()..sort();
    final allocations = <String, Decimal>{};
    var allocated = Decimal.zero;
    for (int i = 0; i < sortedRecipients.length; i++) {
      final recipientId = sortedRecipients[i];
      final isLast = i == sortedRecipients.length - 1;
      final value = isLast
          ? expense.amount - allocated
          : _toOmaniPrecision(distribution[recipientId]!);
      allocations[recipientId] = value;
      allocated += value;
    }
    return allocations;
    ```

- [ ] `lib/features/ledger/providers/expense_provider.dart:356-368` — exact-mode last-absorbs-remainder will silently shift sub-fils slack onto a recipient whose explicit share was zero — *principle: principle of least astonishment*
  - **Why it bites**: the new exact-allocation loop computes `_toOmaniPrecision(...)` for every non-last share and sets `last = expense.amount - allocated`. If the alphabetically-last recipient was explicitly typed `0.0000` and one of the earlier shares had sub-fils precision, the zero-share recipient ends up owing the residual (e.g. `0.001 OMR`). The documented invariant says "rounding remainder → alphabetically-last recipient" — that was correct for equal/shares/percent where every recipient is participating. In exact mode, a user typing `{Alice: 5.0001, Bob: 4.9999, Carol: 0.0000}` for a 10-fil expense legitimately means "Carol owes nothing" — the new code makes Carol owe `10 - (5.000 + 4.999) = 0.001`. UI surprise: the user explicitly set Carol to zero and the balance screen says she owes 0.001. Fix candidates: (a) skip `_toOmaniPrecision` for exact mode and trust the validated values directly, then snap only at persist; (b) compute residual relative to the largest non-zero share instead of the alphabetically-last; (c) document this in `_allocateExact`'s docstring as a known precision-distribution rule.
  - **Code at the time of review**:
    ```dart
    final sortedRecipients = distribution.keys.toList()..sort();
    final allocations = <String, Decimal>{};
    var allocated = Decimal.zero;
    for (int i = 0; i < sortedRecipients.length; i++) {
      final recipientId = sortedRecipients[i];
      final isLast = i == sortedRecipients.length - 1;
      final value = isLast
          ? expense.amount - allocated
          : _toOmaniPrecision(distribution[recipientId]!);
      allocations[recipientId] = value;
      allocated += value;
    }
    return allocations;
    ```

- [ ] `lib/features/ledger/models/expense_model.dart:368-383` — `parsePersistedSubunit` is a top-level public function placed in the model file rather than alongside `MoneySerializer` — *principle: single responsibility / coupling vs cohesion*
  - **Why it bites**: the function is the boundary parser for money/share subunits — it belongs next to the other boundary helper (`MoneySerializer`) in `lib/core/services/money_serializer.dart`, not as a sidecar to `Expense`. Cache repo already imports `package:safar/features/ledger/models/expense_model.dart` only to reach this helper. The next person debugging a Firestore round-trip will grep `money_serializer.dart` and not find it. Also: the function is exposed as a top-level public symbol from a model file, breaking the codebase's convention that public utilities live under `core/services/`. Move it; update the two import sites; delete unused `decimal` re-imports if applicable.
  - **Code at the time of review**:
    ```dart
    /// Parse a persisted money/share value that *must* represent an integer
    /// count of subunits, into an int. Throws [StateError] on any fractional
    /// input. ...
    int parsePersistedSubunit(Object? value, {required String field}) {
      if (value is int) return value;
      if (value == null) {
        throw StateError('Expected integer subunits for $field, got null');
      }
      final asDecimal = value is Decimal
          ? value
          : Decimal.parse(value.toString());
      final asInt = asDecimal.toBigInt().toInt();
      if (Decimal.fromInt(asInt) != asDecimal) {
        throw StateError(
          'Expected integer subunits for $field, got fractional value: $value',
        );
      }
      return asInt;
    }
    ```

- [ ] `lib/features/ledger/models/expense_model.dart:87-152` — dead Supabase-era `Expense.fromJson` / `Expense.toJson` not removed while the file was being touched — *principle: dead code*
  - **Why it bites**: Rihla's Supabase migration is "complete — do not add Supabase keys" per CLAUDE.md, yet the model still carries a 65-line snake_case JSON parser with Supabase-shaped joins (`expense_categories`, `participants.profiles`). The diff added a new top-level function to this same file. While there is no compile-time error, the dead path raises the cognitive cost of edits (every refactor must check whether the legacy decode also needs updating), and the model now has two distinct serialization contracts side-by-side with no comment marking one as dead. Grep already cataloged this as observation 8929 (May 1 2026). Delete both methods + tests; if there are still callers, this becomes a separate "find and migrate callers" follow-up.
  - **Code at the time of review**:
    ```dart
    // expense_model.dart:87-152
    factory Expense.fromJson(Map<String, dynamic> json) {
      final category = json['expense_categories'] as Map<String, dynamic>?;
      final participant = json['participants'] as Map<String, dynamic>?;
      final profiles = participant?['profiles'] as Map<String, dynamic>?;
      ...
      isDeleted: json['is_deleted'] as bool? ?? false,
      deletedAt: json['deleted_at'] != null
          ? DateTime.parse(json['deleted_at'] as String)
          : null,
      );
    }

    Map<String, dynamic> toJson() {
      return {
        'trip_id': tripId,
        'payer_participant_id': payerParticipantId,
        ...
      };
    }
    ```

- [ ] `lib/features/ledger/providers/expense_provider.dart:165` — `_hundred` constant has a single remaining call-site after the percent-slack fix (`_allocatePercent` now uses `totalPercent` as denominator) — *principle: dead-branch-ish / magic constant*
  - **Why it bites**: pre-fix, `_hundred` was the denominator. Post-fix, it survives only at line 380 as the validation tolerance reference (`(totalPercent - _hundred).abs() > _splitTolerance`). A single-use named constant for an integer literal `100` is over-engineering. Either inline `Decimal.fromInt(100)` at the validation site, or rename `_hundred` to `_percentScale` or `_percentRange` so its surviving meaning is explicit. Same applies to noticing it during cleanup — it's a footprint left by the fix.
  - **Code at the time of review**:
    ```dart
    static final Decimal _splitTolerance = Decimal.parse('0.001');
    static final Decimal _hundred = Decimal.fromInt(100);
    ...
    // sole remaining use:
    if ((totalPercent - _hundred).abs() > _splitTolerance) {
      ...
    }
    ```

- [ ] `lib/features/ledger/providers/expense_provider.dart:419-424` — `_toOmaniPrecision` is named for OMR specifically but is now invoked from three code paths (`_allocateExact`, `_allocateWeighted`) each holding an expense whose currency may or may not be OMR — *principle: naming that misleads*
  - **Why it bites**: the helper hardcodes `'OMR'` for both the toSubunits and fromSubunits calls. Its name advertises that fact — "ToOmaniPrecision" — which is accurate but also a bear-trap: every reader has to confirm that the call-site is OMR-only, and the call-sites don't pass an `expense.currency` because the helper doesn't accept one. Rename to `_toCurrencyPrecision(Decimal value, String currency)` and thread the expense's currency through, OR pin all the call-sites with an inline assertion (`assert(expense.currency == 'OMR')`). Today there is no signal that calling `_toOmaniPrecision` on a USD share is wrong; the linter doesn't know, the compiler doesn't know, and the tests are all OMR.
  - **Code at the time of review**:
    ```dart
    static Decimal _toOmaniPrecision(Decimal value) {
      return MoneySerializer.fromSubunits(
        MoneySerializer.toSubunits(value, 'OMR'),
        'OMR',
      );
    }
    ```

- [ ] `test/unit/balance_calculations_test.dart:286-296` — test comment for `percent mode distributes slack proportionally` mis-describes the mechanism (says "each weight is normalized against the actual total" then admits p3 ends up "remainder-absorbing because they sort alphabetically last") — *principle: comments that lie*
  - **Why it bites**: the comment promises proportional slack distribution, but the actual test expectations (`p1: 29.999`, `p2: 29.999`, `p3: 40.002`) show that the slack is still concentrated on the last sorted participant — what changed is that `p1` and `p2` shed `0.001` each (via the new denominator), and `p3` (alphabetically last) absorbs the recombined `+0.002`. The mechanism is "each share is reduced by its share of the over-sum, then the last absorbs the residual" — not "slack distributed proportionally." A future reader auditing this test will believe the system behaves more fairly than it does, then be surprised when they hand-trace `{40, 30, 30.001}` and find the alphabetically-last person still absorbs the rounding asymmetry. Rewrite the comment to match the math actually being asserted, or pick test input that demonstrates proportional vs concentrated outcomes (e.g. add a `{p_a: 30.001, p_b: 30.001, p_c: 40}` case where the alphabetically-last is NOT the over-typed one — then assert that p_c does NOT absorb the slack alone).
  - **Code at the time of review**:
    ```dart
    test(
      'percent mode distributes slack proportionally instead of dumping on alphabetically-last',
      () {
        // p3 typed 40.001 (typo); sum = 100.001, within tolerance. Without
        // the fix, p1 and p2 receive exactly 30.000 each and p3 absorbs the
        // -0.001 slack as 40.000. After the fix, each recipient's weight is
        // normalized against the actual total (100.001), so the over-sum
        // shrinks all three shares proportionally; p3's truncation still
        // ends up as remainder-absorbing because they sort alphabetically
        // last, but the slack is shared rather than concentrated.
        ...
        expect(owedFor(balances, 'p1'), Decimal.parse('29.999'));
        expect(owedFor(balances, 'p2'), Decimal.parse('29.999'));
        expect(owedFor(balances, 'p3'), Decimal.parse('40.002'));
      },
    );
    ```

## NOT RAISED (good calls worth naming)

- `lib/features/ledger/providers/expense_provider.dart:345-350` — `_allocateExact` now throws `StateError` on invalid sum instead of silently rewriting to equal split. Loud failure on persisted-data divergence is the right call for money math; the test in `balance_calculations_test.dart:191-216` locks it in.
- `lib/features/ledger/models/expense_model.dart:368-383` — `parsePersistedSubunit` throwing on fractional input is a real bear-trap closure (the prior `_persistedInt` silently truncated `9.99` to `9`). Modulo the placement nit, the design is correct: fail-loud at the boundary that decides what an integer means.
