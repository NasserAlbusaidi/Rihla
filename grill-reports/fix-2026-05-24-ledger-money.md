# Fix Run — ledger / money — 2026-05-24

> Generated by /fix-my-code from grill-reports/ledger-money-2026-05-23.md
> Mode: foreground, --non-interactive (after interactive triage of an unlabelled Phase D report)

## Summary
  Working set: 8   Fixed: 7   Aborted: 0   Skipped: 1   Unattempted: 0

## FIXED — 7

- [C#3] BalanceCalculator filter isDeleted
  Commit: `584cbb4` fix(ledger): filter isDeleted expenses inside BalanceCalculator
  Files: lib/features/ledger/providers/expense_provider.dart, test/unit/balance_calculations_test.dart
  Target check: "isDeleted expenses do not contribute to totalPaid or totalOwed" (FAIL → PASS)

- [C#4] UserBalance.totalPaid fold settlements (fold approach)
  Commit: `686ad30` fix(ledger): fold settlement payments into UserBalance.totalPaid
  Files: lib/features/ledger/providers/expense_provider.dart, test/unit/balance_calculations_test.dart
  Target check: "totalPaid includes settlement payments made by the participant" (FAIL → PASS)
  Semantics shift: SettlementSummaryCard "Total paid by you" tile now means lifetime paid incl. settlements paid out. Recipient-side adjustment kept in settlementAdjustmentMap to preserve netBalance arithmetic.

- [H#15] Sort tiebreaker on participantId
  Commit: `4e342d2` fix(ledger): tiebreak optimal-settlement sort on participantId
  Files: lib/features/ledger/providers/expense_provider.dart, test/unit/balance_calculations_test.dart
  Target check: "equal-balance debtors are matched in deterministic participantId order" (FAIL → PASS)

- [H#11] _allocateExact remove silent fallback
  Commit: `9afe283` fix(ledger): throw on invalid exact split instead of silent equal-split fallback
  Files: lib/features/ledger/providers/expense_provider.dart, test/unit/balance_calculations_test.dart
  Target check: "exact mode throws on invalid sum instead of silently rewriting to equal split" (FAIL → PASS)
  Existing test "falls back to equal split..." was rewritten as Gate 1 scaffolding to assert the new throw contract.

- [H#16] _allocateExact normalize to OMR precision
  Commit: `97d2c4c` fix(ledger): normalize exact-split shares to OMR precision before allocation
  Files: lib/features/ledger/providers/expense_provider.dart, test/unit/balance_calculations_test.dart
  Target check: "exact mode normalizes sub-fils precision to OMR scale; alphabetically-last absorbs remainder" (FAIL → PASS)

- [H#17] _allocatePercent distribute slack
  Commit: `8876dd7` fix(ledger): distribute percent-split slack proportionally across recipients
  Files: lib/features/ledger/providers/expense_provider.dart, test/unit/balance_calculations_test.dart
  Target check: "percent mode distributes slack proportionally instead of dumping on alphabetically-last" (FAIL → PASS)

- [H#10] parsePersistedSubunit extraction + fractional-input throw
  Commit: `02bde6f` fix(ledger): extract parsePersistedSubunit; throw on fractional input
  Files: lib/features/ledger/models/expense_model.dart, lib/core/services/cache/expense_cache_repository.dart, test/unit/ledger_model_coverage_test.dart
  Target check: "throws on fractional input instead of silently truncating" (FAIL → PASS)
  Two private _persistedInt copies replaced by one public function at the top of expense_model.dart.

## SKIPPED — 1

- [H#13] OptimalSettlement struct
  Reason: Gate 3 Pass 1 rule 1 (out-of-scope files). Grill entry named 3 production files; real cascade is **5 production + 3 test files** (`group_settle_up_screen.dart` and `settle_up_page_body.dart` also consume the Map<String,dynamic> shape; their test fixtures also need updating). The grill verdict itself flagged "the structural items belong in a follow-up `/grill-my-plan` session" — this is that item.
  Action: open a `/grill-my-plan` session to design the typed-struct migration as a coherent refactor PR; do not bundle into a fix-my-code run.

## FLAGGED FOR REVIEW

- **Pre-existing test failure carried through the entire run**: `test/unit/auth_link_hosting_files_test.dart` → `"iOS entitlements declare Universal Links for auth and invites"`. This test asserts the Associated Domains entitlement IS present in `ios/Runner/Runner.entitlements`. The `chore(ios)` commit (a029eb5) earlier in this session removed that entitlement for free-team sideload. The test failed in every Gate 4 snapshot. Not a regression introduced by any ledger fix, but flagged so it gets resolved before the branch lands.
  - Fix options: (a) restore the entitlement and use a separate local-only `Runner-Dev.entitlements` for the free-team build, or (b) update the test to no longer assert Associated Domains (loses the regression-detection value for any future re-add).

- **Parallel bug discovered during H#11**: `_allocatePercent` has the same `debugPrint + return _allocateEqual` silent-fallback pattern as `_allocateExact`. H#11's scope was exact only; percent fallback unchanged. Follow-up to symmetrically throw on invalid percent sum.

- **Parallel bug discovered during C#3**: `Settlement` has an `isDeleted` field that `BalanceCalculator` does not filter either. C#3 only fixed the expense side. Same trust-boundary principle — soft-deleted settlements would still fold into `settlementAdjustmentMap` and `paidMap`. Follow-up to add the symmetric guard.

## DEFERred at triage (9, multi-currency cluster)

CRITICAL #1, #2, #5 + HIGH #6, #7, #8, #9, #12, #14 — all symptoms of "no single source of truth for event currency". Defer per OMR-only v1.2 launch; tackle as one coherent design in a post-v1.2 `/grill-my-plan` session, not nine spot fixes.

## Run statistics

- Triage: interactive (Phase D report had no FIX/DEFER labels)
- 7 fixes × ~35s full suite × 2 snapshots = ~8 min of total test runtime
- Test count progression: 1335 → 1336 → 1337 → 1338 → 1338 (H#11 swapped a test in lock-step) → 1339 → 1340 → 1341
- Working tree state at completion: clean, on branch `codex/auth-release-hardening-fix-pass`, 9 commits ahead of starting `419a043`

## Suggested next move

1. **Address the iOS entitlement test failure** before any new fix runs — every Gate 4 snapshot is currently noisy with one pre-existing FAIL. Strict reading of Gate 4 still works (stable-FAIL across snapshots ≠ regression) but cleaner to resolve.
2. **`/grill-my-plan` on the OptimalSettlement struct migration** — H#13 plus the two parallel discoveries (percent silent fallback, settlement isDeleted filter) form a small structural-cleanup PR worth designing coherently.
3. **`/grill-my-plan` on the multi-currency story** — the 9 DEFERred items only make sense as one design (event-currency source-of-truth + MoneySerializer scoped by event-currency); take them up before any non-OMR group ever exists in production.
