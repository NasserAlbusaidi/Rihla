# Fix Run — ledger/money diff — 2026-05-25

> Generated by /fix-my-code from grill-reports/code-review-2026-05-25-ledger-money-diff.md
> Mode: foreground (--non-interactive --allow-dirty)
> Base: `2b4f53d` → HEAD `7ce3c1d` (8 atomic commits)
> Pre-existing iOS Associated-Domains entitlement test failure persisted throughout (unchanged, unrelated to ledger/money).

## Summary

  Working set: 10   Fixed: 9   Aborted: 0   Skipped: 1   Unattempted: 0
  (Fix [4] rolled into Fix [5]'s commit — same `_toOmaniPrecision` call site.)

## FIXED — 9

- **[2] calculateTotalExpenses isDeleted filter** (broken invariant)
  Commit: `08704d6` `fix(ledger): filter soft-deleted expenses in calculateTotalExpenses`
  Files: `lib/features/ledger/providers/expense_provider.dart`, `test/unit/settlement_optimization_test.dart`
  Target check: `calculateTotalExpenses › Soft-deleted expenses are excluded from total` (FAIL → PASS)

- **[10] Test comment that lies about slack mechanism** (comments that lie)
  Commit: `c1920d2` `test(ledger): clarify percent-mode comment to distinguish over-sum scaling from remainder absorption`
  Files: `test/unit/balance_calculations_test.dart`
  Target check: comment rewrite (no behavior change; documentation-only fix with no Gate 1 testable failure mode — concession noted).

- **[8] _hundred constant — explicit rename to _percentScale** (dead-branch-ish / magic constant)
  Commit: `904e0fd` `refactor(ledger): rename _hundred to _percentScale for explicit intent`
  Files: `lib/features/ledger/providers/expense_provider.dart`
  Target check: full suite unchanged (refactor; no behavior change).

- **[6] Move parsePersistedSubunit to money_serializer module** (single responsibility / coupling vs cohesion)
  Commit: `decfcee` `refactor(money): move parsePersistedSubunit to money_serializer module`
  Files: `lib/core/services/money_serializer.dart`, `lib/features/ledger/models/expense_model.dart`, `test/unit/ledger_model_coverage_test.dart`
  Target check: full suite unchanged from baseline (relocation, no behavior change).

- **[7] Delete dead Supabase Expense.fromJson/toJson** (dead code)
  Commit: `bc3ddd0` `chore(ledger): remove dead Supabase-era Expense.fromJson/toJson`
  Files: `lib/features/ledger/models/expense_model.dart`, `test/unit/expense_split_distribution_test.dart`, `test/unit/ledger_model_coverage_test.dart`
  Target check: suite 1342 → 1336 PASS (−6 tests = the 6 deleted dead-code tests). 185 lines removed.

- **[5] + [4] Exact-split allocates user-typed values verbatim; tolerance gap goes to largest non-zero recipient** (principle of least astonishment + leaky abstraction)
  Commit: `477f323` `fix(ledger): exact-split allocates user-typed values verbatim; tolerance gap goes to largest non-zero recipient`
  Files: `lib/features/ledger/providers/expense_provider.dart`, `test/unit/balance_calculations_test.dart`
  Target checks:
  - `exact mode preserves user-typed sub-fils values when sum matches amount` (FAIL → PASS)
  - `exact mode keeps explicit-zero recipient at zero, even with sub-fils peers` (FAIL → PASS)
  - `exact mode redistributes within-tolerance residual to largest non-zero recipient (not alphabetically-last zero)` (FAIL → PASS)
  Old Gate 1 scaffolding test at `balance_calculations_test.dart:163-189` updated (it locked in the buggy behavior).

- **[9] OMR-only assertion on shares/percent allocation paths** (naming that misleads)
  Commit: `c260271` `fix(ledger): assert OMR-only currency on shares/percent allocation paths`
  Files: `lib/features/ledger/providers/expense_provider.dart`, `test/unit/balance_calculations_test.dart`
  Target check: `shares and percent modes assert OMR-only currency (V1 ships OMR; multi-currency cluster deferred)` (FAIL → PASS)
  Chose grill option (b) — assertion — over (a) rename+thread, since (a) overlaps with the deferred multi-currency cluster (memory: S1924).

- **[1] Cache encode/decode symmetric on OMR-only contract** (broken invariant)
  Commit: `7ce3c1d` `fix(cache): make expense cache encode/decode symmetric on OMR-only contract`
  Files: `lib/core/services/cache/expense_cache_repository.dart`, `test/unit/expense_cache_repository_test.dart`
  Target check: `asserts OMR-only currency when expense has a splitDistribution (cache schema does not persist currency; V1 OMR-only)` (FAIL → PASS)
  Chose grill option (a) — rip currency param from `_encodeSplitValue`, add entry assert on `_encodeSplitDistribution`.

## SKIPPED — 1

- **[3] Settlement fold drops former-member payer contributions** (fail-silent / broken invariant) — `skip_class=other`

  Fix surface spans:
  - `BalanceCalculator.calculateBalances` — internal map seeding + return-list contract change
  - Either (A) extend return list to include former actors AND extend `splitRecipients` in expense scope handlers, OR (B) apply `group_balance_provider`'s union-construction pattern at each of 3 per-event call-sites (`eventBalancesProvider`, `ledger_screen`, `settle_up_screen`)

  Internal-only seeding extension (the minimal interpretation of the grill's literal lines 283-296) does NOT restore the visible sum-to-zero invariant: the function still returns balances only for the passed-in participants, so a former payer's settlement contribution is bookkept internally but invisible to callers — Alice's net still drops by `s.amount` with no offset.

  Both correctness-restoring shapes need design input on return-contract direction. **Recommend hand-edit with a plan session.** Test scenario for future plan: former payer settles to current recipient; assert sum-to-zero on the union of {current participants, former financial actors}.

## FLAGGED FOR REVIEW

- **Stale `_hundred` reference in comment** (`test/unit/balance_calculations_test.dart:267, 270`) — Fix [8] renamed the constant in `expense_provider.dart` but the historical reference in the test comment (Fix [10]'s rewrite, describing pre-fix code state) still says `_hundred`. Factually true (the pre-fix code did use `_hundred`), but a future grep for `_hundred` will miss the symbol. Trivial follow-up.

- **Fix [4] resolved as side effect of Fix [5]** — both targeted the same `_toOmaniPrecision` call inside `_allocateExact`. Removing the call entirely (for exact mode) resolved both bugs in one commit. Confirm acceptable scope-bundling.

## Suggested next move

1. **Open a plan session for Fix [3]**. The right shape (internal extension of return contract, or caller-side union construction at 3 per-event sites) needs design input. Worth grilling the plan before writing code.
2. **Pre-existing iOS test failure** — known per memory obs 19417. Not addressed in this run.
3. **`.fix-my-code.yml` was created** at project root as one-time setup for Flutter detection. Commit or `.gitignore` per preference.
