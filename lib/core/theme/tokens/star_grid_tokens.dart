/// Opacity for the night star-grid painted over dark hero surfaces.
///
/// Kept ≤3% per spec — the grid should read as faint stars on the kohl
/// ground, never as visible noise.
const double kStarGridOpacity = 0.03;
