## auth/ — Anonymous Firebase Auth

- **providers/auth_provider.dart**: `authStateProvider` (stream), `currentUserProvider`, `authServiceProvider`. Class: `AuthService` — token checks, isAuthenticated
