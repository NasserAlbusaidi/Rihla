---
phase: 04-firestore-repository-layer
plan: 04
type: execute
wave: 4
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - lib/core/services/sync_service.dart
  - lib/core/services/offline_repository.dart
  - lib/core/services/balance_cache_repository.dart
  - lib/core/services/cache_service.dart
  - lib/core/providers/connectivity_provider.dart
  - lib/features/events/services/event_service.dart
  - lib/features/events/models/event_model.dart
  - lib/features/events/screens/event_command_center.dart
  - lib/features/groups/providers/group_provider.dart
  - lib/features/ledger/providers/expense_provider.dart
  - lib/features/ledger/screens/ledger_screen.dart
  - lib/features/ledger/screens/add_expense_screen.dart
  - lib/features/ledger/screens/settle_up_screen.dart
  - lib/features/ledger/screens/edit_expense_sheet.dart
  - lib/features/gear/providers/gear_provider.dart
  - lib/features/logistics/providers/sub_group_provider.dart
  - lib/features/activity/services/activity_service.dart
  - lib/features/events/widgets/event_card.dart
  - lib/features/events/widgets/event_module_list.dart
  - lib/features/home/widgets/module_list.dart
  - lib/features/home/widgets/expense_summary_hero.dart
  - test/unit/connectivity_provider_test.dart
  - test/unit/balance_cache_repository_test.dart
autonomous: true
requirements:
  - MIG-03
  - MIG-04
  - MIG-05

must_haves:
  truths:
    - "SyncService class and sync_queue table no longer exist in the codebase"
    - "All Firestore reads/writes flow through FirestoreRepository — no direct FirebaseFirestore.instance calls outside that class"
    - "SQLite serves BalanceCalculator queries via BalanceCacheRepository"
    - "ConnectivityNotifier pings Firestore instead of Supabase"
    - "Supabase bridge pattern is removed from EventService and EventModel"
    - "Screens use EventRef-based providers instead of deprecated tripId-based shims"
    - "GroupService and EventService extend FirestoreRepository"
  artifacts:
    - path: "lib/core/services/balance_cache_repository.dart"
      provides: "Narrow SQLite wrapper for BalanceCalculator expense/settlement queries"
      exports: ["BalanceCacheRepository"]
    - path: "lib/core/providers/connectivity_provider.dart"
      provides: "Firestore-based connectivity check"
      contains: "FirebaseConfig.firestore"
  key_links:
    - from: "lib/features/events/screens/event_command_center.dart"
      to: "lib/features/ledger/providers/expense_provider.dart"
      via: "eventExpensesProvider with EventRef"
      pattern: "eventExpensesProvider"
    - from: "lib/features/groups/providers/group_provider.dart"
      to: "lib/core/services/firestore_repository.dart"
      via: "GroupService extends FirestoreRepository"
      pattern: "extends FirestoreRepository"
    - from: "lib/core/providers/connectivity_provider.dart"
      to: "lib/core/config/firebase_config.dart"
      via: "FirebaseConfig.firestore for health check"
      pattern: "FirebaseConfig\\.firestore"
---

<objective>
Final cleanup after all 7 modules are migrated: delete SyncService and sync_queue, rename OfflineRepository to BalanceCacheRepository, update ConnectivityNotifier to ping Firestore, remove Supabase bridge from EventService/EventModel, refactor GroupService/EventService to extend FirestoreRepository, and update all screens from deprecated tripId-based providers to EventRef-based providers.

Purpose: Satisfies MIG-03 (SyncService deleted), MIG-04 (SQLite retained narrowly for balance queries), and MIG-05 (all Firestore access through FirestoreRepository). Removes all backward-compatibility shims and completes the migration.
Output: Clean codebase with no SyncService, no bridge pattern, all access through FirestoreRepository.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-firestore-repository-layer/04-CONTEXT.md
@.planning/phases/04-firestore-repository-layer/04-RESEARCH.md
@.planning/phases/04-firestore-repository-layer/04-00-SUMMARY.md
@.planning/phases/04-firestore-repository-layer/04-01-SUMMARY.md
@.planning/phases/04-firestore-repository-layer/04-02-SUMMARY.md
@.planning/phases/04-firestore-repository-layer/04-03-SUMMARY.md

@lib/core/services/sync_service.dart
@lib/core/services/offline_repository.dart
@lib/core/services/cache_service.dart
@lib/core/providers/connectivity_provider.dart
@lib/features/events/services/event_service.dart
@lib/features/events/models/event_model.dart
@lib/features/events/screens/event_command_center.dart
@lib/features/groups/providers/group_provider.dart

<interfaces>
<!-- EventRef type used by all new providers -->
From lib/features/ledger/providers/expense_provider.dart:
```dart
typedef EventRef = ({String groupId, String eventId});
```

<!-- New Firestore-backed providers to replace deprecated shims -->
```dart
final eventExpensesProvider = StreamProvider.family<List<Expense>, EventRef>(...);
final eventSettlementsProvider = StreamProvider.family<List<Settlement>, EventRef>(...);
final eventGearItemsProvider = StreamProvider.family<List<GearItem>, EventRef>(...);
final eventSubGroupsProvider = StreamProvider.family<List<SubGroup>, EventRef>(...);
final eventActivityProvider = StreamProvider.family<List<ActivityLog>, EventRef>(...);
final eventDocumentsProvider = StreamProvider.family<List<Document>, EventRef>(...);
final eventMemoriesProvider = StreamProvider.family<List<Memory>, EventRef>(...);
```

<!-- FirestoreRepository base class -->
From lib/core/services/firestore_repository.dart:
```dart
abstract class FirestoreRepository {
  FirestoreRepository();
  @visibleForTesting
  FirestoreRepository.withFirestore(FirebaseFirestore db);
  FirebaseFirestore get db;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SyncService deletion + BalanceCacheRepository + ConnectivityNotifier + base class refactor</name>
  <files>
    lib/core/services/sync_service.dart,
    lib/core/services/offline_repository.dart,
    lib/core/services/balance_cache_repository.dart,
    lib/core/services/cache_service.dart,
    lib/core/providers/connectivity_provider.dart,
    lib/features/groups/providers/group_provider.dart,
    lib/features/events/services/event_service.dart,
    test/unit/connectivity_provider_test.dart,
    test/unit/balance_cache_repository_test.dart
  </files>
  <read_first>
    lib/core/services/sync_service.dart,
    lib/core/services/offline_repository.dart,
    lib/core/services/cache_service.dart,
    lib/core/providers/connectivity_provider.dart,
    lib/features/groups/providers/group_provider.dart,
    lib/features/events/services/event_service.dart,
    lib/core/services/firestore_repository.dart,
    lib/core/services/local_database.dart,
    test/unit/sync_service_test.dart,
    test/unit/sync_retry_test.dart,
    test/unit/offline_repository_test.dart,
    test/unit/connectivity_provider_test.dart,
    test/unit/balance_cache_repository_test.dart
  </read_first>
  <action>
    **1. Delete `lib/core/services/sync_service.dart`** per D-04. This is the final step — all modules are now on Firestore.
    - Also delete `test/unit/sync_service_test.dart` and `test/unit/sync_retry_test.dart` (tests for deleted code)
    - Remove any imports of `sync_service.dart` from other files (search `grep -rn 'sync_service' lib/`)

    **2. Create `lib/core/services/balance_cache_repository.dart`** per D-16 (rename from OfflineRepository):
    - Extract ONLY the balance-related SQLite queries from `offline_repository.dart`:
      - Method to cache expenses to SQLite (for cross-event aggregation in Phase 5)
      - Method to cache settlements to SQLite
      - Method to read expenses from SQLite (for BalanceCalculator)
      - Method to read settlements from SQLite
    - The class does NOT extend FirestoreRepository (it's a SQLite wrapper, not a Firestore service)
    - Provide a Riverpod provider: `final balanceCacheRepositoryProvider = Provider<BalanceCacheRepository>((ref) => BalanceCacheRepository());`
    - **Keep the stream-based interface** for the deprecated backward-compat providers (they will be removed once all screens are updated in Task 2). If any deprecated provider still references `offlineRepositoryProvider`, update it to use `balanceCacheRepositoryProvider`.

    **3. Delete `lib/core/services/offline_repository.dart`** after extracting balance methods.
    - Delete `test/unit/offline_repository_test.dart`
    - Remove `offlineRepositoryProvider` and all imports of it

    **4. Trim `lib/core/services/cache_service.dart`:**
    - Delete sync_queue related methods: `addToSyncQueue()`, `removeSyncItem()`, `getPendingSyncItems()`, `hasPendingSync()`, `getSyncQueueCount()`
    - Delete trip-level cache methods that are no longer needed (trips are managed by EventService now)
    - Keep any methods still used by BalanceCacheRepository or other active code (search for usages before deleting)

    **5. Update `lib/core/providers/connectivity_provider.dart`:**
    - Remove imports of `SyncService` and `CacheService` (sync_queue count)
    - Remove `pendingSyncCountProvider` (sync_queue is gone)
    - Replace `SyncService.isOnline()` call with a Firestore-based ping:
      ```dart
      Future<bool> _isOnline() async {
        try {
          await FirebaseConfig.firestore
              .collection('_health')
              .doc('ping')
              .get(const GetOptions(source: Source.server));
          return true;
        } catch (e) {
          return false;
        }
      }
      ```
    - Update `checkConnectivity()` to call `_isOnline()` instead of `SyncService.isOnline()`
    - Remove the offline->online auto-sync trigger (SyncService no longer exists; Firestore handles offline writes automatically)
    - Remove skip markers from `test/unit/connectivity_provider_test.dart` and write tests

    **6. Refactor `GroupService` to extend FirestoreRepository** per D-06:
    - In `lib/features/groups/providers/group_provider.dart`, change GroupService from:
      ```dart
      class GroupService {
        final Ref _ref;
        GroupService(this._ref);
        FirebaseFirestore get _db => FirebaseConfig.firestore;
      ```
      to:
      ```dart
      class GroupService extends FirestoreRepository {
        final Ref _ref;
        GroupService(this._ref) : super();
        @visibleForTesting
        GroupService.withFirestore(this._ref, FirebaseFirestore db) : super.withFirestore(db);
        // Use `db` from base class instead of `_db`
      ```
    - Replace all `_db` references with `db` (from base class)
    - Remove the `FirebaseConfig.firestore` getter

    **7. Refactor `EventService` to extend FirestoreRepository** per D-06:
    - Change EventService from having its own `_db` field to extending FirestoreRepository
    - Update constructors:
      ```dart
      class EventService extends FirestoreRepository {
        final Ref? _ref;
        final GearService? _gearServiceOverride;
        final bool _skipBridgeInTest;

        EventService(Ref ref)
            : _ref = ref,
              _gearServiceOverride = null,
              _skipBridgeInTest = false,
              super();

        @visibleForTesting
        EventService.withFirestore(FirebaseFirestore db, GearService gearService)
            : _ref = null,
              _gearServiceOverride = gearService,
              _skipBridgeInTest = true,
              super.withFirestore(db);
      ```
    - Replace `_db` with `db` from base class

    **8. Remove skip markers and write tests** for `connectivity_provider_test.dart` and `balance_cache_repository_test.dart`.
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla && flutter analyze lib/core/ && flutter test test/unit/connectivity_provider_test.dart test/unit/balance_cache_repository_test.dart --no-pub</automated>
  </verify>
  <acceptance_criteria>
    - lib/core/services/sync_service.dart does NOT exist (deleted)
    - lib/core/services/offline_repository.dart does NOT exist (deleted)
    - lib/core/services/balance_cache_repository.dart contains `class BalanceCacheRepository`
    - lib/core/providers/connectivity_provider.dart does NOT contain `SyncService`
    - lib/core/providers/connectivity_provider.dart contains `FirebaseConfig.firestore`
    - lib/core/providers/connectivity_provider.dart contains `Source.server` (Firestore server-only read for ping)
    - lib/features/groups/providers/group_provider.dart contains `class GroupService extends FirestoreRepository`
    - lib/features/events/services/event_service.dart contains `class EventService extends FirestoreRepository`
    - grep -rn 'SyncService' lib/ returns NO matches
    - grep -rn 'offlineRepositoryProvider' lib/ returns NO matches (or only in balance_cache_repository.dart as alias)
    - flutter analyze lib/core/ reports no errors
    - test/unit/connectivity_provider_test.dart exits 0
    - test/unit/balance_cache_repository_test.dart exits 0
  </acceptance_criteria>
  <done>SyncService deleted. OfflineRepository replaced by BalanceCacheRepository. ConnectivityNotifier pings Firestore. GroupService and EventService extend FirestoreRepository. All Firestore access flows through the base class.</done>
</task>

<task type="auto">
  <name>Task 2: Bridge teardown + screen migration to EventRef providers</name>
  <files>
    lib/features/events/models/event_model.dart,
    lib/features/events/services/event_service.dart,
    lib/features/events/screens/event_command_center.dart,
    lib/features/events/widgets/event_card.dart,
    lib/features/events/widgets/event_module_list.dart,
    lib/features/ledger/providers/expense_provider.dart,
    lib/features/ledger/screens/ledger_screen.dart,
    lib/features/ledger/screens/add_expense_screen.dart,
    lib/features/ledger/screens/settle_up_screen.dart,
    lib/features/ledger/screens/edit_expense_sheet.dart,
    lib/features/gear/providers/gear_provider.dart,
    lib/features/logistics/providers/sub_group_provider.dart,
    lib/features/activity/services/activity_service.dart,
    lib/features/home/widgets/module_list.dart,
    lib/features/home/widgets/expense_summary_hero.dart
  </files>
  <read_first>
    lib/features/events/models/event_model.dart,
    lib/features/events/services/event_service.dart,
    lib/features/events/screens/event_command_center.dart,
    lib/features/events/widgets/event_card.dart,
    lib/features/events/widgets/event_module_list.dart,
    lib/features/ledger/providers/expense_provider.dart,
    lib/features/ledger/screens/ledger_screen.dart,
    lib/features/ledger/screens/add_expense_screen.dart,
    lib/features/ledger/screens/settle_up_screen.dart,
    lib/features/ledger/screens/edit_expense_sheet.dart,
    lib/features/gear/providers/gear_provider.dart,
    lib/features/logistics/providers/sub_group_provider.dart,
    lib/features/activity/services/activity_service.dart,
    lib/features/home/widgets/module_list.dart,
    lib/features/home/widgets/expense_summary_hero.dart,
    lib/features/home/widgets/trip_header.dart,
    lib/features/home/widgets/trip_recap_card.dart,
    lib/features/activity/screens/activity_feed_screen.dart,
    lib/features/ledger/providers/ledger_provider.dart,
    lib/features/trip/models/trip_model.dart
  </read_first>
  <action>
    **1. Remove bridgeTripId from Event model** per D-17:
    - In `lib/features/events/models/event_model.dart`:
      - Remove the `bridgeTripId` field from the class
      - Remove `required this.bridgeTripId` from constructor
      - Remove `bridgeTripId: data['bridgeTripId'] as String? ?? doc.id` from `fromDoc`
      - Remove `'bridgeTripId': bridgeTripId` from `toFirestoreMap`
      - Remove from `copyWith`
    - Note: We do NOT remove `bridgeTripId` from Firestore documents — the field stays in existing docs for lazy migration. We just stop reading/writing it in the model.

    **2. Remove bridge from EventService** per D-17:
    - In `lib/features/events/services/event_service.dart`:
      - Remove `_createBridgeTrip()` method
      - Remove `_generateBridgeCode()` method
      - Remove the Supabase bridge creation from `createEvent()`
      - Remove `_skipBridgeInTest` flag and related logic
      - Remove imports: `supabase_config.dart`, `sync_service.dart`, `offline_repository.dart`, `cache_service.dart`
      - Remove the `bridgeTripId` from the event data map passed to Firestore (but keep it for reading old events)
      - The `SyncService.downloadTripData()` call in the bridge section — DELETE it
    - EventService.createEvent should now be simplified: just write the event to Firestore, then seed gear if camping. No bridge.

    **3. Update EventCommandCenter** to pass EventRef instead of Trip facade:
    - Remove the `_buildTripFacade()` method
    - Remove the `Trip` import
    - Instead of passing `trip` to module screens, pass `event` directly (and `group`)
    - Update child widget calls: `EventModuleList(event: event)` instead of `EventModuleList(event: event, trip: trip)`
    - The module screens (LedgerScreen, etc.) need to accept an Event or an EventRef instead of a Trip. This is the main screen refactor.

    **4. Update module screens to accept Event/EventRef instead of Trip:**

    For each screen that currently takes a `Trip trip` parameter and uses `trip.id` to call providers:

    **LedgerScreen:**
    - Change constructor from `LedgerScreen({required Trip trip})` to `LedgerScreen({required Event event, required Group group})`
    - Replace `tripExpensesProvider(widget.trip.id)` with `eventExpensesProvider((groupId: widget.event.groupId, eventId: widget.event.id))`
    - Replace `tripSettlementsProvider(widget.trip.id)` with `eventSettlementsProvider((groupId: widget.event.groupId, eventId: widget.event.id))`
    - Replace `tripSubGroupsProvider(widget.trip.id)` with `eventSubGroupsProvider((groupId: widget.event.groupId, eventId: widget.event.id))`

    **AddExpenseScreen:**
    - Change from `AddExpenseScreen({required String tripId})` to `AddExpenseScreen({required String groupId, required String eventId})`
    - Update the ExpenseService call: `ref.read(expenseServiceProvider).addExpense(groupId: widget.groupId, eventId: widget.eventId, ...)`

    **SettleUpScreen:**
    - Change from `Trip trip` to `Event event, Group group`
    - Update all provider calls to use EventRef

    **EditExpenseSheet:**
    - Change `tripId` to `groupId` + `eventId`
    - Update service calls

    **ActivityFeedScreen:**
    - Update provider from `tripActivityProvider` to `eventActivityProvider`

    **5. Update EventCard widget:**
    - Replace `tripExpensesProvider(event.bridgeTripId)` with `eventExpensesProvider((groupId: event.groupId, eventId: event.id))`

    **6. Update EventModuleList widget:**
    - Replace all `tripExpensesProvider(trip.id)`, `tripSubGroupsProvider(trip.id)`, `tripDocumentsProvider(trip.id)` with EventRef-based providers
    - Remove the `trip` parameter (use `event` only)

    **7. Update home widgets** (module_list.dart, expense_summary_hero.dart, trip_header.dart, trip_recap_card.dart):
    - These widgets reference `tripExpensesProvider(trip.id)` for OLD trip-based home screens. Check if these are still in use (home screen was replaced by groups-first layout in Phase 2). If they reference old Trip objects from pre-migration:
      - If the widgets are dead code (unused after Phase 2 home screen replacement): DELETE them
      - If still referenced: update to EventRef providers

    **8. Remove deprecated provider shims:**
    - In `expense_provider.dart`: remove `@Deprecated` `tripExpensesProvider` and `tripSettlementsProvider`
    - In `gear_provider.dart`: remove `@Deprecated` `tripGearItemsProvider`
    - In `sub_group_provider.dart`: remove `@Deprecated` `tripSubGroupsProvider`
    - In `activity_service.dart`: remove `@Deprecated` `tripActivityProvider` and `tripTransactionActivityProvider`
    - In `document_provider.dart`: remove `@Deprecated` `tripDocumentsProvider`

    **9. Verify no remaining references to old providers:**
    ```bash
    grep -rn 'tripExpensesProvider\|tripSettlementsProvider\|tripGearItemsProvider\|tripSubGroupsProvider\|tripActivityProvider\|tripDocumentsProvider\|tripMemoriesProvider' lib/
    ```
    This should return ZERO matches after all updates.
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla && flutter analyze && flutter test --no-pub</automated>
  </verify>
  <acceptance_criteria>
    - lib/features/events/models/event_model.dart does NOT contain `bridgeTripId`
    - lib/features/events/services/event_service.dart does NOT contain `_createBridgeTrip` or `_generateBridgeCode`
    - lib/features/events/services/event_service.dart does NOT contain `SupabaseConfig` or `SyncService`
    - lib/features/events/screens/event_command_center.dart does NOT contain `_buildTripFacade` or `Trip(`
    - grep -rn 'tripExpensesProvider' lib/ returns 0 matches
    - grep -rn 'tripSettlementsProvider' lib/ returns 0 matches
    - grep -rn 'tripGearItemsProvider' lib/ returns 0 matches
    - grep -rn 'tripSubGroupsProvider' lib/ returns 0 matches
    - grep -rn 'tripActivityProvider' lib/ returns 0 matches
    - grep -rn 'tripDocumentsProvider' lib/ returns 0 matches
    - grep -rn 'bridgeTripId' lib/ returns 0 matches (may exist in lazy_migration_service.dart for reading old Firestore docs — that is acceptable)
    - grep -rn 'FirebaseFirestore.instance' lib/ returns matches ONLY in firebase_config.dart (not in service files)
    - flutter analyze reports no errors
    - flutter test --no-pub passes (all tests)
  </acceptance_criteria>
  <done>Bridge pattern completely removed. All screens use EventRef-based Firestore providers. No deprecated provider shims remain. No direct FirebaseFirestore.instance usage outside FirebaseConfig. Full test suite passes.</done>
</task>

</tasks>

<verification>
- `flutter analyze` reports no errors across entire codebase
- `flutter test --no-pub` passes all tests
- `grep -rn 'SyncService' lib/` returns 0 matches
- `grep -rn 'offlineRepositoryProvider' lib/` returns 0 matches
- `grep -rn 'SupabaseClient' lib/features/ledger/ lib/features/gear/ lib/features/logistics/ lib/features/activity/ lib/features/vault/ lib/features/memories/` returns 0 matches
- `grep -rn 'bridgeTripId' lib/` returns 0 matches (except lazy_migration_service.dart)
- `grep -rn 'tripExpensesProvider\|tripSettlementsProvider\|tripGearItemsProvider\|tripSubGroupsProvider\|tripActivityProvider\|tripDocumentsProvider' lib/` returns 0 matches
- `grep -rn 'FirebaseFirestore.instance' lib/` returns matches only in `firebase_config.dart`
</verification>

<success_criteria>
- MIG-03 satisfied: SyncService and sync_queue are gone
- MIG-04 satisfied: SQLite serves balance queries via BalanceCacheRepository
- MIG-05 satisfied: All Firestore access flows through FirestoreRepository base class
- Bridge pattern completely removed (D-17)
- ConnectivityNotifier uses Firestore-based health check
- GroupService and EventService extend FirestoreRepository (D-06)
- All screens use EventRef-based providers — no deprecated shims remain
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-firestore-repository-layer/04-04-SUMMARY.md`
</output>
