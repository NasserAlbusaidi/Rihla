---
phase: 39
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/router/app_router.dart
  - lib/features/events/widgets/event_module_list.dart
  - lib/features/events/widgets/event_modules_card.dart
  - lib/features/events/models/event_model.dart
autonomous: true
requirements: [SHIP-01]
must_haves:
  truths:
    - "App compiles after this plan even though removed feature directories still exist on disk"
    - "No GoRoute matches `/onboarding`, `/event/:eid/{memories,vault,logistics,gear}` paths"
    - "EventCommandCenter renders only Ledger and Activity module cards"
    - "EventModules class exposes only `ledger` (and any survivors), all gear/logistics/vault/memories flags removed"
    - "App boots straight to /home with no onboarding redirect"
  artifacts:
    - path: "lib/core/router/app_router.dart"
      provides: "GoRouter route table without onboarding/memories/vault/logistics/gear routes"
      contains: "AppRoutes"
    - path: "lib/features/events/widgets/event_module_list.dart"
      provides: "Module grid that renders only Ledger + Activity cards"
    - path: "lib/features/events/models/event_model.dart"
      provides: "EventModules with only ledger field"
      contains: "class EventModules"
  key_links:
    - from: "lib/core/router/app_router.dart"
      to: "lib/features/home/screens/home_screen.dart"
      via: "splash redirect — onboardingCompleteProvider gone"
      pattern: "AppRoutes.home"
    - from: "lib/features/events/widgets/event_module_list.dart"
      to: "lib/features/ledger/screens/ledger_screen.dart"
      via: "GoRouter push to /ledger"
      pattern: "/ledger"
---

<objective>
Wave 1 of the strip phase: make the six cut features (memories, vault, logistics, onboarding, gear, multi-currency picker entry points) **unreachable** without deleting their source yet.

Purpose: per CONTEXT.md "Risk & Reversibility" guidance, destructive operations are staged. The router and CommandCenter are the user-facing entry points; once they're gone, deleting source in Wave 2 cannot break the app build mid-wave. Per D-CONTEXT-01 (the cut intent), the CommandCenter for an event must show only Ledger and Activity.

Output: app_router.dart with no routes for the cut features; EventModuleList that renders only Ledger + Activity; EventModules class pruned to just the surviving flags. Build still compiles because the source directories still exist (just nothing routes to them).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/39-strip-to-shippable-v1-remove-memories-vault-logistics-onboar/39-CONTEXT.md
@CLAUDE.md

@lib/core/router/app_router.dart
@lib/features/events/widgets/event_module_list.dart
@lib/features/events/widgets/event_modules_card.dart
@lib/features/events/models/event_model.dart
@lib/features/events/screens/event_command_center.dart

<interfaces>
<!-- Key types/exports the executor needs. Do NOT explore the codebase to find these. -->

From `lib/core/router/app_router.dart`:
- `class AppRoutes` — string constants: `splash='/'`, `onboarding='/onboarding'`, `home='/home'`, `eventGear='/group/:gid/event/:eid/gear'`, `eventLogistics='/group/:gid/event/:eid/logistics'`, `eventVault='/group/:gid/event/:eid/vault'`, `eventMemories`, `eventMemoryDetail`. **Remove the five constants for the cut features and their corresponding `GoRoute(path: '...')` blocks (sub-routes of `event/:eid`).**
- `final onboardingCompleteProvider = FutureProvider<bool>(...)` at line 88 — **delete entirely**.
- The `redirect:` callback (lines ~96-110) reads `onboardingCompleteProvider` to send users to /onboarding. **Replace logic so splash always redirects to AppRoutes.home (no onboarding branch)**.
- Imports to remove (lines ~9-25): `gear_screen.dart`, `logistics_screen.dart`, `memories_screen.dart`, `onboarding_screen.dart`, `vault_screen.dart`.

From `lib/features/events/models/event_model.dart`:
```dart
class EventModules {
  final bool ledger;
  final bool gear;
  final bool logistics;
  final bool vault;
  final bool memories;
  // ... constructors, fromJson, toJson, copyWith, forType
}
```
**Reduce to only `ledger` field. EventModules.forType(...) collapses — every event type is just `EventModules(ledger: true)`. Custom events keep the ability to disable ledger via copyWith if that exists in the codebase (verify — keep behavior identical for the surviving flag).**

From `lib/features/events/widgets/event_module_list.dart`:
- Imports gear/logistics/vault providers (lines 12-20). Remove all five module imports.
- Renders cards in fixed order: Ledger, Gear, Logistics, Vault, Activity, Memories. **Reduce to: Ledger + Activity only.**
- The `_addGearCard`, `_addLogisticsCard`, `_addVaultCard`, `_addMemoriesCard` private methods — delete them.

From `lib/features/events/widgets/event_modules_card.dart`:
- Renders four toggle SwitchListTiles for `gear`, `logistics`, `vault`, `memories` (lines ~55-95). **Remove these toggles. If the card now has zero toggles, the card itself becomes unnecessary — delete the file and remove its usage from any settings screen that imports it.**
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip cut routes + onboarding redirect from app_router.dart</name>
  <files>lib/core/router/app_router.dart</files>
  <read_first>
    - lib/core/router/app_router.dart (entire file, 467 lines)
    - lib/features/home/screens/home_screen.dart (just the class declaration — confirm AppRoutes.home target still works)
  </read_first>
  <action>
    Modify `lib/core/router/app_router.dart` in three contiguous edits:

    1. **Delete imports** for cut features. Remove these import lines:
       - `import '../../features/gear/screens/gear_screen.dart';`
       - `import '../../features/logistics/screens/logistics_screen.dart';`
       - `import '../../features/memories/screens/memories_screen.dart';`
       - `import '../../features/onboarding/screens/onboarding_screen.dart';`
       - `import '../../features/vault/screens/vault_screen.dart';`

    2. **Strip AppRoutes constants** for cut features (in the `class AppRoutes`):
       - `static const String onboarding = '/onboarding';`
       - `static const String eventGear = '/group/:gid/event/:eid/gear';`
       - `static const String eventLogistics = '/group/:gid/event/:eid/logistics';`
       - `static const String eventVault = '/group/:gid/event/:eid/vault';`
       - `static const String eventMemories = '/group/:gid/event/:eid/memories';`
       - `static const String eventMemoryDetail = '/group/:gid/event/:eid/memories/:memId';`

    3. **Delete `onboardingCompleteProvider`** (around line 88, the `FutureProvider<bool>` block).

    4. **Replace `redirect:` callback** in `routerProvider`. Current logic checks if on splash and redirects to onboarding-or-home. New logic: if `state.matchedLocation == AppRoutes.splash`, return `AppRoutes.home`. Drop the onboarding branch entirely. Keep no other redirect logic — any auth/group guards live elsewhere.

    5. **Delete the `/onboarding` GoRoute** block (the `GoRoute(path: AppRoutes.onboarding, ...)` ~lines 121-132).

    6. **Delete the four module sub-route blocks** under `path: 'event/:eid'`:
       - `GoRoute(path: 'gear', ...)` — about 10 lines
       - `GoRoute(path: 'logistics', ...)` — about 10 lines
       - `GoRoute(path: 'vault', ...)` — about 10 lines
       - `GoRoute(path: 'memories', ...)` — about 25 lines including the nested `:memId` route

    Surviving event sub-routes: `ledger` (and its nested add/edit/settle-up), `activity`, `settings`. Surviving top-level routes: splash, home, profile, activity, create-group, join-group, group/:gid (and its sub-routes that are NOT cut).

    DO NOT touch: `_slideRightTransition`, `_SplashScreen`, the `errorBuilder`, transitions, or any surviving GoRoute. This is delete-only — no refactoring imports, no reordering.
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla &amp;&amp; ! grep -E "(import.*\b(memories|vault|logistics|onboarding|gear)\b|GoRoute\(.*\b(memories|vault|logistics|onboarding|gear)\b|AppRoutes\.(eventGear|eventLogistics|eventVault|eventMemories|onboarding))" lib/core/router/app_router.dart &amp;&amp; flutter analyze lib/core/router/app_router.dart 2>&amp;1 | tee /tmp/router-analyze.txt &amp;&amp; ! grep -qE "error •" /tmp/router-analyze.txt</automated>
  </verify>
  <acceptance_criteria>
    - Structural grep returns zero matches: `grep -cE "(import.*\b(memories|vault|logistics|onboarding|gear)\b|GoRoute\(.*\b(memories|vault|logistics|onboarding|gear)\b|AppRoutes\.(eventGear|eventLogistics|eventVault|eventMemories|onboarding))" lib/core/router/app_router.dart` returns 0
    - `grep -c "OnboardingScreen\|MemoriesScreen\|VaultScreen\|GearScreen\|LogisticsScreen" lib/core/router/app_router.dart` returns 0
    - `flutter analyze lib/core/router/app_router.dart` reports zero errors (warnings about unused identifiers in OTHER files are OK; this file alone must be clean)
  </acceptance_criteria>
  <done>
    app_router.dart contains no references to cut features; routerProvider redirects splash → /home unconditionally; flutter analyze on this single file is clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Reduce EventModules class to only `ledger` flag</name>
  <files>lib/features/events/models/event_model.dart</files>
  <read_first>
    - lib/features/events/models/event_model.dart (read the full `EventModules` class — line 34 onward, plus the `forType` factory and `copyWith`)
  </read_first>
  <action>
    In `lib/features/events/models/event_model.dart`, edit the `EventModules` class (starting around line 34):

    1. **Remove fields**: `gear`, `logistics`, `vault`, `memories`. Keep only `final bool ledger;`.

    2. **Update default constructor**: `const EventModules({this.ledger = true});`

    3. **Update `EventModules.forType(EventType type)`**: every case returns `const EventModules(ledger: true)` EXCEPT preserve the existing `nightDayOut` semantics (it was `ledger: true` already — just drop the other false fields). Effectively: `factory EventModules.forType(EventType type) => const EventModules(ledger: true);` — single-line.

    4. **Update fromJson/fromMap**: `factory EventModules.fromMap(Map<String, dynamic> json) => EventModules(ledger: json['ledger'] as bool? ?? true);` Drop reads for the removed keys (silently ignored — back-compat with persisted Firestore docs that still have those keys).

    5. **Update toJson/toMap**: `Map<String, dynamic> toMap() => {'ledger': ledger};` Drop the removed keys.

    6. **Update copyWith**: `EventModules copyWith({bool? ledger}) => EventModules(ledger: ledger ?? this.ledger);`

    7. Update any `==` / `hashCode` overrides if present to reference only `ledger`.

    DO NOT touch the surrounding `Event` class, `EventType` enum, or `currency` field on Event in this task. Currency-field removal happens in plan 39-03 task 4.
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla &amp;&amp; awk '/^class EventModules/,/^}/' lib/features/events/models/event_model.dart | tee /tmp/event-modules.txt &amp;&amp; ! grep -qE "\bgear\b|\blogistics\b|\bvault\b|\bmemories\b" /tmp/event-modules.txt &amp;&amp; flutter analyze lib/features/events/models/event_model.dart 2>&amp;1 | tee /tmp/em-analyze.txt &amp;&amp; ! grep -qE "error •" /tmp/em-analyze.txt</automated>
  </verify>
  <acceptance_criteria>
    - `awk '/^class EventModules/,/^}/' lib/features/events/models/event_model.dart | grep -cE "gear|logistics|vault|memories"` returns 0
    - The EventModules block contains references to `ledger` in: field declaration, default constructor, fromMap, toMap, copyWith
    - `flutter analyze lib/features/events/models/event_model.dart` reports zero errors local to this file
  </acceptance_criteria>
  <done>
    EventModules has a single `ledger` field; forType always returns `EventModules(ledger: true)`; fromMap tolerates legacy keys silently.
  </done>
</task>

<task type="auto">
  <name>Task 3: Strip event_module_list.dart and delete event_modules_card.dart</name>
  <files>
    lib/features/events/widgets/event_module_list.dart
    lib/features/events/widgets/event_modules_card.dart
  </files>
  <read_first>
    - lib/features/events/widgets/event_module_list.dart (entire file — has private `_addGearCard`, `_addLogisticsCard`, `_addVaultCard`, `_addMemoriesCard` methods)
    - lib/features/events/widgets/event_modules_card.dart (entire file)
    - lib/features/events/screens/event_command_center.dart (find any import of `event_modules_card.dart` to confirm callers)
    - grep usages: `grep -rn "EventModulesCard\|event_modules_card" lib/` to find every call site
  </read_first>
  <action>
    **Part A — `event_module_list.dart`:**

    1. **Delete imports** for cut providers/models (lines 12-20 area):
       - `../../gear/models/gear_item_model.dart`
       - `../../gear/providers/gear_provider.dart`
       - `../../logistics/models/sub_group_model.dart`
       - `../../logistics/providers/sub_group_provider.dart`
       - `../../vault/models/document_model.dart`
       - `../../vault/providers/document_provider.dart`
       (Keep ledger imports, decimal, formatters, smart_module_card, event_keys, EventModules.)

    2. **Inside `build()`**: remove `showGear`, `showLogistics`, `showVault`, `showMemories` locals and their conditional `ref.watch(...)` calls (`gearAsync`, `subGroupsAsync`, `docsAsync`).

    3. **Remove the conditional card-add blocks** for gear, logistics, vault, memories. Card list reduces to: Ledger + Activity. Order in CONTEXT.md says "Ledger (and Activity if kept at event level)" — preserve the Activity card exactly as it currently is.

    4. **Delete the private methods**: `_addGearCard`, `_addLogisticsCard`, `_addVaultCard`, `_addMemoriesCard`. Keep `_addLedgerCard` and `_addActivityCard` (or whatever the activity helper is called).

    5. **Update class docstring** that says "2x3 grid" → "list of module cards" (or whatever fits — minimal phrasing change, this is the only doc edit allowed).

    **Part B — `event_modules_card.dart`:**

    The four toggles in this file were for gear, logistics, vault, memories. With those flags removed from `EventModules`, the card has no toggles left. Delete the entire file:

    ```bash
    rm /Users/nasseralbusaidi/Desktop/Personal/Rihla/lib/features/events/widgets/event_modules_card.dart
    ```

    Then update every caller (likely `event_settings_screen.dart` and possibly `event_command_center.dart`):

    - Remove `import '../widgets/event_modules_card.dart';`
    - Remove the `EventModulesCard(...)` widget instance from the build method. If that leaves an empty `Padding` or `Column` child, drop the wrapper too. Do NOT replace with anything — the cut intent is "no module toggles remain".

    Use `grep -rn "EventModulesCard\|event_modules_card" lib/` to find every caller and edit each one.
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla &amp;&amp; ! test -f lib/features/events/widgets/event_modules_card.dart &amp;&amp; ! grep -qE "(gear|logistics|vault|memories)" lib/features/events/widgets/event_module_list.dart &amp;&amp; ! grep -rqE "EventModulesCard|event_modules_card" lib/ &amp;&amp; flutter analyze lib/features/events/ 2>&amp;1 | tee /tmp/events-analyze.txt &amp;&amp; ! grep -qE "error •" /tmp/events-analyze.txt</automated>
  </verify>
  <acceptance_criteria>
    - `! test -f lib/features/events/widgets/event_modules_card.dart`
    - `grep -c "gear\|logistics\|vault\|memories" lib/features/events/widgets/event_module_list.dart` returns 0
    - `grep -rc "EventModulesCard" lib/` returns 0
    - `flutter analyze lib/features/events/` reports zero errors
  </acceptance_criteria>
  <done>
    event_module_list.dart renders only Ledger + Activity cards; event_modules_card.dart is deleted with no surviving import; flutter analyze on lib/features/events/ is clean.
  </done>
</task>

</tasks>

<verification>
After all three tasks complete, run from the repo root:

```bash
flutter analyze lib/core/router/ lib/features/events/ 2>&1 | tail -20
```

Expected: zero `error •` lines. Warnings about UNUSED identifiers in other files (e.g., dead `gear_provider`, `OnboardingScreen` class) are EXPECTED — those files get deleted in Wave 2 plan (39-02).

A second confirmation:

```bash
flutter run --dart-define-from-file=config.json
# Manually: app should boot to /home (no onboarding screen). Tap into a group, create event, the EventCommandCenter shows only Ledger + Activity cards (no Gear/Logistics/Vault/Memories tiles).
```

This manual smoke is optional for this plan; full smoke test happens in plan 39-07.
</verification>

<success_criteria>
- `flutter analyze lib/core/router/ lib/features/events/` reports zero errors
- App still compiles (Wave 2 will report new errors from now-orphaned source files; that is expected)
- Routes to /onboarding, /event/:eid/{gear,logistics,vault,memories} return GoRouter 404
- EventCommandCenter shows only Ledger + Activity module cards
</success_criteria>

<output>
After completion, create `.planning/phases/39-strip-to-shippable-v1-remove-memories-vault-logistics-onboar/39-01-SUMMARY.md` documenting:
- Exact lines/blocks deleted from app_router.dart
- EventModules field reduction
- event_modules_card.dart deletion + caller updates
- Confirmation that `flutter analyze` on touched files is clean
</output>
</content>
