---
phase: 39
plan: 02
type: execute
wave: 2
depends_on: ["39-01"]
files_modified:
  - lib/features/memories/
  - lib/features/vault/
  - lib/features/logistics/
  - lib/features/gear/
  - lib/features/onboarding/
  - test/features/gear/
  - test/features/logistics/
  - test/features/memories/
  - test/features/vault/
  - test/features/gear_screen_mutations_test.dart
  - test/features/logistics_screen_mutations_test.dart
  - test/features/memories_screen_mutations_test.dart
  - test/features/vault_screen_mutations_test.dart
  - test/goldens/onboarding_golden_test.dart
  - test/goldens/goldens/onboarding_light.png
  - test/goldens/goldens/onboarding_dark.png
autonomous: true
requirements: [SHIP-01]
must_haves:
  truths:
    - "Five feature directories no longer exist on disk"
    - "Test files for the five removed features no longer exist"
    - "Onboarding golden test artifacts (PNGs + golden test) are deleted"
    - "Build errors from now-orphaned imports are catalogued (chasing happens in Wave 3)"
  artifacts:
    - path: ".planning/phases/39-strip-to-shippable-v1-remove-memories-vault-logistics-onboar/39-02-SUMMARY.md"
      provides: "List of every file deleted and the orphaned-import errors that need Wave 3 cleanup"
      contains: "deleted directories"
  key_links: []
---

<objective>
Wave 2: physically delete the five cut feature directories and their tests. After this plan, `flutter analyze` will report many errors — those are catalogued in the SUMMARY for Wave 3 (plan 39-03 / 39-04) to clean up.

Purpose: per CONTEXT.md, this phase is destructive. Wave 1 made the routes unreachable; Wave 2 reclaims the disk. The order matters — if we deleted before stripping routes, the build broke at Wave 1 mid-edit.

Output: `lib/features/{memories,vault,logistics,gear,onboarding}/` and matching test/ paths gone. A SUMMARY listing every orphaned-import file path, so the next plan can fix them mechanically without re-grepping.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/39-strip-to-shippable-v1-remove-memories-vault-logistics-onboar/39-CONTEXT.md
@.planning/phases/39-strip-to-shippable-v1-remove-memories-vault-logistics-onboar/39-01-SUMMARY.md

<interfaces>
<!-- No new interfaces created. This plan deletes existing source. -->
<!-- Files known to import from cut features (will produce errors after deletion — catalog these in SUMMARY for plan 39-03 to fix): -->

- lib/core/services/cache/gear_cache_repository.dart — imports gear models
- lib/core/services/sync_service.dart (likely) — references gear_items, documents tables
- lib/core/services/local_database.dart — has CREATE TABLE gear_items (handled in plan 39-06)
- Any provider/service in surviving features that imported from gear/logistics/vault/memories
- README files in surviving features that reference cut modules
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the five Flutter feature directories</name>
  <files>
    lib/features/memories/
    lib/features/vault/
    lib/features/logistics/
    lib/features/gear/
    lib/features/onboarding/
  </files>
  <read_first>
    - Run `ls lib/features/` to confirm the five directories exist before deletion (Wave 1 SUMMARY should also confirm)
  </read_first>
  <action>
    Delete the five feature directories with `rm -rf`:

    ```bash
    cd /Users/nasseralbusaidi/Desktop/Personal/Rihla
    rm -rf lib/features/memories
    rm -rf lib/features/vault
    rm -rf lib/features/logistics
    rm -rf lib/features/gear
    rm -rf lib/features/onboarding
    ```

    DO NOT delete: `lib/features/auth/`, `lib/features/events/`, `lib/features/groups/`, `lib/features/home/`, `lib/features/ledger/`, `lib/features/settings/`, `lib/features/activity/`, `lib/features/trip/`. (`trip/` survives — it contains the `Trip` and `TripModules` model still consumed by sub-group infrastructure; pruning happens in plan 39-04.)

    Verify deletion immediately:

    ```bash
    ls lib/features/  # Should show: activity auth events groups home ledger settings trip (8 dirs)
    ```
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla &amp;&amp; ! test -d lib/features/memories &amp;&amp; ! test -d lib/features/vault &amp;&amp; ! test -d lib/features/logistics &amp;&amp; ! test -d lib/features/gear &amp;&amp; ! test -d lib/features/onboarding &amp;&amp; for d in activity auth events groups home ledger settings trip; do test -d lib/features/$d || { echo "MISSING $d"; exit 1; }; done</automated>
  </verify>
  <acceptance_criteria>
    - `! test -d lib/features/memories`
    - `! test -d lib/features/vault`
    - `! test -d lib/features/logistics`
    - `! test -d lib/features/gear`
    - `! test -d lib/features/onboarding`
    - All eight surviving features present (positive assertion):
      ```
      for d in activity auth events groups home ledger settings trip; do
        test -d lib/features/$d || exit 1
      done
      ```
  </acceptance_criteria>
  <done>
    Five feature directories gone; eight survive (activity, auth, events, groups, home, ledger, settings, trip).
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete tests for removed features</name>
  <files>
    test/features/gear/
    test/features/logistics/
    test/features/memories/
    test/features/vault/
    test/features/gear_screen_mutations_test.dart
    test/features/logistics_screen_mutations_test.dart
    test/features/memories_screen_mutations_test.dart
    test/features/vault_screen_mutations_test.dart
    test/goldens/onboarding_golden_test.dart
    test/goldens/goldens/onboarding_light.png
    test/goldens/goldens/onboarding_dark.png
  </files>
  <read_first>
    - Run `ls test/features/` to confirm directory layout before deletion
    - Run `ls test/goldens/` and `ls test/goldens/goldens/ | grep onboarding` to confirm onboarding goldens
  </read_first>
  <action>
    Delete test directories AND the four `*_screen_mutations_test.dart` files at the top of `test/features/` (they are siblings to the directories, not inside them):

    ```bash
    cd /Users/nasseralbusaidi/Desktop/Personal/Rihla
    rm -rf test/features/gear
    rm -rf test/features/logistics
    rm -rf test/features/memories
    rm -rf test/features/vault
    rm test/features/gear_screen_mutations_test.dart
    rm test/features/logistics_screen_mutations_test.dart
    rm test/features/memories_screen_mutations_test.dart
    rm test/features/vault_screen_mutations_test.dart
    ```

    Delete onboarding golden artifacts:

    ```bash
    rm test/goldens/onboarding_golden_test.dart
    rm test/goldens/goldens/onboarding_light.png
    rm test/goldens/goldens/onboarding_dark.png
    ```

    DO NOT delete: `test/features/groups/`, `test/features/events/`, `test/features/home/`, `test/features/ledger/`, `test/features/settings/`, `test/features/profile/`, `test/features/shared_widgets/`, or top-level `group_*` and `ledger_test.dart` files.

    Note: there's no onboarding test directory (only the golden file). Verify with `find test -name "*onboarding*"` — should return nothing after deletion.
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla &amp;&amp; ! test -d test/features/gear &amp;&amp; ! test -d test/features/logistics &amp;&amp; ! test -d test/features/memories &amp;&amp; ! test -d test/features/vault &amp;&amp; ! test -f test/features/gear_screen_mutations_test.dart &amp;&amp; ! test -f test/features/logistics_screen_mutations_test.dart &amp;&amp; ! test -f test/features/memories_screen_mutations_test.dart &amp;&amp; ! test -f test/features/vault_screen_mutations_test.dart &amp;&amp; ! test -f test/goldens/onboarding_golden_test.dart &amp;&amp; ! test -f test/goldens/goldens/onboarding_light.png &amp;&amp; ! test -f test/goldens/goldens/onboarding_dark.png &amp;&amp; [ -z "$(find test -name '*onboarding*' 2&gt;/dev/null)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `find test -name "*onboarding*"` returns no output
    - `find test -name "*memories*"` returns no output
    - `find test -name "*vault*"` returns no output
    - `find test/features -maxdepth 1 -name "*gear*" -o -name "*logistics*"` returns no output
  </acceptance_criteria>
  <done>
    All test files / directories for removed features are gone; onboarding goldens deleted.
  </done>
</task>

<task type="auto">
  <name>Task 3: Catalog orphaned imports for plan 39-03 to fix</name>
  <files>
    .planning/phases/39-strip-to-shippable-v1-remove-memories-vault-logistics-onboar/39-02-SUMMARY.md
  </files>
  <read_first>
    - None — this is pure investigation
  </read_first>
  <action>
    Run `flutter analyze 2>&1 | tee /tmp/post-delete-analyze.txt` from the repo root.

    Then capture the output into a SUMMARY file at `.planning/phases/39-strip-to-shippable-v1-remove-memories-vault-logistics-onboar/39-02-SUMMARY.md` with three sections:

    1. **Deleted directories and files** — bullet list of every directory/file removed in tasks 1 & 2 (you can copy from the task descriptions).

    2. **Orphaned-import errors** — pipe analyzer output through:
       ```bash
       flutter analyze 2>&1 | grep -E "(error|Target of URI doesn't exist|Undefined name|Couldn't resolve)" | sort -u | tee /tmp/orphaned.txt
       ```
       Paste the catalog into the SUMMARY. Group by file (one line per file), with the error type. Example:
       ```
       - lib/core/services/cache/gear_cache_repository.dart — imports gear_item_model.dart (deleted)
       - lib/core/services/local_database.dart — references gear_items table (no error, schema cleanup is plan 39-06)
       ```

    3. **Files needing edits in plan 39-03/39-04** — derive a clean list of unique file paths from #2. This is the input list for the next wave's work.

    DO NOT attempt to fix any errors in this plan. Wave 2 is delete-only — the analyze output is the exit artifact, not the entry to a fix loop. Wave 3 plans will use this catalog.

    If `flutter analyze` reports zero errors (unlikely but possible), still write the SUMMARY and note "no orphaned imports detected — plan 39-03 may be a no-op for orphan cleanup".
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla &amp;&amp; test -f .planning/phases/39-strip-to-shippable-v1-remove-memories-vault-logistics-onboar/39-02-SUMMARY.md &amp;&amp; grep -q "Orphaned-import" .planning/phases/39-strip-to-shippable-v1-remove-memories-vault-logistics-onboar/39-02-SUMMARY.md &amp;&amp; grep -q "Files needing edits" .planning/phases/39-strip-to-shippable-v1-remove-memories-vault-logistics-onboar/39-02-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - 39-02-SUMMARY.md exists and contains all three required sections
    - At minimum, lists every directory deleted in tasks 1 & 2
    - Orphaned-import section is either populated OR explicitly notes "no orphans detected"
  </acceptance_criteria>
  <done>
    SUMMARY documents the destructive operations and provides a clean list of import-fix targets for the next wave. The SUMMARY is the handoff to plan 39-03.
  </done>
</task>

</tasks>

<verification>
```bash
# Confirm directory absence
ls lib/features/  # Expect: activity auth events groups home ledger settings trip
ls test/features/ # Expect: events groups home ledger profile settings shared_widgets + group_*.dart + ledger_test.dart

# Capture analyze state for handoff
flutter analyze 2>&1 | grep -c "error •"  # Number is informational; will be > 0 due to orphaned imports
```

Expected: directories absent. Errors expected from now-orphaned imports — those are the input to plan 39-03 / 39-04 and are documented in the SUMMARY.
</verification>

<success_criteria>
- Five feature dirs absent from `lib/features/`
- Test counterparts absent from `test/features/` and `test/goldens/`
- 39-02-SUMMARY.md catalogs orphaned imports for the next wave
- No accidental deletion of surviving features (auth, events, groups, home, ledger, settings, activity, trip)
</success_criteria>

<output>
`.planning/phases/39-strip-to-shippable-v1-remove-memories-vault-logistics-onboar/39-02-SUMMARY.md` containing the deletion list and orphaned-import catalog.
</output>
</content>
