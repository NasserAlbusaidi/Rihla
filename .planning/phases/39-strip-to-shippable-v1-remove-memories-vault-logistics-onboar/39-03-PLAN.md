---
phase: 39
plan: 03
type: execute
wave: 3
depends_on: ["39-02"]
files_modified:
  - lib/features/ledger/services/thawani_service.dart
  - lib/features/ledger/README.md
  - lib/features/ledger/screens/settle_up_screen.dart
  - lib/features/ledger/screens/add_expense_screen.dart
  - lib/features/ledger/screens/edit_expense_screen.dart
  - lib/features/ledger/screens/ledger_screen.dart
  - lib/features/groups/screens/create_group_screen.dart
  - lib/features/groups/widgets/group_info_section.dart
  - lib/features/trip/models/trip_model.dart
  - lib/features/events/models/event_model.dart
  - lib/features/events/screens/event_expense_hero.dart
  - lib/features/events/widgets/event_card.dart
  - lib/core/services/cache/trip_cache_repository.dart
  - lib/core/services/cache/gear_cache_repository.dart
  - lib/core/services/sync_service.dart
  - lib/core/services/cache_service.dart
autonomous: true
requirements: [SHIP-01]
must_haves:
  truths:
    - "thawani_service.dart no longer exists"
    - "No code path imports `package:thawani_payment`"
    - "Currency picker UI removed from create_group_screen.dart and group_info_section.dart"
    - "Trip.currency / base_currency field removed from trip_model.dart per ROADMAP SC2"
    - "Event.currency field removed from event_model.dart"
    - "All ledger widgets receive the literal 'OMR' (or AppFormatters default) — no event.currency / trip.currency reads remain"
    - "All orphaned imports identified in 39-02-SUMMARY are fixed"
    - "flutter analyze reports zero errors after this plan"
  artifacts:
    - path: "lib/features/ledger/screens/settle_up_screen.dart"
      provides: "Settle-up screen with no Thawani option (mark-as-settled only)"
    - path: "lib/features/groups/screens/create_group_screen.dart"
      provides: "Create-group screen with no currency picker (always OMR)"
    - path: "lib/features/trip/models/trip_model.dart"
      provides: "Trip model without currency field"
      contains: "class Trip"
    - path: "lib/features/events/models/event_model.dart"
      provides: "Event model without currency field"
      contains: "class Event"
  key_links:
    - from: "lib/features/groups/screens/create_group_screen.dart"
      to: "lib/features/groups/services/group_service.dart"
      via: "create() call passes currency: 'OMR' literal"
      pattern: "currency: 'OMR'"
---

<objective>
Wave 3a: remove Thawani payment integration, delete the currency picker UI, **remove the `currency`/`base_currency` fields from Trip and Event models per ROADMAP SC2**, and clean up every orphaned import catalogued by plan 39-02.

Purpose: per CONTEXT.md cut #6 (multi-currency + Thawani) and ROADMAP SC2 ("`base_currency` field on trips removed"), Rihla is an OMR-only group ledger. The data field MUST go — not just the UI. "Mark as settled" replaces the payment processor. Per the **Wave 3 — Prune cross-feature references** instruction, this plan also fixes any provider/service that imported from now-deleted feature dirs.

Output: Thawani service deleted, currency picker UI gone, Trip and Event models stripped of `currency` field, all ledger widgets fed the literal `'OMR'` at the call site, all `flutter analyze` errors from Wave 2 deletions resolved.

Note: `Group.currency` is NOT removed — group is the unit at which a currency is displayed in the UI, and CONTEXT.md cut #6 names only Trip's `base_currency`. The Group model still defaults to `'OMR'` and is read-only in v1 after this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/39-strip-to-shippable-v1-remove-memories-vault-logistics-onboar/39-CONTEXT.md
@.planning/phases/39-strip-to-shippable-v1-remove-memories-vault-logistics-onboar/39-02-SUMMARY.md

@lib/features/ledger/services/thawani_service.dart
@lib/features/ledger/screens/settle_up_screen.dart
@lib/features/groups/screens/create_group_screen.dart
@lib/features/groups/widgets/group_info_section.dart
@lib/features/trip/models/trip_model.dart
@lib/features/events/models/event_model.dart

<interfaces>
<!-- Confirmed callers (established by grep before plan was written): -->

`ThawaniService.paySettlement(...)` — defined in `lib/features/ledger/services/thawani_service.dart`. Call sites must be found via:
```bash
grep -rn "ThawaniService\|paySettlement\|thawani" lib/ test/
```

`create_group_screen.dart` — has `_selectedCurrency` state (line 32), Currency label/selector at lines 169-186 (a DropdownButtonFormField likely). The currency selector is the only UI control to remove.

`group_info_section.dart` — confirmed structure (read at planning time):
- Line 35: `static const _currencies = ['OMR', 'USD', 'EUR', 'GBP', 'SAR', 'AED', 'KWD', 'BHD', 'QAR'];` — DELETE
- Lines 93-146: `Future<void> _showCurrencyPicker(BuildContext ctx)` — entire method DELETE (the bottom sheet that lists 9 currencies and writes via `groupServiceProvider.updateGroup(currency: c)`)
- Lines 167: call site in `build()` `_buildCurrencyTile()` — DELETE this line and the surrounding Divider (line 168) since the divider was between currency tile and invite-code tile
- Lines 334-388: `Widget _buildCurrencyTile()` — entire method DELETE
- Line 376: `Text(widget.group.currency, ...)` — gone with the deleted method
- This is an interactive picker (GestureDetector → showModalBottomSheet → updateGroup). Per BLOCKER fix this entire interactive path is removed

**Trip.currency removal — confirmed structure:**
File: `lib/features/trip/models/trip_model.dart`
- Line 13: `final String currency;` — DELETE
- Line 26: `this.currency = 'OMR',` (constructor) — DELETE
- Line 50: `currency: json['currency'] as String? ?? 'OMR',` (fromJson) — DELETE (silently ignored on read)
- Line 63: `'currency': currency,` (toJson) — DELETE (no longer written)
- Line 85: `String? currency,` (copyWith param) — DELETE
- Line 98: `currency: currency ?? this.currency,` (copyWith body) — DELETE

**Event.currency removal — confirmed structure:**
File: `lib/features/events/models/event_model.dart`
- Line 165: `final String currency;` — DELETE
- Line 183: `this.currency = 'OMR',` (constructor) — DELETE
- Line 246: `currency: data['currency'] as String? ?? 'OMR',` (fromFirestore) — DELETE
- Line 271: `'currency': currency,` (toFirestoreMap) — DELETE
- Line 305: `currency: currency,` (copyWith body) — DELETE

**Event.currency / Trip.currency call sites (must update at the boundary, hardcode 'OMR'):**
- lib/core/services/cache/trip_cache_repository.dart:36 — `'currency': trip.currency` → DELETE the map entry
- lib/features/ledger/screens/edit_expense_screen.dart:65 — `?.currency ?? ...` → replace with literal `'OMR'`
- lib/features/ledger/screens/ledger_screen.dart:314 — `final currency = event.currency as String? ?? 'OMR';` → `const currency = 'OMR';`
- lib/features/ledger/screens/settle_up_screen.dart — 8 call sites (lines 228, 247, 259, 270, 287, 296, 375, 423) → replace `event.currency` with `'OMR'`
- lib/features/ledger/screens/add_expense_screen.dart:78 — `?.currency ?? 'OMR'` → just `'OMR'`
- lib/features/events/screens/event_expense_hero.dart:110 — `event.currency` → `'OMR'`
- lib/features/events/widgets/event_card.dart:141, 145, 168, 178 — `event.currency` → `'OMR'`

Group.currency call sites (these REMAIN — Group keeps its currency field):
- lib/features/groups/screens/group_detail_screen.dart, group_settle_up_screen.dart, settle_up_tab_layout.dart, group_info_section.dart (read display only after picker removal), record_payment_sheet.dart, etc. — DO NOT TOUCH

**Thawani transitive imports** to scrub:
- `package:thawani_payment/thawani_payment.dart`
- `package:thawani_payment/models/products.dart`
- `package:thawani_payment/models/create.dart`

**Orphaned imports from Wave 2** (from 39-02-SUMMARY — likely candidates):
- `lib/core/services/cache/gear_cache_repository.dart` — imports gear models from deleted dir
- `lib/core/services/sync_service.dart` — sync logic for gear_items, documents
- `lib/core/services/cache_service.dart` — wrapper over cache repos

Use the SUMMARY's "Files needing edits" list as the authoritative input. Do not invent additional files.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete thawani_service.dart and remove all Thawani usages</name>
  <files>
    lib/features/ledger/services/thawani_service.dart
    lib/features/ledger/README.md
    lib/features/ledger/screens/settle_up_screen.dart
  </files>
  <read_first>
    - lib/features/ledger/services/thawani_service.dart (entire file — confirm class boundary)
    - lib/features/ledger/README.md (find the line referencing thawani_service)
    - Run `grep -rn "ThawaniService\|thawani_service\|thawani_payment" lib/ test/` and read every match
  </read_first>
  <action>
    1. **Delete the service file**:
       ```bash
       rm /Users/nasseralbusaidi/Desktop/Personal/Rihla/lib/features/ledger/services/thawani_service.dart
       ```

    2. **Remove the README line**: edit `lib/features/ledger/README.md`, delete line 19 (`- **thawani_service.dart**: Payment gateway (Oman-based)`). Do not reformat the README; just delete that one bullet. If removing leaves an empty section header, also remove the now-empty section.

    3. **Strip every caller**: for each match returned by `grep -rn "ThawaniService\|paySettlement.*thawani" lib/`:
       - If a screen imports `'../services/thawani_service.dart'` or `package:thawani_payment/...`, delete those imports.
       - If a button/widget invokes `ThawaniService.paySettlement(...)`, delete the entire button widget (a "Pay Online" button or similar). The remaining settle-up flow is the existing "mark as settled" record-keeping action — it stays untouched.
       - If a settle-up dialog has a "Pay with Thawani" branch, delete the branch and its conditional logic. Default the flow to "mark as settled".

    4. **DO NOT touch** the surviving SettleUpScreen logic for recording settlements via `SettlementService` / `settle-up` Firestore writes. Mark-as-settled is the ONLY settle action now. Don't refactor the surviving code; just remove the Thawani path.
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla &amp;&amp; ! test -f lib/features/ledger/services/thawani_service.dart &amp;&amp; ! grep -rqE "ThawaniService|thawani_service|thawani_payment" lib/ test/ &amp;&amp; flutter analyze lib/features/ledger/ 2>&amp;1 | grep -v "^$" | tee /tmp/ledger-analyze.txt &amp;&amp; ! grep -qE "error •" /tmp/ledger-analyze.txt</automated>
  </verify>
  <acceptance_criteria>
    - `! test -f lib/features/ledger/services/thawani_service.dart`
    - `grep -rcE "ThawaniService|thawani_service|thawani_payment" lib/ test/ 2>/dev/null | grep -v ":0$"` returns no output
    - `flutter analyze lib/features/ledger/` reports zero errors
    - `grep -c "thawani" lib/features/ledger/README.md` returns 0
  </acceptance_criteria>
  <done>
    Thawani is wholly removed from the codebase: file gone, every import scrubbed, README mention deleted, settle-up flow unchanged except for the deleted Pay Online branch.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove currency picker UI from create_group_screen.dart and group_info_section.dart</name>
  <files>
    lib/features/groups/screens/create_group_screen.dart
    lib/features/groups/widgets/group_info_section.dart
  </files>
  <read_first>
    - lib/features/groups/screens/create_group_screen.dart (full file — find the DropdownButtonFormField currency picker lines 169-186 and the `_selectedCurrency` state)
    - lib/features/groups/widgets/group_info_section.dart (full file)
  </read_first>
  <action>
    **Part A — `create_group_screen.dart`:**

    1. Delete the `String _selectedCurrency = 'OMR';` state field at line 32.

    2. Replace `currency: _selectedCurrency,` at line ~69 in the `create(...)` call with `currency: 'OMR',` (literal).

    3. Delete the Currency label widget (line ~171) and the currency selector (lines ~176-188). The currency-related UI block is the contiguous group: a label `Text('Currency', ...)`, optional `SizedBox`, and the `DropdownButtonFormField`/picker. Remove all of it cleanly. Do NOT replace with a "Currency: OMR" read-only label — the cut intent is "no picker, no label, just OMR everywhere by default".

    4. If the form column now has a stray `SizedBox` separator that was between the currency picker and the next field, delete it too — keep the layout tight.

    **Part B — `group_info_section.dart` (concrete, read-confirmed diffs):**

    Per WARNING 8 fix, here are the EXACT edits — these are interactive currency-picker code paths, all DELETE:

    1. **Delete the `_currencies` constant list** (line 35-45):
       ```dart
       static const _currencies = [
         'OMR', 'USD', 'EUR', 'GBP', 'SAR', 'AED', 'KWD', 'BHD', 'QAR',
       ];
       ```

    2. **Delete the `_showCurrencyPicker` method** (lines 93-146 — the entire method):
       ```dart
       Future<void> _showCurrencyPicker(BuildContext ctx) async {
         await showModalBottomSheet<void>(
           context: ctx,
           // ... 50+ lines including the ListTile loop with onTap that calls
           // ref.read(groupServiceProvider).updateGroup(currency: c)
         );
       }
       ```
       This method is the interactive picker — it MUST go.

    3. **Delete the call to `_buildCurrencyTile()` and its surrounding Divider** in the `build` method (around lines 167-168):
       ```dart
       _buildCurrencyTile(),
       Divider(height: 1, color: context.colors.inputFill),
       ```
       Result: the Column children become `[_buildGroupNameTile(), Divider, _buildInviteCodeTile()]` (one less divider, one less tile).

    4. **Delete the `_buildCurrencyTile()` method** (lines 334-388 — entire method):
       ```dart
       Widget _buildCurrencyTile() {
         return GestureDetector(
           key: GroupKeys.settingsCurrencyTile,
           onTap: () { HapticService.selection(); _showCurrencyPicker(context); },
           // ... renders Currency label + widget.group.currency value + arrow icon
         );
       }
       ```

    5. **Verify** there are no remaining references to `_currencies`, `_showCurrencyPicker`, `_buildCurrencyTile`, or `GroupKeys.settingsCurrencyTile` in the file. Each was used exactly once internally.

    6. **DO NOT** remove the `widget.group.currency` references in OTHER files (group_detail_screen, settle_up_tab_layout, etc.) — those read displays remain. Group.currency stays as a model field.

    DO NOT modify the `Group` model class, `GroupService.create()`, or any Firestore writes. Those are out of scope for this plan.
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla &amp;&amp; ! grep -qE "_selectedCurrency|DropdownButtonFormField.*Currency|Text\('Currency'" lib/features/groups/screens/create_group_screen.dart &amp;&amp; grep -q "currency: 'OMR'" lib/features/groups/screens/create_group_screen.dart &amp;&amp; ! grep -qE "_currencies|_showCurrencyPicker|_buildCurrencyTile|settingsCurrencyTile" lib/features/groups/widgets/group_info_section.dart &amp;&amp; flutter analyze lib/features/groups/ 2>&amp;1 | tee /tmp/groups-analyze.txt &amp;&amp; ! grep -qE "error •" /tmp/groups-analyze.txt</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "_selectedCurrency" lib/features/groups/screens/create_group_screen.dart` returns 0
    - `grep -c "currency: 'OMR'" lib/features/groups/screens/create_group_screen.dart` returns ≥ 1
    - `grep -cE "_currencies|_showCurrencyPicker|_buildCurrencyTile" lib/features/groups/widgets/group_info_section.dart` returns 0
    - `flutter analyze lib/features/groups/` reports zero errors
  </acceptance_criteria>
  <done>
    Currency picker UI is removed from create-group flow; create call uses literal 'OMR'; group_info_section has no interactive currency code paths; group_info_section retains read-only `widget.group.currency` references in other tiles only if they exist outside `_buildCurrencyTile` (none should remain after this plan).
  </done>
</task>

<task type="auto">
  <name>Task 3: Fix orphaned imports catalogued by plan 39-02</name>
  <files>
    lib/core/services/cache/gear_cache_repository.dart
    lib/core/services/sync_service.dart
    lib/core/services/cache_service.dart
  </files>
  <read_first>
    - .planning/phases/39-strip-to-shippable-v1-remove-memories-vault-logistics-onboar/39-02-SUMMARY.md (read the "Files needing edits" section — that is the authoritative input list)
    - Each file listed in the SUMMARY (read in full before editing)
  </read_first>
  <action>
    For each file path listed in 39-02-SUMMARY.md's "Files needing edits" section, choose ONE of three actions and apply it:

    **Action A — Delete the file** if its sole purpose is supporting a now-deleted feature:
    - Example: `lib/core/services/cache/gear_cache_repository.dart` — wraps gear-only SQLite operations. With gear feature gone, the cache repo is dead. Delete it and remove its export from any cache barrel file.
    - Example: any `*_provider.dart` or `*_service.dart` whose entire body references gear/logistics/vault/memories/onboarding domain.

    **Action B — Strip the import + the dependent code path** if the file serves multiple features:
    - Example: `lib/core/services/sync_service.dart` may have `_syncGear()`, `_syncDocuments()` private methods. Delete those methods and the calls in the orchestrator method (`fullSync()` etc). Keep `_syncExpenses()`, `_syncSettlements()`, `_syncGroups()`.
    - Example: `lib/core/services/cache_service.dart` may have `clearGear()`, `clearDocuments()` static methods. Delete those methods. If a method like `clearAllCaches()` calls them, remove those calls.

    **Action C — Replace import with no-op** rare case where a referenced symbol can be replaced with a literal:
    - Generally avoid this. Prefer A or B.

    For each file:
    1. Read the file
    2. Decide A or B based on whether non-cut features still need it
    3. Apply the edit
    4. Run `flutter analyze {file}` and confirm no errors local to the file

    **Hard rules:**
    - DO NOT touch `lib/core/services/local_database.dart` schema (that's plan 39-06's job)
    - DO NOT touch `lib/features/trip/models/trip_model.dart` TripModules (that's plan 39-04's job — this plan handles only the `Trip.currency` field via Task 4 below; TripModules pruning is separate)
    - DO NOT touch `lib/features/events/models/event_model.dart` EventModules (already done in plan 39-01) — but this plan DOES touch the `Event.currency` field via Task 4

    After all individual file fixes, hand off to Task 4 for currency-field removal. The whole-repo analyze gate is enforced at the END of Task 4, not here — orphaned imports may still produce errors that Task 4 resolves (e.g., if a sync service referenced `trip.currency` it'll be cleaned up there).
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla &amp;&amp; flutter analyze 2>&amp;1 | tee /tmp/post-orphan-analyze.txt &amp;&amp; ERRORS=$(grep -cE "error •" /tmp/post-orphan-analyze.txt) &amp;&amp; echo "errors=$ERRORS (will be re-checked after Task 4)" &amp;&amp; { [ "$ERRORS" = "0" ] || grep -E "error •" /tmp/post-orphan-analyze.txt | grep -qE "trip_model|event_model|trip\.currency|event\.currency|trip_cache_repository|local_database"; }</automated>
  </verify>
  <acceptance_criteria>
    - `flutter analyze 2>&1 | grep -cE "error •"` returns 0 OR all remaining errors trace ONLY to: `trip_model.dart`, `event_model.dart`, currency-field call sites, `trip_cache_repository.dart`, or `local_database.dart` (which are Task 4 / plan 39-06 responsibilities)
    - `grep -rcE "package:thawani_payment|thawani_service" lib/ test/` returns 0
    - All file paths from 39-02-SUMMARY's "Files needing edits" list are either deleted or imports-stripped
  </acceptance_criteria>
  <done>
    All orphaned imports from Wave 2 are resolved. Remaining analyzer errors (if any) trace only to currency-field territory, which Task 4 handles next.
  </done>
</task>

<task type="auto">
  <name>Task 4: Remove `currency`/`base_currency` field from Trip and Event models (ROADMAP SC2)</name>
  <files>
    lib/features/trip/models/trip_model.dart
    lib/features/events/models/event_model.dart
    lib/core/services/cache/trip_cache_repository.dart
    lib/features/ledger/screens/ledger_screen.dart
    lib/features/ledger/screens/add_expense_screen.dart
    lib/features/ledger/screens/edit_expense_screen.dart
    lib/features/ledger/screens/settle_up_screen.dart
    lib/features/events/screens/event_expense_hero.dart
    lib/features/events/widgets/event_card.dart
  </files>
  <read_first>
    - lib/features/trip/models/trip_model.dart (lines 1-110 — the Trip class)
    - lib/features/events/models/event_model.dart (lines 150-310 — Event class fields, fromFirestore, toFirestoreMap, copyWith)
    - Run `grep -rn "trip\.currency\|event\.currency\|widget\.event\.currency\|widget\.trip\.currency" lib/ test/` to find every read site
    - Run `grep -rn "'currency':" lib/ | grep -E "(trip|event)"` to find Firestore/SQLite writes
  </read_first>
  <action>
    **Part A — `lib/features/trip/models/trip_model.dart` (Trip class, lines 1-110):**

    Remove the `currency` field everywhere:

    1. Line 13: delete `final String currency;`
    2. Line 26: delete `this.currency = 'OMR',` from the const constructor
    3. Line 50: delete `currency: json['currency'] as String? ?? 'OMR',` from `Trip.fromJson` factory (legacy `currency` key in cached JSON is silently ignored — back-compat)
    4. Line 63: delete `'currency': currency,` from `toJson` (no longer written to SQLite cache)
    5. Line 85: delete `String? currency,` from `copyWith` parameter list
    6. Line 98: delete `currency: currency ?? this.currency,` from `copyWith` body

    Read the file after edits; ensure the class compiles. Trip should now have: `id, name, inviteCode, leaderId, modules, createdAt, updatedAt, startDate, endDate, icon` — no currency.

    **Part B — `lib/features/events/models/event_model.dart` (Event class, lines 150-310):**

    Remove the `currency` field everywhere:

    1. Line 165: delete `final String currency;`
    2. Line 183: delete `this.currency = 'OMR',` from constructor
    3. Line 246: delete `currency: data['currency'] as String? ?? 'OMR',` from `fromFirestore`
    4. Line 271: delete `'currency': currency,` from `toFirestoreMap`
    5. Line 305: delete `currency: currency,` from `copyWith` body
    6. Search for `currency` mentions in any other Event method (e.g., `fromCacheJson`, `toCacheJson` if present) and delete

    **Part C — Hardcode `'OMR'` at every consumer boundary:**

    For each call site previously reading `event.currency` or `trip.currency`, replace with the literal `'OMR'`. Decimal money math is unaffected — only the display string changes.

    1. **lib/core/services/cache/trip_cache_repository.dart line 36** — delete the line `'currency': trip.currency,` from the map. The SQLite trips table column `currency TEXT DEFAULT 'OMR'` will still get its default; we just don't write the value (plan 39-06 will drop the column).

    2. **lib/features/ledger/screens/ledger_screen.dart line 314**:
       Before: `final currency = event.currency as String? ?? 'OMR';`
       After:  `const currency = 'OMR';`

    3. **lib/features/ledger/screens/add_expense_screen.dart line 78**:
       Before: `?.currency ?? 'OMR';`
       After:  Replace the whole `final currency = ...` expression with `const currency = 'OMR';` (drop the `event?.currency` lookup entirely).

    4. **lib/features/ledger/screens/edit_expense_screen.dart line 65**:
       Before: `?.currency ??`
       After:  Replace the surrounding expression so currency is `'OMR'` literal.

    5. **lib/features/ledger/screens/settle_up_screen.dart** — replace `event.currency` with `'OMR'` at lines 228, 247, 259, 270, 287, 296, 375, 423. Eight identical replacements.

    6. **lib/features/events/screens/event_expense_hero.dart line 110**: replace `event.currency` with `'OMR'`.

    7. **lib/features/events/widgets/event_card.dart**: replace `event.currency` at lines 141, 145, 168, 178 with `'OMR'`. (Or extract a `const _currency = 'OMR';` at the top of the widget and reference it.)

    **Part D — back-compat fromJson test (per WARNING 6):**

    Add a unit test at `test/unit/trip_model_back_compat_test.dart`:

    ```dart
    import 'package:flutter_test/flutter_test.dart';
    import 'package:safar/features/trip/models/trip_model.dart';

    void main() {
      group('Trip.fromJson back-compat', () {
        test('tolerates legacy currency key', () {
          final trip = Trip.fromJson({
            'id': 't1',
            'name': 'Test',
            'invite_code': 'ABC',
            'leader_id': 'u1',
            'modules': {},
            'created_at': '2026-04-26T00:00:00.000Z',
            'icon': 'airplane',
            'currency': 'USD',  // legacy key — must be silently ignored
          });
          expect(trip.id, 't1');
          // No assertion on currency — field doesn't exist anymore.
        });
      });

      group('TripModules.fromJson back-compat', () {
        test('tolerates legacy gear/docs/logistics/memories keys', () {
          final modules = TripModules.fromJson({
            'gear': true,
            'docs': true,
            'logistics': true,
            'memories': true,
          });
          // Surviving fields default; legacy keys are silently dropped.
          expect(modules, isA<TripModules>());
        });
      });
    }
    ```

    Note: the `TripModules` test will pass even before plan 39-04 runs (since the current `TripModules.fromJson` reads its current keys and ignores unknowns). After 39-04, it asserts the no-op fromJson still works.

    **Part E — final whole-repo analyzer gate:**

    After all edits, run:

    ```bash
    flutter analyze 2>&1 | tee /tmp/wave3a-final.txt
    ```

    Per WARNING 5, the gate is strict: zero errors, OR every error matches an allowlist of files that plan 39-04/39-06 will touch:

    ```bash
    flutter analyze 2>&1 | grep "error •" | grep -vE "(trip_model\.dart|trip_cache_repository\.dart|local_database\.dart)" | tee /tmp/non-allowlist-errors.txt
    [ ! -s /tmp/non-allowlist-errors.txt ]  # must be empty
    ```

    If any error is outside the allowlist, fix it inline before completing this task.
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla &amp;&amp; ! grep -qE "^\s*final String currency;" lib/features/trip/models/trip_model.dart &amp;&amp; ! grep -qE "^\s*final String currency;" lib/features/events/models/event_model.dart &amp;&amp; ! grep -qE "trip\.currency|event\.currency" lib/features/ledger/ lib/features/events/widgets/event_card.dart lib/features/events/screens/event_expense_hero.dart lib/core/services/cache/trip_cache_repository.dart -r 2>/dev/null &amp;&amp; flutter test test/unit/trip_model_back_compat_test.dart 2>&amp;1 | tee /tmp/back-compat-test.txt &amp;&amp; grep -qE "All tests passed|\\+1.*-0" /tmp/back-compat-test.txt &amp;&amp; flutter analyze 2>&amp;1 | tee /tmp/wave3a-final.txt &amp;&amp; flutter analyze 2>&amp;1 | grep "error •" | grep -vE "(trip_model\.dart|trip_cache_repository\.dart|local_database\.dart)" | tee /tmp/non-allowlist.txt &amp;&amp; [ ! -s /tmp/non-allowlist.txt ]</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^\s*final String currency;" lib/features/trip/models/trip_model.dart` returns 0
    - `grep -c "^\s*final String currency;" lib/features/events/models/event_model.dart` returns 0
    - `grep -rE "trip\.currency|event\.currency" lib/features/ledger/ lib/features/events/widgets/event_card.dart lib/features/events/screens/event_expense_hero.dart lib/core/services/cache/trip_cache_repository.dart` returns no matches
    - `test/unit/trip_model_back_compat_test.dart` exists and passes
    - `flutter analyze` reports zero errors OR every remaining error is in the allowlist (`trip_model.dart`, `trip_cache_repository.dart`, `local_database.dart`) — verified by `flutter analyze 2>&1 | grep "error •" | grep -vE "(trip_model\.dart|trip_cache_repository\.dart|local_database\.dart)"` returning empty
  </acceptance_criteria>
  <done>
    Trip.currency and Event.currency fields removed from models; all read sites hardcode `'OMR'`; back-compat fromJson test passes; analyzer gate enforced with strict allowlist.
  </done>
</task>

</tasks>

<verification>
```bash
flutter analyze 2>&1 | tail -30
```

Expected: zero errors, OR errors only in `lib/features/trip/models/trip_model.dart` and `lib/core/services/cache/trip_cache_repository.dart` (handed off to plan 39-04 for TripModules pruning) or `lib/core/services/local_database.dart` (handed off to plan 39-06 for column drop). Currency-field errors should NOT remain — Task 4 fixed them.
</verification>

<success_criteria>
- Thawani service file deleted and every reference removed
- Currency picker UI removed from create_group_screen and group_info_section
- Trip.currency and Event.currency fields removed (per ROADMAP SC2)
- All ledger / event widgets hardcode `'OMR'` at the boundary
- All orphaned imports from Wave 2 resolved
- Back-compat fromJson test passes
- `flutter analyze` errors limited to the strict allowlist (trip_model, trip_cache_repository, local_database)
</success_criteria>

<output>
`.planning/phases/39-strip-to-shippable-v1-remove-memories-vault-logistics-onboar/39-03-SUMMARY.md` listing:
- Files deleted in Task 1 + 3
- Edits to create_group_screen.dart and group_info_section.dart (with concrete diff snippets for the deleted methods)
- Trip.currency and Event.currency field removals (line-by-line diff)
- Every call site updated to hardcode `'OMR'` (file:line:before:after)
- For each file in 39-02-SUMMARY's catalog, the action taken (A/B/C) and the result
- Confirmation back-compat test passes
- Any errors deliberately deferred to plans 39-04 or 39-06 with file:line references
</output>
</content>
