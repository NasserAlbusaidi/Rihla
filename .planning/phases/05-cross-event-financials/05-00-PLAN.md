---
phase: 05-cross-event-financials
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - test/unit/group_balance_provider_test.dart
  - test/unit/group_activity_service_test.dart
  - test/unit/group_settlement_service_test.dart
  - test/features/group_detail_screen_test.dart
  - test/features/group_balance_card_test.dart
  - test/features/group_settle_up_screen_test.dart
autonomous: true
requirements:
  - FIN-02
  - FIN-03
  - FIN-04
  - GRP-04
  - GRP-05

must_haves:
  truths:
    - "All Phase 5 test files exist so flutter test commands are valid before production code"
    - "Every test file has skip markers referencing the plan that will implement it"
  artifacts:
    - path: "test/unit/group_balance_provider_test.dart"
      provides: "Stub tests for groupBalancesProvider"
      contains: "skip: 'Awaiting Plan 05-03"
    - path: "test/unit/group_activity_service_test.dart"
      provides: "Stub tests for GroupActivityService"
      contains: "skip: 'Awaiting Plan 05-01"
    - path: "test/unit/group_settlement_service_test.dart"
      provides: "Stub tests for GroupSettlementService"
      contains: "skip: 'Awaiting Plan 05-01"
    - path: "test/features/group_detail_screen_test.dart"
      provides: "Widget test stubs for GroupDetailScreen financial sections"
      contains: "skip: 'Awaiting Plan 05-05"
    - path: "test/features/group_balance_card_test.dart"
      provides: "Widget test stubs for GroupMemberBalanceCard"
      contains: "skip: 'Awaiting Plan 05-04"
    - path: "test/features/group_settle_up_screen_test.dart"
      provides: "Widget test stubs for GroupSettleUpScreen"
      contains: "skip: 'Awaiting Plan 05-06"
  key_links: []
---

<objective>
Create all Wave 0 test stub files for Phase 5 so that `flutter test` commands referenced in VALIDATION.md are valid before any production code is written.

Purpose: Nyquist compliance — every verify command in later plans needs a test file to exist. Creating stubs first ensures the test runner doesn't error on missing files.
Output: 6 test stub files with skip markers referencing the plan that will implement each test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-cross-event-financials/05-CONTEXT.md
@.planning/phases/05-cross-event-financials/05-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create unit test stubs for services and provider</name>
  <files>
    test/unit/group_balance_provider_test.dart,
    test/unit/group_activity_service_test.dart,
    test/unit/group_settlement_service_test.dart
  </files>
  <read_first>
    test/unit/activity_service_test.dart,
    test/unit/settlement_service_test.dart,
    test/unit/balance_calculations_test.dart
  </read_first>
  <action>
Create three unit test stub files following the Wave 0 pattern established in Phase 02 and Phase 04:

**test/unit/group_settlement_service_test.dart:**
```dart
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('GroupSettlementService', () {
    test('watchGroupSettlements returns stream of settlements',
        skip: 'Awaiting Plan 05-01: GroupSettlementService implementation',
        () {});

    test('addGroupSettlement writes to groups/{groupId}/settlements',
        skip: 'Awaiting Plan 05-01: GroupSettlementService implementation',
        () {});

    test('addGroupSettlement stores amountFils via MoneySerializer',
        skip: 'Awaiting Plan 05-01: GroupSettlementService implementation',
        () {});

    test('settlement has scope=group and groupId set',
        skip: 'Awaiting Plan 05-01: GroupSettlementService implementation',
        () {});
  });
}
```

**test/unit/group_activity_service_test.dart:**
```dart
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('GroupActivityService', () {
    test('watchRecentActivity returns last 5 entries by default',
        skip: 'Awaiting Plan 05-01: GroupActivityService implementation',
        () {});

    test('watchActivityPage supports cursor pagination',
        skip: 'Awaiting Plan 05-01: GroupActivityService implementation',
        () {});

    test('logGroupEvent writes fire-and-forget to activity subcollection',
        skip: 'Awaiting Plan 05-01: GroupActivityService implementation',
        () {});

    test('activity entry contains type, actorId, actorName, description, timestamp',
        skip: 'Awaiting Plan 05-01: GroupActivityService implementation',
        () {});

    test('watchRecentActivity orders by timestamp descending',
        skip: 'Awaiting Plan 05-01: GroupActivityService implementation',
        () {});
  });
}
```

**test/unit/group_balance_provider_test.dart:**
```dart
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('groupBalancesProvider', () {
    test('aggregates expenses from all events in a group',
        skip: 'Awaiting Plan 05-03: groupBalancesProvider implementation',
        () {});

    test('includes group-level settlements in balance calculation',
        skip: 'Awaiting Plan 05-03: groupBalancesProvider implementation',
        () {});

    test('computes per-event breakdown per member',
        skip: 'Awaiting Plan 05-03: groupBalancesProvider implementation',
        () {});

    test('returns loading when events stream has no value yet',
        skip: 'Awaiting Plan 05-03: groupBalancesProvider implementation',
        () {});

    test('totalSpent sums all expenses across all events',
        skip: 'Awaiting Plan 05-03: groupBalancesProvider implementation',
        () {});

    test('contribution percentage per member computed from totalPaid',
        skip: 'Awaiting Plan 05-03: groupBalancesProvider implementation',
        () {});
  });
}
```
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla && flutter test test/unit/group_settlement_service_test.dart test/unit/group_activity_service_test.dart test/unit/group_balance_provider_test.dart 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - test/unit/group_settlement_service_test.dart exists and contains "skip: 'Awaiting Plan 05-01"
    - test/unit/group_activity_service_test.dart exists and contains "skip: 'Awaiting Plan 05-01"
    - test/unit/group_balance_provider_test.dart exists and contains "skip: 'Awaiting Plan 05-03"
    - `flutter test test/unit/group_settlement_service_test.dart` exits 0 (all tests skipped)
    - `flutter test test/unit/group_activity_service_test.dart` exits 0 (all tests skipped)
    - `flutter test test/unit/group_balance_provider_test.dart` exits 0 (all tests skipped)
  </acceptance_criteria>
  <done>Three unit test stub files exist with skip markers, all pass flutter test with skipped status</done>
</task>

<task type="auto">
  <name>Task 2: Create widget test stubs for screens and widgets</name>
  <files>
    test/features/group_detail_screen_test.dart,
    test/features/group_balance_card_test.dart,
    test/features/group_settle_up_screen_test.dart
  </files>
  <read_first>
    test/features/groups/group_screens_test.dart,
    test/features/home/home_screen_groups_test.dart
  </read_first>
  <action>
Create three widget test stub files following the established pattern:

**test/features/group_detail_screen_test.dart:**
```dart
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('GroupDetailScreen financial sections', () {
    test('shows GroupBalanceHero when expenses exist',
        skip: 'Awaiting Plan 05-05: GroupDetailScreen restructure',
        () {});

    test('hides GroupBalanceHero when no expenses exist (D-19)',
        skip: 'Awaiting Plan 05-05: GroupDetailScreen restructure',
        () {});

    test('shows Members & Balances section with GroupMemberBalanceCard tiles',
        skip: 'Awaiting Plan 05-05: GroupDetailScreen restructure',
        () {});

    test('shows spending stats chips when expenses exist',
        skip: 'Awaiting Plan 05-05: GroupDetailScreen restructure',
        () {});

    test('shows Recent Activity section with 5 entries',
        skip: 'Awaiting Plan 05-05: GroupDetailScreen restructure',
        () {});

    test('invite code section appears below events section (D-30)',
        skip: 'Awaiting Plan 05-05: GroupDetailScreen restructure',
        () {});
  });
}
```

**test/features/group_balance_card_test.dart:**
```dart
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('GroupMemberBalanceCard', () {
    test('shows member name and net balance amount',
        skip: 'Awaiting Plan 05-04: GroupMemberBalanceCard widget',
        () {});

    test('positive balance shown in emerald color',
        skip: 'Awaiting Plan 05-04: GroupMemberBalanceCard widget',
        () {});

    test('negative balance shown in rose color',
        skip: 'Awaiting Plan 05-04: GroupMemberBalanceCard widget',
        () {});

    test('zero balance shows Settled badge',
        skip: 'Awaiting Plan 05-04: GroupMemberBalanceCard widget',
        () {});

    test('tap expands to show per-event breakdown',
        skip: 'Awaiting Plan 05-04: GroupMemberBalanceCard widget',
        () {});

    test('accordion behavior — expanding one collapses others',
        skip: 'Awaiting Plan 05-04: GroupMemberBalanceCard widget',
        () {});
  });
}
```

**test/features/group_settle_up_screen_test.dart:**
```dart
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('GroupSettleUpScreen', () {
    test('shows optimized settlement tiles',
        skip: 'Awaiting Plan 05-06: GroupSettleUpScreen implementation',
        () {});

    test('settlement tile shows pairwise amount',
        skip: 'Awaiting Plan 05-06: GroupSettleUpScreen implementation',
        () {});

    test('Record settlement shows confirmation bottom sheet',
        skip: 'Awaiting Plan 05-06: GroupSettleUpScreen implementation',
        () {});

    test('all-settled state shows tick circle and message',
        skip: 'Awaiting Plan 05-06: GroupSettleUpScreen implementation',
        () {});

    test('settlement confirmation pre-fills suggested amount (D-11)',
        skip: 'Awaiting Plan 05-06: GroupSettleUpScreen implementation',
        () {});
  });
}
```
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla && flutter test test/features/group_detail_screen_test.dart test/features/group_balance_card_test.dart test/features/group_settle_up_screen_test.dart 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - test/features/group_detail_screen_test.dart exists and contains "skip: 'Awaiting Plan 05-05"
    - test/features/group_balance_card_test.dart exists and contains "skip: 'Awaiting Plan 05-04"
    - test/features/group_settle_up_screen_test.dart exists and contains "skip: 'Awaiting Plan 05-06"
    - All three files pass `flutter test` with skipped status (exit 0)
  </acceptance_criteria>
  <done>Three widget test stub files exist with skip markers, all pass flutter test with skipped status</done>
</task>

</tasks>

<verification>
All 6 test files exist and `flutter test test/unit/group_settlement_service_test.dart test/unit/group_activity_service_test.dart test/unit/group_balance_provider_test.dart test/features/group_detail_screen_test.dart test/features/group_balance_card_test.dart test/features/group_settle_up_screen_test.dart` exits 0.
</verification>

<success_criteria>
- 6 test stub files created with appropriate skip markers
- All skip markers reference the correct downstream plan number
- `flutter test` on all 6 files exits 0 (all skipped)
</success_criteria>

<output>
After completion, create `.planning/phases/05-cross-event-financials/05-00-SUMMARY.md`
</output>
