---
phase: 02-groups
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - test/unit/group_model_test.dart
  - test/unit/invite_code_test.dart
  - test/unit/group_service_test.dart
  - test/unit/group_join_test.dart
  - test/features/groups/group_screens_test.dart
  - test/features/home/home_screen_groups_test.dart
autonomous: true
requirements: [GRP-01, GRP-02, GRP-03, GRP-06, GRP-07]

must_haves:
  truths:
    - "Every group data layer task has a corresponding test file that can be run with flutter test"
    - "Every group UI task has a corresponding widget test file that can be run with flutter test"
    - "All test stubs compile and pass (stubs contain skip markers for unimplemented behavior)"
  artifacts:
    - path: "test/unit/group_model_test.dart"
      provides: "Test stubs for Group and GroupMember model serialization"
      contains: "group('Group model'"
    - path: "test/unit/invite_code_test.dart"
      provides: "Test stubs for invite code generation and validation"
      contains: "group('Invite code'"
    - path: "test/unit/group_service_test.dart"
      provides: "Test stubs for GroupService create/update operations"
      contains: "group('GroupService'"
    - path: "test/unit/group_join_test.dart"
      provides: "Test stubs for GroupService join flow"
      contains: "group('GroupService join'"
    - path: "test/features/groups/group_screens_test.dart"
      provides: "Widget test stubs for GroupDetailScreen and GroupSettingsScreen"
      contains: "group('Group screens'"
    - path: "test/features/home/home_screen_groups_test.dart"
      provides: "Widget test stubs for groups-first HomeScreen"
      contains: "group('HomeScreen groups'"
  key_links:
    - from: "test/unit/group_model_test.dart"
      to: "lib/features/groups/models/group_model.dart"
      via: "import of Group class"
      pattern: "import.*group_model"
    - from: "test/unit/group_service_test.dart"
      to: "lib/features/groups/providers/group_provider.dart"
      via: "import of GroupService"
      pattern: "import.*group_provider"
---

<objective>
Create all test stub files for Phase 2 so that every implementation task has a behavioral automated verification command.

Purpose: The Nyquist rule requires every task to have an `<automated>` verify command that runs `flutter test` against a real test file. This Wave 0 plan creates the 6 test stubs referenced in VALIDATION.md. Each stub file contains `group()` blocks with `skip` markers for behaviors that will be implemented in later waves. Once implementation plans execute, the `skip` markers are removed and tests assert real behavior.

Output: 6 test files (4 unit, 2 widget) that compile, run, and pass (all skipped). Later plans reference these files in their `<verify>` blocks.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-groups/2-CONTEXT.md
@.planning/phases/02-groups/02-VALIDATION.md

<interfaces>
<!-- Test patterns from existing codebase -->

From test/unit/money_serializer_test.dart (unit test pattern):
```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:safar/core/services/money_serializer.dart';

void main() {
  group('MoneySerializer', () {
    test('toSubunits: OMR 10.500 returns 10500', () {
      // actual assertion
    });
  });
}
```

From test/features/command_center_test.dart (widget test pattern):
```dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:mocktail/mocktail.dart';
// ... widget test with ProviderScope overrides
```

From pubspec.yaml dev_dependencies (already available):
```yaml
fake_cloud_firestore: ^4.1.0+1
firebase_auth_mocks: ^0.15.1
mocktail: ^1.0.4
sqflite_common_ffi: ^2.3.4
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create unit test stubs for group models and invite code generation</name>
  <files>test/unit/group_model_test.dart, test/unit/invite_code_test.dart</files>
  <read_first>
    - test/unit/money_serializer_test.dart (unit test pattern: imports, group/test structure)
    - test/unit/local_database_migration_test.dart (SQLite test pattern, sqflite_common_ffi usage)
    - .planning/phases/02-groups/02-VALIDATION.md (test file expectations)
  </read_first>
  <action>
**test/unit/group_model_test.dart** — Test stubs for Group and GroupMember model classes (GRP-01):

```dart
import 'package:flutter_test/flutter_test.dart';
// NOTE: Production imports will be added when models are created in Plan 02-01 Task 1.
// For now, stubs use skip markers so the file compiles and passes.

void main() {
  group('Group model', () {
    group('fromDoc / toMap serialization', () {
      test('fromDoc creates Group from Firestore DocumentSnapshot', skip: 'Awaiting Plan 02-01 Task 1: Group model implementation', () {});

      test('toMap produces SQLite-compatible map with snake_case keys', skip: 'Awaiting Plan 02-01 Task 1: Group model implementation', () {});

      test('fromMap creates Group from SQLite row with snake_case keys', skip: 'Awaiting Plan 02-01 Task 1: Group model implementation', () {});

      test('round-trip: Group -> toMap -> fromMap returns equivalent Group', skip: 'Awaiting Plan 02-01 Task 1: Group model implementation', () {});

      test('memberIds is List<String> not Map (per D-14)', skip: 'Awaiting Plan 02-01 Task 1: Group model implementation', () {});
    });

    group('copyWith', () {
      test('copyWith creates new instance with updated name', skip: 'Awaiting Plan 02-01 Task 1: Group model implementation', () {});

      test('copyWith preserves unchanged fields', skip: 'Awaiting Plan 02-01 Task 1: Group model implementation', () {});
    });
  });

  group('GroupMember model', () {
    group('fromDoc / toMap serialization', () {
      test('fromDoc creates GroupMember from Firestore subcollection doc', skip: 'Awaiting Plan 02-01 Task 1: GroupMember model implementation', () {});

      test('toMap produces SQLite-compatible map', skip: 'Awaiting Plan 02-01 Task 1: GroupMember model implementation', () {});

      test('fromMap creates GroupMember from SQLite row', skip: 'Awaiting Plan 02-01 Task 1: GroupMember model implementation', () {});

      test('isShadow stored as int (0/1) in toMap and restored from fromMap', skip: 'Awaiting Plan 02-01 Task 1: GroupMember model implementation', () {});

      test('role is String not enum — accepts CREATOR and MEMBER', skip: 'Awaiting Plan 02-01 Task 1: GroupMember model implementation', () {});
    });
  });
}
```

**test/unit/invite_code_test.dart** — Test stubs for invite code generation (GRP-02):

```dart
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('Invite code generation', () {
    test('generates a 6-character code', skip: 'Awaiting Plan 02-01 Task 2: GroupService._generateInviteCode', () {});

    test('code uses only allowed chars: ABCDEFGHJKLMNPQRSTUVWXYZ23456789 (no O/0/I/l per D-10)', skip: 'Awaiting Plan 02-01 Task 2: GroupService._generateInviteCode', () {});

    test('code is uppercase', skip: 'Awaiting Plan 02-01 Task 2: GroupService._generateInviteCode', () {});

    test('100 generated codes are all unique (probabilistic)', skip: 'Awaiting Plan 02-01 Task 2: GroupService._generateInviteCode', () {});
  });

  group('Invite code validation', () {
    test('valid 6-char code is accepted', skip: 'Awaiting Plan 02-01 Task 2: joinGroup validation', () {});

    test('invalid code throws Exception with "Invalid invite code" message', skip: 'Awaiting Plan 02-01 Task 2: joinGroup validation', () {});

    test('code is uppercased before lookup (D-13)', skip: 'Awaiting Plan 02-01 Task 2: joinGroup validation', () {});
  });
}
```

IMPORTANT: These test files must compile and run immediately. All tests use `skip` parameter so they pass without production code existing. Do NOT import production files that do not exist yet — the imports will be added when Plan 02-01 executes and removes the skip markers.
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla && flutter test test/unit/group_model_test.dart test/unit/invite_code_test.dart 2>&1 | tail -10</automated>
  </verify>
  <done>Two unit test stub files exist, compile, and pass (all skipped). group_model_test.dart covers Group/GroupMember serialization. invite_code_test.dart covers code generation and validation.</done>
</task>

<task type="auto">
  <name>Task 2: Create unit test stubs for GroupService and widget test stubs for group screens</name>
  <files>test/unit/group_service_test.dart, test/unit/group_join_test.dart, test/features/groups/group_screens_test.dart, test/features/home/home_screen_groups_test.dart</files>
  <read_first>
    - test/features/command_center_test.dart (widget test pattern with ProviderScope overrides)
    - test/features/ledger_test.dart (widget test pattern with provider mocking)
    - .planning/phases/02-groups/02-VALIDATION.md (test file expectations per task)
  </read_first>
  <action>
**test/unit/group_service_test.dart** — Test stubs for GroupService create/update (GRP-01, GRP-03):

```dart
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('GroupService', () {
    group('createGroup', () {
      test('creates group doc, inviteCode doc, and member doc atomically via WriteBatch', skip: 'Awaiting Plan 02-01 Task 2: GroupService implementation', () {});

      test('creator is added as member with role CREATOR (D-09)', skip: 'Awaiting Plan 02-01 Task 2: GroupService implementation', () {});

      test('generated invite code is stored in both group doc and inviteCodes collection', skip: 'Awaiting Plan 02-01 Task 2: GroupService implementation', () {});

      test('memberIds array contains creator UID after creation', skip: 'Awaiting Plan 02-01 Task 2: GroupService implementation', () {});

      test('throws if user is not authenticated (uid is null)', skip: 'Awaiting Plan 02-01 Task 2: GroupService implementation', () {});
    });

    group('updateGroup', () {
      test('updates group name in Firestore', skip: 'Awaiting Plan 02-01 Task 2: GroupService implementation', () {});

      test('updates currency in Firestore', skip: 'Awaiting Plan 02-01 Task 2: GroupService implementation', () {});

      test('only updates provided fields (partial update)', skip: 'Awaiting Plan 02-01 Task 2: GroupService implementation', () {});
    });

    group('updateMemberDisplayName', () {
      test('updates displayName field on member subcollection document (D-07)', skip: 'Awaiting Plan 02-01 Task 2: GroupService implementation', () {});
    });
  });
}
```

**test/unit/group_join_test.dart** — Test stubs for GroupService join flow (GRP-03):

```dart
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('GroupService join', () {
    test('valid invite code adds user to group memberIds via arrayUnion', skip: 'Awaiting Plan 02-01 Task 2: GroupService implementation', () {});

    test('valid invite code creates member doc in subcollection with role MEMBER', skip: 'Awaiting Plan 02-01 Task 2: GroupService implementation', () {});

    test('invalid invite code throws Exception("Invalid invite code")', skip: 'Awaiting Plan 02-01 Task 2: GroupService implementation', () {});

    test('already a member throws Exception("Already a member")', skip: 'Awaiting Plan 02-01 Task 2: GroupService implementation', () {});

    test('join uses WriteBatch for atomic member doc + memberIds update', skip: 'Awaiting Plan 02-01 Task 2: GroupService implementation', () {});

    test('invite code is uppercased before Firestore lookup (D-13)', skip: 'Awaiting Plan 02-01 Task 2: GroupService implementation', () {});
  });
}
```

**test/features/groups/group_screens_test.dart** — Widget test stubs for GroupDetailScreen and GroupSettingsScreen (GRP-06):

```dart
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('Group screens', () {
    group('GroupDetailScreen', () {
      test('shows group name in header', skip: 'Awaiting Plan 02-03: GroupDetailScreen implementation', () {});

      test('shows member count chip', skip: 'Awaiting Plan 02-03: GroupDetailScreen implementation', () {});

      test('shows invite code section with code display', skip: 'Awaiting Plan 02-03: GroupDetailScreen implementation', () {});

      test('shows members list from groupMembersProvider', skip: 'Awaiting Plan 02-03: GroupDetailScreen implementation', () {});

      test('shows "No events yet" placeholder for event timeline', skip: 'Awaiting Plan 02-03: GroupDetailScreen implementation', () {});

      test('settings icon navigates to GroupSettingsScreen', skip: 'Awaiting Plan 02-03: GroupDetailScreen implementation', () {});
    });

    group('GroupSettingsScreen', () {
      test('shows group name with edit option for creator (D-15)', skip: 'Awaiting Plan 02-03: GroupSettingsScreen implementation', () {});

      test('hides edit option for non-creator members', skip: 'Awaiting Plan 02-03: GroupSettingsScreen implementation', () {});

      test('shows currency with change option (D-16)', skip: 'Awaiting Plan 02-03: GroupSettingsScreen implementation', () {});

      test('shows invite code with copy button', skip: 'Awaiting Plan 02-03: GroupSettingsScreen implementation', () {});
    });
  });
}
```

**test/features/home/home_screen_groups_test.dart** — Widget test stubs for groups-first HomeScreen (GRP-07):

```dart
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('HomeScreen groups', () {
    test('shows "Your Groups" header', skip: 'Awaiting Plan 02-02: HomeScreen replacement', () {});

    test('shows GroupCard for each group from userGroupsProvider', skip: 'Awaiting Plan 02-02: HomeScreen replacement', () {});

    test('shows empty state when user has no groups', skip: 'Awaiting Plan 02-02: HomeScreen replacement', () {});

    test('FAB opens bottom sheet with Create and Join options', skip: 'Awaiting Plan 02-02: HomeScreen replacement', () {});

    test('tapping GroupCard navigates to group detail', skip: 'Awaiting Plan 02-02: HomeScreen replacement', () {});

    test('does not reference userTripsProvider (trip code removed)', skip: 'Awaiting Plan 02-02: HomeScreen replacement', () {});
  });
}
```

IMPORTANT: Same rule as Task 1 — do NOT import production files that do not exist yet. All tests use `skip` parameter. The `test/features/groups/` directory must be created. The `test/features/home/` directory already exists (contains home screen tests from prior phases).
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla && flutter test test/unit/group_service_test.dart test/unit/group_join_test.dart test/features/groups/group_screens_test.dart test/features/home/home_screen_groups_test.dart 2>&1 | tail -10</automated>
  </verify>
  <done>Four test stub files exist, compile, and pass (all skipped). group_service_test.dart covers create/update operations. group_join_test.dart covers join flow. group_screens_test.dart covers GroupDetail and GroupSettings widgets. home_screen_groups_test.dart covers groups-first HomeScreen.</done>
</task>

</tasks>

<verification>
```bash
# All 6 test stub files exist
test -f test/unit/group_model_test.dart && echo "PASS" || echo "FAIL: group_model_test"
test -f test/unit/invite_code_test.dart && echo "PASS" || echo "FAIL: invite_code_test"
test -f test/unit/group_service_test.dart && echo "PASS" || echo "FAIL: group_service_test"
test -f test/unit/group_join_test.dart && echo "PASS" || echo "FAIL: group_join_test"
test -f test/features/groups/group_screens_test.dart && echo "PASS" || echo "FAIL: group_screens_test"
test -f test/features/home/home_screen_groups_test.dart && echo "PASS" || echo "FAIL: home_screen_groups_test"

# All stubs compile and pass (skipped tests count as passing)
flutter test test/unit/group_model_test.dart test/unit/invite_code_test.dart test/unit/group_service_test.dart test/unit/group_join_test.dart test/features/groups/group_screens_test.dart test/features/home/home_screen_groups_test.dart
```
</verification>

<success_criteria>
- 6 test stub files exist at the paths listed in VALIDATION.md
- All stubs compile and run via `flutter test` with 0 failures (all tests are skipped)
- No production imports in stubs (imports added when implementation plans execute)
- Each stub has meaningful test names that describe the expected behavior
</success_criteria>

<output>
After completion, create `.planning/phases/02-groups/02-00-SUMMARY.md`
</output>
</content>
</invoke>