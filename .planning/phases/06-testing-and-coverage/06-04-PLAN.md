---
phase: 06-testing-and-coverage
plan: 04
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - test/integration/offline_scenario_test.dart
  - test/features/groups/group_screens_test.dart
  - test/features/events/create_event_test.dart
  - test/features/home/home_screen_groups_test.dart
  - test/features/groups/group_settle_up_screen_test.dart
  - test/features/ledger_test.dart
  - test/features/events/event_command_center_test.dart
autonomous: true
requirements: [TST-02, TST-06]
must_haves:
  truths:
    - "Offline scenario: expense write produces both Firestore document and SQLite cache entry"
    - "Offline scenario: settlement write results in correct BalanceCalculator output from cached data"
    - "Widget tests render GroupDetailScreen, CreateEventScreen, HomeScreen, GroupSettleUpScreen, LedgerScreen, EventCommandCenter without errors"
    - "Balance toggle interaction works in GroupDetailScreen widget test"
  artifacts:
    - path: "test/integration/offline_scenario_test.dart"
      provides: "3 offline scenario integration tests per D-12"
      contains: "BalanceCacheRepository"
    - path: "test/features/groups/group_screens_test.dart"
      provides: "GroupDetailScreen + balance toggle widget tests"
      contains: "GroupDetailScreen"
    - path: "test/features/events/create_event_test.dart"
      provides: "CreateEventScreen widget tests"
      contains: "CreateEventScreen"
    - path: "test/features/groups/group_settle_up_screen_test.dart"
      provides: "GroupSettleUpScreen widget tests"
      contains: "GroupSettleUpScreen"
  key_links:
    - from: "test/integration/offline_scenario_test.dart"
      to: "lib/core/services/balance_cache_repository.dart"
      via: "cacheExpenses/getExpenses method calls"
      pattern: "repo\\.cacheExpenses|repo\\.getExpenses"
    - from: "test/features/groups/group_screens_test.dart"
      to: "lib/features/groups/screens/group_detail_screen.dart"
      via: "Widget test render"
      pattern: "GroupDetailScreen"
---

<objective>
Create offline scenario integration tests (TST-06) and expand widget tests for the 7 key screens (TST-02).

Purpose: Offline reliability is critical for a travel app. Widget tests for key screens catch rendering regressions and interaction bugs. Together with unit tests from Plans 02-03, these close the gap toward 80% coverage.
Output: 3 offline integration tests + expanded widget tests for 7 screens.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-testing-and-coverage/06-CONTEXT.md
@.planning/phases/06-testing-and-coverage/06-RESEARCH.md

@lib/core/services/balance_cache_repository.dart
@lib/features/ledger/services/expense_service.dart
@lib/features/ledger/services/settlement_service.dart
@lib/features/groups/screens/group_detail_screen.dart
@lib/features/events/screens/create_event_screen.dart
@lib/features/home/screens/home_screen.dart
@lib/features/groups/screens/group_settle_up_screen.dart
@lib/features/ledger/screens/ledger_screen.dart
@lib/features/events/screens/event_command_center.dart
@test/integration/firebase_money_roundtrip_test.dart
@test/unit/balance_cache_repository_test.dart
@test/features/groups/group_screens_test.dart
@test/features/events/create_event_test.dart
@test/features/home/home_screen_groups_test.dart
@test/features/groups/group_settle_up_screen_test.dart
@test/features/ledger_test.dart
@test/features/events/event_command_center_test.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Offline scenario integration tests (D-11, D-12)</name>
  <read_first>
    - test/unit/balance_cache_repository_test.dart (SQLite test pattern with sqflite_common_ffi)
    - test/integration/firebase_money_roundtrip_test.dart (Firestore integration test pattern)
    - lib/core/services/balance_cache_repository.dart (cacheExpenses, cacheSettlements, getExpenses, getSettlements methods)
    - lib/features/ledger/services/expense_service.dart (ExpenseService.addExpense — writes to Firestore)
    - lib/features/ledger/services/settlement_service.dart (SettlementService.addSettlement — writes to Firestore)
    - lib/features/ledger/models/expense_model.dart (Expense constructor fields)
    - lib/features/ledger/models/settlement_model.dart (Settlement constructor fields)
    - lib/features/ledger/providers/expense_provider.dart (BalanceCalculator.calculateBalances)
    - lib/core/database/local_database.dart (LocalDatabase.clearAll, LocalDatabase.close)
  </read_first>
  <files>test/integration/offline_scenario_test.dart</files>
  <behavior>
    Scenario 1: Expense write -> SQLite side-write verified
    - Write expense via ExpenseService.withFirestore(fakeDb).addExpense()
    - Cache the expense to SQLite via BalanceCacheRepository.cacheExpenses()
    - Read back from SQLite via BalanceCacheRepository.getExpenses()
    - Verify Firestore document exists with correct amountFils
    - Verify SQLite record has correct Decimal amount

    Scenario 2: Settlement write -> SQLite + BalanceCalculator reads correct values
    - Write expense + settlement via services
    - Cache both to SQLite
    - Read from SQLite via BalanceCacheRepository
    - Feed cached data to BalanceCalculator.calculateBalances()
    - Verify net balances reflect the settlement correctly

    Scenario 3: Multiple writes -> all appear in SQLite in correct order
    - Write 3 expenses with different amounts via service
    - Cache all to SQLite
    - Read from SQLite
    - Verify all 3 present with correct amounts
    - Verify ordering (by createdAt if sorted)
  </behavior>
  <action>
    Create test/integration/offline_scenario_test.dart following the established SQLite test pattern:

    ```dart
    import 'package:fake_cloud_firestore/fake_cloud_firestore.dart';
    import 'package:flutter_test/flutter_test.dart';
    import 'package:sqflite_common_ffi/sqflite_ffi.dart';
    import 'package:decimal/decimal.dart';
    // ... model and service imports

    void main() {
      setUpAll(() {
        sqfliteFfiInit();
        databaseFactory = databaseFactoryFfi;
      });

      setUp(() async {
        await LocalDatabase.clearAll();
      });

      tearDownAll(() async {
        await LocalDatabase.close();
      });

      group('Offline scenarios (D-12)', () {
        test('Scenario 1: expense write lands in both Firestore and SQLite', () async {
          final fakeDb = FakeFirebaseFirestore();
          final expenseService = ExpenseService.withFirestore(fakeDb);
          final repo = BalanceCacheRepository();
          // ... write, cache, verify both stores
          repo.dispose();
        });

        test('Scenario 2: settlement write + BalanceCalculator reads from cache', () async {
          // ... write expense + settlement, cache, read, calculate balances
        });

        test('Scenario 3: multiple writes appear in SQLite in correct order', () async {
          // ... write 3 expenses, cache, verify all present
        });
      });
    }
    ```

    Per D-11: This is SQLite-only verification. No Firestore network simulation. We verify the side-write pipeline: service writes to FakeFirebaseFirestore, then we manually cache to SQLite (simulating what the asyncMap listener does in production), then verify SQLite has correct data.

    Per D-13: This file lives in test/integration/ because it tests interaction between Firestore services and SQLite.

    IMPORTANT: Read BalanceCacheRepository to verify exact method signatures. The methods may be:
    - `cacheExpenses(String eventId, List<Expense> expenses)`
    - `getExpenses(String eventId)` -> `Future<List<Expense>>`
    - `cacheSettlements(String eventId, List<Settlement> settlements)`
    - `getSettlements(String eventId)` -> `Future<List<Settlement>>`

    If BalanceCacheRepository requires initialization or has a different API, follow whatever pattern test/unit/balance_cache_repository_test.dart uses.
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla && flutter test test/integration/offline_scenario_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - test/integration/offline_scenario_test.dart exists
    - File contains `sqfliteFfiInit()` in setUpAll
    - File contains `BalanceCacheRepository` in test code
    - File contains `ExpenseService.withFirestore` in test code
    - File contains exactly 3 test() calls (3 scenarios per D-12)
    - File contains `'Scenario 1'` and `'Scenario 2'` and `'Scenario 3'` in test names
    - `flutter test test/integration/offline_scenario_test.dart` exits 0
  </acceptance_criteria>
  <done>3 offline integration scenarios pass, verifying the Firestore -> SQLite side-write pipeline.</done>
</task>

<task type="auto">
  <name>Task 2: Widget tests for 7 key screens (D-18, D-19)</name>
  <read_first>
    - test/features/groups/group_screens_test.dart (existing GroupDetailScreen tests — check what's covered)
    - test/features/events/create_event_test.dart (existing CreateEventScreen tests)
    - test/features/home/home_screen_groups_test.dart (existing HomeScreen tests)
    - test/features/groups/group_settle_up_screen_test.dart (existing GroupSettleUpScreen tests)
    - test/features/ledger_test.dart (existing LedgerScreen tests)
    - test/features/events/event_command_center_test.dart (existing EventCommandCenter tests)
    - lib/features/groups/screens/group_detail_screen.dart (build method — identify all ref.watch calls)
    - lib/features/events/screens/create_event_screen.dart (build method — identify all ref.watch calls)
    - lib/features/home/screens/home_screen.dart (build method — identify all ref.watch calls)
    - lib/features/groups/screens/group_settle_up_screen.dart (build method — identify all ref.watch calls)
    - lib/features/ledger/screens/ledger_screen.dart (build method — identify all ref.watch calls)
    - lib/features/events/screens/event_command_center.dart (build method — identify all ref.watch calls)
  </read_first>
  <files>
    test/features/groups/group_screens_test.dart
    test/features/events/create_event_test.dart
    test/features/home/home_screen_groups_test.dart
    test/features/groups/group_settle_up_screen_test.dart
    test/features/ledger_test.dart
    test/features/events/event_command_center_test.dart
  </files>
  <action>
    For each of the 7 screens (D-19), READ the screen's build() method first, list every `ref.watch()` call, then extend the existing test file with missing tests.

    Per D-18, each screen needs ~5-8 test cases covering:
    1. Renders without errors (pumpWidget + pumpAndSettle completes)
    2. Key data displays correctly (group name, member count, amounts)
    3. Primary interactions work (tap buttons, toggle switches, submit forms)
    4. Empty state renders correctly (no data scenario)
    5. Navigation triggers (per D-21: assert Navigator.push called, don't render destination)

    SCREEN-BY-SCREEN CHECKLIST:

    **GroupDetailScreen** (test/features/groups/group_screens_test.dart):
    - Renders group name and member count
    - Shows balance section with member balance cards
    - Balance toggle interaction (tap toggles between per-event and group-level) — this is the TST-02 "balance toggle" requirement
    - Tap settle-up navigates (assert Navigator.push called)
    - Empty state when no events exist

    **CreateEventScreen** (test/features/events/create_event_test.dart):
    - Renders event type picker
    - Selecting event type shows correct modules
    - Submit button creates event (verify service called or navigation occurs)
    - Form validation (empty name rejected)

    **HomeScreen** (test/features/home/home_screen_groups_test.dart):
    - Renders group list with group cards
    - FAB for creating new group
    - Tap group card navigates to GroupDetailScreen
    - Empty state when no groups

    **GroupSettleUpScreen** (test/features/groups/group_settle_up_screen_test.dart):
    - Renders member list with amounts
    - Shows optimal settlement suggestions
    - Tap settle button triggers settlement action

    **LedgerScreen** (test/features/ledger_test.dart):
    - Renders expense list
    - Shows balance summary
    - Add expense button present
    - Empty state with no expenses

    **EventCommandCenter** (test/features/events/event_command_center_test.dart):
    - Renders module cards for enabled modules
    - Tap module card navigates to module screen
    - Shows event name in header

    CRITICAL PATTERN for all widget tests:
    - EVERY `ref.watch()` in the screen's build() must be overridden in ProviderScope
    - Use `findsWidgets` (not `findsOneWidget`) for text that may appear in multiple places
    - SharedPreferences must be overridden for screens that read settings
    - Use `MaterialApp(home: screen)` wrapper for navigation context
    - Use `tester.pumpAndSettle()` after pumpWidget

    For navigation tests per D-21:
    ```dart
    // Use a mock NavigatorObserver
    final mockObserver = MockNavigatorObserver();
    await tester.pumpWidget(MaterialApp(
      navigatorObservers: [mockObserver],
      home: ProviderScope(overrides: [...], child: screen),
    ));
    await tester.tap(find.text('Settle Up'));
    await tester.pumpAndSettle();
    verify(() => mockObserver.didPush(any(), any())).called(greaterThan(0));
    ```

    DO NOT test shared widgets (ModuleHeader, AppTabBar, EmptyStateView) in isolation per D-20.
    DO NOT render destination screens after navigation per D-21.
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla && flutter test test/features/</automated>
  </verify>
  <acceptance_criteria>
    - test/features/groups/group_screens_test.dart contains `'balance toggle'` or `'Balance toggle'` or `'toggle'` in a test name (TST-02 balance toggle requirement)
    - test/features/events/create_event_test.dart contains at least 5 test() or testWidgets() calls
    - test/features/home/home_screen_groups_test.dart contains at least 4 test() or testWidgets() calls
    - test/features/groups/group_settle_up_screen_test.dart contains at least 3 test() or testWidgets() calls
    - test/features/ledger_test.dart contains at least 4 test() or testWidgets() calls
    - test/features/events/event_command_center_test.dart contains at least 3 test() or testWidgets() calls
    - `flutter test test/features/` exits 0
  </acceptance_criteria>
  <done>7 key screens have widget tests covering render, data display, and key interactions. Balance toggle interaction tested per TST-02.</done>
</task>

</tasks>

<verification>
- `flutter test test/integration/offline_scenario_test.dart` exits 0
- `flutter test test/features/` exits 0
- 3 offline scenarios verified per D-12
- 7 screens have 5+ widget test cases each per D-19
</verification>

<success_criteria>
TST-02 and TST-06 fully addressed. Offline side-write pipeline verified through 3 integration scenarios. All 7 key screens have render + interaction widget tests.
</success_criteria>

<output>
After completion, create `.planning/phases/06-testing-and-coverage/06-04-SUMMARY.md`
</output>
