---
phase: 34-gear-logistics
plan: "00"
type: execute
wave: 1
depends_on: []
files_modified:
  - test/features/gear_screen_mutations_test.dart
  - test/features/logistics_screen_mutations_test.dart
autonomous: true
requirements:
  - GEAR-OFFLINE
  - LOGISTICS-OFFLINE
must_haves:
  truths:
    - "Gear test suite has a failing test asserting OfflineBanner is rendered"
    - "Logistics test suite has a failing test asserting OfflineBanner is rendered"
  artifacts:
    - path: test/features/gear_screen_mutations_test.dart
      provides: "Failing OfflineBanner test stub for GearScreen"
      contains: "GearScreen — OfflineBanner renders in body"
    - path: test/features/logistics_screen_mutations_test.dart
      provides: "Failing OfflineBanner test stub for LogisticsScreen"
      contains: "LogisticsScreen — OfflineBanner renders in body"
  key_links:
    - from: test/features/gear_screen_mutations_test.dart
      to: lib/features/gear/screens/gear_screen.dart
      via: find.byType(OfflineBanner)
      pattern: "find\\.byType\\(OfflineBanner\\)"
    - from: test/features/logistics_screen_mutations_test.dart
      to: lib/features/logistics/screens/logistics_screen.dart
      via: find.byType(OfflineBanner)
      pattern: "find\\.byType\\(OfflineBanner\\)"
---

<objective>
Wave 0: add two failing test stubs — one in each existing test file — asserting OfflineBanner is present in the GearScreen and LogisticsScreen bodies.

Purpose: Establish RED state before Wave 1 adds OfflineBanner to the screens. Prevents the screen fix from landing without test coverage.
Output: Two test files with a new failing test each. All existing tests continue to pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-gear-logistics/34-CONTEXT.md
@.planning/phases/34-gear-logistics/34-RESEARCH.md

@test/features/gear_screen_mutations_test.dart
@test/features/logistics_screen_mutations_test.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing OfflineBanner test to gear_screen_mutations_test.dart</name>
  <files>test/features/gear_screen_mutations_test.dart</files>
  <behavior>
    - Test: `GearScreen — OfflineBanner renders in body` — pumps GearScreen with all required provider overrides (same setup as existing mutation tests), finds the populated state, then `expect(find.byType(OfflineBanner), findsOneWidget)`. Must FAIL before Wave 1.
  </behavior>
  <action>
    Read the full test file to understand the existing setUp/provider override pattern.

    Add one new test group or one new test case at the end of the existing test file:

    ```dart
    testWidgets('GearScreen — OfflineBanner renders in body', (tester) async {
      // Reuse the same provider setup as existing tests (see existing setUp block).
      // Pump the widget and pump a few frames so the loaded state is reached.
      // Assert:
      expect(find.byType(OfflineBanner), findsOneWidget);
    });
    ```

    Import `OfflineBanner` at the top of the test file:
    ```dart
    import 'package:safar/shared/widgets/offline_banner.dart';
    ```

    The test must FAIL at this point (OfflineBanner not yet in the screen).
    Do NOT modify any existing tests.
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla && flutter test test/features/gear_screen_mutations_test.dart 2>&1 | tail -20</automated>
  </verify>
  <done>
    - New test `GearScreen — OfflineBanner renders in body` exists in the file.
    - Running the test file shows this one test FAILING (expect finds 0 widgets) while all other tests in the file PASS.
    - File compiles without errors.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add failing OfflineBanner test to logistics_screen_mutations_test.dart</name>
  <files>test/features/logistics_screen_mutations_test.dart</files>
  <behavior>
    - Test: `LogisticsScreen — OfflineBanner renders in body` — pumps LogisticsScreen with all required provider overrides, asserts `find.byType(OfflineBanner)` findsOneWidget. Must FAIL before Wave 1.
  </behavior>
  <action>
    Read the full test file to understand the existing setUp/provider override pattern.

    Add one new test case at the end of the existing test file:

    ```dart
    testWidgets('LogisticsScreen — OfflineBanner renders in body', (tester) async {
      // Reuse same provider setup as existing tests.
      // Pump and settle to reach loaded state.
      expect(find.byType(OfflineBanner), findsOneWidget);
    });
    ```

    Import `OfflineBanner`:
    ```dart
    import 'package:safar/shared/widgets/offline_banner.dart';
    ```

    The test must FAIL at this point.
    Do NOT modify any existing tests.
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla && flutter test test/features/logistics_screen_mutations_test.dart 2>&1 | tail -20</automated>
  </verify>
  <done>
    - New test `LogisticsScreen — OfflineBanner renders in body` exists in the file.
    - Running the test file shows this one test FAILING while all other tests PASS.
    - File compiles without errors.
  </done>
</task>

</tasks>

<verification>
Run both test files to confirm: existing tests pass, new stubs fail.

```bash
cd /Users/nasseralbusaidi/Desktop/Personal/Rihla && flutter test test/features/gear_screen_mutations_test.dart test/features/logistics_screen_mutations_test.dart 2>&1 | tail -30
```

Expected: all existing tests green, exactly 2 new tests red (OfflineBanner assertions).
</verification>

<success_criteria>
- gear_screen_mutations_test.dart has `GearScreen — OfflineBanner renders in body` test that fails
- logistics_screen_mutations_test.dart has `LogisticsScreen — OfflineBanner renders in body` test that fails
- All other existing tests in both files still pass
- Both files compile
</success_criteria>

<output>
After completion, create `.planning/phases/34-gear-logistics/34-00-SUMMARY.md`
</output>
