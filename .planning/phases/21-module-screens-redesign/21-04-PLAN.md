---
phase: 21-module-screens-redesign
plan: 04
type: execute
wave: 2
depends_on: [21-01]
files_modified:
  - lib/features/memories/widgets/memories_hero_card.dart
  - lib/features/memories/screens/memories_screen.dart
  - lib/features/activity/widgets/activity_hero_card.dart
  - lib/features/activity/widgets/activity_entry_card.dart
  - lib/features/activity/screens/activity_feed_screen.dart
autonomous: true
requirements: [SCRN-04]

must_haves:
  truths:
    - "Memories screen shows hero card with photo count, date range, and Add Photo CTA"
    - "Memories uses a 3-column photo grid (not card list) below the hero card"
    - "Memories screen uses ModuleHeader instead of its custom header"
    - "Activity screen shows hero card with entry count and last update (no CTA)"
    - "Activity timeline is date-grouped with section headers (TODAY, YESTERDAY, etc.)"
    - "Activity entries show avatar circle with deterministic color, action text, and relative time"
  artifacts:
    - path: "lib/features/memories/widgets/memories_hero_card.dart"
      provides: "Photo count + date range + Add Photo CTA"
      contains: "class MemoriesHeroCard"
    - path: "lib/features/memories/screens/memories_screen.dart"
      provides: "Redesigned Memories with ModuleHeader + hero + photo grid"
      contains: "ModuleHeader"
    - path: "lib/features/activity/widgets/activity_hero_card.dart"
      provides: "Activity hero with entry count + last update"
      contains: "class ActivityHeroCard"
    - path: "lib/features/activity/widgets/activity_entry_card.dart"
      provides: "Activity entry with avatar + action + time"
      contains: "class ActivityEntryCard"
    - path: "lib/features/activity/screens/activity_feed_screen.dart"
      provides: "Redesigned Activity with date-grouped timeline"
      contains: "ActivityHeroCard"
  key_links:
    - from: "lib/features/memories/screens/memories_screen.dart"
      to: "lib/shared/widgets/module_header.dart"
      via: "ModuleHeader import (migrated from custom header)"
      pattern: "ModuleHeader"
    - from: "lib/features/activity/screens/activity_feed_screen.dart"
      to: "lib/features/activity/widgets/activity_entry_card.dart"
      via: "ActivityEntryCard in date-grouped list"
      pattern: "ActivityEntryCard"
---

<objective>
Redesign Memories and Activity screens. Memories migrates from custom header to ModuleHeader and switches to a 3-column photo grid. Activity expands from minimal 79-line screen to a full date-grouped timeline with hero card.

Purpose: Memories is the visual exception to the card-list template (D-24) — it uses a photo grid. Activity needs the most structural work, expanding from a near-empty screen to a complete date-grouped timeline. Both screens require unique patterns not shared with Plan 03.
Output: MemoriesHeroCard, ActivityHeroCard, ActivityEntryCard widgets + rewritten screen files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-module-screens-redesign/21-CONTEXT.md
@.planning/phases/21-module-screens-redesign/21-UI-SPEC.md
@.planning/phases/21-module-screens-redesign/21-01-SUMMARY.md

<interfaces>
<!-- Models and providers for Memories and Activity -->

From lib/features/memories/models/memory.dart:
```dart
class Memory {
  final String id;
  final String imageUrl;
  final String? caption;
  final String uploadedBy;
  final String uploadedByName;
  final DateTime createdAt;
  // ...
}
```

From lib/features/activity/models/activity_log.dart:
```dart
class ActivityLog {
  final String id;
  final String actorId;
  final String actorName;
  final String action;  // e.g., 'added_expense', 'settled_up', 'joined'
  final Map<String, dynamic>? metadata;
  final DateTime createdAt;
  // ...
}
```

Photo grid spec (D-24): 3 columns, 8dp gap, 8dp border radius thumbnails
Activity timeline (D-21): date-grouped, section headers in textMuted 12sp ALL-CAPS
ActivityRow avatar: deterministic color from hashCode % Colors.primaries.length (Phase 18 pattern)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Memories and Activity hero cards + ActivityEntryCard</name>
  <files>lib/features/memories/widgets/memories_hero_card.dart, lib/features/activity/widgets/activity_hero_card.dart, lib/features/activity/widgets/activity_entry_card.dart</files>
  <read_first>
    - lib/features/memories/screens/memories_screen.dart
    - lib/features/memories/models/memory.dart
    - lib/features/activity/screens/activity_feed_screen.dart
    - lib/features/activity/models/activity_log.dart
    - lib/core/theme/app_theme.dart
    - .planning/phases/21-module-screens-redesign/21-UI-SPEC.md (Memories Screen section, Activity Screen section)
  </read_first>
  <action>
    **MemoriesHeroCard (lib/features/memories/widgets/memories_hero_card.dart) — D-15:**

    StatelessWidget with parameters:
    - `int photoCount` — number of photos
    - `String dateRange` — pre-formatted string (e.g., "Mar 15 - Mar 28") or empty if no photos
    - `VoidCallback onAddPhoto` — Add Photo CTA

    Layout (Container: surface bg, 24dp radius, cardShadow, 20dp padding):
    - Row 1: "MEMORIES" overline (12sp, w400, textMuted, ALL-CAPS, letter-spacing 0.5)
    - Row 2 (SizedBox height 8): "$photoCount photos" (20sp, w600, textPrimary) + if dateRange not empty: " · $dateRange" appended (14sp, w400, textSecondary)
    - Row 3 (SizedBox height 16): full-width ElevatedButton "Add Photo" (teal, 52dp height) — calls onAddPhoto

    **ActivityHeroCard (lib/features/activity/widgets/activity_hero_card.dart) — D-16:**

    StatelessWidget with parameters:
    - `int entryCount` — number of activity entries
    - `String lastUpdate` — pre-formatted relative time string (e.g., "2 hours ago") or "No activity yet"

    Layout (Container: surface bg, 24dp radius, cardShadow, 20dp padding):
    - Row 1: "ACTIVITY" overline (12sp, w400, textMuted, ALL-CAPS, letter-spacing 0.5)
    - Row 2 (SizedBox height 8): "$entryCount entries" (20sp, w600, textPrimary)
    - Row 3 (SizedBox height 4): "Last update: $lastUpdate" (14sp, w400, textSecondary)
    - No CTA button (read-only feed per D-16)

    **ActivityEntryCard (lib/features/activity/widgets/activity_entry_card.dart) — D-21:**

    StatelessWidget with an ActivityLog parameter:

    Layout (Container: 16dp horizontal margin, 4dp vertical margin, 16dp padding, surface bg, 24dp radius, cardShadow):
    - Row:
      - Avatar circle: 36dp Container with BoxShape.circle, color from `Colors.primaries[actorName.hashCode.abs() % Colors.primaries.length].withValues(alpha: 0.15)`, child Center(Text(actorName[0].toUpperCase(), style: 14sp w600 with same Colors.primaries color))
      - SizedBox(width: 12)
      - Expanded Column:
        - Line 1: RichText or Row: "$actorName " (14sp, w600, textPrimary) + "${actionDescription}" (14sp, w400, textPrimary). Map action strings to human-readable: 'added_expense' -> "added an expense", 'settled_up' -> "settled up", 'joined' -> "joined the event", 'added_gear' -> "added a gear item", etc.
        - Line 2 (SizedBox height 2): relative time from createdAt (12sp, w400, textMuted) — e.g., "2 hours ago", "Yesterday at 3:45 PM"
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla && flutter analyze lib/features/memories/widgets/memories_hero_card.dart lib/features/activity/widgets/activity_hero_card.dart lib/features/activity/widgets/activity_entry_card.dart 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - lib/features/memories/widgets/memories_hero_card.dart contains `class MemoriesHeroCard extends StatelessWidget`
    - lib/features/memories/widgets/memories_hero_card.dart contains `Add Photo` button text
    - lib/features/memories/widgets/memories_hero_card.dart contains `MEMORIES` overline text
    - lib/features/activity/widgets/activity_hero_card.dart contains `class ActivityHeroCard extends StatelessWidget`
    - lib/features/activity/widgets/activity_hero_card.dart contains `ACTIVITY` overline text
    - lib/features/activity/widgets/activity_hero_card.dart does NOT contain `ElevatedButton` (no CTA per D-16)
    - lib/features/activity/widgets/activity_entry_card.dart contains `class ActivityEntryCard extends StatelessWidget`
    - lib/features/activity/widgets/activity_entry_card.dart contains `BoxShape.circle` for avatar
    - lib/features/activity/widgets/activity_entry_card.dart contains `Colors.primaries` for deterministic color
    - `flutter analyze` reports no errors on all three files
  </acceptance_criteria>
  <done>MemoriesHeroCard with photo count + date range + Add Photo CTA. ActivityHeroCard with entry count + last update (no CTA). ActivityEntryCard with deterministic avatar + action text + relative time. All compile without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite Memories screen (photo grid + ModuleHeader migration) and Activity screen (date-grouped timeline)</name>
  <files>lib/features/memories/screens/memories_screen.dart, lib/features/activity/screens/activity_feed_screen.dart</files>
  <read_first>
    - lib/features/memories/screens/memories_screen.dart
    - lib/features/activity/screens/activity_feed_screen.dart
    - lib/features/memories/widgets/memories_hero_card.dart
    - lib/features/activity/widgets/activity_hero_card.dart
    - lib/features/activity/widgets/activity_entry_card.dart
    - lib/features/memories/providers/memory_provider.dart
    - lib/features/activity/providers/activity_provider.dart
    - lib/shared/widgets/module_header.dart
    - lib/shared/widgets/empty_state_view.dart
    - lib/shared/widgets/skeleton_loader.dart
    - lib/shared/animations/staggered_grid.dart
    - lib/shared/animations/fade_in_list.dart
    - .planning/phases/21-module-screens-redesign/21-UI-SPEC.md (Memories + Activity sections)
    - .planning/phases/21-module-screens-redesign/21-RESEARCH.md (Pattern 4: Date-Grouped Activity, Pattern 5: Memories Photo Grid)
  </read_first>
  <action>
    **Memories Screen rewrite (D-24 exception to card-list template):**

    Migrate from custom header to ModuleHeader. Apply unified template with photo grid exception.

    Structure (CustomScrollView with slivers):
    1. SliverToBoxAdapter -> ModuleHeader(title: 'Memories', subtitle: event name, useDarkTheme: true) — replace the custom header entirely. The existing custom header code should be removed.
    2. SliverToBoxAdapter -> MemoriesHeroCard(photoCount, dateRange computed from memory list, onAddPhoto: trigger photo picker/upload)
    3. SliverToBoxAdapter -> 3-column photo grid (D-24):
       ```dart
       GridView.builder(
         shrinkWrap: true,
         physics: const NeverScrollableScrollPhysics(),
         padding: const EdgeInsets.all(16),
         gridDelegate: const SliverGridDelegateWithFixedCrossAxisCount(
           crossAxisCount: 3,
           crossAxisSpacing: 8,
           mainAxisSpacing: 8,
         ),
         itemCount: memories.length,
         itemBuilder: (context, index) {
           final memory = memories[index];
           return GestureDetector(
             onTap: () => _openFullScreen(context, memory),
             child: ClipRRect(
               borderRadius: BorderRadius.circular(8),
               child: /* CachedNetworkImage or Image.network with memory.imageUrl */,
             ),
           );
         },
       )
       ```
       Wrap in StaggeredGrid for entrance animation if practical. Otherwise use FadeInList wrapper.
    - Tap thumbnail opens full-screen viewer via existing `PageRouteBuilder(opaque: false)` pattern (keep the existing full-screen viewer logic).
    - Loading: SkeletonLoader.photoGrid() (created in Plan 01)
    - Empty: EmptyStateView with icon Iconsax.gallery, title "No photos yet", message "Add photos to capture the best moments of this trip.", CTA "Add Photo", accentGradient LinearGradient(colors: [Color(0xFF9B7A5C), Color(0xFFB89878)]) (desert sand per D-18)
    - Remove any CircularProgressIndicator and the custom header widget

    **Activity Screen rewrite (D-21 date-grouped timeline):**

    The current activity_feed_screen.dart is only 79 lines — essentially a placeholder. Expand to full implementation.

    Structure (CustomScrollView with slivers):
    1. SliverToBoxAdapter -> ModuleHeader(title: 'Activity', subtitle: event name, useDarkTheme: true)
    2. SliverToBoxAdapter -> ActivityHeroCard(entryCount, lastUpdate computed from activity list)
    3. SliverToBoxAdapter -> Date-grouped activity list (D-21, Option C from RESEARCH — non-sticky section headers):

    Group activity logs by date. For each date group:
    - Date section header: Padding(16dp horizontal, 16dp top, 4dp bottom), Text with date label ("TODAY", "YESTERDAY", or formatted date like "Mar 28") in 12sp, w400, textMuted, ALL-CAPS, letter-spacing 0.5
    - FadeInList of ActivityEntryCard widgets for that date group

    Date grouping logic:
    ```dart
    Map<String, List<ActivityLog>> _groupByDate(List<ActivityLog> logs) {
      final grouped = <String, List<ActivityLog>>{};
      final now = DateTime.now();
      for (final log in logs) {
        final label = _dateLabel(log.createdAt, now);
        grouped.putIfAbsent(label, () => []).add(log);
      }
      return grouped;
    }

    String _dateLabel(DateTime date, DateTime now) {
      final today = DateTime(now.year, now.month, now.day);
      final logDate = DateTime(date.year, date.month, date.day);
      if (logDate == today) return 'TODAY';
      if (logDate == today.subtract(const Duration(days: 1))) return 'YESTERDAY';
      return DateFormat('MMM d').format(date);
    }
    ```

    Build the list as a Column of [SectionHeader, ...ActivityEntryCards] for each date group. Wrap the whole thing in SliverToBoxAdapter.

    - Loading: SkeletonLoader.cardList() (generic skeleton)
    - Empty: EmptyStateView with icon Iconsax.activity, title "No activity yet", message "Actions by you and your group members will appear here.", no CTA (per D-19), accentGradient LinearGradient(colors: [Color(0xFFA67C5B), Color(0xFFC29A7A)]) (caramel per D-18)
    - Remove any CircularProgressIndicator

    After both rewrites, run `flutter test --no-pub` to verify no regressions.
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla && flutter analyze lib/features/memories/screens/memories_screen.dart lib/features/activity/screens/activity_feed_screen.dart 2>&1 | tail -5 && flutter test --no-pub 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - lib/features/memories/screens/memories_screen.dart contains `ModuleHeader(` with `useDarkTheme: true`
    - lib/features/memories/screens/memories_screen.dart contains `MemoriesHeroCard(`
    - lib/features/memories/screens/memories_screen.dart contains `crossAxisCount: 3`
    - lib/features/memories/screens/memories_screen.dart contains `crossAxisSpacing: 8` and `mainAxisSpacing: 8`
    - lib/features/memories/screens/memories_screen.dart contains `BorderRadius.circular(8)` for thumbnail radius
    - lib/features/memories/screens/memories_screen.dart contains `SkeletonLoader.photoGrid`
    - lib/features/memories/screens/memories_screen.dart contains `EmptyStateView(` with `accentGradient`
    - lib/features/memories/screens/memories_screen.dart does NOT contain `CircularProgressIndicator`
    - lib/features/activity/screens/activity_feed_screen.dart contains `ActivityHeroCard(`
    - lib/features/activity/screens/activity_feed_screen.dart contains `ActivityEntryCard(`
    - lib/features/activity/screens/activity_feed_screen.dart contains `TODAY` or `YESTERDAY` for date group labels
    - lib/features/activity/screens/activity_feed_screen.dart contains `EmptyStateView(` with `accentGradient`
    - lib/features/activity/screens/activity_feed_screen.dart does NOT contain `CircularProgressIndicator`
    - `flutter analyze` reports no errors on both files
    - `flutter test --no-pub` passes with 0 failures
  </acceptance_criteria>
  <done>Memories screen uses ModuleHeader (custom header removed), MemoriesHeroCard, 3-column photo grid with 8dp gap/radius. Activity screen has ActivityHeroCard, date-grouped timeline with section headers, ActivityEntryCard entries. Both use SkeletonLoader and EmptyStateView with earthy gradient circles. All tests pass.</done>
</task>

</tasks>

<verification>
- `flutter analyze lib/features/memories/ lib/features/activity/` — zero errors
- `flutter test --no-pub` — full suite passes
- `grep -c 'CircularProgressIndicator' lib/features/memories/screens/memories_screen.dart lib/features/activity/screens/activity_feed_screen.dart` — both return 0
- `grep -c 'ModuleHeader' lib/features/memories/screens/memories_screen.dart` — returns at least 1
</verification>

<success_criteria>
- Memories shows MemoriesHeroCard with photo count + date range + Add Photo CTA
- Memories uses 3-column photo grid with 8dp gap/radius thumbnails (not card list)
- Memories uses ModuleHeader instead of custom header
- Activity shows ActivityHeroCard with entry count + last update (no CTA)
- Activity timeline groups entries by date with section headers
- Activity entries show deterministic-color avatar + action text + relative time
- Both screens use skeleton loading and earthy gradient circle empty states
</success_criteria>

<output>
After completion, create `.planning/phases/21-module-screens-redesign/21-04-SUMMARY.md`
</output>
