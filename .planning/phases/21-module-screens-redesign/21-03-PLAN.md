---
phase: 21-module-screens-redesign
plan: 03
type: execute
wave: 2
depends_on: [21-01]
files_modified:
  - lib/features/gear/widgets/gear_hero_card.dart
  - lib/features/gear/screens/gear_screen.dart
  - lib/features/logistics/widgets/logistics_hero_card.dart
  - lib/features/logistics/widgets/sub_group_card.dart
  - lib/features/logistics/screens/logistics_screen.dart
  - lib/features/vault/widgets/vault_hero_card.dart
  - lib/features/vault/screens/vault_screen.dart
autonomous: true
requirements: [SCRN-04]

must_haves:
  truths:
    - "Gear screen shows hero card with packed count, priority items, and Add Item CTA"
    - "Logistics screen shows hero card with group/member counts and Create Group CTA"
    - "Logistics has no tabs — single scroll with sub-group cards"
    - "Sub-group cards show member chips and capacity progress bar with dusty teal accent"
    - "Vault screen shows hero card with file count, total size, and Upload File CTA"
    - "All three screens use ModuleHeader with dark gradient and EmptyStateView with earthy gradient circles"
  artifacts:
    - path: "lib/features/gear/widgets/gear_hero_card.dart"
      provides: "Gear hero card with packed progress + priority badge + CTA"
      contains: "class GearHeroCard"
    - path: "lib/features/logistics/widgets/logistics_hero_card.dart"
      provides: "Logistics hero with group/member stats + CTA"
      contains: "class LogisticsHeroCard"
    - path: "lib/features/logistics/widgets/sub_group_card.dart"
      provides: "Sub-group card with member chips and capacity bar"
      contains: "class SubGroupCard"
    - path: "lib/features/vault/widgets/vault_hero_card.dart"
      provides: "Vault hero with file count + size + CTA"
      contains: "class VaultHeroCard"
  key_links:
    - from: "lib/features/gear/screens/gear_screen.dart"
      to: "lib/features/gear/widgets/gear_hero_card.dart"
      via: "GearHeroCard widget import"
      pattern: "GearHeroCard"
    - from: "lib/features/logistics/screens/logistics_screen.dart"
      to: "lib/features/logistics/widgets/logistics_hero_card.dart"
      via: "LogisticsHeroCard widget import"
      pattern: "LogisticsHeroCard"
    - from: "lib/features/vault/screens/vault_screen.dart"
      to: "lib/features/vault/widgets/vault_hero_card.dart"
      via: "VaultHeroCard widget import"
      pattern: "VaultHeroCard"
---

<objective>
Redesign Gear, Logistics, and Vault screens with the unified module template: dark ModuleHeader, summary hero cards, upgraded empty states. Logistics drops its tab bar. All three share the card-list content pattern.

Purpose: These three screens follow nearly identical structure (hero + card list), making them efficient to batch. Logistics tab removal (D-23) is the main structural change; Gear and Vault are primarily reskin + hero card additions.
Output: GearHeroCard, LogisticsHeroCard, SubGroupCard, VaultHeroCard widgets + rewritten screen files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-module-screens-redesign/21-CONTEXT.md
@.planning/phases/21-module-screens-redesign/21-UI-SPEC.md
@.planning/phases/21-module-screens-redesign/21-01-SUMMARY.md

<interfaces>
<!-- Models and providers for these three modules -->

From lib/features/gear/models/gear_item.dart:
```dart
class GearItem {
  final String id;
  final String name;
  final String? assignedTo;
  final String? assignedToName;
  final bool isPacked;
  final bool isPriority;
  final String category;
  // ...
}
```

From lib/features/logistics/models/sub_group.dart:
```dart
class SubGroup {
  final String id;
  final String name;
  final String? icon;
  final int maxCapacity;
  final List<SubGroupMember> members;
  // ...
}
```

From lib/features/vault/models/document.dart:
```dart
class Document {
  final String id;
  final String title;
  final String fileName;
  final int fileSize;
  final String fileType;
  final String uploadedBy;
  final String uploadedByName;
  final DateTime createdAt;
  // ...
}
```

Card style (D-10): 16dp padding, 24dp border radius, cardShadow, AppColors.surface background
Hero card: 20dp padding, surface bg, 24dp radius, cardShadow
Overline: 12sp, w400, textMuted, ALL-CAPS, letter-spacing 0.5
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create hero cards for Gear, Logistics, Vault + SubGroupCard widget</name>
  <files>lib/features/gear/widgets/gear_hero_card.dart, lib/features/logistics/widgets/logistics_hero_card.dart, lib/features/logistics/widgets/sub_group_card.dart, lib/features/vault/widgets/vault_hero_card.dart</files>
  <read_first>
    - lib/features/gear/screens/gear_screen.dart
    - lib/features/gear/models/gear_item.dart
    - lib/features/logistics/screens/logistics_screen.dart
    - lib/features/logistics/models/sub_group.dart
    - lib/features/vault/screens/vault_screen.dart
    - lib/features/vault/models/document.dart
    - lib/core/theme/app_theme.dart
    - .planning/phases/21-module-screens-redesign/21-UI-SPEC.md (Gear, Logistics, Vault sections)
  </read_first>
  <action>
    **GearHeroCard (lib/features/gear/widgets/gear_hero_card.dart) — D-12:**

    StatelessWidget with parameters:
    - `int packedCount`, `int totalCount` — for "Packed X/Y" progress
    - `int priorityCount` — priority items needing attention
    - `VoidCallback onAddItem` — Add Item CTA

    Layout (Container: surface bg, 24dp radius, cardShadow, 20dp padding):
    - Row 1: "PACKING PROGRESS" overline (12sp, w400, textMuted, ALL-CAPS, letter-spacing 0.5)
    - Row 2 (SizedBox height 8): "Packed $packedCount/$totalCount" (20sp, w600, textPrimary)
    - Row 3 (SizedBox height 12): `LinearProgressIndicator(value: packedCount/totalCount, minHeight: 4, backgroundColor: AppColors.border, valueColor: AlwaysStoppedAnimation(AppColors.moduleLedger), borderRadius: BorderRadius.circular(2))`
    - Row 4 (SizedBox height 12): if priorityCount > 0, chip: Container with AppColors.error.withValues(alpha: 0.1) bg, radiusSmall radius, padding 8h/4v, child Text "$priorityCount priority" in errorText (12sp, w600)
    - Row 5 (SizedBox height 16): SizedBox(width: double.infinity, height: 52) `ElevatedButton` "Add Item" — calls onAddItem

    **LogisticsHeroCard (lib/features/logistics/widgets/logistics_hero_card.dart) — D-13:**

    StatelessWidget with parameters:
    - `int groupCount`, `int memberCount`, `int unassignedCount`
    - `VoidCallback onCreateGroup`

    Layout (Container: surface bg, 24dp radius, cardShadow, 20dp padding):
    - Row 1: "ORGANIZATION" overline
    - Row 2 (SizedBox height 8): "$groupCount groups · $memberCount members" (20sp, w600, textPrimary)
    - Row 3 (SizedBox height 8): if unassignedCount > 0: "$unassignedCount unassigned" (14sp, w400, errorText)
    - Row 4 (SizedBox height 16): full-width ElevatedButton "Create Group" — calls onCreateGroup

    **SubGroupCard (lib/features/logistics/widgets/sub_group_card.dart) — D-22:**

    StatelessWidget with `SubGroup subGroup`:

    Layout (Container: 16dp horizontal margin, 6dp vertical margin, 16dp padding, surface bg, 24dp radius, cardShadow, PLUS `Border(top: BorderSide(color: AppColors.moduleLogistics, width: 3))` for dusty teal top border):
    - Line 1: Row: Icon (subGroup.icon mapped to Iconsax, 20dp, textSecondary) + SizedBox(8) + Text subGroup.name (14sp, w600, textPrimary) + Spacer + Text "${subGroup.members.length}/${subGroup.maxCapacity}" (14sp, w400, textSecondary)
    - Line 2 (SizedBox height 8): `LinearProgressIndicator(value: members.length / maxCapacity, minHeight: 4, backgroundColor: AppColors.border, valueColor: AlwaysStoppedAnimation(AppColors.moduleLogistics))` with borderRadius 2
    - Line 3 (SizedBox height 8): Wrap of member name chips. Each chip: Container with AppColors.selectionFill bg, radiusSmall radius, padding 8h/4v, child Text member.displayName (12sp, w400, textSecondary)

    **VaultHeroCard (lib/features/vault/widgets/vault_hero_card.dart) — D-14:**

    StatelessWidget with parameters:
    - `int fileCount`, `String totalSize` (pre-formatted, e.g. "12.5 MB")
    - `VoidCallback onUpload`

    Layout (Container: surface bg, 24dp radius, cardShadow, 20dp padding):
    - Row 1: "DOCUMENTS" overline
    - Row 2 (SizedBox height 8): "$fileCount files · $totalSize" (20sp, w600, textPrimary)
    - Row 3 (SizedBox height 16): full-width ElevatedButton "Upload File" — calls onUpload
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla && flutter analyze lib/features/gear/widgets/gear_hero_card.dart lib/features/logistics/widgets/logistics_hero_card.dart lib/features/logistics/widgets/sub_group_card.dart lib/features/vault/widgets/vault_hero_card.dart 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - lib/features/gear/widgets/gear_hero_card.dart contains `class GearHeroCard extends StatelessWidget`
    - lib/features/gear/widgets/gear_hero_card.dart contains `Add Item` button text
    - lib/features/gear/widgets/gear_hero_card.dart contains `LinearProgressIndicator`
    - lib/features/logistics/widgets/logistics_hero_card.dart contains `class LogisticsHeroCard extends StatelessWidget`
    - lib/features/logistics/widgets/logistics_hero_card.dart contains `Create Group` button text
    - lib/features/logistics/widgets/sub_group_card.dart contains `class SubGroupCard extends StatelessWidget`
    - lib/features/logistics/widgets/sub_group_card.dart contains `BorderSide(color: AppColors.moduleLogistics, width: 3)`
    - lib/features/logistics/widgets/sub_group_card.dart contains `LinearProgressIndicator`
    - lib/features/vault/widgets/vault_hero_card.dart contains `class VaultHeroCard extends StatelessWidget`
    - lib/features/vault/widgets/vault_hero_card.dart contains `Upload File` button text
    - `flutter analyze` reports no errors on all four files
  </acceptance_criteria>
  <done>Four new hero/card widgets created: GearHeroCard with packed progress + priority badge, LogisticsHeroCard with group/member stats, SubGroupCard with capacity bar + member chips + teal top border, VaultHeroCard with file count + upload CTA. All compile without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite Gear, Logistics, and Vault screens with unified module template</name>
  <files>lib/features/gear/screens/gear_screen.dart, lib/features/logistics/screens/logistics_screen.dart, lib/features/vault/screens/vault_screen.dart</files>
  <read_first>
    - lib/features/gear/screens/gear_screen.dart
    - lib/features/logistics/screens/logistics_screen.dart
    - lib/features/vault/screens/vault_screen.dart
    - lib/features/gear/widgets/gear_hero_card.dart
    - lib/features/logistics/widgets/logistics_hero_card.dart
    - lib/features/logistics/widgets/sub_group_card.dart
    - lib/features/vault/widgets/vault_hero_card.dart
    - lib/shared/widgets/module_header.dart
    - lib/shared/widgets/empty_state_view.dart
    - lib/shared/widgets/skeleton_loader.dart
    - lib/shared/widgets/search_filter_bar.dart
    - lib/shared/animations/fade_in_list.dart
    - test/features/gear_screen_mutations_test.dart
    - test/features/logistics_screen_mutations_test.dart
    - .planning/phases/21-module-screens-redesign/21-UI-SPEC.md (Gear, Logistics, Vault sections)
  </read_first>
  <action>
    Apply the unified module template (D-08) to all three screens. Each follows: ModuleHeader(useDarkTheme: true) -> Hero Card -> Section overline -> Content list.

    **Gear Screen rewrite:**

    Keep existing ConsumerStatefulWidget structure (needs SearchFilterBar state). Apply unified layout:
    1. ModuleHeader(title: 'Gear', subtitle: event name, useDarkTheme: true)
    2. GearHeroCard(packedCount, totalCount, priorityCount, onAddItem: navigate to add gear)
    3. SearchFilterBar (retained per discretion — keep existing search/filter functionality)
    4. Section overline "GEAR ITEMS"
    5. FadeInList of gear item cards (keep existing card rendering but ensure surface bg, 24dp radius, cardShadow styling per D-10; wrap in TapBounce)
    - Loading: SkeletonLoader.gearList() (already exists)
    - Empty: EmptyStateView with icon Iconsax.bag_2, title "Nothing packed yet", message "Add gear items to track what everyone needs to bring.", CTA "Add Gear Item", accentGradient LinearGradient(colors: [Color(0xFF7A8C5E), Color(0xFF96A876)]) (olive per D-18)
    - Remove any CircularProgressIndicator

    **Logistics Screen rewrite (D-23 — tab bar removal):**

    Change from ConsumerStatefulWidget with SingleTickerProviderStateMixin to ConsumerWidget (or ConsumerStatefulWidget without the mixin). Remove:
    - `SingleTickerProviderStateMixin`
    - `_tabController` field and all references
    - `initState` TabController initialization
    - `dispose` TabController disposal
    - `AppTabBar` widget
    - `TabBarView` widget

    Apply unified layout:
    1. ModuleHeader(title: 'Logistics', subtitle: event name, useDarkTheme: true)
    2. LogisticsHeroCard(groupCount, memberCount, unassignedCount, onCreateGroup: navigate)
    3. Section overline "SUB-GROUPS"
    4. FadeInList of SubGroupCard widgets
    - Loading: SkeletonLoader.groupList() (or similar existing factory)
    - Empty: EmptyStateView with icon Iconsax.people, title "No sub-groups yet", message "Create sub-groups to organize transport, accommodation, or activities.", CTA "Create Sub-group", accentGradient LinearGradient(colors: [Color(0xFF5B7B8C), Color(0xFF7B9BAC)]) (dusty teal per D-18)
    - Remove any CircularProgressIndicator

    **Vault Screen rewrite:**

    Apply unified layout:
    1. ModuleHeader(title: 'Vault', subtitle: event name, useDarkTheme: true)
    2. VaultHeroCard(fileCount, totalSize, onUpload: navigate or trigger upload)
    3. SearchFilterBar (retained — keep existing search/filter functionality)
    4. Section overline "FILES"
    5. FadeInList of document cards. Update document card styling per D-20: 52dp icon container with AppColors.moduleVaultLight background, file type icon in AppColors.moduleVault color, title line, "fileSize · uploadDate · uploaderName" metadata line. Surface bg, 24dp radius, cardShadow.
    - Loading: SkeletonLoader.documentList() (already exists)
    - Empty: EmptyStateView with icon Iconsax.document, title "No documents yet", message "Upload tickets, booking confirmations, or any trip files.", CTA "Upload Document", accentGradient LinearGradient(colors: [Color(0xFF8B7355), Color(0xFFA89372)]) (warm bronze per D-18)
    - Remove any CircularProgressIndicator

    After all three rewrites, run tests for gear and logistics to verify no regressions.
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla && flutter analyze lib/features/gear/screens/gear_screen.dart lib/features/logistics/screens/logistics_screen.dart lib/features/vault/screens/vault_screen.dart 2>&1 | tail -5 && flutter test test/features/gear_screen_mutations_test.dart test/features/logistics_screen_mutations_test.dart --no-pub 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - lib/features/gear/screens/gear_screen.dart contains `ModuleHeader(` with `useDarkTheme: true`
    - lib/features/gear/screens/gear_screen.dart contains `GearHeroCard(`
    - lib/features/gear/screens/gear_screen.dart contains `EmptyStateView(` with `accentGradient`
    - lib/features/gear/screens/gear_screen.dart does NOT contain `CircularProgressIndicator`
    - lib/features/logistics/screens/logistics_screen.dart contains `LogisticsHeroCard(`
    - lib/features/logistics/screens/logistics_screen.dart contains `SubGroupCard(`
    - lib/features/logistics/screens/logistics_screen.dart does NOT contain `AppTabBar` or `TabBarView` or `TabController` or `SingleTickerProviderStateMixin`
    - lib/features/logistics/screens/logistics_screen.dart contains `EmptyStateView(` with `accentGradient`
    - lib/features/logistics/screens/logistics_screen.dart does NOT contain `CircularProgressIndicator`
    - lib/features/vault/screens/vault_screen.dart contains `VaultHeroCard(`
    - lib/features/vault/screens/vault_screen.dart contains `EmptyStateView(` with `accentGradient`
    - lib/features/vault/screens/vault_screen.dart does NOT contain `CircularProgressIndicator`
    - `flutter analyze` reports no errors on all three files
    - gear and logistics tests pass
  </acceptance_criteria>
  <done>Gear, Logistics, and Vault screens rewritten with unified module template: dark ModuleHeader, hero cards, section overlines, FadeInList content. Logistics tab bar removed. All use SkeletonLoader for loading, EmptyStateView with earthy gradient circles for empty state. Tests pass.</done>
</task>

</tasks>

<verification>
- `flutter analyze lib/features/gear/ lib/features/logistics/ lib/features/vault/` — zero errors
- `flutter test test/features/gear_screen_mutations_test.dart test/features/logistics_screen_mutations_test.dart --no-pub` — all tests pass
- `grep -c 'CircularProgressIndicator' lib/features/gear/screens/gear_screen.dart lib/features/logistics/screens/logistics_screen.dart lib/features/vault/screens/vault_screen.dart` — all return 0
- `grep -c 'AppTabBar\|TabBarView\|TabController' lib/features/logistics/screens/logistics_screen.dart` — returns 0
</verification>

<success_criteria>
- Gear screen shows GearHeroCard with packed progress, priority badge, Add Item CTA
- Logistics screen shows LogisticsHeroCard, SubGroupCard list, no tabs
- Vault screen shows VaultHeroCard with file count and upload CTA
- All three screens use dark ModuleHeader, SkeletonLoader, EmptyStateView with earthy gradient circles
- No CircularProgressIndicator remains in any of the three screens
- Existing tests pass without regressions
</success_criteria>

<output>
After completion, create `.planning/phases/21-module-screens-redesign/21-03-SUMMARY.md`
</output>
