---
phase: 21-module-screens-redesign
plan: 06
type: execute
wave: 3
depends_on: [21-01]
files_modified:
  - lib/features/onboarding/screens/onboarding_screen.dart
  - lib/core/router/app_router.dart
autonomous: true
requirements: [SCRN-06]

must_haves:
  truths:
    - "Onboarding pages have white background with warm gradient icon circles"
    - "Onboarding dot indicators use terracotta (#CC6B49) filled/outlined dots"
    - "Onboarding final page CTA is a terracotta ElevatedButton (not teal) with text 'Get Started'"
    - "Splash screen has warm sand (#F2E8D6) background with dark brown centered app name"
    - "Onboarding title in dark brown (textPrimary), subtitle in warm gray (textSecondary)"
  artifacts:
    - path: "lib/features/onboarding/screens/onboarding_screen.dart"
      provides: "Redesigned onboarding with light background, earthy circles, terracotta dots"
      contains: "DotStepIndicator"
    - path: "lib/core/router/app_router.dart"
      provides: "Splash screen with warm sand background"
      contains: "Color(0xFFF2E8D6)"
  key_links:
    - from: "lib/features/onboarding/screens/onboarding_screen.dart"
      to: "lib/shared/widgets/dot_step_indicator.dart"
      via: "DotStepIndicator for page dots"
      pattern: "DotStepIndicator"
    - from: "lib/features/onboarding/screens/onboarding_screen.dart"
      to: "lib/core/theme/app_theme.dart"
      via: "AppColors.terracotta for CTA button"
      pattern: "AppColors\\.terracotta"
---

<objective>
Redesign onboarding flow and splash screen with warm earthy aesthetics. Onboarding switches from dark background to light with earthy gradient circles and terracotta dots. Splash changes to warm sand background.

Purpose: First impressions matter. The onboarding and splash are the user's first encounter with the app. Warm sand/terracotta aesthetics set the visual tone before they see any data screens.
Output: Redesigned onboarding_screen.dart and updated _SplashScreen in app_router.dart.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-module-screens-redesign/21-CONTEXT.md
@.planning/phases/21-module-screens-redesign/21-UI-SPEC.md
@.planning/phases/21-module-screens-redesign/21-01-SUMMARY.md

<interfaces>
From lib/features/onboarding/screens/onboarding_screen.dart (current):
```dart
// Current: dark background (AppColors.surfaceDark), animated blobs, 3-page PageView
// PageController for page navigation
// SharedPreferences to mark onboarding complete
// D-29: switch to light background, terracotta dots, earthy icon circles
```

From lib/core/router/app_router.dart (_SplashScreen):
```dart
class _SplashScreen extends StatelessWidget {
  // Current: surfaceDark background, teal gradient logo box
  // D-30: warm sand (#F2E8D6) background, dark brown app name
}
```

From lib/shared/widgets/dot_step_indicator.dart:
```dart
class DotStepIndicator extends StatelessWidget {
  final int stepCount;
  final int currentStep;
  final Color activeColor;
  final bool showCheckmarks;  // false for onboarding (no complete state)
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign onboarding screen with light background, earthy circles, and terracotta dots</name>
  <files>lib/features/onboarding/screens/onboarding_screen.dart</files>
  <read_first>
    - lib/features/onboarding/screens/onboarding_screen.dart
    - lib/shared/widgets/dot_step_indicator.dart
    - lib/core/theme/app_theme.dart
    - .planning/phases/21-module-screens-redesign/21-UI-SPEC.md (Onboarding Screen section)
    - .planning/phases/21-module-screens-redesign/21-RESEARCH.md (Pitfall 6: Onboarding Screen Background)
  </read_first>
  <action>
    Redesign the onboarding screen per D-29:

    **Scaffold background:**
    Change from `AppColors.surfaceDark` (#111827 dark) to `AppColors.background` (#FFFFFF white). The warm feel comes from the icon circles and terracotta dots, not the page background.

    **Remove animated blob background:**
    The current onboarding has animated background blobs with per-page accent colors. Remove these entirely — the new design uses a clean white background. Delete the blob animation widgets, AnimationControllers for blobs, and any per-page color configuration for blobs.

    **Page content for each of the 3 pages:**
    Each page should contain (centered in a Column):
    - Large icon (48dp) inside a 72dp warm gradient circle: Container(width: 72, height: 72, decoration: BoxDecoration(shape: BoxShape.circle, gradient: LinearGradient(colors: [earthy color 1, earthy color 2])), child: Icon(pageIcon, size: 48, color: Colors.white))
    - Use different earthy gradients for each page (Claude's discretion — e.g., page 1: terracotta, page 2: olive, page 3: dusty teal from the D-18 palette)
    - SizedBox(height: 24)
    - Title text: 20sp, w600, AppColors.textPrimary (#111827 dark)
    - SizedBox(height: 8)
    - Subtitle text: 14sp, w400, AppColors.textSecondary (#6B7280)
    - Keep existing page titles and subtitles (the actual copy), just change the text styling

    **Dot indicators:**
    Replace any existing page indicator with DotStepIndicator:
    ```dart
    DotStepIndicator(
      stepCount: 3,
      currentStep: _currentPage,  // from PageController
      activeColor: AppColors.terracotta,  // #CC6B49
      showCheckmarks: false,  // onboarding has no "complete" state, only active/upcoming
    )
    ```
    Position below the page content, above the CTA button area.

    **Final page CTA (D-29 locked — terracotta, not teal):**
    The "Get Started" button on the final page must use terracotta background:
    ```dart
    SizedBox(
      width: double.infinity,
      height: AppColors.buttonHeight,  // 52dp
      child: ElevatedButton(
        onPressed: () { /* existing onboarding complete logic */ },
        style: ElevatedButton.styleFrom(
          backgroundColor: AppColors.terracotta,  // #CC6B49, NOT primary teal
          foregroundColor: AppColors.textOnPrimary,  // white
        ),
        child: const Text('Get Started'),
      ),
    )
    ```

    **Non-final page "Next" or "Skip" buttons:**
    Keep existing navigation logic. Style any "Next" button as teal ElevatedButton (standard primary). Style any "Skip" button as TextButton with textSecondary color.

    Important: Keep the PageController, page change logic, SharedPreferences writing for onboarding completion, and all existing navigation callbacks intact. Only change the visual treatment.
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla && flutter analyze lib/features/onboarding/screens/onboarding_screen.dart 2>&1 | tail -5 && flutter test --no-pub 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - lib/features/onboarding/screens/onboarding_screen.dart contains `AppColors.background` for scaffold background (NOT `surfaceDark`)
    - lib/features/onboarding/screens/onboarding_screen.dart contains `DotStepIndicator(`
    - lib/features/onboarding/screens/onboarding_screen.dart contains `showCheckmarks: false`
    - lib/features/onboarding/screens/onboarding_screen.dart contains `AppColors.terracotta` for dot indicator activeColor
    - lib/features/onboarding/screens/onboarding_screen.dart contains `Get Started` button text
    - lib/features/onboarding/screens/onboarding_screen.dart contains `backgroundColor: AppColors.terracotta` for final CTA button
    - lib/features/onboarding/screens/onboarding_screen.dart contains `BoxShape.circle` for icon circles
    - lib/features/onboarding/screens/onboarding_screen.dart contains `Icon(` with `size: 48`
    - lib/features/onboarding/screens/onboarding_screen.dart does NOT contain `surfaceDark` as scaffold bg
    - `flutter analyze` reports no errors
    - `flutter test --no-pub` passes
  </acceptance_criteria>
  <done>Onboarding screen has white background, 72dp warm gradient icon circles with 48dp white icons, terracotta DotStepIndicator (no checkmarks), and terracotta "Get Started" CTA on final page. Animated blob background removed. Page content text uses textPrimary/textSecondary. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Update splash screen to warm sand background with dark brown app name</name>
  <files>lib/core/router/app_router.dart</files>
  <read_first>
    - lib/core/router/app_router.dart
    - lib/core/theme/app_theme.dart
    - .planning/phases/21-module-screens-redesign/21-UI-SPEC.md (Splash Screen section)
  </read_first>
  <action>
    Update the `_SplashScreen` widget in app_router.dart per D-30:

    **Change scaffold background:**
    From `AppColors.surfaceDark` to `const Color(0xFFF2E8D6)` (warm sand). This is a D-30 locked value — use the hex directly since it is NOT an AppColors constant (it's a one-off splash-specific color).

    **Change body content:**
    Replace the current teal gradient logo box with a minimal warm design:
    ```dart
    @override
    Widget build(BuildContext context) {
      return Scaffold(
        backgroundColor: const Color(0xFFF2E8D6),  // warm sand per D-30
        body: Center(
          child: Text(
            'Rihla',
            style: TextStyle(
              fontSize: 28,
              fontWeight: FontWeight.w600,
              color: AppColors.textPrimary,  // #111827 dark
              fontFamily: 'Plus Jakarta Sans',
            ),
          ),
        ),
      );
    }
    ```

    Remove the teal gradient container, the icon, and the box shadow. The splash is now minimal: warm sand background, centered app name in dark text, nothing else. No animation (Phase 22 concern).

    Note: The splash screen is a private class `_SplashScreen` defined at the bottom of app_router.dart. It is only used as the builder for the '/' route. Be careful not to modify the route configuration or redirect logic — only change the widget's build method.
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla && flutter analyze lib/core/router/app_router.dart 2>&1 | tail -5 && flutter test --no-pub 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - lib/core/router/app_router.dart contains `Color(0xFFF2E8D6)` as splash background
    - lib/core/router/app_router.dart _SplashScreen build method contains `Rihla` text
    - lib/core/router/app_router.dart _SplashScreen build method contains `fontSize: 28` and `fontWeight: FontWeight.w600`
    - lib/core/router/app_router.dart _SplashScreen does NOT contain `AppColors.surfaceDark` in the splash widget
    - lib/core/router/app_router.dart _SplashScreen does NOT contain the teal gradient logo container
    - `flutter analyze` reports no errors
    - `flutter test --no-pub` passes
  </acceptance_criteria>
  <done>Splash screen has warm sand (#F2E8D6) background with centered "Rihla" text in 28sp w600 dark brown. Teal gradient logo removed. Minimal and warm. All tests pass.</done>
</task>

</tasks>

<verification>
- `flutter analyze lib/features/onboarding/ lib/core/router/app_router.dart` — zero errors
- `flutter test --no-pub` — full suite passes
- `grep -c 'surfaceDark' lib/features/onboarding/screens/onboarding_screen.dart` — returns 0
- `grep 'Color(0xFFF2E8D6)' lib/core/router/app_router.dart` — returns a match
</verification>

<success_criteria>
- Onboarding has white background, warm gradient icon circles, terracotta dots, terracotta final CTA
- Animated blob background is removed from onboarding
- Splash has warm sand (#F2E8D6) background with dark centered app name
- Teal gradient logo box is removed from splash
- All onboarding logic (PageController, SharedPreferences, navigation) unchanged
- All tests pass without regressions
</success_criteria>

<output>
After completion, create `.planning/phases/21-module-screens-redesign/21-06-SUMMARY.md`
</output>
