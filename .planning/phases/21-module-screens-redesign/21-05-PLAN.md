---
phase: 21-module-screens-redesign
plan: 05
type: execute
wave: 3
depends_on: [21-01]
files_modified:
  - lib/features/ledger/screens/add_expense_screen.dart
  - lib/features/groups/screens/create_group_screen.dart
  - lib/features/groups/screens/join_group_screen.dart
  - lib/features/events/screens/create_event_screen.dart
  - lib/features/settings/screens/settings_screen.dart
autonomous: true
requirements: [SCRN-05]

must_haves:
  truths:
    - "Add Expense screen shows terracotta step indicator dots (filled for current, outlined for upcoming, checked for complete)"
    - "All form fields globally use sand fill (#F5EDE1), warm gray border (#E5D5C0), terracotta focus border (#CC6B49)"
    - "Form sections in all flows are wrapped in surface card containers with 24dp radius"
    - "Settings screen uses iOS-style grouped section cards (Profile, Preferences, About)"
    - "All form submit buttons use teal ElevatedButton (primary) per established pattern"
  artifacts:
    - path: "lib/features/ledger/screens/add_expense_screen.dart"
      provides: "Add Expense with DotStepIndicator and card-wrapped sections"
      contains: "DotStepIndicator"
    - path: "lib/features/groups/screens/create_group_screen.dart"
      provides: "Create Group reskinned with card sections"
      contains: "AppColors.surface"
    - path: "lib/features/groups/screens/join_group_screen.dart"
      provides: "Join Group reskinned with card section"
      contains: "AppColors.surface"
    - path: "lib/features/events/screens/create_event_screen.dart"
      provides: "Create Event reskinned with card sections"
      contains: "AppColors.surface"
    - path: "lib/features/settings/screens/settings_screen.dart"
      provides: "Settings with grouped section cards"
      contains: "Profile"
  key_links:
    - from: "lib/features/ledger/screens/add_expense_screen.dart"
      to: "lib/shared/widgets/dot_step_indicator.dart"
      via: "DotStepIndicator import for step display"
      pattern: "DotStepIndicator"
    - from: "all form screens"
      to: "lib/core/theme/app_theme.dart"
      via: "InputDecorationTheme applies globally (no per-screen changes needed)"
      pattern: "AppColors\\.sandLight"
---

<objective>
Reskin all form flows (Add Expense, Create/Join Group, Create Event) and Settings screen with the new design language. Form logic stays unchanged — only visual treatment (D-25).

Purpose: Forms are the primary write interaction in the app. Warm card containers and terracotta focus rings give forms the same earthy feel as the redesigned module screens. The InputDecorationTheme from Plan 01 handles most styling globally; this plan adds card containers and the DotStepIndicator.
Output: Reskinned form flows with card-wrapped sections + Settings with iOS-style section cards.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-module-screens-redesign/21-CONTEXT.md
@.planning/phases/21-module-screens-redesign/21-UI-SPEC.md
@.planning/phases/21-module-screens-redesign/21-01-SUMMARY.md

<interfaces>
<!-- The InputDecorationTheme is already updated globally by Plan 01. Form fields pick up sand fill, warm gray border, terracotta focus automatically. -->
<!-- DotStepIndicator is created in Plan 01. -->

From lib/shared/widgets/dot_step_indicator.dart:
```dart
class DotStepIndicator extends StatelessWidget {
  final int stepCount;
  final int currentStep;  // 0-indexed
  final Color activeColor;
  final bool showCheckmarks;
  // ...
}
```

Card wrapper pattern for form sections:
```dart
Container(
  margin: const EdgeInsets.symmetric(horizontal: 16, vertical: 8),
  padding: const EdgeInsets.all(16),
  decoration: BoxDecoration(
    color: AppColors.surface,
    borderRadius: BorderRadius.circular(24), // radiusLarge per D-10
    boxShadow: AppColors.cardShadow,
  ),
  child: /* form fields */,
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add DotStepIndicator to Add Expense + wrap form sections in cards</name>
  <files>lib/features/ledger/screens/add_expense_screen.dart</files>
  <read_first>
    - lib/features/ledger/screens/add_expense_screen.dart
    - lib/shared/widgets/dot_step_indicator.dart
    - lib/core/theme/app_theme.dart
    - .planning/phases/21-module-screens-redesign/21-UI-SPEC.md (Add Expense Screen section)
  </read_first>
  <action>
    The Add Expense screen has a 3-step flow. Apply two changes:

    **1. Add DotStepIndicator (D-27):**

    Below the ModuleHeader (or AppBar), add the DotStepIndicator widget:
    ```dart
    Padding(
      padding: const EdgeInsets.symmetric(vertical: 16),
      child: DotStepIndicator(
        stepCount: 3,
        currentStep: _currentStep, // the existing step index variable
        activeColor: AppColors.terracotta,
        showCheckmarks: true,
      ),
    )
    ```
    Position this between the header and the form content. If the screen uses a stepper or page view, place the DotStepIndicator as a fixed element above the page content.

    **2. Wrap form sections in card containers (D-25):**

    Each step's form content should be wrapped in a card container:
    ```dart
    Container(
      margin: const EdgeInsets.symmetric(horizontal: 16, vertical: 8),
      padding: const EdgeInsets.all(16),
      decoration: BoxDecoration(
        color: AppColors.surface,
        borderRadius: BorderRadius.circular(24),
        boxShadow: AppColors.cardShadow,
      ),
      child: /* existing form fields for this step */,
    )
    ```

    Do NOT change the form logic, validation, submit handlers, or step navigation. Only add the visual wrapper and step indicator.

    The InputDecorationTheme already applies globally from Plan 01 — all TextFormField widgets will pick up sand fill (#F5EDE1), warm gray border (#E5D5C0), terracotta focus (#CC6B49) automatically. No per-field decoration changes needed.

    Remove any existing step indicator that may conflict (e.g., old dot indicators, linear progress bars for step tracking).
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla && flutter analyze lib/features/ledger/screens/add_expense_screen.dart 2>&1 | tail -5 && flutter test test/features/ledger_test.dart --no-pub 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - lib/features/ledger/screens/add_expense_screen.dart contains `DotStepIndicator(`
    - lib/features/ledger/screens/add_expense_screen.dart contains `stepCount: 3`
    - lib/features/ledger/screens/add_expense_screen.dart contains `AppColors.terracotta` as activeColor
    - lib/features/ledger/screens/add_expense_screen.dart contains at least one Container with `AppColors.surface` and `borderRadius: BorderRadius.circular(24)` for card wrapping
    - `flutter analyze` reports no errors
    - ledger tests pass
  </acceptance_criteria>
  <done>Add Expense screen has terracotta DotStepIndicator for 3-step flow and form sections wrapped in surface card containers with 24dp radius. Form logic unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Reskin Create Group, Join Group, Create Event forms + Settings section cards</name>
  <files>lib/features/groups/screens/create_group_screen.dart, lib/features/groups/screens/join_group_screen.dart, lib/features/events/screens/create_event_screen.dart, lib/features/settings/screens/settings_screen.dart</files>
  <read_first>
    - lib/features/groups/screens/create_group_screen.dart
    - lib/features/groups/screens/join_group_screen.dart
    - lib/features/events/screens/create_event_screen.dart
    - lib/features/settings/screens/settings_screen.dart
    - lib/core/theme/app_theme.dart
    - test/features/groups/create_join_group_test.dart
    - .planning/phases/21-module-screens-redesign/21-UI-SPEC.md (Form Flows section, Settings section)
  </read_first>
  <action>
    **Create Group Screen (D-25 reskin):**
    - Wrap form fields in a card container: Container(margin: EdgeInsets.symmetric(horizontal: 16, vertical: 8), padding: EdgeInsets.all(16), decoration: BoxDecoration(color: AppColors.surface, borderRadius: BorderRadius.circular(24), boxShadow: AppColors.cardShadow), child: Column of form fields)
    - Ensure the submit button is a full-width teal ElevatedButton with text "Create Group" (already should be — verify and fix if not)
    - Do NOT change form logic, validation, or navigation
    - InputDecorationTheme applies globally — form fields get sand fill, warm gray border, terracotta focus automatically

    **Join Group Screen (D-25 reskin):**
    - Same card container pattern for the invite code input field
    - Full-width teal ElevatedButton "Join Group" for submit
    - The terracotta focus ring on the code input field comes from the global InputDecorationTheme (Plan 01)
    - Do NOT change form logic

    **Create Event Screen (D-25 reskin):**
    - Wrap form sections in card containers
    - Event type picker cards: selected state uses AppColors.moduleLedgerLight (#E6F5F3, teal-10% tint) background, unselected uses AppColors.border for border only. This replaces any previous selected/unselected styling.
    - Full-width teal ElevatedButton "Create Event" for submit
    - Do NOT change event type logic, module toggle behavior, or participant selection

    **Settings Screen (D-28 — iOS grouped section cards):**

    Reorganize the settings content into three grouped sections. Each section is a card:
    ```dart
    Container(
      margin: const EdgeInsets.symmetric(horizontal: 16, vertical: 8),
      decoration: BoxDecoration(
        color: AppColors.surface,
        borderRadius: BorderRadius.circular(24),
        boxShadow: AppColors.cardShadow,
      ),
      child: Column(
        children: [
          // Section header (optional — "Profile", "Preferences", "About")
          // ListTile items with AppColors.textPrimary title and AppColors.textSecondary subtitle
        ],
      ),
    )
    ```

    Three sections:
    - **Profile card**: Device name display/edit, user ID, or any profile-related settings
    - **Preferences card**: Notification toggles, currency preferences, or any preference-related settings
    - **About card**: App version, licenses, support/feedback links

    Use standard ListTile widgets inside each card. The ListTile inherits textPrimary title and textSecondary subtitle from the theme. Add dividers between ListTile items using Divider(height: 1, indent: 16, endIndent: 16, color: AppColors.border).

    Remove any existing layout that doesn't match this card pattern. Keep all setting logic, toggle handlers, navigation callbacks unchanged.

    After all four files, run tests to verify no regressions.
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla && flutter analyze lib/features/groups/screens/create_group_screen.dart lib/features/groups/screens/join_group_screen.dart lib/features/events/screens/create_event_screen.dart lib/features/settings/screens/settings_screen.dart 2>&1 | tail -5 && flutter test test/features/groups/ --no-pub 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - lib/features/groups/screens/create_group_screen.dart contains `AppColors.surface` in a card decoration
    - lib/features/groups/screens/create_group_screen.dart contains `BorderRadius.circular(24)` for card wrapping
    - lib/features/groups/screens/join_group_screen.dart contains `AppColors.surface` in a card decoration
    - lib/features/events/screens/create_event_screen.dart contains `AppColors.surface` in a card decoration
    - lib/features/events/screens/create_event_screen.dart contains `AppColors.moduleLedgerLight` for selected event type
    - lib/features/settings/screens/settings_screen.dart contains `ListTile` (for grouped section items)
    - lib/features/settings/screens/settings_screen.dart contains at least 3 Container widgets with `AppColors.surface` and `borderRadius: BorderRadius.circular(24)` for the three section cards
    - `flutter analyze` reports no errors on all four files
    - group tests pass
  </acceptance_criteria>
  <done>Create Group, Join Group, Create Event forms wrapped in surface card containers. Settings restructured into three iOS-style grouped section cards (Profile, Preferences, About) with ListTile items. All form logic unchanged. InputDecorationTheme provides sand/warm gray/terracotta styling globally. Tests pass.</done>
</task>

</tasks>

<verification>
- `flutter analyze lib/features/groups/screens/ lib/features/events/screens/ lib/features/settings/screens/ lib/features/ledger/screens/add_expense_screen.dart` — zero errors
- `flutter test test/features/groups/ --no-pub` — all tests pass
- `flutter test --no-pub` — full suite passes
- `grep -c 'DotStepIndicator' lib/features/ledger/screens/add_expense_screen.dart` — returns at least 1
</verification>

<success_criteria>
- Add Expense has terracotta DotStepIndicator with 3 steps
- All form flows have form sections wrapped in surface card containers (24dp radius, cardShadow)
- Create Group, Join Group, Create Event have full-width teal submit buttons
- Settings has three section cards (Profile, Preferences, About) with ListTile items
- InputDecorationTheme applies sand fill and terracotta focus globally
- All form logic is unchanged — only visual treatment updated
</success_criteria>

<output>
After completion, create `.planning/phases/21-module-screens-redesign/21-05-SUMMARY.md`
</output>
