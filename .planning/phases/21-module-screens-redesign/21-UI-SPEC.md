---
phase: 21
slug: module-screens-redesign
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-30
---

# Phase 21 — UI Design Contract

> Visual and interaction contract for Module Screens Redesign. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (Flutter native — ThemeExtension token system) |
| Preset | not applicable |
| Component library | Flutter Material 3 + custom shared widgets (`lib/shared/widgets/`) |
| Icon library | Iconsax (already used across all prior phases) |
| Font | Plus Jakarta Sans (via `google_fonts` package) |

**Source:** `lib/core/theme/app_theme.dart`, CONTEXT.md, RESEARCH.md

---

## Spacing Scale

All values from `AppSpacingTokens.standard` (`lib/core/theme/tokens/spacing_tokens.dart`).

| Token | Value | Usage |
|-------|-------|-------|
| `space4` | 4dp | Icon gaps, inline padding, overline bottom margins |
| `space8` | 8dp | Compact element spacing, between card sub-elements |
| `space12` | 12dp | Grid gaps (module grid crossAxisSpacing / mainAxisSpacing), between cards. Non-canonical but named token in `AppSpacingTokens.standard`; justified for tight grid contexts where 8dp is too cramped and 16dp wastes too much horizontal space. |
| `space16` | 16dp | Card internal padding (all sides), screen horizontal padding, section margins |
| `space20` | 20dp | Hero card internal padding, FAB/bottom-edge clearance. Non-canonical but named token in `AppSpacingTokens.standard`; justified for hero card breathing room where 16dp feels tight against large display text and 24dp wastes vertical space. |
| `space24` | 24dp | Section separators, large title horizontal padding, empty state outer padding |
| `space32` | 32dp | Empty state outer padding (all sides), header bottom padding |

**Border Radius** (from `AppSpacingTokens.standard`):

| Token | Value | Usage |
|-------|-------|-------|
| `radiusSmall` | 8dp | Chips, tags, photo grid thumbnails (Memories) |
| `radiusMedium` | 12dp | Buttons, input fields |
| `radiusLarge` | 16dp | All content cards, hero cards, sheets, module cards |

**Fixed Dimensions (non-token but established):**

| Element | Value | Source |
|---------|-------|--------|
| Button height | 52dp (`AppColors.buttonHeight`) | AppSpacingTokens.standard |
| Minimum touch target | 48dp | WCAG 2.1 AA (applies to all tappable rows) |
| ModuleHeader icon touch target | 44dp | Established in Phase 20 |
| Empty state icon container | 72dp circle | D-17, consistent with Phase 18 pattern |
| Empty state icon size | 48dp | D-17 |
| Activity row height (min) | 48dp | WCAG touch target |
| Logistics capacity bar height | 4dp | D-22, thin progress indicator |
| Memories photo grid gap | 8dp | D-24 |
| Step indicator dot size | 8dp filled / 8dp outlined | D-27 |

**Exceptions:** None beyond listed fixed dimensions.

**Source:** `lib/core/theme/tokens/spacing_tokens.dart`, CONTEXT.md D-10, D-17, D-22, D-24, D-27

---

## Typography

Font: **Plus Jakarta Sans** for all text. All sizes in logical pixels (Flutter `sp` equivalent).

| Role | Size | Weight | Line Height | Token / Usage |
|------|------|--------|-------------|---------------|
| Display | 28sp | 600 | 1.2 | Module screen hero amount, large balance figures, app name on splash |
| Heading | 20sp | 600 | 1.2 | Hero card section titles, screen section headings, onboarding page titles |
| Body | 14sp | 400 | 1.5 | Card primary text (expense title, file name, gear item name), card secondary text, metadata, payer · date · participant count, overlines (ALL-CAPS, letter-spacing 0.5), capacity fractions, chip text, step dots |
| Small | 12sp | 400 | 1.3 | Timestamps, file sizes, decorative metadata, avatar initials |

**Declared weights: 400 (regular) and 600 (semibold).** These are the only two weights used in this phase. Display and Heading use 600 (semibold). Body and Small use 400 (regular). No other weights are introduced.

**Migration note:** Prior spec draft listed 700 and 800 separately. Those uses are consolidated to 600 here. The visual distinction between heading hierarchy levels is achieved through size contrast (28sp vs 20sp), not weight variation.

**Color rules:**
- Primary body text: `AppColors.textPrimary` (#111827) — 17.15:1 on white (AAA)
- Secondary / metadata: `AppColors.textSecondary` (#6B7280) — 5.74:1 on white (AA)
- Muted / decorative: `AppColors.textMuted` (#9CA3AF) — DECORATIVE ONLY (2.86:1, below AA). Never use for functional labels, amounts, or status text.
- On primary (teal button text): `AppColors.textOnPrimary` (#FFFFFF) — 5.12:1 on teal (AA)
- Financial positive ("Owed to you"): `AppColors.successText` (#047857) — 5.92:1 on white (AA)
- Financial negative ("You owe"): `AppColors.errorText` (#B91C1C) — 6.57:1 on white (AA)
- Financial neutral ("Settled"): `AppColors.textSecondary` (#6B7280) — 5.74:1 on white (AA)
- Header text (on dark gradient): `AppColors.textOnPrimary` (#FFFFFF)
- Header metadata (on dark gradient): `Colors.white.withValues(alpha: 0.5)` — decorative sub-text only

**Source:** `lib/core/theme/app_theme.dart` `_buildTextTheme()`, CONTEXT.md D-02, Phase 16 WCAG verification

---

## Color

Canonical palette: **AppColorTokens.light** (`lib/core/theme/tokens/color_tokens.dart`). Reference by `AppColors.*` facade constants.

| Role | Token | Hex | Usage |
|------|-------|-----|-------|
| Dominant (60%) | `AppColors.background` | #FFFFFF | Scaffold background, page surface |
| Secondary (30%) | `AppColors.surface` / `AppColors.cardSurface` | #F8F9FA | Card surfaces, hero card backgrounds, skeleton backgrounds |
| Accent (10%) | `AppColors.primary` / `AppColors.moduleLedger` | #0D7B74 | See accent reserved-for list below |
| Input fill | `AppColors.sandLight` / `AppColors.inputFill` | #F5EDE1 | Form field fill colors (D-26) |
| Border | `AppColors.border` | #E5E7EB | Card hairline borders (1px), skeleton shimmer bars |
| Destructive | `AppColors.errorText` | #B91C1C | Balance "you owe" text, delete confirmation text |
| Destructive display | `AppColors.error` | #EF4444 | Error icons, error border in form fields |

**Accent (#0D7B74 teal) is reserved for:**
- Primary CTA buttons (`ElevatedButton` background)
- Ledger module icon container fill (`AppColors.moduleLedgerLight` #E6F5F3) and icon itself
- Ledger empty state icon circle background gradient
- Settlement timeline rows checkmark icon and border accent
- Add Expense / Settle Up hero card inline CTA buttons
- Step indicator dots (active/complete) in Add Expense flow (D-27)
- Onboarding dot indicators (active) (D-29)
- Balance total line color for positive net balance (`AppColors.successText` is dark green, not teal — see financial color rule)

**Non-Ledger module accents** (gray, not teal):
- Gear, Logistics, Vault, Activity, Memories icon containers: `AppColors.moduleGearLight` etc. (#F3F4F6 gray-100)
- Gear, Logistics, Vault, Activity, Memories icons: `AppColors.moduleGear` etc. (#6B7280 gray-500)

**Empty state icon circle gradients** (D-18 — earthy palette retained for empty state circles only):
- Ledger: terracotta circle — `Color(0xFFCC6B49)` / `Color(0xFFE0896A)` gradient
- Gear: olive circle — `Color(0xFF7A8C5E)` / `Color(0xFF96A876)` gradient
- Logistics: dusty teal circle — `Color(0xFF5B7B8C)` / `Color(0xFF7B9BAC)` gradient
- Vault: warm bronze circle — `Color(0xFF8B7355)` / `Color(0xFFA89372)` gradient
- Memories: desert sand circle — `Color(0xFF9B7A5C)` / `Color(0xFFB89878)` gradient
- Activity: caramel circle — `Color(0xFFA67C5B)` / `Color(0xFFC29A7A)` gradient
- Note: These earthy gradient values are for empty state circles only. All other UI uses the monochrome + teal token system.

**Header gradient** (all module screens):
- Start: `AppColors.headerGradientStart` (#111827 gray-900)
- End: `AppColors.headerGradientEnd` (#1F2937 gray-800)

**Form field colors** (D-26 — locked decision):
- Fill: `AppColors.sandLight` (#F5EDE1)
- Enabled border: `AppColors.warmGray` (#E5D5C0 / 1.5dp)
- Focused border: `AppColors.terracotta` (#CC6B49 / 2dp)
- Error border: `AppColors.error` (#EF4444 / 1.5dp)
- Label text: `AppColors.textSecondary`
- Floating label: `AppColors.terracotta` (#CC6B49)
- Hint text: `AppColors.textMuted` (decorative only)
- Border radius: 12dp (`radiusMedium`)

**Source:** CONTEXT.md D-02, D-09, D-17, D-18, D-26; `lib/core/theme/tokens/color_tokens.dart`; Phase 16 palette reconciliation

---

## Screen Inventory and State Matrix

Each screen must implement all four states using Phase 17 animation and skeleton primitives.

### Module Screens (6 total)

All follow the unified template (D-08): `ModuleHeader(useDarkTheme: true)` → Summary Hero Card → section overline → content list/grid.

#### Ledger Screen

**Layout:** Single scroll — no tabs (D-04). Removes current AppTabBar.

| State | Component |
|-------|-----------|
| Loading | `SkeletonLoader.expenseList()` below hero skeleton |
| Empty | `EmptyStateView` with Ledger earthy circle + "Add Expense" CTA |
| Loaded | Hero card → `FadeInList` of expense cards + settlement rows |
| Error | `OfflineBanner` + `EmptyStateView` error config + "Reload" CTA |

**Hero card (D-03, D-11):** 2-row layout.
- Row 1: YOUR BALANCE (color-coded via `successText` / `errorText` / `textSecondary`) + EVENT TOTAL
- Row 2: expense count + settlement count
- Row 3: `[+ Add Expense]` teal `ElevatedButton` + `[Settle Up]` `OutlinedButton`

**Expense card (D-01, D-02):** Three-line card, `radiusLarge`, `surface` background, `cardShadow`.
- Line 1: category icon (Iconsax, 20dp, left) · expense title (body style, weight 600) · amount (body style, weight 600, right-aligned)
- Line 2: payer name · date · participant count (body style, weight 400, `textSecondary`)
- Line 3: balance status (D-02): `successText` "Owed to you OMR X" / `errorText` "You owe OMR X" / `textSecondary` "Settled"
- Card background: `AppColors.surface` — never tinted regardless of balance sign (D-02)
- Tap interaction: opens `EditExpenseSheet` bottom sheet (D-07)

**Settlement row (D-05):** Inline with expense timeline.
- Checkmark icon in `moduleLedger` teal, "Payer → Recipient" text, amount in `successText`, date in `textMuted`
- Visually distinct: left 3dp `moduleLedger` teal border accent on the card container

#### Gear Screen

**Hero card (D-12):** Packed X/Y progress line + Priority N items badge + `[+ Add Item]` teal button.
- Progress bar: thin (4dp) `moduleLedger` teal fill on `border` track — Claude's discretion on exact widget.
- Priority badge: `errorText` label on `surface` background chip.

**Content:** `FadeInList` of gear item cards. `SearchFilterBar` retained (D-08 discretion confirmed in CONTEXT.md canonical_refs).

#### Logistics Screen

**Layout:** Single scroll — no tabs (D-23). Removes current AppTabBar.

**Hero card (D-13):** N groups · M members + unassigned count + `[Create Group]` teal button.

**Sub-group card (D-22):**
- Line 1: group name + icon
- Line 2: capacity progress bar (4dp, `moduleLogistics` gray fill on `border` track) — dusty teal top border (3dp `moduleLogistics`)
- Member chips below: `selectionFill` background, `textSecondary` label text

#### Vault Screen

**Hero card (D-14):** N files + total size + `[Upload File]` teal button.

**Document card (D-20):** 52dp icon container (`moduleVaultLight` fill), file type icon (`moduleVault` gray), title, file size · upload date · uploader. Standard card style. `SearchFilterBar` retained.

#### Memories Screen

**Exception to card-list template (D-24):** Hero card above → 3-column photo grid.
- Hero card: photo count + date range + `[Add Photo]` teal button.
- Grid: `GridView.builder` (Claude's discretion on SliverGrid vs GridView) with 8dp gap, 8dp radius thumbnails.
- Tap thumbnail: full-screen viewer via `PageRouteBuilder(opaque: false)`.

#### Activity Screen

**Hero card (D-16):** N entries + last update. No CTA (read-only).

**Timeline (D-21):** Date-grouped flat list. Section headers ("TODAY", "YESTERDAY", "Mar 28") as sticky overlines (`textMuted`, 12sp, weight 400, ALL-CAPS, letter-spacing 0.5). Entry card: avatar circle (deterministic color from `hashCode % Colors.primaries.length`) + action text + relative time. No vertical connector line.

---

### Form Flows (SCRN-05)

#### Add Expense Screen (3-step, D-27)

**Step indicator:** Three terracotta dots (filled for current step, outlined for upcoming, checkmark icon for complete). Positioned below ModuleHeader. Uses terracotta `Color(0xFFCC6B49)` for dot fill — same earthy accent as Ledger empty state circle (consistent visual language).

**Form sections:** Warm card containers (surface background, radiusLarge, cardShadow) wrapping form fields within each step.

**InputDecorationTheme** updated in `app_theme.dart` to match D-26 (sand fill, terracotta focus border — see Form field colors above).

#### Create Group Screen

Reskin only (D-25): Apply D-26 `InputDecorationTheme`, wrap sections in surface cards, use `ElevatedButton` (primary teal) for submit.

#### Join Group Screen

Reskin only (D-25): Same card + button pattern. Single code-entry field with terracotta focus ring.

#### Create Event Screen

Reskin only (D-25): Type picker cards use `moduleLedgerLight` for selected state (teal-10% tint), `border` for unselected. Submit button teal `ElevatedButton`.

#### Settings Screen (D-28)

Section cards (iOS grouped table style): Profile card, Preferences card, About card. Each: `surface` background, `radiusLarge`, `cardShadow`. `ListTile` items inside. No custom rows — uses `Theme`-inherited `ListTile` with `AppColors.textPrimary` title and `AppColors.textSecondary` subtitle.

---

### Onboarding and Splash (SCRN-06)

#### Onboarding Screen (D-29)

- 3-page `PageView` structure (unchanged).
- Each page: large icon (48dp) in 72dp warm gradient circle (earthy palette per D-18 style), title 20sp weight 600 `textPrimary`, subtitle 14sp weight 400 `textSecondary`.
- Dot indicators: terracotta `Color(0xFFCC6B49)` filled dot (8dp) for active page, outlined ring same color for inactive. Extracted as shared `DotIndicator` widget if not already existing.
- Final page CTA: `ElevatedButton` (primary teal, full-width, 52dp height). Text: "Get Started".

#### Splash Screen (D-30)

- `Scaffold` with `AppColors.surface` (#F8F9FA) warm gray-50 background (not pure white — provides subtle warmth).
- App logo/name centered: name in 28sp weight 600 `AppColors.textPrimary`.
- No animation (Phase 22 concern).

---

## Copywriting Contract

### Primary CTAs

| Screen | CTA Label | Button Style |
|--------|-----------|-------------|
| Ledger hero | Add Expense | `ElevatedButton` (teal, 52dp) |
| Ledger hero secondary | Settle Up | `OutlinedButton` (border, 52dp) |
| Gear hero | Add Item | `ElevatedButton` (teal) |
| Logistics hero | Create Group | `ElevatedButton` (teal) |
| Vault hero | Upload File | `ElevatedButton` (teal) |
| Memories hero | Add Photo | `ElevatedButton` (teal) |
| Onboarding final page | Get Started | `ElevatedButton` (teal, full-width) |
| Create Group submit | Create Group | `ElevatedButton` (teal, full-width) |
| Join Group submit | Join Group | `ElevatedButton` (teal, full-width) |
| Create Event submit | Create Event | `ElevatedButton` (teal, full-width) |

### Empty State Copy

| Screen | Heading | Body | CTA |
|--------|---------|------|-----|
| Ledger | No expenses yet | Add your first expense to start tracking who paid what. | Add Expense |
| Gear | Nothing packed yet | Add gear items to track what everyone needs to bring. | Add Gear Item |
| Logistics | No sub-groups yet | Create sub-groups to organize transport, accommodation, or activities. | Create Sub-group |
| Vault | No documents yet | Upload tickets, booking confirmations, or any trip files. | Upload Document |
| Memories | No photos yet | Add photos to capture the best moments of this trip. | Add Photo |
| Activity | No activity yet | Actions by you and your group members will appear here. | (no CTA — read-only) |

### Error State Copy

| Screen | Heading | Body | CTA |
|--------|---------|------|-----|
| All module screens | Couldn't load [screen name] | Check your connection and try again. | Reload |
| Form submit failure | Something went wrong | Your changes weren't saved. Check your connection and try again. | Try Again |

### Destructive Actions

| Action | Confirmation Approach | Confirmation Copy |
|--------|----------------------|-------------------|
| Delete expense | `showDialog` — two-button | "Delete this expense? This can't be undone." [Cancel] [Delete] |
| Delete gear item | `showDialog` — two-button | "Remove this item from your gear list?" [Cancel] [Remove] |
| Delete document | `showDialog` — two-button | "Delete this document? It will be permanently removed." [Cancel] [Delete] |
| Remove photo | `showDialog` — two-button | "Remove this photo from Memories?" [Cancel] [Remove] |
| Leave group (Settings) | `showDialog` — two-button | "Leave this group? You'll lose access to all group data." [Cancel] [Leave] |

**Confirmation button colors:**
- Cancel: `OutlinedButton` with `textPrimary`
- Destructive action: `TextButton` with `errorText` (#B91C1C) — never filled red, per established Phase 20 pattern

### Form Validation Copy

| Field | Error Copy |
|-------|-----------|
| Required field empty | This field is required. |
| Amount zero or negative | Enter an amount greater than 0. |
| Amount format invalid | Enter a valid amount (e.g., 10.500). |
| Invite code wrong length | Invite codes are 6 characters. |
| Invite code not found | That invite code doesn't exist. Ask your group admin to check it. |

---

## Component Inventory

Pre-existing shared widgets to use without modification unless noted:

| Widget | File | Phase 21 Usage |
|--------|------|----------------|
| `ModuleHeader` | `lib/shared/widgets/module_header.dart` | All 6 module screens + form flows. Memories needs migration from custom header. |
| `EmptyStateView` | `lib/shared/widgets/empty_state_view.dart` | All 6 module screens + form error states. Needs `accentColor` parameter for D-17/D-18 earthy circle gradient. |
| `SkeletonLoader` | `lib/shared/widgets/skeleton_loader.dart` | Gear/Logistics/Vault have existing factories. Ledger needs `expenseList` factory (new). Memories needs `photoGrid` factory (new). |
| `SearchFilterBar` | `lib/shared/widgets/search_filter_bar.dart` | Gear and Vault — retained alongside hero cards. |
| `FadeInList` | `lib/shared/animations/fade_in_list.dart` | All module content lists. |
| `TapBounce` | `lib/shared/animations/tap_bounce.dart` | Gear items, expense cards, document cards. |
| `StaggeredGrid` | `lib/shared/animations/staggered_grid.dart` | Memories photo grid entrance animation. |
| `OfflineBanner` | `lib/shared/widgets/offline_banner.dart` | All module screen error states. |

**New widgets to create:**

| Widget | File | Purpose |
|--------|------|---------|
| `LedgerHeroCard` | `lib/features/ledger/widgets/ledger_hero_card.dart` | Balance + total + dual CTA (D-03, D-11) |
| `ExpenseCard` | `lib/features/ledger/widgets/expense_card.dart` | Three-line expense card (D-01, D-02) |
| `SettlementRow` | `lib/features/ledger/widgets/settlement_row.dart` | Settlement timeline entry (D-05) |
| `GearHeroCard` | `lib/features/gear/widgets/gear_hero_card.dart` | Packed progress + priority + CTA (D-12) |
| `LogisticsHeroCard` | `lib/features/logistics/widgets/logistics_hero_card.dart` | Groups/members + unassigned + CTA (D-13) |
| `SubGroupCard` | `lib/features/logistics/widgets/sub_group_card.dart` | Member chips + capacity bar (D-22) |
| `VaultHeroCard` | `lib/features/vault/widgets/vault_hero_card.dart` | File count + size + upload CTA (D-14) |
| `MemoriesHeroCard` | `lib/features/memories/widgets/memories_hero_card.dart` | Photo count + date range + add CTA (D-15) |
| `ActivityHeroCard` | `lib/features/activity/widgets/activity_hero_card.dart` | Entry count + last update (D-16) |
| `ActivityEntryCard` | `lib/features/activity/widgets/activity_entry_card.dart` | Avatar + action + time (D-21) |
| `DotStepIndicator` | `lib/shared/widgets/dot_step_indicator.dart` | Terracotta step dots for Add Expense + onboarding (D-27, D-29) |

---

## Interaction Contracts

### Navigation Transitions

All module screen navigations use the established `_slideRightTransition` (Offset(1,0) → zero, `Curves.easeOutCubic`) from `app_router.dart`. All bottom sheets use `showModalBottomSheet` with `isScrollControlled: true`.

| Trigger | Action | Transition |
|---------|--------|-----------|
| Tap back in ModuleHeader | `context.pop()` | Slide-left (reverse slide-right) |
| Tap expense card | Open `EditExpenseSheet` (bottom sheet) | Slide-up |
| Tap Vault document card | Open document viewer / signed URL | Slide-right |
| Tap Memories thumbnail | Full-screen viewer (`PageRouteBuilder(opaque: false)`) | Fade |
| Tap destructive action | `showDialog` confirmation | Standard dialog |
| Skeleton → content | `AnimatedSwitcher` crossfade | 300ms fade |
| Error → retry | Reload provider | Loading → loaded/error |

### Animation Contracts

| Element | Animation | Duration | Curve |
|---------|-----------|----------|-------|
| Module content list entrance | `FadeInList` stagger (50ms delay between items) | 200ms per item | `Curves.easeOut` |
| Memories photo grid entrance | `StaggeredGrid` fade-in | 150ms per cell | `Curves.easeOut` |
| Expense card tap | `TapBounce` (scale 0.97) | 100ms | `Curves.easeInOut` |
| Skeleton shimmer | `shimmer` package continuous loop | — | Linear |
| Skeleton → content crossfade | `AnimatedSwitcher` | 300ms | `Curves.easeIn` |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | not applicable (Flutter project) | not applicable |
| Third-party | none | not applicable |

No shadcn or third-party registries. All components are Flutter-native using existing project libraries.

---

## WCAG Compliance Summary

All text-on-background pairs verified against Phase 15 token validation:

| Text Color | Background | Ratio | Status | Use |
|------------|-----------|-------|--------|-----|
| `textPrimary` #111827 | `background` #FFFFFF | 17.15:1 | AAA | Card titles, headings |
| `textPrimary` #111827 | `surface` #F8F9FA | 16.27:1 | AAA | Card content on gray-50 |
| `textSecondary` #6B7280 | `background` #FFFFFF | 5.74:1 | AA | Metadata, body |
| `textSecondary` #6B7280 | `surface` #F8F9FA | 5.03:1 | AA | Card metadata |
| `textOnPrimary` #FFFFFF | `primary` #0D7B74 | 5.12:1 | AA | Button labels |
| `successText` #047857 | `background` #FFFFFF | 5.92:1 | AA | "Owed to you" balance |
| `errorText` #B91C1C | `background` #FFFFFF | 6.57:1 | AA | "You owe" balance |
| `textMuted` #9CA3AF | any | 2.86:1 | FAIL (decorative only) | Timestamps, overlines |
| `textOnPrimary` #FFFFFF | `headerGradientStart` #111827 | 17.15:1 | AAA | Header title |

**CRITICAL:** `textMuted` must never be used for functional text — only decorative overlines, timestamps, and icon hints.

---

## Token Reference Card

Quick reference for executor — canonical token-to-hex mapping for Phase 21:

| Token | Hex | Purpose |
|-------|-----|---------|
| `AppColors.background` | #FFFFFF | Page scaffold |
| `AppColors.surface` | #F8F9FA | Cards, heroes, skeleton bg |
| `AppColors.sandLight` | #F5EDE1 | Input fill (D-26) |
| `AppColors.primary` | #0D7B74 | Teal CTA, Ledger accent |
| `AppColors.terracotta` | #CC6B49 | Form focused border, step dots, onboarding dots (D-26) |
| `AppColors.warmGray` | #E5D5C0 | Form enabled border (D-26) |
| `AppColors.border` | #E5E7EB | Hairline borders, skeleton bars |
| `AppColors.textPrimary` | #111827 | All primary text |
| `AppColors.textSecondary` | #6B7280 | Metadata, secondary text |
| `AppColors.textMuted` | #9CA3AF | DECORATIVE ONLY |
| `AppColors.textOnPrimary` | #FFFFFF | Text on teal buttons/header |
| `AppColors.successText` | #047857 | "Owed to you" financial text |
| `AppColors.errorText` | #B91C1C | "You owe" financial text, destructive confirm |
| `AppColors.error` | #EF4444 | Error icons, error borders |
| `AppColors.moduleLedger` | #0D7B74 | Ledger icon, settlement accent |
| `AppColors.moduleLedgerLight` | #E6F5F3 | Ledger icon container |
| `AppColors.moduleGear` etc. | #6B7280 | All other module icons |
| `AppColors.moduleGearLight` etc. | #F3F4F6 | All other module icon containers |
| `AppColors.headerGradientStart` | #111827 | ModuleHeader gradient top |
| `AppColors.headerGradientEnd` | #1F2937 | ModuleHeader gradient bottom |
| `AppColors.selectionFill` | #E6F5F3 | Selected chip/item bg |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
