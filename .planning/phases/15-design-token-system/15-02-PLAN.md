---
phase: 15-design-token-system
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - lib/core/router/app_router.dart
  - lib/core/theme/error_widgets.dart
  - lib/features/onboarding/screens/onboarding_screen.dart
  - lib/features/events/widgets/event_spending_hero.dart
  - lib/features/events/screens/event_expense_hero.dart
  - lib/features/groups/widgets/group_balance_hero.dart
  - .github/workflows/release_android.yml
autonomous: true
requirements:
  - FOUND-04

must_haves:
  truths:
    - "CI build fails if any file outside the allowlist introduces a Color(0xFF...) literal"
    - "Zero hardcoded Color(0xFF...) literals exist in lib/ outside app_theme.dart, tokens/*.dart, and expense_category_model.dart"
    - "All six migrated files compile and render using AppColors tokens instead of inline hex values"
  artifacts:
    - path: "lib/core/router/app_router.dart"
      provides: "Error/splash screen with AppColors tokens replacing 4 hardcoded colors"
      min_lines: 10
    - path: "lib/core/theme/error_widgets.dart"
      provides: "Error widgets with AppColors.warning and AppColors.textSecondary replacing 3 hardcoded colors"
      min_lines: 10
    - path: "lib/features/onboarding/screens/onboarding_screen.dart"
      provides: "Onboarding screen with AppColors tokens replacing 3 hardcoded colors"
      min_lines: 10
    - path: "lib/features/events/widgets/event_spending_hero.dart"
      provides: "Spending hero with AppColors replacing 2 hardcoded colors"
      min_lines: 10
    - path: "lib/features/events/screens/event_expense_hero.dart"
      provides: "Expense hero with AppColors replacing 2 hardcoded colors"
      min_lines: 10
    - path: "lib/features/groups/widgets/group_balance_hero.dart"
      provides: "Balance hero with AppColors replacing 1 hardcoded color"
      min_lines: 10
    - path: ".github/workflows/release_android.yml"
      provides: "Hardcoded color lint CI step that hard-fails on violations"
      contains: "Hardcoded color lint"
  key_links:
    - from: ".github/workflows/release_android.yml"
      to: "lib/"
      via: "grep -rn Color(0x scanning all dart files"
      pattern: "grep.*Color\\(0x.*lib/"
    - from: "lib/core/router/app_router.dart"
      to: "lib/core/theme/app_theme.dart"
      via: "AppColors.surfaceDark, AppColors.primary references"
      pattern: "AppColors\\."
---

<objective>
Migrate all 15 hardcoded Color(0xFF...) literals in 6 non-exempt lib/ files to AppColors token references, then add the CI lint step that hard-fails on any future violations.

Purpose: After this plan, the CI pipeline enforces that no developer can introduce hardcoded color values outside the token system (FOUND-04). The migration clears the existing violations so the CI step passes from day one.

Output: 6 updated Dart files with zero hardcoded colors, and a new GitHub Actions step in release_android.yml.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-design-token-system/15-CONTEXT.md
@.planning/phases/15-design-token-system/15-RESEARCH.md

Prior plan output:
@.planning/phases/15-design-token-system/15-01-SUMMARY.md

Source files:
@lib/core/router/app_router.dart
@lib/core/theme/error_widgets.dart
@lib/features/onboarding/screens/onboarding_screen.dart
@lib/features/events/widgets/event_spending_hero.dart
@lib/features/events/screens/event_expense_hero.dart
@lib/features/groups/widgets/group_balance_hero.dart
@.github/workflows/release_android.yml
</context>

<interfaces>
<!-- AppColors facade values available after Plan 01 -->

From lib/core/theme/app_theme.dart (post-Plan 01):
```dart
class AppColors {
  // Primary
  static const Color primary = Color(0xFFCC6B49);      // terracotta
  static const Color primaryLight = Color(0xFFF5DDD3);  // terracotta 15%
  static const Color primaryDark = Color(0xFFB85E3D);   // dark terracotta

  // Backgrounds
  static const Color background = Color(0xFFF2E8D6);    // sand
  static const Color surface = Color(0xFFFFF9F2);        // warm white
  static const Color surfaceDark = Color(0xFF2C1A0E);    // dark brown

  // Text
  static const Color textPrimary = Color(0xFF2C1A0E);
  static const Color textSecondary = Color(0xFF6B5B4E);
  static const Color textOnPrimary = Color(0xFFFFFFFF);

  // Semantic
  static const Color warning = Color(0xFFF59E0B);       // amber (unchanged)
  static const Color error = Color(0xFFEF4444);          // red (unchanged)

  // Gradients
  static const LinearGradient darkHeaderGradient = LinearGradient(
    colors: [Color(0xFF2C1A0E), Color(0xFF3D2B1E)],
  );
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Migrate 15 hardcoded Color(0xFF...) literals in 6 files to AppColors references</name>
  <files>
    lib/core/router/app_router.dart
    lib/core/theme/error_widgets.dart
    lib/features/onboarding/screens/onboarding_screen.dart
    lib/features/events/widgets/event_spending_hero.dart
    lib/features/events/screens/event_expense_hero.dart
    lib/features/groups/widgets/group_balance_hero.dart
  </files>
  <read_first>
    lib/core/theme/app_theme.dart
    lib/core/router/app_router.dart
    lib/core/theme/error_widgets.dart
    lib/features/onboarding/screens/onboarding_screen.dart
    lib/features/events/widgets/event_spending_hero.dart
    lib/features/events/screens/event_expense_hero.dart
    lib/features/groups/widgets/group_balance_hero.dart
  </read_first>
  <action>
For each file, replace hardcoded `Color(0xFF...)` literals with `AppColors.*` references. All files already import `app_theme.dart` (verify and add if missing). The mapping below covers every occurrence found by audit.

**File 1: `lib/core/router/app_router.dart`** (4 occurrences)

| Line Pattern | Current | Replace With |
|---|---|---|
| `backgroundColor: const Color(0xFF0F172A)` | Slate 900 dark background | `backgroundColor: AppColors.surfaceDark` |
| `colors: [Color(0xFF13EC92), Color(0xFF0BAE6B)]` | Mint gradient | `colors: [AppColors.primary, AppColors.primaryDark]` |
| `color: const Color(0xFF13EC92).withAlpha(100)` | Mint with alpha | `color: AppColors.primary.withAlpha(100)` (note: loses `const` — acceptable since `.withAlpha()` is non-const anyway) |
| `color: Color(0xFF13EC92)` | Mint text | `color: AppColors.primary` |

Ensure `import 'package:safar/core/theme/app_theme.dart';` is present.

**File 2: `lib/core/theme/error_widgets.dart`** (3 occurrences)

| Line Pattern | Current | Replace With |
|---|---|---|
| `this.iconColor = const Color(0xFFF59E0B)` | Amber default param | `this.iconColor = AppColors.warning` |
| `iconColor: const Color(0xFFF59E0B)` | Amber in factory | `iconColor: AppColors.warning` |
| `iconColor: const Color(0xFF64748B)` | Slate 500 in empty factory | `iconColor: AppColors.textSecondary` |

Note: If the constructor uses `const` for the default value and `AppColors.warning` is `const`, this works directly. `AppColors.warning` is `static const Color` so it can be used as a default parameter value.

**File 3: `lib/features/onboarding/screens/onboarding_screen.dart`** (3 occurrences)

| Line Pattern | Current | Replace With |
|---|---|---|
| `backgroundColor: const Color(0xFF0F172A)` | Slate 900 scaffold | `backgroundColor: AppColors.surfaceDark` |
| `colors: [Color(0xFF0F172A), Color(0xFF1E293B)]` | Slate gradient | `colors: [AppColors.darkHeaderGradient.colors[0], AppColors.darkHeaderGradient.colors[1]]` — OR simpler: replace the entire LinearGradient constructor with `gradient: AppColors.darkHeaderGradient` if the gradient is used as a `BoxDecoration.gradient`. If it's a custom gradient construction, use `colors: [AppColors.surfaceDark, AppColors.textPrimary.withValues(alpha: 0.85)]`. Read the file first and pick the cleanest approach. Preferred: just use `AppColors.darkHeaderGradient` directly. |
| `color: const Color(0xFF334155).withValues(alpha: 0.15)` | Slate 700 with alpha | `color: AppColors.textSecondary.withValues(alpha: 0.15)` |

**File 4: `lib/features/events/widgets/event_spending_hero.dart`** (2 occurrences)

| Line Pattern | Current | Replace With |
|---|---|---|
| `colors: [Color(0xFF0F172A), Color(0xFF1E293B)]` | Slate gradient | `colors: AppColors.darkHeaderGradient.colors` |
| `color: const Color(0xFF0F172A).withValues(alpha: 0.2)` | Slate shadow | `color: AppColors.surfaceDark.withValues(alpha: 0.2)` |

**File 5: `lib/features/events/screens/event_expense_hero.dart`** (2 occurrences)

| Line Pattern | Current | Replace With |
|---|---|---|
| `colors: [Color(0xFF0F172A), Color(0xFF1E293B)]` | Slate gradient | `colors: AppColors.darkHeaderGradient.colors` |
| `color: const Color(0xFF0F172A).withValues(alpha: 0.2)` | Slate shadow | `color: AppColors.surfaceDark.withValues(alpha: 0.2)` |

**File 6: `lib/features/groups/widgets/group_balance_hero.dart`** (1 occurrence)

| Line Pattern | Current | Replace With |
|---|---|---|
| `color: const Color(0xFF0F172A).withValues(alpha: 0.2)` | Slate shadow | `color: AppColors.surfaceDark.withValues(alpha: 0.2)` |

After all changes, run the CI grep command locally to verify zero violations:
```bash
grep -rn "Color(0x" lib/ --include="*.dart" \
  | grep -v "lib/core/theme/app_theme.dart" \
  | grep -v "lib/core/theme/tokens/" \
  | grep -v "lib/features/ledger/models/expense_category_model.dart"
```
This must return zero lines (exit code 1 from grep = no matches = success).
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla && VIOLATIONS=$(grep -rn "Color(0x" lib/ --include="*.dart" | grep -v "lib/core/theme/app_theme.dart" | grep -v "lib/core/theme/tokens/" | grep -v "lib/features/ledger/models/expense_category_model.dart" | wc -l | tr -d ' ') && echo "Violations: $VIOLATIONS" && [ "$VIOLATIONS" -eq "0" ] && flutter analyze lib/core/router/app_router.dart lib/core/theme/error_widgets.dart lib/features/onboarding/screens/onboarding_screen.dart lib/features/events/widgets/event_spending_hero.dart lib/features/events/screens/event_expense_hero.dart lib/features/groups/widgets/group_balance_hero.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "Color(0x" lib/core/router/app_router.dart` returns 0 lines
    - `grep -rn "Color(0x" lib/core/theme/error_widgets.dart` returns 0 lines
    - `grep -rn "Color(0x" lib/features/onboarding/screens/onboarding_screen.dart` returns 0 lines
    - `grep -rn "Color(0x" lib/features/events/widgets/event_spending_hero.dart` returns 0 lines
    - `grep -rn "Color(0x" lib/features/events/screens/event_expense_hero.dart` returns 0 lines
    - `grep -rn "Color(0x" lib/features/groups/widgets/group_balance_hero.dart` returns 0 lines
    - lib/core/router/app_router.dart contains `AppColors.surfaceDark`
    - lib/core/router/app_router.dart contains `AppColors.primary`
    - lib/core/theme/error_widgets.dart contains `AppColors.warning`
    - lib/features/onboarding/screens/onboarding_screen.dart contains `AppColors.surfaceDark`
    - `flutter analyze` on all 6 files exits with 0 errors
    - `flutter test` passes (all tests still green)
  </acceptance_criteria>
  <done>All 15 hardcoded Color(0xFF...) literals in non-exempt files are replaced with AppColors references. Zero violations remain outside the allowlist.</done>
</task>

<task type="auto">
  <name>Task 2: Add hardcoded color lint CI step to GitHub Actions</name>
  <files>
    .github/workflows/release_android.yml
  </files>
  <read_first>
    .github/workflows/release_android.yml
    .planning/phases/15-design-token-system/15-RESEARCH.md
  </read_first>
  <action>
Add a new CI step to `.github/workflows/release_android.yml` per D-19, D-20, D-21.

Insert the step **immediately after** the existing `find.text() regression warning` step (after line ~60, before `Install lcov`). This placement groups all lint checks together.

Add this exact YAML block:

```yaml
      - name: Hardcoded color lint
        run: |
          VIOLATIONS=$(grep -rn "Color(0x" lib/ \
            --include="*.dart" \
            | grep -v "lib/core/theme/app_theme.dart" \
            | grep -v "lib/core/theme/tokens/" \
            | grep -v "lib/features/ledger/models/expense_category_model.dart" \
            | wc -l | tr -d ' ')
          if [ "$VIOLATIONS" -gt "0" ]; then
            echo "::error::$VIOLATIONS hardcoded Color(0xFF...) literal(s) found outside the token system."
            grep -rn "Color(0x" lib/ \
              --include="*.dart" \
              | grep -v "lib/core/theme/app_theme.dart" \
              | grep -v "lib/core/theme/tokens/" \
              | grep -v "lib/features/ledger/models/expense_category_model.dart"
            exit 1
          fi
          echo "Hardcoded color lint: PASS (0 violations)"
```

Key details per locked decisions:
- **D-19**: Grep-based, zero dependencies — uses only `grep` and `wc` (available on ubuntu-latest)
- **D-20**: Hard fail — `exit 1` kills the build on any violation
- **D-21**: Allowlist covers exactly 3 paths:
  1. `lib/core/theme/app_theme.dart` — token definition file
  2. `lib/core/theme/tokens/` — all token extension files
  3. `lib/features/ledger/models/expense_category_model.dart` — category color definitions
- Test files (`test/**/*.dart`) are NOT scanned because the grep is scoped to `lib/` only

The step does NOT use `--exclude-dir=".git"` because scoping to `lib/` already excludes .git, .github, test, etc.

Indentation must match the existing YAML structure (6 spaces for step names under `steps:`, 10 spaces for `run:` content).
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla && grep -A 15 "Hardcoded color lint" .github/workflows/release_android.yml && echo "---" && grep -c "exit 1" .github/workflows/release_android.yml</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/release_android.yml contains `name: Hardcoded color lint`
    - .github/workflows/release_android.yml contains `exit 1` within the hardcoded color lint step
    - .github/workflows/release_android.yml contains `grep -rn "Color(0x" lib/`
    - .github/workflows/release_android.yml contains `grep -v "lib/core/theme/app_theme.dart"`
    - .github/workflows/release_android.yml contains `grep -v "lib/core/theme/tokens/"`
    - .github/workflows/release_android.yml contains `grep -v "lib/features/ledger/models/expense_category_model.dart"`
    - The hardcoded color lint step appears after `find.text() regression warning` and before `Install lcov`
    - YAML is valid (no indentation errors — can verify by checking the step is recognized by a YAML parser)
  </acceptance_criteria>
  <done>CI pipeline has a hard-fail lint step that blocks any PR or release build introducing a hardcoded Color(0xFF...) outside the allowlisted token files. FOUND-04 enforcement is complete.</done>
</task>

</tasks>

<verification>
1. `grep -rn "Color(0x" lib/ --include="*.dart" | grep -v "lib/core/theme/app_theme.dart" | grep -v "lib/core/theme/tokens/" | grep -v "lib/features/ledger/models/expense_category_model.dart" | wc -l` — returns 0
2. `grep "Hardcoded color lint" .github/workflows/release_android.yml` — returns 1 match
3. `flutter test` — all 624+ tests pass
4. `flutter analyze` — zero errors
</verification>

<success_criteria>
- All 15 hardcoded Color(0xFF...) literals outside exempt files are replaced with AppColors references
- CI hard-fail lint step blocks any new hardcoded color introduction
- All existing tests pass without modification
- The allowlist exactly matches D-21 (app_theme.dart, tokens/*.dart, expense_category_model.dart)
</success_criteria>

<output>
After completion, create `.planning/phases/15-design-token-system/15-02-SUMMARY.md`
</output>
