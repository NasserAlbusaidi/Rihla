---
phase: 03-events
plan: 04
type: execute
wave: 4
depends_on: ["03-02", "03-03"]
files_modified:
  - lib/features/events/screens/event_command_center.dart
  - lib/features/events/widgets/event_module_list.dart
  - lib/features/events/screens/create_event_screen.dart
  - lib/features/groups/screens/group_detail_screen.dart
  - test/features/events/event_command_center_test.dart
autonomous: false
requirements:
  - EVT-03
  - EVT-08

must_haves:
  truths:
    - "EventCommandCenter shows only module cards enabled in event.modules"
    - "Tapping a module card opens the existing module screen with the bridge trip ID"
    - "Event hub header shows event name, type badge, and group name"
    - "Tapping an event card in GroupDetailScreen navigates to EventCommandCenter"
    - "After creating an event, the user lands on EventCommandCenter"
    - "Existing module screens (Ledger, Gear, Logistics, Vault, Memories) work within events via Trip facade"
  artifacts:
    - path: "lib/features/events/screens/event_command_center.dart"
      provides: "Adapted CommandCenter for events with Trip facade"
      exports: ["EventCommandCenter"]
    - path: "lib/features/events/widgets/event_module_list.dart"
      provides: "Module card list filtered by EventModules"
      exports: ["EventModuleList"]
  key_links:
    - from: "lib/features/events/screens/event_command_center.dart"
      to: "lib/features/trip/models/trip_model.dart"
      via: "Trip facade construction from Event"
      pattern: "Trip\\("
    - from: "lib/features/events/widgets/event_module_list.dart"
      to: "lib/features/ledger/screens/ledger_screen.dart"
      via: "Navigator.push with Trip facade"
      pattern: "LedgerScreen\\(trip:"
    - from: "lib/features/events/widgets/event_module_list.dart"
      to: "lib/features/gear/screens/gear_screen.dart"
      via: "Navigator.push with Trip facade"
      pattern: "GearScreen\\(trip:"
    - from: "lib/features/groups/screens/group_detail_screen.dart"
      to: "lib/features/events/screens/event_command_center.dart"
      via: "EventCard onTap Navigator.push"
      pattern: "EventCommandCenter"
    - from: "lib/features/events/screens/create_event_screen.dart"
      to: "lib/features/events/screens/event_command_center.dart"
      via: "Post-creation navigation"
      pattern: "EventCommandCenter"
---

<objective>
Build the EventCommandCenter (adapted CommandCenter for events) and wire up all navigation: event card taps in GroupDetailScreen, post-creation navigation in CreateEventScreen, and module screen launches via Trip facade.

Purpose: This is the final plan that connects everything. EventCommandCenter is the per-event hub that shows module cards based on the event type's module configuration. It uses a Trip facade to pass bridge trip IDs to existing module screens without modifying them. This plan also wires up the TODOs left in Plans 02 and 03 for navigation.

Output: Working EventCommandCenter, EventModuleList, and all navigation wired up end-to-end with a human verification checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-events/03-CONTEXT.md
@.planning/phases/03-events/03-RESEARCH.md
@.planning/phases/03-events/03-UI-SPEC.md
@.planning/phases/03-events/03-00-SUMMARY.md
@.planning/phases/03-events/03-01-SUMMARY.md
@.planning/phases/03-events/03-02-SUMMARY.md
@.planning/phases/03-events/03-03-SUMMARY.md

<interfaces>
<!-- From all prior plans -->

From lib/features/events/models/event_model.dart:
```dart
class Event {
  final String id, name, groupId, createdBy, currency, bridgeTripId;
  final EventType type;
  final List<String> participantIds;
  final Map<String, String> participantNames;
  final EventModules modules;
  final DateTime? startDate, endDate;
  final DateTime createdAt;
  bool get isPast;
  bool get isOngoing;
}

class EventModules {
  final bool ledger, gear, logistics, vault, memories;
}
```

From lib/features/events/models/event_type_config.dart:
```dart
class EventTypeConfig {
  final String label;
  final IconData icon;
  final Color color;
  static EventTypeConfig forType(EventType type);
}
```

From lib/features/trip/models/trip_model.dart:
```dart
class Trip {
  final String id, name, inviteCode, leaderId, icon, currency;
  final TripModules modules;
  final DateTime createdAt;
  final DateTime? updatedAt, startDate, endDate;
  const Trip({required this.id, required this.name, required this.inviteCode, required this.leaderId, required this.modules, required this.createdAt, ...});
}

class TripModules {
  final bool docs, gear, itinerary, logistics;
  const TripModules({this.docs = true, this.gear = true, this.itinerary = false, this.logistics = true});
}
```

From lib/features/groups/models/group_model.dart:
```dart
class Group {
  final String id, name, inviteCode, createdBy, currency;
  final List<String> memberIds;
}
```

From lib/features/home/widgets/module_list.dart:
```dart
class ModuleList extends ConsumerWidget {
  final Trip trip;
  // Builds SmartModuleCard list with: Ledger, Gear (if modules.gear), Logistics (if modules.logistics), Vault (if modules.docs)
  // Each card uses existing screen navigation: LedgerScreen(trip:), GearScreen(trip:), LogisticsScreen(trip:), VaultScreen(trip:)
}
```

From lib/shared/widgets/module_header.dart:
```dart
class ModuleHeader extends StatelessWidget {
  final String title;
  final String subtitle;
  final List<Widget>? actions;
  final bool useDarkTheme;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create EventCommandCenter and EventModuleList</name>
  <files>lib/features/events/screens/event_command_center.dart, lib/features/events/widgets/event_module_list.dart</files>
  <read_first>
    - lib/features/home/screens/command_center.dart (full file — CommandCenter structure to adapt)
    - lib/features/home/widgets/module_list.dart (full file — ModuleList with Trip parameter, card configs)
    - lib/features/home/widgets/expense_summary_hero.dart (ExpenseSummaryHero constructor signature — what props it needs)
    - lib/features/trip/models/trip_model.dart (Trip constructor for facade creation)
    - lib/features/memories/screens/memories_screen.dart (first 30 lines — constructor signature for memories module)
    - lib/shared/widgets/module_header.dart (ModuleHeader props)
    - lib/shared/widgets/smart_module_card.dart (SmartModuleCard props)
    - lib/shared/widgets/offline_banner.dart (OfflineBanner usage)
    - lib/core/theme/app_theme.dart (AppColors tokens)
    - .planning/phases/03-events/03-UI-SPEC.md (Adapted Event Hub spec)
    - .planning/phases/03-events/03-RESEARCH.md (Pattern 5: EventCommandCenter, Trip facade)
    - .planning/phases/03-events/03-CONTEXT.md (D-17 to D-21 for event hub behavior)
  </read_first>
  <action>
    **1. Create lib/features/events/widgets/event_module_list.dart:**

    This is an adapted version of ModuleList that:
    - Accepts an `Event` (not Trip) plus a `Trip` facade
    - Only renders cards for modules enabled in `event.modules`
    - Uses the same SmartModuleCard widget with the same data providers
    - Adds Memories card (not in original ModuleList since TripModules has no memories field)

    ```dart
    import 'package:flutter/material.dart';
    import 'package:flutter_animate/flutter_animate.dart';
    import 'package:flutter_riverpod/flutter_riverpod.dart';
    import 'package:iconsax/iconsax.dart';
    // ... same imports as ModuleList for screens, providers, models
    import '../../events/models/event_model.dart';
    ```

    Class: `EventModuleList extends ConsumerWidget`
    - Constructor: `required Event event, required Trip trip`

    Build method mirrors ModuleList.build:
    - Watch providers using `trip.id` (bridge trip ID): `tripExpensesProvider(trip.id)`, `tripBalancesProvider(trip.id)`, `currentParticipantProvider(trip.id)`
    - Conditionally watch gear/logistics/vault/memories providers based on `event.modules`
    - Build `_ModuleCardConfig` list same as ModuleList but:
      - Always add Ledger card (event.modules.ledger is always true)
      - Add Gear card if `event.modules.gear`
      - Add Logistics card if `event.modules.logistics`
      - Add Vault card if `event.modules.vault`
      - Add Memories card if `event.modules.memories` — new card:
        - icon: `Iconsax.gallery`
        - title: 'Memories'
        - description: 'Capture and share photos from your event'
        - color: `AppColors.mint` (per UI-SPEC module color mapping)
        - onTap: navigate to MemoriesScreen with trip
        - priority: 10 (default, low)
        - isEmpty: true (no data provider wired for memories count yet)
    - Sort by priority, render with SmartModuleCard + staggered animation (same as ModuleList)

    Navigation methods (same as ModuleList but using `trip` parameter):
    - `_openLedger`: `Navigator.push(AppPageRoute(builder: (_) => LedgerScreen(trip: trip)))`
    - `_openGear`: `Navigator.push(AppPageRoute(builder: (_) => GearScreen(trip: trip)))`
    - `_openLogistics`: `Navigator.push(AppPageRoute(builder: (_) => LogisticsScreen(trip: trip)))`
    - `_openVault`: `Navigator.push(AppPageRoute(builder: (_) => VaultScreen(trip: trip)))`
    - `_openMemories`: Check MemoriesScreen constructor — it likely takes a `trip` or `tripId` parameter. Navigate accordingly. If constructor is `MemoriesScreen({required Trip trip})`, use that. If it uses `tripId`, pass `trip.id`.

    **2. Create lib/features/events/screens/event_command_center.dart:**

    `EventCommandCenter extends ConsumerWidget`

    Constructor: `required Event event, required Group group`

    **Trip facade builder** (private method per Research Pattern 5):
    ```dart
    Trip _buildTripFacade() {
      return Trip(
        id: event.bridgeTripId,
        name: event.name,
        inviteCode: '',
        leaderId: event.createdBy,
        modules: TripModules(
          docs: event.modules.vault,
          gear: event.modules.gear,
          itinerary: false,
          logistics: event.modules.logistics,
        ),
        createdAt: event.createdAt,
        startDate: event.startDate,
        endDate: event.endDate,
        currency: event.currency,
      );
    }
    ```

    Build method structure (mirrors CommandCenter but simpler — no userTripsProvider, no seed provider):

    ```dart
    @override
    Widget build(BuildContext context, WidgetRef ref) {
      final trip = _buildTripFacade();
      final config = EventTypeConfig.forType(event.type);

      return Scaffold(
        backgroundColor: AppColors.background,
        floatingActionButton: FloatingActionButton(
          onPressed: () {
            HapticService.medium();
            Navigator.of(context).push(
              AppPageRoute(builder: (_) => AddExpenseScreen(trip: trip)),
            );
          },
          backgroundColor: AppColors.primary,
          shape: const CircleBorder(),
          child: const Icon(Iconsax.add, color: Colors.black),
        ),
        body: Column(
          children: [
            ModuleHeader(
              title: event.name,
              subtitle: '${config.label} \u00B7 ${group.name}',
              actions: [
                Semantics(
                  label: 'Event options',
                  button: true,
                  child: IconButton(
                    icon: const Icon(Iconsax.more_circle, color: Colors.white, size: 22),
                    onPressed: () {
                      // TODO(Phase 3+): Event options menu (edit name/dates, delete)
                    },
                  ),
                ),
              ],
              useDarkTheme: true,
            ),
            const OfflineBanner(),
            Expanded(
              child: SingleChildScrollView(
                padding: const EdgeInsets.symmetric(horizontal: AppColors.space24),
                child: Column(
                  crossAxisAlignment: CrossAxisAlignment.start,
                  children: [
                    const SizedBox(height: AppColors.space20),
                    ExpenseSummaryHero(trip: trip),
                    const SizedBox(height: AppColors.space24),
                    EventModuleList(event: event, trip: trip),
                    const SizedBox(height: AppColors.space32),
                  ],
                ),
              ),
            ),
          ],
        ),
      );
    }
    ```

    Header subtitle format: `'{Type} \u00B7 {Group name}'` (middle dot U+00B7 with spaces, per Copywriting Contract).

    FAB: `Iconsax.add` on primary with `CircleBorder()`. Opens `AddExpenseScreen(trip: trip)` per D-19.

    **No seed provider needed:** Per Research: existing module data for bridge trips was written to Supabase at creation time. Supabase providers load on demand. Do NOT wire _tripDataSeedProvider.

    **Import list:**
    ```dart
    import 'package:flutter/material.dart';
    import 'package:flutter_riverpod/flutter_riverpod.dart';
    import 'package:iconsax/iconsax.dart';
    import '../../../core/services/haptic_service.dart';
    import '../../../core/theme/app_theme.dart';
    import '../../../core/utils/page_transitions.dart';
    import '../../../shared/widgets/module_header.dart';
    import '../../../shared/widgets/offline_banner.dart';
    import '../../groups/models/group_model.dart';
    import '../../home/widgets/expense_summary_hero.dart';
    import '../../ledger/screens/add_expense_screen.dart';
    import '../../trip/models/trip_model.dart';
    import '../models/event_model.dart';
    import '../models/event_type_config.dart';
    import '../widgets/event_module_list.dart';
    ```
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla && flutter analyze lib/features/events/screens/event_command_center.dart lib/features/events/widgets/event_module_list.dart 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - lib/features/events/screens/event_command_center.dart contains `class EventCommandCenter extends ConsumerWidget`
    - EventCommandCenter constructor has `required this.event` and `required this.group`
    - _buildTripFacade creates Trip with id=event.bridgeTripId
    - Trip facade maps event.modules.vault to TripModules.docs
    - ModuleHeader title is event.name
    - ModuleHeader subtitle uses middle dot U+00B7 between type label and group name
    - ModuleHeader useDarkTheme is true
    - FAB opens AddExpenseScreen(trip: trip)
    - lib/features/events/widgets/event_module_list.dart contains `class EventModuleList`
    - EventModuleList conditionally renders cards based on event.modules booleans
    - EventModuleList includes Memories card (icon: Iconsax.gallery, color: AppColors.mint) when event.modules.memories is true
    - No _tripDataSeedProvider used (no SyncService dependency)
    - `flutter analyze` reports no issues
  </acceptance_criteria>
  <done>EventCommandCenter shows event header with type badge and group name, expense summary hero, and module cards filtered by event type. Trip facade enables existing module screens to function via bridge trip ID.</done>
</task>

<task type="auto">
  <name>Task 2: Wire navigation in CreateEventScreen and GroupDetailScreen, update tests</name>
  <files>lib/features/events/screens/create_event_screen.dart, lib/features/groups/screens/group_detail_screen.dart, test/features/events/event_command_center_test.dart</files>
  <read_first>
    - lib/features/events/screens/create_event_screen.dart (full file — find TODO for post-creation navigation)
    - lib/features/groups/screens/group_detail_screen.dart (full file — find TODO for event card onTap)
    - lib/features/events/screens/event_command_center.dart (just created — constructor signature)
    - lib/features/groups/providers/group_provider.dart (groupDetailProvider for getting Group object)
    - test/features/events/event_command_center_test.dart (stub file to update)
  </read_first>
  <action>
    **1. Wire CreateEventScreen post-creation navigation:**

    In create_event_screen.dart, find the `_submitForm` method's success handler (the TODO comment left in Plan 02). Replace it with:

    ```dart
    if (mounted) {
      final group = ref.read(groupDetailProvider(widget.groupId)).valueOrNull;
      // Pop both CreateEventScreen and EventTypePickerScreen
      Navigator.of(context)..pop()..pop();
      // Push EventCommandCenter for the new event
      if (group != null) {
        Navigator.of(context).push(
          AppPageRoute(
            builder: (_) => EventCommandCenter(event: event, group: group),
          ),
        );
      }
    }
    ```

    Add import: `import 'event_command_center.dart';`

    **2. Wire GroupDetailScreen event card navigation:**

    In group_detail_screen.dart, find the EventCard onTap TODO in `_buildEventsSection`. Replace the commented-out code with:

    ```dart
    EventCard(
      event: events[i],
      onTap: () {
        Navigator.of(context).push(
          AppPageRoute(
            builder: (_) => EventCommandCenter(
              event: events[i],
              group: group,
            ),
          ),
        );
      },
    ),
    ```

    The `group` object needs to be available in `_buildEventsSection`. Update the method signature:
    `Widget _buildEventsSection(BuildContext context, WidgetRef ref, Group group)`

    Update the call site in `_buildContent` to pass `group`.

    Add import: `import '../../events/screens/event_command_center.dart';`

    **3. Update test/features/events/event_command_center_test.dart:**

    Replace skip markers with real widget tests:
    - Test: EventCommandCenter shows event name in header — pump widget with mock Event (type=trip, all modules), verify event name text appears
    - Test: EventCommandCenter shows type badge — verify type label text appears
    - Test: EventCommandCenter shows group name — verify group name text appears
    - Test: EventCommandCenter shows correct number of module cards for Trip type (5 modules = up to 5 cards including memories)
    - Test: EventCommandCenter shows only Ledger card for Night/Day Out type (1 module)

    Use `ProviderScope.overrides` to mock:
    - `tripExpensesProvider` returning empty list
    - `tripBalancesProvider` returning empty list
    - `currentParticipantProvider` returning null
    - `tripGearProvider`, `tripSubGroupsProvider`, `tripDocumentsProvider` as needed

    Create test Event objects:
    ```dart
    final testEvent = Event(
      id: 'test-event-1',
      name: 'Summer Trip',
      type: EventType.trip,
      groupId: 'test-group-1',
      createdBy: 'user-1',
      participantIds: ['user-1'],
      participantNames: {'user-1': 'Nasser'},
      modules: EventModules.forType(EventType.trip),
      currency: 'OMR',
      isDeleted: false,
      createdAt: DateTime.now(),
      bridgeTripId: 'test-event-1',
    );

    final testGroup = Group(
      id: 'test-group-1',
      name: 'Friends',
      inviteCode: 'ABC123',
      createdBy: 'user-1',
      memberIds: ['user-1'],
      createdAt: DateTime.now(),
    );
    ```
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla && flutter analyze lib/features/events/ lib/features/groups/screens/group_detail_screen.dart 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - CreateEventScreen no longer has TODO comment for EventCommandCenter navigation
    - CreateEventScreen imports event_command_center.dart
    - CreateEventScreen navigates to EventCommandCenter after successful creation (pops twice then pushes)
    - GroupDetailScreen _buildEventsSection passes group parameter
    - GroupDetailScreen EventCard onTap navigates to EventCommandCenter with event and group
    - GroupDetailScreen imports event_command_center.dart
    - test/features/events/event_command_center_test.dart has 0 skip markers
    - `flutter analyze` reports no issues on all modified files
  </acceptance_criteria>
  <done>All navigation is wired: event card taps go to EventCommandCenter, post-creation goes to EventCommandCenter, module card taps open existing screens via Trip facade. Widget tests verify EventCommandCenter renders correctly for different event types.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify end-to-end event creation flow</name>
  <files>none</files>
  <action>
    Human verifies the complete event system end-to-end on a real device. All code was automated in Tasks 1-2.

    Steps to verify:
    1. Run `flutter run --dart-define-from-file=config.json`
    2. Open an existing group from the home screen
    3. Verify the FAB (green + button) appears on GroupDetailScreen
    4. Tap the FAB — EventTypePickerScreen should appear with 5 type cards
    5. Tap "Camping" — CreateEventScreen should appear with title "New Camping Event"
    6. Verify all group members are pre-checked in the participant list
    7. Enter an event name (e.g., "Desert Camping Weekend")
    8. Tap "Create Event" — should navigate to EventCommandCenter
    9. Verify the event hub header shows: event name, "Camping" badge, group name
    10. Verify module cards appear: Ledger, Gear, Logistics, Memories (4 cards for Camping — no Vault)
    11. Tap "Gear" module — verify GearScreen opens and shows the 3 preset items: Tent, Sleeping Bag, Cooler
    12. Go back to the group detail screen
    13. Verify the event card appears in the events section with the event name and "Camping" badge
    14. Test pull-to-refresh on the home screen groups list — groups should reload
    15. Create a "Night/Day Out" event — verify only Ledger module card appears in the event hub
    16. Create a "Custom" event — verify module toggles appear on the creation form with Ledger locked on

    Resume: Type "approved" if the flow works, or describe any issues found.
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla && flutter test test/features/events/ && flutter analyze lib/features/events/</automated>
  </verify>
  <done>User has verified the complete event creation flow works end-to-end on a real device: type picker, creation form, event hub with filtered modules, camping gear presets, and event timeline in group detail.</done>
</task>

</tasks>

<verification>
- `flutter analyze lib/features/events/ lib/features/groups/screens/group_detail_screen.dart` reports no issues
- `flutter test test/features/events/` passes all tests
- End-to-end flow: FAB -> type picker -> form -> event hub -> module screens all work
- Camping preset gear items appear in GearScreen
- Night/Day Out shows only Ledger module
- Custom events show module toggles
</verification>

<success_criteria>
The complete event system works end-to-end: users can create typed events inside groups, event type controls module visibility, camping events get preset gear, and all existing module screens function via the Supabase bridge. Pull-to-refresh is fixed on the home screen.
</success_criteria>

<output>
After completion, create `.planning/phases/03-events/03-04-SUMMARY.md`
</output>
