---
phase: 03-events
plan: 02
type: execute
wave: 3
depends_on: ["03-01"]
files_modified:
  - lib/features/events/screens/event_type_picker_screen.dart
  - lib/features/events/screens/create_event_screen.dart
  - test/features/events/create_event_test.dart
autonomous: true
requirements:
  - EVT-02
  - EVT-05
  - EVT-06

must_haves:
  truths:
    - "User sees 5 event type cards with icon, name, description, and module chips"
    - "Tapping a type card navigates to CreateEventScreen pre-filled for that type"
    - "CreateEventScreen shows all group members pre-checked in the participant picker"
    - "Custom event type shows module toggle switches with Ledger locked on"
    - "Form validates that event name is required and at least one participant is selected"
    - "Successful creation navigates to EventCommandCenter after popping creation screens"
  artifacts:
    - path: "lib/features/events/screens/event_type_picker_screen.dart"
      provides: "Full-screen type picker with 5 visual type cards"
      exports: ["EventTypePickerScreen"]
    - path: "lib/features/events/screens/create_event_screen.dart"
      provides: "Event creation form with name, dates, participants, module toggles (Custom)"
      exports: ["CreateEventScreen"]
  key_links:
    - from: "lib/features/events/screens/event_type_picker_screen.dart"
      to: "lib/features/events/screens/create_event_screen.dart"
      via: "Navigator.push on type card tap"
      pattern: "Navigator\\.of\\(context\\)\\.push"
    - from: "lib/features/events/screens/create_event_screen.dart"
      to: "lib/features/events/providers/event_provider.dart"
      via: "eventServiceProvider.createEvent call"
      pattern: "ref\\.read\\(eventServiceProvider\\)"
    - from: "lib/features/events/screens/create_event_screen.dart"
      to: "lib/features/groups/providers/group_provider.dart"
      via: "groupMembersProvider for participant list"
      pattern: "groupMembersProvider"
---

<objective>
Build the event creation UI flow: EventTypePickerScreen (Step 1) and CreateEventScreen (Step 2).

Purpose: These two screens implement the user-facing event creation flow. The type picker provides visual selection of 5 event types. The creation form collects event name, optional dates, participant selection, and module toggles (Custom type only). On submit, the form calls EventService.createEvent and navigates to EventCommandCenter.

Output: Two complete screens matching the UI-SPEC with form validation, loading states, and error handling.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-events/03-CONTEXT.md
@.planning/phases/03-events/03-RESEARCH.md
@.planning/phases/03-events/03-UI-SPEC.md
@.planning/phases/03-events/03-00-SUMMARY.md
@.planning/phases/03-events/03-01-SUMMARY.md

<interfaces>
<!-- From Plan 03-00 and 03-01 outputs -->

From lib/features/events/models/event_model.dart:
```dart
enum EventType { trip('trip'), camping('camping'), travel('travel'), nightDayOut('night_day_out'), custom('custom'); }
class EventModules {
  final bool ledger, gear, logistics, vault, memories;
  factory EventModules.forType(EventType type);
  EventModules copyWith({bool? gear, bool? logistics, bool? vault, bool? memories});
}
```

From lib/features/events/models/event_type_config.dart:
```dart
class EventTypeConfig {
  final EventType type;
  final String label;
  final String description;
  final IconData icon;
  final Color color;
  static EventTypeConfig forType(EventType type);
  static List<EventTypeConfig> get allTypes;
}
```

From lib/features/events/providers/event_provider.dart:
```dart
final eventServiceProvider = Provider<EventService>((ref) => EventService(ref));
final eventLoadingProvider = StateProvider<bool>((ref) => false);
final eventErrorProvider = StateProvider<String?>((ref) => null);
final groupEventsProvider = StreamProvider.family<List<Event>, String>(...);
```

From lib/features/events/services/event_service.dart:
```dart
class EventService {
  Future<Event> createEvent({
    required String groupId,
    required String name,
    required EventType type,
    required List<String> participantIds,
    required Map<String, String> participantNames,
    required EventModules modules,
    required String currency,
    DateTime? startDate,
    DateTime? endDate,
  });
}
```

From lib/features/groups/providers/group_provider.dart:
```dart
final groupMembersProvider = StreamProvider.family<List<GroupMember>, String>((ref, groupId) { ... });
final groupDetailProvider = StreamProvider.family<Group?, String>((ref, groupId) { ... });
```

From lib/features/groups/models/group_member_model.dart:
```dart
class GroupMember {
  final String id, userId, groupId, displayName, role;
}
```

From lib/shared/widgets/loading_button.dart:
```dart
class LoadingButton extends StatelessWidget {
  final String label;
  final bool isLoading;
  final VoidCallback? onPressed;
}
```

From lib/core/utils/page_transitions.dart:
```dart
class AppPageRoute<T> extends PageRouteBuilder<T> {
  final WidgetBuilder builder;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build EventTypePickerScreen</name>
  <files>lib/features/events/screens/event_type_picker_screen.dart</files>
  <read_first>
    - lib/features/events/models/event_type_config.dart (EventTypeConfig with allTypes, labels, icons, colors)
    - lib/features/events/models/event_model.dart (EventType enum, EventModules.forType)
    - lib/shared/widgets/smart_module_card.dart (PressableWrapper animation pattern to replicate)
    - lib/core/theme/app_theme.dart (AppColors tokens: spacing, radii, shadows, colors)
    - .planning/phases/03-events/03-UI-SPEC.md (EventTypePickerScreen component spec)
    - .planning/phases/03-events/03-CONTEXT.md (D-02, D-16 for type picker behavior)
  </read_first>
  <action>
    Create lib/features/events/screens/event_type_picker_screen.dart as a StatelessWidget:

    **Constructor:**
    - Required parameter: `String groupId` (passed from GroupDetailScreen FAB)

    **Scaffold:**
    - `backgroundColor: AppColors.background`
    - `appBar: AppBar(title: Text('Choose Event Type'), leading: CloseButton())`
    - AppBar should use light theme (default Material 3 light)

    **Body:** `ListView.separated` with:
    - `padding: EdgeInsets.all(AppColors.space24)` (top/bottom/sides per UI-SPEC)
    - `itemCount: EventTypeConfig.allTypes.length` (5 items)
    - `separatorBuilder: (_,_) => SizedBox(height: AppColors.space12)` (inter-card spacing per UI-SPEC)

    **Each type card (Container):**
    - Container decoration: `color: AppColors.surface`, `borderRadius: BorderRadius.circular(AppColors.radiusLarge)` (20dp), `boxShadow: [AppColors.shadowRaised]`
    - Padding: `EdgeInsets.all(AppColors.space16)` (16dp all sides)
    - Internal Row:
      - Left: 48x48dp Container with `borderRadius: BorderRadius.circular(14)`, `color: config.color.withValues(alpha: 0.1)`, centered Icon at 24dp in `config.color`
      - `SizedBox(width: AppColors.space12)` gap
      - Expanded Column (crossAxisAlignment start):
        - Type name: `Text(config.label, style: Theme.of(context).textTheme.titleMedium)` (16sp w700)
        - `SizedBox(height: AppColors.space4)` gap
        - Description: `Text(config.description, style: Theme.of(context).textTheme.bodySmall?.copyWith(color: AppColors.textMuted))` (12sp w700 textMuted)
        - `SizedBox(height: AppColors.space8)` gap
        - Module chips: `Wrap(spacing: AppColors.space4, runSpacing: AppColors.space4, children: _buildModuleChips(config.type))` — show only enabled modules for this type
      - `SizedBox(width: AppColors.space8)` gap
      - Trailing: `Icon(Iconsax.arrow_right_3, size: 18, color: AppColors.textMuted)`

    **Module chips helper:** For each module enabled in `EventModules.forType(config.type)`, create a Chip with:
    - `backgroundColor: AppColors.surfaceLight`
    - `shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(AppColors.radiusSmall))`
    - Label: module name ('Ledger', 'Gear', 'Logistics', 'Vault', 'Memories') at 12sp w700
    - `materialTapTargetSize: MaterialTapTargetSize.shrinkWrap`, `visualDensity: VisualDensity.compact`

    **Press animation:** Wrap each card in a `_PressableCard` widget that uses `GestureDetector` + `AnimatedScale`:
    - onTapDown: scale to 0.98 over 80ms
    - onTapUp/onTapCancel: scale to 1.0 over 80ms
    - onTap: `Navigator.of(context).push(AppPageRoute(builder: (_) => CreateEventScreen(groupId: groupId, eventType: config.type)))`
    - Curve: `Curves.easeInOut`

    **Staggered entry animation:** Use `flutter_animate`:
    - Each card: `.animate().fadeIn(delay: (40 * index).ms, duration: 400.ms).slideY(begin: 0.05, end: 0, delay: (40 * index).ms, duration: 400.ms, curve: Curves.easeOutCubic)`
    - Respect `MediaQuery.of(context).disableAnimations` — skip animation if true

    **Accessibility:**
    - Wrap each card in `Semantics(label: '${config.label}: ${config.description}. Modules: ${enabledModuleNames.join(", ")}', button: true)`
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla && flutter analyze lib/features/events/screens/event_type_picker_screen.dart 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - lib/features/events/screens/event_type_picker_screen.dart contains `class EventTypePickerScreen`
    - EventTypePickerScreen constructor has `required this.groupId`
    - AppBar title is 'Choose Event Type'
    - Uses EventTypeConfig.allTypes to iterate 5 type cards
    - Each card has 48x48 icon container with borderRadius 14 and alpha 0.1 background
    - Module chips use AppColors.surfaceLight background
    - Press animation scales to 0.98 over 80ms
    - Staggered animation uses 40ms delay per item
    - Tapping card calls Navigator.push with CreateEventScreen
    - Semantics wrapper on each card with button: true
    - `flutter analyze` reports no issues
  </acceptance_criteria>
  <done>EventTypePickerScreen displays 5 event type cards with icons, descriptions, module chips, press animation, staggered entry animation, and accessibility labels. Tapping a card navigates to CreateEventScreen.</done>
</task>

<task type="auto">
  <name>Task 2: Build CreateEventScreen with participant picker and module toggles</name>
  <files>lib/features/events/screens/create_event_screen.dart, test/features/events/create_event_test.dart</files>
  <read_first>
    - lib/features/events/screens/event_type_picker_screen.dart (just created in Task 1)
    - lib/features/events/models/event_model.dart (EventType, EventModules)
    - lib/features/events/models/event_type_config.dart (EventTypeConfig for title)
    - lib/features/events/providers/event_provider.dart (eventServiceProvider, loading/error providers)
    - lib/features/events/services/event_service.dart (createEvent signature)
    - lib/features/groups/providers/group_provider.dart (groupMembersProvider, groupDetailProvider)
    - lib/features/groups/models/group_member_model.dart (GroupMember fields)
    - lib/features/groups/screens/create_group_screen.dart (form pattern to mirror: ConsumerStatefulWidget, LoadingButton, SnackBar errors)
    - lib/shared/widgets/loading_button.dart (LoadingButton widget signature)
    - lib/core/theme/app_theme.dart (AppColors tokens)
    - .planning/phases/03-events/03-UI-SPEC.md (CreateEventScreen component spec, Copywriting Contract, Interaction Contracts)
    - .planning/phases/03-events/03-CONTEXT.md (D-03, D-04, D-05, D-08, D-14 for form behavior)
    - test/features/events/create_event_test.dart (stub file to update)
  </read_first>
  <action>
    Create lib/features/events/screens/create_event_screen.dart as a ConsumerStatefulWidget:

    **Constructor:**
    - Required: `String groupId`, `EventType eventType`

    **State fields:**
    - `_formKey = GlobalKey<FormState>()`
    - `_nameController = TextEditingController()`
    - `Set<String> _selectedParticipantIds` — initialized with all group member UIDs
    - `EventModules _modules` — initialized from `EventModules.forType(eventType)`
    - `DateTime? _startDate`, `DateTime? _endDate`

    **initState:**
    - Read group members from `ref.read(groupMembersProvider(groupId))` and pre-populate `_selectedParticipantIds` with all member user IDs (per D-04: all pre-checked by default)

    **Scaffold:**
    - `backgroundColor: AppColors.background`
    - `appBar: AppBar(title: Text('New ${EventTypeConfig.forType(widget.eventType).label} Event'))` per Copywriting Contract
    - Leading: default BackButton (pops to type picker)

    **Body:** `SingleChildScrollView` with `Form` wrapping all fields:

    **Form fields in order (horizontal padding: space24):**

    1. `SizedBox(height: AppColors.space32)` top gap
    2. "Event Name" label (`titleSmall` style) + `SizedBox(height: AppColors.space8)` + `TextFormField`:
       - hint: 'e.g. Summer camping trip'
       - validator: if empty, return "Event name can't be empty."
       - Uses `InputDecoration` from theme (focused border = AppColors.primary)
    3. `SizedBox(height: AppColors.space24)`
    4. Row: "Dates" label + `SizedBox(width: AppColors.space8)` + Text("(optional)", style: bodySmall textMuted)
    5. `SizedBox(height: AppColors.space8)`
    6. Row of two date picker buttons:
       - Each: `Expanded` > `OutlinedButton` with `borderRadius: AppColors.radiusMedium`
       - Left button: shows "Start date" or formatted date (DateFormat('MMM d, yyyy'))
       - Right button: shows "End date" or formatted date
       - `SizedBox(width: AppColors.space12)` between them
       - onPressed: `showDatePicker(...)` with result stored in `_startDate`/`_endDate`
    7. `SizedBox(height: AppColors.space24)`
    8. "Participants" label
    9. `SizedBox(height: AppColors.space8)`
    10. Participant checkbox list — watch `groupMembersProvider(groupId)`:
        - Each row: 56dp height Container with Row:
          - `Icon(Iconsax.user, size: 16)` in a 32x32 circular container with `AppColors.surfaceLight` background
          - `SizedBox(width: AppColors.space12)`
          - `Text(member.displayName, style: titleSmall)` expanded
          - `Checkbox(value: _selectedParticipantIds.contains(member.userId), onChanged: ...)`
        - onChanged: toggle member.userId in `_selectedParticipantIds` via setState (immutable: create new Set)
        - Accessibility: `Semantics(label: member.displayName, checked: isSelected)`
    11. If `widget.eventType == EventType.custom`:
        - `SizedBox(height: AppColors.space24)`
        - "Modules" label
        - `SizedBox(height: AppColors.space8)`
        - Module toggle rows — one for each module (Ledger, Gear, Logistics, Vault, Memories):
          - Row height 56dp: module icon 24dp in semantic color + `SizedBox(width: AppColors.space12)` + module name (`titleSmall`) expanded + `Switch`
          - Ledger row: `Switch(value: true, onChanged: null)` — locked on, row at opacity 0.5 (per D-14, UI-SPEC)
          - Other modules: `Switch(value: _modules.gear/logistics/vault/memories, activeColor: AppColors.primary, onChanged: (v) => setState(() => _modules = _modules.copyWith(gear: v)))`
          - Module semantic colors: Ledger=emerald, Gear=amber, Logistics=sky, Vault=indigo, Memories=mint (per UI-SPEC module color mapping)
    12. `SizedBox(height: 48)` (matches CreateGroupScreen pre-submit gap)
    13. `LoadingButton(label: 'Create Event', isLoading: ref.watch(eventLoadingProvider), onPressed: _submitForm)`
    14. `SizedBox(height: AppColors.space32)` bottom

    **_submitForm method:**
    - If `!_formKey.currentState!.validate()` return early
    - If `_selectedParticipantIds.isEmpty`:
      - Show SnackBar: 'Select at least one participant.' per Copywriting Contract
      - Return early
    - Set loading: `ref.read(eventLoadingProvider.notifier).state = true`
    - Clear error: `ref.read(eventErrorProvider.notifier).state = null`
    - Build participantNames map from group members: `{member.userId: member.displayName}` for each selected member
    - Get group's currency from `ref.read(groupDetailProvider(groupId)).valueOrNull?.currency ?? 'OMR'`
    - try:
      - `final event = await ref.read(eventServiceProvider).createEvent(...)` with all fields
      - Reset loading
      - Navigate: `Navigator.of(context).popUntil((route) => route.isFirst || route.settings.name == 'group_detail')` — pop back to GroupDetailScreen, then push EventCommandCenter
      - Actually, simpler approach: pop twice (picker + form) then push EventCommandCenter:
        ```dart
        if (mounted) {
          // Pop CreateEventScreen and EventTypePickerScreen
          Navigator.of(context)..pop()..pop();
          // Push EventCommandCenter for the new event
          Navigator.of(context).push(
            AppPageRoute(builder: (_) => EventCommandCenter(event: event, group: group)),
          );
        }
        ```
      - Note: EventCommandCenter doesn't exist yet (Plan 04). For now, just pop twice and let the user see the event in the timeline. Add a TODO: `// TODO(Plan 03-04): Navigate to EventCommandCenter after creation`
    - catch (e):
      - Reset loading
      - Show SnackBar: "Couldn't create event. Check your connection and try again." with 5s duration per Copywriting Contract

    **Update test/features/events/create_event_test.dart:**
    Remove skip markers and add real widget tests:
    - Test: EventTypePickerScreen displays all 5 type cards (find by label text)
    - Test: CreateEventScreen pre-checks all group members
    - Test: CreateEventScreen shows module toggles for Custom type
    - Test: CreateEventScreen validates event name is required
    - Use `ProviderScope.overrides` to mock `groupMembersProvider` and `groupDetailProvider`
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla && flutter analyze lib/features/events/screens/ 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - lib/features/events/screens/create_event_screen.dart contains `class CreateEventScreen extends ConsumerStatefulWidget`
    - CreateEventScreen constructor has `required this.groupId` and `required this.eventType`
    - AppBar title contains 'New' and the event type label
    - TextFormField has hint 'e.g. Summer camping trip'
    - Validator returns "Event name can't be empty." for empty input
    - Participant list watches groupMembersProvider(groupId)
    - All participants pre-checked by default (_selectedParticipantIds initialized from members)
    - Custom type shows module toggle switches (grep for `EventType.custom`)
    - Ledger toggle is disabled (onChanged: null) with value always true
    - Submit calls ref.read(eventServiceProvider).createEvent
    - Error SnackBar shows "Couldn't create event. Check your connection and try again."
    - `flutter analyze lib/features/events/screens/` reports no issues
  </acceptance_criteria>
  <done>EventTypePickerScreen and CreateEventScreen implement the full two-step event creation flow per D-02. Form validates name and participants. Custom events show module toggles with Ledger locked on. Widget tests verify key behaviors.</done>
</task>

</tasks>

<verification>
- `flutter analyze lib/features/events/screens/` reports no issues
- `flutter test test/features/events/create_event_test.dart` passes
- EventTypePickerScreen shows 5 type cards with correct labels
- CreateEventScreen pre-checks all group members
- Custom type shows module toggles with Ledger locked on
</verification>

<success_criteria>
Event creation UI flow is complete. User can select an event type from 5 visual cards, fill out the creation form with name/dates/participants, and submit. Custom events allow module selection. All form validation matches the Copywriting Contract.
</success_criteria>

<output>
After completion, create `.planning/phases/03-events/03-02-SUMMARY.md`
</output>
