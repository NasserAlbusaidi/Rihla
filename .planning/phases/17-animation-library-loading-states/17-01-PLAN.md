---
phase: 17-animation-library-loading-states
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/shared/widgets/skeleton_loader.dart
  - lib/shared/widgets/skeleton_primitives.dart
  - test/unit/skeleton_loader_test.dart
autonomous: true
requirements: [NAV-05]

must_haves:
  truths:
    - "SkeletonLoader.dashboardHero() renders a content-aware skeleton with circle, bars, and blocks mirroring a dashboard hero layout"
    - "SkeletonLoader.eventCard() renders a skeleton mirroring an event card with icon circle, title bar, subtitle bar, and trailing amount block"
    - "SkeletonLoader.groupList() renders content-aware skeletons with avatar circle and text bars instead of opaque rectangles"
    - "SkeletonLoader.expenseList() renders skeletons matching expense row layout (avatar circle, two text lines, trailing amount)"
    - "SkeletonLoader.gearList() renders skeletons matching gear item layout (checkbox block, name bar, assignee bar)"
    - "SkeletonLoader.generic() remains available as fallback for screens without a named variant"
    - "Shimmer uses warm-neutral colors from AppColors tokens (surfaceLight #F3F4F6 base, surface #F8F9FA highlight)"
  artifacts:
    - path: "lib/shared/widgets/skeleton_primitives.dart"
      provides: "Composable skeleton building blocks: SkeletonCircle, SkeletonBar, SkeletonBlock, SkeletonRow, SkeletonCard"
      exports: ["SkeletonCircle", "SkeletonBar", "SkeletonBlock", "SkeletonRow", "SkeletonCard"]
    - path: "lib/shared/widgets/skeleton_loader.dart"
      provides: "Named skeleton variant factories using primitives"
      exports: ["SkeletonLoader"]
    - path: "test/unit/skeleton_loader_test.dart"
      provides: "Widget tests for all 6 skeleton variants + primitives"
      min_lines: 80
  key_links:
    - from: "lib/shared/widgets/skeleton_loader.dart"
      to: "lib/shared/widgets/skeleton_primitives.dart"
      via: "import and usage in factory constructors"
      pattern: "import.*skeleton_primitives"
    - from: "lib/shared/widgets/skeleton_loader.dart"
      to: "shimmer package"
      via: "Shimmer.fromColors wrapping all skeleton children"
      pattern: "Shimmer\\.fromColors"
---

<objective>
Build a composable skeleton primitive library and 6 named skeleton variant factories that produce content-aware loading placeholders matching real widget layouts.

Purpose: Replace the current opaque-rectangle skeletons with content-aware loading states (per D-01, D-02) so layout does not jump when data loads. This directly satisfies NAV-05 (skeleton loading states instead of spinners).

Output: `skeleton_primitives.dart` (5 primitive widgets), refactored `skeleton_loader.dart` (6 named factories using primitives), and comprehensive widget tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-animation-library-loading-states/17-CONTEXT.md
@.planning/phases/17-animation-library-loading-states/17-RESEARCH.md

@lib/shared/widgets/skeleton_loader.dart
@lib/core/theme/app_theme.dart
@lib/core/theme/tokens/color_tokens.dart
@lib/core/theme/tokens/spacing_tokens.dart

<interfaces>
<!-- Key types and contracts the executor needs. -->

From lib/core/theme/app_theme.dart:
```dart
class AppColors {
  static const Color surface = Color(0xFFF8F9FA);     // shimmer highlight
  static const Color surfaceLight = Color(0xFFF3F4F6); // shimmer base
  static const double radiusSmall = 8;
  static const double radiusMedium = 12;
  static const double radiusLarge = 16;
  static const double space4 = 4;
  static const double space8 = 8;
  static const double space12 = 12;
  static const double space16 = 16;
  static const double space24 = 24;
}
```

From lib/shared/widgets/skeleton_loader.dart (current — to be refactored):
```dart
class SkeletonLoader extends StatelessWidget {
  final int itemCount;
  final Widget Function(BuildContext, int) itemBuilder;
  const SkeletonLoader({super.key, required this.itemCount, required this.itemBuilder});
  factory SkeletonLoader.cardList({int count = 5});
  factory SkeletonLoader.documentList({int count = 4});
  factory SkeletonLoader.groupList({int count = 3});
}
```

Existing consumers (do NOT break these):
- `gear_screen.dart` calls `SkeletonLoader.cardList()`
- `vault_screen.dart` calls `SkeletonLoader.documentList()`
- `home_screen.dart` calls `SkeletonLoader.groupList(count: 3)`
- `logistics_screen.dart` calls `SkeletonLoader.groupList`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create skeleton primitives and write tests</name>
  <files>lib/shared/widgets/skeleton_primitives.dart, test/unit/skeleton_loader_test.dart</files>
  <read_first>
    lib/shared/widgets/skeleton_loader.dart
    lib/core/theme/app_theme.dart
    lib/core/theme/tokens/color_tokens.dart
    lib/core/theme/tokens/spacing_tokens.dart
  </read_first>
  <behavior>
    - Test: SkeletonCircle renders a Container with the specified size and circular border radius
    - Test: SkeletonBar renders a Container with the specified width, height defaults to 14.0, and rounded corners
    - Test: SkeletonBlock renders a Container with the specified width and height, with rounded corners
    - Test: SkeletonRow renders a Row with a SkeletonCircle and Column of SkeletonBars
    - Test: SkeletonCard renders a Container with rounded corners at AppColors.radiusLarge (16dp) and internal padding
  </behavior>
  <action>
    Create `lib/shared/widgets/skeleton_primitives.dart` with 5 public StatelessWidget primitives per D-07:

    1. **SkeletonCircle** — `const SkeletonCircle({required double size})`.
       Renders `Container(width: size, height: size, decoration: BoxDecoration(color: AppColors.surface, shape: BoxShape.circle))`.

    2. **SkeletonBar** — `const SkeletonBar({required double width, double height = 14.0, double? borderRadius})`.
       Renders `Container(width: width, height: height, decoration: BoxDecoration(color: AppColors.surface, borderRadius: BorderRadius.circular(borderRadius ?? AppColors.radiusSmall)))`.

    3. **SkeletonBlock** — `const SkeletonBlock({required double width, required double height, double? borderRadius})`.
       Renders `Container(width: width, height: height, decoration: BoxDecoration(color: AppColors.surface, borderRadius: BorderRadius.circular(borderRadius ?? AppColors.radiusMedium)))`.

    4. **SkeletonRow** — `const SkeletonRow({double circleSize = 40, double barWidth = 140, double barHeight = 14, double smallBarWidth = 80, double smallBarHeight = 12, double gap = 12})`.
       Renders `Row(children: [SkeletonCircle(size: circleSize), SizedBox(width: gap), Expanded(child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [SkeletonBar(width: barWidth, height: barHeight), SizedBox(height: 6), SkeletonBar(width: smallBarWidth, height: smallBarHeight)]))])`.

    5. **SkeletonCard** — `const SkeletonCard({double height = 72, double? borderRadius})`.
       Renders `Container(height: height, decoration: BoxDecoration(color: AppColors.surface, borderRadius: BorderRadius.circular(borderRadius ?? AppColors.radiusLarge)))`.

    All primitives import `../../core/theme/app_theme.dart` for `AppColors` constants.
    All containers use `AppColors.surface` as fill color (the shimmer parent handles the shimmer animation — children just need an opaque fill).

    Then create `test/unit/skeleton_loader_test.dart` with widget tests for each primitive. Use `tester.pump()` (never `pumpAndSettle` — shimmer uses `repeat()`). Verify widget existence with `find.byType()`. Verify Container dimensions where possible using `tester.getSize()`.
  </action>
  <verify>
    <automated>flutter test test/unit/skeleton_loader_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/shared/widgets/skeleton_primitives.dart` contains `class SkeletonCircle extends StatelessWidget`
    - `lib/shared/widgets/skeleton_primitives.dart` contains `class SkeletonBar extends StatelessWidget`
    - `lib/shared/widgets/skeleton_primitives.dart` contains `class SkeletonBlock extends StatelessWidget`
    - `lib/shared/widgets/skeleton_primitives.dart` contains `class SkeletonRow extends StatelessWidget`
    - `lib/shared/widgets/skeleton_primitives.dart` contains `class SkeletonCard extends StatelessWidget`
    - `test/unit/skeleton_loader_test.dart` contains at least 5 `testWidgets` calls
    - `test/unit/skeleton_loader_test.dart` does NOT contain `pumpAndSettle`
    - `flutter test test/unit/skeleton_loader_test.dart` exits 0
  </acceptance_criteria>
  <done>All 5 skeleton primitive widgets exist as public StatelessWidgets with tests passing</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor SkeletonLoader with named variant factories and warm shimmer</name>
  <files>lib/shared/widgets/skeleton_loader.dart, test/unit/skeleton_loader_test.dart</files>
  <read_first>
    lib/shared/widgets/skeleton_loader.dart
    lib/shared/widgets/skeleton_primitives.dart
    lib/core/theme/app_theme.dart
    lib/features/gear/screens/gear_screen.dart
    lib/features/vault/screens/vault_screen.dart
    lib/features/home/screens/home_screen.dart
    lib/features/logistics/screens/logistics_screen.dart
    test/unit/skeleton_loader_test.dart
  </read_first>
  <behavior>
    - Test: SkeletonLoader.dashboardHero() renders Shimmer with SkeletonBlock and SkeletonBar children (hero card + stats row)
    - Test: SkeletonLoader.eventCard() renders Shimmer with SkeletonCircle, SkeletonBar, and SkeletonBlock children
    - Test: SkeletonLoader.groupList() renders Shimmer with SkeletonRow children (avatar circle + text bars)
    - Test: SkeletonLoader.expenseList() renders Shimmer with SkeletonRow children matching expense layout
    - Test: SkeletonLoader.gearList() renders Shimmer with SkeletonBlock (checkbox) + SkeletonBar (name + assignee)
    - Test: SkeletonLoader.generic() renders Shimmer wrapping plain SkeletonCard items (backward compatible fallback)
    - Test: SkeletonLoader.cardList() still works (backward compat — delegates to generic or remains)
    - Test: SkeletonLoader.documentList() still works (backward compat)
    - Test: Shimmer base color is AppColors.surfaceLight (#F3F4F6)
    - Test: Shimmer highlight color is AppColors.surface (#F8F9FA)
  </behavior>
  <action>
    Refactor `lib/shared/widgets/skeleton_loader.dart` to:

    1. **Add import** for `skeleton_primitives.dart` at top: `import 'skeleton_primitives.dart';`

    2. **Keep existing constructor** `SkeletonLoader({required this.itemCount, required this.itemBuilder})` unchanged.

    3. **Keep existing factories** `cardList`, `documentList`, `groupList` for backward compatibility. Update `groupList` to use content-aware primitives instead of opaque `_SkeletonCard`:
       ```dart
       factory SkeletonLoader.groupList({int count = 3}) {
         return SkeletonLoader(
           itemCount: count,
           itemBuilder: (context, index) => Padding(
             padding: const EdgeInsets.symmetric(horizontal: 24, vertical: 8),
             child: Column(
               crossAxisAlignment: CrossAxisAlignment.start,
               children: [
                 SkeletonRow(circleSize: 44, barWidth: 160, smallBarWidth: 100),
                 const SizedBox(height: 12),
                 SkeletonBar(width: 200, height: 12),
               ],
             ),
           ),
         );
       }
       ```

    4. **Add 5 new named factories** per D-08:

       **`dashboardHero({int count = 1})`** — Mirrors the balance hero card:
       - `SkeletonBlock(width: double.infinity, height: 120, borderRadius: AppColors.radiusLarge)` for the hero card
       - `SizedBox(height: 16)`
       - `Row` with 3x `Expanded(child: SkeletonBlock(width: double.infinity, height: 56))` separated by `SizedBox(width: 12)` for quick-action buttons
       - `SizedBox(height: 16)`
       - 2x `SkeletonRow()` for recent activity items

       **`eventCard({int count = 3})`** — Mirrors event card layout:
       - `Row`: `SkeletonCircle(size: 40)` + `SizedBox(width: 12)` + `Expanded(Column: SkeletonBar(width: 120, height: 16) + SizedBox(height: 6) + SkeletonBar(width: 80, height: 12))` + `SkeletonBlock(width: 48, height: 20)`

       **`expenseList({int count = 5})`** — Mirrors expense row layout:
       - `SkeletonRow(circleSize: 40, barWidth: 140, smallBarWidth: 80)` + trailing `SkeletonBar(width: 60, height: 16)`
       Wrap in a Row: `[Expanded(child: SkeletonRow(...)), SkeletonBar(width: 60, height: 16)]`

       **`gearList({int count = 5})`** — Mirrors gear item layout:
       - `Row`: `SkeletonBlock(width: 24, height: 24, borderRadius: 4)` (checkbox) + `SizedBox(width: 12)` + `Expanded(Column: SkeletonBar(width: 160, height: 14) + SizedBox(height: 6) + SkeletonBar(width: 100, height: 12))`

       **`generic({int count = 5})`** — Backward-compatible fallback:
       - Same as current `cardList` but with name `generic`. Plain `SkeletonCard(height: 72)`.

    5. **Replace `_SkeletonCard`** class at bottom of file with a comment referencing `skeleton_primitives.dart`. The `_SkeletonCard` private class is replaced by the public `SkeletonCard` from primitives.

    6. **Change `build()` method** to use `Column` instead of `ListView.builder` for the skeleton list (per Research pitfall 6 — avoids zero-height issue with unbounded parents). Wrap in `Shimmer.fromColors`:
       ```dart
       @override
       Widget build(BuildContext context) {
         return Shimmer.fromColors(
           baseColor: AppColors.surfaceLight,   // #F3F4F6 (per Research shimmer token)
           highlightColor: AppColors.surface,    // #F8F9FA (per Research shimmer token)
           child: Padding(
             padding: const EdgeInsets.only(top: 12),
             child: Column(
               children: List.generate(itemCount, (index) => itemBuilder(context, index)),
             ),
           ),
         );
       }
       ```

    7. **Add tests** to `test/unit/skeleton_loader_test.dart` for each new factory. Test structure: pump widget in MaterialApp > Scaffold, then verify `find.byType(Shimmer)` finds widget, verify child primitives exist via `find.byType(SkeletonCircle)`, `find.byType(SkeletonBar)`, etc. Verify shimmer colors by finding Shimmer widget and checking its `baseColor` and `highlightColor` properties.

    IMPORTANT: Use `tester.pump()` everywhere. NEVER use `tester.pumpAndSettle()` anywhere in this test file — shimmer animation uses `AnimationController.repeat()` which never settles.
  </action>
  <verify>
    <automated>flutter test test/unit/skeleton_loader_test.dart && flutter test</automated>
  </verify>
  <acceptance_criteria>
    - `lib/shared/widgets/skeleton_loader.dart` contains `import 'skeleton_primitives.dart'`
    - `lib/shared/widgets/skeleton_loader.dart` contains `factory SkeletonLoader.dashboardHero`
    - `lib/shared/widgets/skeleton_loader.dart` contains `factory SkeletonLoader.eventCard`
    - `lib/shared/widgets/skeleton_loader.dart` contains `factory SkeletonLoader.expenseList`
    - `lib/shared/widgets/skeleton_loader.dart` contains `factory SkeletonLoader.gearList`
    - `lib/shared/widgets/skeleton_loader.dart` contains `factory SkeletonLoader.generic`
    - `lib/shared/widgets/skeleton_loader.dart` contains `factory SkeletonLoader.cardList` (backward compat preserved)
    - `lib/shared/widgets/skeleton_loader.dart` contains `factory SkeletonLoader.documentList` (backward compat preserved)
    - `lib/shared/widgets/skeleton_loader.dart` contains `AppColors.surfaceLight` as Shimmer baseColor
    - `lib/shared/widgets/skeleton_loader.dart` contains `AppColors.surface` as Shimmer highlightColor
    - `lib/shared/widgets/skeleton_loader.dart` does NOT contain `class _SkeletonCard` (replaced by primitives)
    - `lib/shared/widgets/skeleton_loader.dart` contains `Column(` in build method (not ListView.builder)
    - `test/unit/skeleton_loader_test.dart` contains tests for `dashboardHero`, `eventCard`, `expenseList`, `gearList`, `generic`
    - `flutter test test/unit/skeleton_loader_test.dart` exits 0
    - `flutter test` exits 0 (no regression in existing tests)
  </acceptance_criteria>
  <done>SkeletonLoader has 6 named factories using composable primitives, shimmer uses warm AppColors tokens, all existing consumers still compile, and all tests pass</done>
</task>

</tasks>

<verification>
1. `flutter test test/unit/skeleton_loader_test.dart` — all skeleton primitive and factory tests pass
2. `flutter test` — full suite green (no regression from SkeletonLoader refactor)
3. `grep -c 'factory SkeletonLoader\.' lib/shared/widgets/skeleton_loader.dart` returns at least 8 (cardList, documentList, groupList, dashboardHero, eventCard, expenseList, gearList, generic)
4. `grep -c 'class Skeleton' lib/shared/widgets/skeleton_primitives.dart` returns 5
5. `grep 'pumpAndSettle' test/unit/skeleton_loader_test.dart` returns no matches
</verification>

<success_criteria>
- 5 skeleton primitive widgets exist as composable building blocks in skeleton_primitives.dart
- 6 named skeleton variant factories (dashboardHero, eventCard, groupList, expenseList, gearList, generic) exist on SkeletonLoader
- 3 existing factories (cardList, documentList, groupList) preserved for backward compatibility
- Shimmer uses AppColors.surfaceLight as base and AppColors.surface as highlight
- No _SkeletonCard private class remains (replaced by public SkeletonCard primitive)
- All tests pass including full regression suite
</success_criteria>

<output>
After completion, create `.planning/phases/17-animation-library-loading-states/17-01-SUMMARY.md`
</output>
