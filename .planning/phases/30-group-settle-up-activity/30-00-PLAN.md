---
phase: 30-group-settle-up-activity
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - test/features/groups/group_settle_up_screen_test.dart
  - test/features/groups/group_activity_screen_test.dart
  - test/unit/group_activity_service_test.dart
autonomous: true
requirements: []
must_haves:
  truths:
    - "Failing test stubs exist for all Phase 30 screen behaviors before implementation begins"
    - "Test infrastructure validates settle-up tabs, activity date groups, filter chips, and activity logging"
  artifacts:
    - path: "test/features/groups/group_settle_up_screen_test.dart"
      provides: "Failing stubs for 4-tab layout, card tiles, history tab"
    - path: "test/features/groups/group_activity_screen_test.dart"
      provides: "Failing stubs for date sections, filter chips, infinite scroll"
    - path: "test/unit/group_activity_service_test.dart"
      provides: "Failing stubs for logGroupEvent call verification"
  key_links:
    - from: "group_settle_up_screen_test.dart"
      to: "group_settle_up_screen.dart"
      via: "pumps GroupSettleUpScreen widget"
      pattern: "GroupSettleUpScreen"
    - from: "group_activity_screen_test.dart"
      to: "group_activity_screen.dart"
      via: "pumps GroupActivityScreen widget"
      pattern: "GroupActivityScreen"
---

<objective>
Create failing test stubs (RED phase) for all Phase 30 screen behaviors before any implementation begins.

Purpose: Establishes TDD contract — Plans 01-03 implement against these tests. Ensures Nyquist compliance by placing test infrastructure in Wave 0 before implementation waves.

Output: 3 test files with failing stubs that will turn green as Plans 01-03 execute.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/30-group-settle-up-activity/30-CONTEXT.md
@.planning/phases/30-group-settle-up-activity/30-UI-SPEC.md

<interfaces>
<!-- Key types the test stubs will reference -->

From lib/features/groups/screens/group_settle_up_screen.dart:
```dart
class GroupSettleUpScreen extends ConsumerStatefulWidget {
  final String groupId;
  final String? preSelectedMemberId;
  const GroupSettleUpScreen({required this.groupId, this.preSelectedMemberId, super.key});
}
```

From lib/features/groups/screens/group_activity_screen.dart:
```dart
class GroupActivityScreen extends ConsumerStatefulWidget {
  final String groupId;
  const GroupActivityScreen({required this.groupId, super.key});
}
```

From lib/features/groups/widgets/group_stats_grid.dart:
```dart
class GroupStatsGrid extends StatelessWidget {
  final Decimal userNetBalance;
  // After Plan 01: will have subtitle showing 'You owe' / 'Owed to you' / 'Settled'
}
```

From lib/features/groups/keys/group_keys.dart (after Plan 01):
```dart
static const settleUpTabBar = Key('group_settle_up_tab_bar');
static const settleUpYouOweTab = Key('group_settle_up_you_owe_tab');
static const settleUpOwedToYouTab = Key('group_settle_up_owed_to_you_tab');
static const settleUpBetweenOthersTab = Key('group_settle_up_between_others_tab');
static const settleUpHistoryTab = Key('group_settle_up_history_tab');
static const settleUpRecordPaymentButton = Key('group_settle_up_record_payment_button');
static const activityFilterAll = Key('group_activity_filter_all');
static const activityFilterSettlements = Key('group_activity_filter_settlements');
static const activityFilterEvents = Key('group_activity_filter_events');
static const activityFilterMembers = Key('group_activity_filter_members');
```

From lib/features/groups/services/group_activity_service.dart:
```dart
class GroupActivityService extends FirestoreRepository {
  void logGroupEvent({required String groupId, required String type, ...});
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create failing test stubs for settle-up screen (D-01 through D-06)</name>
  <files>test/features/groups/group_settle_up_screen_test.dart</files>
  <read_first>
    - test/features/groups/group_settle_up_screen_test.dart (FULL — existing tests to preserve)
    - lib/features/groups/screens/group_settle_up_screen.dart (current API — constructor params)
    - lib/features/groups/keys/group_keys.dart (existing keys)
    - lib/features/groups/providers/group_balance_provider.dart (providers to override)
  </read_first>
  <action>
    Add NEW test stubs to the EXISTING test file. Do NOT modify or remove any existing tests — they verify current behavior and should still pass. The new stubs will FAIL (RED) because they test behaviors that Plans 02 will implement.

    Add the following `skip`-annotated test stubs at the END of the existing test group. Mark each with `skip: 'Phase 30 — will pass after Plan 02 implementation'` so the test file still passes but stubs are visible:

    ```dart
    // --- Phase 30 Stubs (RED — will turn GREEN after Plan 02) ---

    testWidgets('renders 4 tab labels in AppTabBar', (tester) async {
      // Pump GroupSettleUpScreen with standard overrides
      // Expect: find.text('You Owe'), find.text('Owed to You'),
      //         find.text('Between Others'), find.text('History')
      //         all findsOneWidget
    }, skip: 'Phase 30 — will pass after Plan 02 implementation');

    testWidgets('You Owe tab shows debtor settlements only', (tester) async {
      // Override currentUserIdProvider with test uid
      // Override balances where test uid owes others
      // Expect: debtor settlement tiles visible on first tab
    }, skip: 'Phase 30 — will pass after Plan 02 implementation');

    testWidgets('History tab shows past settlements from groupSettlementsProvider', (tester) async {
      // Override groupSettlementsProvider with test Settlement objects
      // Tap 'History' tab
      // Expect: payer/recipient names and amounts visible
    }, skip: 'Phase 30 — will pass after Plan 02 implementation');

    testWidgets('card-style settlement tiles with collapsible breakdown', (tester) async {
      // Pump with settlements data
      // Expect: tiles have cardSurface background, borderRadius 16
      // Tap tile to expand — expect per-event breakdown visible
    }, skip: 'Phase 30 — will pass after Plan 02 implementation');

    testWidgets('shows per-tab empty state when You Owe has no items', (tester) async {
      // Override with zero balances for current user
      // Expect: find.text('Nothing to pay') on You Owe tab
    }, skip: 'Phase 30 — will pass after Plan 02 implementation');

    testWidgets('all settled state when no settlements and no history', (tester) async {
      // Override with all-zero balances and empty history
      // Expect: find.text('All settled up')
    }, skip: 'Phase 30 — will pass after Plan 02 implementation');
    ```

    Also add a behavioral test for GroupStatsGrid balance subtitle (D-12, to verify Plan 01 Task 1):

    ```dart
    testWidgets('GroupStatsGrid shows "You owe" subtitle for negative balance', (tester) async {
      // Pump GroupStatsGrid with userNetBalance = Decimal.parse('-5.000')
      // Expect: find.text('You owe') findsOneWidget
    }, skip: 'Phase 30 — will pass after Plan 01 implementation');

    testWidgets('GroupStatsGrid shows "Owed to you" subtitle for positive balance', (tester) async {
      // Pump GroupStatsGrid with userNetBalance = Decimal.parse('5.000')
      // Expect: find.text('Owed to you') findsOneWidget
    }, skip: 'Phase 30 — will pass after Plan 01 implementation');
    ```

    The stubs should have enough scaffold code to compile (imports, pump calls) but test assertions that will fail against the current implementation. Using `skip` ensures the test file remains green while stubs are visible as pending.

    IMPORTANT: Preserve all existing tests exactly as they are.
  </action>
  <verify>
    <automated>flutter test test/features/groups/group_settle_up_screen_test.dart --no-pub</automated>
  </verify>
  <done>Test file has 8 new skip-annotated stubs for Phase 30 behaviors. Existing tests still pass. Stubs will be unskipped and fleshed out by Plans 01 and 02.</done>
</task>

<task type="auto">
  <name>Task 2: Create failing test stubs for activity screen + activity service (D-07 through D-14)</name>
  <files>
    test/features/groups/group_activity_screen_test.dart,
    test/unit/group_activity_service_test.dart
  </files>
  <read_first>
    - test/features/groups/group_activity_screen_test.dart (FULL — existing tests to preserve)
    - lib/features/groups/screens/group_activity_screen.dart (current API)
    - lib/features/groups/services/group_activity_service.dart (logGroupEvent signature)
    - lib/features/groups/models/group_activity_log_model.dart (GroupActivityLog constructor)
    - lib/features/groups/keys/group_keys.dart (existing keys)
  </read_first>
  <action>
    **Activity screen test stubs** — Add to EXISTING file, same `skip` pattern:

    ```dart
    // --- Phase 30 Stubs (RED — will turn GREEN after Plan 03) ---

    testWidgets('renders date section headers (TODAY, YESTERDAY)', (tester) async {
      // Seed activities with today and yesterday timestamps
      // Expect: find.text('TODAY'), find.text('YESTERDAY')
    }, skip: 'Phase 30 — will pass after Plan 03 implementation');

    testWidgets('renders 4 filter chips', (tester) async {
      // Pump screen
      // Expect: find.byKey(GroupKeys.activityFilterAll),
      //         find.byKey(GroupKeys.activityFilterSettlements),
      //         find.byKey(GroupKeys.activityFilterEvents),
      //         find.byKey(GroupKeys.activityFilterMembers)
    }, skip: 'Phase 30 — will pass after Plan 03 implementation');

    testWidgets('Settlements filter shows only settlement activities', (tester) async {
      // Seed mixed activities (settlement + event + member)
      // Tap Settlements filter chip
      // Expect: only settlement description visible
    }, skip: 'Phase 30 — will pass after Plan 03 implementation');

    testWidgets('renders activity tile with actor name and description', (tester) async {
      // Seed one activity
      // Expect: actor name and description text visible
    }, skip: 'Phase 30 — will pass after Plan 03 implementation');

    testWidgets('no Load more button present (infinite scroll replaces it)', (tester) async {
      // Pump screen with activities
      // Expect: find.text('Load more') findsNothing
    }, skip: 'Phase 30 — will pass after Plan 03 implementation');
    ```

    **Activity service test file** — Create NEW file `test/unit/group_activity_service_test.dart`:

    ```dart
    // Stubs for verifying logGroupEvent is called from all action sites (D-14)
    // These test the SERVICE layer, not the UI. They verify that:
    // 1. logGroupEvent writes to Firestore with correct type field
    // 2. All 5 action types produce valid activity log documents

    testWidgets('logGroupEvent writes event_created to Firestore', (tester) async {
      // Create FakeFirebaseFirestore
      // Call logGroupEvent with type: 'event_created'
      // Verify document exists in groups/{gid}/activity with type == 'event_created'
    }, skip: 'Phase 30 — will pass after Plan 01 Task 2');

    testWidgets('logGroupEvent writes member_joined to Firestore', (tester) async {
      // Same pattern with type: 'member_joined'
    }, skip: 'Phase 30 — will pass after Plan 01 Task 2');

    testWidgets('logGroupEvent writes member_left to Firestore', (tester) async {
      // Same pattern with type: 'member_left'
    }, skip: 'Phase 30 — will pass after Plan 01 Task 2');
    ```

    IMPORTANT: Preserve all existing tests in group_activity_screen_test.dart exactly as they are.
  </action>
  <verify>
    <automated>flutter test test/features/groups/group_activity_screen_test.dart test/unit/group_activity_service_test.dart --no-pub</automated>
  </verify>
  <done>Activity screen test file has 5 new skip-annotated stubs. New group_activity_service_test.dart has 3 skip-annotated stubs for D-14 verification. All existing tests still pass.</done>
</task>

</tasks>

<verification>
- `flutter test test/features/groups/group_settle_up_screen_test.dart --no-pub` — passes (stubs are skipped)
- `flutter test test/features/groups/group_activity_screen_test.dart --no-pub` — passes (stubs are skipped)
- `flutter test test/unit/group_activity_service_test.dart --no-pub` — passes (stubs are skipped)
- All stubs are visible in test output as "skipped"
</verification>

<success_criteria>
1. 8 skip-annotated stubs in group_settle_up_screen_test.dart (including 2 for GroupStatsGrid D-12)
2. 5 skip-annotated stubs in group_activity_screen_test.dart
3. 3 skip-annotated stubs in new group_activity_service_test.dart
4. All existing tests still pass
5. Wave 0 requirements from VALIDATION.md are satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/30-group-settle-up-activity/30-00-SUMMARY.md`
</output>
