---
phase: 30
slug: group-settle-up-activity
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-04
revised: 2026-04-04
---

# Phase 30 — UI Design Contract

> Visual and interaction contract for GroupSettleUpScreen + GroupActivityScreen redesign.
> Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (Flutter-native ThemeExtension) |
| Preset | not applicable |
| Component library | Material 3 (Flutter SDK) |
| Icon library | iconsax ^0.0.8 |
| Font | Plus Jakarta Sans (google_fonts) |

Source: `lib/core/theme/app_theme.dart`, `lib/core/theme/tokens/`

---

## Spacing Scale

Declared values — all from `AppSpacingTokens.standard` in `lib/core/theme/tokens/spacing_tokens.dart`:

| Token | Value | Usage |
|-------|-------|-------|
| space4 | 4dp | Icon gaps, badge padding, chip inner padding |
| space8 | 8dp | Compact row gaps, avatar-to-text gap, chip label padding |
| space16 | 16dp | Card horizontal padding, section body padding, list item height contribution |
| space24 | 24dp | Horizontal page margin, header horizontal padding |
| space32 | 32dp | Header bottom padding (dark theme), section header vertical gap |

### Project Spacing Exceptions

Authority: `AppSpacingTokens.standard` in `lib/core/theme/tokens/spacing_tokens.dart` is the canonical spacing source. The following values appear in that token file and are used in this phase; they are multiples of 4 but fall between the checker's standard set entries:

| Token | Value | Rationale |
|-------|-------|-----------|
| space12 | 12dp | Exists in `AppSpacingTokens.standard` as `space12`. Used for internal card padding (vertical) and filter chip vertical padding — 8dp is too tight for readable tap targets inside cards, 16dp adds unnecessary height to short metadata rows. |
| space20 | 20dp | Exists in `AppSpacingTokens.standard` as `space20`. Used for input field content padding (vertical) and tab bar vertical margin — matches `buttonHeight: 52dp` rhythm established in `app_theme.dart`; 16dp produces cramped text input fields below the established button height. |

These are declared project tokens, not ad-hoc deviations. Any spacing value in this spec must map to a named `AppSpacingTokens` constant — no magic numbers in implementation.

Border radii:

| Token | Value | Usage |
|-------|-------|-------|
| radiusSmall | 8dp | Filter chips, tab indicator, tab container corners |
| radiusMedium | 12dp | Tab bar container, input fields, bottom sheet input fields |
| radiusLarge | 16dp | Settlement cards, activity tile cards |

Exceptions:
- Touch targets (back button, filter chips): minimum 44dp height enforced via `Container(width: 44, height: 44)` pattern already in `ModuleHeader`
- Bottom sheet top corners: 20dp radius (from `bottomSheetTheme` in `app_theme.dart`)
- Card containers used in sections (Phase 26/29 pattern): 24dp radius (`borderRadius: 24`)

---

## Typography

All weights and sizes are from `AppTheme._buildTextTheme` in `lib/core/theme/app_theme.dart`. Font: Plus Jakarta Sans.

Declared scale — exactly 4 sizes, exactly 2 weights:

| Role | Size | Weight | Line Height | Token | Usage |
|------|------|--------|-------------|-------|-------|
| Screen heading | 28sp | 700 | 1.2 | `displaySmall` | ModuleHeader title ("Settle Up", "Activity") |
| Card title | 17sp | 700 | 1.3 | `titleLarge` | Settlement tile participant names, settlement amount display, history tile primary text |
| Body / description | 14sp | 400 | 1.5 | `bodyMedium` | Activity tile actor name, description text, settlement note text, per-event breakdown rows |
| Label / metadata | 12sp | 400 | 1.4 | `bodySmall` | Date section headers (uppercase, 0.5 letter-spacing), relative timestamps, filter chip counts, "OMR" currency label, event count sub-label |

Notes on consolidation from prior draft:
- 15sp actor name absorbed into 14sp body — the initials circle (48dp) provides sufficient visual separation without a distinct size
- 20sp prominent amount absorbed into 17sp card title — weight 700 and semantic color (`errorText` / `successText`) provide visual dominance without a fifth size slot
- 13sp tab bar labels are internal to `AppTabBar` widget and are not declared in the phase spec (widget owns its own internal sizing)
- 800 weight replaced with 700 throughout — the visual difference between 700 and 800 at 17–28sp is negligible at 390dp screen width and costs a weight slot
- 600 weight replaced with 700 — consolidates emphasis to a single non-body weight

Color differentiation replaces size differentiation for amounts:
- "You Owe" amount: 17sp/700, `errorText` (#B91C1C)
- "Owed to You" amount: 17sp/700, `successText` (#047857)
- "Between Others" amount: 17sp/700, `textPrimary` (#111827)

---

## Color

Source: `AppColorTokens.light` in `lib/core/theme/tokens/color_tokens.dart`.

| Role | Token | Hex | Usage |
|------|-------|-----|-------|
| Dominant (60%) | `scaffoldBackground` | #FFFFFF | Page background for both screens |
| Secondary (30%) | `cardSurface` | #F8F9FA | Settlement cards, activity tile backgrounds, section containers, bottom sheet surface |
| Accent (10%) | `primary` | #0D7B74 | Active tab indicator pill, gradient CTA button (paired with `primaryDark`), filter chip selected border |

Additional semantic colors for this phase:

| Token | Hex | Reserved For |
|-------|-----|-------------|
| `errorText` | #B91C1C | "You Owe" amount in settlement tiles and "You Owe" tab — debtor direction only |
| `successText` | #047857 | "Owed to You" amount in settlement tiles — creditor direction only |
| `textPrimary` | #111827 | "Between Others" amounts, participant names, card titles |
| `textSecondary` | #6B7280 | Activity tile description text, settlement note text, secondary metadata |
| `textMuted` | #9CA3AF | Date section headers, relative timestamps, inactive tab labels — decorative/metadata only, never for functional amounts or labels |
| `selectionFill` | #E6F5F3 | Filter chip selected background |
| `inputFill` | #F3F4F6 | Tab bar container background, unselected filter chip background |
| `border` | #E5E7EB | Card borders, dividers, input borders in bottom sheet |
| `inputFillWarm` | #F5EDE1 | Bottom sheet input field fill (Record Settlement form) |
| `focusBorderWarm` | #CC6B49 | Bottom sheet input field focus border |
| `success` | #10B981 | "All settled" empty state checkmark circle icon |
| `headerGradientStart` / `headerGradientEnd` | #111827 / #1F2937 | ModuleHeader dark gradient (both screens, D-01, D-07) |
| `primaryGradient` | #0D7B74 → #0A6B65 | Record Settlement CTA button gradient (D-05) |

Accent reserved for:
- Active tab indicator pill (AppTabBar)
- Filter chip selected-state border (1.5dp stroke on `selectionFill` background)
- Record Settlement "Record Payment" button gradient
- `preSelectedMemberId` auto-scroll highlight (brief teal tint on card, fade-out 300ms)
- Settlement type icon (`Iconsax.money_recive`) in activity tiles — only this icon type uses accent color; all others use `textMuted`

WCAG note: `textMuted` (#9CA3AF) is 2.86:1 on white — decorative use only. All functional text uses `textSecondary` (5.74:1) or `textPrimary` (17.15:1).

---

## Component Inventory

### Reused without modification

| Widget | File | Used On |
|--------|------|---------|
| `ModuleHeader` (useDarkTheme: true) | `lib/shared/widgets/module_header.dart` | Both screens — replaces custom headers (D-01, D-07) |
| `AppTabBar` | `lib/shared/widgets/app_tab_bar.dart` | GroupSettleUpScreen — 4 tabs: You Owe / Owed to You / Between Others / History |
| `EmptyStateView` | `lib/shared/widgets/empty_state_view.dart` | Per-tab empty states, activity feed empty state |
| `SkeletonLoader` | `lib/shared/widgets/skeleton_loader.dart` | Tab loading states, initial activity load |

### Modified / redesigned

| Widget | File | Change |
|--------|------|--------|
| `GroupSettlementTile` | `lib/features/groups/widgets/group_settlement_tile.dart` | Redesign as card — `cardSurface` bg, `shadowRaised`, `borderRadius: 16`, avatar initials circle (40dp), prominent amount, collapsible per-event breakdown (D-04) |
| `GroupActivityTile` | `lib/features/groups/widgets/group_activity_tile.dart` | Redesign as rich tile — 48dp initials circle, actor name (14sp/400/textPrimary), description (14sp/400/textSecondary), relative timestamp (12sp/400/textMuted), type icon (right-aligned, 20dp, semantic color per mapping) (D-09) |

### New layout patterns (inline, not extracted to separate files)

| Pattern | Used On | Notes |
|---------|---------|-------|
| `_DateSectionHeader` | GroupActivityScreen | Copy from `ActivityFeedScreen` — identical logic, different model field (`.timestamp` not `.createdAt`) |
| Filter chip row | GroupActivityScreen | `SingleChildScrollView(scrollDirection: Axis.horizontal)` containing `FilterChip` widgets — 4 chips: All / Settlements / Events / Members |
| History settlement tile | GroupSettleUpScreen History tab | New design: avatar pair (payer + recipient initials, 32dp each, overlapping -8dp offset), "X paid Y" text, formatted amount, formatted date |

---

## Screen Layouts

### GroupSettleUpScreen

```
Scaffold(backgroundColor: scaffoldBackground)
  Column
    ModuleHeader(title: 'Settle Up', useDarkTheme: true)
      subtitle: group name
    GroupSettlementSummaryCard  ← keep existing, token polish only
    AppTabBar(4 tabs)
    Expanded
      TabBarView
        Tab 0 – You Owe:     ListView of GroupSettlementTile (debtor direction)
        Tab 1 – Owed to You: ListView of GroupSettlementTile (creditor direction)
        Tab 2 – Between Others: ListView of GroupSettlementTile (others)
        Tab 3 – History:     ListView of history settlement tiles
```

Each tab body: `ListView.builder` directly inside `TabBarView` child (not wrapped in Column). Outer `TabBarView` is in `Expanded` to fill remaining height. Avoids nested-scroll conflict (RESEARCH.md Pitfall 1).

Settlement card anatomy (D-04):
- Background: `cardSurface`, `borderRadius: 16`, `shadowRaised`
- Padding: 16dp all sides
- Row: `InitialsCircle(40dp)` | column(participant name 17sp/700, event count label 12sp/400/textMuted) | Spacer | amount column(amount 17sp/700 semantic-colored, "OMR" label 12sp/400/textMuted)
- Collapsible section: per-event breakdown — `ExpansionTile`-style, triggered by chevron icon; list of event rows at 14sp/400

### GroupActivityScreen

Primary visual anchor: the date-grouped activity timeline. The first visible date section header ("TODAY") and its immediately following activity tile are the focal point — the dark `ModuleHeader` gradient above compresses attention downward into this area. The date header + first tile together act as the entry point the eye lands on after the header.

```
Scaffold(backgroundColor: scaffoldBackground)
  Column
    ModuleHeader(title: 'Activity', useDarkTheme: true)
      subtitle: group name
    Filter chip row (horizontal scroll, 12dp vertical padding, 24dp left margin)
    Expanded
      ListView.builder (ScrollController for infinite scroll)
        _DateSectionHeader(label)  ← at group boundaries
        GroupActivityTile           ← per activity entry
        loading indicator at bottom (CircularProgressIndicator, 16dp, teal) if _hasMore
```

Activity tile anatomy (D-09):
- Background: `cardSurface`, `borderRadius: 16`, `shadowRaised`
- Margin: 12dp horizontal, 4dp vertical (tight stacking to feel like a timeline)
- Row: `InitialsCircle(48dp)` | column(actor name 14sp/400/textPrimary, description 14sp/400/textSecondary, relative timestamp 12sp/400/textMuted) | Spacer | type icon (20dp, semantic color, wrapped in `Semantics(label: ...)` — see accessibility note below)

Activity type icon mapping:

| Action type | Icon | Color | Semantics label |
|-------------|------|-------|----------------|
| `group_settlement` | `Iconsax.money_recive` | `primary` (#0D7B74) | "Payment recorded" |
| `event_created` | `Iconsax.calendar_add` | `textMuted` (#9CA3AF) | "Event created" |
| `event_deleted` | `Iconsax.calendar_remove` | `textMuted` (#9CA3AF) | "Event removed" |
| `member_joined` | `Iconsax.profile_add` | `textMuted` (#9CA3AF) | "Member joined" |
| `member_left` | `Iconsax.profile_delete` | `textMuted` (#9CA3AF) | "Member left" |

Accessibility note: each activity type icon is decorative in the sense that the description text conveys the same information, but screen readers should still read the icon label for users navigating by elements. Wrap each icon with `Semantics(label: '{label from table above}', child: Icon(...))`. Do not use `excludeSemantics: true` on these icons.

---

## Interaction Contracts

### Settle-up: Tab switching
- `AppTabBar.onTap` fires `HapticService.selection()` (already in AppTabBar)
- Tab switch: default `TabController.animateTo` animation (300ms)
- `preSelectedMemberId`: auto-switch to "You Owe" (if `fromUserId == currentUid`) or "Owed to You" tab, then `Scrollable.ensureVisible` in next frame

### Settle-up: Record Settlement bottom sheet (D-05)
- Triggered by "Record Payment" button on settlement card
- Bottom sheet: `showModalBottomSheet`, `isScrollControlled: true`, `useSafeArea: true`
- Input fill: `inputFillWarm` (#F5EDE1)
- Input border enabled: `borderWarm` (#E5D5C0), 1.5dp
- Input border focused: `focusBorderWarm` (#CC6B49), 2dp
- CTA button: `Container` with `primaryGradient`, height 52dp, `radiusMedium`, white text 17sp/700
- Label: "Record Payment" (not "Save" or "Submit")
- Loading state: `LoadingButton` with teal spinner

### Settle-up: Collapsible per-event breakdown
- Tap card body to expand/collapse
- Expansion animation: 200ms ease-out
- Chevron icon rotates 180 degrees on expand (AnimatedRotation)
- No haptic on expand (informational interaction, not action)

### Activity: Filter chips
- Selected state: `selectionFill` background (#E6F5F3), `primary` border 1.5dp, `textPrimary` label
- Unselected state: `inputFill` background (#F3F4F6), no border, `textSecondary` label
- Tap fires `HapticService.selection()`
- Filter is client-side — instant response, no loading state
- "All" chip is always visible and resets to no filter

### Activity: Infinite scroll
- Threshold: 200dp from bottom of scroll extent
- Loading indicator: centered `CircularProgressIndicator(strokeWidth: 2, color: primary)` in a 48dp height container
- "End of feed" state: no UI indicator — list simply does not load more

### Entrance animations (both screens)
Pattern from all recent phases (27–29):
```dart
widget.animate()
  .fadeIn(delay: Duration(milliseconds: index * 50))
  .slideY(begin: 0.1, curve: Curves.easeOutCubic)
```
- Settlement cards: stagger by tab index within tab content
- Activity tiles: stagger per section group (reset index at each date section boundary)
- Date section headers: no stagger — instant appear

---

## Copywriting Contract

### GroupSettleUpScreen

| Element | Copy |
|---------|------|
| Screen title | "Settle Up" |
| Screen subtitle | {group name} |
| Tab 0 | "You Owe" |
| Tab 1 | "Owed to You" |
| Tab 2 | "Between Others" |
| Tab 3 | "History" |
| Settlement card amount (debtor) | "{amount} OMR" in errorText |
| Settlement card amount (creditor) | "{amount} OMR" in successText |
| Settlement card sub-label (debtor) | "You owe {name}" |
| Settlement card sub-label (creditor) | "{name} owes you" |
| Settlement card sub-label (between others) | "{name A} owes {name B}" |
| CTA button | "Record Payment" |
| Empty state — You Owe tab | Icon: Iconsax.wallet_check, Title: "Nothing to pay", Body: "You don't owe anyone in this group right now." |
| Empty state — Owed to You tab | Icon: Iconsax.money_recive, Title: "Nothing owed to you", Body: "No one owes you in this group right now." |
| Empty state — Between Others tab | Icon: Iconsax.people, Title: "All balanced", Body: "No outstanding amounts between other members." |
| Empty state — History tab | Icon: Iconsax.receipt_1, Title: "No recorded payments", Body: "Payments recorded in this group will appear here." |
| All settled (global empty — no settlements in any tab) | Icon: checkmark circle (success green), Title: "All settled up", Body: "Everyone is square. No outstanding amounts." (D-06 — keep as-is) |
| Record Settlement note placeholder | "Optional note (e.g. Venmo, cash)" |
| Bottom sheet title | "Record Payment" |

### GroupActivityScreen

| Element | Copy |
|---------|------|
| Screen title | "Activity" |
| Screen subtitle | {group name} |
| Filter chip — all | "All" |
| Filter chip — settlements | "Payments" |
| Filter chip — events | "Events" |
| Filter chip — members | "Members" |
| Date section header — today | "TODAY" |
| Date section header — yesterday | "YESTERDAY" |
| Date section header — other | "{Month} {Day}" (e.g. "Mar 28") — `DateFormat('MMM d')` |
| Activity description — settlement | "{payer} paid {recipient}" |
| Activity description — event created | "{actor} created {event name}" |
| Activity description — event deleted | "{actor} removed {event name}" |
| Activity description — member joined | "{actor} joined the group" |
| Activity description — member left | "{actor} left the group" |
| Empty state — no activity | Icon: Iconsax.activity, Title: "No activity yet", Body: "Group events, payments, and member changes will appear here." |
| Empty state — filter with no results | Icon: Iconsax.search_normal, Title: "No {filter} activity", Body: "Try selecting a different filter." |
| Error state | Icon: Iconsax.warning_2, Title: "Couldn't load activity", Body: "Check your connection and pull to refresh." |

---

## Registry Safety

Not applicable. This is a Flutter project — no shadcn, no web component registry. All widgets are from Flutter SDK (Material 3), existing project shared widgets, and already-installed packages (`iconsax`, `flutter_animate`, `intl`, `timeago`). No new packages are added in this phase.

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| n/a — Flutter project | n/a | n/a |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
