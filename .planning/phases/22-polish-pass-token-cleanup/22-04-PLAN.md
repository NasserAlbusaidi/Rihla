---
phase: 22-polish-pass-token-cleanup
plan: 04
type: execute
wave: 3
depends_on: [22-01, 22-02]
files_modified:
  - lib/features/home/widgets/balance_hero_card.dart
  - lib/features/ledger/widgets/ledger_hero_card.dart
  - lib/features/events/screens/event_expense_hero.dart
  - lib/features/events/widgets/event_spending_hero.dart
  - lib/features/groups/widgets/group_balance_hero.dart
  - lib/features/gear/widgets/gear_hero_card.dart
  - lib/features/vault/widgets/vault_hero_card.dart
  - lib/features/logistics/widgets/logistics_hero_card.dart
  - lib/features/memories/widgets/memories_hero_card.dart
  - lib/features/activity/widgets/activity_hero_card.dart
  - lib/shared/widgets/module_header.dart
autonomous: true
requirements: [PLSH-05]

must_haves:
  truths:
    - "All hero cards (BalanceHeroCard, LedgerHeroCard, EventExpenseHero, EventSpendingHero, GroupBalanceHero, GearHeroCard, VaultHeroCard, LogisticsHeroCard, MemoriesHeroCard, ActivityHeroCard) display a subtle paper grain texture"
    - "ModuleHeader displays grain at ~2% opacity over the dark gradient"
    - "Content list cards remain clean/flat without grain"
    - "Grain texture reads as warm paper/stationery rather than digital noise"
  artifacts:
    - path: "lib/features/home/widgets/balance_hero_card.dart"
      provides: "Grain texture on home balance hero"
      contains: "grain.png"
    - path: "lib/shared/widgets/module_header.dart"
      provides: "Grain texture on dark gradient header at 2% opacity"
      contains: "grain.png"
  key_links:
    - from: "lib/features/home/widgets/balance_hero_card.dart"
      to: "assets/textures/grain.png"
      via: "DecorationImage in BoxDecoration"
      pattern: "grain\\.png"
    - from: "lib/shared/widgets/module_header.dart"
      to: "assets/textures/grain.png"
      via: "DecorationImage at 2% opacity"
      pattern: "grain\\.png.*0\\.02"
---

<objective>
Apply paper grain texture to all hero cards, scaffold backgrounds, and ModuleHeaders.

Purpose: Satisfies PLSH-05 (grain/texture overlays for visual warmth). Creates two-tier visual hierarchy: textured heroes/scaffold vs clean content cards.

Output: All hero cards and ModuleHeaders render with subtle grain texture. Content list cards remain flat/clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/22-polish-pass-token-cleanup/22-CONTEXT.md
@.planning/phases/22-polish-pass-token-cleanup/RESEARCH.md
@.planning/phases/22-polish-pass-token-cleanup/22-01-SUMMARY.md
@.planning/phases/22-polish-pass-token-cleanup/22-02-SUMMARY.md

@lib/shared/widgets/grain_overlay.dart
@lib/features/home/widgets/balance_hero_card.dart
@lib/features/ledger/widgets/ledger_hero_card.dart
@lib/shared/widgets/module_header.dart

<interfaces>
From lib/shared/widgets/grain_overlay.dart (created in Plan 01):
```dart
class GrainOverlay extends StatelessWidget {
  final Widget child;
  final double opacity; // default 0.035

  const GrainOverlay({super.key, required this.child, this.opacity = 0.035});
}
```

Application pattern for BoxDecoration (inline, no GrainOverlay widget needed):
```dart
BoxDecoration(
  color: baseColor,
  borderRadius: BorderRadius.circular(radius),
  boxShadow: shadows,
  image: const DecorationImage(
    image: AssetImage('assets/textures/grain.png'),
    repeat: ImageRepeat.repeat,
    opacity: 0.035,  // 3.5% for hero cards; 0.02 for headers
    fit: BoxFit.none,
    alignment: Alignment.topLeft,
  ),
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply grain texture to all hero cards</name>
  <files>
    lib/features/home/widgets/balance_hero_card.dart
    lib/features/ledger/widgets/ledger_hero_card.dart
    lib/features/events/screens/event_expense_hero.dart
    lib/features/events/widgets/event_spending_hero.dart
    lib/features/groups/widgets/group_balance_hero.dart
    lib/features/gear/widgets/gear_hero_card.dart
    lib/features/vault/widgets/vault_hero_card.dart
    lib/features/logistics/widgets/logistics_hero_card.dart
    lib/features/memories/widgets/memories_hero_card.dart
    lib/features/activity/widgets/activity_hero_card.dart
  </files>
  <action>
Per D-09, D-10: Apply paper grain noise overlay at ~3-5% opacity to all hero/summary cards. Content list cards stay clean/flat.

For each hero card file, read the file and find the primary `Container` or `DecoratedBox` that wraps the card content (the one with `BoxDecoration` containing `color`, `borderRadius`, and typically `boxShadow`). Add a `DecorationImage` to the existing `BoxDecoration`:

```dart
image: const DecorationImage(
  image: AssetImage('assets/textures/grain.png'),
  repeat: ImageRepeat.repeat,
  opacity: 0.035,
  fit: BoxFit.none,
  alignment: Alignment.topLeft,
),
```

**Hero cards to modify (all at 3.5% opacity):**

1. **BalanceHeroCard** (`balance_hero_card.dart`) — the Container at line ~60 with `BoxDecoration(color: AppColors.surface, ...)`. Add `image:` to that BoxDecoration. Also apply to the error card at line ~112.

2. **LedgerHeroCard** (`ledger_hero_card.dart`) — the Container at line ~51 with `BoxDecoration(color: AppColors.surface, ...)`.

3. **EventExpenseHero** (`event_expense_hero.dart`) — read file, find the main Container BoxDecoration.

4. **EventSpendingHero** (`event_spending_hero.dart`) — read file, find the main Container BoxDecoration.

5. **GroupBalanceHero** (`group_balance_hero.dart`) — read file, find the main Container BoxDecoration.

6. **GearHeroCard** (`gear_hero_card.dart`) — read file, find the main Container BoxDecoration.

7. **VaultHeroCard** (`vault_hero_card.dart`) — read file, find the main Container BoxDecoration.

8. **LogisticsHeroCard** (`logistics_hero_card.dart`) — read file, find the main Container BoxDecoration.

9. **MemoriesHeroCard** (`memories_hero_card.dart`) — read file, find the main Container BoxDecoration.

10. **ActivityHeroCard** (`activity_hero_card.dart`) — read file, find the main Container BoxDecoration.

**Important constraints:**
- Only modify hero/summary cards — NOT content cards like ExpenseCard, SettlementRow, GearItem rows, etc.
- Use `const DecorationImage(...)` where possible (it is const-constructable with AssetImage)
- If a BoxDecoration already has an `image:` parameter, the grain replaces it (no hero card currently uses DecorationImage)
- If a hero card uses a gradient (like GroupBalanceHero with dark gradient), the grain should still work — DecorationImage renders ON TOP of the gradient, which is the desired effect (grain over gradient = textured surface)
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla && grep -rl "grain.png" lib/features/ | wc -l | xargs echo "Files with grain:" && flutter analyze lib/features/ 2>&1 | tail -3</automated>
  </verify>
  <done>All 10 hero cards render with paper grain texture at 3.5% opacity. Content list cards remain flat. Zero analyze errors.</done>
</task>

<task type="auto">
  <name>Task 2: Apply grain texture to ModuleHeader</name>
  <files>
    lib/shared/widgets/module_header.dart
  </files>
  <action>
Per D-11: ModuleHeaders get grain at ~2% opacity. The dark gradient background gains a subtle canvas/leather texture.

Read `module_header.dart`. Find the `BoxDecoration` that applies the dark gradient (`darkHeaderGradient` or `headerGradient`). Add a `DecorationImage` with lower opacity (0.02):

```dart
image: const DecorationImage(
  image: AssetImage('assets/textures/grain.png'),
  repeat: ImageRepeat.repeat,
  opacity: 0.02,
  fit: BoxFit.none,
  alignment: Alignment.topLeft,
),
```

**Note on dark backgrounds:** At 2% opacity, white noise dots on a dark gray-900 background will read as very subtle canvas texture — enough to add depth without competing with white title text. This is intentional per D-11.

If the header uses a `LinearGradient` via `gradient:` in BoxDecoration, the `image:` parameter coexists with `gradient:` in BoxDecoration (Flutter renders image on top of gradient). This is exactly what we want — grain overlaying the gradient.

Per D-12: No frosted glass, no blur effects. The grain is the only texture treatment.
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla && grep "grain.png" lib/shared/widgets/module_header.dart && flutter analyze lib/shared/widgets/module_header.dart 2>&1 | tail -3</automated>
  </verify>
  <done>ModuleHeader renders grain texture at 2% opacity over the dark gradient background. Zero analyze errors.</done>
</task>

</tasks>

<verification>
flutter test test/features/ test/shared/ 2>&1 | tail -20
flutter analyze 2>&1 | tail -5
</verification>

<success_criteria>
1. `grep -rl "grain.png" lib/features/ lib/shared/` returns at least 11 files (10 hero cards + ModuleHeader)
2. ModuleHeader grain opacity is 0.02 (not 0.035)
3. Hero card grain opacity is 0.035
4. No content list cards (ExpenseCard, SettlementRow, etc.) contain grain references
5. `flutter analyze` passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/22-polish-pass-token-cleanup/22-04-SUMMARY.md`
</output>
