---
phase: 16-stitch-workflow-design-reference
plan: 02
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - .planning/design/home-screen-spec.md
  - .planning/design/group-detail-spec.md
  - .planning/design/event-hub-spec.md
  - CLAUDE.md
autonomous: false
requirements: [FOUND-03]

must_haves:
  truths:
    - "Three annotated visual specification documents exist in .planning/design/, each containing token mapping tables, component hierarchy, spacing specs, and interaction notes for all 4 screen states"
    - "CLAUDE.md contains a Stitch-to-Flutter workflow section documenting the oracle-only role, prompt preparation, post-generation checklist path, spec annotation process, and token replacement rule"
    - "Every color reference in spec documents maps to an existing AppColors/AppColorTokens constant — no unresolved colors remain"
    - "The three spec documents serve as contracts that Phases 18-22 can implement against without further design clarification"
  artifacts:
    - path: ".planning/design/home-screen-spec.md"
      provides: "Annotated visual spec for Home screen with token mappings, component hierarchy, interaction notes"
      contains: "Token Mapping"
    - path: ".planning/design/group-detail-spec.md"
      provides: "Annotated visual spec for Group Detail screen with token mappings, component hierarchy, interaction notes"
      contains: "Token Mapping"
    - path: ".planning/design/event-hub-spec.md"
      provides: "Annotated visual spec for Event Hub screen with token mappings, component hierarchy, interaction notes"
      contains: "Token Mapping"
    - path: "CLAUDE.md"
      provides: "Stitch-to-Flutter workflow documentation section"
      contains: "Stitch"
  key_links:
    - from: ".planning/design/home-screen-spec.md"
      to: "lib/core/theme/tokens/color_tokens.dart"
      via: "token mapping table references AppColors constants"
      pattern: "AppColors\\."
    - from: ".planning/design/home-screen-spec.md"
      to: "lib/shared/widgets/"
      via: "component hierarchy references shared widgets"
      pattern: "ModuleHeader|EmptyStateView|SkeletonLoader"
    - from: "CLAUDE.md"
      to: ".planning/phases/16-stitch-workflow-design-reference/post-generation-checklist.md"
      via: "workflow section references checklist file path"
      pattern: "post-generation-checklist"
---

<objective>
After the user generates Stitch mockups, create three annotated visual specification documents and update CLAUDE.md with the Stitch-to-Flutter workflow.

Purpose: Transforms Stitch-generated mockup images into structured, text-first design specifications that serve as implementation contracts for Phases 18-22. Each spec maps every visual area to AppColors tokens, identifies component hierarchy using shared widgets, specifies spacing at every level, and documents interactions and transitions. The CLAUDE.md workflow section ensures all future phases follow the same Stitch-to-Flutter pipeline.

Output: 3 spec files in `.planning/design/` + CLAUDE.md updated with workflow section
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-stitch-workflow-design-reference/16-CONTEXT.md
@.planning/phases/16-stitch-workflow-design-reference/16-RESEARCH.md

# Plan 01 output — prompts and checklist created in Plan 01
@.planning/phases/16-stitch-workflow-design-reference/16-01-SUMMARY.md
@.planning/phases/16-stitch-workflow-design-reference/post-generation-checklist.md
@.planning/phases/16-stitch-workflow-design-reference/prompts/home-screen-prompt.md
@.planning/phases/16-stitch-workflow-design-reference/prompts/group-detail-prompt.md
@.planning/phases/16-stitch-workflow-design-reference/prompts/event-hub-prompt.md

# Token source files — spec token mappings must reference these
@lib/core/theme/tokens/color_tokens.dart
@lib/core/theme/tokens/spacing_tokens.dart
@lib/core/theme/app_theme.dart

# Current screen implementations — spec describes target state
@lib/features/home/screens/home_screen.dart
@lib/features/groups/screens/group_detail_screen.dart
@lib/features/events/screens/event_command_center.dart

# Shared widgets — spec component hierarchy references these
@lib/shared/widgets/module_header.dart
@lib/shared/widgets/empty_state_view.dart
@lib/shared/widgets/smart_module_card.dart
@lib/shared/widgets/skeleton_loader.dart

# CLAUDE.md — will be updated with workflow section
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: User runs Stitch to generate mockups for all 3 screens</name>
  <what-built>Plan 01 created three ready-to-paste Stitch prompt files:
    - `.planning/phases/16-stitch-workflow-design-reference/prompts/home-screen-prompt.md`
    - `.planning/phases/16-stitch-workflow-design-reference/prompts/group-detail-prompt.md`
    - `.planning/phases/16-stitch-workflow-design-reference/prompts/event-hub-prompt.md`
  </what-built>
  <how-to-verify>
    1. Open Google Stitch in your browser
    2. For each of the 3 prompt files:
       a. Copy the full content of the prompt .md file
       b. Paste into Stitch as the design input
       c. Generate the mockup
       d. Review the output — does it roughly match the described layout and palette?
       e. If satisfactory, save/screenshot the output (store externally per D-18 — not committed to repo)
       f. If not satisfactory, iterate once with refinements (per D-14: 1-2 rounds)
    3. When all 3 screens have acceptable Stitch output, resume this task
    4. Share the Stitch output with Claude — either as:
       - Screenshots/images (Claude can view them)
       - Text descriptions of what Stitch produced
       - URLs to externally stored images
  </how-to-verify>
  <resume-signal>Share Stitch output (images, URLs, or descriptions) for all 3 screens, then type "continue" to proceed with spec annotation</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Create three annotated visual specification documents</name>
  <files>
    .planning/design/home-screen-spec.md
    .planning/design/group-detail-spec.md
    .planning/design/event-hub-spec.md
  </files>
  <read_first>
    .planning/phases/16-stitch-workflow-design-reference/16-CONTEXT.md
    .planning/phases/16-stitch-workflow-design-reference/16-RESEARCH.md
    .planning/phases/16-stitch-workflow-design-reference/post-generation-checklist.md
    .planning/phases/16-stitch-workflow-design-reference/prompts/home-screen-prompt.md
    .planning/phases/16-stitch-workflow-design-reference/prompts/group-detail-prompt.md
    .planning/phases/16-stitch-workflow-design-reference/prompts/event-hub-prompt.md
    lib/core/theme/tokens/color_tokens.dart
    lib/core/theme/tokens/spacing_tokens.dart
    lib/core/theme/app_theme.dart
    lib/shared/widgets/module_header.dart
    lib/shared/widgets/empty_state_view.dart
    lib/shared/widgets/smart_module_card.dart
    lib/shared/widgets/skeleton_loader.dart
    lib/features/home/screens/home_screen.dart
    lib/features/groups/screens/group_detail_screen.dart
    lib/features/events/screens/event_command_center.dart
  </read_first>
  <action>
    Create the `.planning/design/` directory. Then create three annotated visual specification documents, one per screen.

    ALL THREE specs MUST follow the EXACT SAME template structure (per Pitfall 3 in RESEARCH.md — format must not diverge). The template is defined in RESEARCH.md "Pattern 2: Annotated Design Spec Structure":

    ```
    # [Screen Name] — Visual Specification

    **Phase:** 16
    **Status:** Stitch-reviewed
    **Target:** ~390px width, single breakpoint
    **Stitch Reference:** [External URL or description provided by user in Task 1]

    ## Screen States

    ### State 1: Loaded (Primary)
    [Detailed natural-language layout description informed by Stitch output]

    #### Token Mapping
    | Visual Area | Token Reference | Hex | Notes |
    |-------------|----------------|-----|-------|
    | [every distinct visual area] | AppColors.[name] | #hex | [WCAG ratio if text] |

    #### Component Hierarchy
    [Flutter widget tree structure: Scaffold > CustomScrollView > SliverList > ...]
    [Per D-23: structural layout hints — describe Stack, Column, Row, Grid alongside visual]
    [Per D-22: explicitly name shared widgets — ModuleHeader, EmptyStateView, etc.]

    #### Spacing Spec
    | Location | Token | Value |
    |----------|-------|-------|
    | [every spacing decision] | space16 | 16dp |

    #### Interaction Notes
    | Trigger | Action | Transition |
    |---------|--------|-----------|
    | [per D-21] | [navigation/state change] | [AppPageRoute/AppBottomSheetRoute/fade] |

    ### State 2: Empty State
    [Same 4 subsections: description, Token Mapping, Component Hierarchy, Spacing, Interactions]

    ### State 3: Loading/Skeleton
    [Same 4 subsections]

    ### State 4: Error State
    [Same 4 subsections]

    ## Stitch Image References
    [External URLs or descriptions from user — per D-18, NOT committed to repo]

    ## Token Gaps Identified
    | Gap | Structural/Cosmetic | Recommended Action |
    |-----|---------------------|-------------------|
    | [any token not in system] | [structural = needs now / cosmetic = defer] | [add to tokens / defer to Phase NN] |
    ```

    SCREEN-SPECIFIC CONTENT:

    **home-screen-spec.md** (per D-06, informed by Stitch output from Task 1):
    - Loaded state layout: balance hero at top (background: AppColors.surface #FFF9F2, balance amount: AppColors.successText #047857 or AppColors.errorText #B91C1C or AppColors.textSecondary #6B5B4E depending on positive/negative/zero). Group cards list (card: AppColors.surface, border: AppColors.border #E5D5C0, title: AppColors.textPrimary, subtitle: AppColors.textSecondary). Quick-action FAB tray. Recent activity strip.
    - Component hierarchy: Scaffold (background: AppColors.background) > CustomScrollView > [SliverToBoxAdapter(BalanceHero), SliverList.builder(GroupCards), SliverToBoxAdapter(ActivityStrip)] + FloatingActionButton cluster
    - Empty state: EmptyStateView widget (already exists in lib/shared/widgets/)
    - Loading state: SkeletonLoader variants
    - Error state: OfflineBanner + EmptyStateView
    - Token gap candidates: "balanced" (zero) state may use textSecondary — document this decision

    **group-detail-spec.md** (per D-07, informed by Stitch output from Task 1):
    - Loaded state layout: ModuleHeader (headerGradientStart #2C1A0E to headerGradientEnd #3D2B1E, text: textOnPrimary #FFFFFF). Summary chips. Balance hero card. Member balance list (each row: name in textPrimary, balance in successText/errorText/textSecondary). Event timeline cards with type-specific accents (Trip=#CC6B49, Camping=#7A8C5E, Day Out=#5B7B8C, Dinner=#8B7355, Custom=#A67C5B). Invite code chip. Activity strip.
    - Component hierarchy: Scaffold > Column > [ModuleHeader, Expanded(CustomScrollView > [SliverToBoxAdapter(SummaryChips), SliverToBoxAdapter(BalanceHero), SliverToBoxAdapter(MemberBalances), SliverList(EventCards), SliverToBoxAdapter(InviteCode), SliverToBoxAdapter(ActivityStrip)])]
    - Dense dashboard constraint per D-07: all key data visible without scrolling
    - Token gap candidates: event type colors mapped to module colors per RESEARCH.md recommendation; document mapping decision

    **event-hub-spec.md** (per D-08, informed by Stitch output from Task 1):
    - Loaded state layout: ModuleHeader. Expense hero card (amount: AppColors.primary #CC6B49 large). Module card grid using SmartModuleCard pattern with CORRECT intended module accent colors:
      - Ledger: moduleLedger #CC6B49 / moduleLedgerLight #ECD5C0
      - Gear: moduleGear #7A8C5E / moduleGearLight #E0DAC4
      - Logistics: moduleLogistics #5B7B8C / moduleLogisticsLight #DBD7CA
      - Vault: moduleVault #8B7355 / moduleVaultLight #E2D6C2
      - Activity: moduleActivity #A67C5B / moduleActivityLight #E6D7C3
      - Memories: moduleMemories #9B7A5C / moduleMemoriesLight #E4D7C3
    - Component hierarchy: Scaffold > Column > [ModuleHeader, Expanded(ListView > [ExpenseHeroCard, ...SmartModuleCard x6])] + FAB
    - Per D-08: each module card shows live summary text
    - Token gap candidates: gear urgent state color (amber #F59E0B not in palette — document both options per RESEARCH.md)

    Run the post-generation checklist against each spec:
    - Check 1 (Color Token Mapping): verify every color in the spec maps to an existing token
    - Check 2 (Spacing): verify all spacing uses token names
    - Check 3 (Component Reuse): verify shared widgets referenced where applicable
    - Check 4 (Accessibility): verify all text-on-background pairs are in WCAG verified set

    CRITICAL: Reconcile Stitch output colors with Phase 15 tokens per D-24 — snap to nearest existing token, NEVER modify tokens to match Stitch. If Stitch generated a color not in the system, map it to the nearest token and note the difference in the Token Gaps table.
  </action>
  <verify>
    <automated>
      test -d .planning/design && \
      test -f .planning/design/home-screen-spec.md && \
      test -f .planning/design/group-detail-spec.md && \
      test -f .planning/design/event-hub-spec.md && \
      grep -q "Token Mapping" .planning/design/home-screen-spec.md && \
      grep -q "Component Hierarchy" .planning/design/home-screen-spec.md && \
      grep -q "Spacing Spec" .planning/design/home-screen-spec.md && \
      grep -q "Interaction Notes" .planning/design/home-screen-spec.md && \
      grep -q "Token Mapping" .planning/design/group-detail-spec.md && \
      grep -q "Component Hierarchy" .planning/design/group-detail-spec.md && \
      grep -q "Token Mapping" .planning/design/event-hub-spec.md && \
      grep -q "Component Hierarchy" .planning/design/event-hub-spec.md && \
      grep -q "AppColors\." .planning/design/home-screen-spec.md && \
      grep -q "AppColors\." .planning/design/group-detail-spec.md && \
      grep -q "AppColors\." .planning/design/event-hub-spec.md && \
      grep -q "#CC6B49" .planning/design/home-screen-spec.md && \
      grep -q "ModuleHeader" .planning/design/group-detail-spec.md && \
      grep -q "SmartModuleCard" .planning/design/event-hub-spec.md && \
      grep -q "EmptyStateView" .planning/design/home-screen-spec.md && \
      grep -q "Token Gaps" .planning/design/home-screen-spec.md && \
      grep -q "Token Gaps" .planning/design/group-detail-spec.md && \
      grep -q "Token Gaps" .planning/design/event-hub-spec.md && \
      grep -q "State 1.*Loaded\|Loaded.*Primary" .planning/design/home-screen-spec.md && \
      grep -q "State 2.*Empty\|Empty State" .planning/design/home-screen-spec.md && \
      grep -q "State 3.*Loading\|Loading.*Skeleton" .planning/design/home-screen-spec.md && \
      grep -q "State 4.*Error\|Error State" .planning/design/home-screen-spec.md && \
      echo "ALL CHECKS PASSED" || echo "CHECKS FAILED"
    </automated>
  </verify>
  <acceptance_criteria>
    - `.planning/design/` directory exists with 3 files: `home-screen-spec.md`, `group-detail-spec.md`, `event-hub-spec.md`
    - Each spec contains all 4 state sections: "State 1: Loaded", "State 2: Empty", "State 3: Loading/Skeleton", "State 4: Error"
    - Each state section contains 4 subsections: Token Mapping table, Component Hierarchy, Spacing Spec table, Interaction Notes table
    - All token references use the format "AppColors.[name] | #hex" (both name AND hex, per RESEARCH.md code example)
    - home-screen-spec.md references: EmptyStateView, SkeletonLoader, OfflineBanner, balance hero, group cards
    - group-detail-spec.md references: ModuleHeader, member balances, event timeline with type-specific colors (Trip=#CC6B49, Camping=#7A8C5E, Day Out=#5B7B8C, Dinner=#8B7355, Custom=#A67C5B), settle-up CTA
    - event-hub-spec.md references: ModuleHeader, SmartModuleCard, all 6 module accent colors with correct intended mapping (Ledger=#CC6B49 not #7A8C5E)
    - Each spec has a "Token Gaps Identified" section (may be empty if no gaps)
    - Each spec has "Stitch Image References" section (populated from Task 1 user input)
    - All three specs follow identical template structure (no format divergence per Pitfall 3)
  </acceptance_criteria>
  <done>Three annotated visual specifications exist in .planning/design/, each with complete token mappings, component hierarchy, spacing specs, and interaction notes for all 4 states. Post-generation checklist has been applied to each. All colors resolve to existing Phase 15 tokens.</done>
</task>

<task type="auto">
  <name>Task 3: Update CLAUDE.md with Stitch-to-Flutter workflow section</name>
  <files>
    CLAUDE.md
  </files>
  <read_first>
    CLAUDE.md
    .planning/phases/16-stitch-workflow-design-reference/16-CONTEXT.md
    .planning/phases/16-stitch-workflow-design-reference/16-RESEARCH.md
    .planning/phases/16-stitch-workflow-design-reference/post-generation-checklist.md
  </read_first>
  <action>
    Add a new `## Stitch-to-Flutter Workflow` section to CLAUDE.md. Place it after the `## Architecture` section and before the `## Conventions` section.

    The section must contain (per D-09 through D-15, D-24, and RESEARCH.md "Pattern 4"):

    ```markdown
    ## Stitch-to-Flutter Workflow

    Google Stitch is a **visual oracle only** — used purely for design exploration. Never commit Stitch-generated code. All Flutter implementation is written from scratch using AppColors tokens.

    ### Workflow Steps

    1. **Prepare Stitch input prompt** — Create a prompt .md file in `.planning/phases/{phase}/prompts/` containing:
       - Full earthy palette (all hex values from `AppColorTokens.earthyLight` in `lib/core/theme/tokens/color_tokens.dart`)
       - Spacing scale (4dp through 32dp from `AppSpacingTokens.standard`)
       - Typography (Plus Jakarta Sans, weight hierarchy: 800/700/600/400)
       - Component inventory (shared widgets from `lib/shared/widgets/`)
       - Screen-specific layout description with all states (loaded, empty, loading, error)
       - Constraints (~390px width, generic placeholder data, dense information density)

    2. **Run Stitch** — Copy prompt content into Google Stitch. Generate mockup. Review output (1-2 rounds per screen). Store output images externally (not in repo).

    3. **Apply post-generation checklist** — Run the 4 mandatory checks from `.planning/phases/16-stitch-workflow-design-reference/post-generation-checklist.md`:
       - Color token mapping — every color maps to AppColors/AppColorTokens
       - Spacing consistency — 4dp grid, token names only
       - Component reuse — shared widgets where applicable
       - Accessibility — WCAG AA text pairs, 48dp touch targets

    4. **Create annotated design spec** — Write a spec doc in `.planning/design/{screen}-spec.md` with:
       - Token mapping table (every visual area to AppColors constant + hex)
       - Component hierarchy (Flutter widget tree structure)
       - Spacing spec (every spacing decision as token name)
       - Interaction notes (tap/swipe actions, transition types)
       - All 4 screen states documented
       - Token gaps identified

    5. **Reconcile palette** — Token replacement rule: snap every Stitch-generated color to the nearest existing AppColors token. Stitch adapts to the token system, never the reverse. Update `app_tokens.dart` only for structural gaps.

    ### Reference Files

    - **Palette source of truth:** `lib/core/theme/tokens/color_tokens.dart` (AppColorTokens.earthyLight)
    - **Spacing source of truth:** `lib/core/theme/tokens/spacing_tokens.dart` (AppSpacingTokens.standard)
    - **Post-generation checklist:** `.planning/phases/16-stitch-workflow-design-reference/post-generation-checklist.md`
    - **Design specs:** `.planning/design/{screen}-spec.md`
    - **Prompt templates:** `.planning/phases/{phase}/prompts/{screen}-prompt.md`

    ### Key Rules

    - Never commit Stitch-generated Flutter code — implement from scratch using design spec as visual target
    - All hex values in specs must match AppColorTokens.earthyLight exactly
    - textMuted (#A89888) is decorative only — never use for functional text (fails WCAG AA at 2.30:1)
    - Every text-on-background pair must be in the Phase 15 WCAG-verified set
    - Module accent colors use Phase 15 token mapping (Ledger=#CC6B49, Gear=#7A8C5E, Logistics=#5B7B8C, Vault=#8B7355, Activity=#A67C5B, Memories=#9B7A5C)
    ```

    IMPORTANT: Read CLAUDE.md first to find the exact insertion point. The section goes after `## Architecture` and before `## Conventions`. Do NOT modify any existing content — only add the new section.
  </action>
  <verify>
    <automated>
      grep -q "## Stitch-to-Flutter Workflow" CLAUDE.md && \
      grep -q "visual oracle only" CLAUDE.md && \
      grep -q "post-generation-checklist.md" CLAUDE.md && \
      grep -q "AppColorTokens.earthyLight" CLAUDE.md && \
      grep -q "textMuted.*decorative\|decorative.*textMuted" CLAUDE.md && \
      grep -q "Never commit Stitch-generated" CLAUDE.md && \
      grep -q ".planning/design/" CLAUDE.md && \
      echo "ALL CHECKS PASSED" || echo "CHECKS FAILED"
    </automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md contains `## Stitch-to-Flutter Workflow` section
    - Section is positioned after `## Architecture` and before `## Conventions`
    - Section contains all 5 workflow steps: prepare prompt, run Stitch, apply checklist, create spec, reconcile palette
    - Section contains "Reference Files" subsection listing: color_tokens.dart, spacing_tokens.dart, post-generation-checklist.md, .planning/design/, prompts/
    - Section contains "Key Rules" subsection with: never commit Stitch code, hex values must match, textMuted decorative only, WCAG verified pairs, module accent colors
    - Section states "visual oracle only" (per D-09)
    - Section references the post-generation checklist file path
    - No existing CLAUDE.md content was modified or removed
  </acceptance_criteria>
  <done>CLAUDE.md contains a complete Stitch-to-Flutter workflow section documenting the 5-step process, reference files, and key rules. The workflow is reusable by Phases 20-22.</done>
</task>

</tasks>

<verification>
- All 3 design spec files exist in `.planning/design/` with consistent template structure
- Each spec has token mapping tables, component hierarchy, spacing specs, interaction notes for all 4 states
- CLAUDE.md has Stitch-to-Flutter workflow section between Architecture and Conventions
- Post-generation checklist was applied to all 3 specs (all colors resolved to tokens)
- All hex values match Phase 15 AppColorTokens.earthyLight
- Event Hub spec uses correct intended module colors (Ledger=#CC6B49, not #7A8C5E)
</verification>

<success_criteria>
- Three visual specifications in .planning/design/ serve as implementation contracts for Phases 18-22
- CLAUDE.md workflow section enables future phases to follow the same Stitch-to-Flutter pipeline
- All token mappings are complete — no unresolved colors in any spec
- Stitch output has been reconciled with Phase 15 tokens (snap to nearest, never reverse)
</success_criteria>

<output>
After completion, create `.planning/phases/16-stitch-workflow-design-reference/16-02-SUMMARY.md`
</output>
