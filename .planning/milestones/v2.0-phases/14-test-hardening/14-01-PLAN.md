---
phase: 14-test-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/keys/shared_keys.dart
  - lib/features/events/keys/event_keys.dart
  - lib/features/groups/keys/group_keys.dart
  - lib/features/home/keys/home_keys.dart
  - lib/features/ledger/keys/ledger_keys.dart
  - lib/features/gear/keys/gear_keys.dart
  - lib/features/logistics/keys/logistics_keys.dart
  - lib/features/memories/keys/memories_keys.dart
  - lib/features/onboarding/keys/onboarding_keys.dart
  - lib/features/settings/keys/settings_keys.dart
  - lib/features/vault/keys/vault_keys.dart
  - lib/features/activity/keys/activity_keys.dart
  - lib/features/events/screens/event_command_center.dart
  - lib/features/events/screens/create_event_screen.dart
  - lib/features/events/screens/event_type_picker_screen.dart
  - lib/features/events/screens/event_expense_hero.dart
  - lib/features/events/widgets/event_module_list.dart
  - lib/features/events/widgets/event_card.dart
  - lib/features/groups/screens/group_detail_screen.dart
  - lib/features/groups/screens/create_group_screen.dart
  - lib/features/groups/screens/join_group_screen.dart
  - lib/features/groups/screens/group_settle_up_screen.dart
  - lib/features/groups/screens/group_settings_screen.dart
  - lib/features/groups/screens/group_activity_screen.dart
  - lib/features/groups/widgets/group_balance_hero.dart
  - lib/features/groups/widgets/invite_code_display.dart
  - lib/features/groups/widgets/group_card.dart
  - lib/features/groups/widgets/group_member_balance_card.dart
  - lib/features/groups/widgets/group_settlement_tile.dart
  - lib/features/home/screens/home_screen.dart
  - lib/features/ledger/screens/ledger_screen.dart
  - lib/features/ledger/screens/add_expense_screen.dart
  - lib/features/ledger/screens/edit_expense_sheet.dart
  - lib/features/ledger/screens/settle_up_screen.dart
  - lib/features/gear/screens/gear_screen.dart
  - lib/features/logistics/screens/logistics_screen.dart
  - lib/features/logistics/widgets/subgroup_card.dart
  - lib/features/logistics/widgets/unassigned_pool.dart
  - lib/features/memories/screens/memories_screen.dart
  - lib/features/onboarding/screens/onboarding_screen.dart
  - lib/features/settings/screens/settings_screen.dart
  - lib/features/vault/screens/vault_screen.dart
  - lib/features/activity/screens/activity_feed_screen.dart
  - lib/shared/widgets/smart_module_card.dart
  - lib/shared/widgets/app_tab_bar.dart
  - lib/shared/widgets/loading_button.dart
  - lib/shared/widgets/module_header.dart
  - lib/shared/widgets/offline_banner.dart
  - lib/shared/widgets/empty_state_view.dart
  - lib/shared/widgets/skeleton_loader.dart
  - test/unit/widget_coverage_test.dart
  - test/features/groups/create_join_group_test.dart
  - test/features/events/event_command_center_test.dart
  - test/features/groups/group_settle_up_screen_test.dart
autonomous: true
requirements:
  - FOUND-05

must_haves:
  truths:
    - "12 key class files exist with abstract final class declarations and screen-level keys for all 22 screens"
    - "All structural find.text() calls in the 4 heaviest test files (108 calls total) are classified and converted to find.byKey() where structural"
    - "Full test suite passes green after all key additions and test migrations"
  artifacts:
    - path: "lib/core/keys/shared_keys.dart"
      provides: "SharedKeys class with keys for shared widgets (ModuleHeader, AppTabBar, OfflineBanner, EmptyStateView, InviteCodeDisplay, GroupBalanceHero, LoadingButton, SmartModuleCard)"
      contains: "abstract final class SharedKeys"
    - path: "lib/features/events/keys/event_keys.dart"
      provides: "EventKeys class with screen keys and module card keys"
      contains: "abstract final class EventKeys"
    - path: "lib/features/groups/keys/group_keys.dart"
      provides: "GroupKeys class with screen keys and action button keys"
      contains: "abstract final class GroupKeys"
    - path: "lib/features/home/keys/home_keys.dart"
      provides: "HomeKeys class with screen key and home-specific keys"
      contains: "abstract final class HomeKeys"
  key_links:
    - from: "test/unit/widget_coverage_test.dart"
      to: "lib/core/keys/shared_keys.dart"
      via: "import and find.byKey(SharedKeys.*)"
      pattern: "find\\.byKey\\(SharedKeys\\."
    - from: "test/features/events/event_command_center_test.dart"
      to: "lib/features/events/keys/event_keys.dart"
      via: "import and find.byKey(EventKeys.*)"
      pattern: "find\\.byKey\\(EventKeys\\."
    - from: "test/features/groups/create_join_group_test.dart"
      to: "lib/features/groups/keys/group_keys.dart"
      via: "import and find.byKey(GroupKeys.*)"
      pattern: "find\\.byKey\\(GroupKeys\\."
---

<objective>
Create the semantic Key infrastructure for all features and migrate the 4 heaviest test files from structural find.text()/find.byType() to find.byKey().

Purpose: Establishes the key class system (per D-01 through D-04) and delivers the highest-value conversions first — the 4 files with the most find.text() calls account for 42% of all calls in the test suite.

Output: 12 key class files, keys applied to ~22 screen widgets and ~15 feature widgets, 4 test files migrated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-test-hardening/14-CONTEXT.md
@.planning/phases/14-test-hardening/14-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create key class files and apply keys to all screen/widget files</name>
  <files>
    lib/core/keys/shared_keys.dart,
    lib/features/events/keys/event_keys.dart,
    lib/features/groups/keys/group_keys.dart,
    lib/features/home/keys/home_keys.dart,
    lib/features/ledger/keys/ledger_keys.dart,
    lib/features/gear/keys/gear_keys.dart,
    lib/features/logistics/keys/logistics_keys.dart,
    lib/features/memories/keys/memories_keys.dart,
    lib/features/onboarding/keys/onboarding_keys.dart,
    lib/features/settings/keys/settings_keys.dart,
    lib/features/vault/keys/vault_keys.dart,
    lib/features/activity/keys/activity_keys.dart,
    lib/features/events/screens/event_command_center.dart,
    lib/features/events/screens/create_event_screen.dart,
    lib/features/events/screens/event_type_picker_screen.dart,
    lib/features/events/screens/event_expense_hero.dart,
    lib/features/events/widgets/event_module_list.dart,
    lib/features/events/widgets/event_card.dart,
    lib/features/groups/screens/group_detail_screen.dart,
    lib/features/groups/screens/create_group_screen.dart,
    lib/features/groups/screens/join_group_screen.dart,
    lib/features/groups/screens/group_settle_up_screen.dart,
    lib/features/groups/screens/group_settings_screen.dart,
    lib/features/groups/screens/group_activity_screen.dart,
    lib/features/groups/widgets/group_balance_hero.dart,
    lib/features/groups/widgets/invite_code_display.dart,
    lib/features/groups/widgets/group_card.dart,
    lib/features/groups/widgets/group_member_balance_card.dart,
    lib/features/groups/widgets/group_settlement_tile.dart,
    lib/features/home/screens/home_screen.dart,
    lib/features/ledger/screens/ledger_screen.dart,
    lib/features/ledger/screens/add_expense_screen.dart,
    lib/features/ledger/screens/edit_expense_sheet.dart,
    lib/features/ledger/screens/settle_up_screen.dart,
    lib/features/gear/screens/gear_screen.dart,
    lib/features/logistics/screens/logistics_screen.dart,
    lib/features/logistics/widgets/subgroup_card.dart,
    lib/features/logistics/widgets/unassigned_pool.dart,
    lib/features/memories/screens/memories_screen.dart,
    lib/features/onboarding/screens/onboarding_screen.dart,
    lib/features/settings/screens/settings_screen.dart,
    lib/features/vault/screens/vault_screen.dart,
    lib/features/activity/screens/activity_feed_screen.dart,
    lib/shared/widgets/smart_module_card.dart,
    lib/shared/widgets/app_tab_bar.dart,
    lib/shared/widgets/loading_button.dart,
    lib/shared/widgets/module_header.dart,
    lib/shared/widgets/offline_banner.dart,
    lib/shared/widgets/empty_state_view.dart,
    lib/shared/widgets/skeleton_loader.dart
  </files>
  <read_first>
    test/unit/widget_coverage_test.dart,
    test/features/groups/create_join_group_test.dart,
    test/features/events/event_command_center_test.dart,
    test/features/groups/group_settle_up_screen_test.dart,
    lib/features/events/screens/event_command_center.dart,
    lib/features/events/widgets/event_module_list.dart,
    lib/features/groups/screens/create_group_screen.dart,
    lib/features/groups/screens/join_group_screen.dart,
    lib/features/groups/screens/group_settle_up_screen.dart,
    lib/features/groups/widgets/group_balance_hero.dart,
    lib/features/groups/widgets/invite_code_display.dart,
    lib/shared/widgets/smart_module_card.dart,
    lib/shared/widgets/app_tab_bar.dart,
    lib/shared/widgets/module_header.dart,
    lib/shared/widgets/offline_banner.dart,
    lib/shared/widgets/empty_state_view.dart,
    lib/shared/widgets/loading_button.dart,
    lib/shared/widgets/skeleton_loader.dart,
    lib/features/home/screens/home_screen.dart,
    lib/features/ledger/screens/ledger_screen.dart,
    lib/features/gear/screens/gear_screen.dart,
    lib/features/logistics/screens/logistics_screen.dart,
    lib/features/memories/screens/memories_screen.dart,
    lib/features/onboarding/screens/onboarding_screen.dart,
    lib/features/settings/screens/settings_screen.dart,
    lib/features/vault/screens/vault_screen.dart,
    lib/features/activity/screens/activity_feed_screen.dart,
    .planning/phases/14-test-hardening/14-RESEARCH.md
  </read_first>
  <action>
**Phase A: Read all 4 target test files thoroughly.** Classify every `find.text()` and structural `find.byType()` call using the taxonomy from 14-RESEARCH.md:

- STRUCTURAL (convert to find.byKey): module card presence/absence, screen navigation confirmation (AppBar titles like 'New Group', 'Join a Group'), section headers ('Members and Balances', 'Invite Code', 'Events'), tap targets (`tester.tap(find.text('Create Group'))`), empty state labels ('No groups yet'), screen type checks (`find.byType(LedgerScreen)`)
- CONTENT (keep find.text): formatted amounts ('25.500 OMR'), user fixture data ('Desert Crew', 'Alice'), validation errors ("Group name can't be empty."), computed summaries ('2 expenses')

Per D-05, D-06, D-07.

**Phase B: Create all 12 key class files.** Each file follows this exact pattern (per D-01, D-02, D-03):

```dart
import 'package:flutter/material.dart';

abstract final class {Feature}Keys {
  // Screen keys (D-10: unconditional)
  static const screen = Key('{feature}_{screen_name}_screen');

  // Structural keys (driven by test references in Phase A)
  static const someWidget = Key('{feature}_{widget}_{role}');

  // Parameterized keys (D-04: for list items)
  static Key itemCard(String id) => Key('{feature}_{widget}_$id');
}
```

Create these 12 files in their `keys/` subdirectories (create the `keys/` directory under each feature if it does not exist):

1. `lib/core/keys/shared_keys.dart` — `SharedKeys` class. Keys for: `moduleHeaderBackButton` (`'shared_module_header_back_button'`), `offlineBanner` (`'shared_offline_banner'`), `emptyStateView` (`'shared_empty_state_view'`), `emptyStateCtaButton` (`'shared_empty_state_cta_button'`), `groupBalanceHero` (`'shared_group_balance_hero'`), `groupBalanceSettleUpButton` (`'shared_group_balance_settle_up_button'`), `inviteCodeDisplay` (`'shared_invite_code_display'`), `inviteCodeCopyButton` (`'shared_invite_code_copy_button'`), `inviteCodeShareButton` (`'shared_invite_code_share_button'`). Parameterized: `appTabBarTab(String label) => Key('shared_tab_bar_tab_$label')`, `loadingButton(String label) => Key('shared_loading_button_$label')`.

2. `lib/features/events/keys/event_keys.dart` — `EventKeys` class. Screen keys: `screen` (`'event_command_center_screen'`), `createEventScreen` (`'create_event_screen'`), `eventTypePickerScreen` (`'event_type_picker_screen'`). Module card keys: `ledgerCard` (`'event_ledger_card'`), `gearCard` (`'event_gear_card'`), `logisticsCard` (`'event_logistics_card'`), `vaultCard` (`'event_vault_card'`), `memoriesCard` (`'event_memories_card'`), `activityCard` (`'event_activity_card'`). Actions: `addExpenseFab` (`'event_add_expense_fab'`). Sections: `spendingHero` (`'event_spending_hero'`), `moduleList` (`'event_module_list'`). Parameterized: `eventCard(String eventId) => Key('event_card_$eventId')`.

3. `lib/features/groups/keys/group_keys.dart` — `GroupKeys` class. Screen keys: `detailScreen` (`'group_detail_screen'`), `createScreen` (`'group_create_screen'`), `joinScreen` (`'group_join_screen'`), `settleUpScreen` (`'group_settle_up_screen'`), `settingsScreen` (`'group_settings_screen'`), `activityScreen` (`'group_activity_screen'`). Section keys: `membersAndBalancesSection` (`'group_members_and_balances_section'`), `inviteCodeSection` (`'group_invite_code_section'`), `eventsSection` (`'group_events_section'`). Action keys: `createGroupButton` (`'group_create_button'`), `joinGroupButton` (`'group_join_button'`), `settleUpButton` (`'group_settle_up_button'`), `recordSettlementButton` (`'group_record_settlement_button'`), `markAsPaidButton` (`'group_mark_as_paid_button'`), `notNowButton` (`'group_not_now_button'`), `deleteGroupButton` (`'group_delete_button'`), `leaveGroupButton` (`'group_leave_button'`). Input keys: `groupNameInput` (`'group_name_input'`), `joinCodeInput` (`'group_join_code_input'`), `deviceNameInput` (`'group_device_name_input'`). Labels: `noGroupsEmpty` (`'group_no_groups_empty'`), `noEventsEmpty` (`'group_no_events_empty'`), `settingsTitle` (`'group_settings_title'`). Parameterized: `groupCard(String groupId) => Key('group_card_$groupId')`, `memberBalanceCard(String memberId) => Key('group_member_balance_card_$memberId')`, `settlementTile(String id) => Key('group_settlement_tile_$id')`.

4. `lib/features/home/keys/home_keys.dart` — `HomeKeys` class. Screen key: `screen` (`'home_screen'`). Actions: `createGroupFab` (`'home_create_group_fab'`), `createGroupOption` (`'home_create_group_option'`), `joinGroupOption` (`'home_join_group_option'`).

5. `lib/features/ledger/keys/ledger_keys.dart` — `LedgerKeys` class. Screen keys: `screen` (`'ledger_screen'`), `addExpenseScreen` (`'ledger_add_expense_screen'`), `editExpenseSheet` (`'ledger_edit_expense_sheet'`), `settleUpScreen` (`'ledger_settle_up_screen'`). Parameterized: `expenseCard(String id) => Key('ledger_expense_card_$id')`.

6. `lib/features/gear/keys/gear_keys.dart` — `GearKeys` class. Screen key: `screen` (`'gear_screen'`). Actions: `addButton` (`'gear_add_button'`). Parameterized: `gearItem(String id) => Key('gear_item_$id')`.

7. `lib/features/logistics/keys/logistics_keys.dart` — `LogisticsKeys` class. Screen key: `screen` (`'logistics_screen'`). Actions: `createGroupButton` (`'logistics_create_group_button'`), `removeButton` (`'logistics_remove_button'`), `deleteButton` (`'logistics_delete_button'`). Sections: `unassignedPool` (`'logistics_unassigned_pool'`). Parameterized: `subgroupCard(String id) => Key('logistics_subgroup_card_$id')`.

8. `lib/features/memories/keys/memories_keys.dart` — `MemoriesKeys` class. Screen key: `screen` (`'memories_screen'`).

9. `lib/features/onboarding/keys/onboarding_keys.dart` — `OnboardingKeys` class. Screen key: `screen` (`'onboarding_screen'`).

10. `lib/features/settings/keys/settings_keys.dart` — `SettingsKeys` class. Screen key: `screen` (`'settings_screen'`).

11. `lib/features/vault/keys/vault_keys.dart` — `VaultKeys` class. Screen key: `screen` (`'vault_screen'`).

12. `lib/features/activity/keys/activity_keys.dart` — `ActivityKeys` class. Screen key: `screen` (`'activity_feed_screen'`).

**IMPORTANT:** After reading the 4 target test files, add any ADDITIONAL key constants needed for structural conversions in those test files that are not listed above. The lists above are the baseline; the executor must read each test file and add keys for every structural call found. Add keys to the feature key class that owns the widget being keyed.

**Phase C: Add `key:` parameters to widget/screen files.**

For every screen widget (22 screens): add the screen key on the root `Scaffold` (or outermost container widget) inside `build()`. Add `import '../keys/{feature}_keys.dart';` (or the appropriate relative path) at the top of the file.

Pattern:
```dart
import '../keys/event_keys.dart';

// In build():
return Scaffold(
  key: EventKeys.screen,  // <-- added
  // ... rest unchanged
);
```

For every widget referenced by a structural test assertion in the 4 target test files: add the corresponding key on the widget at the call site (per D-09 — only widgets tests actually reference). For example, in `event_module_list.dart`, each `SmartModuleCard(...)` call gets `key: EventKeys.ledgerCard` / `EventKeys.gearCard` etc.

**Anti-patterns to avoid (per 14-RESEARCH.md):**
- Do NOT put key on inner private widgets — put it on the outermost public widget
- Do NOT modify the constructor's `super.key` parameter — the key goes on the root widget inside `build()`
- Do NOT use `const Key()` with string interpolation for parameterized keys
- Do NOT add keys to widgets that no test references (except screen keys per D-10)

**Phase D: Run full test suite** to confirm adding keys breaks nothing:
```bash
flutter test
```
All 624 tests must pass. If any test fails from key addition alone (Pitfall 3 from RESEARCH.md), investigate: the key may be on the wrong widget level or causing a duplicate key conflict.

Commit all key class files and widget modifications together as one atomic commit.
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla && flutter test</automated>
  </verify>
  <acceptance_criteria>
    - lib/core/keys/shared_keys.dart exists and contains `abstract final class SharedKeys`
    - lib/features/events/keys/event_keys.dart exists and contains `abstract final class EventKeys`
    - lib/features/groups/keys/group_keys.dart exists and contains `abstract final class GroupKeys`
    - lib/features/home/keys/home_keys.dart exists and contains `abstract final class HomeKeys`
    - lib/features/ledger/keys/ledger_keys.dart exists and contains `abstract final class LedgerKeys`
    - lib/features/gear/keys/gear_keys.dart exists and contains `abstract final class GearKeys`
    - lib/features/logistics/keys/logistics_keys.dart exists and contains `abstract final class LogisticsKeys`
    - lib/features/memories/keys/memories_keys.dart exists and contains `abstract final class MemoriesKeys`
    - lib/features/onboarding/keys/onboarding_keys.dart exists and contains `abstract final class OnboardingKeys`
    - lib/features/settings/keys/settings_keys.dart exists and contains `abstract final class SettingsKeys`
    - lib/features/vault/keys/vault_keys.dart exists and contains `abstract final class VaultKeys`
    - lib/features/activity/keys/activity_keys.dart exists and contains `abstract final class ActivityKeys`
    - Every key class file contains `import 'package:flutter/material.dart';`
    - Every key class has at least one `static const screen = Key(` field
    - All key string values follow `feature_widget_role` pattern (lowercase, underscores)
    - Parameterized keys use factory methods: `static Key methodName(String id) => Key('prefix_$id');` (not const)
    - Each of the 22 screen files contains a `key:` parameter on its root Scaffold/container referencing the corresponding Keys class
    - `flutter test` exits with code 0 (all 624 tests pass)
    - `flutter analyze --no-fatal-infos` exits with code 0 (no new analysis errors)
  </acceptance_criteria>
  <done>All 12 key class files created with screen keys and structural keys needed by the 4 target test files. Keys applied to all 22 screen widgets and relevant feature widgets. Full test suite passes green.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate the 4 heaviest test files from find.text() to find.byKey()</name>
  <files>
    test/unit/widget_coverage_test.dart,
    test/features/groups/create_join_group_test.dart,
    test/features/events/event_command_center_test.dart,
    test/features/groups/group_settle_up_screen_test.dart
  </files>
  <read_first>
    test/unit/widget_coverage_test.dart,
    test/features/groups/create_join_group_test.dart,
    test/features/events/event_command_center_test.dart,
    test/features/groups/group_settle_up_screen_test.dart,
    lib/core/keys/shared_keys.dart,
    lib/features/events/keys/event_keys.dart,
    lib/features/groups/keys/group_keys.dart,
    .planning/phases/14-test-hardening/14-RESEARCH.md
  </read_first>
  <action>
Migrate each of the 4 test files by converting structural `find.text()` and structural `find.byType()` calls to `find.byKey()`. Work file-by-file. After EACH file, run `flutter test {that_file}` to confirm green.

**Classification rules (per D-05, D-06, D-07 and taxonomy in 14-RESEARCH.md):**

CONVERT to `find.byKey()`:
- `find.text('Ledger')`, `find.text('Gear')`, etc. when checking if a module card is present/absent
- `find.text('New Group')`, `find.text('Join a Group')` when confirming screen navigation
- `find.text('Members & Balances')`, `find.text('Invite Code')`, `find.text('Events')` as section markers
- `tester.tap(find.text('Create Group'))`, `tester.tap(find.text('Settle Up'))`, `tester.tap(find.text('Copy Code'))`, `tester.tap(find.text('Share'))`, `tester.tap(find.text('Record Settlement'))`, `tester.tap(find.text('Mark as Paid'))`, `tester.tap(find.text('Not Now'))` as tap targets
- `find.text('No groups yet')`, `find.text('No events yet')` as empty state presence checks
- `find.byType(EventCommandCenter)`, `find.byType(CreateGroupScreen)`, `find.byType(JoinGroupScreen)`, etc. when checking if the correct screen rendered after navigation

KEEP as `find.text()`:
- `find.text('25.500 OMR')`, `find.text('0.000 OMR')` — formatted money amounts
- `find.text('Desert Crew')`, `find.text('Beach Trip')`, `find.text('Alice')`, `find.text('ABC123')` — user fixture data
- `find.text("Group name can't be empty.")`, `find.text('Enter your name so others know who you are.')` — validation errors
- `find.textContaining('2 expenses')` — computed summaries
- `find.text('YOU OWE')`, `find.text('YOU ARE OWED')`, `find.text('All balances settled')` — financial state labels
- `find.text('GROUP BALANCES')`, `find.text('SPENDING')` — header labels that validate the actual text content

**Per-file migration instructions:**

**File 1: `test/unit/widget_coverage_test.dart` (35 find.text, 12 find.byType)**
- Add imports: `import 'package:safar/core/keys/shared_keys.dart';` and `import 'package:safar/features/groups/keys/group_keys.dart';`
- Convert structural: AppTabBar tab tap targets (e.g., `tester.tap(find.text('B'))` for tab switch → `tester.tap(find.byKey(SharedKeys.appTabBarTab('B')))`), module header back button taps, SmartModuleCard presence checks, InviteCodeDisplay copy/share button taps, GroupBalanceHero settle up button tap, OfflineBanner presence check, empty state CTA button tap
- Keep content: tab label text existence checks in `renders tab labels` test (those validate actual text content), formatted balance amounts, invite code text
- Convert structural byType: `find.byType(SmartModuleCard)` where checking card presence
- Keep byType: `find.byType(CircularProgressIndicator)`, `find.byType(SnackBar)`, `find.byType(ShimmerPlaceholder)` — genuine type checks
- Run: `flutter test test/unit/widget_coverage_test.dart`

**File 2: `test/features/groups/create_join_group_test.dart` (30 find.text, 9 find.byType)**
- Add imports: `import 'package:safar/features/groups/keys/group_keys.dart';`
- Convert structural: `find.text('New Group')` (screen title), `find.text('Group Name')` (form label), `find.text('Currency')` (form label), `tester.tap(find.text('Create Group'))` (submit button), `find.text('Join a Group')` (screen title), `find.text('Group Settings')` (screen title), `tester.tap(find.text('Delete Group'))`, `tester.tap(find.text('Leave Group'))`, `find.text('Invite Code')` (section header), `tester.tap(find.text('Copy Code'))`, `tester.tap(find.text('Share'))`
- Keep content: `find.text('OMR')` (currency value display), `find.text("Group name can't be empty.")` (validation), `find.text('Enter your name so others know who you are.')` (validation), `find.text('Desert Crew')` (fixture data), `find.text('ABC123')` (invite code value)
- Convert structural byType: `find.byType(CreateGroupScreen)`, `find.byType(JoinGroupScreen)`, `find.byType(GroupSettingsScreen)` — navigation confirmations
- Keep byType: `find.byType(CircularProgressIndicator)`, `find.byType(SnackBar)` — genuine type checks
- Run: `flutter test test/features/groups/create_join_group_test.dart`

**File 3: `test/features/events/event_command_center_test.dart` (23 find.text, 3 find.byType)**
- Add imports: `import 'package:safar/features/events/keys/event_keys.dart';`
- Convert structural: `find.text('Ledger')`, `find.text('Gear')`, `find.text('Logistics')`, `find.text('Vault')`, `find.text('Memories')`, `find.text('Activity Feed')` — module card presence/absence checks. `find.text('SPENDING')` if it's checking hero presence (but keep if testing the label text itself — use judgment per RESEARCH.md borderline table). Section headers for modules.
- Keep content: `find.text('Summer Trip')` (event name), `find.text('25.500 OMR')` (money), `find.text('Adventure Crew')` (group name), `find.text('0.000 OMR')` (zero amount)
- Convert structural byType: `find.byType(EventCommandCenter)` if used for navigation confirmation
- Run: `flutter test test/features/events/event_command_center_test.dart`

**File 4: `test/features/groups/group_settle_up_screen_test.dart` (20 find.text, 3 find.byType)**
- Add imports: `import 'package:safar/features/groups/keys/group_keys.dart';` and `import 'package:safar/core/keys/shared_keys.dart';`
- Convert structural: `tester.tap(find.text('Settle Up'))` (button tap), `tester.tap(find.text('Record Settlement'))` (button tap), `tester.tap(find.text('Mark as Paid'))` (button tap), `tester.tap(find.text('Not Now'))` (dialog action), `find.text('Members & Balances')` (section header), settle up screen title check
- Keep content: `find.text('YOU OWE')`, `find.text('YOU ARE OWED')`, `find.text('All balances settled')` (financial state labels), formatted amounts, member names
- Run: `flutter test test/features/groups/group_settle_up_screen_test.dart`

**After all 4 files are migrated, run the full test suite:**
```bash
flutter test
```

Commit each test file migration as an atomic commit: `test(14-01): migrate {test_file} from find.text to find.byKey`. Per D-12.
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla && flutter test test/unit/widget_coverage_test.dart && flutter test test/features/groups/create_join_group_test.dart && flutter test test/features/events/event_command_center_test.dart && flutter test test/features/groups/group_settle_up_screen_test.dart && flutter test</automated>
  </verify>
  <acceptance_criteria>
    - test/unit/widget_coverage_test.dart contains `import 'package:safar/core/keys/shared_keys.dart'`
    - test/unit/widget_coverage_test.dart contains at least 10 `find.byKey(SharedKeys.` calls (structural conversions)
    - test/features/groups/create_join_group_test.dart contains `import 'package:safar/features/groups/keys/group_keys.dart'`
    - test/features/groups/create_join_group_test.dart contains at least 12 `find.byKey(GroupKeys.` calls
    - test/features/events/event_command_center_test.dart contains `import 'package:safar/features/events/keys/event_keys.dart'`
    - test/features/events/event_command_center_test.dart contains at least 15 `find.byKey(EventKeys.` calls
    - test/features/groups/group_settle_up_screen_test.dart contains at least 8 `find.byKey(GroupKeys.` calls
    - No structural find.text() calls remain for module card presence, screen titles, section headers, or tap targets in these 4 files
    - Content find.text() calls for amounts, fixture data, validation messages are preserved
    - `flutter test` exits with code 0 (all tests pass)
    - Combined find.text() count across these 4 files is reduced by at least 60 calls from baseline of 108
  </acceptance_criteria>
  <done>The 4 heaviest test files (108 total find.text calls) are migrated: structural assertions use find.byKey(), content assertions remain find.text(). Full test suite green.</done>
</task>

</tasks>

<verification>
- `flutter test` — full suite passes (all 624 tests)
- `flutter analyze --no-fatal-infos` — no new analysis errors from key imports
- `grep -rn "abstract final class.*Keys" lib/` — returns 12 key class declarations
- `grep -rn "find\.byKey(" test/unit/widget_coverage_test.dart test/features/groups/create_join_group_test.dart test/features/events/event_command_center_test.dart test/features/groups/group_settle_up_screen_test.dart | wc -l` — at least 45 find.byKey() calls across the 4 files
</verification>

<success_criteria>
- 12 key class files exist with correct abstract final class pattern
- All 22 screen widgets have screen-level keys on their root Scaffold
- 4 heaviest test files (~42% of all find.text calls) migrated from structural find.text to find.byKey
- Full test suite (624 tests) passes green
- No new analysis errors
</success_criteria>

<output>
After completion, create `.planning/phases/14-test-hardening/14-01-SUMMARY.md`
</output>
