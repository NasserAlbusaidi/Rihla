---
phase: 14-test-hardening
plan: 03
type: execute
wave: 3
depends_on: ["14-02"]
files_modified:
  - test/features/home/home_screen_groups_test.dart
  - test/features/group_balance_card_test.dart
  - test/unit/page_transitions_test.dart
  - test/features/ledger/payer_currency_rewiring_test.dart
  - test/features/ledger_test.dart
  - test/unit/empty_state_view_test.dart
  - test/features/groups/group_activity_screen_test.dart
  - test/features/gear_screen_mutations_test.dart
  - test/widget_test.dart
  - test/integration/happy_path_test.dart
  - lib/features/home/keys/home_keys.dart
  - lib/features/groups/keys/group_keys.dart
  - lib/features/ledger/keys/ledger_keys.dart
  - lib/features/gear/keys/gear_keys.dart
  - lib/core/keys/shared_keys.dart
  - .github/workflows/release_android.yml
autonomous: true
requirements:
  - FOUND-05

must_haves:
  truths:
    - "All remaining structural find.text() calls across the entire test suite are converted to find.byKey()"
    - "Renaming 'Ledger' to 'Treasury' in source causes zero find.byKey() test failures — only content find.text() tests fail"
    - "CI pipeline warns when find.text() count exceeds baseline"
    - "Full test suite of 624 tests passes green with no modifications needed"
  artifacts:
    - path: ".github/workflows/release_android.yml"
      provides: "CI warning step for find.text() regression"
      contains: "find.text() regression warning"
    - path: "test/features/home/home_screen_groups_test.dart"
      provides: "Migrated home screen test"
      contains: "find.byKey(HomeKeys."
  key_links:
    - from: ".github/workflows/release_android.yml"
      to: "test/"
      via: "grep count baseline comparison"
      pattern: "BASELINE="
    - from: "test/features/home/home_screen_groups_test.dart"
      to: "lib/features/home/keys/home_keys.dart"
      via: "import and find.byKey(HomeKeys.*)"
      pattern: "find\\.byKey\\(HomeKeys\\."
---

<objective>
Migrate the remaining 10 low-priority test files, add CI regression warning, and execute rename resilience verification (D-13) to prove the phase goal is achieved.

Purpose: Completes the full find.text() migration, adds CI guardrails (D-14), and documents proof that renaming any UI label causes zero structural test failures (success criterion #3).

Output: All 22 test files with find.text() migrated, CI warning step added, rename verification proof documented.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-test-hardening/14-CONTEXT.md
@.planning/phases/14-test-hardening/14-RESEARCH.md
@.planning/phases/14-test-hardening/14-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate remaining 10 test files (priority 13-22, ~35 calls)</name>
  <files>
    test/features/home/home_screen_groups_test.dart,
    test/features/group_balance_card_test.dart,
    test/unit/page_transitions_test.dart,
    test/features/ledger/payer_currency_rewiring_test.dart,
    test/features/ledger_test.dart,
    test/unit/empty_state_view_test.dart,
    test/features/groups/group_activity_screen_test.dart,
    test/features/gear_screen_mutations_test.dart,
    test/widget_test.dart,
    test/integration/happy_path_test.dart,
    lib/features/home/keys/home_keys.dart,
    lib/features/groups/keys/group_keys.dart,
    lib/features/ledger/keys/ledger_keys.dart,
    lib/features/gear/keys/gear_keys.dart,
    lib/core/keys/shared_keys.dart
  </files>
  <read_first>
    test/features/home/home_screen_groups_test.dart,
    test/features/group_balance_card_test.dart,
    test/unit/page_transitions_test.dart,
    test/features/ledger/payer_currency_rewiring_test.dart,
    test/features/ledger_test.dart,
    test/unit/empty_state_view_test.dart,
    test/features/groups/group_activity_screen_test.dart,
    test/features/gear_screen_mutations_test.dart,
    test/widget_test.dart,
    test/integration/happy_path_test.dart,
    lib/features/home/keys/home_keys.dart,
    lib/features/groups/keys/group_keys.dart,
    lib/features/ledger/keys/ledger_keys.dart,
    lib/features/gear/keys/gear_keys.dart,
    lib/core/keys/shared_keys.dart,
    .planning/phases/14-test-hardening/14-RESEARCH.md
  </read_first>
  <action>
Migrate the remaining 10 test files using the same structural vs content classification. These files have 1-8 find.text() calls each — smaller and faster to migrate. Group files with 4 or fewer calls from the same feature area into a single commit (per D-12 discretion).

**Classification rules (same as Plans 01 and 02):**
- STRUCTURAL (convert): screen presence/navigation, section headers, tap targets, empty state labels, structural byType
- CONTENT (keep): amounts, fixture data, validation errors, summaries, state labels

**File 13: `test/features/home/home_screen_groups_test.dart` (8 find.text, 3 find.byType)**
- Structural: FAB tap target, "Create a Group" / "Join a Group" bottom sheet option labels (navigation targets), home screen title, empty state.
- Add keys to `HomeKeys`: `createGroupOption` (`'home_create_group_option'`), `joinGroupOption` (`'home_join_group_option'`), `createGroupFab` (`'home_create_group_fab'`) — if not already added in Plan 01.
- Keep content: group name fixture data.
- Run: `flutter test test/features/home/home_screen_groups_test.dart`

**File 14: `test/features/group_balance_card_test.dart` (7 find.text, 2 find.byType)**
- Structural: GroupBalanceHero settle up button, balance section header.
- Keep content: formatted amounts ('25.500 OMR', '0.000 OMR'), financial state labels ('YOU OWE', 'YOU ARE OWED', 'All settled!'), 'GROUP BALANCES' header text.
- Most calls here are content — estimate ~3 structural conversions.
- Run: `flutter test test/features/group_balance_card_test.dart`

**File 15: `test/unit/page_transitions_test.dart` (4 find.text, 0 find.byType)**
- Read file. These likely test page transition widgets — classify each call.
- Structural: screen presence after navigation transition.
- Keep content: screen-specific text content.
- Run: `flutter test test/unit/page_transitions_test.dart`

**File 16: `test/features/ledger/payer_currency_rewiring_test.dart` (4 find.text, 1 find.byType)**
- Read file. Identify structural: screen/section presence.
- Keep content: currency codes, amount formatting.
- Add keys to `LedgerKeys` as needed.
- Run: `flutter test test/features/ledger/payer_currency_rewiring_test.dart`

**File 17: `test/features/ledger_test.dart` (4 find.text, 0 find.byType)**
- Read file. Classify calls.
- Add keys to `LedgerKeys` as needed.
- Run: `flutter test test/features/ledger_test.dart`

**File 18: `test/unit/empty_state_view_test.dart` (3 find.text, 3 find.byType)**
- Structural: empty state view presence (`find.byType(EmptyStateView)` → `find.byKey(SharedKeys.emptyStateView)`), CTA button tap.
- Keep content: custom message text, CTA button label text (these validate the EmptyStateView renders its constructor arguments correctly).
- Run: `flutter test test/unit/empty_state_view_test.dart`

**File 19: `test/features/groups/group_activity_screen_test.dart` (3 find.text, 2 find.byType)**
- Structural: screen presence, section header.
- Keep content: activity descriptions (fixture data).
- Run: `flutter test test/features/groups/group_activity_screen_test.dart`

**File 20: `test/features/gear_screen_mutations_test.dart` (2 find.text, 4 find.byType)**
- Structural: gear item presence, add button tap, delete confirmation.
- Add keys to `GearKeys`: action buttons as needed.
- Run: `flutter test test/features/gear_screen_mutations_test.dart`

**File 21: `test/widget_test.dart` (1 find.text, 0 find.byType)**
- Read file. Likely a basic smoke test. Classify the single call.
- Run: `flutter test test/widget_test.dart`

**File 22: `test/integration/happy_path_test.dart` (1 find.text, 1 find.byType)**
- Read file. Classify the single call.
- Run: `flutter test test/integration/happy_path_test.dart`

**Commit strategy (per D-12 with grouping discretion):**
- Files 13 (home) — individual commit: `test(14-03): migrate home_screen_groups_test`
- Files 14 (group_balance_card) — individual commit: `test(14-03): migrate group_balance_card_test`
- Files 15-17 (page_transitions + ledger tests, <=4 calls each) — group commit: `test(14-03): migrate page_transitions, ledger, and payer_currency tests`
- Files 18-19 (empty_state + group_activity, <=3 calls each) — group commit: `test(14-03): migrate empty_state_view and group_activity_screen tests`
- Files 20-22 (gear, widget_test, happy_path, <=2 calls each) — group commit: `test(14-03): migrate gear, widget, and happy_path tests`

After all files, run full suite: `flutter test`
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla && flutter test</automated>
  </verify>
  <acceptance_criteria>
    - test/features/home/home_screen_groups_test.dart contains `find.byKey(HomeKeys.` (at least 3 calls)
    - test/features/group_balance_card_test.dart contains `find.byKey(` (at least 2 calls)
    - test/features/gear_screen_mutations_test.dart contains `find.byKey(GearKeys.` or `find.byKey(SharedKeys.` (at least 1 call)
    - All 10 remaining test files have been reviewed and structural calls converted
    - `flutter test` exits with code 0 (all 624 tests pass)
    - Total find.text() count across entire test suite is approximately 70-90 (only content assertions remain)
  </acceptance_criteria>
  <done>All 22 test files with find.text() calls migrated. Only content assertions remain as find.text(). Full suite green.</done>
</task>

<task type="auto">
  <name>Task 2: CI warning step + rename resilience verification</name>
  <files>
    .github/workflows/release_android.yml
  </files>
  <read_first>
    .github/workflows/release_android.yml,
    lib/features/events/widgets/event_module_list.dart,
    .planning/phases/14-test-hardening/14-RESEARCH.md,
    .planning/phases/14-test-hardening/14-CONTEXT.md
  </read_first>
  <action>
**Part A: Determine the baseline find.text() count.**

Run:
```bash
grep -rn "find\.text(" test/ | wc -l | tr -d ' '
```

This count (expected ~70-90) is the baseline of remaining content-only `find.text()` calls. Record this number — it goes into the CI step.

**Part B: Add CI warning step to `.github/workflows/release_android.yml`.**

Add this step AFTER the "Run Tests with Coverage" step (line 51-52 area) and BEFORE the "Install lcov" step:

```yaml
      - name: find.text() regression warning
        run: |
          BASELINE={ACTUAL_COUNT_FROM_PART_A}
          CURRENT=$(grep -rn "find\.text(" test/ | wc -l | tr -d ' ')
          if [ "$CURRENT" -gt "$BASELINE" ]; then
            echo "::warning::find.text() calls increased from $BASELINE to $CURRENT. New structural assertions should use find.byKey() instead."
          fi
          echo "find.text() calls: $CURRENT (baseline: $BASELINE)"
```

Replace `{ACTUAL_COUNT_FROM_PART_A}` with the actual number from Part A.

Per D-14: This is a NON-BLOCKING WARNING (uses `::warning::` not `::error::`, and never `exit 1`). Content tests legitimately use find.text().

**Part C: Rename resilience verification (D-13).**

This is the proof that the phase goal is achieved. Execute this protocol:

1. Temporarily rename 'Ledger' to 'Treasury' in source:
```bash
sed -i '' "s/title: 'Ledger'/title: 'Treasury'/g" lib/features/events/widgets/event_module_list.dart
```

2. Run the full test suite:
```bash
flutter test 2>&1 | tee /tmp/rename_test_output.txt
```

3. Analyze the results:
   - `find.byKey(EventKeys.ledgerCard)` tests MUST PASS (these use keys, not text)
   - Only tests that have `find.text('Ledger')` (content assertions specifically checking the label text) should fail
   - If ANY `find.byKey()` test fails, investigate — the key may be on the wrong widget

4. Record the verification result. Save the output showing which tests passed/failed.

5. REVERT the rename IMMEDIATELY:
```bash
sed -i '' "s/title: 'Treasury'/title: 'Ledger'/g" lib/features/events/widgets/event_module_list.dart
```

6. Confirm revert by running:
```bash
flutter test
```
All 624 tests must pass again.

7. Verify no rename residue in source:
```bash
grep -rn "Treasury" lib/
```
This must return 0 results (or only results that are unrelated to this rename).

**CRITICAL:** Never commit the temporary rename. The rename-and-revert is purely for verification. If `git diff` shows any changes to `lib/features/events/widgets/event_module_list.dart` after the revert, something went wrong.

Commit the CI warning step: `ci(14-03): add find.text() regression warning to CI pipeline`
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla && flutter test && grep -q "find.text() regression warning" .github/workflows/release_android.yml && grep -q "BASELINE=" .github/workflows/release_android.yml</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/release_android.yml contains the step named `find.text() regression warning`
    - .github/workflows/release_android.yml contains `BASELINE=` with a number between 60 and 100
    - .github/workflows/release_android.yml step uses `::warning::` (not `::error::`) — non-blocking
    - .github/workflows/release_android.yml step does NOT contain `exit 1` — non-blocking warning only
    - Rename resilience verification was executed: 'Ledger' was renamed to 'Treasury', test suite run, only content tests failed, rename reverted
    - After revert: `flutter test` exits with code 0 (all 624 tests pass)
    - After revert: `grep -rn "Treasury" lib/` returns 0 matches related to the test rename
    - `git diff lib/features/events/widgets/event_module_list.dart` shows no changes (rename fully reverted)
  </acceptance_criteria>
  <done>CI warning step added with actual baseline count. Rename resilience verification proves success criterion #3: renaming a UI label causes zero find.byKey() test failures. Full suite passes green after revert.</done>
</task>

</tasks>

<verification>
- `flutter test` — full suite passes (624 tests)
- `flutter analyze --no-fatal-infos` — no new analysis errors
- `grep -rn "find\.text(" test/ | wc -l` — returns a number between 60 and 100 (only content assertions remain)
- `grep -rn "find\.byKey(" test/ | wc -l` — returns at least 100 find.byKey() calls across the test suite
- `grep "find.text() regression warning" .github/workflows/release_android.yml` — CI step exists
- Rename verification output documented — proves rename resilience
</verification>

<success_criteria>
- All 22 test files with find.text() reviewed and structural calls migrated
- Only content find.text() calls remain (~70-90 calls)
- CI warning step prevents regression of find.text() count
- Rename resilience verified: 'Ledger' → 'Treasury' causes zero find.byKey() failures
- Full test suite (624 tests) passes green
- Phase 14 success criteria #1-4 from ROADMAP.md are all met:
  1. Every interactive widget and structural landmark has a semantic Key
  2. All structural navigation assertions use find.byKey()
  3. Renaming any UI label causes zero test failures outside content tests
  4. Full test suite passes without modification after Keys are added
</success_criteria>

<output>
After completion, create `.planning/phases/14-test-hardening/14-03-SUMMARY.md`
</output>
