---
phase: 14-test-hardening
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - lib/features/groups/keys/group_keys.dart
  - lib/features/events/keys/event_keys.dart
  - lib/features/logistics/keys/logistics_keys.dart
  - lib/core/keys/shared_keys.dart
  - lib/features/home/keys/home_keys.dart
  - lib/features/gear/keys/gear_keys.dart
  - lib/features/groups/widgets/group_member_balance_card.dart
  - lib/features/groups/widgets/group_spending_stats.dart
  - lib/features/groups/widgets/group_settlement_summary.dart
  - lib/features/groups/widgets/group_activity_tile.dart
  - lib/features/events/widgets/event_spending_hero.dart
  - test/features/groups/group_screens_test.dart
  - test/features/events/create_event_test.dart
  - test/features/events/event_module_list_test.dart
  - test/features/events/group_detail_events_test.dart
  - test/features/logistics_screen_mutations_test.dart
  - test/features/group_settle_up_screen_test.dart
  - test/unit/shared_widgets_test.dart
  - test/features/group_detail_screen_test.dart
autonomous: true
requirements:
  - FOUND-05

must_haves:
  truths:
    - "All structural find.text() calls in medium-priority test files (priority 5-12, 115 calls total) are converted to find.byKey()"
    - "Content find.text() calls for amounts, fixture data, validation errors are preserved in every file"
    - "Full test suite passes green after all 8 file migrations"
  artifacts:
    - path: "test/features/groups/group_screens_test.dart"
      provides: "Migrated group screens test with find.byKey() for structural assertions"
      contains: "find.byKey(GroupKeys."
    - path: "test/features/events/event_module_list_test.dart"
      provides: "Migrated event module list test with find.byKey() for all module card presence checks"
      contains: "find.byKey(EventKeys."
    - path: "test/features/logistics_screen_mutations_test.dart"
      provides: "Migrated logistics test with find.byKey() for structural assertions"
      contains: "find.byKey(LogisticsKeys."
    - path: "test/unit/shared_widgets_test.dart"
      provides: "Migrated shared widgets test with find.byKey() for structural assertions"
      contains: "find.byKey(SharedKeys."
  key_links:
    - from: "test/features/events/event_module_list_test.dart"
      to: "lib/features/events/keys/event_keys.dart"
      via: "import and find.byKey(EventKeys.ledgerCard) etc."
      pattern: "find\\.byKey\\(EventKeys\\."
    - from: "test/features/logistics_screen_mutations_test.dart"
      to: "lib/features/logistics/keys/logistics_keys.dart"
      via: "import and find.byKey(LogisticsKeys.*)"
      pattern: "find\\.byKey\\(LogisticsKeys\\."
---

<objective>
Migrate 8 medium-priority test files (priority 5-12) from structural find.text()/find.byType() to find.byKey(), continuing the systematic conversion started in Plan 01.

Purpose: Converts the next 115 find.text() calls covering groups, events, logistics, shared widgets, and group detail tests. After this plan, ~87% of all structural conversions are complete.

Output: 8 test files migrated, key classes enriched with additional keys, all tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-test-hardening/14-CONTEXT.md
@.planning/phases/14-test-hardening/14-RESEARCH.md
@.planning/phases/14-test-hardening/14-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate priority 5-8 test files (groups, events — 66 calls)</name>
  <files>
    test/features/groups/group_screens_test.dart,
    test/features/events/create_event_test.dart,
    test/features/events/event_module_list_test.dart,
    test/features/events/group_detail_events_test.dart,
    lib/features/groups/keys/group_keys.dart,
    lib/features/events/keys/event_keys.dart,
    lib/features/groups/widgets/group_member_balance_card.dart,
    lib/features/groups/widgets/group_spending_stats.dart,
    lib/features/groups/widgets/group_settlement_summary.dart,
    lib/features/events/widgets/event_spending_hero.dart
  </files>
  <read_first>
    test/features/groups/group_screens_test.dart,
    test/features/events/create_event_test.dart,
    test/features/events/event_module_list_test.dart,
    test/features/events/group_detail_events_test.dart,
    lib/features/groups/keys/group_keys.dart,
    lib/features/events/keys/event_keys.dart,
    lib/features/groups/screens/group_detail_screen.dart,
    lib/features/groups/screens/group_settings_screen.dart,
    lib/features/events/screens/create_event_screen.dart,
    lib/features/events/widgets/event_module_list.dart,
    lib/features/events/widgets/event_spending_hero.dart,
    .planning/phases/14-test-hardening/14-RESEARCH.md
  </read_first>
  <action>
For each of these 4 test files, apply the same classification methodology from Plan 01:

**Classification rules (from D-05, D-06, D-07):**
- STRUCTURAL (convert): module card presence/absence, screen titles, section headers, tap targets, empty state labels, navigation type checks
- CONTENT (keep): amounts, fixture data names, validation errors, computed summaries, financial state labels

**File 5: `test/features/groups/group_screens_test.dart` (19 find.text, 6 find.byType)**
- Read the file. Identify structural calls: screen title checks (`find.text('Group Settings')` etc.), section header checks, tap targets, navigation confirmations.
- Add any NEW key constants needed to `lib/features/groups/keys/group_keys.dart` that were not already added in Plan 01. For example: additional section markers, additional action buttons not in the first 4 files.
- If any NEW widget files need `key:` parameters added (e.g., `group_spending_stats.dart`, `group_settlement_summary.dart`), add them.
- Convert structural calls to `find.byKey(GroupKeys.*)`. Keep content calls unchanged.
- Run: `flutter test test/features/groups/group_screens_test.dart`
- Commit: `test(14-02): migrate group_screens_test from find.text to find.byKey`

**File 6: `test/features/events/create_event_test.dart` (18 find.text, 4 find.byType)**
- Read the file. Identify structural: event type picker options, create event form labels, create button tap, screen title checks, event type selection taps.
- Add keys to `EventKeys`: `eventTypeTripOption` (`'event_type_trip_option'`), `eventTypeCampingOption` (`'event_type_camping_option'`), `eventTypeDayOutOption` (`'event_type_day_out_option'`), `eventTypeDinnerOption` (`'event_type_dinner_option'`), `eventTypeCustomOption` (`'event_type_custom_option'`), `createEventButton` (`'event_create_button'`), `eventNameInput` (`'event_name_input'`), and any other keys needed.
- Add `key:` parameters to the corresponding widget files (e.g., `event_type_picker_screen.dart`, `create_event_screen.dart` — if not already keyed from Plan 01).
- Convert structural calls. Keep content calls (event names, validation messages).
- Run: `flutter test test/features/events/create_event_test.dart`
- Commit: `test(14-02): migrate create_event_test from find.text to find.byKey`

**File 7: `test/features/events/event_module_list_test.dart` (15 find.text, 0 find.byType)**
- This file is almost entirely structural — it tests which module cards appear based on `EventModules` config.
- Nearly all `find.text('Ledger')`, `find.text('Gear')`, etc. are module card presence/absence → convert to `find.byKey(EventKeys.ledgerCard)`, `find.byKey(EventKeys.gearCard)`, etc.
- Keep any content assertions (likely very few — ~1 based on RESEARCH estimate).
- Run: `flutter test test/features/events/event_module_list_test.dart`
- Commit: `test(14-02): migrate event_module_list_test from find.text to find.byKey`

**File 8: `test/features/events/group_detail_events_test.dart` (14 find.text, 4 find.byType)**
- Read the file. Identify structural: event card presence checks, "No events yet" empty state, section headers, navigation taps.
- Keep content: event names (fixture data), dates, member counts.
- Add keys as needed: `EventKeys.noEventsEmpty` or `GroupKeys.noEventsEmpty` (whichever owns the empty state widget), event card parameterized keys.
- Convert structural calls. Run test. Commit.
- Run: `flutter test test/features/events/group_detail_events_test.dart`
- Commit: `test(14-02): migrate group_detail_events_test from find.text to find.byKey`

After all 4 files, run full suite: `flutter test`
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla && flutter test test/features/groups/group_screens_test.dart && flutter test test/features/events/create_event_test.dart && flutter test test/features/events/event_module_list_test.dart && flutter test test/features/events/group_detail_events_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - test/features/groups/group_screens_test.dart contains `find.byKey(GroupKeys.` (at least 8 calls)
    - test/features/events/create_event_test.dart contains `find.byKey(EventKeys.` (at least 10 calls)
    - test/features/events/event_module_list_test.dart contains `find.byKey(EventKeys.` (at least 12 calls — nearly all calls are structural module card checks)
    - test/features/events/group_detail_events_test.dart contains `find.byKey(` (at least 5 calls)
    - No structural find.text() calls remain for module card presence, screen titles, section headers, or tap targets in these 4 files
    - Content find.text() calls for amounts, fixture data, validation messages are preserved
    - All 4 individual test files pass green
  </acceptance_criteria>
  <done>Priority 5-8 test files migrated with structural find.text/byType converted to find.byKey. All 4 files pass green.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate priority 9-12 test files (logistics, settle up, shared, group detail — 46 calls)</name>
  <files>
    test/features/logistics_screen_mutations_test.dart,
    test/features/group_settle_up_screen_test.dart,
    test/unit/shared_widgets_test.dart,
    test/features/group_detail_screen_test.dart,
    lib/features/logistics/keys/logistics_keys.dart,
    lib/features/groups/keys/group_keys.dart,
    lib/core/keys/shared_keys.dart,
    lib/features/groups/widgets/group_activity_tile.dart
  </files>
  <read_first>
    test/features/logistics_screen_mutations_test.dart,
    test/features/group_settle_up_screen_test.dart,
    test/unit/shared_widgets_test.dart,
    test/features/group_detail_screen_test.dart,
    lib/features/logistics/keys/logistics_keys.dart,
    lib/features/groups/keys/group_keys.dart,
    lib/core/keys/shared_keys.dart,
    lib/features/logistics/screens/logistics_screen.dart,
    lib/features/logistics/widgets/subgroup_card.dart,
    lib/features/logistics/widgets/unassigned_pool.dart,
    lib/features/groups/screens/group_settle_up_screen.dart,
    lib/shared/widgets/empty_state_view.dart,
    lib/shared/widgets/module_header.dart,
    .planning/phases/14-test-hardening/14-RESEARCH.md
  </read_first>
  <action>
Continue the same classification and migration methodology for 4 more test files:

**File 9: `test/features/logistics_screen_mutations_test.dart` (12 find.text, 10 find.byType)**
- Read the file. Identify structural: `tester.tap(find.text('CREATE GROUP'))` or similar action buttons, section labels, subgroup card presence, unassigned pool labels, remove/delete button taps.
- Add any new keys to `LogisticsKeys`: action buttons like `createGroupButton` (`'logistics_create_group_button'`), `removeButton` (`'logistics_remove_button'`), `deleteConfirmButton` (`'logistics_delete_confirm_button'`), `cancelButton` (`'logistics_cancel_button'`). Add `key:` to widgets as needed.
- Convert structural byType: `find.byType(LogisticsScreen)` → `find.byKey(LogisticsKeys.screen)`, subgroup card type checks → use parameterized keys.
- Keep content: member names, subgroup names (fixture data).
- Run: `flutter test test/features/logistics_screen_mutations_test.dart`
- Commit: `test(14-02): migrate logistics_screen_mutations_test from find.text to find.byKey`

**File 10: `test/features/group_settle_up_screen_test.dart` (12 find.text, 3 find.byType)**
- NOTE: This is a DIFFERENT file from `test/features/groups/group_settle_up_screen_test.dart` (migrated in Plan 01). Check the exact path — this one is at `test/features/` (not `test/features/groups/`).
- Read the file. Apply same classification: settle up action buttons, screen presence, member balance display.
- Add keys as needed to `GroupKeys`.
- Convert structural. Keep content.
- Run: `flutter test test/features/group_settle_up_screen_test.dart`
- Commit: `test(14-02): migrate group_settle_up_screen_test (features/) from find.text to find.byKey`

**File 11: `test/unit/shared_widgets_test.dart` (11 find.text, 15 find.byType)**
- This tests shared widgets: ModuleHeader, EmptyStateView, OfflineBanner, AppTabBar, LoadingButton, SkeletonLoader.
- Convert structural: back button taps (`find.byKey(SharedKeys.moduleHeaderBackButton)`), empty state CTA taps (`find.byKey(SharedKeys.emptyStateCtaButton)`), offline banner presence, skeleton loader type checks.
- Keep content: text passed in via constructors that validates rendering (e.g., header title text, empty state message text).
- Convert structural byType: `find.byType(ModuleHeader)`, `find.byType(EmptyStateView)`, `find.byType(OfflineBanner)` — if they check presence as structural assertions → `find.byKey(SharedKeys.*)`.
- Keep byType: `find.byType(CircularProgressIndicator)`, `find.byType(ShimmerPlaceholder)`, `find.byType(SkeletonLoader)` — genuine type checks.
- Run: `flutter test test/unit/shared_widgets_test.dart`
- Commit: `test(14-02): migrate shared_widgets_test from find.text to find.byKey`

**File 12: `test/features/group_detail_screen_test.dart` (11 find.text, 0 find.byType)**
- Read the file. Identify structural: section headers ('Members & Balances', 'Events', 'Invite Code'), screen title, tap targets, empty state checks.
- Keep content: group name, member names, invite code value, balance amounts.
- Add keys to GroupKeys as needed.
- Run: `flutter test test/features/group_detail_screen_test.dart`
- Commit: `test(14-02): migrate group_detail_screen_test from find.text to find.byKey`

After all 4 files, run full suite: `flutter test`
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla && flutter test test/features/logistics_screen_mutations_test.dart && flutter test test/features/group_settle_up_screen_test.dart && flutter test test/unit/shared_widgets_test.dart && flutter test test/features/group_detail_screen_test.dart && flutter test</automated>
  </verify>
  <acceptance_criteria>
    - test/features/logistics_screen_mutations_test.dart contains `find.byKey(LogisticsKeys.` (at least 6 calls)
    - test/features/group_settle_up_screen_test.dart contains `find.byKey(GroupKeys.` (at least 6 calls)
    - test/unit/shared_widgets_test.dart contains `find.byKey(SharedKeys.` (at least 4 calls)
    - test/features/group_detail_screen_test.dart contains `find.byKey(GroupKeys.` (at least 5 calls)
    - No structural find.text() calls remain for section headers, screen titles, or tap targets in these 4 files
    - Content find.text() calls are preserved
    - `flutter test` exits with code 0 (full suite green)
  </acceptance_criteria>
  <done>Priority 9-12 test files migrated. All 12 heaviest test files (priority 1-12) now use find.byKey() for structural assertions. Full test suite green.</done>
</task>

</tasks>

<verification>
- `flutter test` — full suite passes
- `grep -rn "find\.byKey(" test/features/groups/group_screens_test.dart test/features/events/create_event_test.dart test/features/events/event_module_list_test.dart test/features/events/group_detail_events_test.dart test/features/logistics_screen_mutations_test.dart test/features/group_settle_up_screen_test.dart test/unit/shared_widgets_test.dart test/features/group_detail_screen_test.dart | wc -l` — at least 56 find.byKey() calls across 8 files
- Structural find.text calls in these 8 files are converted
</verification>

<success_criteria>
- 8 medium-priority test files migrated from structural find.text/byType to find.byKey
- Key class files enriched with all constants needed by these test files
- Full test suite (624 tests) passes green
- Combined with Plan 01, the 12 highest-call-count test files are fully migrated
</success_criteria>

<output>
After completion, create `.planning/phases/14-test-hardening/14-02-SUMMARY.md`
</output>
