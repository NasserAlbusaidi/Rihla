---
phase: 20-group-detail-event-hub-redesign
plan: 02
type: execute
wave: 2
slug: event-hub-container-transform
status: draft
depends_on: [20-01]
files_modified:
  - lib/features/events/screens/event_command_center.dart
  - lib/features/events/screens/event_expense_hero.dart
  - lib/features/events/widgets/event_module_list.dart
  - lib/features/events/keys/event_keys.dart
  - lib/features/home/screens/home_screen.dart
  - test/features/events/event_command_center_test.dart
autonomous: true
requirements:
  - SCRN-02
must_haves:
  truths:
    - "Event hub renders modules in a 2x3 grid layout (not vertical column)"
    - "Module grid uses fixed order: Ledger, Gear, Logistics, Vault, Activity, Memories"
    - "Ledger module card uses AppColors.moduleLedger; all other modules use AppColors.moduleGear"
    - "Expense hero card uses light AppColors.surface background with teal chip button, not dark gradient"
    - "Activity module card always appears in the grid at position 5 (even when empty)"
    - "ContainerTransform plays when tapping a GroupCard on HomeScreen (manual verification)"
    - "Modules with data show live summary; modules without data show description text"
    - "OpenContainer widget is present in HomeScreen group cards section"
  artifacts:
    - path: "lib/features/events/widgets/event_module_list.dart"
      provides: "2x3 GridView module layout with corrected colors and fixed order"
    - path: "lib/features/events/screens/event_expense_hero.dart"
      provides: "Light-themed expense hero with teal add-expense chip"
    - path: "lib/features/events/screens/event_command_center.dart"
      provides: "Redesigned event hub using CustomScrollView with slivers"
    - path: "lib/features/home/screens/home_screen.dart"
      provides: "OpenContainer wrapping GroupCard for ContainerTransform"
    - path: "test/features/events/event_command_center_test.dart"
      provides: "Updated tests for grid layout, corrected colors, redesigned hero, OpenContainer presence"
  key_links:
    - from: "lib/features/events/screens/event_command_center.dart"
      to: "lib/features/events/widgets/event_module_list.dart"
      via: "EventModuleList widget used inside CustomScrollView"
      pattern: "EventModuleList"
    - from: "lib/features/home/screens/home_screen.dart"
      to: "lib/features/groups/screens/group_detail_screen.dart"
      via: "OpenContainer openBuilder returns GroupDetailScreen"
      pattern: "OpenContainer.*GroupDetailScreen"
    - from: "lib/features/events/screens/event_command_center.dart"
      to: "lib/features/events/screens/event_expense_hero.dart"
      via: "EventExpenseHero widget"
      pattern: "EventExpenseHero"
---

<objective>
Rebuild the EventCommandCenter (event hub) with 2x3 module grid, corrected module accent colors, redesigned light-themed expense hero, and Activity module card. Add OpenContainer ContainerTransform transition on HomeScreen GroupCard. Satisfies SCRN-02 and the ContainerTransform success criterion.

Purpose: Event hub becomes a scannable dashboard — users see module status at a glance via the 2x3 grid with live summaries.
Output: Redesigned event_command_center.dart, event_expense_hero.dart, event_module_list.dart, OpenContainer on home_screen.dart.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-group-detail-event-hub-redesign/20-CONTEXT.md
@.planning/phases/20-group-detail-event-hub-redesign/20-RESEARCH.md
@.planning/phases/20-group-detail-event-hub-redesign/20-VALIDATION.md

<interfaces>
From lib/features/events/screens/event_command_center.dart (144 lines):
- ConsumerWidget
- Watches: eventDetailProvider, groupDetailProvider, tripExpensesProvider (shim)
- Current layout: Column > SingleChildScrollView > Column with EventExpenseHero + EventModuleList
- _tripDataSeedProvider for eager data download on entry

From lib/features/events/screens/event_expense_hero.dart (181 lines):
- Dark gradient card with TweenAnimationBuilder for amount display
- Uses ClipRRect, Stack, decorative circle overlay
- Watches eventExpensesProvider
- Has EventKeys.spendingHero key on GestureDetector

From lib/features/events/widgets/event_module_list.dart (336 lines):
- Column layout with priority sorting
- Current module colors: accentSecondary, amber, sky, indigo, mint (ALL WRONG — must use moduleLedger, moduleGear, etc.)
- No Activity card currently
- SmartModuleCard used for each module

From lib/features/events/keys/event_keys.dart:
- Existing: spendingHero, ledgerCard, gearCard, logisticsCard, vaultCard, memoriesCard

From lib/features/home/screens/home_screen.dart:
- GroupCard wrapped in TapBounce with onTap: context.push('/group/${group.id}')
- Inside FadeInList children mapping

From lib/core/theme/tokens/color_tokens.dart:
- moduleLedger, moduleGear, moduleLogistics, moduleVault, moduleActivity, moduleMemories tokens
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test preparation — update event hub test assertions and extend EventKeys (TDD red)</name>
  <files>lib/features/events/keys/event_keys.dart, test/features/events/event_command_center_test.dart</files>
  <read_first>
    - lib/features/events/keys/event_keys.dart (existing key definitions)
    - test/features/events/event_command_center_test.dart (existing test assertions, _wrap helper)
    - lib/features/events/widgets/event_module_list.dart (current module card keys)
  </read_first>
  <action>
    **Per D-12, D-13, D-14 and VALIDATION.md Wave 0:**

    1. **Extend EventKeys** in `lib/features/events/keys/event_keys.dart`:
       - `static const moduleGrid = Key('event_module_grid');` — for the 2x3 GridView
       - `static const expenseHeroTotal = Key('event_expense_hero_total');` — for total amount text in redesigned hero
       - `static const addExpenseChip = Key('event_add_expense_chip');` — for "+ Add Expense" chip button
       - `static const activityCard = Key('event_activity_card');` — for new Activity module card

    2. **Update existing tests** in `test/features/events/event_command_center_test.dart`:
       - Find `expect(find.text('SPENDING'), findsOneWidget)` assertions. Update to `expect(find.text('TOTAL EXPENSES'), findsOneWidget)` — redesigned hero uses "TOTAL EXPENSES" overline.

    3. **Add new test cases** (TDD red phase):
       - `module grid renders 6 cards in fixed order`: Pump with all modules enabled, assert all 6 card keys findsOneWidget (ledgerCard, gearCard, logisticsCard, vaultCard, activityCard, memoriesCard).
       - `module grid includes Activity card`: Assert `find.byKey(EventKeys.activityCard)` findsOneWidget.
       - `expense hero shows add-expense chip button`: Assert `find.byKey(EventKeys.addExpenseChip)` findsOneWidget.
       - `OpenContainer present in HomeScreen group cards`: Add a separate test (or note for Task 3 test) asserting `find.byType(OpenContainer)` findsWidgets in HomeScreen group list.
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla && flutter analyze lib/features/events/keys/event_keys.dart && flutter test test/features/events/event_command_center_test.dart 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - event_keys.dart contains moduleGrid, expenseHeroTotal, addExpenseChip, activityCard keys
    - event_command_center_test.dart has 'SPENDING' replaced with 'TOTAL EXPENSES'
    - 3+ new test cases added for module grid, Activity card, and add-expense chip
    - All test files compile without analyzer errors
    - Some tests FAIL (expected TDD red phase)
  </acceptance_criteria>
  <done>EventKeys extended with 4 new keys, existing tests updated for TOTAL EXPENSES label, 3+ new test cases added (compilable but failing red). flutter analyze passes on key files.</done>
</task>

<task type="auto">
  <name>Task 2: Redesign EventExpenseHero (light theme) and EventModuleList (2x3 grid, corrected colors, Activity card)</name>
  <files>lib/features/events/screens/event_expense_hero.dart, lib/features/events/widgets/event_module_list.dart</files>
  <read_first>
    - lib/features/events/screens/event_expense_hero.dart (full file — dark gradient layout, TweenAnimationBuilder, EventKeys.spendingHero)
    - lib/features/events/widgets/event_module_list.dart (full file — Column layout, priority sorting, wrong module colors)
    - lib/features/events/keys/event_keys.dart (keys from Task 1)
    - lib/core/theme/tokens/color_tokens.dart (verify moduleLedger, moduleGear, moduleLogistics, moduleVault, moduleActivity, moduleMemories values)
    - lib/shared/widgets/smart_module_card.dart (SmartModuleCard API — summary/description display modes)
  </read_first>
  <action>
    **Per D-11, D-12, D-13, D-14 and research color token corrections:**

    **A. Redesign EventExpenseHero** (`lib/features/events/screens/event_expense_hero.dart`):

    1. Replace dark gradient card with light surface card (D-14):
       - Container bg: `AppColors.surface` (not darkHeaderGradient)
       - Border: `Border.all(color: AppColors.border)`
       - BorderRadius: `AppColors.radiusLarge` (20dp)
       - BoxShadow: `AppColors.shadowRaised`
       - Remove ClipRRect, Stack, decorative circle overlay
    2. Redesign content:
       - Overline: "TOTAL EXPENSES" (fontSize: 10, w700, textMuted, letterSpacing: 0.5)
       - Total amount: Keep TweenAnimationBuilder, change to `TextStyle(fontSize: 36, w800, textPrimary)`. Key with `EventKeys.expenseHeroTotal`.
       - Expense count: `TextStyle(fontSize: 12, w600, textSecondary)`
       - Add "+ Add Expense" chip: `ActionChip(label: Text('+ Add Expense'), backgroundColor: AppColors.primary, labelStyle: TextStyle(color: Colors.white, w700), onPressed: onTap)`. Key with `EventKeys.addExpenseChip`. Position top-right via Row with Spacer.
    3. Keep `EventKeys.spendingHero` key on outer GestureDetector.
    4. Remove Icon(Iconsax.wallet_3) header icon and Icon(Iconsax.arrow_right_3) trailing chevron.
    5. Remove `height: 140` constraint — let card size to content. Padding `EdgeInsets.all(AppColors.space16)`.

    **B. Redesign EventModuleList** (`lib/features/events/widgets/event_module_list.dart`):

    1. **Add Activity card**: Following _addMemoriesCard pattern:
       ```
       cards.add(_ModuleCardConfig(
         widgetKey: EventKeys.activityCard,
         icon: Iconsax.activity,
         title: 'Activity',
         description: 'Timeline logs',
         color: AppColors.moduleActivity,
         onTap: () => context.push('/group/$groupId/event/$eventId/activity'),
         isEmpty: true,
       ));
       ```

    2. **Correct all module colors** (critical fix):
       - Ledger: `AppColors.accentSecondary` → `AppColors.moduleLedger`
       - Gear: `AppColors.amber` / `AppColors.accentSecondary` → `AppColors.moduleGear`
       - Logistics: `AppColors.sky` → `AppColors.moduleLogistics`
       - Vault: `AppColors.indigo` → `AppColors.moduleVault`
       - Memories: `AppColors.mint` → `AppColors.moduleMemories`
       - Activity (new): `AppColors.moduleActivity`

    3. **Remove priority sorting** (D-13): Delete `cards.sort((a, b) => b.priority.compareTo(a.priority))`. Fixed order: Ledger, Gear, Logistics, Vault, Activity, Memories (call order of _add*Card methods).

    4. **Convert to GridView**: Change return from Column to `GridView.count(crossAxisCount: 2, shrinkWrap: true, physics: NeverScrollableScrollPhysics(), crossAxisSpacing: 12, mainAxisSpacing: 12, childAspectRatio: 2.0)`. Key with `EventKeys.moduleGrid`.

    5. **Remove priority field** from `_ModuleCardConfig` — no longer needed.

    6. Keep flutter_animate stagger animations, adjust delay indices for grid layout.
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla && flutter analyze lib/features/events/screens/event_expense_hero.dart lib/features/events/widgets/event_module_list.dart && flutter test test/features/events/event_command_center_test.dart 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - event_expense_hero.dart uses AppColors.surface background (not dark gradient)
    - event_expense_hero.dart has "TOTAL EXPENSES" overline label
    - event_expense_hero.dart has ActionChip keyed EventKeys.addExpenseChip
    - event_module_list.dart renders 6 modules in GridView.count with crossAxisCount: 2
    - event_module_list.dart has Activity card with EventKeys.activityCard key
    - All module colors use AppColors.module* tokens (no accentSecondary, amber, sky, indigo, mint)
    - Priority sorting removed — fixed order Ledger, Gear, Logistics, Vault, Activity, Memories
    - EventKeys.moduleGrid key on GridView
    - flutter analyze passes on both files
    - event_command_center_test.dart module/hero tests pass green
  </acceptance_criteria>
  <done>EventExpenseHero redesigned as light card with TOTAL EXPENSES overline and teal add-expense chip. EventModuleList renders 2x3 grid with corrected module colors, Activity card added, priority sorting removed. Tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Rebuild EventCommandCenter as CustomScrollView + add OpenContainer on HomeScreen</name>
  <files>lib/features/events/screens/event_command_center.dart, lib/features/home/screens/home_screen.dart</files>
  <read_first>
    - lib/features/events/screens/event_command_center.dart (full file — current Column > SingleChildScrollView layout, provider watches)
    - lib/features/events/screens/event_expense_hero.dart (Task 2 output — redesigned widget interface)
    - lib/features/events/widgets/event_module_list.dart (Task 2 output — GridView widget)
    - lib/features/home/screens/home_screen.dart (GroupCard section with TapBounce + context.push)
    - lib/features/groups/screens/group_detail_screen.dart (GroupDetailScreen constructor for OpenContainer openBuilder)
    - lib/shared/widgets/skeleton_loader.dart (SkeletonBlock, SkeletonBar for loading state)
  </read_first>
  <action>
    **Per D-04, D-05, D-06 and research Pattern 4:**

    **A. Rebuild EventCommandCenter** (`lib/features/events/screens/event_command_center.dart`):

    1. Replace Column > SingleChildScrollView with CustomScrollView + slivers (matching Phase 18 home dashboard pattern):
       ```dart
       body: CustomScrollView(
         slivers: [
           SliverToBoxAdapter(child: ModuleHeader(...)),
           SliverToBoxAdapter(child: OfflineBanner()),
           SliverPadding(
             padding: EdgeInsets.fromLTRB(space24, space20, space24, space24),
             sliver: SliverToBoxAdapter(child: EventExpenseHero(...)),
           ),
           SliverPadding(
             padding: EdgeInsets.symmetric(horizontal: space24),
             sliver: SliverToBoxAdapter(child: EventModuleList(...)),
           ),
           SliverToBoxAdapter(child: SizedBox(height: space32)),
         ],
       )
       ```
    2. Replace loading CircularProgressIndicator with skeleton composites using SliverToBoxAdapter wrappers.
    3. Keep all existing provider watches (eventDetailProvider, groupDetailProvider).
    4. Keep FloatingActionButton for adding expenses.
    5. Keep EmptyStateView not-found fallback.

    **B. Add OpenContainer on HomeScreen** (`lib/features/home/screens/home_screen.dart`):

    1. Add import: `import 'package:animations/animations.dart';`
    2. Add import: `import '../../groups/screens/group_detail_screen.dart';`
    3. In group cards section, find TapBounce wrapping GroupCard. Replace with:
       ```dart
       OpenContainer<void>(
         closedColor: Colors.transparent,
         openColor: AppColors.background,
         closedElevation: 0,
         openElevation: 0,
         closedShape: RoundedRectangleBorder(
           borderRadius: BorderRadius.circular(AppColors.radiusLarge),
         ),
         openShape: const RoundedRectangleBorder(),
         transitionDuration: const Duration(milliseconds: 400),
         transitionType: ContainerTransitionType.fade,
         useRootNavigator: false,
         closedBuilder: (context, openContainer) => GroupCard(
           group: group,
           onTap: openContainer,
         ),
         openBuilder: (context, closeContainer) => GroupDetailScreen(
           groupId: group.id,
         ),
       )
       ```
    4. Remove TapBounce wrapper from group cards. Keep Padding(bottom: space8).
    5. Per research Pitfall 1: Accept URL does not update during OpenContainer transition. GoRouter's `/group/:gid` route remains for deep links and sub-route navigation.
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla && flutter analyze lib/features/events/screens/event_command_center.dart lib/features/home/screens/home_screen.dart && flutter test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - event_command_center.dart uses CustomScrollView with slivers (not Column > SingleChildScrollView)
    - event_command_center.dart loading state uses SkeletonLoader (not CircularProgressIndicator)
    - home_screen.dart contains OpenContainer wrapping GroupCard
    - home_screen.dart imports `animations.dart` and `group_detail_screen.dart`
    - TapBounce removed from group cards in HomeScreen
    - OpenContainer uses useRootNavigator: false
    - find.byType(OpenContainer) present in HomeScreen widget tree (manual or test verification)
    - flutter analyze passes on both files
    - Full test suite passes
  </acceptance_criteria>
  <done>EventCommandCenter uses CustomScrollView + slivers with skeleton loading. HomeScreen GroupCard wrapped in OpenContainer for ContainerTransform transition (URL desync accepted per D-06). Full test suite green.</done>
</task>

</tasks>
