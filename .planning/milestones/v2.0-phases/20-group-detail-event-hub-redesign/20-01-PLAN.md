---
phase: 20-group-detail-event-hub-redesign
plan: 01
type: execute
wave: 1
slug: group-detail-redesign
status: draft
depends_on: []
files_modified:
  - pubspec.yaml
  - lib/features/groups/screens/group_detail_screen.dart
  - lib/features/groups/widgets/group_stats_grid.dart
  - lib/features/groups/keys/group_keys.dart
  - lib/features/events/widgets/event_card.dart
  - test/features/group_detail_screen_test.dart
autonomous: true
requirements:
  - SCRN-01
must_haves:
  truths:
    - "Group detail shows 2x2 stats grid with YOUR BALANCE, GROUP TOTAL, ACTIVE MEMBERS, EVENTS above the fold"
    - "Stats grid YOUR BALANCE tile is color-coded: errorText when negative, successText when positive, textPrimary when zero"
    - "Settle-up CTA button appears when any balance is non-zero; hidden when all settled"
    - "Section order below fold is Events then Member Balances then Recent Activity"
    - "Event cards show left accent bar: teal for active events, gray for past events"
    - "Event cards show inline personal balance text color-coded red/green/gray"
    - "Past events render at 60% opacity"
    - "Loading state uses SkeletonLoader composites instead of CircularProgressIndicator"
  artifacts:
    - path: "lib/features/groups/widgets/group_stats_grid.dart"
      provides: "2x2 stats grid widget with StatCard"
      min_lines: 50
    - path: "lib/features/groups/screens/group_detail_screen.dart"
      provides: "Redesigned group detail with stats grid, reordered sections, skeleton loading"
    - path: "lib/features/groups/keys/group_keys.dart"
      provides: "New keys for stats grid and settle-up CTA"
    - path: "lib/features/events/widgets/event_card.dart"
      provides: "EventCard with left accent bar, inline personal balance, 60% past opacity"
    - path: "test/features/group_detail_screen_test.dart"
      provides: "Updated tests for stats grid, section order, settle-up CTA"
  key_links:
    - from: "lib/features/groups/screens/group_detail_screen.dart"
      to: "lib/features/groups/widgets/group_stats_grid.dart"
      via: "GroupStatsGrid widget import"
      pattern: "GroupStatsGrid"
    - from: "lib/features/groups/screens/group_detail_screen.dart"
      to: "groupBalancesProvider"
      via: "ref.watch for stats grid data"
      pattern: "ref\\.watch\\(groupBalancesProvider"
---

<objective>
Rebuild the GroupDetailScreen layout to match the Phase 20 design spec: 2x2 stats grid above the fold, settle-up CTA, event cards with left accent bars and inline personal balances, section reorder (Events > Members > Activity), and skeleton loading state. Satisfies SCRN-01.

Purpose: Group detail screen becomes the primary information hub — users scan rather than read to understand financial state and event status.
Output: Redesigned group_detail_screen.dart with GroupStatsGrid widget, updated EventCard, and comprehensive tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-group-detail-event-hub-redesign/20-CONTEXT.md
@.planning/phases/20-group-detail-event-hub-redesign/20-RESEARCH.md
@.planning/phases/20-group-detail-event-hub-redesign/20-VALIDATION.md

<interfaces>
From lib/features/groups/screens/group_detail_screen.dart (current structure):
- 609 lines, ConsumerStatefulWidget
- Watches: groupDetailProvider(groupId), groupBalancesProvider(groupId), groupMemberBalancesProvider(groupId)
- Has _buildStatsRow, _buildEventsSection, _buildMembersBalancesSection, _buildActivitySection, _buildInviteSection
- GroupBalanceHero and GroupSpendingStats widgets used in conditional block

From lib/features/events/widgets/event_card.dart:
- Uses TapBounce wrapper, 48x48 icon container, Row layout
- event.isPast check with Opacity(0.6) already implemented
- No personalBalance parameter currently

From lib/features/groups/keys/group_keys.dart:
- Existing keys: groupBalancesLabel, eventsSection, membersAndBalancesSection, recentActivitySection
- Keys used in test assertions

From lib/shared/widgets/skeleton_loader.dart:
- SkeletonBlock, SkeletonBar, SkeletonCircle available for loading composites

From lib/core/theme/app_theme.dart:
- AppColors: space4-space32, radiusSmall=12, radiusMedium=16, radiusLarge=20, buttonHeight=52
- shadowFlat, shadowRaised, shadowFloating for elevations
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Infrastructure — Add animations package, extend GroupKeys, prepare tests (TDD red)</name>
  <files>pubspec.yaml, lib/features/groups/keys/group_keys.dart, test/features/group_detail_screen_test.dart</files>
  <read_first>
    - pubspec.yaml (check current dependencies)
    - lib/features/groups/keys/group_keys.dart (existing key definitions)
    - test/features/group_detail_screen_test.dart (existing test assertions to update)
    - lib/features/groups/screens/group_detail_screen.dart (understand current GroupBalanceHero usage)
  </read_first>
  <action>
    **Per D-05, D-07, D-08, D-09, D-10 and VALIDATION.md Wave 0:**

    1. **Add animations package**: Run `flutter pub add animations` (version ^2.0.0). This is needed by Plan 02 for OpenContainer but must be added once in Wave 0.

    2. **Extend GroupKeys** in `lib/features/groups/keys/group_keys.dart`:
       - `static const statsGrid = Key('group_stats_grid');`
       - `static const statYourBalance = Key('group_stat_your_balance');`
       - `static const statGroupTotal = Key('group_stat_group_total');`
       - `static const statActiveMembers = Key('group_stat_active_members');`
       - `static const statEvents = Key('group_stat_events');`
       - `static const settleUpCta = Key('group_settle_up_cta');`

    3. **Update existing tests** in `test/features/group_detail_screen_test.dart`:
       - Test `shows GroupBalanceHero when expenses exist`: Change assertion from `find.byKey(GroupKeys.groupBalancesLabel)` to `find.byKey(GroupKeys.statsGrid)` — stats grid replaces GroupBalanceHero.
       - Test `hides GroupBalanceHero when no expenses exist (D-19)`: Stats grid is always visible (per D-08), so update to: assert `find.byKey(GroupKeys.statsGrid)` is `findsOneWidget` and `find.byKey(GroupKeys.settleUpCta)` is `findsNothing` (hidden when zero balance per D-10).

    4. **Add new test cases** (TDD red phase — these will fail until Wave 1 implements):
       - `stats grid shows 4 stat tiles`: Pump with `_balancesWithExpenses`, assert all 4 stat keys findsOneWidget.
       - `shows settle-up CTA when balance is non-zero`: Pump with non-zero balance, assert `GroupKeys.settleUpCta` findsOneWidget.
       - `hides settle-up CTA when all balances are zero`: Pump with zero balance, assert `GroupKeys.settleUpCta` findsNothing.
       - `section order: Events before Members before Activity (D-09)`: Assert Y positions: eventsSection < membersAndBalancesSection < recentActivitySection.
       - `event card shows personal balance when provided`: Create EventCard with personalBalance parameter, assert balance text appears color-coded.
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla && flutter pub get && flutter analyze lib/features/groups/keys/group_keys.dart && flutter test test/features/group_detail_screen_test.dart 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - pubspec.yaml contains `animations:` dependency
    - group_keys.dart contains all 6 new Key constants (statsGrid, statYourBalance, statGroupTotal, statActiveMembers, statEvents, settleUpCta)
    - group_detail_screen_test.dart has updated GroupBalanceHero assertions to use statsGrid keys
    - group_detail_screen_test.dart contains 5 new test cases for stats grid, settle-up CTA, section order, and event card balance
    - All test files compile without analyzer errors
    - Some tests FAIL (expected TDD red phase) — implementation in Wave 1
  </acceptance_criteria>
  <done>GroupKeys extended with 6 new keys, animations package added to pubspec.yaml, existing tests updated for stats grid replacement, 5 new test cases added (compilable but failing red). flutter analyze passes on key/test files.</done>
</task>

<task type="auto">
  <name>Task 2: Create GroupStatsGrid widget and redesign EventCard with accent bar + inline balance</name>
  <files>lib/features/groups/widgets/group_stats_grid.dart, lib/features/events/widgets/event_card.dart</files>
  <read_first>
    - lib/features/groups/keys/group_keys.dart (keys from Task 1)
    - lib/features/events/widgets/event_card.dart (current layout — 48x48 icon container, Row, TapBounce)
    - lib/core/theme/tokens/color_tokens.dart (AppColorTokens.light — verify errorText, successText, textPrimary, textMuted, primary, surface, border)
    - lib/core/utils/app_formatters.dart (AppFormatters.formatCurrency)
    - lib/shared/widgets/smart_module_card.dart (reusable card pattern reference)
  </read_first>
  <action>
    **Per D-01, D-02, D-03, D-07, D-08:**

    **A. Create GroupStatsGrid** (`lib/features/groups/widgets/group_stats_grid.dart`):

    1. Create a `GroupStatsGrid` widget taking: `Decimal userNetBalance`, `Decimal groupTotal`, `int activeMembers`, `int eventCount`, `String currency`.
    2. Render `GridView.count(crossAxisCount: 2, shrinkWrap: true, physics: NeverScrollableScrollPhysics(), crossAxisSpacing: 8, mainAxisSpacing: 8, childAspectRatio: 1.6)` with 4 `_StatCard` children.
    3. Key the grid with `GroupKeys.statsGrid`.
    4. Tile 1 — "YOUR BALANCE": valueColor from Dart 3 switch on `userNetBalance.compareTo(Decimal.zero)`: `< 0 => AppColors.errorText`, `> 0 => AppColors.successText`, `_ => AppColors.textPrimary`. Key: `GroupKeys.statYourBalance`.
    5. Tile 2 — "GROUP TOTAL": default textPrimary color. Key: `GroupKeys.statGroupTotal`.
    6. Tile 3 — "ACTIVE MEMBERS": value = member count string. Key: `GroupKeys.statActiveMembers`.
    7. Tile 4 — "EVENTS": value = event count string. Key: `GroupKeys.statEvents`.
    8. Private `_StatCard`: Container with `AppColors.surface` bg, `AppColors.border` border, `BorderRadius.circular(AppColors.radiusSmall)`, padding `AppColors.space12`. Column: overline Text (fontSize: 10, w700, textMuted, letterSpacing: 0.5), SizedBox(height: 4), value Text (fontSize: 18, w800, valueColor).
    9. Use `AppFormatters.formatCurrency(amount, currency)` for financial values.

    **B. Redesign EventCard** (`lib/features/events/widgets/event_card.dart`):

    1. Add optional `Decimal? personalBalance` parameter to EventCard constructor.
    2. Replace existing Row layout (48x48 icon container + content) with IntrinsicHeight Row:
       - Left accent bar: `Container(width: 3.0, decoration: BoxDecoration(color: event.isPast ? AppColors.textMuted : AppColors.primary, borderRadius: BorderRadius.horizontal(left: Radius.circular(AppColors.radiusLarge))))`.
       - Expanded content area with padding: event name (titleMedium), date range (textSecondary), personal balance line, expense count line.
    3. Personal balance line (D-01): If personalBalance non-null and non-zero, Dart 3 switch: `< 0 => ('You owe OMR ${abs.toStringAsFixed(3)}', AppColors.errorText)`, `> 0 => ('You are owed OMR ${toStringAsFixed(3)}', AppColors.successText)`, `_ => ('Settled', AppColors.textSecondary)`. If null, show existing total spent line.
    4. Expense count: ` · N expenses` in textSecondary (NOT textMuted — functional text per WCAG).
    5. Keep existing `Opacity(opacity: 0.6)` wrapper for past events (D-02).
    6. Keep TapBounce wrapper.
    7. Remove type badge chip and 48x48 icon container — accent bar replaces type discrimination (D-03).
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla && flutter analyze lib/features/groups/widgets/group_stats_grid.dart lib/features/events/widgets/event_card.dart && flutter test test/features/group_detail_screen_test.dart 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - group_stats_grid.dart exists with GroupStatsGrid widget, 4 _StatCard children, all keyed correctly
    - GroupStatsGrid uses AppFormatters.formatCurrency for financial values
    - YOUR BALANCE tile color-codes via Dart 3 switch (errorText/successText/textPrimary)
    - event_card.dart has `Decimal? personalBalance` parameter
    - EventCard renders left accent bar (teal active, gray past)
    - EventCard shows personal balance text when personalBalance is non-null
    - EventCard no longer has 48x48 icon container or type badge chip
    - flutter analyze passes on both files
  </acceptance_criteria>
  <done>GroupStatsGrid renders 2x2 grid with 4 stat tiles (YOUR BALANCE color-coded, GROUP TOTAL, ACTIVE MEMBERS, EVENTS). EventCard shows left accent bar (teal/gray) with inline personal balance. Both files pass flutter analyze.</done>
</task>

<task type="auto">
  <name>Task 3: Rebuild GroupDetailScreen layout — stats grid, settle-up CTA, section reorder, skeleton loading</name>
  <files>lib/features/groups/screens/group_detail_screen.dart</files>
  <read_first>
    - lib/features/groups/screens/group_detail_screen.dart (full file — understand _buildContent, _buildStatsRow, _buildEventsSection, _buildMembersBalancesSection, _buildActivitySection, _buildLoading)
    - lib/features/groups/widgets/group_stats_grid.dart (Task 2 output — widget interface)
    - lib/features/events/widgets/event_card.dart (Task 2 output — personalBalance parameter)
    - lib/features/groups/providers/group_balance_provider.dart (understand balancesData structure, perEventBreakdown, currentUid)
    - lib/features/auth/providers/auth_provider.dart (how to get current user UID — currentUserIdProvider or FirebaseAuth.instance.currentUser?.uid)
    - lib/shared/widgets/skeleton_loader.dart (SkeletonBlock, SkeletonBar API)
  </read_first>
  <action>
    **Per D-07, D-09, D-10 and research findings:**

    1. **Remove old components**:
       - Remove `_buildStatsRow` call (member count + currency chips)
       - Remove `GroupBalanceHero` and `GroupSpendingStats` widgets and their conditional rendering
       - Remove imports for `group_balance_hero.dart` and `group_spending_stats.dart`
       - Add import for `GroupStatsGrid` from `../widgets/group_stats_grid.dart`

    2. **Add GroupStatsGrid above the fold**:
       ```dart
       GroupStatsGrid(
         userNetBalance: currentUserBalance?.netBalance ?? Decimal.zero,
         groupTotal: balancesData?.totalSpent ?? Decimal.zero,
         activeMembers: group.memberIds.length,
         eventCount: balancesData?.eventCount ?? 0,
         currency: group.currency,
       ),
       ```

    3. **Add settle-up CTA** (D-10): Full-width `ElevatedButton` with text "Settle Up", height `AppColors.buttonHeight` (52dp), backgroundColor `AppColors.primary`, keyed `GroupKeys.settleUpCta`. Show only when `currentUserBalance?.netBalance != null && currentUserBalance!.netBalance != Decimal.zero`. onPressed: `context.push('/group/$groupId/settle-up')`.

    4. **Reorder sections** (D-09): Change order from `[_buildMembersBalancesSection, _buildEventsSection, _buildInviteSection, _buildActivitySection]` to `[_buildEventsSection, _buildMembersBalancesSection, _buildInviteSection, _buildActivitySection]`.

    5. **Wire personal balance to EventCard**: In `_buildEventsSection`, obtain currentUid via `ref.watch(currentUserIdProvider)` or `FirebaseAuth.instance.currentUser?.uid`. For each event: `final eventBalance = balancesData?.perEventBreakdown?[currentUid]?[events[i].id]`. Pass `personalBalance: eventBalance` to each `EventCard`.

    6. **Replace loading state**: Replace `CircularProgressIndicator` in `_buildLoading` with skeleton composites: `SkeletonBlock` for stats grid area (2x2 grid), `SkeletonBar` at 52dp for CTA, section header `SkeletonBar` + event card `SkeletonBlock` items. Import `SkeletonLoader`.

    7. **File size target**: ~450-500 lines after removing GroupBalanceHero/GroupSpendingStats and extracting stats grid.
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla && flutter analyze lib/features/groups/screens/group_detail_screen.dart && flutter test test/features/group_detail_screen_test.dart && flutter test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - group_detail_screen.dart uses GroupStatsGrid (not GroupBalanceHero/GroupSpendingStats)
    - Settle-up CTA button appears keyed with GroupKeys.settleUpCta, conditionally visible
    - Section order: _buildEventsSection before _buildMembersBalancesSection
    - EventCard receives personalBalance parameter wired from balancesData.perEventBreakdown
    - Loading state uses SkeletonLoader composites (no CircularProgressIndicator)
    - GroupBalanceHero and GroupSpendingStats imports removed
    - All group_detail_screen_test.dart tests pass (green)
    - Full test suite passes
  </acceptance_criteria>
  <done>GroupDetailScreen redesigned: 2x2 stats grid above the fold with color-coded balance, settle-up CTA conditionally visible, Events > Members > Activity section order, event cards show inline personal balance, skeleton loading replaces spinner. All tests green.</done>
</task>

</tasks>
