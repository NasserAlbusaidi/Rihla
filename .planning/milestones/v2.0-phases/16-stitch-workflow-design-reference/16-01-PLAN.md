---
phase: 16-stitch-workflow-design-reference
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/16-stitch-workflow-design-reference/prompts/home-screen-prompt.md
  - .planning/phases/16-stitch-workflow-design-reference/prompts/group-detail-prompt.md
  - .planning/phases/16-stitch-workflow-design-reference/prompts/event-hub-prompt.md
  - .planning/phases/16-stitch-workflow-design-reference/post-generation-checklist.md
autonomous: true
requirements: [FOUND-03]

must_haves:
  truths:
    - "Three Stitch prompt files exist, each containing the complete earthy palette, spacing scale, typography, component inventory, and screen-specific layout description for all 4 states (loaded, empty, loading, error)"
    - "A post-generation checklist file exists with 4 mandatory checks: color token mapping, spacing consistency, component reuse, accessibility"
    - "All hex values in prompt files exactly match AppColorTokens.earthyLight values from Phase 15"
  artifacts:
    - path: ".planning/phases/16-stitch-workflow-design-reference/prompts/home-screen-prompt.md"
      provides: "Complete Stitch input prompt for Home screen (all 4 states per D-02, D-06)"
      contains: "#CC6B49"
    - path: ".planning/phases/16-stitch-workflow-design-reference/prompts/group-detail-prompt.md"
      provides: "Complete Stitch input prompt for Group Detail screen (all 4 states per D-02, D-07)"
      contains: "#CC6B49"
    - path: ".planning/phases/16-stitch-workflow-design-reference/prompts/event-hub-prompt.md"
      provides: "Complete Stitch input prompt for Event Hub screen (all 4 states per D-02, D-08)"
      contains: "#CC6B49"
    - path: ".planning/phases/16-stitch-workflow-design-reference/post-generation-checklist.md"
      provides: "Reusable 4-check verification checklist per D-16"
      contains: "Color Token Mapping"
  key_links:
    - from: "prompts/home-screen-prompt.md"
      to: "lib/core/theme/tokens/color_tokens.dart"
      via: "hex values copied verbatim from earthyLight"
      pattern: "#CC6B49.*#F2E8D6.*#2C1A0E"
    - from: "post-generation-checklist.md"
      to: "prompts/*.md"
      via: "checklist applied to Stitch output generated from each prompt"
      pattern: "Color Token Mapping.*Spacing Consistency.*Component Reuse.*Accessibility"
---

<objective>
Create three ready-to-paste Stitch input prompt files (one per screen: Home, Group Detail, Event Hub) and the post-generation review checklist.

Purpose: Provides the user with complete, copy-paste-ready prompts to run in Google Stitch. Each prompt contains the full earthy palette, spacing scale, typography, component inventory, and a detailed natural-language description of all 4 screen states (loaded, empty, loading/skeleton, error). The post-generation checklist ensures Stitch output is verified against the token system before being annotated into a spec.

Output: 3 prompt files in `.planning/phases/16-.../prompts/` + 1 checklist file
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-stitch-workflow-design-reference/16-CONTEXT.md
@.planning/phases/16-stitch-workflow-design-reference/16-RESEARCH.md

# Phase 15 summaries — token system this phase builds upon
@.planning/phases/15-design-token-system/15-01-SUMMARY.md

# Token source files — hex values must be copied verbatim into prompts
@lib/core/theme/tokens/color_tokens.dart
@lib/core/theme/tokens/spacing_tokens.dart
@lib/core/theme/tokens/shadow_tokens.dart

# Current screen implementations — prompts describe target state, informed by current state
@lib/features/home/screens/home_screen.dart
@lib/features/groups/screens/group_detail_screen.dart
@lib/features/events/screens/event_command_center.dart

# Shared widgets — component inventory for prompts
@lib/shared/widgets/module_header.dart
@lib/shared/widgets/empty_state_view.dart
@lib/shared/widgets/smart_module_card.dart
@lib/shared/widgets/skeleton_loader.dart
@lib/shared/widgets/offline_banner.dart
@lib/shared/widgets/app_tab_bar.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create three Stitch input prompt files</name>
  <files>
    .planning/phases/16-stitch-workflow-design-reference/prompts/home-screen-prompt.md
    .planning/phases/16-stitch-workflow-design-reference/prompts/group-detail-prompt.md
    .planning/phases/16-stitch-workflow-design-reference/prompts/event-hub-prompt.md
  </files>
  <read_first>
    lib/core/theme/tokens/color_tokens.dart
    lib/core/theme/tokens/spacing_tokens.dart
    lib/core/theme/tokens/shadow_tokens.dart
    lib/core/theme/app_theme.dart
    lib/features/home/screens/home_screen.dart
    lib/features/groups/screens/group_detail_screen.dart
    lib/features/events/screens/event_command_center.dart
    lib/shared/widgets/module_header.dart
    lib/shared/widgets/empty_state_view.dart
    lib/shared/widgets/smart_module_card.dart
    lib/shared/widgets/skeleton_loader.dart
    lib/shared/widgets/offline_banner.dart
    lib/shared/widgets/app_tab_bar.dart
    .planning/phases/16-stitch-workflow-design-reference/16-CONTEXT.md
    .planning/phases/16-stitch-workflow-design-reference/16-RESEARCH.md
  </read_first>
  <action>
    Create the `prompts/` directory under `.planning/phases/16-stitch-workflow-design-reference/`.

    Create three markdown files — each is a complete, ready-to-paste Stitch input prompt. Every prompt file follows the same 6-section structure defined in the RESEARCH.md "Pattern 1: Stitch Input Prompt Structure". The sections are:

    **Section A — Palette** (identical across all 3 files, copied verbatim from AppColorTokens.earthyLight):
    ```
    Palette:
    - Primary (terracotta): #CC6B49
    - Background (sand): #F2E8D6
    - Card surface (warm white): #FFF9F2
    - Input fill: #F5EDE1
    - Border: #E5D5C0
    - Text primary (dark brown): #2C1A0E
    - Text secondary: #6B5B4E
    - Text muted: #A89888
    - Text on primary (white): #FFFFFF
    - Success display: #10B981
    - Success text (WCAG safe): #047857
    - Error display: #EF4444
    - Error text (WCAG safe): #B91C1C
    - Module Ledger: #CC6B49  |  Module Ledger light: #ECD5C0
    - Module Gear: #7A8C5E    |  Module Gear light: #E0DAC4
    - Module Logistics: #5B7B8C | Module Logistics light: #DBD7CA
    - Module Vault: #8B7355   |  Module Vault light: #E2D6C2
    - Module Activity: #A67C5B | Module Activity light: #E6D7C3
    - Module Memories: #9B7A5C | Module Memories light: #E4D7C3
    - Header gradient start: #2C1A0E  |  end: #3D2B1E
    ```

    **Section B — Spacing scale** (identical across all 3 files):
    ```
    Spacing: 4dp, 8dp, 12dp, 16dp (base), 20dp, 24dp, 32dp
    Border radii: 12dp (chips/tags), 16dp (buttons/inputs), 20dp (cards/sheets)
    Button height: 52dp
    Touch targets: minimum 48dp
    ```

    **Section C — Typography** (identical across all 3 files):
    Plus Jakarta Sans. Weight hierarchy: 800 headings, 700 subtitles, 600 body emphasis, 400 body.

    **Section D — Component inventory** (identical across all 3 files):
    ```
    Existing shared components:
    - ModuleHeader: dark gradient header (#2C1A0E -> #3D2B1E), white title + subtitle, optional action icons
    - AppTabBar: horizontal tabs with gradient pill indicator on active tab
    - EmptyStateView: centered icon + title + message + optional CTA button
    - SmartModuleCard: list-style card with icon (44x44, colored background), title, subtitle/summary, chevron
    - OfflineBanner: thin connectivity indicator strip
    - SkeletonLoader: grey placeholder blocks for loading states
    ```

    **Section E — Screen description** (UNIQUE per file — this is where the three files diverge):

    For **home-screen-prompt.md** (per D-06):
    - State 1 (Loaded): Balance hero card at top showing net cross-group balance (e.g., "You owe OMR 15.250" in error text red, or "You are owed OMR 8.500" in success text green, or "All settled" in text secondary gray). Below: scrollable list of group cards (each card: group name, member count chip, inline balance summary, last activity date). Below cards: recent activity strip (last 3 activity items). FAB tray visible without scrolling: 4 circular buttons (Add Expense, Settle Up, Invite, Activity) in a horizontal row above the main FAB.
    - State 2 (Empty): EmptyStateView pattern — centered illustration placeholder + "Create your first group" heading + "Plan trips, track expenses, and settle up with friends" body + single CTA button "Create Group" in primary terracotta.
    - State 3 (Loading/Skeleton): Balance hero skeleton (rounded rect, pulsing) + 3 group card skeletons (matching card shape with grey placeholder lines).
    - State 4 (Error): OfflineBanner at top + EmptyStateView with error icon + "Something went wrong" heading + "Check your connection and try again" body + "Retry" CTA button.
    - Generic placeholder data per D-04: "User 1", "Group A", "Group B", "OMR 10.000", "OMR 25.500".

    For **group-detail-prompt.md** (per D-07):
    - State 1 (Loaded): ModuleHeader (dark gradient, group name "Group A", settings gear icon). Below: summary strip with member count chip ("4 members") + currency chip ("OMR"). Balance hero card: total group spend ("OMR 125.500"), current user net balance (green/red/gray), settle-up CTA button. Member balances section: compact list showing each member name + their balance (color-coded). Event timeline: cards for upcoming events (full color, type accent) and past events (muted/desaturated). Each event card shows: event name, type icon, date, expense summary. Below events: invite code chip. Bottom: recent activity strip (last 3 items).
    - State 2 (Empty — no events yet): ModuleHeader + summary strip + balance hero showing "OMR 0.000" + empty events section using EmptyStateView ("Create your first event" + CTA). Member list still shows.
    - State 3 (Loading/Skeleton): ModuleHeader (real, not skeleton) + skeleton blocks for summary strip, balance hero, member list, event cards.
    - State 4 (Error): ModuleHeader (real) + OfflineBanner + EmptyStateView with error messaging.
    - Event type color mapping for event cards: Trip = #CC6B49 (terracotta), Camping = #7A8C5E (olive), Day Out = #5B7B8C (teal), Dinner = #8B7355 (bronze), Custom = #A67C5B (caramel).
    - Dense dashboard layout per D-07: all key data visible without scrolling on ~390px screen.

    For **event-hub-prompt.md** (per D-08):
    - State 1 (Loaded): ModuleHeader (dark gradient, event name "Trip A", subtitle "Trip - Group A", options icon). Expense hero card: large total amount ("OMR 45.500") in terracotta primary, participant count, date range. Module card grid: 6 SmartModuleCard items, each using its CORRECT intended accent color (per Phase 15 tokens, NOT current code colors):
      - Ledger (#CC6B49 / #ECD5C0): "3 expenses - OMR 45.500"
      - Gear (#7A8C5E / #E0DAC4): "5 items - 2 unclaimed"
      - Logistics (#5B7B8C / #DBD7CA): "2 transport entries"
      - Vault (#8B7355 / #E2D6C2): "4 documents"
      - Activity (#A67C5B / #E6D7C3): "12 actions today"
      - Memories (#9B7A5C / #E4D7C3): "8 photos"
    Each module card: 44x44 circle icon container with 12% module-color fill, icon in accent color, title, summary text, chevron. FAB: add expense shortcut.
    - State 2 (Empty — new event, no data yet): ModuleHeader + expense hero showing "OMR 0.000" + module cards with empty state summaries ("No expenses yet", "No items yet", etc.) using 6% opacity icon fill instead of 12%.
    - State 3 (Loading/Skeleton): ModuleHeader (real) + expense hero skeleton + 6 module card skeletons.
    - State 4 (Error): ModuleHeader (real) + OfflineBanner + EmptyStateView with error messaging.

    **Section F — Constraints** (identical across all 3 files):
    ```
    Target width: ~390px (iPhone 14 / Pixel 7)
    Single breakpoint only
    Generic placeholder data: "User 1", "User 2", "Group A", "Trip A", "OMR 10.000"
    Dense information density: dashboard-style, minimal scrolling for key data
    ```

    CRITICAL CONSTRAINTS:
    - All hex values MUST exactly match the values in `AppColorTokens.earthyLight` from `color_tokens.dart` — do NOT approximate or modify any color.
    - Use the INTENDED module color mapping (Ledger=#CC6B49, Gear=#7A8C5E, etc.) from Phase 15 tokens, NOT the current buggy code mapping found in event_module_list.dart.
    - textMuted (#A89888) must be noted as "decorative only — do not use for functional text" wherever it appears.
    - Per D-04, use generic placeholder content only: "User 1", "User 2", "Group A", "Trip A", "OMR 10.000".
    - Per D-05, single breakpoint ~390px width only.
  </action>
  <verify>
    <automated>
      test -f .planning/phases/16-stitch-workflow-design-reference/prompts/home-screen-prompt.md && \
      test -f .planning/phases/16-stitch-workflow-design-reference/prompts/group-detail-prompt.md && \
      test -f .planning/phases/16-stitch-workflow-design-reference/prompts/event-hub-prompt.md && \
      grep -q "#CC6B49" .planning/phases/16-stitch-workflow-design-reference/prompts/home-screen-prompt.md && \
      grep -q "#CC6B49" .planning/phases/16-stitch-workflow-design-reference/prompts/group-detail-prompt.md && \
      grep -q "#CC6B49" .planning/phases/16-stitch-workflow-design-reference/prompts/event-hub-prompt.md && \
      grep -q "#F2E8D6" .planning/phases/16-stitch-workflow-design-reference/prompts/home-screen-prompt.md && \
      grep -q "4dp.*8dp.*12dp.*16dp" .planning/phases/16-stitch-workflow-design-reference/prompts/home-screen-prompt.md && \
      grep -q "ModuleHeader" .planning/phases/16-stitch-workflow-design-reference/prompts/home-screen-prompt.md && \
      grep -q "EmptyStateView" .planning/phases/16-stitch-workflow-design-reference/prompts/home-screen-prompt.md && \
      grep -q "balance hero" .planning/phases/16-stitch-workflow-design-reference/prompts/home-screen-prompt.md && \
      grep -q "Module.*grid\|module.*card" .planning/phases/16-stitch-workflow-design-reference/prompts/event-hub-prompt.md && \
      grep -q "settle-up\|Settle Up\|settle up" .planning/phases/16-stitch-workflow-design-reference/prompts/group-detail-prompt.md && \
      grep -q "~390px" .planning/phases/16-stitch-workflow-design-reference/prompts/home-screen-prompt.md && \
      echo "ALL CHECKS PASSED" || echo "CHECKS FAILED"
    </automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/16-stitch-workflow-design-reference/prompts/home-screen-prompt.md` exists and contains: "#CC6B49", "#F2E8D6", "#2C1A0E", "balance hero", "group card", "FAB tray", "EmptyStateView", "SkeletonLoader", "OfflineBanner", "~390px", "Plus Jakarta Sans", "4dp" spacing values, "ModuleHeader"
    - `.planning/phases/16-stitch-workflow-design-reference/prompts/group-detail-prompt.md` exists and contains: "#CC6B49", "ModuleHeader", "member balances", "event timeline", "settle-up", "invite code", "EmptyStateView", "dense dashboard"
    - `.planning/phases/16-stitch-workflow-design-reference/prompts/event-hub-prompt.md` exists and contains: "#CC6B49", "#7A8C5E", "#5B7B8C", "#8B7355", "#A67C5B", "#9B7A5C", "ModuleHeader", "SmartModuleCard", "Ledger", "Gear", "Logistics", "Vault", "Activity", "Memories", "44x44" or "44×44"
    - All three files contain sections A through F (Palette, Spacing, Typography, Component inventory, Screen description, Constraints)
    - All three files use "User 1", "User 2", "Group A", "Trip A" placeholder data per D-04
    - No hex value in any prompt file differs from the values in AppColorTokens.earthyLight
    - Event Hub prompt uses Ledger=#CC6B49 (NOT #7A8C5E which is the current buggy code mapping)
  </acceptance_criteria>
  <done>Three complete Stitch prompt files exist in prompts/, each containing full palette, spacing, typography, component inventory, 4 screen states, and constraints. All hex values match Phase 15 tokens exactly.</done>
</task>

<task type="auto">
  <name>Task 2: Create post-generation review checklist</name>
  <files>
    .planning/phases/16-stitch-workflow-design-reference/post-generation-checklist.md
  </files>
  <read_first>
    .planning/phases/16-stitch-workflow-design-reference/16-CONTEXT.md
    .planning/phases/16-stitch-workflow-design-reference/16-RESEARCH.md
    lib/core/theme/tokens/color_tokens.dart
  </read_first>
  <action>
    Create the post-generation review checklist at `.planning/phases/16-stitch-workflow-design-reference/post-generation-checklist.md`.

    This file is applied every time Stitch output is reviewed — for all 3 screens in this phase, and reused by Phases 20-22. It implements the 4 mandatory checks from D-16.

    The file must contain:

    **Title:** `# Post-Generation Review Checklist`

    **Preamble:** Brief explanation that this checklist is applied to every Stitch-generated screen mockup before the output is annotated into a visual specification. Reference D-16 and D-24 through D-26.

    **Check 1: Color Token Mapping** (per D-16.1, D-24)
    - Fillable table with columns: Visual Area | Color in Stitch Output | Nearest AppColors Token | Hex Match? | Action Needed
    - Instruction: "For every distinct color in the Stitch output, map it to the nearest AppColors.* constant. If no exact hex match exists, snap to the nearest token (D-24 — Stitch adapts to tokens, never the reverse)."
    - Checkbox: "All colors map to existing AppColors/AppColorTokens fields"
    - Checkbox: "No color exists that is not in the token system (flag if found)"
    - Checkbox: "All text-on-background pairs are in the Phase 15 WCAG-verified set"
    - Reference table of all 30 token hex values (copy from RESEARCH.md "Complete Color Token Inventory" — include token field name, hex value, and AppColors alias for each)

    **Check 2: Spacing Consistency** (per D-16.2)
    - Checkbox: "All padding/gap values align to 4dp grid"
    - Checkbox: "Named using token names: space4, space8, space12, space16, space20, space24, space32"
    - Checkbox: "No non-token spacing values (e.g., 6dp, 10dp, 15dp)"
    - Checkbox: "Border radii use only: 12dp (radiusSmall), 16dp (radiusMedium), 20dp (radiusLarge)"
    - Checkbox: "Button heights are 52dp"
    - Checkbox: "Touch targets are >= 48dp"

    **Check 3: Component Reuse** (per D-16.3)
    - Checkbox: "ModuleHeader used for dark gradient header sections (not custom gradient)"
    - Checkbox: "EmptyStateView used for empty states (not custom-built empty views)"
    - Checkbox: "SmartModuleCard used for module list items in Event Hub (not custom cards)"
    - Checkbox: "SkeletonLoader used for loading states (not custom shimmer)"
    - Checkbox: "OfflineBanner included in error state mockups"
    - Checkbox: "AppTabBar used for tab layouts (if applicable)"

    **Check 4: Accessibility** (per D-16.4, D-25, D-26)
    - Checkbox: "All body text uses textPrimary (#2C1A0E) or textSecondary (#6B5B4E) on sand/surface — verified AA"
    - Checkbox: "Success text uses successText (#047857, 4.51:1), NOT success (#10B981) for readable text"
    - Checkbox: "Error text uses errorText (#B91C1C, 5.33:1), NOT error (#EF4444) for readable text"
    - Checkbox: "textMuted (#A89888) used decoratively only — NEVER for functional labels, amounts, or actions"
    - Checkbox: "textOnPrimary (#FFFFFF) used for all text on primary (#CC6B49) backgrounds"
    - Checkbox: "All interactive elements are visually distinct from non-interactive"
    - Checkbox: "All touch targets >= 48dp"
    - Include the pre-computed WCAG verified pairs:
      - textPrimary (#2C1A0E) on sand (#F2E8D6): 13.71:1 (AAA)
      - textSecondary (#6B5B4E) on sand: 5.35:1 (AA)
      - successText (#047857) on sand: 4.51:1 (AA body)
      - errorText (#B91C1C) on sand: 5.33:1 (AA body)
      - textOnPrimary (#FFFFFF) on primary (#CC6B49): 3.64:1 (AA large)
    - Note: "Any color combination NOT in the verified set above requires manual WCAG ratio calculation before inclusion in the spec (per D-26)."

    **Token Gap Log** section at the bottom:
    - Fillable table: Gap Description | Structural or Cosmetic? | Action (add token now / defer to Phase NN)
    - Instruction: "Per Claude's Discretion in CONTEXT.md, structural gaps (missing token that blocks spec completeness) are added to color_tokens.dart immediately. Cosmetic gaps (nice-to-have refinement) are logged here and deferred to the implementation phase."
  </action>
  <verify>
    <automated>
      test -f .planning/phases/16-stitch-workflow-design-reference/post-generation-checklist.md && \
      grep -q "Color Token Mapping" .planning/phases/16-stitch-workflow-design-reference/post-generation-checklist.md && \
      grep -q "Spacing Consistency" .planning/phases/16-stitch-workflow-design-reference/post-generation-checklist.md && \
      grep -q "Component Reuse" .planning/phases/16-stitch-workflow-design-reference/post-generation-checklist.md && \
      grep -q "Accessibility" .planning/phases/16-stitch-workflow-design-reference/post-generation-checklist.md && \
      grep -q "#CC6B49" .planning/phases/16-stitch-workflow-design-reference/post-generation-checklist.md && \
      grep -q "textMuted" .planning/phases/16-stitch-workflow-design-reference/post-generation-checklist.md && \
      grep -q "13.71:1" .planning/phases/16-stitch-workflow-design-reference/post-generation-checklist.md && \
      grep -q "Token Gap" .planning/phases/16-stitch-workflow-design-reference/post-generation-checklist.md && \
      echo "ALL CHECKS PASSED" || echo "CHECKS FAILED"
    </automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/16-stitch-workflow-design-reference/post-generation-checklist.md` exists
    - File contains exactly 4 check sections: "Color Token Mapping", "Spacing Consistency", "Component Reuse", "Accessibility"
    - Check 1 includes all 30 token hex values from AppColorTokens.earthyLight
    - Check 4 includes all 5 WCAG verified pairs with exact ratios: 13.71:1, 5.35:1, 4.51:1, 5.33:1, 3.64:1
    - File contains "textMuted" with "decorative only" warning
    - File contains a "Token Gap Log" section with fillable table
    - All checkbox items use `- [ ]` markdown format
    - File references D-16, D-24, D-25, D-26 decision IDs
  </acceptance_criteria>
  <done>Post-generation checklist exists with 4 mandatory checks (color token mapping, spacing consistency, component reuse, accessibility), complete token reference table, WCAG verified pairs, and token gap log. Ready to be applied against Stitch output for all 3 screens.</done>
</task>

</tasks>

<verification>
- All 3 prompt files exist in `.planning/phases/16-stitch-workflow-design-reference/prompts/`
- Each prompt file contains the complete palette section with all hex values matching AppColorTokens.earthyLight exactly
- Each prompt file describes all 4 states per D-02 (loaded, empty, loading/skeleton, error)
- Each prompt file contains the component inventory section referencing ModuleHeader, EmptyStateView, SmartModuleCard, SkeletonLoader, OfflineBanner
- Event Hub prompt uses INTENDED module color mapping (Ledger=#CC6B49), not current code mapping
- Post-generation checklist contains all 4 mandatory checks from D-16
- Post-generation checklist includes complete token reference table and WCAG verified pairs
</verification>

<success_criteria>
- Three ready-to-paste Stitch prompt files exist that the user can copy directly into Google Stitch
- Post-generation checklist is complete and reusable for Phases 20-22
- All hex values match Phase 15 tokens exactly (zero approximations)
- All prompts follow the 6-section structure (Palette, Spacing, Typography, Components, Screen, Constraints)
</success_criteria>

<output>
After completion, create `.planning/phases/16-stitch-workflow-design-reference/16-01-SUMMARY.md`
</output>
