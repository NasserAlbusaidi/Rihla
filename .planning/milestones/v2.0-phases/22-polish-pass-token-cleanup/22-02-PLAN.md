---
phase: 22-polish-pass-token-cleanup
plan: 02
type: execute
wave: 2
depends_on: [22-01]
files_modified:
  - lib/features/ledger/screens/add_expense_screen.dart
  - lib/features/ledger/screens/settle_up_screen.dart
  - lib/features/groups/screens/join_group_screen.dart
  - lib/features/home/widgets/balance_hero_card.dart
  - lib/features/ledger/widgets/ledger_hero_card.dart
autonomous: true
requirements: [PLSH-01, PLSH-04]

must_haves:
  truths:
    - "Adding an expense triggers HapticService.success() immediately after validation passes, before async write"
    - "Recording a settlement triggers HapticService.success() immediately on tap, before async write"
    - "Joining a group triggers HapticService.success() instead of raw HapticFeedback.mediumImpact()"
    - "BalanceHeroCard balance amount animates smoothly when the value changes (no jump cut)"
    - "LedgerHeroCard balance amount animates smoothly when the value changes (no jump cut)"
    - "Balance color transitions green/red/gray based on the animated value sign during interpolation"
  artifacts:
    - path: "lib/features/ledger/screens/add_expense_screen.dart"
      provides: "HapticService.success() call in _submit()"
      contains: "HapticService.success()"
    - path: "lib/features/ledger/screens/settle_up_screen.dart"
      provides: "HapticService.success() call in submit handler"
      contains: "HapticService.success()"
    - path: "lib/features/groups/screens/join_group_screen.dart"
      provides: "HapticService.success() replacing raw HapticFeedback"
      contains: "HapticService.success()"
    - path: "lib/features/home/widgets/balance_hero_card.dart"
      provides: "AnimatedCurrencyText integration for home balance"
      contains: "AnimatedCurrencyText"
    - path: "lib/features/ledger/widgets/ledger_hero_card.dart"
      provides: "AnimatedCurrencyText integration for ledger balance"
      contains: "AnimatedCurrencyText"
  key_links:
    - from: "lib/features/home/widgets/balance_hero_card.dart"
      to: "lib/shared/widgets/animated_currency_text.dart"
      via: "import and usage of AnimatedCurrencyText widget"
      pattern: "AnimatedCurrencyText"
    - from: "lib/features/ledger/widgets/ledger_hero_card.dart"
      to: "lib/shared/widgets/animated_currency_text.dart"
      via: "import and usage of AnimatedCurrencyText widget"
      pattern: "AnimatedCurrencyText"
---

<objective>
Add haptic feedback to the 3 required write actions and integrate animated balance counters into BalanceHeroCard and LedgerHeroCard.

Purpose: Satisfies PLSH-01 (haptic feedback on primary write actions) and PLSH-04 (animated balance counters). These are the two most user-facing polish improvements.

Output: 3 screens with haptic feedback on success actions, 2 hero cards with smooth animated number transitions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/22-polish-pass-token-cleanup/22-CONTEXT.md
@.planning/phases/22-polish-pass-token-cleanup/RESEARCH.md
@.planning/phases/22-polish-pass-token-cleanup/22-01-SUMMARY.md

@lib/core/services/haptic_service.dart
@lib/features/ledger/screens/add_expense_screen.dart
@lib/features/ledger/screens/settle_up_screen.dart
@lib/features/groups/screens/join_group_screen.dart
@lib/features/home/widgets/balance_hero_card.dart
@lib/features/ledger/widgets/ledger_hero_card.dart
@lib/shared/widgets/animated_currency_text.dart

<interfaces>
From lib/core/services/haptic_service.dart:
```dart
class HapticService {
  static Future<void> success() async; // double medium impact, 100ms gap
  static Future<void> lightClick() async;
  static Future<void> warning() async;
  static Future<void> selection() async;
  static Future<void> medium() async;
}
```

From lib/shared/widgets/animated_currency_text.dart (created in Plan 01):
```dart
class AnimatedCurrencyText extends StatefulWidget {
  final Decimal value;
  final String currency;
  final TextStyle? style;
  final Duration duration; // default 600ms

  const AnimatedCurrencyText({
    super.key,
    required this.value,
    required this.currency,
    this.style,
    this.duration = const Duration(milliseconds: 600),
  });
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add HapticService.success() to 3 write actions</name>
  <files>
    lib/features/ledger/screens/add_expense_screen.dart
    lib/features/ledger/screens/settle_up_screen.dart
    lib/features/groups/screens/join_group_screen.dart
  </files>
  <action>
Per D-01, D-02, D-03: Add `HapticService.success()` to exactly 3 actions, fired immediately on tap after validation passes (not after async write).

**1. add_expense_screen.dart — `_submit()` method:**
Read the file to find the `_submit()` method. Add `HapticService.success();` (no await — fire and forget) AFTER validation passes (after amount parsing and > 0 check) but BEFORE the async Firestore write. The haptic import (`import '../../../core/services/haptic_service.dart'`) is already present in the file. Pattern:
```dart
// After validation passes...
HapticService.success(); // D-02: fire on tap, not after write (D-03)
// ... async write follows
```

**2. settle_up_screen.dart — submit handler:**
Read the file to find the settlement submission logic. Look for the method that records a settlement (likely `_recordSettlement` or similar). Add `HapticService.success();` at the top of the method, after any validation checks but before the async Firestore call. Add the import for `HapticService` if not already present.

**3. join_group_screen.dart — join handler:**
Read the file. Line 59 has `await HapticFeedback.mediumImpact();`. Replace this single `HapticFeedback.mediumImpact()` call with `HapticService.success();` (no await needed). Ensure the file imports `haptic_service.dart`. Remove the `flutter/services.dart` import ONLY if `HapticFeedback` is no longer used elsewhere in the file (check for other usages first). Per D-02: HapticService.success() gives the double-tap "done" feel vs the single mediumImpact.
  </action>
  <verify>
    <automated>flutter analyze lib/features/ledger/screens/add_expense_screen.dart lib/features/ledger/screens/settle_up_screen.dart lib/features/groups/screens/join_group_screen.dart 2>&1 | tail -5</automated>
  </verify>
  <done>All 3 write actions fire HapticService.success() immediately on validated tap. No HapticFeedback.mediumImpact() remains in join_group_screen. Zero analyze errors.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate AnimatedCurrencyText into BalanceHeroCard and LedgerHeroCard</name>
  <files>
    lib/features/home/widgets/balance_hero_card.dart
    lib/features/ledger/widgets/ledger_hero_card.dart
  </files>
  <action>
Per D-13, D-14, D-15: Replace static balance text with AnimatedCurrencyText in both hero cards.

**BalanceHeroCard** (`lib/features/home/widgets/balance_hero_card.dart`):

Currently a `ConsumerWidget`. The balance amount is displayed as a static `Text` widget with the formatted OMR amount (line ~86-91). The color is determined by the Dart 3 switch on `net.compareTo(Decimal.zero)`.

Replace the static amount `Text` widget with `AnimatedCurrencyText`:
```dart
AnimatedCurrencyText(
  value: net,
  currency: 'OMR',
  style: const TextStyle(
    fontSize: 20,
    fontWeight: FontWeight.w700,
  ),
)
```

Remove the static `amountText` from the switch expression (keep color, icon, descriptionText). The `AnimatedCurrencyText` handles color internally based on the animated value sign. Remove the `color` parameter from the amount Text style since AnimatedCurrencyText manages it.

Note: `BalanceHeroCard` does NOT need to become `ConsumerStatefulWidget` — the `AnimatedCurrencyText` widget itself is stateful and tracks `_previousValue` internally. The `ConsumerWidget` just passes the Decimal to it.

Add import: `import '../../../shared/widgets/animated_currency_text.dart';`

**LedgerHeroCard** (`lib/features/ledger/widgets/ledger_hero_card.dart`):

Currently a `StatelessWidget` receiving `netBalance: Decimal` and `currency: String`. The balance text is at line ~85-93 showing `'$currency $formattedBalance'` with `_balanceColor`.

Replace the balance amount `Text` widget (the one showing the formatted balance, not the "YOUR BALANCE" label) with `AnimatedCurrencyText`:
```dart
AnimatedCurrencyText(
  value: netBalance,
  currency: currency,
  style: const TextStyle(
    fontSize: 28,
    fontWeight: FontWeight.w600,
    letterSpacing: -0.5,
  ),
)
```

Remove the `_balanceColor` getter since AnimatedCurrencyText handles color internally.

Note: Like BalanceHeroCard, `LedgerHeroCard` does NOT need to become `StatefulWidget` — the `AnimatedCurrencyText` is stateful internally. The `StatelessWidget` just passes props through.

Add import: `import '../../../shared/widgets/animated_currency_text.dart';`
  </action>
  <verify>
    <automated>flutter analyze lib/features/home/widgets/balance_hero_card.dart lib/features/ledger/widgets/ledger_hero_card.dart 2>&1 | tail -5</automated>
  </verify>
  <done>BalanceHeroCard and LedgerHeroCard use AnimatedCurrencyText for balance amounts. Numbers animate smoothly with 600ms easeOutCubic. Color tracks sign of animated value. Zero analyze errors.</done>
</task>

</tasks>

<verification>
flutter test test/features/home/ test/features/ledger/ test/features/groups/ 2>&1 | tail -20
flutter analyze lib/features/ledger/ lib/features/home/widgets/ lib/features/groups/screens/ 2>&1 | tail -5
</verification>

<success_criteria>
1. `grep -r "HapticService.success" lib/features/ledger/screens/add_expense_screen.dart` returns at least 1 match
2. `grep -r "HapticService.success" lib/features/ledger/screens/settle_up_screen.dart` returns at least 1 match
3. `grep -r "HapticService.success" lib/features/groups/screens/join_group_screen.dart` returns at least 1 match (replacing the raw HapticFeedback call)
4. `grep -r "AnimatedCurrencyText" lib/features/home/widgets/balance_hero_card.dart` returns at least 1 match
5. `grep -r "AnimatedCurrencyText" lib/features/ledger/widgets/ledger_hero_card.dart` returns at least 1 match
6. `flutter analyze` passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/22-polish-pass-token-cleanup/22-02-SUMMARY.md`
</output>
