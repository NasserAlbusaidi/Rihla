---
phase: 21-module-screens-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/theme/app_theme.dart
  - lib/shared/widgets/empty_state_view.dart
  - lib/shared/widgets/skeleton_loader.dart
  - lib/shared/widgets/dot_step_indicator.dart
autonomous: true
requirements: [SCRN-03, SCRN-04, SCRN-05, SCRN-06]

must_haves:
  truths:
    - "AppColors.sandLight, AppColors.terracotta, and AppColors.warmGray are available as static const Color values"
    - "AppColors facade includes module color aliases (moduleLedger, moduleGearLight, etc.) so module screens reference AppColors.* not AppColorTokens.light.*"
    - "InputDecorationTheme uses sand fill (#F5EDE1), warm gray enabled border (#E5D5C0), terracotta focused border (#CC6B49), dark brown label (#2C1A0E), sand gray hint (#A89888), 12dp border radius"
    - "EmptyStateView renders a 72dp gradient circle when accentGradient parameter is provided"
    - "SkeletonLoader.photoGrid() factory exists and renders a 3-column grid of skeleton blocks"
    - "DotStepIndicator widget renders filled/outlined/checked dots based on step state"
  artifacts:
    - path: "lib/core/theme/app_theme.dart"
      provides: "Missing color tokens (sandLight, terracotta, warmGray) + module color facade aliases + updated InputDecorationTheme"
      contains: "static const Color sandLight"
    - path: "lib/shared/widgets/empty_state_view.dart"
      provides: "Gradient circle icon container via accentGradient parameter"
      contains: "accentGradient"
    - path: "lib/shared/widgets/skeleton_loader.dart"
      provides: "photoGrid factory for Memories loading state"
      contains: "factory SkeletonLoader.photoGrid"
    - path: "lib/shared/widgets/dot_step_indicator.dart"
      provides: "Terracotta step/page dot indicator"
      contains: "class DotStepIndicator"
  key_links:
    - from: "lib/shared/widgets/empty_state_view.dart"
      to: "lib/core/theme/app_theme.dart"
      via: "AppColors.* token references"
      pattern: "AppColors\\."
    - from: "lib/shared/widgets/dot_step_indicator.dart"
      to: "lib/core/theme/app_theme.dart"
      via: "AppColors.terracotta for dot fill"
      pattern: "AppColors\\.terracotta"
---

<objective>
Add missing color tokens, upgrade shared widgets (EmptyStateView, SkeletonLoader, DotStepIndicator), and update the global InputDecorationTheme. This is the Wave 0 foundation that all subsequent Phase 21 plans depend on.

Purpose: Without these token additions and widget upgrades, no module screen or form flow can compile against the new design language. This plan resolves all token gaps and provides the upgraded shared components.
Output: Updated app_theme.dart with 3 new color tokens + module facade aliases + InputDecorationTheme, upgraded EmptyStateView with gradient circle, new SkeletonLoader.photoGrid factory, new DotStepIndicator widget.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-module-screens-redesign/21-CONTEXT.md
@.planning/phases/21-module-screens-redesign/21-RESEARCH.md
@.planning/phases/21-module-screens-redesign/21-UI-SPEC.md

<interfaces>
<!-- Token system interfaces the executor needs -->

From lib/core/theme/app_theme.dart (AppColors class):
```dart
class AppColors {
  static const Color primary = Color(0xFF0D7B74);
  static const Color primaryLight = Color(0xFFE6F5F3);
  static const Color surface = Color(0xFFF8F9FA);
  static const Color surfaceLight = Color(0xFFF3F4F6);
  static const Color border = Color(0xFFE5E7EB);
  static const Color borderLight = Color(0xFFF3F4F6);
  static const Color textPrimary = Color(0xFF111827);
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color textMuted = Color(0xFF9CA3AF);
  static const Color textOnPrimary = Color(0xFFFFFFFF);
  static const Color errorText = Color(0xFFB91C1C);
  static const Color successText = Color(0xFF047857);
  static const Color error = Color(0xFFEF4444);
  // ... spacing, radius, shadows, gradients follow
}
```

From lib/core/theme/tokens/color_tokens.dart (module colors):
```dart
// AppColorTokens.light values:
moduleLedger: Color(0xFF0D7B74),
moduleLedgerLight: Color(0xFFE6F5F3),
moduleGear: Color(0xFF6B7280),
moduleGearLight: Color(0xFFF3F4F6),
moduleLogistics: Color(0xFF6B7280),
moduleLogisticsLight: Color(0xFFF3F4F6),
moduleVault: Color(0xFF6B7280),
moduleVaultLight: Color(0xFFF3F4F6),
moduleActivity: Color(0xFF6B7280),
moduleActivityLight: Color(0xFFF3F4F6),
moduleMemories: Color(0xFF6B7280),
moduleMemoriesLight: Color(0xFFF3F4F6),
selectionFill: Color(0xFFE6F5F3),
cardSurface: Color(0xFFF8F9FA),
```

From lib/shared/widgets/empty_state_view.dart:
```dart
class EmptyStateView extends StatelessWidget {
  final IconData icon;
  final String title;
  final String message;
  final String? actionLabel;
  final VoidCallback? onAction;
  final Color? iconColor;
  // ...
}
```

From lib/shared/widgets/skeleton_loader.dart:
```dart
// Existing factories: expenseList, cardList, documentList, groupList, gearList
// Missing: photoGrid
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add missing color tokens + module facade aliases + update InputDecorationTheme</name>
  <files>lib/core/theme/app_theme.dart</files>
  <read_first>
    - lib/core/theme/app_theme.dart
    - lib/core/theme/tokens/color_tokens.dart
    - .planning/phases/21-module-screens-redesign/21-UI-SPEC.md (Token Reference Card section)
    - .planning/phases/21-module-screens-redesign/21-RESEARCH.md (Pattern 6: InputDecorationTheme)
  </read_first>
  <action>
    Add three new static const Color fields to the AppColors class (after the existing "Borders" section, before spacing scale):

    ```dart
    // Earthy accents (Phase 21 — form fields and empty state circles)
    static const Color sandLight = Color(0xFFF5EDE1);   // Form field fill (D-26)
    static const Color terracotta = Color(0xFFCC6B49);   // Form focus border, step dots, onboarding CTA (D-26, D-27, D-29)
    static const Color warmGray = Color(0xFFE5D5C0);     // Form enabled border (D-26)
    ```

    Add module color facade aliases to AppColors (after the earthy accents section). These mirror AppColorTokens.light values as static const for direct use in widget code:

    ```dart
    // Module color facade aliases (mirrors AppColorTokens.light)
    static const Color moduleLedger = Color(0xFF0D7B74);
    static const Color moduleLedgerLight = Color(0xFFE6F5F3);
    static const Color moduleGear = Color(0xFF6B7280);
    static const Color moduleGearLight = Color(0xFFF3F4F6);
    static const Color moduleLogistics = Color(0xFF6B7280);
    static const Color moduleLogisticsLight = Color(0xFFF3F4F6);
    static const Color moduleVault = Color(0xFF6B7280);
    static const Color moduleVaultLight = Color(0xFFF3F4F6);
    static const Color moduleActivity = Color(0xFF6B7280);
    static const Color moduleActivityLight = Color(0xFFF3F4F6);
    static const Color moduleMemories = Color(0xFF6B7280);
    static const Color moduleMemoriesLight = Color(0xFFF3F4F6);
    static const Color selectionFill = Color(0xFFE6F5F3);
    static const Color cardSurface = Color(0xFFF8F9FA);
    ```

    Update the InputDecorationTheme inside AppTheme.lightTheme to match D-26 locked decision:

    ```dart
    inputDecorationTheme: InputDecorationTheme(
      filled: true,
      fillColor: AppColors.sandLight,  // was AppColors.surfaceLight
      contentPadding: const EdgeInsets.symmetric(horizontal: 24, vertical: 20),
      border: OutlineInputBorder(
        borderRadius: BorderRadius.circular(AppColors.radiusMedium), // 12dp (was radiusLarge=16dp)
        borderSide: BorderSide.none,
      ),
      enabledBorder: OutlineInputBorder(
        borderRadius: BorderRadius.circular(AppColors.radiusMedium),
        borderSide: const BorderSide(color: AppColors.warmGray, width: 1.5), // was borderLight
      ),
      focusedBorder: OutlineInputBorder(
        borderRadius: BorderRadius.circular(AppColors.radiusMedium),
        borderSide: const BorderSide(color: AppColors.terracotta, width: 2), // was primary
      ),
      errorBorder: OutlineInputBorder(
        borderRadius: BorderRadius.circular(AppColors.radiusMedium),
        borderSide: const BorderSide(color: AppColors.error, width: 1.5),
      ),
      focusedErrorBorder: OutlineInputBorder(
        borderRadius: BorderRadius.circular(AppColors.radiusMedium),
        borderSide: const BorderSide(color: AppColors.error, width: 2),
      ),
      labelStyle: GoogleFonts.getFont(
        AppTheme.fontFamily,
        color: const Color(0xFF2C1A0E), // dark brown per D-26
        fontSize: 14,
        fontWeight: FontWeight.w600,
      ),
      hintStyle: GoogleFonts.getFont(
        AppTheme.fontFamily,
        color: const Color(0xFFA89888), // sand gray per D-26
        fontSize: 15,
        fontWeight: FontWeight.w500,
      ),
      floatingLabelStyle: GoogleFonts.getFont(
        AppTheme.fontFamily,
        color: AppColors.terracotta, // was primary
        fontSize: 12,
        fontWeight: FontWeight.w700,
      ),
    ),
    ```

    These are the exact D-26 locked values. The border radius changes from radiusLarge (16dp) to radiusMedium (12dp). The fill color changes from surfaceLight (#F3F4F6) to sandLight (#F5EDE1). The focus border changes from primary teal to terracotta.
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla && flutter analyze lib/core/theme/app_theme.dart 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - lib/core/theme/app_theme.dart contains `static const Color sandLight = Color(0xFFF5EDE1)`
    - lib/core/theme/app_theme.dart contains `static const Color terracotta = Color(0xFFCC6B49)`
    - lib/core/theme/app_theme.dart contains `static const Color warmGray = Color(0xFFE5D5C0)`
    - lib/core/theme/app_theme.dart contains `static const Color moduleLedger = Color(0xFF0D7B74)`
    - lib/core/theme/app_theme.dart contains `static const Color moduleGearLight = Color(0xFFF3F4F6)`
    - lib/core/theme/app_theme.dart contains `static const Color selectionFill = Color(0xFFE6F5F3)`
    - lib/core/theme/app_theme.dart contains `fillColor: AppColors.sandLight`
    - lib/core/theme/app_theme.dart contains `color: AppColors.warmGray, width: 1.5`
    - lib/core/theme/app_theme.dart contains `color: AppColors.terracotta, width: 2`
    - lib/core/theme/app_theme.dart contains `BorderRadius.circular(AppColors.radiusMedium)` in inputDecorationTheme
    - lib/core/theme/app_theme.dart contains `color: const Color(0xFF2C1A0E)` in labelStyle
    - lib/core/theme/app_theme.dart contains `color: const Color(0xFFA89888)` in hintStyle
    - `flutter analyze lib/core/theme/app_theme.dart` reports no errors
  </acceptance_criteria>
  <done>AppColors has sandLight, terracotta, warmGray tokens. All 12 module color facade aliases are present. InputDecorationTheme uses sand fill, warm gray border, terracotta focus, dark brown labels, 12dp radius. No analysis errors.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade EmptyStateView with gradient circle + add SkeletonLoader.photoGrid + create DotStepIndicator</name>
  <files>lib/shared/widgets/empty_state_view.dart, lib/shared/widgets/skeleton_loader.dart, lib/shared/widgets/dot_step_indicator.dart</files>
  <read_first>
    - lib/shared/widgets/empty_state_view.dart
    - lib/shared/widgets/skeleton_loader.dart
    - lib/core/keys/shared_keys.dart
    - .planning/phases/21-module-screens-redesign/21-UI-SPEC.md (Component Inventory section, Spacing Scale section)
    - .planning/phases/21-module-screens-redesign/21-RESEARCH.md (Pattern 2: EmptyStateView, Pattern 7: DotStepIndicator, SkeletonLoader photoGrid Factory)
  </read_first>
  <action>
    **EmptyStateView upgrade (D-17/D-18):**

    Add a new optional parameter `accentGradient` of type `LinearGradient?` to EmptyStateView constructor. When provided, the 72dp icon container renders as a circle with the gradient background instead of the current tinted square.

    Modify the icon container in build():
    - When `accentGradient != null`: use `BoxDecoration(gradient: accentGradient, shape: BoxShape.circle)` on a 72x72 Container, icon size stays 48dp (D-17), icon color becomes `Colors.white`
    - When `accentGradient == null`: preserve current behavior (tinted square with 8% opacity, radiusLarge border radius)

    The 6 module earthy gradient values for call sites (NOT in EmptyStateView itself — documented here for reference):
    - Ledger: `LinearGradient(colors: [Color(0xFFCC6B49), Color(0xFFE0896A)])`
    - Gear: `LinearGradient(colors: [Color(0xFF7A8C5E), Color(0xFF96A876)])`
    - Logistics: `LinearGradient(colors: [Color(0xFF5B7B8C), Color(0xFF7B9BAC)])`
    - Vault: `LinearGradient(colors: [Color(0xFF8B7355), Color(0xFFA89372)])`
    - Memories: `LinearGradient(colors: [Color(0xFF9B7A5C), Color(0xFFB89878)])`
    - Activity: `LinearGradient(colors: [Color(0xFFA67C5B), Color(0xFFC29A7A)])`

    Also update the icon size in EmptyStateView from 32 to 48 per D-17.

    **SkeletonLoader.photoGrid factory:**

    Add a new named constructor factory to SkeletonLoader:

    ```dart
    factory SkeletonLoader.photoGrid({int count = 9}) {
      return SkeletonLoader(
        itemCount: 1,
        itemBuilder: (context, index) => Padding(
          padding: const EdgeInsets.symmetric(horizontal: 16, vertical: 8),
          child: GridView.builder(
            shrinkWrap: true,
            physics: const NeverScrollableScrollPhysics(),
            gridDelegate: const SliverGridDelegateWithFixedCrossAxisCount(
              crossAxisCount: 3,
              crossAxisSpacing: 8,
              mainAxisSpacing: 8,
            ),
            itemCount: count,
            itemBuilder: (_, __) => const SkeletonBlock(
              width: double.infinity,
              height: double.infinity,
              borderRadius: 8,
            ),
          ),
        ),
      );
    }
    ```

    **DotStepIndicator widget (D-27, D-29):**

    Create new file `lib/shared/widgets/dot_step_indicator.dart`:

    ```dart
    import 'package:flutter/material.dart';
    import 'package:iconsax/iconsax.dart';
    import '../../core/theme/app_theme.dart';

    class DotStepIndicator extends StatelessWidget {
      final int stepCount;
      final int currentStep;  // 0-indexed
      final Color activeColor;
      final bool showCheckmarks;  // true for Add Expense steps, false for onboarding pages

      const DotStepIndicator({
        super.key,
        required this.stepCount,
        required this.currentStep,
        this.activeColor = const Color(0xFFCC6B49), // terracotta default
        this.showCheckmarks = true,
      });

      @override
      Widget build(BuildContext context) {
        return Row(
          mainAxisAlignment: MainAxisAlignment.center,
          children: List.generate(stepCount, (index) {
            if (showCheckmarks && index < currentStep) {
              // Complete step: filled circle with check
              return _buildDot(filled: true, isCheck: true, index: index);
            } else if (index == currentStep) {
              // Active step: filled circle
              return _buildDot(filled: true, isCheck: false, index: index);
            } else {
              // Upcoming step: outlined ring
              return _buildDot(filled: false, isCheck: false, index: index);
            }
          }),
        );
      }

      Widget _buildDot({required bool filled, required bool isCheck, required int index}) {
        return Padding(
          padding: const EdgeInsets.symmetric(horizontal: 4),
          child: Container(
            width: 8,
            height: 8,
            decoration: BoxDecoration(
              shape: BoxShape.circle,
              color: filled ? activeColor : Colors.transparent,
              border: filled ? null : Border.all(color: activeColor, width: 1.5),
            ),
            child: isCheck
                ? const Icon(Iconsax.tick_circle, size: 8, color: Colors.white)
                : null,
          ),
        );
      }
    }
    ```

    Run `flutter test --no-pub` to verify no regressions from these shared widget changes.
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla && flutter analyze lib/shared/widgets/empty_state_view.dart lib/shared/widgets/skeleton_loader.dart lib/shared/widgets/dot_step_indicator.dart 2>&1 | tail -5 && flutter test --no-pub 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - lib/shared/widgets/empty_state_view.dart contains `final LinearGradient? accentGradient`
    - lib/shared/widgets/empty_state_view.dart contains `BoxShape.circle` inside a conditional on accentGradient
    - lib/shared/widgets/empty_state_view.dart contains `Icon(icon, size: 48`
    - lib/shared/widgets/skeleton_loader.dart contains `factory SkeletonLoader.photoGrid`
    - lib/shared/widgets/skeleton_loader.dart contains `crossAxisCount: 3`
    - lib/shared/widgets/dot_step_indicator.dart exists and contains `class DotStepIndicator extends StatelessWidget`
    - lib/shared/widgets/dot_step_indicator.dart contains `final int stepCount`
    - lib/shared/widgets/dot_step_indicator.dart contains `final int currentStep`
    - lib/shared/widgets/dot_step_indicator.dart contains `Color(0xFFCC6B49)` as default activeColor
    - `flutter analyze` reports no errors on all three files
    - `flutter test --no-pub` passes with 0 failures
  </acceptance_criteria>
  <done>EmptyStateView supports gradient circle via accentGradient parameter (backward compatible). SkeletonLoader.photoGrid renders 3-column skeleton grid. DotStepIndicator renders terracotta filled/outlined/checked dots. All existing tests pass.</done>
</task>

</tasks>

<verification>
- `flutter analyze lib/core/theme/ lib/shared/widgets/` — zero errors
- `flutter test --no-pub` — all 752+ tests pass
- `grep -c 'sandLight\|terracotta\|warmGray' lib/core/theme/app_theme.dart` returns 6+ (3 definitions + 3 usages in InputDecorationTheme)
- `grep -c 'moduleLedger' lib/core/theme/app_theme.dart` returns at least 2
</verification>

<success_criteria>
- Three new AppColors tokens compile: sandLight (#F5EDE1), terracotta (#CC6B49), warmGray (#E5D5C0)
- 12 module color facade aliases on AppColors match AppColorTokens.light values
- InputDecorationTheme uses sand fill, warm gray border, terracotta focus, dark brown label, sand gray hint, 12dp radius
- EmptyStateView accepts accentGradient and renders gradient circle when provided
- SkeletonLoader.photoGrid() factory compiles and renders 3-column skeleton grid
- DotStepIndicator widget exists and compiles
- Full test suite passes without regression
</success_criteria>

<output>
After completion, create `.planning/phases/21-module-screens-redesign/21-01-SUMMARY.md`
</output>
