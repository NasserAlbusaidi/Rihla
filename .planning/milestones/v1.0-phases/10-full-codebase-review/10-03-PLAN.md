---
phase: 10-full-codebase-review
plan: 03
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - lib/features/groups/screens/group_settle_up_screen.dart
  - lib/features/groups/widgets/group_settlement_tile.dart
  - lib/features/groups/widgets/group_settlement_summary.dart
  - lib/features/logistics/screens/logistics_screen.dart
  - lib/features/logistics/widgets/subgroup_card.dart
  - lib/features/logistics/widgets/unassigned_pool.dart
  - lib/features/memories/screens/memories_screen.dart
  - lib/features/memories/widgets/photo_grid.dart
  - lib/features/memories/widgets/full_screen_photo.dart
autonomous: true
requirements: []

must_haves:
  truths:
    - "group_settle_up_screen.dart is under 800 lines"
    - "logistics_screen.dart is under 800 lines"
    - "memories_screen.dart is under 800 lines"
    - "Extracted widgets are importable and functional"
    - "All existing tests pass without modification"
  artifacts:
    - path: "lib/features/groups/screens/group_settle_up_screen.dart"
      provides: "Group settle-up screen (refactored)"
      min_lines: 10
    - path: "lib/features/logistics/screens/logistics_screen.dart"
      provides: "Logistics screen (refactored)"
      min_lines: 10
    - path: "lib/features/memories/screens/memories_screen.dart"
      provides: "Memories screen (refactored)"
      min_lines: 10
    - path: "lib/features/groups/widgets/group_settlement_tile.dart"
      provides: "Extracted group settlement tile widget"
      min_lines: 30
    - path: "lib/features/logistics/widgets/subgroup_card.dart"
      provides: "Extracted subgroup card widget"
      min_lines: 30
    - path: "lib/features/memories/widgets/full_screen_photo.dart"
      provides: "Extracted full-screen photo viewer"
      min_lines: 30
  key_links:
    - from: "lib/features/groups/screens/group_settle_up_screen.dart"
      to: "lib/features/groups/widgets/group_settlement_tile.dart"
      via: "import and widget usage"
      pattern: "import.*group_settlement_tile"
    - from: "lib/features/logistics/screens/logistics_screen.dart"
      to: "lib/features/logistics/widgets/subgroup_card.dart"
      via: "import and widget usage"
      pattern: "import.*subgroup_card"
    - from: "lib/features/memories/screens/memories_screen.dart"
      to: "lib/features/memories/widgets/full_screen_photo.dart"
      via: "import and widget usage"
      pattern: "import.*full_screen_photo"
---

<objective>
Split group_settle_up_screen.dart (1021 lines), logistics_screen.dart (887 lines), and memories_screen.dart (783 lines) to under 800 lines each by extracting widget builder methods.

Purpose: Per D-03, all 5 files over 800 lines must be split. This plan handles the remaining three files across groups, logistics, and memories features.
Output: Three refactored screen files under 800 lines + new widget files in respective feature widgets/ directories.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-full-codebase-review/10-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split group_settle_up_screen.dart from 1021 to under 800 lines</name>
  <files>
    lib/features/groups/screens/group_settle_up_screen.dart,
    lib/features/groups/widgets/group_settlement_tile.dart,
    lib/features/groups/widgets/group_settlement_summary.dart
  </files>
  <read_first>
    lib/features/groups/screens/group_settle_up_screen.dart,
    lib/features/groups/widgets/group_member_balance_card.dart
  </read_first>
  <action>
Extract large widget methods from `_GroupSettleUpScreenState` into standalone widget files in `lib/features/groups/widgets/`.

The file has these extractable sections:

1. **`_buildSettlementGroup` + `_buildSettlementTile` + `_buildAvatar`** (~lines 382-737, ~355 lines) -> Extract to `group_settlement_tile.dart`. Create `GroupSettlementGroupCard` widget (renders a group of settlements for one debtor) and `GroupSettlementTile` widget (renders a single settlement row with payer/payee/amount and Record button). `_buildAvatar` becomes a private helper. The `GroupSettlementTile` takes: payer name, payee name, amount (Decimal), currency (String), optional event breakdown list (List of records with eventName, amount), onRecord callback, isCompleted bool. The `GroupSettlementGroupCard` takes: debtor name, list of settlements, onRecord callback.

2. **`_buildSummaryCard`** (~lines 304-363, ~60 lines) -> Extract to `group_settlement_summary.dart` as `GroupSettlementSummaryCard` StatelessWidget. Takes: total owed (String), total settlements (String), optimized transactions count (int), icon data.

After extraction, `group_settle_up_screen.dart` keeps: the `GroupSettleUpScreen` class, `_GroupSettleUpScreenState`, `build`, `_buildHeader`, `_buildContent`, `_buildSectionHeader`, `_buildAllSettled`, scroll controller logic, and the settlement recording modal bottom sheet. The screen imports the new widgets.

**Critical rules:**
- Extracted widgets that call `ref.watch()` or `ref.read()` must be `ConsumerWidget`
- The modal bottom sheet for recording settlements stays in the screen file (it mutates state)
- Pass data via constructors, not WidgetRef
- Preserve all existing visual behavior exactly
- Create `lib/features/logistics/widgets/` directory if it doesn't exist (it doesn't)
- The file must be under 800 lines after extraction
  </action>
  <verify>
    <automated>wc -l lib/features/groups/screens/group_settle_up_screen.dart | awk '{if ($1 < 800) print "PASS: "$1" lines"; else print "FAIL: "$1" lines"}'</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l lib/features/groups/screens/group_settle_up_screen.dart` reports under 800
    - `lib/features/groups/widgets/group_settlement_tile.dart` exists and contains `class GroupSettlementTile`
    - `lib/features/groups/widgets/group_settlement_summary.dart` exists and contains `class GroupSettlementSummaryCard`
    - `group_settle_up_screen.dart` contains `import` for both new widget files
    - `flutter analyze 2>&1 | grep "warning"` returns no warnings for these files
    - `flutter test` passes with 599+ tests
  </acceptance_criteria>
  <done>group_settle_up_screen.dart under 800 lines. Two widget files extracted. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Split logistics_screen.dart and memories_screen.dart to under 800 lines</name>
  <files>
    lib/features/logistics/screens/logistics_screen.dart,
    lib/features/logistics/widgets/subgroup_card.dart,
    lib/features/logistics/widgets/unassigned_pool.dart,
    lib/features/memories/screens/memories_screen.dart,
    lib/features/memories/widgets/photo_grid.dart,
    lib/features/memories/widgets/full_screen_photo.dart
  </files>
  <read_first>
    lib/features/logistics/screens/logistics_screen.dart,
    lib/features/memories/screens/memories_screen.dart
  </read_first>
  <action>
**Logistics screen (887 lines -> under 800):**

Extract from `_LogisticsScreenState`:

1. **`_buildGroupCard` + `_buildMemberSlot` + `_buildEmptySlot`** (~lines 325-585, ~260 lines) -> Extract to `lib/features/logistics/widgets/subgroup_card.dart` as `SubgroupCard` ConsumerWidget. Takes: SubGroup object, eventRef, participants list, onAddMember/onRemoveMember/onDeleteGroup callbacks. `_buildMemberSlot` and `_buildEmptySlot` become private methods in the new file.

2. **`_buildUnassignedPool` + `_buildPoolItem`** (~lines 145-270, ~125 lines) -> Extract to `lib/features/logistics/widgets/unassigned_pool.dart` as `UnassignedPool` StatelessWidget. Takes: list of unassigned participants, SubGroupType, onAssign callback.

Create `lib/features/logistics/widgets/` directory (it doesn't exist yet).

After extraction, `logistics_screen.dart` keeps: `LogisticsScreen` class, `_LogisticsScreenState`, `build`, `_buildTabContent`, `_buildGroupList`, `_buildEmptyState`, dialogs (create/rename/delete), and bottom sheet logic.

**Memories screen (783 lines -> under 800):**

The file is only 783 lines, but it's over the target. Extract:

1. **`_FullScreenPhoto` class** (~lines 637-783, ~146 lines, already a separate class in the same file) -> Move to `lib/features/memories/widgets/full_screen_photo.dart` as `FullScreenPhoto` StatelessWidget (remove underscore prefix to make it public). Keep exact same implementation.

2. **`_buildPhotoGrid` + `_buildStaggeredGrid` + `_buildPhotoTile`** (~lines 384-636, ~250 lines) -> Extract to `lib/features/memories/widgets/photo_grid.dart` as `MemoriesPhotoGrid` ConsumerWidget. Takes: list of memories, onTap callback (for full-screen view), onDelete callback. `_buildStaggeredGrid` and `_buildPhotoTile` become private methods.

Create `lib/features/memories/widgets/` directory (it doesn't exist yet).

After extraction, `memories_screen.dart` keeps: `MemoriesScreen` class, `_MemoriesScreenState`, `build`, `_buildHeader`, `_buildEmptyState`, `_buildSourceOption`, upload logic, and the source selection bottom sheet.

**Critical rules for both files:**
- Create the `widgets/` directories before writing files
- Extracted widgets that use `ref` must be `ConsumerWidget`
- Pass data via constructors — no WidgetRef passing
- Preserve all visual behavior exactly
- Both files must be under 800 lines after extraction
  </action>
  <verify>
    <automated>L1=$(wc -l < lib/features/logistics/screens/logistics_screen.dart); L2=$(wc -l < lib/features/memories/screens/memories_screen.dart); if [ "$L1" -lt 800 ] && [ "$L2" -lt 800 ]; then echo "PASS: logistics=$L1 memories=$L2"; else echo "FAIL: logistics=$L1 memories=$L2"; fi</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l lib/features/logistics/screens/logistics_screen.dart` reports under 800
    - `wc -l lib/features/memories/screens/memories_screen.dart` reports under 800
    - `lib/features/logistics/widgets/subgroup_card.dart` exists and contains `class SubgroupCard`
    - `lib/features/logistics/widgets/unassigned_pool.dart` exists and contains `class UnassignedPool`
    - `lib/features/memories/widgets/photo_grid.dart` exists and contains `class MemoriesPhotoGrid`
    - `lib/features/memories/widgets/full_screen_photo.dart` exists and contains `class FullScreenPhoto`
    - `logistics_screen.dart` contains imports for both new logistics widget files
    - `memories_screen.dart` contains imports for both new memories widget files
    - `flutter analyze 2>&1 | grep "warning"` returns no warnings
    - `flutter test` passes with 599+ tests
  </acceptance_criteria>
  <done>logistics_screen.dart and memories_screen.dart both under 800 lines. Four widget files extracted. All tests pass. Zero warnings.</done>
</task>

</tasks>

<verification>
- `wc -l lib/features/groups/screens/group_settle_up_screen.dart` < 800
- `wc -l lib/features/logistics/screens/logistics_screen.dart` < 800
- `wc -l lib/features/memories/screens/memories_screen.dart` < 800
- `flutter analyze 2>&1 | grep "warning"` returns empty
- `flutter test` exits 0
- 6 new widget files exist across groups/widgets, logistics/widgets, memories/widgets
</verification>

<success_criteria>
1. group_settle_up_screen.dart under 800 lines
2. logistics_screen.dart under 800 lines
3. memories_screen.dart under 800 lines
4. 6 new widget files created with proper class definitions
5. Zero flutter analyze warnings
6. All 599+ tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/10-full-codebase-review/10-03-SUMMARY.md`
</output>
