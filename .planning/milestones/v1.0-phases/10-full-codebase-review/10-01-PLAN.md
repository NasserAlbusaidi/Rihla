---
phase: 10-full-codebase-review
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/activity/screens/activity_feed_screen.dart
  - lib/features/events/screens/event_command_center.dart
  - lib/features/events/screens/event_expense_hero.dart
  - lib/features/events/widgets/event_card.dart
  - lib/features/events/widgets/event_spending_hero.dart
  - lib/features/gear/screens/gear_screen.dart
  - lib/features/ledger/screens/add_expense_screen.dart
  - lib/features/ledger/screens/ledger_screen.dart
  - lib/features/ledger/screens/settle_up_screen.dart
  - lib/features/logistics/screens/logistics_screen.dart
  - lib/features/memories/screens/memories_screen.dart
  - lib/features/vault/screens/vault_screen.dart
  - test/features/events/event_module_list_test.dart
  - test/features/groups/group_activity_screen_test.dart
  - test/features/ledger_test.dart
  - test/integration/offline_scenario_test.dart
  - test/unit/empty_state_view_test.dart
  - test/unit/group_balance_provider_test.dart
  - CLAUDE.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "flutter analyze reports zero warnings"
    - "flutter test passes with all 599 tests green"
    - "CLAUDE.md Conventions section documents naming patterns"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Naming conventions documentation"
      contains: "## Conventions"
  key_links:
    - from: "all Dart files"
      to: "flutter analyze"
      via: "zero warnings output"
      pattern: "0 issues found|No issues found|found.*0 warnings"
---

<objective>
Fix all 26 flutter analyze warnings and document naming conventions in CLAUDE.md.

Purpose: Clean analysis baseline before large file refactoring in subsequent plans. Establishes naming conventions for the project per D-05.
Output: Zero-warning codebase + documented conventions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-full-codebase-review/10-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix all 26 flutter analyze warnings</name>
  <files>
    lib/features/activity/screens/activity_feed_screen.dart,
    lib/features/events/screens/event_command_center.dart,
    lib/features/events/screens/event_expense_hero.dart,
    lib/features/events/widgets/event_card.dart,
    lib/features/events/widgets/event_spending_hero.dart,
    lib/features/gear/screens/gear_screen.dart,
    lib/features/ledger/screens/add_expense_screen.dart,
    lib/features/ledger/screens/ledger_screen.dart,
    lib/features/ledger/screens/settle_up_screen.dart,
    lib/features/logistics/screens/logistics_screen.dart,
    lib/features/memories/screens/memories_screen.dart,
    lib/features/vault/screens/vault_screen.dart,
    test/features/events/event_module_list_test.dart,
    test/features/groups/group_activity_screen_test.dart,
    test/features/ledger_test.dart,
    test/integration/offline_scenario_test.dart,
    test/unit/empty_state_view_test.dart,
    test/unit/group_balance_provider_test.dart
  </files>
  <read_first>
    lib/features/activity/screens/activity_feed_screen.dart,
    lib/features/events/screens/event_command_center.dart,
    lib/features/events/screens/event_expense_hero.dart,
    lib/features/events/widgets/event_card.dart,
    lib/features/events/widgets/event_spending_hero.dart,
    lib/features/gear/screens/gear_screen.dart,
    lib/features/ledger/screens/add_expense_screen.dart,
    lib/features/ledger/screens/ledger_screen.dart,
    lib/features/ledger/screens/settle_up_screen.dart,
    lib/features/logistics/screens/logistics_screen.dart,
    lib/features/memories/screens/memories_screen.dart,
    lib/features/vault/screens/vault_screen.dart,
    test/features/events/event_module_list_test.dart,
    test/features/groups/group_activity_screen_test.dart,
    test/features/ledger_test.dart,
    test/integration/offline_scenario_test.dart,
    test/unit/empty_state_view_test.dart,
    test/unit/group_balance_provider_test.dart
  </read_first>
  <action>
Fix all 26 warnings identified by `flutter analyze`. Here is the exact list:

**Unused imports to remove (17 total):**
1. `activity_feed_screen.dart` line 8: remove `import '../../../core/types/event_ref.dart';`
2. `event_command_center.dart` line 7: remove `import '../../../core/types/event_ref.dart';`
3. `event_command_center.dart` line 12: remove `import '../../ledger/providers/expense_provider.dart';`
4. `event_expense_hero.dart` line 7: remove `import '../../../core/types/event_ref.dart';`
5. `event_card.dart` line 8: remove `import '../../../core/types/event_ref.dart';`
6. `gear_screen.dart` line 8: remove `import '../../../core/types/event_ref.dart';`
7. `ledger_screen.dart` line 10: remove `import '../../../core/types/event_ref.dart';`
8. `settle_up_screen.dart` line 10: remove `import '../../../core/types/event_ref.dart';`
9. `settle_up_screen.dart` line 14: remove `import '../../logistics/models/sub_group_model.dart';`
10. `settle_up_screen.dart` line 20: remove `import '../services/settlement_service.dart';`
11. `logistics_screen.dart` line 7: remove `import '../../../core/types/event_ref.dart';`
12. `memories_screen.dart` line 11: remove `import '../../../core/types/event_ref.dart';`
13. `vault_screen.dart` line 9: remove `import '../../../core/types/event_ref.dart';`
14. `event_module_list_test.dart` line 16: remove `import 'package:safar/core/types/event_ref.dart';`
15. `offline_scenario_test.dart` line 9: remove `import 'package:safar/features/ledger/models/settlement_model.dart';`
16. `group_balance_provider_test.dart` line 13: remove `import 'package:safar/features/groups/services/group_activity_service.dart';`
17. `group_balance_provider_test.dart` line 14: remove `import 'package:safar/features/groups/services/group_settlement_service.dart';`

**Unnecessary cast (1):**
18. `event_spending_hero.dart` line 30: remove the `as` cast — the expression already has the correct type

**Unnecessary null comparisons (2):**
19. `add_expense_screen.dart` line 222: the operand can't be null, remove the `!= null` check (keep the body of the if-block)
20. `settle_up_screen.dart` line 886: the operand can't be null, remove the `!= null` check (keep the body of the if-block)

**Unused elements in tests (4):**
21. `event_module_list_test.dart` line 67: remove the `_makeSettlement` helper function (unused)
22. `ledger_test.dart` line 49: remove the `_mockEvent2Participants` variable (unused)
23. `ledger_test.dart` line 65: remove the `_mockExpense` variable (unused)
24. `group_balance_provider_test.dart` line 103: remove the `_pumpAsync` function (unused)

**Unused element parameter (1):**
25. `group_activity_screen_test.dart` line 25: remove the `activities` optional parameter that is never given a value

**Unused local variable (1):**
26. `empty_state_view_test.dart` line 33: remove or use the `tapped` variable — change it to just call `tester.tap(...)` without capturing the return value

After making all changes, run `flutter analyze` and verify zero warnings. Then run `flutter test` to verify no regressions.
  </action>
  <verify>
    <automated>flutter analyze 2>&1 | grep -c "warning" | grep -q "^0$" && echo "PASS: zero warnings" || echo "FAIL: warnings remain"</automated>
  </verify>
  <acceptance_criteria>
    - `flutter analyze 2>&1 | grep "warning"` returns no output
    - `flutter analyze` summary shows 0 warnings (info-level issues are acceptable per D-04)
    - `flutter test` passes with 599+ tests green
    - activity_feed_screen.dart does NOT contain `import '../../../core/types/event_ref.dart'`
    - event_command_center.dart does NOT contain `import '../../../core/types/event_ref.dart'`
    - settle_up_screen.dart does NOT contain `import '../services/settlement_service.dart'`
    - settle_up_screen.dart does NOT contain `import '../../logistics/models/sub_group_model.dart'`
  </acceptance_criteria>
  <done>flutter analyze reports zero warnings. flutter test passes. All 26 warning sources removed.</done>
</task>

<task type="auto">
  <name>Task 2: Document naming conventions in CLAUDE.md</name>
  <files>CLAUDE.md</files>
  <read_first>
    CLAUDE.md,
    lib/features/ledger/providers/expense_provider.dart,
    lib/features/gear/providers/gear_provider.dart,
    lib/features/vault/providers/document_provider.dart,
    lib/features/memories/providers/memory_provider.dart,
    lib/features/activity/services/activity_service.dart,
    lib/features/trip/providers/trip_provider.dart
  </read_first>
  <action>
Replace the placeholder `## Conventions` section in CLAUDE.md (currently "Conventions not yet established. Will populate as patterns emerge during development.") with documented naming conventions per D-05.

Write the following content for the Conventions section:

```
## Conventions

### Provider Naming

- **New code**: Use `event*` prefix for all event-scoped providers (e.g., `eventExpensesProvider`, `eventGearItemsProvider`, `eventSubGroupsProvider`, `eventActivityProvider`, `eventDocumentsProvider`, `eventMemoriesProvider`)
- **Group-scoped**: Use `group*` prefix (e.g., `groupBalancesProvider`, `groupSettlementsProvider`, `groupActivityProvider`, `userGroupsProvider`)
- **Legacy shims**: `trip*` prefix providers (`tripExpensesProvider`, `tripSettlementsProvider`, `tripGearProvider`, `tripDocumentsProvider`, `tripMemoriesProvider`, `tripActivityProvider`, `tripLogisticsParticipantsProvider`) are deprecated compatibility aliases. They delegate to the `event*` equivalents. Do NOT create new `trip*` providers. Do NOT remove existing shims while screens still reference them.

### Service Naming

- Services are named after their domain: `ExpenseService`, `GearService`, `EventService`, `GroupSettlementService`, `GroupActivityService`
- All services extend `FirestoreRepository` (except `ThawaniService`, `OcrService`, `ReceiptService` which are external integrations)

### Model Naming

- Models match their Firestore collection: `Expense`, `Settlement`, `GearItem`, `SubGroup`, `Document`, `Memory`, `Event`, `Group`, `GroupMember`
- Firestore serialization: `fromFirestore(Map<String, dynamic>)` and `toFirestore()` methods on each model
- SQLite serialization: `fromMap(Map<String, dynamic>)` and `toMap()` methods where SQLite caching exists

### File Organization

- Feature-first: `lib/features/{feature}/models/`, `providers/`, `screens/`, `services/`, `widgets/`
- Shared widgets: `lib/shared/widgets/` for cross-feature components
- Core utilities: `lib/core/` for config, theme, types, services, utils, providers
- Screen files: one primary screen class per file, private helper widget methods allowed but large widget builders should be extracted to `widgets/` directory

### Type Definitions

- `EventRef` typedef `({String groupId, String eventId})` is the canonical way to identify an event across the app (defined in `lib/core/types/event_ref.dart`, re-exported by `expense_provider.dart`)
```

Preserve the `<!-- GSD:conventions-end -->` comment after the section if it exists. Do NOT modify any other section of CLAUDE.md.
  </action>
  <verify>
    <automated>grep -c "### Provider Naming" CLAUDE.md | grep -q "1" && grep -c "event\*" CLAUDE.md | grep -q "[1-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md contains `### Provider Naming` heading
    - CLAUDE.md contains `### Service Naming` heading
    - CLAUDE.md contains `### Model Naming` heading
    - CLAUDE.md contains `### File Organization` heading
    - CLAUDE.md contains `### Type Definitions` heading
    - CLAUDE.md contains `EventRef` in the Type Definitions section
    - CLAUDE.md contains the string `trip*` in Provider Naming section with "deprecated" context
    - CLAUDE.md `## Conventions` section does NOT contain "not yet established"
  </acceptance_criteria>
  <done>CLAUDE.md Conventions section fully documents naming patterns for providers, services, models, files, and types. Deprecated trip* shims are flagged. New code guidance is clear.</done>
</task>

</tasks>

<verification>
- `flutter analyze 2>&1 | grep "warning"` returns empty (zero warnings)
- `flutter test` exits with code 0 and 599+ tests pass
- `grep "### Provider Naming" CLAUDE.md` returns a match
- `grep "not yet established" CLAUDE.md` returns no match
</verification>

<success_criteria>
1. Zero flutter analyze warnings (info-level acceptable per D-04)
2. All tests pass with no regressions
3. CLAUDE.md Conventions section documents provider/service/model/file naming patterns
4. Deprecated trip* shims are documented with "do not create new" guidance
</success_criteria>

<output>
After completion, create `.planning/phases/10-full-codebase-review/10-01-SUMMARY.md`
</output>
