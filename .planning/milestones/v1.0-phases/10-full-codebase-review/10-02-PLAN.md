---
phase: 10-full-codebase-review
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - lib/features/ledger/screens/settle_up_screen.dart
  - lib/features/ledger/widgets/settlement_summary_card.dart
  - lib/features/ledger/widgets/settlement_tile.dart
  - lib/features/ledger/widgets/recorded_settlements_section.dart
  - lib/features/ledger/widgets/recent_expenses_section.dart
  - lib/features/ledger/screens/ledger_screen.dart
  - lib/features/ledger/widgets/member_balances_section.dart
  - lib/features/ledger/widgets/spending_summary_section.dart
  - lib/features/ledger/widgets/transaction_list.dart
autonomous: true
requirements: []

must_haves:
  truths:
    - "settle_up_screen.dart is under 800 lines"
    - "ledger_screen.dart is under 800 lines"
    - "Extracted widgets are importable and functional"
    - "All existing tests pass without modification"
  artifacts:
    - path: "lib/features/ledger/screens/settle_up_screen.dart"
      provides: "Settle-up screen (refactored)"
      min_lines: 10
    - path: "lib/features/ledger/screens/ledger_screen.dart"
      provides: "Ledger screen (refactored)"
      min_lines: 10
    - path: "lib/features/ledger/widgets/settlement_tile.dart"
      provides: "Extracted settlement tile widget"
      min_lines: 30
    - path: "lib/features/ledger/widgets/transaction_list.dart"
      provides: "Extracted transaction list widget"
      min_lines: 30
  key_links:
    - from: "lib/features/ledger/screens/settle_up_screen.dart"
      to: "lib/features/ledger/widgets/settlement_tile.dart"
      via: "import and widget usage"
      pattern: "import.*settlement_tile"
    - from: "lib/features/ledger/screens/ledger_screen.dart"
      to: "lib/features/ledger/widgets/transaction_list.dart"
      via: "import and widget usage"
      pattern: "import.*transaction_list"
---

<objective>
Split settle_up_screen.dart (1062 lines) and ledger_screen.dart (1059 lines) to under 800 lines each by extracting widget builder methods into dedicated widget files.

Purpose: Per D-03, all 5 files over 800 lines must be split. This plan handles the two largest files, both in the ledger feature.
Output: Refactored screen files under 800 lines + new widget files in lib/features/ledger/widgets/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-full-codebase-review/10-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split settle_up_screen.dart from 1062 to under 800 lines</name>
  <files>
    lib/features/ledger/screens/settle_up_screen.dart,
    lib/features/ledger/widgets/settlement_summary_card.dart,
    lib/features/ledger/widgets/settlement_tile.dart,
    lib/features/ledger/widgets/recorded_settlements_section.dart,
    lib/features/ledger/widgets/recent_expenses_section.dart
  </files>
  <read_first>
    lib/features/ledger/screens/settle_up_screen.dart
  </read_first>
  <action>
Extract large private widget methods from `SettleUpScreen` into standalone widget classes in `lib/features/ledger/widgets/`.

The file currently has these extractable builder methods (approximate line ranges):

1. **`_buildPremiumSummary` + `_buildSummaryMiniItem`** (~lines 248-415, ~170 lines) -> Extract to `settlement_summary_card.dart` as `SettlementSummaryCard` StatelessWidget. Parameters: total owed amount, total settlements, net balance, currency string. The `_buildSummaryMiniItem` becomes a private method in the new file.

2. **`_buildSettlementGroup` + `_buildSettlementTile` + `_buildSmallAvatar`** (~lines 434-600 + 1037-1062, ~190 lines) -> Extract to `settlement_tile.dart` as `SettlementGroupCard` and `SettlementTile` widgets. `_buildSmallAvatar` becomes a private helper in the new file. The `SettlementTile` takes: payer name, payee name, amount, currency, onRecord callback, isCompleted bool.

3. **`_buildRecordedSettlements` + `_buildHistoryItem`** (~lines 598-722, ~125 lines) -> Extract to `recorded_settlements_section.dart` as `RecordedSettlementsSection` widget. Takes: list of settlements, participants list for name lookup, currency.

4. **`_buildRecentExpenses` + `_buildExpenseItem`** (~lines 724-830, ~110 lines) -> Extract to `recent_expenses_section.dart` as `RecentExpensesSection` widget. Takes: list of expenses, participants for name lookup.

After extraction, `settle_up_screen.dart` keeps: the `SettleUpScreen` class, `build` method, `_buildHeader`, `_buildContent`, `_buildAllSettled`, `_buildSectionHeader`, and the modal bottom sheet logic. The screen imports the new widget files and uses the extracted widgets in place of the old private methods.

**Critical rules:**
- Each extracted widget is a `StatelessWidget` or `ConsumerWidget` (match original ref usage)
- Pass data via constructor parameters — do NOT pass `WidgetRef` as a parameter
- If the extracted code uses `ref.watch()` or `ref.read()`, make it a `ConsumerWidget`
- Preserve ALL existing behavior exactly — no logic changes, only structural extraction
- Keep the same visual output pixel-for-pixel
- The settle_up_screen.dart file must be under 800 lines after extraction
  </action>
  <verify>
    <automated>wc -l lib/features/ledger/screens/settle_up_screen.dart | awk '{if ($1 < 800) print "PASS: "$1" lines"; else print "FAIL: "$1" lines"}'</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l lib/features/ledger/screens/settle_up_screen.dart` reports under 800
    - `lib/features/ledger/widgets/settlement_summary_card.dart` exists and contains `class SettlementSummaryCard`
    - `lib/features/ledger/widgets/settlement_tile.dart` exists and contains `class SettlementTile`
    - `lib/features/ledger/widgets/recorded_settlements_section.dart` exists and contains `class RecordedSettlementsSection`
    - `lib/features/ledger/widgets/recent_expenses_section.dart` exists and contains `class RecentExpensesSection`
    - `settle_up_screen.dart` contains `import` for each of the four new widget files
    - `flutter analyze 2>&1 | grep "warning"` returns no warnings for these files
    - `flutter test` passes with 599+ tests
  </acceptance_criteria>
  <done>settle_up_screen.dart is under 800 lines. Four widget files extracted to ledger/widgets/. All tests pass. Zero new warnings.</done>
</task>

<task type="auto">
  <name>Task 2: Split ledger_screen.dart from 1059 to under 800 lines</name>
  <files>
    lib/features/ledger/screens/ledger_screen.dart,
    lib/features/ledger/widgets/member_balances_section.dart,
    lib/features/ledger/widgets/spending_summary_section.dart,
    lib/features/ledger/widgets/transaction_list.dart
  </files>
  <read_first>
    lib/features/ledger/screens/ledger_screen.dart
  </read_first>
  <action>
Extract large private widget methods from `_LedgerScreenState` into standalone widget classes in `lib/features/ledger/widgets/`.

The file currently has these extractable builder methods:

1. **`_buildMemberBalances` + `_buildBalanceRow`** (~lines 153-420, ~270 lines) -> Extract to `member_balances_section.dart` as `MemberBalancesSection` ConsumerWidget. It needs: event ref (groupId + eventId), participants list, and the balance data. Since it uses `ref.watch`, make it ConsumerWidget. `_buildBalanceRow` becomes a private method in the new file.

2. **`_buildSpendingSummary`** (~lines 420-632, ~210 lines) -> Extract to `spending_summary_section.dart` as `SpendingSummarySection` widget. Takes: list of expenses, participants, currency. This section renders spending breakdown charts/stats.

3. **`_buildTransactionList` + `_buildTransactionCard`** (~lines 633-1059, ~425 lines) -> Extract to `transaction_list.dart` as `TransactionList` ConsumerWidget and `TransactionCard` StatelessWidget. The `TransactionList` needs: eventRef, and it watches expense/settlement providers. `TransactionCard` takes: expense data, participant names, onTap callback.

After extraction, `ledger_screen.dart` keeps: `LedgerScreen` class, `_LedgerScreenState`, `build` method, `_buildContent`, `_buildBalanceHeader`, `_buildHeaderAction`, tab management, and the dialog/bottom sheet invocations. The screen imports the new widget files.

**Critical rules:**
- Each extracted widget is `StatelessWidget` or `ConsumerWidget` (match original ref usage)
- Pass data via constructor — no WidgetRef as parameter
- If extracted code calls `ref.watch()` or `ref.read()`, use `ConsumerWidget` / `ConsumerStatefulWidget`
- Preserve ALL existing behavior exactly
- The ledger_screen.dart file must be under 800 lines after extraction
  </action>
  <verify>
    <automated>wc -l lib/features/ledger/screens/ledger_screen.dart | awk '{if ($1 < 800) print "PASS: "$1" lines"; else print "FAIL: "$1" lines"}'</automated>
  </verify>
  <acceptance_criteria>
    - `wc -l lib/features/ledger/screens/ledger_screen.dart` reports under 800
    - `lib/features/ledger/widgets/member_balances_section.dart` exists and contains `class MemberBalancesSection`
    - `lib/features/ledger/widgets/spending_summary_section.dart` exists and contains `class SpendingSummarySection`
    - `lib/features/ledger/widgets/transaction_list.dart` exists and contains `class TransactionList`
    - `ledger_screen.dart` contains `import` for each of the three new widget files
    - `flutter analyze 2>&1 | grep "warning"` returns no warnings for these files
    - `flutter test` passes with 599+ tests
  </acceptance_criteria>
  <done>ledger_screen.dart is under 800 lines. Three widget files extracted to ledger/widgets/. All tests pass. Zero new warnings.</done>
</task>

</tasks>

<verification>
- `wc -l lib/features/ledger/screens/settle_up_screen.dart` < 800
- `wc -l lib/features/ledger/screens/ledger_screen.dart` < 800
- `flutter analyze 2>&1 | grep "warning"` returns empty
- `flutter test` exits 0
- All 7 new widget files exist in lib/features/ledger/widgets/
</verification>

<success_criteria>
1. settle_up_screen.dart under 800 lines
2. ledger_screen.dart under 800 lines
3. 7 new widget files created with proper class definitions
4. Zero flutter analyze warnings
5. All 599+ tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/10-full-codebase-review/10-02-SUMMARY.md`
</output>
