---
phase: 09-dead-code-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/ledger/providers/expense_provider.dart
  - lib/features/auth/providers/firebase_auth_provider.dart
  - lib/features/logistics/providers/sub_group_provider.dart
autonomous: true
requirements: []

must_haves:
  truths:
    - "tripBalancesProvider no longer exists in the codebase"
    - "firebaseAuthStateProvider and firebaseCurrentUserProvider no longer exist in the codebase"
    - "subGroupsByTypeProvider no longer exists in the codebase"
    - "All existing tests pass after removals"
    - "Static analysis reports zero new warnings"
  artifacts:
    - path: "lib/features/ledger/providers/expense_provider.dart"
      provides: "Active expense/settlement/balance providers without dead tripBalancesProvider"
      contains: "eventBalancesProvider"
    - path: "lib/features/logistics/providers/sub_group_provider.dart"
      provides: "Active sub-group providers without dead subGroupsByTypeProvider"
      contains: "eventSubGroupsProvider"
  key_links:
    - from: "lib/features/ledger/providers/expense_provider.dart"
      to: "lib/features/ledger/screens/"
      via: "eventExpensesProvider, eventSettlementsProvider, eventBalancesProvider still exported"
      pattern: "eventExpensesProvider|eventSettlementsProvider|eventBalancesProvider"
    - from: "lib/features/logistics/providers/sub_group_provider.dart"
      to: "lib/features/logistics/screens/"
      via: "eventSubGroupsProvider, eventLogisticsParticipantsProvider still exported"
      pattern: "eventSubGroupsProvider|eventLogisticsParticipantsProvider"
---

<objective>
Remove the 3 orphaned providers identified in the v1.0 milestone audit: `tripBalancesProvider`, `firebaseAuthStateProvider`/`firebaseCurrentUserProvider`, and `subGroupsByTypeProvider`.

Purpose: Close tech debt items from milestone audit. These dead providers add confusion and false import targets.
Output: 3 files cleaned (1 deleted entirely, 2 edited), zero test/analysis regressions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-dead-code-cleanup/09-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove 3 orphaned providers and delete empty file</name>
  <files>
    lib/features/ledger/providers/expense_provider.dart
    lib/features/auth/providers/firebase_auth_provider.dart
    lib/features/logistics/providers/sub_group_provider.dart
  </files>
  <read_first>
    lib/features/ledger/providers/expense_provider.dart
    lib/features/auth/providers/firebase_auth_provider.dart
    lib/features/logistics/providers/sub_group_provider.dart
    lib/features/auth/providers/auth_provider.dart
  </read_first>
  <action>
Three independent removals, all per D-01 through D-06 from 09-CONTEXT.md:

**1. Delete `lib/features/auth/providers/firebase_auth_provider.dart` entirely (per D-03)**

Delete the entire file. It contains only `firebaseAuthStateProvider` and `firebaseCurrentUserProvider`. Both are orphaned — zero imports of this file exist anywhere in the codebase. The canonical auth providers live in `lib/features/auth/providers/auth_provider.dart` (`authStateProvider`, `currentUserProvider`).

**2. Remove `tripBalancesProvider` from `lib/features/ledger/providers/expense_provider.dart` (per D-02)**

Delete lines 194-224 (the entire `tripBalancesProvider` block including its section comment header starting at line 194 `// LEGACY: Backward-compatible...` through line 224 `});`).

Also delete lines 196-197 (the section comment about `// Balance calculation providers (unchanged...`) — wait, that is lines 194-197 (the comment block above tripBalancesProvider). The exact lines to remove are:

- The comment block: `// ---------------------------------------------------------------------------` / `// LEGACY: Backward-compatible SQLite-backed stream providers` / `// These remain so existing screen code continues to compile during migration.` / `// They will be removed in Plan 04-05 when all screens migrate to EventRef providers.` / `// ---------------------------------------------------------------------------` (lines 170-174)

Actually, looking more carefully: lines 170-174 are the comment header for `tripExpensesProvider` and `tripSettlementsProvider` (which are STILL ACTIVE — do NOT remove those). The block to remove starts at line 193:

Remove from line 193 (`// ---------------------------------------------------------------------------`) through line 224 (`});`). This includes:
- The section comment (lines 193-197): "Balance calculation providers (unchanged...)"
- The `tripBalancesProvider` definition (lines 199-224)

After this removal, also remove the now-unused import on line 10:
```
import '../../trip/providers/trip_provider.dart';
```
This import provided `tripLogisticsParticipantsProvider` which was only used inside `tripBalancesProvider`. The remaining code in the file does not reference anything from `trip_provider.dart`.

Keep all other imports — they are used by active providers (`eventBalancesProvider`, `eventExpensesProvider`, `eventSettlementsProvider`, `tripExpensesProvider`, `tripSettlementsProvider`).

**3. Remove `subGroupsByTypeProvider` from `lib/features/logistics/providers/sub_group_provider.dart` (per D-04)**

Delete lines 41-51 (the `subGroupsByTypeProvider` definition and its comment):
```dart
/// Sub-groups filtered by type (uses SQLite legacy stream)
final subGroupsByTypeProvider =
    Provider.family<
      AsyncValue<List<SubGroup>>,
      ({String tripId, SubGroupType type})
    >((ref, params) {
      final allGroups = ref.watch(tripSubGroupsProvider(params.tripId));
      return allGroups.whenData(
        (groups) => groups.where((g) => g.type == params.type).toList(),
      );
    });
```

All imports in this file remain used by the remaining providers (`eventSubGroupsProvider`, `tripSubGroupsProvider`, `eventLogisticsParticipantsProvider`). No import cleanup needed.

**Per D-06:** Stage all 3 changes and commit atomically.
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla && ! test -f lib/features/auth/providers/firebase_auth_provider.dart && ! grep -q 'tripBalancesProvider' lib/features/ledger/providers/expense_provider.dart && ! grep -q 'subGroupsByTypeProvider' lib/features/logistics/providers/sub_group_provider.dart && ! grep -q 'trip_provider.dart' lib/features/ledger/providers/expense_provider.dart && flutter analyze --no-fatal-infos 2>&1 | tail -5 && flutter test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `lib/features/auth/providers/firebase_auth_provider.dart` does not exist (file deleted)
    - `lib/features/ledger/providers/expense_provider.dart` does NOT contain the string `tripBalancesProvider`
    - `lib/features/ledger/providers/expense_provider.dart` does NOT contain `import '../../trip/providers/trip_provider.dart'`
    - `lib/features/ledger/providers/expense_provider.dart` DOES contain `eventBalancesProvider` (active provider preserved)
    - `lib/features/ledger/providers/expense_provider.dart` DOES contain `tripExpensesProvider` (active legacy shim preserved)
    - `lib/features/logistics/providers/sub_group_provider.dart` does NOT contain the string `subGroupsByTypeProvider`
    - `lib/features/logistics/providers/sub_group_provider.dart` DOES contain `eventSubGroupsProvider` (active provider preserved)
    - `lib/features/logistics/providers/sub_group_provider.dart` DOES contain `eventLogisticsParticipantsProvider` (active provider preserved)
    - `flutter analyze` exits with 0 (no errors, no new warnings)
    - `flutter test` exits with 0 (all tests pass)
  </acceptance_criteria>
  <done>All 3 orphaned providers removed. firebase_auth_provider.dart deleted. expense_provider.dart and sub_group_provider.dart edited with dead code removed and unused imports cleaned. flutter analyze and flutter test pass with zero failures.</done>
</task>

</tasks>

<verification>
1. `grep -r 'tripBalancesProvider\|firebaseAuthStateProvider\|firebaseCurrentUserProvider\|subGroupsByTypeProvider' lib/` returns zero matches
2. `flutter analyze` reports zero errors
3. `flutter test` reports zero failures
4. `test -f lib/features/auth/providers/firebase_auth_provider.dart` returns false (file deleted)
</verification>

<success_criteria>
- Zero references to tripBalancesProvider, firebaseAuthStateProvider, firebaseCurrentUserProvider, or subGroupsByTypeProvider exist anywhere in lib/ or test/
- flutter analyze exits 0
- flutter test exits 0
- firebase_auth_provider.dart no longer exists on disk
</success_criteria>

<output>
After completion, create `.planning/phases/09-dead-code-cleanup/09-01-SUMMARY.md`
</output>
