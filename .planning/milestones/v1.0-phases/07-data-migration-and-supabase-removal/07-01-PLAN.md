---
phase: 07-data-migration-and-supabase-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - lib/main.dart
  - lib/core/router/app_router.dart
  - lib/core/config/supabase_config.dart
  - lib/core/services/lazy_migration_service.dart
  - lib/features/trip/screens/create_trip_screen.dart
  - lib/features/trip/screens/edit_trip_screen.dart
  - lib/features/trip/screens/join_trip_screen.dart
  - lib/features/trip/screens/manage_members_screen.dart
  - lib/features/trip/services/trip_export_service.dart
  - lib/features/trip/providers/shadow_provider.dart
  - lib/features/trip/models/shadow_profile.dart
  - test/unit/lazy_migration_service_test.dart
  - .github/workflows/release_android.yml
  - config.json
autonomous: true
requirements:
  - MIG-06
  - MIG-07

must_haves:
  truths:
    - "supabase_flutter package is not in pubspec.yaml dependencies"
    - "No Supabase initialization runs during app boot"
    - "No legacy trip screens (create/edit/join/manage) exist in the codebase"
    - "No LazyMigrationService exists in the codebase"
    - "GoRouter has no /create-trip or /join-trip routes"
    - "flutter pub get succeeds without supabase_flutter"
  artifacts:
    - path: "pubspec.yaml"
      provides: "Dependency manifest without supabase_flutter"
      contains: "NOT supabase_flutter"
    - path: "lib/main.dart"
      provides: "Boot sequence without Supabase init"
      contains: "FirebaseConfig.initialize"
    - path: "lib/core/router/app_router.dart"
      provides: "Router without trip routes"
      contains: "NOT CreateTripScreen"
  key_links:
    - from: "lib/main.dart"
      to: "lib/core/config/firebase_config.dart"
      via: "FirebaseConfig.initialize() and ensureAnonymousSession()"
      pattern: "FirebaseConfig\\.initialize|FirebaseConfig\\.ensureAnonymousSession"
---

<objective>
Remove the supabase_flutter package dependency, delete all dead Supabase-only code (SupabaseConfig, LazyMigrationService, legacy trip screens/services), clean the app boot sequence, and remove legacy trip routes from the router.

Purpose: This is the bulk deletion that eliminates the Supabase dependency from the project. MIG-06 is descoped per D-01 (no data recovery flow -- old trip data abandoned). MIG-07 (Supabase removal) is the active target.

Output: A codebase where `flutter pub get` succeeds without supabase_flutter and the app boots on Firebase only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-data-migration-and-supabase-removal/07-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete all Supabase-only files and directories</name>
  <read_first>
    - lib/core/config/supabase_config.dart
    - lib/core/services/lazy_migration_service.dart
    - lib/features/trip/screens/create_trip_screen.dart
    - lib/features/trip/screens/edit_trip_screen.dart
    - lib/features/trip/screens/join_trip_screen.dart
    - lib/features/trip/screens/manage_members_screen.dart
    - lib/features/trip/services/trip_export_service.dart
    - lib/features/trip/providers/shadow_provider.dart
    - lib/features/trip/models/shadow_profile.dart
    - test/unit/lazy_migration_service_test.dart
  </read_first>
  <files>
    lib/core/config/supabase_config.dart,
    lib/core/services/lazy_migration_service.dart,
    lib/features/trip/screens/create_trip_screen.dart,
    lib/features/trip/screens/edit_trip_screen.dart,
    lib/features/trip/screens/join_trip_screen.dart,
    lib/features/trip/screens/manage_members_screen.dart,
    lib/features/trip/services/trip_export_service.dart,
    lib/features/trip/providers/shadow_provider.dart,
    lib/features/trip/models/shadow_profile.dart,
    test/unit/lazy_migration_service_test.dart,
    supabase/
  </files>
  <action>
    Delete the following files entirely (per D-02, D-04, D-06):

    1. `lib/core/config/supabase_config.dart` -- SupabaseConfig class + SupabaseLogging extension (98 lines). Per D-04.
    2. `lib/core/services/lazy_migration_service.dart` -- LazyMigrationService for Supabase-to-Firestore migration (318 lines). Per D-02.
    3. `lib/features/trip/screens/create_trip_screen.dart` -- Legacy create trip screen. Per D-06.
    4. `lib/features/trip/screens/edit_trip_screen.dart` -- Legacy edit trip screen. Per D-06.
    5. `lib/features/trip/screens/join_trip_screen.dart` -- Legacy join trip screen. Per D-06.
    6. `lib/features/trip/screens/manage_members_screen.dart` -- Legacy manage members screen. Per D-06.
    7. `lib/features/trip/services/trip_export_service.dart` -- Legacy trip export (Supabase-based). Per D-06.
    8. `lib/features/trip/providers/shadow_provider.dart` -- Shadow profile provider (only used by manage_members_screen). Per D-06.
    9. `lib/features/trip/models/shadow_profile.dart` -- Shadow profile model (only used by shadow_provider). Per D-06.
    10. `test/unit/lazy_migration_service_test.dart` -- Tests for the deleted LazyMigrationService. Per D-10.
    11. Delete `supabase/` directory recursively (contains `migrations/` with 29 SQL files and `functions/`). Per D-05.

    Also delete the now-empty directories:
    - `lib/features/trip/screens/` (if empty after deletions)
    - `lib/features/trip/services/` (if empty after deletions)

    IMPORTANT: Do NOT delete these files that are still used by other features:
    - `lib/features/trip/models/trip_model.dart` -- Trip, TripModules, Participant, ParticipantRole classes are imported by 15+ files across ledger, logistics, events, groups, and cache_service
    - `lib/features/trip/providers/trip_provider.dart` -- currentParticipantProvider, tripLogisticsParticipantsProvider, userTripsProvider are imported by expense_provider, add_expense_screen, edit_expense_sheet, split_scope_selector, event_spending_hero
  </action>
  <verify>
    <automated>test -f lib/core/config/supabase_config.dart && echo "FAIL: supabase_config.dart still exists" || echo "PASS: supabase_config.dart deleted"; test -f lib/core/services/lazy_migration_service.dart && echo "FAIL: lazy_migration_service.dart still exists" || echo "PASS: lazy_migration_service.dart deleted"; test -d lib/features/trip/screens && echo "FAIL: trip/screens/ still exists" || echo "PASS: trip/screens/ deleted"; test -d supabase && echo "FAIL: supabase/ still exists" || echo "PASS: supabase/ deleted"; test -f lib/features/trip/models/trip_model.dart && echo "PASS: trip_model.dart preserved" || echo "FAIL: trip_model.dart was deleted"</automated>
  </verify>
  <acceptance_criteria>
    - lib/core/config/supabase_config.dart does NOT exist
    - lib/core/services/lazy_migration_service.dart does NOT exist
    - lib/features/trip/screens/ directory does NOT exist
    - lib/features/trip/services/ directory does NOT exist
    - lib/features/trip/providers/shadow_provider.dart does NOT exist
    - lib/features/trip/models/shadow_profile.dart does NOT exist
    - supabase/ directory does NOT exist
    - test/unit/lazy_migration_service_test.dart does NOT exist
    - lib/features/trip/models/trip_model.dart DOES still exist (preserved)
    - lib/features/trip/providers/trip_provider.dart DOES still exist (preserved)
  </acceptance_criteria>
  <done>All Supabase-only files and legacy trip screens/services are deleted. Trip model and provider files that are still referenced by active code are preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Remove supabase_flutter dependency, clean boot sequence, clean router, update CI</name>
  <read_first>
    - pubspec.yaml
    - lib/main.dart
    - lib/core/router/app_router.dart
    - .github/workflows/release_android.yml
    - config.json
  </read_first>
  <files>
    pubspec.yaml,
    lib/main.dart,
    lib/core/router/app_router.dart,
    .github/workflows/release_android.yml,
    config.json
  </files>
  <action>
    1. **pubspec.yaml** -- Remove the `supabase_flutter: ^2.3.4` line from dependencies (line 38). Also remove the `# Backend - Supabase` comment on line 37. Per D-03.

    2. **lib/main.dart** -- Remove Supabase initialization from the boot sequence (per D-08):
       - Remove `import 'core/config/supabase_config.dart';` (line 8)
       - Remove the two Supabase init lines (lines 29-31):
         ```
         // Initialize Supabase (dual auth -- existing features still use Supabase)
         await SupabaseConfig.initialize();
         await SupabaseConfig.ensureAnonymousSession();
         ```
       - Update the comment on line 26 from "Establish Firebase anonymous auth before Supabase (D-06)" to "Establish Firebase anonymous auth"
       - The resulting boot sequence should be: FirebaseConfig.initialize() -> FirebaseConfig.ensureAnonymousSession() -> SharedPreferences.getInstance() -> system UI setup -> runApp()

    3. **lib/core/router/app_router.dart** -- Remove legacy trip routes:
       - Remove `import '../../features/trip/screens/create_trip_screen.dart';` (line 13)
       - Remove `import '../../features/trip/screens/join_trip_screen.dart';` (line 14)
       - Remove `static const String createTrip = '/create-trip';` from AppRoutes (line 21)
       - Remove `static const String joinTrip = '/join-trip';` from AppRoutes (line 22)
       - Remove the entire `// Create Trip` GoRoute block (lines 89-110)
       - Remove the entire `// Join Trip` GoRoute block (lines 113-134)

    4. **.github/workflows/release_android.yml** -- Remove the lcov exclude line referencing supabase_config.dart:
       - Remove line 84: `'lib/core/config/supabase_config.dart' \`

    5. **config.json** -- Remove Supabase keys (per D-09). Update contents to:
       ```json
       {
           "SENTRY_DSN": ""
       }
       ```
       The SUPABASE_URL and SUPABASE_ANON_KEY entries are removed. Only SENTRY_DSN remains. Firebase config comes from google-services.json and GoogleService-Info.plist, not from config.json.

    6. After all edits, run `flutter pub get` to regenerate pubspec.lock without supabase packages.
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla && grep -c "supabase_flutter" pubspec.yaml | grep "^0$" && echo "PASS: no supabase_flutter in pubspec.yaml" || echo "FAIL: supabase_flutter still in pubspec.yaml"; grep -c "SupabaseConfig\|supabase_config" lib/main.dart | grep "^0$" && echo "PASS: no Supabase in main.dart" || echo "FAIL: Supabase still in main.dart"; grep -c "CreateTripScreen\|JoinTripScreen\|create-trip\|join-trip" lib/core/router/app_router.dart | grep "^0$" && echo "PASS: no trip routes in router" || echo "FAIL: trip routes still in router"; grep -c "SUPABASE" config.json | grep "^0$" && echo "PASS: no SUPABASE in config.json" || echo "FAIL: SUPABASE still in config.json"; flutter pub get 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - pubspec.yaml does NOT contain string "supabase_flutter"
    - pubspec.yaml does NOT contain string "Backend - Supabase"
    - lib/main.dart does NOT contain string "SupabaseConfig" or "supabase_config"
    - lib/main.dart contains "await FirebaseConfig.initialize();" and "await FirebaseConfig.ensureAnonymousSession();"
    - lib/main.dart does NOT contain "dual auth" comment
    - lib/core/router/app_router.dart does NOT contain "CreateTripScreen" or "JoinTripScreen" or "createTrip" or "joinTrip"
    - .github/workflows/release_android.yml does NOT contain "supabase_config.dart"
    - config.json contains only SENTRY_DSN (not SUPABASE_URL or SUPABASE_ANON_KEY)
    - `flutter pub get` succeeds
    - pubspec.lock does NOT contain "supabase" (any supabase package)
  </acceptance_criteria>
  <done>supabase_flutter is removed from dependencies, the app boots on Firebase only, legacy trip routes are gone from the router, CI no longer references supabase_config.dart, and config.json has no Supabase keys.</done>
</task>

</tasks>

<verification>
1. `flutter pub get` succeeds with no errors
2. `grep -r "supabase_flutter" pubspec.yaml pubspec.lock` returns zero matches
3. `grep -r "SupabaseConfig\|supabase_config" lib/main.dart lib/core/router/` returns zero matches
4. `test ! -d supabase` exits 0
5. `test ! -f lib/core/config/supabase_config.dart` exits 0
6. `test ! -f lib/core/services/lazy_migration_service.dart` exits 0
7. `test ! -d lib/features/trip/screens` exits 0
8. `test -f lib/features/trip/models/trip_model.dart` exits 0 (preserved)
</verification>

<success_criteria>
- supabase_flutter package completely removed from pubspec.yaml and pubspec.lock
- All Supabase-only files deleted (SupabaseConfig, LazyMigrationService, trip screens/services/shadow)
- supabase/ directory deleted
- App boot sequence runs Firebase only (no SupabaseConfig calls)
- Router has no legacy trip routes
- CI workflow has no supabase_config.dart reference
- config.json has no Supabase credentials
- Trip model and provider files preserved for downstream consumers
</success_criteria>

<output>
After completion, create `.planning/phases/07-data-migration-and-supabase-removal/07-01-SUMMARY.md`
</output>
