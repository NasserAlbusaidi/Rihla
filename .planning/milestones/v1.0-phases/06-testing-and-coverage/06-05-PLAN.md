---
phase: 06-testing-and-coverage
plan: 05
type: execute
wave: 3
depends_on: [06-02, 06-03, 06-04]
files_modified:
  - .github/workflows/release_android.yml
autonomous: true
requirements: [TST-05]
must_haves:
  truths:
    - "CI runs flutter test --coverage and fails build if filtered coverage is below 80%"
    - "lcov exclusion list removes legacy/generated files per D-02 and D-15"
    - "flutter test --coverage reports 80%+ on filtered codebase after all prior plans complete"
  artifacts:
    - path: ".github/workflows/release_android.yml"
      provides: "Updated release workflow with inline coverage gate (replaces bare flutter test)"
      contains: "lcov --remove"
  key_links:
    - from: ".github/workflows/release_android.yml"
      to: "coverage/lcov_filtered.info"
      via: "lcov --remove generates filtered coverage inline"
      pattern: "lcov_filtered"
---

<objective>
Configure CI coverage enforcement at 80% threshold inline in the release workflow, and close any remaining coverage gaps to reach the target.

Purpose: TST-05 requires CI enforcement of 80%+ coverage. The approach keeps everything inline in release_android.yml — simpler for a single-workflow repo. The lcov exclusion list resolves the D-02/D-15 conflict by excluding legacy paths from measurement. Any remaining coverage gaps are identified and closed with mandatory fallback targets.
Output: Release workflow with inline coverage gate enforcing 80% on filtered codebase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-testing-and-coverage/06-CONTEXT.md
@.planning/phases/06-testing-and-coverage/06-RESEARCH.md

@.github/workflows/release_android.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add inline coverage gate to release_android.yml</name>
  <read_first>
    - .github/workflows/release_android.yml (existing CI workflow — understand structure, Flutter setup, test step)
  </read_first>
  <files>
    .github/workflows/release_android.yml
  </files>
  <action>
    Update .github/workflows/release_android.yml to add coverage enforcement inline. This is the ONLY CI workflow change — no separate test_coverage.yml file.

    REPLACE the existing test step (line 48-49, "Run Automated Tests: flutter test") with a combined coverage step. The replacement runs tests with coverage AND checks the threshold, avoiding running tests twice:

    ```yaml
      - name: Run Tests with Coverage
        run: |
          rm -f coverage/lcov.info
          flutter test --coverage

      - name: Install lcov
        run: sudo apt-get install -y lcov

      - name: Check Coverage Threshold (80%)
        run: |
          lcov --remove coverage/lcov.info \
            'lib/firebase_options.dart' \
            'lib/main.dart' \
            'lib/app.dart' \
            '*.g.dart' \
            '*.freezed.dart' \
            'lib/features/trip/*' \
            'lib/features/logistics/*' \
            'lib/features/gear/*' \
            'lib/features/memories/*' \
            'lib/features/vault/*' \
            'lib/features/settings/*' \
            'lib/core/services/cache_service.dart' \
            'lib/core/services/lazy_migration_service.dart' \
            -o coverage/lcov_filtered.info
          COVERAGE=$(lcov --summary coverage/lcov_filtered.info 2>&1 \
            | grep -oP '\d+\.\d+(?=%)' | head -1)
          echo "Coverage: ${COVERAGE}%"
          if (( $(echo "$COVERAGE < 80" | bc -l) )); then
            echo "::error::Coverage ${COVERAGE}% is below 80% threshold"
            exit 1
          fi
          echo "Coverage ${COVERAGE}% meets 80% threshold"
    ```

    These 3 new steps REPLACE the single "Run Automated Tests" step. The rest of the workflow (checkout, java setup, flutter setup, keystore decode, config creation, pub get, static analysis, build, upload) stays exactly as-is.

    The build step that follows will only run if coverage passes (GitHub Actions stops on non-zero exit by default).

    LCOV EXCLUSION RATIONALE (resolving D-02 vs D-15 conflict):
    - D-15 says exclude: firebase_options.dart, *.g.dart, *.freezed.dart, main.dart, app.dart
    - D-02 says skip legacy/Supabase code from testing
    - Resolution: expand exclusions to include legacy feature directories (trip/, logistics/, gear/, memories/, vault/, settings/) and legacy services (cache_service.dart, lazy_migration_service.dart)
    - This makes 80% achievable on the ~5,653 lines of active code without contradicting D-02
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla && grep -c "flutter test --coverage" .github/workflows/release_android.yml && grep -c "lcov --remove" .github/workflows/release_android.yml && ! grep -q "^        run: flutter test$" .github/workflows/release_android.yml && echo "PASS: bare flutter test removed"</automated>
  </verify>
  <acceptance_criteria>
    - release_android.yml contains `flutter test --coverage` (not bare `flutter test`)
    - release_android.yml contains `lcov --remove coverage/lcov.info`
    - release_android.yml contains `lib/features/trip/*` in lcov --remove arguments
    - release_android.yml contains `lib/features/logistics/*` in lcov --remove arguments
    - release_android.yml contains `80` in the threshold check
    - The bare `flutter test` step (without --coverage) does NOT exist in the file
    - No separate test_coverage.yml workflow file is created
    - The build step still comes after the coverage check
  </acceptance_criteria>
  <done>Release workflow updated with inline coverage gate. Bare `flutter test` replaced with coverage-instrumented version. Build blocked on coverage failure.</done>
</task>

<task type="auto">
  <name>Task 2: Run coverage locally and close mandatory gaps if below 80%</name>
  <read_first>
    - .github/workflows/release_android.yml (just updated — verify lcov exclusion list)
    - coverage/lcov.info (if it exists from prior runs)
  </read_first>
  <files>
    (varies — gap closure files identified by coverage report; mandatory fallback targets below)
  </files>
  <action>
    1. Run full test suite with coverage locally:
    ```bash
    cd /Users/nasseralbusaidi/Desktop/Personal/Rihla
    rm -f coverage/lcov.info
    flutter test --coverage
    ```

    2. Analyze coverage to identify remaining gaps. Parse the raw lcov.info to compute filtered coverage, applying the same exclusion list as CI.

    3. If coverage is already >= 80% on the filtered codebase after Plans 01-04, this task is DONE. Record the coverage percentage in the SUMMARY.

    4. If coverage is below 80%, the following fallback targets are MANDATORY (not optional):

       **Priority 1: group_provider.dart** (13.6% coverage, 89 uncovered lines per RESEARCH)
       - Add tests to test/unit/provider_tests.dart (created in Plan 03) with ProviderContainer overrides
       - Test: groupDetailProvider emits correct Group from stream override
       - Test: userGroupsProvider emits list of groups
       - Test: groupMembersProvider emits member list
       - Target: cover the main code paths (stream emissions, error handling)

       **Priority 2: event_provider.dart** (20.9% coverage, 34 uncovered lines per RESEARCH)
       - Add tests to test/unit/provider_tests.dart
       - Test: eventExpensesProvider emits expense list
       - Test: eventSettlementsProvider emits settlement list
       - Test: groupEventsProvider emits event list for group
       - Target: cover stream provider definitions and any transform logic

       **Priority 3: create_group_screen.dart** (0.9% coverage, 105 uncovered lines per RESEARCH)
       - Add widget tests to test/features/groups/
       - Test: renders form fields, validates input, submit triggers service call
       - Target: cover form rendering and submission paths

       **Priority 4: join_group_screen.dart** (1.5% coverage, 65 uncovered lines per RESEARCH)
       - Add widget tests to test/features/groups/
       - Test: renders code input, validates format, join triggers service call

       Work through priorities 1-4 in order. Stop when coverage >= 80%.

    5. Re-run coverage and verify >= 80%:
    ```bash
    rm -f coverage/lcov.info
    flutter test --coverage
    ```

    6. Record final coverage percentage in task output.

    IMPORTANT: Do NOT write tests for legacy/Supabase code per D-02. If a file belongs to features/trip/, features/logistics/, features/gear/, features/memories/, features/vault/, features/settings/, or core/services/cache_service.dart, skip it — lcov exclusion handles it.
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla && flutter test --coverage && echo "Coverage generated" && flutter test</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test` exits 0 (all tests pass)
    - `flutter test --coverage` generates coverage/lcov.info
    - coverage/lcov.info file exists and is non-empty
    - The filtered coverage (excluding legacy paths) is at or above 80% line coverage
    - If gap closure was needed, at least group_provider.dart and event_provider.dart have new tests
  </acceptance_criteria>
  <done>Full test suite passes. Coverage on filtered codebase meets 80% threshold. Mandatory gap targets addressed if needed.</done>
</task>

</tasks>

<verification>
- `flutter test` exits 0
- `flutter test --coverage` generates lcov.info
- .github/workflows/release_android.yml has inline coverage gate (no separate workflow file)
- Bare `flutter test` step no longer exists in release_android.yml
- Filtered coverage >= 80%
</verification>

<success_criteria>
TST-05 fully addressed: CI enforces 80%+ coverage on filtered codebase inline in the release workflow. Legacy code excluded per D-02/D-15 resolution. Coverage target met with mandatory fallback gap closure.
</success_criteria>

<output>
After completion, create `.planning/phases/06-testing-and-coverage/06-05-SUMMARY.md`
</output>
