---
phase: 06-testing-and-coverage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - test/unit/balance_calculations_test.dart
  - test/unit/settlement_optimization_test.dart
autonomous: true
requirements: [TST-01]
must_haves:
  truths:
    - "All 4 expense scopes (global, subGroup, personal, custom) have dedicated test coverage"
    - "Cross-event aggregation (expenses from multiple tripIds) produces correct net balances"
    - "Settlement optimization handles multi-event data correctly"
    - "Edge cases (zero amounts, single member, over-settlement, 50+ expenses) all pass"
  artifacts:
    - path: "test/unit/balance_calculations_test.dart"
      provides: "Exhaustive BalanceCalculator tests (~40+ test cases)"
      contains: "ExpenseScope.personal"
    - path: "test/unit/settlement_optimization_test.dart"
      provides: "Extended settlement optimization tests"
      contains: "calculateOptimalSettlements"
  key_links:
    - from: "test/unit/balance_calculations_test.dart"
      to: "lib/features/ledger/providers/expense_provider.dart"
      via: "BalanceCalculator.calculateBalances import"
      pattern: "BalanceCalculator\\.calculateBalances"
    - from: "test/unit/settlement_optimization_test.dart"
      to: "lib/features/ledger/providers/expense_provider.dart"
      via: "BalanceCalculator.calculateOptimalSettlements import"
      pattern: "calculateOptimalSettlements"
---

<objective>
Expand BalanceCalculator and settlement optimization tests to exhaustive coverage per D-03.

Purpose: Financial precision is the app's core value proposition. BalanceCalculator is currently at 60.4% coverage. This plan adds ~30+ new test cases covering all 4 scopes, cross-event aggregation, settlement optimization edge cases, and stress tests. This is the crown jewel of Phase 6.
Output: Exhaustive financial unit tests with ~40+ total test cases.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-testing-and-coverage/06-CONTEXT.md
@.planning/phases/06-testing-and-coverage/06-RESEARCH.md

@lib/features/ledger/providers/expense_provider.dart
@lib/features/ledger/models/expense_model.dart
@lib/features/ledger/models/settlement_model.dart
@lib/features/logistics/models/sub_group_model.dart
@lib/features/trip/models/trip_model.dart
@test/unit/balance_calculations_test.dart
@test/unit/settlement_optimization_test.dart
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Exhaustive BalanceCalculator scope and edge case tests</name>
  <read_first>
    - test/unit/balance_calculations_test.dart (existing 542 lines — understand what's already covered)
    - lib/features/ledger/providers/expense_provider.dart (BalanceCalculator class starting line 228 — all static methods, switch cases, scope handling)
    - lib/features/ledger/models/expense_model.dart (Expense model, ExpenseScope enum: global, subGroup, personal, custom)
    - lib/features/ledger/models/settlement_model.dart (Settlement model fields)
    - lib/features/logistics/models/sub_group_model.dart (SubGroup and SubGroupMember models for subGroup scope tests)
    - lib/features/trip/models/trip_model.dart (Participant model — id, tripId, role, displayName)
  </read_first>
  <files>test/unit/balance_calculations_test.dart</files>
  <behavior>
    GLOBAL SCOPE (verify existing, add gaps):
    - 3+ people share expense equally — each owes equal share
    - Multiple global expenses from different payers — net balances correct
    - Single participant group — payer owes themselves, net = 0

    PERSONAL SCOPE (new):
    - Personal expense: only payer is charged, others unaffected, net = 0 for all
    - Multiple personal expenses from different payers — each payer net = 0
    - Personal + global mixed: personal does not affect global split

    SUBGROUP SCOPE (new):
    - SubGroup expense split among subgroup members only — non-members unaffected
    - SubGroup with missing subGroups parameter — expense falls through (verify behavior)
    - SubGroup with mismatched subGroupId — verify graceful handling
    - Multiple subgroups in same trip — each expense split correctly

    CUSTOM SCOPE (new):
    - Custom scope with splitParticipantIds — only listed participants share cost
    - Custom scope with empty splitParticipantIds — verify behavior

    CROSS-EVENT AGGREGATION (new):
    - Expenses from multiple tripIds in single calculateBalances call — balances aggregate correctly
    - D-06 confirmed: BalanceCalculator is scope-agnostic, tripId is irrelevant to math

    EDGE CASES (new, per D-03):
    - Zero amount expense — net balance stays 0 for all participants
    - Over-settlement: settlement amount exceeds debt — creditor/debtor roles flip
    - Negative balance from over-settlement — verify sign is correct
    - 50+ expenses stress test — verify no precision loss with Decimal
    - Mixed currencies — if expenses have different currency, verify amounts still aggregate (BalanceCalculator does not convert currencies)
    - Deleted expenses (is_deleted = true) — verify they are excluded (or confirm they are pre-filtered before reaching BalanceCalculator)
    - Settlement reduces net balance correctly — verify post-settlement net
    - Empty expense list — returns zero balances for all participants
    - Empty participant list — returns empty list
  </behavior>
  <action>
    Extend test/unit/balance_calculations_test.dart with new test groups. Keep existing passing tests untouched.

    Create shared test helpers at the top of the file:
    ```dart
    // Helper to create participants with minimal boilerplate
    Participant _participant(String id, String name) => Participant(
      id: id, tripId: 't1', role: ParticipantRole.member,
      joinedAt: DateTime(2026), displayName: name,
    );

    // Helper to create a global expense
    Expense _globalExpense(String id, String payerId, String amount) => Expense(
      id: id, tripId: 't1', payerParticipantId: payerId,
      amount: Decimal.parse(amount), scope: ExpenseScope.global,
      createdAt: DateTime(2026),
    );
    ```

    Add test groups in this order:
    1. `group('Personal scope')` — 3-4 tests
    2. `group('SubGroup scope')` — 4 tests (requires SubGroup/SubGroupMember construction)
    3. `group('Custom scope')` — 2-3 tests
    4. `group('Cross-event aggregation')` — 2 tests (expenses with different tripIds)
    5. `group('Edge cases')` — 8-10 tests (zero amounts, over-settlement, stress test, empty lists, etc.)
    6. `group('Settlement integration')` — 3-4 tests (settlement reduces balance, multiple settlements, over-settlement)

    For the 50+ expenses stress test:
    ```dart
    test('50+ expenses maintain Decimal precision', () {
      final expenses = List.generate(55, (i) => _globalExpense('e$i', 'p1', '1.001'));
      final balances = BalanceCalculator.calculateBalances(
        expenses: expenses, participants: [_participant('p1', 'A'), _participant('p2', 'B')],
      );
      final total = Decimal.parse('1.001') * Decimal.fromInt(55);
      final p1 = balances.firstWhere((b) => b.participantId == 'p1');
      expect(p1.totalPaid, equals(total));
    });
    ```

    Use Decimal.parse() for ALL amount values — never double literals. This is per project convention (all money math uses Decimal).
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla && flutter test test/unit/balance_calculations_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - test/unit/balance_calculations_test.dart contains `ExpenseScope.personal` in at least 2 test cases
    - test/unit/balance_calculations_test.dart contains `ExpenseScope.subGroup` in at least 3 test cases
    - test/unit/balance_calculations_test.dart contains `ExpenseScope.custom` in at least 2 test cases
    - test/unit/balance_calculations_test.dart contains `'Cross-event'` or `'cross-event'` group name
    - test/unit/balance_calculations_test.dart contains `'50+'` or `'stress'` in a test name
    - test/unit/balance_calculations_test.dart contains `'over-settlement'` or `'Over-settlement'` in a test name
    - `flutter test test/unit/balance_calculations_test.dart` exits 0
    - File has 30+ individual test() calls total (was ~15, adding ~20+)
  </acceptance_criteria>
  <done>BalanceCalculator has exhaustive test coverage across all 4 scopes, cross-event aggregation, and edge cases. 40+ test cases total.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extended settlement optimization tests</name>
  <read_first>
    - test/unit/settlement_optimization_test.dart (existing 406 lines — understand current coverage)
    - lib/features/ledger/providers/expense_provider.dart (calculateOptimalSettlements method starting around line 359, and calculateTotalExpenses at line 409)
  </read_first>
  <files>test/unit/settlement_optimization_test.dart</files>
  <behavior>
    - Multi-event settlement data: balances from 3+ events, optimizer produces minimum transactions
    - All-zero balances: no settlements needed — returns empty list
    - Single debtor, multiple creditors: verify minimum transaction count
    - Single creditor, multiple debtors: verify minimum transaction count
    - Large group (8+ participants) with complex cross-debts: verify greedy algorithm correctness
    - Rounding edge case: uneven split (10.000 / 3 = 3.333...) — verify total settlements sum matches total debt
    - Over-settlement scenario: pre-existing settlement exceeds computed optimal — verify handling
  </behavior>
  <action>
    Extend test/unit/settlement_optimization_test.dart with additional test groups. Keep existing passing tests untouched.

    Add test groups:
    1. `group('Multi-event settlement')` — 2-3 tests using UserBalance objects with balances spanning multiple events
    2. `group('Edge cases')` — 4-5 tests:
       - All-zero balances → empty settlement list
       - Single participant → no settlements
       - 8+ participants with complex debts → verify settlement count <= N-1
       - Rounding: verify Decimal.parse('10.000') / 3 produces correct settlements that sum to total
    3. `group('calculateTotalExpenses')` — 2-3 tests:
       - Empty list → Decimal.zero
       - Multiple expenses → correct sum
       - Mix of scopes → all included in total

    Use the existing `_balance()` helper function (lines 7-21) for creating UserBalance objects.

    For rounding tests, use Decimal division with `.toDecimal(scaleOnInfinitePrecision: 10)` per project convention (STATE.md: decimal v3 division returns Rational).
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla && flutter test test/unit/settlement_optimization_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - test/unit/settlement_optimization_test.dart contains `'Multi-event'` or `'multi-event'` in a group/test name
    - test/unit/settlement_optimization_test.dart contains `'All-zero'` or `'all zero'` or `'no settlements'` in a test name
    - test/unit/settlement_optimization_test.dart contains `calculateTotalExpenses` in test assertions
    - `flutter test test/unit/settlement_optimization_test.dart` exits 0
    - File has 15+ individual test() calls total
  </acceptance_criteria>
  <done>Settlement optimization has comprehensive edge case coverage including multi-event data, rounding, and large groups.</done>
</task>

</tasks>

<verification>
- `flutter test test/unit/balance_calculations_test.dart test/unit/settlement_optimization_test.dart` exits 0
- Combined test count across both files exceeds 55 individual test cases
- All 4 ExpenseScope values (global, subGroup, personal, custom) appear in balance_calculations_test.dart
</verification>

<success_criteria>
BalanceCalculator and settlement optimization have exhaustive unit test coverage per D-03. All financial edge cases (zero, over-settlement, stress, rounding) are verified.
</success_criteria>

<output>
After completion, create `.planning/phases/06-testing-and-coverage/06-02-SUMMARY.md`
</output>
