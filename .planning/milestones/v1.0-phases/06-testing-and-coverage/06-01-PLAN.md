---
phase: 06-testing-and-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/features/command_center_test.dart
  - test/unit/group_service_test.dart
  - test/unit/group_join_test.dart
  - test/features/events/group_detail_events_test.dart
autonomous: true
requirements: [TST-05]
must_haves:
  truths:
    - "All existing tests pass with zero failures"
    - "No test file imports a widget or class that no longer exists"
    - "Tests using Firebase-touching providers override them with mocks"
    - "Existing 39 test files have been audited for weak assertions and naming consistency"
  artifacts:
    - path: "test/features/command_center_test.dart"
      provides: "Deleted (tests dead CommandCenter widget)"
    - path: "test/unit/group_service_test.dart"
      provides: "Fixed tests with FakeFirebaseFirestore injection"
    - path: "test/unit/group_join_test.dart"
      provides: "Fixed tests with provider override"
    - path: "test/features/events/group_detail_events_test.dart"
      provides: "Fixed assertions for EventCard changes"
  key_links:
    - from: "test/unit/group_service_test.dart"
      to: "GroupService.withFirestore(fakeDb)"
      via: "FakeFirebaseFirestore constructor injection"
      pattern: "GroupService\\.withFirestore"
---

<objective>
Fix the 9 failing tests across 4 test files so the entire test suite is green, then audit existing test files for quality per D-07.

Purpose: A green baseline is required before writing new tests or measuring coverage. The 9 failures span 4 files with 3 distinct root causes: deleted widget import, Firebase not initialized, and stale assertions. The audit ensures 80% is meaningful coverage, not just line-touching.
Output: All existing tests pass (`flutter test` exits 0). Audit notes recorded for Plans 02-04 to act on.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-testing-and-coverage/06-CONTEXT.md
@.planning/phases/06-testing-and-coverage/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete command_center_test.dart and fix Firebase provider tests</name>
  <read_first>
    - test/features/command_center_test.dart (imports deleted CommandCenter widget)
    - test/unit/group_service_test.dart (3 failing tests — Firebase not initialized)
    - test/unit/group_join_test.dart (1 failing test — same Firebase issue)
    - lib/features/groups/services/group_service.dart (to find .withFirestore constructor)
    - lib/features/groups/providers/group_provider.dart (to find groupServiceProvider definition)
  </read_first>
  <files>
    test/features/command_center_test.dart
    test/unit/group_service_test.dart
    test/unit/group_join_test.dart
  </files>
  <action>
    1. DELETE test/features/command_center_test.dart entirely — the file imports `lib/features/home/screens/command_center.dart` which no longer exists (replaced by home_screen.dart in Phase 2/3). HomeScreen already has its own test file at test/features/home/home_screen_groups_test.dart.

    2. FIX test/unit/group_service_test.dart:
       - The 3 failing tests call `container.read(groupServiceProvider)` which triggers real Firebase initialization.
       - Replace `container.read(groupServiceProvider)` with direct `GroupService.withFirestore(fakeDb)` construction.
       - Follow the established pattern from test/unit/expense_service_test.dart:
         ```dart
         late FakeFirebaseFirestore fakeDb;
         late GroupService service;
         setUp(() {
           fakeDb = FakeFirebaseFirestore();
           service = GroupService.withFirestore(fakeDb);
         });
         ```
       - If GroupService does not have a `.withFirestore` constructor, check if it extends FirestoreRepository (which provides it via `super.withFirestore(db)`). If it uses a different pattern, read the class and follow whatever test injection pattern exists.
       - Remove the `container.read(groupServiceProvider)` calls and test the service directly.
       - Keep the SharedPreferences setup if the service reads device name.

    3. FIX test/unit/group_join_test.dart:
       - Same root cause as group_service_test.dart — line `container.read(groupServiceProvider)` triggers Firebase.
       - Apply the same fix: construct GroupService directly with FakeFirebaseFirestore.
       - The test "joinGroup throws when Firebase not initialized" should be DELETED or rewritten — it tests a negative case that is now irrelevant (we inject fakes, not rely on Firebase initialization).
       - Keep the invite code normalization test (line 12-17: `lowerCode.toUpperCase()`) as-is — it passes and tests real logic.
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla && flutter test test/unit/group_service_test.dart test/unit/group_join_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - test/features/command_center_test.dart does not exist (deleted)
    - test/unit/group_service_test.dart contains `FakeFirebaseFirestore()` (not `container.read(groupServiceProvider)`)
    - test/unit/group_join_test.dart does not call `container.read(groupServiceProvider)`
    - `flutter test test/unit/group_service_test.dart` exits 0
    - `flutter test test/unit/group_join_test.dart` exits 0
  </acceptance_criteria>
  <done>group_service_test.dart and group_join_test.dart pass. command_center_test.dart deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Fix group_detail_events_test.dart assertions and verify full suite green</name>
  <read_first>
    - test/features/events/group_detail_events_test.dart (4 failing tests)
    - lib/features/events/widgets/event_card.dart (to see current EventCard widget structure)
    - lib/features/ledger/providers/expense_provider.dart (tripExpensesProvider — may be deprecated shim)
    - lib/features/events/providers/event_provider.dart (eventExpensesProvider — current provider)
  </read_first>
  <files>
    test/features/events/group_detail_events_test.dart
  </files>
  <action>
    1. READ the 4 failing tests in group_detail_events_test.dart and the current EventCard widget to understand the mismatch.

    2. FIX the "shows 0.000 OMR" test:
       - Change `findsOneWidget` to `findsWidgets` for any text assertion where the amount appears in multiple widget tree nodes (e.g., card header AND summary row).
       - Per RESEARCH Pitfall 3: use `findsWidgets` as default for text assertions, `findsOneWidget` only for unique text like section headers.

    3. FIX the remaining 3 EventCard tests:
       - Read the current EventCard build() method to identify what providers it watches and what data it displays.
       - If EventCard now watches `eventExpensesProvider` instead of `tripExpensesProvider`, update the test's provider overrides accordingly.
       - If EventCard's widget structure changed (different Text widgets, different layout), update assertions to match.
       - Ensure all providers that EventCard's build() method calls `ref.watch()` on are overridden in the test's ProviderScope.

    4. Run the full test suite to confirm zero failures:
       ```
       flutter test
       ```
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla && flutter test</automated>
  </verify>
  <acceptance_criteria>
    - test/features/events/group_detail_events_test.dart does not contain `findsOneWidget` for amount text (uses `findsWidgets` or `findsNWidgets`)
    - `flutter test test/features/events/group_detail_events_test.dart` exits 0
    - `flutter test` (full suite) exits 0 with zero failures
  </acceptance_criteria>
  <done>All 4 group_detail_events_test.dart failures fixed. Full test suite passes with zero failures.</done>
</task>

<task type="auto">
  <name>Task 3: Audit existing test files for weak assertions and naming consistency (D-07)</name>
  <read_first>
    - Run `find test/ -name "*_test.dart" | sort` to list all 39 test files
    - Read 5-10 of the highest-priority test files (financial, service, provider tests) to assess assertion quality
  </read_first>
  <files>
    (no files modified — this task produces audit notes in the SUMMARY)
  </files>
  <action>
    Per D-07 (locked decision): Audit existing 39 test files for weak assertions, missing edge cases, and naming consistency.

    1. LIST all test files: `find test/ -name "*_test.dart" | sort`

    2. For each test file, scan for these quality issues:
       a. **Weak assertions**: `expect(result, isNotNull)` when value should be checked, `expect(list, isNotEmpty)` without checking contents, `expect(widget, findsWidgets)` without verifying what was found
       b. **Missing edge cases**: only happy path tested, no error path, no empty input, no boundary values
       c. **Naming consistency**: test names should describe behavior ("calculates balance for 3 participants"), not implementation ("test calculateBalances method")
       d. **Stale mocks**: mocks that return hardcoded values that no longer match real service signatures

    3. Categorize findings into:
       - **Critical** (assertion is wrong or misleading — could pass even with broken code)
       - **Improvement** (assertion exists but could be stronger)
       - **Naming** (inconsistent test name style)

    4. Record findings in the SUMMARY for this plan. The findings will guide Plans 02-04:
       - Plan 02 executor should fix weak assertions in balance_calculations_test.dart and settlement_optimization_test.dart while extending them
       - Plan 03 executor should fix weak assertions in formatters_test.dart and provider test files while extending them
       - Plan 04 executor should fix weak assertions in widget test files while extending them

    5. Do NOT modify any test files in this task — only audit and document. Fixes happen in Plans 02-04 where those files are being extended anyway.

    Focus audit time on the financial and provider test files (highest impact). Skim widget test files (lower priority for assertion quality since they primarily test rendering).
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla && flutter test</automated>
  </verify>
  <acceptance_criteria>
    - All 39 test files have been scanned (file list recorded in SUMMARY)
    - At least 10 test files have detailed audit notes
    - Findings are categorized as Critical/Improvement/Naming
    - SUMMARY contains actionable notes for Plans 02-04 executors
  </acceptance_criteria>
  <done>Audit of 39 test files complete. Weak assertions, missing edge cases, and naming issues documented. Findings fed forward to Plans 02-04.</done>
</task>

</tasks>

<verification>
- `flutter test` exits 0 with zero test failures
- No test file imports `command_center.dart`
- No test uses `container.read(groupServiceProvider)` without proper Firebase mock injection
- Audit findings documented in SUMMARY
</verification>

<success_criteria>
The entire existing test suite is green (zero failures). All 39 test files audited for quality per D-07. This is the baseline for all subsequent plans in this phase.
</success_criteria>

<output>
After completion, create `.planning/phases/06-testing-and-coverage/06-01-SUMMARY.md`

IMPORTANT: The SUMMARY must include an "Audit Findings" section with categorized issues per test file. Plans 02-04 executors will read this section to know which existing assertions to strengthen while extending those files.
</output>
