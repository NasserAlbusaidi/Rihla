---
phase: 13-final-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/ledger/providers/ledger_provider.dart
  - lib/features/trip/providers/trip_provider.dart
  - lib/features/logistics/providers/sub_group_provider.dart
  - lib/features/auth/providers/auth_provider.dart
  - CLAUDE.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "tripUnifiedLedgerProvider no longer exists in the codebase"
    - "tripSeedProvider no longer exists in the codebase"
    - "tripSubGroupsProvider no longer exists in the codebase"
    - "auth_provider.dart has no comment referencing firebase_auth_provider.dart"
    - "CLAUDE.md documents all remaining trip* legacy providers in a mapping table"
    - "flutter analyze reports zero new warnings"
    - "flutter test passes with zero failures"
  artifacts:
    - path: "lib/features/ledger/providers/ledger_provider.dart"
      provides: "eventUnifiedLedgerProvider only (tripUnifiedLedgerProvider removed)"
      contains: "eventUnifiedLedgerProvider"
    - path: "lib/features/trip/providers/trip_provider.dart"
      provides: "currentParticipantProvider, tripLogisticsParticipantsProvider (tripSeedProvider removed)"
      contains: "currentParticipantProvider"
    - path: "lib/features/logistics/providers/sub_group_provider.dart"
      provides: "eventSubGroupsProvider, eventLogisticsParticipantsProvider (tripSubGroupsProvider removed)"
      contains: "eventSubGroupsProvider"
    - path: "CLAUDE.md"
      provides: "Legacy provider mapping table"
      contains: "trip* Provider"
  key_links:
    - from: "lib/features/ledger/screens/ledger_screen.dart"
      to: "lib/features/ledger/providers/ledger_provider.dart"
      via: "import and ref.watch(eventUnifiedLedgerProvider)"
      pattern: "eventUnifiedLedgerProvider"
    - from: "lib/features/ledger/screens/add_expense_screen.dart"
      to: "lib/features/trip/providers/trip_provider.dart"
      via: "import for currentParticipantProvider"
      pattern: "currentParticipantProvider"
---

<objective>
Remove 3 orphaned providers, clean 1 stale comment, and document all remaining trip* legacy providers in CLAUDE.md.

Purpose: Close the final tech debt items from the v1.0 milestone audit. After this plan, the codebase has no undocumented legacy shims and no orphaned providers from the Supabase-to-Firestore migration.

Output: Clean provider files with orphaned code removed, updated CLAUDE.md with legacy provider mapping table, passing analyze and tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-final-cleanup/13-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove 3 orphaned providers + stale comment + same-file collateral</name>
  <files>
    lib/features/ledger/providers/ledger_provider.dart
    lib/features/trip/providers/trip_provider.dart
    lib/features/logistics/providers/sub_group_provider.dart
    lib/features/auth/providers/auth_provider.dart
  </files>
  <read_first>
    lib/features/ledger/providers/ledger_provider.dart
    lib/features/trip/providers/trip_provider.dart
    lib/features/logistics/providers/sub_group_provider.dart
    lib/features/auth/providers/auth_provider.dart
    lib/features/ledger/providers/expense_provider.dart
  </read_first>
  <action>
**Verification step (do FIRST):** Before any edits, grep lib/ and test/ for each provider name to confirm zero consumers:
- `grep -r "tripUnifiedLedgerProvider" lib/ test/ --include="*.dart"` -- should only appear in ledger_provider.dart definition
- `grep -r "tripSeedProvider" lib/ test/ --include="*.dart"` -- should only appear in trip_provider.dart definition and a comment in home_screen_groups_test.dart
- `grep -r "tripSubGroupsProvider" lib/ test/ --include="*.dart"` -- should only appear in sub_group_provider.dart definition and doc comment

If any grep reveals actual consumers (ref.watch, ref.read, override), STOP and report.

**1. ledger_provider.dart** -- Remove `tripUnifiedLedgerProvider` (lines 44-77):
- Delete the entire `tripUnifiedLedgerProvider` provider definition (the `LEGACY:` doc comment through the closing `});`)
- Remove the import `import 'expense_provider.dart';` ONLY IF the remaining `eventUnifiedLedgerProvider` does not use it. Check: `eventUnifiedLedgerProvider` uses `eventExpensesProvider` and `eventSettlementsProvider` which ARE from expense_provider.dart, so the import MUST stay.
- The file should retain only `eventUnifiedLedgerProvider` (lines 1-42) plus its imports.

**2. trip_provider.dart** -- Remove `tripSeedProvider` (lines 75-80):
- Delete the `tripSeedProvider` FutureProvider definition including its `@Deprecated` doc comment and the no-op body.
- Also remove these 3 orphaned providers that have zero consumers per D-03 (same-file collateral cleanup):
  - `tripLoadingProvider` (line 17) -- zero consumers in lib/
  - `tripErrorProvider` (line 20) -- zero consumers in lib/
  - `currentTripProvider` (line 23) -- zero consumers in lib/
- After removals, check if the `cache_service.dart` import is still needed. `tripLogisticsParticipantsProvider` uses `CacheService.getCachedParticipants`, so the import stays.
- The file should retain: `userGroupsForParticipantProvider`, `tripLogisticsParticipantsProvider`, `currentParticipantProvider`, and all their imports.

**3. sub_group_provider.dart** -- Remove `tripSubGroupsProvider` (lines 31-39):
- Delete the `tripSubGroupsProvider` StreamProvider definition including its `@Deprecated` doc comment.
- Also remove the doc comment reference on line 23 that says `Replaces [tripSubGroupsProvider].` -- update the `eventSubGroupsProvider` doc comment to just say `/// Firestore-backed stream of sub-groups for an event.` (remove the "Replaces" line).
- Check imports after deletion: `event_model.dart` is used by `eventLogisticsParticipantsProvider` (takes Event param), keep it. `trip_model.dart` is used by `eventLogisticsParticipantsProvider` which returns `List<Participant>`, keep it.
- The file should retain: `subGroupLoadingProvider`, `subGroupErrorProvider`, `subGroupServiceProvider`, `eventSubGroupsProvider`, `eventLogisticsParticipantsProvider`, and all their imports.

**4. auth_provider.dart** -- Remove stale comment (line 8) per D-05:
- Line 8 currently reads: `/// Re-exports from firebase_auth_provider.dart for backward compatibility.`
- Replace the entire doc comment for `authStateProvider` (lines 7-8) with: `/// Auth state provider -- listens to Firebase auth changes.`
- This is a single-line doc comment with no reference to the deleted file.

**5. Commit** (per D-08): Create a single commit with message: `chore(13): remove 3 orphaned providers + stale comment`
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla && grep -r "tripUnifiedLedgerProvider\|tripSeedProvider\|tripSubGroupsProvider\|tripLoadingProvider\|tripErrorProvider\|currentTripProvider" lib/ --include="*.dart" | grep -v "worktree" | wc -l</automated>
    The grep count should be 0 (all six orphaned providers removed). Then:
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla && flutter analyze 2>&1 | tail -5</automated>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla && flutter test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "tripUnifiedLedgerProvider" lib/ --include="*.dart"` returns zero matches
    - `grep -r "tripSeedProvider" lib/ --include="*.dart"` returns zero matches
    - `grep -r "tripSubGroupsProvider" lib/ --include="*.dart"` returns zero matches
    - `grep -r "tripLoadingProvider" lib/ --include="*.dart"` returns zero matches
    - `grep -r "tripErrorProvider" lib/ --include="*.dart"` returns zero matches
    - `grep -r "currentTripProvider" lib/ --include="*.dart"` returns zero matches
    - `grep -n "firebase_auth_provider" lib/features/auth/providers/auth_provider.dart` returns zero matches
    - ledger_provider.dart contains `eventUnifiedLedgerProvider` (line 10 area)
    - trip_provider.dart contains `currentParticipantProvider` and `tripLogisticsParticipantsProvider`
    - sub_group_provider.dart contains `eventSubGroupsProvider` and `eventLogisticsParticipantsProvider`
    - `flutter analyze` exits with 0 warnings
    - `flutter test` exits with 0 failures
  </acceptance_criteria>
  <done>All 3 orphaned providers (tripUnifiedLedgerProvider, tripSeedProvider, tripSubGroupsProvider) plus 3 same-file orphans (tripLoadingProvider, tripErrorProvider, currentTripProvider) removed. Stale firebase_auth_provider.dart comment removed from auth_provider.dart. flutter analyze and flutter test pass.</done>
</task>

<task type="auto">
  <name>Task 2: Document remaining trip* legacy providers in CLAUDE.md mapping table</name>
  <files>
    CLAUDE.md
  </files>
  <read_first>
    CLAUDE.md
    lib/features/ledger/providers/expense_provider.dart
    lib/features/gear/providers/gear_provider.dart
    lib/features/vault/providers/document_provider.dart
    lib/features/memories/providers/memory_provider.dart
    lib/features/activity/services/activity_service.dart
    lib/features/ledger/providers/category_provider.dart
    lib/features/trip/providers/trip_provider.dart
  </read_first>
  <action>
**Edit CLAUDE.md** at the "Provider Naming" subsection (line 249). After the existing "Legacy shims" bullet (line 253), insert the following mapping table per D-06 and D-07:

Insert this content directly after the "Legacy shims" bullet paragraph (after line 253, before the blank line at 254):

```
| trip* Provider | Delegates To | Used By | Status |
|----------------|-------------|---------|--------|
| `tripExpensesProvider` | `eventExpensesProvider` (via SQLite `BalanceCacheRepository`) | `tripUnifiedLedgerProvider` (deleted) | Deprecated shim |
| `tripSettlementsProvider` | `eventSettlementsProvider` (via SQLite `BalanceCacheRepository`) | `tripUnifiedLedgerProvider` (deleted) | Deprecated shim |
| `tripGearProvider` | Returns `Stream.value([])` | `gearByStatusProvider` | Deprecated shim |
| `tripDocumentsProvider` | Returns `Stream.empty()` | None (no active consumers) | Deprecated shim |
| `tripMemoriesProvider` | Returns `[]` | None (no active consumers) | Deprecated shim |
| `tripActivityProvider` | Returns `Stream.value([])` | None (no active consumers) | Deprecated shim |
| `tripTransactionActivityProvider` | Returns `Stream.value([])` | None (no active consumers) | Deprecated shim |
| `tripLogisticsParticipantsProvider` | SQLite `CacheService.getCachedParticipants` | None (no active consumers) | Deprecated shim |
| `tripCategoriesProvider` | Hardcoded 6 default categories | `add_expense_screen.dart`, `edit_expense_sheet.dart` | Active (not a shim) |
```

**Also update the "Legacy shims" bullet** (line 253) to remove `tripLogisticsParticipantsProvider` from the inline list since it is now in the table and no longer in the ROADMAP.md success criteria list. The updated bullet should read:

```
- **Legacy shims**: `trip*` prefix providers are deprecated compatibility aliases. See the table below for the full mapping. Do NOT create new `trip*` providers. Do NOT remove existing shims while screens still reference them.
```

**Commit** (per D-08): Create a single commit with message: `docs(13): add trip* legacy provider mapping table to CLAUDE.md`
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla && grep -c "trip\* Provider" CLAUDE.md</automated>
    Should return 1 (table header row).
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla && grep "tripExpensesProvider" CLAUDE.md | head -1</automated>
    Should show the table row with tripExpensesProvider.
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla && grep -c "tripCategoriesProvider" CLAUDE.md</automated>
    Should return 1 (table row for tripCategoriesProvider).
  </verify>
  <acceptance_criteria>
    - CLAUDE.md contains `| trip* Provider | Delegates To | Used By | Status |` (table header)
    - CLAUDE.md contains `| \`tripExpensesProvider\`` in a table row
    - CLAUDE.md contains `| \`tripSettlementsProvider\`` in a table row
    - CLAUDE.md contains `| \`tripGearProvider\`` in a table row
    - CLAUDE.md contains `| \`tripDocumentsProvider\`` in a table row
    - CLAUDE.md contains `| \`tripMemoriesProvider\`` in a table row
    - CLAUDE.md contains `| \`tripActivityProvider\`` in a table row
    - CLAUDE.md contains `| \`tripTransactionActivityProvider\`` in a table row
    - CLAUDE.md contains `| \`tripLogisticsParticipantsProvider\`` in a table row
    - CLAUDE.md contains `| \`tripCategoriesProvider\`` in a table row
    - Table has exactly 9 data rows (one per remaining trip* provider)
    - The "Legacy shims" bullet says "See the table below" instead of listing individual provider names
    - `flutter analyze` still passes (no CLAUDE.md-related issues)
  </acceptance_criteria>
  <done>CLAUDE.md contains a 9-row mapping table documenting every remaining trip* legacy provider with its delegation target, active consumers, and deprecation status. The table is placed directly under the "Legacy shims" bullet in the Provider Naming subsection.</done>
</task>

</tasks>

<verification>
Phase-level verification:
1. `grep -r "tripUnifiedLedgerProvider\|tripSeedProvider\|tripSubGroupsProvider" lib/ --include="*.dart"` returns 0 results
2. `grep -n "firebase_auth_provider" lib/features/auth/providers/auth_provider.dart` returns 0 results
3. `grep -c "trip\* Provider" CLAUDE.md` returns 1 (table header present)
4. `flutter analyze` exits cleanly
5. `flutter test` passes with 0 failures
</verification>

<success_criteria>
- The 3 orphaned providers (tripUnifiedLedgerProvider, tripSeedProvider, tripSubGroupsProvider) are deleted from the codebase
- The stale comment in auth_provider.dart referencing firebase_auth_provider.dart is removed
- CLAUDE.md documents all 9 remaining trip* legacy providers in a mapping table
- flutter analyze and flutter test pass after all changes
- Two commits: one for deletions, one for documentation
</success_criteria>

<output>
After completion, create `.planning/phases/13-final-cleanup/13-01-SUMMARY.md`
</output>
