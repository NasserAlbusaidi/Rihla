---
phase: 04-firestore-repository-layer
plan: 04
type: execute
wave: 4
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - lib/core/services/sync_service.dart
  - lib/core/services/offline_repository.dart
  - lib/core/services/balance_cache_repository.dart
  - lib/core/services/cache_service.dart
  - lib/core/providers/connectivity_provider.dart
  - lib/features/groups/providers/group_provider.dart
  - lib/features/events/services/event_service.dart
  - test/unit/connectivity_provider_test.dart
  - test/unit/balance_cache_repository_test.dart
autonomous: true
requirements:
  - MIG-03
  - MIG-04
  - MIG-05

must_haves:
  truths:
    - "SyncService class and sync_queue table no longer exist in the codebase"
    - "All Firestore reads/writes flow through FirestoreRepository -- no direct FirebaseFirestore.instance calls outside that class"
    - "SQLite serves BalanceCalculator queries via BalanceCacheRepository"
    - "ConnectivityNotifier pings Firestore instead of Supabase"
    - "GroupService and EventService extend FirestoreRepository"
    - "eventExpensesProvider asyncMap calls BalanceCacheRepository.cacheExpenses() (wiring from 04-01 is verified)"
  artifacts:
    - path: "lib/core/services/balance_cache_repository.dart"
      provides: "Narrow SQLite wrapper for BalanceCalculator expense/settlement queries"
      exports: ["BalanceCacheRepository"]
    - path: "lib/core/providers/connectivity_provider.dart"
      provides: "Firestore-based connectivity check"
      contains: "FirebaseConfig.firestore"
  key_links:
    - from: "lib/features/groups/providers/group_provider.dart"
      to: "lib/core/services/firestore_repository.dart"
      via: "GroupService extends FirestoreRepository"
      pattern: "extends FirestoreRepository"
    - from: "lib/features/events/services/event_service.dart"
      to: "lib/core/services/firestore_repository.dart"
      via: "EventService extends FirestoreRepository"
      pattern: "extends FirestoreRepository"
    - from: "lib/core/providers/connectivity_provider.dart"
      to: "lib/core/config/firebase_config.dart"
      via: "FirebaseConfig.firestore for health check"
      pattern: "FirebaseConfig\\.firestore"
    - from: "lib/features/ledger/providers/expense_provider.dart"
      to: "lib/core/services/balance_cache_repository.dart"
      via: "asyncMap side-write calling BalanceCacheRepository.cacheExpenses()"
      pattern: "BalanceCacheRepository\\.cacheExpenses"
---

<objective>
Core infrastructure teardown after all 7 modules are migrated: delete SyncService and sync_queue, rename OfflineRepository to BalanceCacheRepository, update ConnectivityNotifier to ping Firestore, refactor GroupService/EventService to extend FirestoreRepository, and update EventService gear seeding for new GearService API.

Purpose: Satisfies MIG-03 (SyncService deleted), MIG-04 (SQLite retained narrowly for balance queries), and MIG-05 (all Firestore access through FirestoreRepository). This plan handles infrastructure cleanup; screen migration is in 04-05.
Output: Clean core infrastructure with no SyncService, all access through FirestoreRepository, Firestore-based connectivity.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-firestore-repository-layer/04-CONTEXT.md
@.planning/phases/04-firestore-repository-layer/04-RESEARCH.md
@.planning/phases/04-firestore-repository-layer/04-00-SUMMARY.md
@.planning/phases/04-firestore-repository-layer/04-01-SUMMARY.md
@.planning/phases/04-firestore-repository-layer/04-02-SUMMARY.md
@.planning/phases/04-firestore-repository-layer/04-03-SUMMARY.md

@lib/core/services/sync_service.dart
@lib/core/services/offline_repository.dart
@lib/core/services/cache_service.dart
@lib/core/providers/connectivity_provider.dart
@lib/features/events/services/event_service.dart
@lib/features/groups/providers/group_provider.dart
@lib/core/services/firestore_repository.dart

<interfaces>
<!-- FirestoreRepository base class -->
From lib/core/services/firestore_repository.dart:
```dart
abstract class FirestoreRepository {
  FirestoreRepository();
  @visibleForTesting
  FirestoreRepository.withFirestore(FirebaseFirestore db);
  FirebaseFirestore get db;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SyncService deletion + BalanceCacheRepository + ConnectivityNotifier</name>
  <files>
    lib/core/services/sync_service.dart,
    lib/core/services/offline_repository.dart,
    lib/core/services/balance_cache_repository.dart,
    lib/core/services/cache_service.dart,
    lib/core/providers/connectivity_provider.dart,
    test/unit/connectivity_provider_test.dart,
    test/unit/balance_cache_repository_test.dart
  </files>
  <read_first>
    lib/core/services/sync_service.dart,
    lib/core/services/offline_repository.dart,
    lib/core/services/cache_service.dart,
    lib/core/providers/connectivity_provider.dart,
    lib/core/services/firestore_repository.dart,
    lib/core/services/local_database.dart,
    test/unit/sync_service_test.dart,
    test/unit/sync_retry_test.dart,
    test/unit/offline_repository_test.dart,
    test/unit/connectivity_provider_test.dart,
    test/unit/balance_cache_repository_test.dart,
    lib/features/ledger/providers/expense_provider.dart
  </read_first>
  <action>
    **1. Delete `lib/core/services/sync_service.dart`** per D-04. This is the final step -- all modules are now on Firestore.
    - Also delete `test/unit/sync_service_test.dart` and `test/unit/sync_retry_test.dart` (tests for deleted code)
    - Remove any imports of `sync_service.dart` from other files (search `grep -rn 'sync_service' lib/`)

    **2. Create `lib/core/services/balance_cache_repository.dart`** per D-16 (rename from OfflineRepository):
    - Extract ONLY the balance-related SQLite queries from `offline_repository.dart`:
      - Method to cache expenses to SQLite (for cross-event aggregation in Phase 5)
      - Method to cache settlements to SQLite
      - Method to read expenses from SQLite (for BalanceCalculator)
      - Method to read settlements from SQLite
    - The class does NOT extend FirestoreRepository (it's a SQLite wrapper, not a Firestore service)
    - Provide a Riverpod provider: `final balanceCacheRepositoryProvider = Provider<BalanceCacheRepository>((ref) => BalanceCacheRepository());`
    - **Keep the stream-based interface** for the deprecated backward-compat providers (they will be removed in 04-05 when all screens are updated). If any deprecated provider still references `offlineRepositoryProvider`, update it to use `balanceCacheRepositoryProvider`.

    **3. Delete `lib/core/services/offline_repository.dart`** after extracting balance methods.
    - Delete `test/unit/offline_repository_test.dart`
    - Remove `offlineRepositoryProvider` and all imports of it

    **4. Trim `lib/core/services/cache_service.dart`:**
    - Delete sync_queue related methods: `addToSyncQueue()`, `removeSyncItem()`, `getPendingSyncItems()`, `hasPendingSync()`, `getSyncQueueCount()`
    - Delete trip-level cache methods that are no longer needed (trips are managed by EventService now)
    - Keep any methods still used by BalanceCacheRepository or other active code (search for usages before deleting)
    - Keep `cacheExpenses()` and `cacheSettlements()` -- these are called by the asyncMap side-write in expense_provider.dart (from 04-01). They will be migrated to BalanceCacheRepository in this step or in 04-05.

    **5. Update `lib/core/providers/connectivity_provider.dart`:**
    - Remove imports of `SyncService` and `CacheService` (sync_queue count)
    - Remove `pendingSyncCountProvider` (sync_queue is gone)
    - Replace `SyncService.isOnline()` call with a Firestore-based ping:
      ```dart
      Future<bool> _isOnline() async {
        try {
          await FirebaseConfig.firestore
              .collection('_health')
              .doc('ping')
              .get(const GetOptions(source: Source.server));
          return true;
        } catch (e) {
          return false;
        }
      }
      ```
    - Update `checkConnectivity()` to call `_isOnline()` instead of `SyncService.isOnline()`
    - Remove the offline->online auto-sync trigger (SyncService no longer exists; Firestore handles offline writes automatically)
    - Remove skip markers from `test/unit/connectivity_provider_test.dart` and write tests

    **6. Verify the asyncMap side-write wiring from 04-01:**
    - Confirm that `eventExpensesProvider` in `expense_provider.dart` contains the `asyncMap` call that writes to SQLite
    - If 04-01 used `CacheService.cacheExpenses()`, update the call to use `BalanceCacheRepository.cacheExpenses()` now that BalanceCacheRepository exists
    - This is the verification of the D-15 wiring: Firestore snapshot -> asyncMap -> SQLite write -> downstream providers

    **7. Remove skip markers and write tests** for `connectivity_provider_test.dart` and `balance_cache_repository_test.dart`.
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla && flutter analyze lib/core/ && flutter test test/unit/connectivity_provider_test.dart test/unit/balance_cache_repository_test.dart --no-pub</automated>
  </verify>
  <acceptance_criteria>
    - lib/core/services/sync_service.dart does NOT exist (deleted)
    - lib/core/services/offline_repository.dart does NOT exist (deleted)
    - lib/core/services/balance_cache_repository.dart contains `class BalanceCacheRepository`
    - lib/core/providers/connectivity_provider.dart does NOT contain `SyncService`
    - lib/core/providers/connectivity_provider.dart contains `FirebaseConfig.firestore`
    - lib/core/providers/connectivity_provider.dart contains `Source.server` (Firestore server-only read for ping)
    - grep -rn 'SyncService' lib/ returns NO matches
    - grep -rn 'offlineRepositoryProvider' lib/ returns NO matches
    - lib/features/ledger/providers/expense_provider.dart contains `asyncMap` (side-write wiring confirmed)
    - flutter analyze lib/core/ reports no errors
    - test/unit/connectivity_provider_test.dart exits 0
    - test/unit/balance_cache_repository_test.dart exits 0
  </acceptance_criteria>
  <done>SyncService deleted. OfflineRepository replaced by BalanceCacheRepository. ConnectivityNotifier pings Firestore. asyncMap side-write wiring from 04-01 verified. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: GroupService + EventService base class refactor + EventService cleanup</name>
  <files>
    lib/features/groups/providers/group_provider.dart,
    lib/features/events/services/event_service.dart
  </files>
  <read_first>
    lib/features/groups/providers/group_provider.dart,
    lib/features/events/services/event_service.dart,
    lib/core/services/firestore_repository.dart,
    lib/features/gear/services/gear_service.dart
  </read_first>
  <action>
    **1. Refactor `GroupService` to extend FirestoreRepository** per D-06:
    - In `lib/features/groups/providers/group_provider.dart`, change GroupService from:
      ```dart
      class GroupService {
        final Ref _ref;
        GroupService(this._ref);
        FirebaseFirestore get _db => FirebaseConfig.firestore;
      ```
      to:
      ```dart
      class GroupService extends FirestoreRepository {
        final Ref _ref;
        GroupService(this._ref) : super();
        @visibleForTesting
        GroupService.withFirestore(this._ref, FirebaseFirestore db) : super.withFirestore(db);
        // Use `db` from base class instead of `_db`
      ```
    - Replace all `_db` references with `db` (from base class)
    - Remove the `FirebaseConfig.firestore` getter

    **2. Refactor `EventService` to extend FirestoreRepository** per D-06:
    - Change EventService from having its own `_db` field to extending FirestoreRepository
    - Update constructors:
      ```dart
      class EventService extends FirestoreRepository {
        final Ref? _ref;
        final GearService? _gearServiceOverride;
        final bool _skipBridgeInTest;

        EventService(Ref ref)
            : _ref = ref,
              _gearServiceOverride = null,
              _skipBridgeInTest = false,
              super();

        @visibleForTesting
        EventService.withFirestore(FirebaseFirestore db, GearService gearService)
            : _ref = null,
              _gearServiceOverride = gearService,
              _skipBridgeInTest = true,
              super.withFirestore(db);
      ```
    - Replace `_db` with `db` from base class

    **3. Update EventService gear seeding** for the new Firestore-backed GearService API:
    - Read the current `_seedCampingGear()` method to see how `_gearService.addItem(...)` is called
    - Update to call `_gearService.addGearItem(groupId: ..., eventId: ..., itemName: ...)` with the new Firestore signature from Plan 04-02
    - The `EventService.withFirestore` test constructor already accepts a GearService -- after this change, tests can pass `GearService.withFirestore(fakeDb)` instead of `MockGearService`
  </action>
  <verify>
    <automated>cd /Users/nasseralbusaidi/Desktop/Personal/Rihla && flutter analyze lib/features/groups/providers/group_provider.dart lib/features/events/services/event_service.dart && flutter test test/unit/event_service_test.dart --no-pub</automated>
  </verify>
  <acceptance_criteria>
    - lib/features/groups/providers/group_provider.dart contains `class GroupService extends FirestoreRepository`
    - lib/features/groups/providers/group_provider.dart does NOT contain `FirebaseConfig.firestore` getter (uses `db` from base class)
    - lib/features/events/services/event_service.dart contains `class EventService extends FirestoreRepository`
    - lib/features/events/services/event_service.dart does NOT contain `FirebaseFirestore _db` field (uses `db` from base class)
    - grep -rn 'FirebaseFirestore.instance' lib/ returns matches ONLY in firebase_config.dart
    - flutter analyze reports no errors
    - test/unit/event_service_test.dart exits 0
  </acceptance_criteria>
  <done>GroupService and EventService extend FirestoreRepository. All Firestore access flows through the base class (MIG-05). EventService gear seeding updated for new GearService API. No direct FirebaseFirestore.instance usage outside FirebaseConfig.</done>
</task>

</tasks>

<verification>
- `flutter analyze lib/core/ lib/features/groups/ lib/features/events/` reports no errors
- `flutter test test/unit/connectivity_provider_test.dart test/unit/balance_cache_repository_test.dart test/unit/event_service_test.dart --no-pub` passes
- `grep -rn 'SyncService' lib/` returns 0 matches
- `grep -rn 'offlineRepositoryProvider' lib/` returns 0 matches
- `grep -rn 'FirebaseFirestore.instance' lib/` returns matches only in `firebase_config.dart`
</verification>

<success_criteria>
- MIG-03 satisfied: SyncService and sync_queue are gone
- MIG-04 satisfied: SQLite serves balance queries via BalanceCacheRepository
- MIG-05 satisfied: All Firestore access flows through FirestoreRepository base class
- ConnectivityNotifier uses Firestore-based health check
- GroupService and EventService extend FirestoreRepository (D-06)
- EventService gear seeding works with new GearService API
- asyncMap side-write wiring from D-15 verified
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-firestore-repository-layer/04-04-SUMMARY.md`
</output>
